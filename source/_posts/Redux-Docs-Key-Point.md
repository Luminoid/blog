---
title: Redux Docs Key Point
date: 2017-12-25 20:41:46
updated:
categories:
- Web
- JavaScript
tags:
- Redux
- Note
keywords:
- Redux
---

[Redux Docs](https://redux.js.org)

<!-- TOC -->

## Three Principles
### Single source of truth
The [state](https://redux.js.org/docs/Glossary.html#state) of your whole application is stored in an object tree within a single [store](https://redux.js.org/docs/Glossary.html#store).

### State is read-only
The only way to change the state is to emit an [action](https://redux.js.org/docs/Glossary.html#action), an object describing what happened.

### Changes are made with pure functions
To specify how the state tree is transformed by actions, you write pure [reducers](https://redux.js.org/docs/Glossary.html#reducer).

<!-- more -->

## Action
### Action creator
Action creator: function that returns an action.

## Reducers
### Handling Actions
- Don't mutate the `state`, create a copy with `Object.assign({}, state, newState)`.
- Return the previous `state` in the `default` case.

### Splitting Reducers
Reducer composition: Each of reducers is managing its own part of the global state. The `state` parameter is different for every reducer, and corresponds to the part of the state it manages.
Redux provides a utility called [`combineReducers()`](https://redux.js.org/docs/api/combineReducers.html) to generate a function that calls your reducers with the slices of state selected according to their keys, and combining their results into a single object again.

## Store
The store has the following responsibilities:
- Holds application state;
- Allows access to state via [`getState()`](https://redux.js.org/docs/api/Store.html#getState);
- Allows state to be updated via [`dispatch(action)`](https://redux.js.org/docs/api/Store.html#dispatch);
- Registers listeners via [`subscribe(listener)`](https://redux.js.org/docs/api/Store.html#subscribe);
- Handles unregistering of listeners via the function returned by [`subscribe(listener)`](https://redux.js.org/docs/api/Store.html#subscribe).

## Data Flow
Redux architecture revolves around a strict unidirectional data flow.
The data lifecycle in any Redux app follows these 4 steps:
1. You call `store.dispatch(action)`.
2. The Redux store calls the reducer function you gave it.
3. The root reducer may combine the output of multiple reducers into a single state tree.
4. The Redux store saves the complete state tree returned by the root reducer.
Every listener registered with `store.subscribe(listener)` will now be invoked; listeners may call `store.getState()` to get the current state.

## Usage with React
### Presentational and Container Components
|   | Presentational Components | Container Components |
|---|---|---|
| Purpose | How things look (markup, styles) | How things work (data fetching, state updates) |
| Aware of Redux | No | Yes |
| To read data | Read data from props | Subscribe to Redux state |
| To change data | Invoke callbacks from props | Dispatch Redux actions |
| Are written | By hand | Usually generated by React Redux |

### Presentational Components
Write functional stateless components unless you need to use local state or the lifecycle methods.

### Container Components
Use React Redux library's [`connect()`](https://github.com/reactjs/react-redux/blob/master/docs/api.md#connectmapstatetoprops-mapdispatchtoprops-mergeprops-options) function to generate container components.
To use `connect()`, you need to define a special function called `mapStateToProps()` that tells how to transform the current Redux store state into the props you want to pass to a presentational component you are wrapping.
In a similar fashion, you can define a function called `mapDispatchToProps()` that receives the `dispatch()` method and returns callback props that you want to inject into the presentational component.

### Passing the Store
Use a special React Redux component called `<Provider>` to magically make the store available to all container components in the application without passing it explicitly.
