<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Deploy a Static Site to Cloudflare Pages with GitHub Auto-Deploy</title>
    <url>/Deploy-Static-Site-to-Cloudflare-Pages/</url>
    <content><![CDATA[<p>How to set up a zero-config deployment pipeline: push to GitHub, site updates automatically on Cloudflare Pages with a custom domain.</p>
<span id="more"></span>

<h2 id="Why-Cloudflare-Pages"><a href="#Why-Cloudflare-Pages" class="headerlink" title="Why Cloudflare Pages"></a>Why Cloudflare Pages</h2><p>The three big static hosts in 2026 are Cloudflare Pages, Netlify, and Vercel. They all do auto-deploy from Git, custom domains, automatic SSL, and PR previews. The honest comparison:</p>
<table>
<thead>
<tr>
<th></th>
<th>Cloudflare Pages</th>
<th>Netlify</th>
<th>Vercel</th>
</tr>
</thead>
<tbody><tr>
<td>Free tier bandwidth</td>
<td><strong>Unlimited</strong></td>
<td>100 GB &#x2F; mo</td>
<td>100 GB &#x2F; mo</td>
</tr>
<tr>
<td>Free tier builds</td>
<td>500 &#x2F; mo</td>
<td>300 build min &#x2F; mo</td>
<td>6,000 build min &#x2F; mo</td>
</tr>
<tr>
<td>Overage charges</td>
<td><strong>None on Free; bandwidth still free on paid</strong></td>
<td>Bandwidth $55&#x2F;100 GB</td>
<td>Bandwidth $40&#x2F;100 GB</td>
</tr>
<tr>
<td>Cold-build speed</td>
<td>Slower (minute-class)</td>
<td>Fast</td>
<td>Fast</td>
</tr>
<tr>
<td>Edge network</td>
<td>300+ POPs</td>
<td>~6 regions + global edge</td>
<td>~30 regions</td>
</tr>
<tr>
<td>Best at</td>
<td>Cheap fan-out, surprise traffic</td>
<td>Form&#x2F;auth integrations</td>
<td>Next.js</td>
</tr>
</tbody></table>
<p>If a single post catches fire on Hacker News, Pages charges $0; Netlify or Vercel can charge real money. The tradeoff is slower cold builds and fewer one-click integrations. For static sites and small SSGs, Pages is the safe default.</p>
<h2 id="What-You-Need"><a href="#What-You-Need" class="headerlink" title="What You Need"></a>What You Need</h2><ul>
<li>A GitHub account</li>
<li>A Cloudflare account (free plan works)</li>
<li>A domain managed by Cloudflare DNS</li>
<li>A static site (plain HTML&#x2F;CSS&#x2F;JS, no build step required)</li>
</ul>
<h2 id="1-Push-Your-Site-to-GitHub"><a href="#1-Push-Your-Site-to-GitHub" class="headerlink" title="1. Push Your Site to GitHub"></a>1. Push Your Site to GitHub</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> ~/Projects/my-site</span><br><span class="line">git init</span><br><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;Initial commit&quot;</span></span><br><span class="line">gh repo create my-site --public --<span class="built_in">source</span>=. --push</span><br></pre></td></tr></table></figure>

<p>If you don’t have the <code>gh</code> CLI, create the repo on GitHub manually and push:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git remote add origin git@github.com:YOUR_USERNAME/my-site.git</span><br><span class="line">git branch -M main</span><br><span class="line">git push -u origin main</span><br></pre></td></tr></table></figure>

<h2 id="2-Create-a-Cloudflare-Pages-Project"><a href="#2-Create-a-Cloudflare-Pages-Project" class="headerlink" title="2. Create a Cloudflare Pages Project"></a>2. Create a Cloudflare Pages Project</h2><ol>
<li>Go to <strong>Cloudflare Dashboard</strong></li>
<li>Navigate to <strong>Workers &amp; Pages</strong></li>
<li>Click <strong>Create</strong> &gt; <strong>Pages</strong> &gt; <strong>Connect to Git</strong></li>
<li>Select your GitHub account and the repository</li>
<li>Set the production branch to <code>main</code></li>
</ol>
<h3 id="Build-Settings"><a href="#Build-Settings" class="headerlink" title="Build Settings"></a>Build Settings</h3><p>For a plain static site (no framework, no bundler):</p>
<table>
<thead>
<tr>
<th>Setting</th>
<th>Value</th>
</tr>
</thead>
<tbody><tr>
<td>Build command</td>
<td><em>(leave blank)</em></td>
</tr>
<tr>
<td>Build output directory</td>
<td><code>/</code></td>
</tr>
</tbody></table>
<p>If your site uses a framework:</p>
<table>
<thead>
<tr>
<th>Framework</th>
<th>Build command</th>
<th>Output directory</th>
</tr>
</thead>
<tbody><tr>
<td>Hugo</td>
<td><code>hugo</code></td>
<td><code>public</code></td>
</tr>
<tr>
<td>Hexo</td>
<td><code>hexo generate</code></td>
<td><code>public</code></td>
</tr>
<tr>
<td>Astro</td>
<td><code>npm run build</code></td>
<td><code>dist</code></td>
</tr>
<tr>
<td>SvelteKit (static adapter)</td>
<td><code>npm run build</code></td>
<td><code>build</code></td>
</tr>
<tr>
<td>Eleventy</td>
<td><code>npx eleventy</code></td>
<td><code>_site</code></td>
</tr>
<tr>
<td>Next.js (static export, Next 14+)</td>
<td><code>next build</code> (with <code>output: &#39;export&#39;</code> in <code>next.config.js</code>)</td>
<td><code>out</code></td>
</tr>
<tr>
<td>Vite</td>
<td><code>npm run build</code></td>
<td><code>dist</code></td>
</tr>
</tbody></table>
<p>Click <strong>Save and Deploy</strong>. Cloudflare builds and deploys immediately.</p>
<p>Your site is now live at <code>my-site.pages.dev</code>.</p>
<h2 id="3-Add-a-Custom-Domain"><a href="#3-Add-a-Custom-Domain" class="headerlink" title="3. Add a Custom Domain"></a>3. Add a Custom Domain</h2><ol>
<li>In your Pages project, go to <strong>Custom domains</strong></li>
<li>Click <strong>Set up a custom domain</strong></li>
<li>Enter your subdomain (e.g., <code>app.example.com</code>)</li>
<li>Cloudflare automatically creates the DNS record:</li>
</ol>
<table>
<thead>
<tr>
<th>Type</th>
<th>Name</th>
<th>Content</th>
</tr>
</thead>
<tbody><tr>
<td>CNAME</td>
<td><code>app</code></td>
<td><code>my-site.pages.dev</code></td>
</tr>
</tbody></table>
<p>SSL is provisioned automatically. The domain is usually active within seconds since DNS is already on Cloudflare.</p>
<h2 id="4-Daily-Workflow"><a href="#4-Daily-Workflow" class="headerlink" title="4. Daily Workflow"></a>4. Daily Workflow</h2><p>That’s it. From now on, every push to <code>main</code> triggers an automatic deploy:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Edit your files</span></span><br><span class="line">vim index.html</span><br><span class="line"></span><br><span class="line"><span class="comment"># Push</span></span><br><span class="line">git add -A</span><br><span class="line">git commit -m <span class="string">&quot;update feature&quot;</span></span><br><span class="line">git push</span><br><span class="line"></span><br><span class="line"><span class="comment"># Site updates in ~30 seconds at your custom domain</span></span><br></pre></td></tr></table></figure>

<p>No deploy commands. No CI config files. No build scripts.</p>
<h2 id="Alternative-Manual-Deploy-with-Wrangler"><a href="#Alternative-Manual-Deploy-with-Wrangler" class="headerlink" title="Alternative: Manual Deploy with Wrangler"></a>Alternative: Manual Deploy with Wrangler</h2><p>If you prefer not to connect GitHub (or need to deploy from a different source), you can use the Wrangler CLI:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Install wrangler (one-time)</span></span><br><span class="line">npm install -g wrangler</span><br><span class="line"></span><br><span class="line"><span class="comment"># Create the project (one-time)</span></span><br><span class="line">npx wrangler pages project create my-site --production-branch main</span><br><span class="line"></span><br><span class="line"><span class="comment"># Deploy (every time)</span></span><br><span class="line">npx wrangler pages deploy . --project-name my-site</span><br></pre></td></tr></table></figure>

<p>This uploads the current directory directly. No Git integration: you run the command manually each time.</p>
<p><strong>Note:</strong> Projects created via <code>wrangler</code> cannot be connected to GitHub after the fact. If you want auto-deploy, delete the project in the dashboard and recreate it through the UI with Git connected.</p>
<h2 id="gitignore"><a href="#gitignore" class="headerlink" title=".gitignore"></a>.gitignore</h2><p>For a static site deployed to Cloudflare Pages:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.DS_Store</span><br><span class="line">Thumbs.db</span><br><span class="line">*.swp</span><br><span class="line">*.swo</span><br><span class="line">*~</span><br><span class="line">.wrangler/</span><br><span class="line">node_modules/</span><br></pre></td></tr></table></figure>

<p><code>.wrangler/</code> contains local deploy cache from the Wrangler CLI. <code>node_modules/</code> in case you install wrangler locally.</p>
<h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><table>
<thead>
<tr>
<th>Step</th>
<th>Action</th>
<th>Frequency</th>
</tr>
</thead>
<tbody><tr>
<td>Create GitHub repo</td>
<td><code>gh repo create</code></td>
<td>Once</td>
</tr>
<tr>
<td>Connect to Cloudflare Pages</td>
<td>Dashboard UI</td>
<td>Once</td>
</tr>
<tr>
<td>Add custom domain</td>
<td>Dashboard &gt; Custom domains</td>
<td>Once</td>
</tr>
<tr>
<td>Deploy</td>
<td><code>git push</code></td>
<td>Every change</td>
</tr>
</tbody></table>
<p>Total setup time: ~5 minutes. Zero ongoing maintenance.</p>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>Cloudflare</tag>
        <tag>GitHub</tag>
        <tag>static-site</tag>
      </tags>
  </entry>
  <entry>
    <title>AI services guide: subscriptions, free tiers, and APIs (May 2026)</title>
    <url>/AI-Services-Guide-May-2026/</url>
    <content><![CDATA[<p>A May 2026 snapshot of every major AI provider, with the eight weeks of releases since the <a href="/AI-Services-Guide-March-2026/" title="AI services guide: subscriptions, free tiers, and APIs (March 2026)">March guide</a> and a deeper look at the platform stacks behind the top providers.</p>
<span id="more"></span>

<h2 id="Eight-weeks-that-aged-the-March-guide"><a href="#Eight-weeks-that-aged-the-March-guide" class="headerlink" title="Eight weeks that aged the March guide"></a>Eight weeks that aged the March guide</h2><p>I shipped the <a href="/AI-Services-Guide-March-2026/" title="AI services guide: subscriptions, free tiers, and APIs (March 2026)">March 2026 guide</a> the week Claude Opus 4.6 still felt new. Two months later it’s already a previous-generation model, and most of the interesting movement is around the model rather than inside it.</p>
<h3 id="New-flagship-models"><a href="#New-flagship-models" class="headerlink" title="New flagship models"></a>New flagship models</h3><ul>
<li><strong>Claude Opus 4.7</strong> (<a href="https://platform.claude.com/docs/en/about-claude/models/whats-new-claude-4-7">April 16</a>). Same $5 &#x2F; $25 per 1M tokens, but 1M context is now standard with no long-context premium, image input goes to 2,576 px &#x2F; 3.75 MP, and a new “task budgets” feature shows the model a token countdown for the full agentic loop (thinking + tool calls + output). SWE-bench Pro went 53% to 64%. The catch buried in the release: the new tokenizer can use up to 35% more tokens for the same text, so the per-token rate held but effective cost can creep.</li>
<li><strong>GPT-5.5</strong> (<a href="https://openai.com/index/introducing-gpt-5-5/">April 23</a>) at $5 &#x2F; $30, 1M context in the API, 400K in Codex. Anything past 272K input flips to 2x input &#x2F; 1.5x output for the rest of the session. <strong>GPT-5.5 Pro</strong> at $30 &#x2F; $180 for extended reasoning. <strong>GPT-5.5 Instant</strong> (<a href="https://openai.com/index/gpt-5-5-instant/">May 5</a>) replaces GPT-5.3 Instant as the free-tier default, with the headline being lower hallucination rates in legal &#x2F; medical &#x2F; financial answers at the same latency.</li>
<li><strong>DeepSeek V4 Preview</strong> (<a href="https://api-docs.deepseek.com/news/news260424">April 24</a>). V4-Flash (284B &#x2F; 13B active) at $0.14 &#x2F; $0.28 with cache hits at $0.0028. V4-Pro (1.6T &#x2F; 49B active) at list price $1.74 input &#x2F; $3.48 output, currently 75% off through May 31 ($0.435 &#x2F; $0.87). Both ship with 1M context and 384K max output. Legacy <code>deepseek-chat</code> and <code>deepseek-reasoner</code> endpoints retire July 24. DeepSeek also disclosed <a href="https://www.cnbc.com/2026/04/24/deepseek-v4-llm-preview-open-source-ai-competition-china.html">a Huawei Ascend 950 hardware partnership</a> covering part of the V4 training run.</li>
<li><strong>Mistral Medium 3.5</strong> (<a href="https://mistral.ai/news/vibe-remote-agents-mistral-medium-3-5">April 29</a>). 128B dense, 256K context, 77.6% on SWE-Bench Verified, ahead of Devstral 2 and Qwen3.5 397B-A17B. It’s the first Mistral “merged” model: instruction following, reasoning, and coding behind one set of weights, with a per-request reasoning-effort knob. It replaces Mistral Medium 3.1 and Magistral in Le Chat and Devstral 2 in Vibe; powers the new Vibe remote agents.</li>
<li><strong>Qwen3.6-Plus</strong> (<a href="https://www.alibabacloud.com/blog/alibaba-unveils-qwen3-6-plus-to-accelerate-agentic-ai-deployment-for-enterprises-and-alibaba%E2%80%99s-ai-applications_603000">April 2</a>) with a 1M default context and an agentic-coding focus. <strong>Qwen3.6-35B-A3B</strong> (April 16) under Apache 2.0. Qwen3.5-Omni also dropped, but only on the Alibaba Cloud platform.</li>
<li><strong>Meta Muse Spark</strong> (<a href="https://ai.meta.com/blog/introducing-muse-spark-msl/">April 8</a>). Meta’s first proprietary closed-weight model from the new Superintelligence Labs unit. Available only on meta.ai. The Llama line continues, but Muse Spark is the real strategic break with “weights for everyone.”</li>
</ul>
<h3 id="New-agent-infrastructure"><a href="#New-agent-infrastructure" class="headerlink" title="New agent infrastructure"></a>New agent infrastructure</h3><ul>
<li><strong>Anthropic Managed Agents</strong> went public beta on <a href="https://platform.claude.com/docs/en/managed-agents/overview">April 8</a> at $0.08 per session-hour plus tokens. May 6 added <a href="https://claude.com/blog/new-in-claude-managed-agents">three more pieces</a>: <strong>Dreaming</strong> (Claude reviews past sessions to find patterns and self-improve), <strong>Outcomes</strong> (you write a success rubric and a separate grader evaluates the agent’s output in its own context window, pinpointing what to fix; up to 10 pp gain over a standard prompting loop), and <strong>Multiagent Orchestration</strong> (a lead agent decomposes work and delegates to specialists with their own model &#x2F; prompt &#x2F; tools). This is the first time Anthropic has shipped a hosted runtime, not just a coding tool.</li>
<li><strong><a href="https://code.claude.com/docs/en/claude-code-on-the-web">Claude Code on the web</a></strong> runs at claude.ai&#x2F;code. Sessions persist when you close the browser, and the iOS app monitors them.</li>
<li><strong>Cursor 3</strong> (April 2) added a dedicated Agents Window with parallel agents on cloud VMs, <code>/worktree</code> for isolated branch changes, and self-hosted runners. Multiple agents at once across multiple repos is the headline shift.</li>
<li><strong>OpenAI Codex</strong> picked up background computer use, an in-app browser, GitHub PR reviews, image generation, 90+ plugins, and an automatic-approval reviewer agent that handles routine permission prompts before they reach you. GPT-5.5 is the recommended model.</li>
<li><strong>Claude Cowork</strong> got an Enterprise group &#x2F; role layer (with SCIM provisioning), a Zoom connector that pulls AI Companion meeting summaries into the agent context, and ten ready-to-run finance agent templates available across Cowork, Claude Code, and Managed Agents.</li>
</ul>
<h3 id="New-ideas"><a href="#New-ideas" class="headerlink" title="New ideas"></a>New ideas</h3><ul>
<li><strong>Claude <a href="https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills">Skills</a></strong>. Reusable filesystem-based capabilities packaged as <code>SKILL.md</code>. Progressive disclosure means Claude only loads a skill when it looks relevant. The same files now run inside Claude Code, Cursor, Codex CLI, Gemini CLI, and Antigravity, so it’s effectively a portable standard rather than an Anthropic-only feature.</li>
<li><strong>MCP changed hands</strong>. Anthropic <a href="https://www.anthropic.com/news/donating-the-model-context-protocol-and-establishing-of-the-agentic-ai-foundation">donated</a> the Model Context Protocol to the Linux Foundation’s new Agentic AI Foundation, co-founded with OpenAI and Block, and backed by Google, Microsoft, AWS, Cloudflare, and Bloomberg. The Linux Foundation reports more than 10,000 active public MCP servers; the Python SDK alone passed 164 million monthly downloads in April. <strong>MCP Apps</strong> (interactive iframe &#x2F; widget UI rendered inline in chat) shipped at the start of the year and is now a real authoring target.</li>
<li><strong>The desktop super-app</strong>. OpenAI confirmed in March it’s <a href="https://thetechportal.com/2026/03/20/openai-could-launch-a-desktop-super-app-integrating-chatgpt-codex-and-the-atlas-browser/">merging</a> ChatGPT, Codex, and the Atlas browser into a single desktop application. Atlas itself is on macOS now with multi-account support and an optional agent mode that drives the browser.</li>
</ul>
<h3 id="Strategic-moves"><a href="#Strategic-moves" class="headerlink" title="Strategic moves"></a>Strategic moves</h3><ul>
<li><strong>SpaceX <a href="https://www.cnbc.com/2026/02/02/elon-musk-spacex-xai-ipo.html">acquired</a> xAI</strong> on February 2 (announced; $1.25T combined valuation). <strong>Grok 4.3</strong> Beta rolled out as Early Access for SuperGrok &#x2F; Premium+ on April 17, with formal launch <a href="https://artificialanalysis.ai/articles/xai-launches-grok-4-3-with-improved-agentic-performance-and-lower-pricing">April 30</a> at $1.25 &#x2F; $2.50 per 1M (with 2x premium past 200K input), aggressive pricing aimed straight at GPT-5.5 &#x2F; Opus 4.7. Grok 5 (6T parameters, MoE, trained on the gigawatt-scale Colossus 2 cluster) slipped from Q1 to a Q2 public beta, expected May or June.</li>
<li><strong>xAI’s <a href="https://en.wikipedia.org/wiki/XAI_(company)">Anysphere option</a></strong> (April 21). xAI struck a deal for the right to acquire Cursor’s parent company Anysphere for $60B later this year, the first time a frontier model lab has lined up a third-party agent vendor on its balance sheet.</li>
<li><strong>Sora is winding down</strong>. The standalone Sora app shut down <a href="https://en.wikipedia.org/wiki/Sora_(text-to-video_model)">April 26</a>; the API ends September 24, 2026. Reporting on the unit economics ranged from “around $1M&#x2F;day” (early estimates) to <a href="https://medium.com/@shubhamnv2/openai-sora-shutdown-15m-day-costs-2-1m-revenue-the-full-story-088380118243">“$15M&#x2F;day in inference costs against ~$2.1M lifetime revenue”</a>; active users had also fallen from ~1M to under 500K, and Disney pulled a $1B character-licensing deal. OpenAI says video generation continues inside ChatGPT under a model codenamed Spud.</li>
<li><strong>Perplexity Comet</strong> went free across iOS, Android, Mac, and Windows; the <a href="https://9to5mac.com/2026/04/28/perplexity-just-gave-its-comet-ai-browser-an-upgrade-for-ipad-users-with-these-features/">iPad build</a> (April 28) supports multi-window. Samsung’s Galaxy S26 wires Perplexity into Bixby and Samsung Internet at the OS level, the first OS-level distribution deal for a non-OpenAI&#x2F;Google AI provider.</li>
</ul>
<p>The headline pattern: less “new model, higher price, more capabilities” and more “same price, faster shipping cadence, much wider product surface.” Nearly every interesting difference now sits around the model.</p>
<hr>
<h2 id="Two-ways-to-use-AI-models"><a href="#Two-ways-to-use-AI-models" class="headerlink" title="Two ways to use AI models"></a>Two ways to use AI models</h2><p>Two layers, two bills:</p>
<ol>
<li><strong>Chat product</strong>, the app you talk to (ChatGPT, Claude, Gemini, Grok, Le Chat, Qwen, Copilot, Perplexity, Nova, DeepSeek). Free tiers exist; paid subscriptions ($8-$300&#x2F;mo) unlock premium models, higher limits, and product-side features like image gen, voice, video, and agents.</li>
<li><strong>API</strong>, pay-per-token programmatic access for building apps. Separate billing, separate account, separate pricing.</li>
</ol>
<p>Three overlaps, all small. Google AI Pro &#x2F; Ultra subscribers get $10 &#x2F; $100 per month in Google Cloud credits toward Gemini API usage. Perplexity Pro subscribers get $5&#x2F;month in API credits. xAI Business tier ($30-300&#x2F;seat) bundles API access. Everyone else: your subscription only buys the chat app.</p>
<hr>
<h2 id="Quick-comparison"><a href="#Quick-comparison" class="headerlink" title="Quick comparison"></a>Quick comparison</h2><table>
<thead>
<tr>
<th>Provider</th>
<th>Chat product</th>
<th>Cheapest API (in&#x2F;out per 1M)</th>
<th>Free API tier</th>
<th>Max context</th>
<th>Open weights</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Google Gemini</strong></td>
<td>Gemini</td>
<td>$0.10 &#x2F; $0.40 (Flash-Lite 2.5)</td>
<td>Unlimited, no card</td>
<td>1M</td>
<td>Gemma 4</td>
</tr>
<tr>
<td><strong>OpenAI</strong></td>
<td>ChatGPT</td>
<td>$0.05 &#x2F; $0.40 (GPT-5 Nano)</td>
<td>Limited trial credits</td>
<td>1M (400K Codex)</td>
<td>GPT-OSS (Apache 2.0)</td>
</tr>
<tr>
<td><strong>Anthropic</strong></td>
<td>Claude</td>
<td>$1 &#x2F; $5 (Haiku 4.5)</td>
<td>$5 trial credits</td>
<td>1M (Opus 4.7 standard)</td>
<td>No</td>
</tr>
<tr>
<td><strong>xAI</strong></td>
<td>Grok</td>
<td>$0.20 &#x2F; $0.50 (Grok 4.1 Fast)</td>
<td>$25 sign-up credits</td>
<td>2M</td>
<td>Partial (Grok 1, 2.5)</td>
</tr>
<tr>
<td><strong>DeepSeek</strong></td>
<td>DeepSeek</td>
<td>$0.14 &#x2F; $0.28 (V4-Flash)</td>
<td>5M tokens free</td>
<td>1M</td>
<td>Yes (MIT)</td>
</tr>
<tr>
<td><strong>Mistral</strong></td>
<td>Le Chat</td>
<td>$0.02 &#x2F; $0.04 (Nemo)</td>
<td>API trial credits</td>
<td>256K</td>
<td>Partial</td>
</tr>
<tr>
<td><strong>Meta</strong></td>
<td>–</td>
<td>Free (self-host)</td>
<td>Models are free</td>
<td>10M (Llama 4 Scout)</td>
<td>Yes (Llama); Muse Spark closed</td>
</tr>
<tr>
<td><strong>Alibaba Qwen</strong></td>
<td>Qwen</td>
<td>$0.05 &#x2F; $0.20 (Qwen3-8B)</td>
<td>1M tokens&#x2F;model</td>
<td>1M (Qwen3.6-Plus)</td>
<td>Yes (Apache 2.0)</td>
</tr>
<tr>
<td><strong>Amazon Nova</strong></td>
<td>Nova</td>
<td>$0.035 &#x2F; $0.14 (Nova Micro)</td>
<td>$200 AWS credits</td>
<td>1M (Premier &#x2F; Nova 2)</td>
<td>No</td>
</tr>
<tr>
<td><strong>Microsoft Phi</strong></td>
<td>Copilot</td>
<td>$0.075 &#x2F; $0.30 (Phi-4-mini)</td>
<td>GitHub Models free</td>
<td>128K</td>
<td>Yes (MIT)</td>
</tr>
<tr>
<td><strong>Perplexity</strong></td>
<td>Perplexity</td>
<td>$1 &#x2F; $1 (Sonar) + per-req fee</td>
<td>No free tier</td>
<td>200K</td>
<td>No</td>
</tr>
<tr>
<td><strong>Groq</strong></td>
<td>–</td>
<td>$0.05 &#x2F; $0.08 (Llama 3.1 8B)</td>
<td>Rate-limited, no card</td>
<td>256K</td>
<td>Hosts OSS only</td>
</tr>
</tbody></table>
<div class="note info"><p><strong>“Open weights” vs “open source”</strong>: most providers in the column above release <strong>open weights</strong> (model parameters you can download and run) but not full training code or data. Apache 2.0 &#x2F; MIT models (Qwen, DeepSeek, Phi, GPT-OSS, Gemma 4) are the closest to true open source. Llama uses a more restrictive community license. Anthropic, Amazon, Perplexity, and now Meta’s Muse Spark line release nothing.</p>
</div>

<hr>
<h2 id="Subscription-comparison"><a href="#Subscription-comparison" class="headerlink" title="Subscription comparison"></a>Subscription comparison</h2><h3 id="All-providers-at-a-glance"><a href="#All-providers-at-a-glance" class="headerlink" title="All providers at a glance"></a>All providers at a glance</h3><table>
<thead>
<tr>
<th>Provider</th>
<th>Chat product</th>
<th>Free tier</th>
<th>Cheapest paid</th>
<th>~$20 tier</th>
<th>High-end</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Google Gemini</strong></td>
<td><a href="https://gemini.google.com/">Gemini</a></td>
<td>50 daily credits</td>
<td>AI Plus $8&#x2F;mo</td>
<td>AI Pro $20&#x2F;mo</td>
<td>AI Ultra $250&#x2F;mo</td>
</tr>
<tr>
<td><strong>OpenAI</strong></td>
<td><a href="https://chatgpt.com/">ChatGPT</a></td>
<td>GPT-5.5 Instant, ~10 msgs &#x2F; 5 hrs</td>
<td>Go $8&#x2F;mo</td>
<td>Plus $20&#x2F;mo</td>
<td>Pro $200&#x2F;mo</td>
</tr>
<tr>
<td><strong>Anthropic</strong></td>
<td><a href="https://claude.ai/">Claude</a></td>
<td>~15-40 msgs &#x2F; 5 hrs</td>
<td>–</td>
<td>Pro $20&#x2F;mo</td>
<td>Max 20x $200&#x2F;mo</td>
</tr>
<tr>
<td><strong>xAI</strong></td>
<td><a href="https://grok.com/">Grok</a></td>
<td>~10 msgs &#x2F; 2 hrs</td>
<td>X Premium $8&#x2F;mo</td>
<td>SuperGrok $30&#x2F;mo</td>
<td>Heavy $300&#x2F;mo</td>
</tr>
<tr>
<td><strong>DeepSeek</strong></td>
<td><a href="https://chat.deepseek.com/">DeepSeek</a></td>
<td>Unlimited (no sub)</td>
<td>–</td>
<td>–</td>
<td>–</td>
</tr>
<tr>
<td><strong>Mistral</strong></td>
<td><a href="https://chat.mistral.ai/">Le Chat</a></td>
<td>~25 msgs</td>
<td>Student $6&#x2F;mo</td>
<td>Pro $15&#x2F;mo</td>
<td>Enterprise (custom)</td>
</tr>
<tr>
<td><strong>Meta</strong></td>
<td><a href="https://meta.ai/">meta.ai</a></td>
<td>Free (Muse Spark)</td>
<td>–</td>
<td>–</td>
<td>–</td>
</tr>
<tr>
<td><strong>Alibaba Qwen</strong></td>
<td><a href="https://chat.qwen.ai/">Qwen</a></td>
<td>Unlimited (no sub)</td>
<td>–</td>
<td>–</td>
<td>–</td>
</tr>
<tr>
<td><strong>Amazon Nova</strong></td>
<td><a href="https://nova.amazon.com/">Nova</a></td>
<td>Free (US only)</td>
<td>–</td>
<td>–</td>
<td>–</td>
</tr>
<tr>
<td><strong>Microsoft</strong></td>
<td><a href="https://copilot.microsoft.com/">Copilot</a></td>
<td>Limited GPT-5.x</td>
<td>–</td>
<td>Copilot Pro $20&#x2F;mo</td>
<td>M365 Enterprise $30&#x2F;seat&#x2F;mo</td>
</tr>
<tr>
<td><strong>Perplexity</strong></td>
<td><a href="https://www.perplexity.ai/">Perplexity</a></td>
<td>Limited Pro Searches</td>
<td>–</td>
<td>Pro $20&#x2F;mo</td>
<td>Max $200&#x2F;mo</td>
</tr>
<tr>
<td><strong>Groq</strong></td>
<td>–</td>
<td>Rate-limited API</td>
<td>Pay-as-you-go</td>
<td>–</td>
<td>–</td>
</tr>
</tbody></table>
<h3 id="What-20-actually-buys"><a href="#What-20-actually-buys" class="headerlink" title="What ~$20 actually buys"></a>What ~$20 actually buys</h3><table>
<thead>
<tr>
<th>Feature</th>
<th>ChatGPT Plus ($20)</th>
<th>Claude Pro ($20)</th>
<th>Gemini AI Pro ($20)</th>
<th>SuperGrok ($30)</th>
<th>Le Chat Pro ($15)</th>
<th>Copilot Pro ($20)</th>
<th>Perplexity Pro ($20)</th>
</tr>
</thead>
<tbody><tr>
<td>Top model</td>
<td>GPT-5.5 Thinking</td>
<td>Opus 4.7</td>
<td>Gemini 3.1 Pro</td>
<td>Grok 4.20 &#x2F; 4.3 (rolling)</td>
<td>Mistral Medium 3.5</td>
<td>GPT-5.x</td>
<td>Opus 4.7, GPT-5.5, Gemini 3.1</td>
</tr>
<tr>
<td>Context</td>
<td>32K (196K Thinking)</td>
<td>200K (1M opt-in via &#x2F;extra-usage)</td>
<td>1M</td>
<td>128K</td>
<td>256K</td>
<td>–</td>
<td>200K</td>
</tr>
<tr>
<td>Image gen</td>
<td>gpt-image-1 (~180&#x2F;day)</td>
<td>No</td>
<td>Nano Banana 2</td>
<td>Aurora &#x2F; Imagine</td>
<td>Yes</td>
<td>gpt-image-1 (M365 only)</td>
<td>Yes</td>
</tr>
<tr>
<td>Video gen</td>
<td>Limited (Spud preview)</td>
<td>No</td>
<td>Veo 3.1 (limited)</td>
<td>Imagine (720p, 6s)</td>
<td>No</td>
<td>No</td>
<td>Yes</td>
</tr>
<tr>
<td>Voice mode</td>
<td>Advanced (video &#x2F; screen)</td>
<td>No</td>
<td>Yes</td>
<td>Extended</td>
<td>No</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td>Web search</td>
<td>Yes</td>
<td>Yes</td>
<td>Deep Search</td>
<td>DeepSearch</td>
<td>AFP-verified</td>
<td>Bing</td>
<td>Grounded (citations)</td>
</tr>
<tr>
<td>Coding agent</td>
<td>Codex</td>
<td>Claude Code</td>
<td>Jules (5x) + Antigravity</td>
<td>Grok Build (waitlist)</td>
<td>Vibe remote agents</td>
<td>–</td>
<td>–</td>
</tr>
<tr>
<td>Deep reasoning</td>
<td>GPT-5.5 Thinking</td>
<td>Extended Thinking</td>
<td>Deep Research</td>
<td>Big Brain Mode</td>
<td>Configurable per request</td>
<td>–</td>
<td>Deep Research</td>
</tr>
<tr>
<td>No training</td>
<td>No</td>
<td>Yes</td>
<td>No</td>
<td>No</td>
<td>Yes (Pro tier)</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td>API credits</td>
<td>No</td>
<td>No</td>
<td>$10&#x2F;mo GCP</td>
<td>No</td>
<td>No</td>
<td>No</td>
<td>$5&#x2F;mo</td>
</tr>
<tr>
<td>Unique value</td>
<td>Atlas browser + super-app</td>
<td>Skills + Cowork + Claude Code</td>
<td>Workspace + Antigravity + Jules</td>
<td>2M context, X data</td>
<td>Cheapest, no telemetry, EU</td>
<td>M365 integration</td>
<td>Multi-model + Comet browser</td>
</tr>
</tbody></table>
<p>The interesting column changes since March: ChatGPT Plus dropped Sora video and gained an AI browser; Claude Pro added Skills and a 1M default context on Opus 4.7; Gemini AI Pro added Antigravity at no extra cost; Perplexity Pro added Comet on every device.</p>
<p><strong>High-end tiers</strong>: Claude Max 20x ($200) for near-unlimited Opus 4.7 plus Claude Code. ChatGPT Pro ($200) for unlimited everything. Google AI Ultra ($250) for 25K credits, Deep Think, multi-agent Jules, YouTube Premium. SuperGrok Heavy ($300) for Grok 4 Heavy multi-agent and (when it ships) priority Grok 5 access.</p>
<hr>
<h2 id="Coding-agents-comparison"><a href="#Coding-agents-comparison" class="headerlink" title="Coding agents comparison"></a>Coding agents comparison</h2><p>Every major provider ships one. The interesting change in the last two months is that the gap closed, mostly upward.</p>
<h3 id="First-party-agents"><a href="#First-party-agents" class="headerlink" title="First-party agents"></a>First-party agents</h3><table>
<thead>
<tr>
<th>Agent</th>
<th>Provider</th>
<th>Type</th>
<th>Cheapest access</th>
<th>Rules</th>
<th>Memory</th>
<th>MCP</th>
<th>Sub-agents</th>
</tr>
</thead>
<tbody><tr>
<td><a href="https://openai.com/index/introducing-codex/">Codex</a></td>
<td>OpenAI</td>
<td>Cloud + IDE + browser</td>
<td>Plus $20&#x2F;mo</td>
<td><code>AGENTS.md</code></td>
<td>Yes (browser memories)</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://claude.com/product/claude-code">Claude Code</a></td>
<td>Anthropic</td>
<td>Terminal CLI + web + mobile</td>
<td>Pro $20&#x2F;mo</td>
<td><code>CLAUDE.md</code>, Skills</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://jules.google/">Jules</a></td>
<td>Google</td>
<td>Async GitHub</td>
<td>Free (15 tasks&#x2F;day)</td>
<td><code>AGENTS.md</code></td>
<td>Partial</td>
<td>Yes</td>
<td>Multi-agent at AI Ultra</td>
</tr>
<tr>
<td><a href="https://geminicli.com/">Gemini CLI</a></td>
<td>Google</td>
<td>Terminal CLI</td>
<td>Free (1K req&#x2F;day)</td>
<td><code>GEMINI.md</code>, Skills</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://developers.googleblog.com/build-with-google-antigravity-our-new-agentic-development-platform/">Antigravity</a></td>
<td>Google</td>
<td>Agentic IDE</td>
<td>Free (preview)</td>
<td>Skills</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes (manager surface)</td>
</tr>
<tr>
<td><a href="https://grokai.build/">Grok Build</a></td>
<td>xAI</td>
<td>Local CLI</td>
<td>Waitlist</td>
<td><code>.grok/GROK.md</code></td>
<td>Partial</td>
<td>Yes</td>
<td>Yes (8x)</td>
</tr>
<tr>
<td><a href="https://mistral.ai/news/vibe-remote-agents-mistral-medium-3-5">Vibe 2.0 + remote agents</a></td>
<td>Mistral</td>
<td>CLI + cloud</td>
<td>Le Chat Pro $15&#x2F;mo</td>
<td><code>AGENTS.md</code></td>
<td>No</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://github.com/QwenLM/qwen-code">Qwen Code</a></td>
<td>Alibaba</td>
<td>Terminal CLI</td>
<td>Free (1K req&#x2F;day)</td>
<td><code>QWEN.md</code></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://aws.amazon.com/q/developer/">Amazon Q Developer</a></td>
<td>Amazon</td>
<td>IDE + CLI</td>
<td>Free (50 agentic&#x2F;mo)</td>
<td><code>.amazonq/rules/</code></td>
<td>Partial</td>
<td>Yes</td>
<td>No</td>
</tr>
</tbody></table>
<h3 id="Third-party-agents"><a href="#Third-party-agents" class="headerlink" title="Third-party agents"></a>Third-party agents</h3><table>
<thead>
<tr>
<th>Agent</th>
<th>Type</th>
<th>Pricing</th>
<th>Rules</th>
<th>Memory</th>
<th>MCP</th>
<th>Sub-agents</th>
</tr>
</thead>
<tbody><tr>
<td><a href="https://cursor.com/">Cursor 3</a></td>
<td>VS Code fork + Agents Window</td>
<td>Free &#x2F; Pro $20 &#x2F; Ultra $200</td>
<td><code>.cursor/rules/*.mdc</code>, Skills</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes (parallel)</td>
</tr>
<tr>
<td><a href="https://windsurf.com/">Windsurf</a></td>
<td>Agentic IDE</td>
<td>Free &#x2F; Pro $15 &#x2F; Teams $30</td>
<td><code>.windsurf/rules/*.md</code></td>
<td>Yes</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr>
<td><a href="https://github.com/features/copilot/plans">GitHub Copilot</a></td>
<td>IDE + GitHub</td>
<td>Free &#x2F; Pro $10 &#x2F; Pro+ $39</td>
<td><code>.github/copilot-instructions.md</code></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://cline.bot/">Cline</a></td>
<td>VS Code extension</td>
<td>Free (BYOM) &#x2F; Teams $20</td>
<td><code>.clinerules</code></td>
<td>Yes</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr>
<td><a href="https://replit.com/">Replit Agent 3</a></td>
<td>Hosted full-stack scaffolder (slower, ~3-10 min&#x2F;prompt)</td>
<td>Free &#x2F; paid tiers</td>
<td>–</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://devin.ai/">Devin</a></td>
<td>Autonomous task agent</td>
<td>$500&#x2F;mo Team</td>
<td>–</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://github.com/OpenHands/OpenHands">OpenHands</a></td>
<td>OSS agent platform (CLI + cloud)</td>
<td>Free (OSS, BYOM) &#x2F; hosted Cloud</td>
<td>Skills (markdown)</td>
<td>Yes (event-sourced)</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://aider.chat/">Aider</a></td>
<td>Terminal</td>
<td>Free (BYOM)</td>
<td><code>CONVENTIONS.md</code></td>
<td>No</td>
<td>Partial</td>
<td>No</td>
</tr>
</tbody></table>
<div class="note info"><p><strong>Skills format</strong> (<code>SKILL.md</code>): now portable across Claude Code, Cursor, Gemini CLI, Codex CLI, and Antigravity. The same skill file invokes the same behaviour everywhere. This is the closest the agent ecosystem has come to a real cross-vendor standard, after MCP.</p>
<p><strong>Cloud sessions</strong>: Claude Code, Codex, and Cursor 3 all ship cloud-hosted sessions on isolated VMs. The shape is converging: kick off a task, close the laptop, monitor from your phone, get a PR.</p>
</div>

<hr>
<h2 id="Deep-dive-what-each-top-provider-does-beyond-the-model"><a href="#Deep-dive-what-each-top-provider-does-beyond-the-model" class="headerlink" title="Deep dive: what each top provider does beyond the model"></a>Deep dive: what each top provider does beyond the model</h2><p>The model is one component. Browser, desktop agent, IDE, mobile SDK, enterprise console, that’s where the providers are diverging fast. Six are worth walking through.</p>
<h3 id="OpenAI-a-desktop-OS-in-waiting"><a href="#OpenAI-a-desktop-OS-in-waiting" class="headerlink" title="OpenAI: a desktop OS in waiting"></a>OpenAI: a desktop OS in waiting</h3><p><a href="https://chatgpt.com/">chatgpt.com</a> | <a href="https://platform.openai.com/">platform.openai.com</a> | <a href="https://chatgpt.com/atlas/">Atlas</a></p>
<p>OpenAI has the broadest product surface, and the announced direction is to collapse it into one desktop app.</p>
<ul>
<li><strong><a href="https://chatgpt.com/atlas/">Atlas</a></strong> is a Chromium-based AI browser, macOS today, with Windows &#x2F; iOS &#x2F; Android in the queue. Sidebar ChatGPT on every page; “browser memories” stores what you’ve looked at; an opt-in agent mode drives the browser for you (paid only). The March 2026 announcement of the <a href="https://thetechportal.com/2026/03/20/openai-could-launch-a-desktop-super-app-integrating-chatgpt-codex-and-the-atlas-browser/">super-app</a> confirms ChatGPT, Codex, and Atlas merge into a single application: chat, code, browse, automate, all behind one window.</li>
<li><strong><a href="https://openai.com/index/introducing-codex/">Codex</a></strong> is the most-changed agent in the field this spring. Background computer use, an in-app browser, GitHub PR reviews, image generation, 90+ plugins, and an automatic-approval reviewer agent that handles routine permission prompts. GPT-5.5 is the recommended model. Codex Cloud runs sessions on managed VMs.</li>
<li><strong><a href="https://platform.openai.com/docs/guides/apps-sdk">Apps SDK</a></strong> (preview) lets you build apps that run inside ChatGPT, with the chat UI as the chrome. Distribution problem inverted: instead of getting users to install your app, you put it where they already type.</li>
<li><strong>Realtime API</strong> for client-side voice &#x2F; text streaming over WebRTC. Build voice agents that handle interruptions natively.</li>
<li><strong>ChatGPT Pulse</strong> (Pro $200&#x2F;mo) sends proactive briefings based on your context.</li>
<li><strong>Sora sunset</strong>. The standalone Sora app shut down April 26, the API ends September 24, 2026. Video generation continues inside ChatGPT (codename Spud) at lower fidelity and lower cost. OpenAI was, by their own framing, burning ~$1M&#x2F;day on Sora compute, and the unit economics didn’t shake out.</li>
</ul>
<p>If you’re shipping production: pure REST against the API, OpenAI-compatible client libraries everywhere. No first-party native iOS&#x2F;Android SDK, but <a href="https://github.com/MacPaw/OpenAI">MacPaw&#x2F;OpenAI</a> is the standard Swift pick.</p>
<h3 id="Anthropic-agents-skills-and-a-coding-tool-monoculture"><a href="#Anthropic-agents-skills-and-a-coding-tool-monoculture" class="headerlink" title="Anthropic: agents, skills, and a coding-tool monoculture"></a>Anthropic: agents, skills, and a coding-tool monoculture</h3><p><a href="https://claude.ai/">claude.ai</a> | <a href="https://console.anthropic.com/">console.anthropic.com</a> | <a href="https://github.com/anthropics/skills">Skills repo</a></p>
<p>The smallest model lineup of any major provider, paired with the most aggressive agent-platform investment.</p>
<ul>
<li><strong><a href="https://claude.com/product/claude-code">Claude Code</a></strong> is still the daily driver for many developers. Three new surfaces this spring: web at claude.ai&#x2F;code with persistent cloud sessions, the iOS app for monitoring and resuming those sessions, and <code>CLAUDE.md</code> plus Skills auto-loaded from the project filesystem.</li>
<li><strong><a href="https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills">Skills</a></strong> package domain expertise as <code>SKILL.md</code> files with progressive disclosure. The agent only loads a skill when the task calls for it. Anthropic ships official Excel &#x2F; Word &#x2F; PowerPoint &#x2F; PDF skills; the <a href="https://github.com/anthropics/skills">GitHub repo</a> hosts thousands of community skills. Cross-tool portability is the real story: the same file works in Claude Code, Cursor, Codex CLI, Gemini CLI, and Antigravity.</li>
<li><strong><a href="https://platform.claude.com/docs/en/managed-agents/overview">Managed Agents</a></strong> (public beta April 8, $0.08&#x2F;session-hour + tokens) is the hosted runtime: sandboxing, state persistence, permissions, error recovery, all behind a hosted session. May 6 added <a href="https://claude.com/blog/new-in-claude-managed-agents"><strong>Dreaming</strong></a> (Claude reviews past sessions to extract patterns and self-improve), <strong>Outcomes</strong> (rubric-driven grader runs in its own context window and pinpoints what to fix; +10 pp over a standard prompting loop in internal tests), and <strong>Multiagent Orchestration</strong> (lead agent decomposes work and delegates to specialists with their own model &#x2F; prompt &#x2F; tools). This is Anthropic’s first real production deployment story.</li>
<li><strong><a href="https://claude.com/product/cowork">Cowork</a></strong> is the desktop agent for non-developers: researchers, analysts, ops, legal, finance. It reads, edits, and creates files in folders you grant access to. April 9 brought general availability across all paid plans, Enterprise group &#x2F; role provisioning via SCIM, and a <a href="https://news.zoom.com/zoom-meeting-intelligence-in-claude/">Zoom MCP connector</a> that pulls meeting summaries, transcripts, and recordings into the agent’s context. May 5 added ten ready-to-run finance agent templates plus Microsoft 365 add-ins for Excel, PowerPoint, and Word (Outlook coming soon).</li>
<li><strong><a href="https://www.anthropic.com/news/claude-design-anthropic-labs">Claude Design</a></strong> (April 17), a collaborative visual creation tool at <a href="https://claude.ai/design">claude.ai&#x2F;design</a>, powered by Opus 4.7. Prompts in, designs &#x2F; prototypes &#x2F; slides &#x2F; one-pagers out. Available in research preview to Pro, Max, Team, and Enterprise.</li>
<li><strong><a href="https://red.anthropic.com/2026/mythos-preview/">Mythos Preview</a></strong> (April 7) is Anthropic’s most capable model yet, with cybersecurity capabilities so strong Anthropic is deliberately <em>not</em> releasing it broadly: Mythos has already been used to find thousands of zero-day vulnerabilities across major OSes and browsers. Instead, <a href="https://www.anthropic.com/glasswing"><strong>Project Glasswing</strong></a> bundles it with 12 launch partners (AWS, Apple, Broadcom, Cisco, CrowdStrike, Google, JPMorganChase, Linux Foundation, Microsoft, NVIDIA, Palo Alto Networks, plus Anthropic) and 40+ critical-infrastructure orgs, with $100M in usage credits and $4M in donations to OSS security work.</li>
<li><strong><a href="https://www.builder.io/blog/the-claude-advisor-pattern">Advisor tool</a></strong> (API beta since April 9): Sonnet or Haiku executes; Opus advises on demand inside a single API call. Haiku-with-Opus-advisor more than doubles solo Haiku on SWE-bench Multilingual at 85% lower cost than running Sonnet directly.</li>
</ul>
<p>No first-party mobile SDK. Seven server-side SDKs (Python, TypeScript, Java, Go, Ruby, C#, PHP). <a href="https://github.com/jamesrochabrun/SwiftAnthropic">SwiftAnthropic</a> is the de facto Swift package. <strong>No model weights released</strong>, ever, by design.</p>
<h3 id="Google-ecosystem-maximalist"><a href="#Google-ecosystem-maximalist" class="headerlink" title="Google: ecosystem maximalist"></a>Google: ecosystem maximalist</h3><p><a href="https://gemini.google.com/">gemini.google.com</a> | <a href="https://aistudio.google.com/">AI Studio</a> | <a href="https://antigravity.google/">Antigravity</a></p>
<p>The widest product surface and the only first-party mobile SDK. Most of what’s new this spring is around the model rather than a new flagship.</p>
<ul>
<li><strong><a href="https://developers.googleblog.com/build-with-google-antigravity-our-new-agentic-development-platform/">Antigravity</a></strong> is Google’s agentic IDE, in public preview, free for individuals. Two surfaces: an Editor View with the usual tab completions, plus a Manager Surface where you spawn, orchestrate, and observe multiple agents working asynchronously across workspaces. Runs on Gemini 3.1 Pro &#x2F; Flash by default. Claude Sonnet 4.6, Claude Opus 4.6, and GPT-OSS 120B are selectable from settings, billed through Google with no separate API key required, which is the unique part. (Caveat: <a href="https://discuss.ai.google.dev/t/claude-opus-4-7-has-been-available-for-24-hours-antigravity-is-now-the-only-major-ai-coding-tool-without-it/140842">Opus 4.7 still hasn’t shipped to the picker</a> three weeks after launch, leaving Antigravity as the only major coding tool without it.)</li>
<li><strong><a href="https://jules.google/">Jules</a></strong> is the async GitHub agent: ship it a task, it returns a PR. AI Pro gets 5x the free tier, AI Ultra gets 20x and multi-agent Jules.</li>
<li><strong>Gemini Code Assist</strong> in IDEs, <strong>Gemini CLI</strong> in the terminal, all free at hobbyist rate limits.</li>
<li><strong><a href="https://workspace.google.com/solutions/ai/">Workspace AI</a></strong>: Gemini directly in Gmail, Docs, Sheets, Slides, Meet, and Drive. The integration competitors haven’t matched.</li>
<li><strong><a href="https://notebooklm.google.com/">NotebookLM</a></strong> for research workflows. AI Pro raises the cap to 500 notebooks &#x2F; 300 sources &#x2F; 500 queries&#x2F;day; AI Ultra further.</li>
<li><strong><a href="https://cloud.google.com/blog/topics/google-cloud-next/welcome-to-google-cloud-next-26">Cloud Next ‘26</a></strong> (April) introduced the <strong>Gemini Enterprise Agent Platform</strong> and eighth-generation TPUs, alongside <a href="https://blog.google/technology/developers/gemma-4/">Gemma 4</a> (the most capable open model byte-for-byte, per Google).</li>
<li><strong>Image and video gen</strong>: <a href="https://blog.google/">Nano Banana 2</a> and Gemini 3.1 Flash Image Preview for fast image gen at low cost; Veo 3.1 for video; Gemini 3.1 Flash TTS for speech.</li>
<li><strong><a href="https://firebase.google.com/products/firebase-ai-logic">Firebase AI Logic SDKs</a></strong> are still the only first-party mobile story for Gemini: Swift, Kotlin, Flutter, React Native, Unity, Web. Everyone else, you write REST.</li>
</ul>
<p>Google I&#x2F;O 2026 lands May 19. Gemini 3.5 hasn’t shipped yet despite Polymarket bets and a Thomas Kurian “very, very soon” tease on April 24. Bet accordingly.</p>
<h3 id="xAI-the-social-data-play"><a href="#xAI-the-social-data-play" class="headerlink" title="xAI: the social-data play"></a>xAI: the social-data play</h3><p><a href="https://grok.com/">grok.com</a> | <a href="https://x.ai/api">x.ai&#x2F;api</a></p>
<p>The narrowest deep-dive in this list. xAI’s value isn’t a sprawling product surface, it’s a corpus and a distribution channel nobody else has.</p>
<ul>
<li><strong>Real-time X data</strong>. Grok is the only frontier-model chat with live access to the entire X firehose. For news, sentiment, market chatter, breaking events, this is genuinely differentiating.</li>
<li><strong>Grok in Tesla</strong>. In-vehicle voice assistant on Model 3 &#x2F; Y &#x2F; S &#x2F; X &#x2F; Cybertruck running recent firmware. Hands-on a steering wheel, the only competitor with this kind of car-cabin distribution is the Apple &#x2F; Google &#x2F; Amazon trio with their respective in-car offerings.</li>
<li><strong><a href="https://x.ai/imagine">Imagine</a></strong>: text-to-image and 720p &#x2F; 6-second video generation, available from SuperGrok onward.</li>
<li><strong>Aurora</strong> image editing in the chat interface.</li>
<li><strong>SuperGrok Heavy</strong> ($300&#x2F;mo) runs Grok 4 Heavy multi-agent, the most expensive consumer subscription on the market. 256K context, 100% AIME 2025, dramatic priority routing.</li>
<li><strong>OpenAI-compatible API</strong> means any OpenAI client library works against <a href="https://x.ai/api">x.ai&#x2F;api</a>. $25 sign-up credits; pay-as-you-go after.</li>
<li><strong>SpaceX acquisition</strong> (announced February 2). On paper, this folds xAI into a single Elon-controlled entity with deep-pocketed compute backing. Grok 5 (6T params, MoE on Colossus 2) is targeting a Q2 public beta, expected May or June.</li>
</ul>
<p>If you’re using Grok in a product: Grok 4.20 still offers a 2M-token context window (the largest among hosted closed-weight models; Llama 4 Scout’s 10M is open-weight self-host), while Grok 4.3 is 1M. The per-request fee structure is leaner than Perplexity’s.</p>
<h3 id="Microsoft-where-work-already-lives"><a href="#Microsoft-where-work-already-lives" class="headerlink" title="Microsoft: where work already lives"></a>Microsoft: where work already lives</h3><p><a href="https://copilot.microsoft.com/">copilot.microsoft.com</a> | <a href="https://github.com/features/models">GitHub Models</a> | <a href="https://azure.microsoft.com/en-us/products/ai-foundry/">Azure AI Foundry</a></p>
<p>Microsoft’s strategy is to be the cheapest place to <em>deploy</em> anyone’s model and the deepest place to integrate any AI into the apps people already use.</p>
<ul>
<li><strong>GitHub Copilot</strong> is now a marketplace, not a single product. Free &#x2F; Pro $10 &#x2F; Pro+ $39. Pro+ includes an autonomous coding agent that creates PRs from issues, and you choose the underlying model (GPT-5.5, Claude Opus 4.7, Gemini 3.1, others) per task.</li>
<li><strong><a href="https://github.com/features/models">GitHub Models</a></strong> is the easiest free playground in the industry: pick any of 30+ frontier and open models, get a chat UI plus an API endpoint, no credit card. Rate-limited (50-150 req&#x2F;day) but enough for evaluation.</li>
<li><strong>Microsoft 365 Copilot</strong>: Copilot inside Word, Excel, PowerPoint, Outlook, Teams. New this spring: meeting recap, a redesigned Edge with built-in Copilot controls, AI-generated icons, multi-agent workflows, richer connector understanding, Foundry agents, expanded Copilot Studio extensibility. None of this is replicable on a competitor stack.</li>
<li><strong><a href="https://azure.microsoft.com/en-us/products/ai-foundry/">Azure AI Foundry</a></strong> hosts your model, anyone’s model, including direct competitors. GPT-5.5, Claude, Gemini, Llama, Mistral, Qwen, all behind one billing relationship and one compliance posture.</li>
<li><strong><a href="https://www.microsoft.com/en-us/microsoft-copilot/microsoft-copilot-studio">Copilot Studio</a></strong> for low-code agent building. The 2026 release wave 1 added publishing pipelines, multi-agent orchestration, governance controls.</li>
<li><strong>Phi-Silica</strong> on Copilot+ PCs runs Microsoft’s small models (Phi-4-mini, Phi-4-multimodal) on the device’s NPU, on Windows, with no network call. The first credible Apple Intelligence equivalent on the PC side.</li>
<li><strong>MAI-1</strong>, Microsoft’s own ~500B frontier model, is internal so far; the <a href="https://huggingface.co/microsoft/phi-4">Phi family</a> (MIT-licensed, 3.8B-14B) covers the open-weight position.</li>
</ul>
<p>For app developers: ONNX Runtime Mobile (CoreML on iOS, NNAPI on Android) is Microsoft’s pre-quantized on-device pipeline.</p>
<h3 id="Perplexity-search-shaped-surface"><a href="#Perplexity-search-shaped-surface" class="headerlink" title="Perplexity: search-shaped surface"></a>Perplexity: search-shaped surface</h3><p><a href="https://www.perplexity.ai/">perplexity.ai</a> | <a href="https://docs.perplexity.ai/">docs.perplexity.ai</a> | <a href="https://www.perplexity.ai/comet">Comet</a></p>
<p>Perplexity is the only top-tier provider that doesn’t really sell its own model. The pitch is shape: every response is grounded in citations, every product is search-first, and the subscription gives you third-party models in one place.</p>
<ul>
<li><strong><a href="https://www.perplexity.ai/comet">Comet</a></strong> went free across iOS, Android, Mac, and Windows this spring. The April 28 <a href="https://9to5mac.com/2026/04/28/perplexity-just-gave-its-comet-ai-browser-an-upgrade-for-ipad-users-with-these-features/">iPad build</a> added multi-window and Split View. Comet hit #3 overall on the US App Store within 48 hours of iOS launch.</li>
<li><strong>Samsung Galaxy S26</strong> is the first phone to integrate Perplexity at the OS level: Bixby uses Perplexity for real-time search and reasoning, and Samsung Internet inherits Comet’s agentic browsing. This is the first OS-level distribution deal a non-OpenAI &#x2F; non-Google AI provider has landed.</li>
<li><strong>Multi-model access in Pro</strong>. $20&#x2F;mo unlocks GPT-5.5, Claude Opus 4.7, Sonnet 4.6, and Gemini 3.1 Pro through one interface. If you only want one chat sub and want to compare models, this is the strongest value proposition, full stop.</li>
<li><strong>Computer</strong> (Max $200&#x2F;mo) is Perplexity’s multi-agent system: spawn long-running agents that browse, research, and synthesize, with citations that survive the full chain.</li>
<li><strong>Spaces</strong> for shared research workspaces and <strong>Comet Plus</strong> for premium publisher access (NYT, Reuters, etc.) without separate subscriptions.</li>
<li><strong><a href="https://docs.perplexity.ai/api-reference/search-post">Search API</a></strong> at $5&#x2F;1K requests gives you raw web results without the generation layer. Useful as a grounding primitive for your own agents.</li>
<li><strong>API credits</strong>: $5&#x2F;month included with Pro, the only ~$20 sub that bundles them.</li>
</ul>
<p>API itself is hybrid pricing: tokens <em>plus</em> per-request fees ($5-$14 per 1K calls depending on model and search depth). Pure token-based providers usually beat it on price-per-call; Perplexity wins on retrieval quality.</p>
<hr>
<h2 id="What-I’d-actually-pick"><a href="#What-I’d-actually-pick" class="headerlink" title="What I’d actually pick"></a>What I’d actually pick</h2><p><strong>Free chat</strong>: DeepSeek and Qwen still give you the most for $0. DeepSeek V4 in the chat app is unlimited. Gemini’s free API is the most generous of the API-side free tiers.</p>
<p><strong>One $20 sub</strong>: Claude Pro is the strongest single sub for builders, with Skills, Cowork, Claude Code on web + mobile, and Opus 4.7 at 200K context (1M opt-in via <code>/extra-usage</code>; the auto-1M default is Max-and-up). Plus is the broadest if you want everything-in-one. Perplexity Pro stretches the dollar by giving you GPT-5.5, Opus 4.7, and Gemini 3.1 in one interface plus the Comet browser. Le Chat Pro at $15 stays the cheapest with no telemetry.</p>
<p><strong>Coding</strong>: Claude Code is still the daily driver here. Cursor 3’s Agents Window matches it on parallelism and beats it on inline UX. Codex is the gap-closer of 2026, the April update made it credible for agentic tasks for the first time. Antigravity is the wildcard if you live in Google’s ecosystem.</p>
<p><strong>Production app</strong>: Google’s Firebase AI Logic remains the only first-party native mobile SDK story. Everyone else is REST or community packages. If your app is Mac&#x2F;Windows desktop and AI-first, the OpenAI super-app architecture and Anthropic’s Skills + Managed Agents are both betting that you’ll integrate at a chat-shaped surface, not a chat completion endpoint.</p>
<p><strong>Open weights</strong>: DeepSeek V4 (MIT, 1M context, hardware-portable), Qwen3.6-35B-A3B (Apache 2.0, agentic), Gemma 4, Llama 4 Scout (10M context if you can afford to run it), Phi-4 family (MIT, the best small models). Meta’s Muse Spark is the news that <em>isn’t</em> on this list, and probably won’t be.</p>
<p>The mental shift since March: I now pick a provider partly for its model and largely for what it bundles. Anthropic for skills + agents. Google for Workspace. OpenAI for browser + super-app. Microsoft for M365 and GitHub. xAI for X data and Tesla. Perplexity for grounded search and Comet. Run two or three subs without much overlap, because each is reaching for a different surface.</p>
<p>Eight more weeks and the model column will turn over again. The platform column won’t.</p>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>ai</tag>
        <tag>api</tag>
        <tag>llm</tag>
        <tag>subscription</tag>
        <tag>agent</tag>
      </tags>
  </entry>
  <entry>
    <title>Apple Audio Frameworks: A Complete Guide to iOS and macOS Audio</title>
    <url>/Apple-Audio-Frameworks-Complete-Guide/</url>
    <content><![CDATA[<p>Six layers of Apple audio framework, from <code>AudioObject</code> (raw hardware) up to <code>AVPlayer</code> (one-line playback). What each layer is for, and when to drop down.</p>
<span id="more"></span>

<h2 id="The-Audio-Stack"><a href="#The-Audio-Stack" class="headerlink" title="The Audio Stack"></a>The Audio Stack</h2><p>Apple’s audio architecture is a layered system. Each layer adds convenience at the cost of control:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌──────────────────────────────────────────────────┐</span><br><span class="line">│  AVFoundation                                    │  Highest level</span><br><span class="line">│  (AVPlayer, AVAssetReader, AVMutableComposition) │  Media playback, editing, export</span><br><span class="line">├──────────────────────────────────────────────────┤</span><br><span class="line">│  AVFAudio                                        │  Audio-specific</span><br><span class="line">│  (AVAudioEngine, AVAudioSession, AVAudioPlayer,  │  Engine graph, session config,</span><br><span class="line">│   AVAudioRecorder, AVAudioFile)                  │  recording, file I/O</span><br><span class="line">├──────────────────────────────────────────────────┤</span><br><span class="line">│  AudioToolbox                                    │  C API</span><br><span class="line">│  (Audio Queue, Audio File, Audio Converter,      │  Queues, files, codecs,</span><br><span class="line">│   Extended Audio File, Music Sequence)           │  stream parsing, MIDI</span><br><span class="line">├──────────────────────────────────────────────────┤</span><br><span class="line">│  Audio Units                                     │  Plug-in layer</span><br><span class="line">│  (AUAudioUnit, built-in effects, AUv3 extensions)│  Real-time DSP, effects, instruments</span><br><span class="line">├──────────────────────────────────────────────────┤</span><br><span class="line">│  Core Audio / CoreAudioTypes                     │  Foundation types</span><br><span class="line">│  (ASBD, AudioBuffer, AudioTimeStamp,             │  Format descriptors, buffers,</span><br><span class="line">│   AudioChannelLayout)                            │  timing, channel layouts</span><br><span class="line">├──────────────────────────────────────────────────┤</span><br><span class="line">│  Audio HAL (macOS only)                          │  Hardware access</span><br><span class="line">│  (AudioObject, AudioDevice, AudioStream)         │  Device enumeration, I/O</span><br><span class="line">└──────────────────────────────────────────────────┘</span><br></pre></td></tr></table></figure>

<p>The sections below are ordered bottom-up: foundation types first, highest-level APIs last.</p>
<p><strong>Rule of thumb</strong>: Start at the highest level that meets your needs. Drop down only when you need more control.</p>
<table>
<thead>
<tr>
<th>Need</th>
<th>Start here</th>
</tr>
</thead>
<tbody><tr>
<td>Play a URL or stream</td>
<td>AVPlayer (AVFoundation)</td>
</tr>
<tr>
<td>Play a local file</td>
<td>AVAudioPlayer (AVFAudio)</td>
</tr>
<tr>
<td>Real-time effects or mixing</td>
<td>AVAudioEngine (AVFAudio)</td>
</tr>
<tr>
<td>Custom audio processing</td>
<td>Audio Units</td>
</tr>
<tr>
<td>Format conversion, stream parsing</td>
<td>AudioToolbox</td>
</tr>
<tr>
<td>Sound classification (ML)</td>
<td>Sound Analysis</td>
</tr>
<tr>
<td>Raw hardware access (macOS)</td>
<td>Core Audio HAL</td>
</tr>
</tbody></table>
<hr>
<h2 id="Core-Audio-CoreAudioTypes"><a href="#Core-Audio-CoreAudioTypes" class="headerlink" title="Core Audio (CoreAudioTypes)"></a>Core Audio (CoreAudioTypes)</h2><p>Core Audio is the foundation. Every other audio framework on Apple platforms is built on these C types.</p>
<h3 id="AudioStreamBasicDescription-ASBD"><a href="#AudioStreamBasicDescription-ASBD" class="headerlink" title="AudioStreamBasicDescription (ASBD)"></a>AudioStreamBasicDescription (ASBD)</h3><p>The most important type in all of Apple audio. It describes any audio format:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">AudioStreamBasicDescription</span> &#123;</span></span><br><span class="line">    Float64 mSampleRate;        <span class="comment">// 44100.0, 48000.0, etc.</span></span><br><span class="line">    UInt32  mFormatID;          <span class="comment">// kAudioFormatLinearPCM, kAudioFormatMPEG4AAC, etc.</span></span><br><span class="line">    UInt32  mFormatFlags;       <span class="comment">// Float, BigEndian, Packed, NonInterleaved, etc.</span></span><br><span class="line">    UInt32  mBytesPerPacket;</span><br><span class="line">    UInt32  mFramesPerPacket;   <span class="comment">// 1 for PCM, 1024 for AAC</span></span><br><span class="line">    UInt32  mBytesPerFrame;</span><br><span class="line">    UInt32  mChannelsPerFrame;  <span class="comment">// 1 = mono, 2 = stereo</span></span><br><span class="line">    UInt32  mBitsPerChannel;    <span class="comment">// 16, 24, 32</span></span><br><span class="line">    UInt32  mReserved;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><strong>Format IDs</strong> (the codecs Apple supports natively):</p>
<table>
<thead>
<tr>
<th>Constant</th>
<th>Format</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td><code>kAudioFormatLinearPCM</code></td>
<td>Uncompressed PCM</td>
<td>The raw format – all processing happens in PCM</td>
</tr>
<tr>
<td><code>kAudioFormatMPEG4AAC</code></td>
<td>AAC</td>
<td>Default for Apple devices, excellent quality&#x2F;size</td>
</tr>
<tr>
<td><code>kAudioFormatMPEGLayer3</code></td>
<td>MP3</td>
<td>Decode only on iOS (no encode)</td>
</tr>
<tr>
<td><code>kAudioFormatAppleLossless</code></td>
<td>ALAC</td>
<td>Lossless, Apple’s alternative to FLAC</td>
</tr>
<tr>
<td><code>kAudioFormatFLAC</code></td>
<td>FLAC</td>
<td>Free Lossless Audio Codec</td>
</tr>
<tr>
<td><code>kAudioFormatOpus</code></td>
<td>Opus</td>
<td>Low-latency, used in WebRTC</td>
</tr>
<tr>
<td><code>kAudioFormatAppleIMA4</code></td>
<td>IMA 4:1 ADPCM</td>
<td>Compressed, low CPU decode</td>
</tr>
<tr>
<td><code>kAudioFormatAMR</code></td>
<td>AMR Narrowband</td>
<td>Voice calls</td>
</tr>
<tr>
<td><code>kAudioFormatAMR_WB</code></td>
<td>AMR Wideband</td>
<td>HD voice</td>
</tr>
<tr>
<td><code>kAudioFormatAudible</code></td>
<td>Audible AA&#x2F;AAX</td>
<td>Audiobook DRM format</td>
</tr>
<tr>
<td><code>kAudioFormatMPEG4AAC_HE</code></td>
<td>HE-AAC</td>
<td>Low bitrate streaming</td>
</tr>
<tr>
<td><code>kAudioFormatMPEG4AAC_HE_V2</code></td>
<td>HE-AAC v2</td>
<td>Even lower bitrate</td>
</tr>
<tr>
<td><code>kAudioFormatMPEG4AAC_ELD</code></td>
<td>AAC-ELD</td>
<td>Ultra-low latency for FaceTime</td>
</tr>
</tbody></table>
<p><strong>Format flags</strong> for PCM:</p>
<table>
<thead>
<tr>
<th>Flag</th>
<th>Meaning</th>
</tr>
</thead>
<tbody><tr>
<td><code>kAudioFormatFlagIsFloat</code></td>
<td>32-bit float samples (vs integer)</td>
</tr>
<tr>
<td><code>kAudioFormatFlagIsBigEndian</code></td>
<td>Big-endian byte order</td>
</tr>
<tr>
<td><code>kAudioFormatFlagIsSignedInteger</code></td>
<td>Signed integer samples</td>
</tr>
<tr>
<td><code>kAudioFormatFlagIsPacked</code></td>
<td>No padding between samples</td>
</tr>
<tr>
<td><code>kAudioFormatFlagIsNonInterleaved</code></td>
<td>Separate buffer per channel</td>
</tr>
</tbody></table>
<h3 id="Canonical-Formats"><a href="#Canonical-Formats" class="headerlink" title="Canonical Formats"></a>Canonical Formats</h3><p>AVAudioEngine uses <strong>non-interleaved 32-bit float</strong> internally:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Non-interleaved (AVAudioEngine default):</span><br><span class="line">  Buffer 0: [L0, L1, L2, L3, ...]  (left channel)</span><br><span class="line">  Buffer 1: [R0, R1, R2, R3, ...]  (right channel)</span><br><span class="line"></span><br><span class="line">Interleaved (common in files):</span><br><span class="line">  Buffer 0: [L0, R0, L1, R1, L2, R2, ...]</span><br></pre></td></tr></table></figure>

<p>In Swift, <code>AVAudioFormat</code> wraps ASBD:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Standard non-interleaved float format (what AVAudioEngine uses)</span></span><br><span class="line"><span class="keyword">let</span> format <span class="operator">=</span> <span class="type">AVAudioFormat</span>(standardFormatWithSampleRate: <span class="number">44100</span>, channels: <span class="number">2</span>)<span class="operator">!</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// From an ASBD</span></span><br><span class="line"><span class="keyword">var</span> asbd <span class="operator">=</span> <span class="type">AudioStreamBasicDescription</span>(<span class="operator">...</span>)</span><br><span class="line"><span class="keyword">let</span> format <span class="operator">=</span> <span class="type">AVAudioFormat</span>(streamDescription: <span class="operator">&amp;</span>asbd)</span><br></pre></td></tr></table></figure>

<h3 id="AudioBuffer-and-AudioBufferList"><a href="#AudioBuffer-and-AudioBufferList" class="headerlink" title="AudioBuffer and AudioBufferList"></a>AudioBuffer and AudioBufferList</h3><p>The raw sample containers:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">AudioBuffer</span> &#123;</span></span><br><span class="line">    UInt32 mNumberChannels;     <span class="comment">// Channels in this buffer</span></span><br><span class="line">    UInt32 mDataByteSize;       <span class="comment">// Size of mData</span></span><br><span class="line">    <span class="type">void</span>*  mData;               <span class="comment">// The samples</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">AudioBufferList</span> &#123;</span></span><br><span class="line">    UInt32      mNumberBuffers;  <span class="comment">// 1 for interleaved, N for non-interleaved</span></span><br><span class="line">    AudioBuffer mBuffers[<span class="number">1</span>];     <span class="comment">// Variable-length array</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>In Swift, <code>AVAudioPCMBuffer</code> provides safe access:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> buffer <span class="operator">=</span> <span class="type">AVAudioPCMBuffer</span>(pcmFormat: format, frameCapacity: <span class="number">4096</span>)<span class="operator">!</span></span><br><span class="line"><span class="comment">// Float channel data (non-interleaved)</span></span><br><span class="line"><span class="keyword">let</span> leftChannel <span class="operator">=</span> buffer.floatChannelData<span class="operator">!</span>[<span class="number">0</span>]   <span class="comment">// UnsafeMutablePointer&lt;Float&gt;</span></span><br><span class="line"><span class="keyword">let</span> rightChannel <span class="operator">=</span> buffer.floatChannelData<span class="operator">!</span>[<span class="number">1</span>]</span><br></pre></td></tr></table></figure>

<h3 id="AudioTimeStamp"><a href="#AudioTimeStamp" class="headerlink" title="AudioTimeStamp"></a>AudioTimeStamp</h3><p>Timing information with multiple representations:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">AudioTimeStamp</span> &#123;</span></span><br><span class="line">    Float64        mSampleTime;   <span class="comment">// Sample frame position</span></span><br><span class="line">    UInt64         mHostTime;     <span class="comment">// Mach absolute time (nanoseconds)</span></span><br><span class="line">    Float64        mRateScalar;   <span class="comment">// Rate relative to host clock</span></span><br><span class="line">    UInt64         mWordClockTime;</span><br><span class="line">    SMPTETime      mSMPTETime;</span><br><span class="line">    AudioTimeStampFlags mFlags;   <span class="comment">// Which fields are valid</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="AudioChannelLayout"><a href="#AudioChannelLayout" class="headerlink" title="AudioChannelLayout"></a>AudioChannelLayout</h3><p>Maps channels to speaker positions:</p>
<table>
<thead>
<tr>
<th>Layout Tag</th>
<th>Channels</th>
<th>Description</th>
</tr>
</thead>
<tbody><tr>
<td><code>kAudioChannelLayoutTag_Mono</code></td>
<td>1</td>
<td>Single center</td>
</tr>
<tr>
<td><code>kAudioChannelLayoutTag_Stereo</code></td>
<td>2</td>
<td>Left + Right</td>
</tr>
<tr>
<td><code>kAudioChannelLayoutTag_MPEG_5_1_A</code></td>
<td>6</td>
<td>5.1 surround</td>
</tr>
<tr>
<td><code>kAudioChannelLayoutTag_MPEG_7_1_A</code></td>
<td>8</td>
<td>7.1 surround</td>
</tr>
<tr>
<td><code>kAudioChannelLayoutTag_Atmos_5_1_4</code></td>
<td>10</td>
<td>Dolby Atmos 5.1.4</td>
</tr>
</tbody></table>
<h3 id="Real-Time-Programming-Rules"><a href="#Real-Time-Programming-Rules" class="headerlink" title="Real-Time Programming Rules"></a>Real-Time Programming Rules</h3><p>Audio render callbacks run on a <strong>real-time priority thread</strong>. Violating these rules causes audio glitches (clicks, pops, dropouts):</p>
<table>
<thead>
<tr>
<th>Rule</th>
<th>Reason</th>
</tr>
</thead>
<tbody><tr>
<td>No <code>malloc</code> &#x2F; <code>free</code></td>
<td>Memory allocator uses locks</td>
</tr>
<tr>
<td>No Objective-C messaging (<code>objc_msgSend</code>)</td>
<td>May trigger autorelease pools or locks</td>
</tr>
<tr>
<td>No locks (<code>pthread_mutex_lock</code>)</td>
<td>Priority inversion with lower-priority threads</td>
</tr>
<tr>
<td>No file I&#x2F;O</td>
<td>Disk access is unbounded latency</td>
</tr>
<tr>
<td>No Swift ARC (<code>retain</code> &#x2F; <code>release</code>)</td>
<td>Atomic reference counting uses locks</td>
</tr>
<tr>
<td>No <code>NSLog</code> &#x2F; <code>print</code> &#x2F; <code>os_log</code></td>
<td>I&#x2F;O operations</td>
</tr>
<tr>
<td>No <code>dispatch_sync</code> to another queue</td>
<td>Can cause deadlock or priority inversion</td>
</tr>
</tbody></table>
<p><strong>Safe on the render thread</strong>: Plain C&#x2F;C++ math, pre-allocated buffer access, atomic operations, block&#x2F;closure calls (if the block itself follows these rules), <code>Accelerate.framework</code> vDSP functions.</p>
<h3 id="Swift-Interop-Challenges"><a href="#Swift-Interop-Challenges" class="headerlink" title="Swift Interop Challenges"></a>Swift Interop Challenges</h3><p>Core Audio’s C APIs require careful handling in Swift:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C callback functions can&#x27;t capture context -- use UnsafeMutableRawPointer</span></span><br><span class="line"><span class="keyword">let</span> renderCallback: <span class="type">AURenderCallback</span> <span class="operator">=</span> &#123; (</span><br><span class="line">    inRefCon: <span class="type">UnsafeMutableRawPointer</span>,</span><br><span class="line">    ioActionFlags: <span class="type">UnsafeMutablePointer</span>&lt;<span class="type">AudioUnitRenderActionFlags</span>&gt;,</span><br><span class="line">    inTimeStamp: <span class="type">UnsafePointer</span>&lt;<span class="type">AudioTimeStamp</span>&gt;,</span><br><span class="line">    inBusNumber: <span class="type">UInt32</span>,</span><br><span class="line">    inNumberFrames: <span class="type">UInt32</span>,</span><br><span class="line">    ioData: <span class="type">UnsafeMutablePointer</span>&lt;<span class="type">AudioBufferList</span>&gt;?</span><br><span class="line">) -&gt; <span class="type">OSStatus</span> <span class="keyword">in</span></span><br><span class="line">    <span class="comment">// Process audio here</span></span><br><span class="line">    <span class="keyword">return</span> noErr</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// AudioBufferList manipulation</span></span><br><span class="line"><span class="keyword">let</span> ablPointer <span class="operator">=</span> <span class="type">UnsafeMutableAudioBufferListPointer</span>(ioData<span class="operator">!</span>)</span><br><span class="line"><span class="keyword">for</span> buffer <span class="keyword">in</span> ablPointer &#123;</span><br><span class="line">    <span class="keyword">let</span> samples <span class="operator">=</span> <span class="type">UnsafeMutableBufferPointer</span>&lt;<span class="type">Float</span>&gt;(buffer)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="number">0</span><span class="operator">..&lt;</span><span class="type">Int</span>(inNumberFrames) &#123;</span><br><span class="line">        samples[i] <span class="operator">*=</span> <span class="number">0.5</span>  <span class="comment">// halve volume</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="Audio-Units"><a href="#Audio-Units" class="headerlink" title="Audio Units"></a>Audio Units</h2><p>Audio Units are Apple’s real-time audio plug-in architecture. Every node in <code>AVAudioEngine</code> is an Audio Unit under the hood. Understanding Audio Units gives you access to the full power that AVAudioEngine abstracts.</p>
<h3 id="AUAudioUnit-v3-API"><a href="#AUAudioUnit-v3-API" class="headerlink" title="AUAudioUnit (v3 API)"></a>AUAudioUnit (v3 API)</h3><p>The modern Objective-C&#x2F;Swift class (iOS 9+) for building and hosting audio plug-ins:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Create an Audio Unit by description</span></span><br><span class="line"><span class="keyword">let</span> description <span class="operator">=</span> <span class="type">AudioComponentDescription</span>(</span><br><span class="line">    componentType: kAudioUnitType_Effect,</span><br><span class="line">    componentSubType: kAudioUnitSubType_NBandEQ,</span><br><span class="line">    componentManufacturer: kAudioUnitManufacturer_Apple,</span><br><span class="line">    componentFlags: <span class="number">0</span>,</span><br><span class="line">    componentFlagsMask: <span class="number">0</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="type">AUAudioUnit</span>.instantiate(with: description) &#123; audioUnit, error <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">guard</span> <span class="keyword">let</span> au <span class="operator">=</span> audioUnit <span class="keyword">else</span> &#123; <span class="keyword">return</span> &#125;</span><br><span class="line">    <span class="comment">// Configure parameters via au.parameterTree</span></span><br><span class="line">    <span class="comment">// Connect to engine via AVAudioUnit(audioUnit: au)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Key concepts</strong>:</p>
<ul>
<li><strong>Component Description</strong>: Type + subtype + manufacturer identifies the AU</li>
<li><strong>Parameter Tree</strong>: <code>AUParameterTree</code> containing <code>AUParameterGroup</code>s and <code>AUParameter</code> leaves. Each parameter has address, value, range, unit, and name</li>
<li><strong>Render Block</strong>: <code>AURenderBlock</code> &#x2F; <code>AUInternalRenderBlock</code> – the function called on the real-time thread to process audio</li>
<li><strong>Buses</strong>: Input and output <code>AUAudioUnitBus</code> arrays with format negotiation</li>
<li><strong>Presets</strong>: Factory (read-only, index &gt;&#x3D; 0) and user (saved, index &lt; 0, iOS 13+)</li>
<li><strong>Latency &#x2F; Tail Time</strong>: AU reports its processing delay and how long output continues after input stops</li>
</ul>
<h3 id="Built-in-Audio-Unit-Types"><a href="#Built-in-Audio-Unit-Types" class="headerlink" title="Built-in Audio Unit Types"></a>Built-in Audio Unit Types</h3><p>Apple provides dozens of built-in Audio Units. Here is the complete enumeration by category:</p>
<h4 id="Output-Units"><a href="#Output-Units" class="headerlink" title="Output Units"></a>Output Units</h4><table>
<thead>
<tr>
<th>Subtype</th>
<th>Constant</th>
<th>Platform</th>
<th>Purpose</th>
</tr>
</thead>
<tbody><tr>
<td>Remote I&#x2F;O</td>
<td><code>kAudioUnitSubType_RemoteIO</code></td>
<td>iOS</td>
<td>Hardware I&#x2F;O (microphone + speaker)</td>
</tr>
<tr>
<td>Voice Processing I&#x2F;O</td>
<td><code>kAudioUnitSubType_VoiceProcessingIO</code></td>
<td>All</td>
<td>Echo cancellation + noise suppression</td>
</tr>
<tr>
<td>Generic Output</td>
<td><code>kAudioUnitSubType_GenericOutput</code></td>
<td>All</td>
<td>Offline rendering (no hardware)</td>
</tr>
<tr>
<td>HAL Output</td>
<td><code>kAudioUnitSubType_HALOutput</code></td>
<td>macOS</td>
<td>Direct hardware access</td>
</tr>
<tr>
<td>Default Output</td>
<td><code>kAudioUnitSubType_DefaultOutput</code></td>
<td>macOS</td>
<td>System default device</td>
</tr>
<tr>
<td>System Output</td>
<td><code>kAudioUnitSubType_SystemOutput</code></td>
<td>macOS</td>
<td>System alert device</td>
</tr>
</tbody></table>
<h4 id="Effects-Cross-Platform"><a href="#Effects-Cross-Platform" class="headerlink" title="Effects (Cross-Platform)"></a>Effects (Cross-Platform)</h4><table>
<thead>
<tr>
<th>Subtype</th>
<th>Constant</th>
<th>What it does</th>
</tr>
</thead>
<tbody><tr>
<td>N-Band EQ</td>
<td><code>kAudioUnitSubType_NBandEQ</code></td>
<td>Multi-band parametric equalizer</td>
</tr>
<tr>
<td>Dynamics Processor</td>
<td><code>kAudioUnitSubType_DynamicsProcessor</code></td>
<td>Compressor + expander + limiter</td>
</tr>
<tr>
<td>Peak Limiter</td>
<td><code>kAudioUnitSubType_PeakLimiter</code></td>
<td>Brickwall peak limiter</td>
</tr>
<tr>
<td>Distortion</td>
<td><code>kAudioUnitSubType_Distortion</code></td>
<td>25+ distortion presets</td>
</tr>
<tr>
<td>Delay</td>
<td><code>kAudioUnitSubType_Delay</code></td>
<td>Time-domain delay</td>
</tr>
<tr>
<td>Reverb 2</td>
<td><code>kAudioUnitSubType_Reverb2</code></td>
<td>Algorithmic reverb</td>
</tr>
<tr>
<td>Low Pass Filter</td>
<td><code>kAudioUnitSubType_LowPassFilter</code></td>
<td>Butterworth low pass</td>
</tr>
<tr>
<td>High Pass Filter</td>
<td><code>kAudioUnitSubType_HighPassFilter</code></td>
<td>Butterworth high pass</td>
</tr>
<tr>
<td>Band Pass Filter</td>
<td><code>kAudioUnitSubType_BandPassFilter</code></td>
<td>Band pass filter</td>
</tr>
<tr>
<td>High Shelf Filter</td>
<td><code>kAudioUnitSubType_HighShelfFilter</code></td>
<td>High shelf EQ</td>
</tr>
<tr>
<td>Low Shelf Filter</td>
<td><code>kAudioUnitSubType_LowShelfFilter</code></td>
<td>Low shelf EQ</td>
</tr>
<tr>
<td>Parametric EQ</td>
<td><code>kAudioUnitSubType_ParametricEQ</code></td>
<td>Single-band parametric EQ</td>
</tr>
</tbody></table>
<h4 id="macOS-Only-Effects"><a href="#macOS-Only-Effects" class="headerlink" title="macOS-Only Effects"></a>macOS-Only Effects</h4><table>
<thead>
<tr>
<th>Subtype</th>
<th>Constant</th>
</tr>
</thead>
<tbody><tr>
<td>Sample Delay</td>
<td><code>kAudioUnitSubType_SampleDelay</code></td>
</tr>
<tr>
<td>Graphic EQ</td>
<td><code>kAudioUnitSubType_GraphicEQ</code></td>
</tr>
<tr>
<td>Multi-Band Compressor</td>
<td><code>kAudioUnitSubType_MultiBandCompressor</code></td>
</tr>
<tr>
<td>Matrix Reverb</td>
<td><code>kAudioUnitSubType_MatrixReverb</code></td>
</tr>
<tr>
<td>Pitch</td>
<td><code>kAudioUnitSubType_Pitch</code></td>
</tr>
<tr>
<td>Filter</td>
<td><code>kAudioUnitSubType_AUFilter</code></td>
</tr>
<tr>
<td>Net Send &#x2F; Net Receive</td>
<td><code>kAudioUnitSubType_NetSend</code> &#x2F; <code>NetReceive</code></td>
</tr>
<tr>
<td>Roger Beep</td>
<td><code>kAudioUnitSubType_RogerBeep</code></td>
</tr>
</tbody></table>
<h4 id="Format-Converters"><a href="#Format-Converters" class="headerlink" title="Format Converters"></a>Format Converters</h4><table>
<thead>
<tr>
<th>Subtype</th>
<th>Constant</th>
<th>Purpose</th>
</tr>
</thead>
<tbody><tr>
<td>Converter</td>
<td><code>kAudioUnitSubType_AUConverter</code></td>
<td>Sample rate, bit depth, channel conversion</td>
</tr>
<tr>
<td>New Time Pitch</td>
<td><code>kAudioUnitSubType_NewTimePitch</code></td>
<td>Independent rate + pitch shift</td>
</tr>
<tr>
<td>Time Pitch (legacy)</td>
<td><code>kAudioUnitSubType_TimePitch</code></td>
<td>Older time-pitch</td>
</tr>
<tr>
<td>Varispeed</td>
<td><code>kAudioUnitSubType_Varispeed</code></td>
<td>Rate change (pitch follows)</td>
</tr>
<tr>
<td>Splitter</td>
<td><code>kAudioUnitSubType_Splitter</code></td>
<td>One input to N outputs</td>
</tr>
<tr>
<td>Merger</td>
<td><code>kAudioUnitSubType_Merger</code></td>
<td>N inputs to one output</td>
</tr>
<tr>
<td>Deferred Renderer</td>
<td><code>kAudioUnitSubType_DeferredRenderer</code></td>
<td>Pull from non-real-time thread</td>
</tr>
</tbody></table>
<h4 id="Mixers"><a href="#Mixers" class="headerlink" title="Mixers"></a>Mixers</h4><table>
<thead>
<tr>
<th>Subtype</th>
<th>Constant</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Multi-Channel Mixer</td>
<td><code>kAudioUnitSubType_MultiChannelMixer</code></td>
<td>Most common – N inputs, 1 output</td>
</tr>
<tr>
<td>Stereo Mixer</td>
<td><code>kAudioUnitSubType_StereoMixer</code></td>
<td>Simplified stereo</td>
</tr>
<tr>
<td>3D Mixer</td>
<td><code>kAudioUnitSubType_3DMixer</code></td>
<td>Spatial audio (deprecated, use <code>AVAudioEnvironmentNode</code>)</td>
</tr>
<tr>
<td>Matrix Mixer</td>
<td><code>kAudioUnitSubType_MatrixMixer</code></td>
<td>Arbitrary NxM routing</td>
</tr>
</tbody></table>
<h4 id="Generators"><a href="#Generators" class="headerlink" title="Generators"></a>Generators</h4><table>
<thead>
<tr>
<th>Subtype</th>
<th>Constant</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Scheduled Sound Player</td>
<td><code>kAudioUnitSubType_ScheduledSoundPlayer</code></td>
<td>Schedule audio buffers at specific times</td>
</tr>
<tr>
<td>Audio File Player</td>
<td><code>kAudioUnitSubType_AudioFilePlayer</code></td>
<td>Play regions of audio files</td>
</tr>
<tr>
<td>Speech Synthesis</td>
<td><code>kAudioUnitSubType_SpeechSynthesis</code></td>
<td>macOS text-to-speech</td>
</tr>
</tbody></table>
<h4 id="Music-Devices-Instruments"><a href="#Music-Devices-Instruments" class="headerlink" title="Music Devices &#x2F; Instruments"></a>Music Devices &#x2F; Instruments</h4><table>
<thead>
<tr>
<th>Subtype</th>
<th>Constant</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>DLS Synth</td>
<td><code>kAudioUnitSubType_DLSSynth</code></td>
<td>macOS DLS&#x2F;SoundFont player</td>
</tr>
<tr>
<td>MIDI Synth</td>
<td><code>kAudioUnitSubType_MIDISynth</code></td>
<td>General MIDI synth</td>
</tr>
<tr>
<td>Sampler</td>
<td><code>kAudioUnitSubType_Sampler</code></td>
<td>SoundFont&#x2F;DLS&#x2F;EXS24&#x2F;Aupreset player</td>
</tr>
</tbody></table>
<h3 id="AVAudioEngine-Node-to-Audio-Unit-Mapping"><a href="#AVAudioEngine-Node-to-Audio-Unit-Mapping" class="headerlink" title="AVAudioEngine Node to Audio Unit Mapping"></a>AVAudioEngine Node to Audio Unit Mapping</h3><p>Every <code>AVAudioUnit*</code> convenience class wraps a specific Audio Unit subtype:</p>
<table>
<thead>
<tr>
<th>AVAudioEngine Class</th>
<th>Audio Unit Subtype</th>
<th>Category</th>
</tr>
</thead>
<tbody><tr>
<td><code>AVAudioUnitEQ</code></td>
<td><code>kAudioUnitSubType_NBandEQ</code></td>
<td>Effect</td>
</tr>
<tr>
<td><code>AVAudioUnitReverb</code></td>
<td><code>kAudioUnitSubType_Reverb2</code></td>
<td>Effect</td>
</tr>
<tr>
<td><code>AVAudioUnitDelay</code></td>
<td><code>kAudioUnitSubType_Delay</code></td>
<td>Effect</td>
</tr>
<tr>
<td><code>AVAudioUnitDistortion</code></td>
<td><code>kAudioUnitSubType_Distortion</code></td>
<td>Effect</td>
</tr>
<tr>
<td><code>AVAudioUnitTimePitch</code></td>
<td><code>kAudioUnitSubType_NewTimePitch</code></td>
<td>Converter</td>
</tr>
<tr>
<td><code>AVAudioUnitVarispeed</code></td>
<td><code>kAudioUnitSubType_Varispeed</code></td>
<td>Converter</td>
</tr>
<tr>
<td><code>AVAudioUnitSampler</code></td>
<td><code>kAudioUnitSubType_Sampler</code></td>
<td>Instrument</td>
</tr>
<tr>
<td><code>AVAudioPlayerNode</code></td>
<td><code>kAudioUnitSubType_ScheduledSoundPlayer</code></td>
<td>Generator</td>
</tr>
<tr>
<td><code>AVAudioMixerNode</code></td>
<td><code>kAudioUnitSubType_MultiChannelMixer</code></td>
<td>Mixer</td>
</tr>
<tr>
<td><code>AVAudioEnvironmentNode</code></td>
<td><code>kAudioUnitSubType_SpatialMixer</code></td>
<td>Mixer</td>
</tr>
<tr>
<td><code>AVAudioInputNode</code></td>
<td><code>kAudioUnitSubType_RemoteIO</code> (iOS)</td>
<td>I&#x2F;O</td>
</tr>
<tr>
<td><code>AVAudioOutputNode</code></td>
<td><code>kAudioUnitSubType_RemoteIO</code> (iOS)</td>
<td>I&#x2F;O</td>
</tr>
</tbody></table>
<h3 id="AUv3-Extensions"><a href="#AUv3-Extensions" class="headerlink" title="AUv3 Extensions"></a>AUv3 Extensions</h3><p>AUv3 (Audio Unit version 3) is the modern plug-in format. Audio Units can be distributed as App Extensions, allowing third-party effects and instruments to be used inside any host app (like GarageBand):</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Discover third-party AUv3 extensions</span></span><br><span class="line"><span class="keyword">let</span> description <span class="operator">=</span> <span class="type">AudioComponentDescription</span>(</span><br><span class="line">    componentType: kAudioUnitType_Effect,</span><br><span class="line">    componentSubType: <span class="number">0</span>, componentManufacturer: <span class="number">0</span>,</span><br><span class="line">    componentFlags: <span class="number">0</span>, componentFlagsMask: <span class="number">0</span></span><br><span class="line">)</span><br><span class="line"><span class="type">AVAudioUnitComponentManager</span>.shared().components(matching: description)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Instantiate and host a third-party AU</span></span><br><span class="line"><span class="type">AVAudioUnit</span>.instantiate(with: componentDescription) &#123; avAudioUnit, error <span class="keyword">in</span></span><br><span class="line">    engine.attach(avAudioUnit<span class="operator">!</span>)</span><br><span class="line">    engine.connect(playerNode, to: avAudioUnit<span class="operator">!</span>, format: format)</span><br><span class="line">    engine.connect(avAudioUnit<span class="operator">!</span>, to: engine.mainMixerNode, format: format)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>AUv3 vs AUv2 comparison</strong>:</p>
<table>
<thead>
<tr>
<th>Aspect</th>
<th>AUv2 (Legacy)</th>
<th>AUv3 (Modern)</th>
</tr>
</thead>
<tbody><tr>
<td>API</td>
<td>C functions + property listeners</td>
<td>ObjC&#x2F;Swift class (<code>AUAudioUnit</code>)</td>
</tr>
<tr>
<td>Distribution</td>
<td>Component bundles (macOS)</td>
<td>App Extensions (all platforms)</td>
</tr>
<tr>
<td>Sandboxing</td>
<td>None</td>
<td>App sandbox (secure)</td>
</tr>
<tr>
<td>UI</td>
<td>Carbon&#x2F;Cocoa views</td>
<td>UIKit&#x2F;SwiftUI views</td>
</tr>
<tr>
<td>iOS support</td>
<td>Built-in only (no third-party plug-in distribution)</td>
<td>Full (including third-party App Extensions)</td>
</tr>
<tr>
<td>Inter-process</td>
<td>In-process only</td>
<td>In-process or out-of-process</td>
</tr>
</tbody></table>
<hr>
<h2 id="AudioToolbox"><a href="#AudioToolbox" class="headerlink" title="AudioToolbox"></a>AudioToolbox</h2><p>AudioToolbox is Apple’s mid-level C framework for audio file I&#x2F;O, format conversion, stream parsing, queue-based playback&#x2F;recording, and MIDI. It builds on Audio Units and Core Audio types, providing higher-level services below AVFAudio.</p>
<h3 id="Audio-Queue-Services"><a href="#Audio-Queue-Services" class="headerlink" title="Audio Queue Services"></a>Audio Queue Services</h3><p>Buffer-callback model for playback and recording. You allocate buffers, fill them with audio data, and enqueue them. The system calls you back when it needs more data (playback) or has captured data (recording).</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Create output queue for playback</span></span><br><span class="line">AudioQueueNewOutput(</span><br><span class="line">    &amp;dataFormat,          <span class="comment">// AudioStreamBasicDescription</span></span><br><span class="line">    outputCallback,       <span class="comment">// Called when buffer needs refilling</span></span><br><span class="line">    userData,             <span class="comment">// Context pointer</span></span><br><span class="line">    <span class="literal">NULL</span>, <span class="literal">NULL</span>, <span class="number">0</span>,</span><br><span class="line">    &amp;audioQueue           <span class="comment">// Output: the queue</span></span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Allocate and enqueue buffers</span></span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">    AudioQueueAllocateBuffer(audioQueue, bufferSize, &amp;buffers[i]);</span><br><span class="line">    outputCallback(userData, audioQueue, buffers[i]);  <span class="comment">// Pre-fill</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">AudioQueueStart(audioQueue, <span class="literal">NULL</span>);  <span class="comment">// Begin playback</span></span><br></pre></td></tr></table></figure>

<p><strong>Key properties</strong>:</p>
<ul>
<li><code>kAudioQueueParam_Volume</code> – 0.0 to 1.0</li>
<li><code>kAudioQueueParam_PlayRate</code> – playback speed (macOS only)</li>
<li><code>kAudioQueueProperty_EnableLevelMetering</code> – audio level metering</li>
</ul>
<p><strong>When to use</strong>: When you need low-level control over buffering but don’t want to deal with Audio Units directly. Useful for custom streaming implementations where AVPlayer’s buffering strategy doesn’t fit.</p>
<h3 id="Audio-File-Services"><a href="#Audio-File-Services" class="headerlink" title="Audio File Services"></a>Audio File Services</h3><p>Low-level file reading and writing:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">AudioFileOpenURL(fileURL, kAudioFileReadPermission, <span class="number">0</span>, &amp;audioFile);</span><br><span class="line">AudioFileReadPacketData(audioFile, <span class="literal">false</span>, &amp;numBytes, <span class="literal">NULL</span>, startPacket, &amp;numPackets, buffer);</span><br><span class="line">AudioFileClose(audioFile);</span><br></pre></td></tr></table></figure>

<p><strong>Supported file types</strong>:</p>
<table>
<thead>
<tr>
<th>Constant</th>
<th>Format</th>
</tr>
</thead>
<tbody><tr>
<td><code>kAudioFileAIFFType</code></td>
<td>AIFF</td>
</tr>
<tr>
<td><code>kAudioFileWAVEType</code></td>
<td>WAV</td>
</tr>
<tr>
<td><code>kAudioFileCAFType</code></td>
<td>Core Audio Format</td>
</tr>
<tr>
<td><code>kAudioFileMP3Type</code></td>
<td>MP3</td>
</tr>
<tr>
<td><code>kAudioFileM4AType</code></td>
<td>M4A (AAC)</td>
</tr>
<tr>
<td><code>kAudioFileFLACType</code></td>
<td>FLAC</td>
</tr>
<tr>
<td><code>kAudioFileAAC_ADTSType</code></td>
<td>AAC ADTS (raw AAC stream)</td>
</tr>
</tbody></table>
<h3 id="Extended-Audio-File-Services"><a href="#Extended-Audio-File-Services" class="headerlink" title="Extended Audio File Services"></a>Extended Audio File Services</h3><p>Higher-level file API that handles format conversion automatically:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Open any audio file</span></span><br><span class="line">ExtAudioFileOpenURL(fileURL, &amp;extAudioFile);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Set output format to PCM (auto-converts from MP3/AAC/FLAC/etc.)</span></span><br><span class="line">ExtAudioFileSetProperty(</span><br><span class="line">    extAudioFile,</span><br><span class="line">    kExtAudioFileProperty_ClientDataFormat,</span><br><span class="line">    <span class="keyword">sizeof</span>(pcmFormat),</span><br><span class="line">    &amp;pcmFormat</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Read PCM samples (auto-decoded from source format)</span></span><br><span class="line">ExtAudioFileRead(extAudioFile, &amp;frameCount, &amp;bufferList);</span><br></pre></td></tr></table></figure>

<p>This is the workhorse for format conversion – open any supported format, set a PCM client format, and read decoded samples. In Swift, <code>AVAudioFile</code> provides the same functionality with a nicer API.</p>
<h3 id="Audio-Converter-Services"><a href="#Audio-Converter-Services" class="headerlink" title="Audio Converter Services"></a>Audio Converter Services</h3><p>Direct codec conversion:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Create converter (e.g., PCM to AAC)</span></span><br><span class="line">AudioConverterNew(&amp;sourceFormat, &amp;destinationFormat, &amp;converter);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Convert with callback</span></span><br><span class="line">AudioConverterFillComplexBuffer(</span><br><span class="line">    converter,</span><br><span class="line">    inputDataProc,     <span class="comment">// Called to supply input data</span></span><br><span class="line">    userData,</span><br><span class="line">    &amp;outputFrameCount,</span><br><span class="line">    &amp;outputBufferList,</span><br><span class="line">    outputPacketDescriptions</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p><strong>Key capabilities</strong>:</p>
<ul>
<li>Sample rate conversion (e.g., 44100 Hz to 48000 Hz)</li>
<li>Bit depth conversion (16-bit to 32-bit float)</li>
<li>Channel count conversion (mono to stereo, downmix)</li>
<li>Codec encode&#x2F;decode (PCM to AAC, AAC to PCM)</li>
<li>Hardware-accelerated encoding on devices with media engines</li>
</ul>
<h3 id="Audio-File-Stream-Services"><a href="#Audio-File-Stream-Services" class="headerlink" title="Audio File Stream Services"></a>Audio File Stream Services</h3><p>Progressive parsing of streamed audio – essential for building custom streaming players:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Open stream parser</span></span><br><span class="line">AudioFileStreamOpen(</span><br><span class="line">    userData,</span><br><span class="line">    propertyListenerCallback,   <span class="comment">// Called when stream properties are discovered</span></span><br><span class="line">    packetsCallback,            <span class="comment">// Called when audio packets are parsed</span></span><br><span class="line">    kAudioFileMP3Type,          <span class="comment">// Hint for file type</span></span><br><span class="line">    &amp;streamID</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Feed data as it arrives from the network</span></span><br><span class="line">AudioFileStreamParseBytes(streamID, dataLength, data, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>The parser discovers format info (sample rate, channels, codec) from the stream header, then delivers decoded packets as they’re parsed. This is what powers custom audio streaming implementations that go beyond what AVPlayer provides.</p>
<h3 id="System-Sound-Services"><a href="#System-Sound-Services" class="headerlink" title="System Sound Services"></a>System Sound Services</h3><p>Short sounds, alerts, and haptics:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Play system sound (&lt; 30 seconds, no volume control)</span></span><br><span class="line"><span class="keyword">var</span> soundID: <span class="type">SystemSoundID</span> <span class="operator">=</span> <span class="number">0</span></span><br><span class="line"><span class="type">AudioServicesCreateSystemSoundID</span>(soundURL <span class="keyword">as</span> <span class="type">CFURL</span>, <span class="operator">&amp;</span>soundID)</span><br><span class="line"><span class="type">AudioServicesPlaySystemSound</span>(soundID)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Play alert sound (respects silent switch, may vibrate)</span></span><br><span class="line"><span class="type">AudioServicesPlayAlertSound</span>(soundID)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Vibrate</span></span><br><span class="line"><span class="type">AudioServicesPlaySystemSound</span>(kSystemSoundID_Vibrate)</span><br></pre></td></tr></table></figure>

<p>Best for UI feedback sounds. For anything more complex (volume control, effects, long audio), use AVAudioPlayer or AVAudioEngine instead.</p>
<h3 id="Music-Player-Music-Sequence-MIDI"><a href="#Music-Player-Music-Sequence-MIDI" class="headerlink" title="Music Player &#x2F; Music Sequence (MIDI)"></a>Music Player &#x2F; Music Sequence (MIDI)</h3><p>Programmatic MIDI playback:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">NewMusicSequence(&amp;sequence);</span><br><span class="line">MusicSequenceFileLoad(sequence, midiFileURL, kMusicSequenceFile_MIDIType, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">NewMusicPlayer(&amp;player);</span><br><span class="line">MusicPlayerSetSequence(player, sequence);</span><br><span class="line">MusicPlayerStart(player);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Tempo track</span></span><br><span class="line">MusicSequenceGetTempoTrack(sequence, &amp;tempoTrack);</span><br><span class="line">MusicTrackNewExtendedTempoEvent(tempoTrack, <span class="number">0</span>, <span class="number">120.0</span>);  <span class="comment">// 120 BPM</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Iterate events</span></span><br><span class="line">MusicEventIteratorNextEvent(iterator);</span><br><span class="line">MusicEventIteratorGetEventInfo(iterator, &amp;timeStamp, &amp;eventType, &amp;eventData, &amp;eventDataSize);</span><br></pre></td></tr></table></figure>

<h3 id="MTAudioProcessingTap"><a href="#MTAudioProcessingTap" class="headerlink" title="MTAudioProcessingTap"></a>MTAudioProcessingTap</h3><p>Tap into the AVPlayer audio pipeline for real-time sample access:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Create processing tap</span></span><br><span class="line"><span class="keyword">var</span> callbacks <span class="operator">=</span> <span class="type">MTAudioProcessingTapCallbacks</span>(</span><br><span class="line">    version: kMTAudioProcessingTapCallbacksVersion_0,</span><br><span class="line">    clientInfo: <span class="type">UnsafeMutableRawPointer</span>(<span class="type">Unmanaged</span>.passRetained(<span class="keyword">self</span>).toOpaque()),</span><br><span class="line">    init: tapInit,</span><br><span class="line">    finalize: tapFinalize,</span><br><span class="line">    prepare: tapPrepare,</span><br><span class="line">    unprepare: tapUnprepare,</span><br><span class="line">    process: tapProcess  <span class="comment">// This is where you access/modify audio samples</span></span><br><span class="line">)</span><br><span class="line"><span class="type">MTAudioProcessingTapCreate</span>(kCFAllocatorDefault, <span class="operator">&amp;</span>callbacks, kMTAudioProcessingTapCreationFlag_PostEffects, <span class="operator">&amp;</span>tap)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Attach to AVPlayerItem</span></span><br><span class="line"><span class="keyword">let</span> params <span class="operator">=</span> <span class="type">AVMutableAudioMixInputParameters</span>(track: audioTrack)</span><br><span class="line">params.audioTapProcessor <span class="operator">=</span> tap</span><br><span class="line"><span class="keyword">let</span> audioMix <span class="operator">=</span> <span class="type">AVMutableAudioMix</span>()</span><br><span class="line">audioMix.inputParameters <span class="operator">=</span> [params]</span><br><span class="line">playerItem.audioMix <span class="operator">=</span> audioMix</span><br></pre></td></tr></table></figure>

<p>The <code>process</code> callback receives <code>AudioBufferList</code> data on the render thread – same real-time rules apply. This is the <strong>only way</strong> to access AVPlayer’s audio samples in real-time (AVAudioEngine cannot route AVPlayer audio through its graph).</p>
<h3 id="Core-Audio-Format-CAF"><a href="#Core-Audio-Format-CAF" class="headerlink" title="Core Audio Format (CAF)"></a>Core Audio Format (CAF)</h3><p>Apple’s container format that supports every codec and has no size limits:</p>
<table>
<thead>
<tr>
<th>Feature</th>
<th>CAF</th>
<th>WAV</th>
<th>AIFF</th>
</tr>
</thead>
<tbody><tr>
<td>Max file size</td>
<td>Unlimited</td>
<td>4 GB</td>
<td>4 GB</td>
</tr>
<tr>
<td>Codec support</td>
<td>All Apple codecs</td>
<td>PCM, ADPCM</td>
<td>PCM</td>
</tr>
<tr>
<td>Metadata</td>
<td>Rich</td>
<td>RIFF chunks</td>
<td>Limited</td>
</tr>
<tr>
<td>Markers&#x2F;regions</td>
<td>Yes</td>
<td>No</td>
<td>No</td>
</tr>
</tbody></table>
<p>CAF is ideal for intermediate audio processing where you need a container that won’t limit you.</p>
<hr>
<h2 id="AVFAudio"><a href="#AVFAudio" class="headerlink" title="AVFAudio"></a>AVFAudio</h2><p>AVFAudio is the modern Swift&#x2F;Objective-C framework for audio engine, session management, playback, recording, and file I&#x2F;O.</p>
<h3 id="AVAudioEngine"><a href="#AVAudioEngine" class="headerlink" title="AVAudioEngine"></a>AVAudioEngine</h3><p>A node-based audio processing graph that wraps Audio Units in a Swift-friendly API:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> engine <span class="operator">=</span> <span class="type">AVAudioEngine</span>()</span><br><span class="line"><span class="keyword">let</span> player <span class="operator">=</span> <span class="type">AVAudioPlayerNode</span>()</span><br><span class="line"><span class="keyword">let</span> eq <span class="operator">=</span> <span class="type">AVAudioUnitEQ</span>(numberOfBands: <span class="number">3</span>)</span><br><span class="line"><span class="keyword">let</span> reverb <span class="operator">=</span> <span class="type">AVAudioUnitReverb</span>()</span><br><span class="line"></span><br><span class="line"><span class="comment">// Build graph: player → EQ → reverb → output</span></span><br><span class="line">engine.attach(player)</span><br><span class="line">engine.attach(eq)</span><br><span class="line">engine.attach(reverb)</span><br><span class="line">engine.connect(player, to: eq, format: format)</span><br><span class="line">engine.connect(eq, to: reverb, format: format)</span><br><span class="line">engine.connect(reverb, to: engine.mainMixerNode, format: format)</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span> engine.start()</span><br><span class="line"></span><br><span class="line"><span class="comment">// Schedule and play audio</span></span><br><span class="line"><span class="keyword">let</span> file <span class="operator">=</span> <span class="keyword">try</span> <span class="type">AVAudioFile</span>(forReading: audioURL)</span><br><span class="line">player.scheduleFile(file, at: <span class="literal">nil</span>)</span><br><span class="line">player.play()</span><br></pre></td></tr></table></figure>

<p><strong>Singleton nodes</strong> (always exist):</p>
<ul>
<li><code>engine.inputNode</code> – microphone</li>
<li><code>engine.outputNode</code> – speaker</li>
<li><code>engine.mainMixerNode</code> – main mixer (auto-created when first accessed)</li>
</ul>
<p><strong>Key operations</strong>:</p>
<table>
<thead>
<tr>
<th>Method</th>
<th>Purpose</th>
</tr>
</thead>
<tbody><tr>
<td><code>attach(_:)</code></td>
<td>Add node to engine</td>
</tr>
<tr>
<td><code>connect(_:to:format:)</code></td>
<td>Wire nodes together</td>
</tr>
<tr>
<td><code>start()</code></td>
<td>Begin processing</td>
</tr>
<tr>
<td><code>pause()</code></td>
<td>Pause (keep resources)</td>
</tr>
<tr>
<td><code>stop()</code></td>
<td>Stop (release resources)</td>
</tr>
<tr>
<td><code>reset()</code></td>
<td>Reset all nodes</td>
</tr>
</tbody></table>
<p><strong>Manual (offline) rendering</strong>:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> engine.enableManualRenderingMode(.offline, format: format, maximumFrameCount: <span class="number">4096</span>)</span><br><span class="line"><span class="keyword">try</span> engine.start()</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> outputBuffer <span class="operator">=</span> <span class="type">AVAudioPCMBuffer</span>(pcmFormat: format, frameCapacity: <span class="number">4096</span>)<span class="operator">!</span></span><br><span class="line"><span class="keyword">while</span> player.isPlaying &#123;</span><br><span class="line">    <span class="keyword">let</span> status <span class="operator">=</span> <span class="keyword">try</span> engine.renderOffline(<span class="number">4096</span>, to: outputBuffer)</span><br><span class="line">    <span class="comment">// Write outputBuffer to file</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>This enables processing audio without real-time constraints – perfect for batch effects, format conversion, or analysis.</p>
<p><strong>Installing taps</strong> for analysis:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> bus <span class="operator">=</span> <span class="number">0</span></span><br><span class="line"><span class="keyword">let</span> bufferSize: <span class="type">AVAudioFrameCount</span> <span class="operator">=</span> <span class="number">1024</span></span><br><span class="line">engine.mainMixerNode.installTap(onBus: bus, bufferSize: bufferSize, format: format) &#123; buffer, time <span class="keyword">in</span></span><br><span class="line">    <span class="comment">// Analyze buffer.floatChannelData for visualization, level metering, etc.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Built-in-Effect-Nodes"><a href="#Built-in-Effect-Nodes" class="headerlink" title="Built-in Effect Nodes"></a>Built-in Effect Nodes</h3><table>
<thead>
<tr>
<th>Class</th>
<th>Properties</th>
<th>Use case</th>
</tr>
</thead>
<tbody><tr>
<td><code>AVAudioUnitEQ</code></td>
<td><code>bands[]: {frequency, bandwidth, gain, filterType, bypass}</code>, <code>globalGain</code></td>
<td>Multi-band parametric equalizer</td>
</tr>
<tr>
<td><code>AVAudioUnitTimePitch</code></td>
<td><code>pitch</code> (-2400 to +2400 cents), <code>rate</code> (0.03 to 32x), <code>overlap</code></td>
<td>Independent pitch shift + tempo change</td>
</tr>
<tr>
<td><code>AVAudioUnitVarispeed</code></td>
<td><code>rate</code></td>
<td>Speed change (pitch follows, like a turntable)</td>
</tr>
<tr>
<td><code>AVAudioUnitReverb</code></td>
<td><code>wetDryMix</code> (0-100), 13 factory presets</td>
<td>Room&#x2F;hall&#x2F;chamber&#x2F;plate&#x2F;cathedral reverb</td>
</tr>
<tr>
<td><code>AVAudioUnitDelay</code></td>
<td><code>delayTime</code>, <code>feedback</code>, <code>lowPassCutoff</code>, <code>wetDryMix</code></td>
<td>Echo&#x2F;delay effect</td>
</tr>
<tr>
<td><code>AVAudioUnitDistortion</code></td>
<td>25+ presets, <code>wetDryMix</code>, <code>preGain</code></td>
<td>Overdrive, fuzz, decimation, etc.</td>
</tr>
</tbody></table>
<p><strong>EQ filter types</strong>: <code>parametric</code>, <code>lowPass</code>, <code>highPass</code>, <code>resonantLowPass</code>, <code>resonantHighPass</code>, <code>bandPass</code>, <code>bandStop</code>, <code>lowShelf</code>, <code>highShelf</code>, <code>resonantLowShelf</code>, <code>resonantHighShelf</code></p>
<p><strong>Reverb presets</strong>: <code>smallRoom</code>, <code>mediumRoom</code>, <code>largeRoom</code>, <code>largeRoom2</code>, <code>mediumHall</code>, <code>mediumHall2</code>, <code>mediumHall3</code>, <code>largeHall</code>, <code>largeHall2</code>, <code>mediumChamber</code>, <code>largeChamber</code>, <code>cathedral</code>, <code>plate</code></p>
<h3 id="AVAudioSession"><a href="#AVAudioSession" class="headerlink" title="AVAudioSession"></a>AVAudioSession</h3><p>Configures how your app interacts with the system audio:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> session <span class="operator">=</span> <span class="type">AVAudioSession</span>.sharedInstance()</span><br><span class="line"><span class="keyword">try</span> session.setCategory(.playback, mode: .spokenAudio, options: [.allowBluetooth])</span><br><span class="line"><span class="keyword">try</span> session.setActive(<span class="literal">true</span>)</span><br></pre></td></tr></table></figure>

<p><strong>Categories</strong> (what your app does with audio):</p>
<table>
<thead>
<tr>
<th>Category</th>
<th>Behavior</th>
</tr>
</thead>
<tbody><tr>
<td><code>.ambient</code></td>
<td>Mixes with other apps, silenced by switch</td>
</tr>
<tr>
<td><code>.soloAmbient</code></td>
<td>Default. Silences other apps, silenced by switch</td>
</tr>
<tr>
<td><code>.playback</code></td>
<td>Silences other apps, ignores silent switch</td>
</tr>
<tr>
<td><code>.record</code></td>
<td>Recording only</td>
</tr>
<tr>
<td><code>.playAndRecord</code></td>
<td>Simultaneous playback + recording</td>
</tr>
<tr>
<td><code>.multiRoute</code></td>
<td>Route to multiple outputs simultaneously</td>
</tr>
</tbody></table>
<p><strong>Modes</strong> (optimize for content type):</p>
<table>
<thead>
<tr>
<th>Mode</th>
<th>Optimization</th>
</tr>
</thead>
<tbody><tr>
<td><code>.default</code></td>
<td>No optimization</td>
</tr>
<tr>
<td><code>.voiceChat</code></td>
<td>Echo cancellation, AGC</td>
</tr>
<tr>
<td><code>.gameChat</code></td>
<td>Gaming voice chat</td>
</tr>
<tr>
<td><code>.videoRecording</code></td>
<td>Video recording</td>
</tr>
<tr>
<td><code>.measurement</code></td>
<td>Minimal signal processing</td>
</tr>
<tr>
<td><code>.moviePlayback</code></td>
<td>Enhanced movie audio</td>
</tr>
<tr>
<td><code>.spokenAudio</code></td>
<td>Podcasts, audiobooks (ducks properly)</td>
</tr>
<tr>
<td><code>.voicePrompt</code></td>
<td>Short voice prompts (e.g., navigation)</td>
</tr>
</tbody></table>
<p><strong>Options</strong>: <code>.mixWithOthers</code>, <code>.duckOthers</code>, <code>.allowBluetooth</code>, <code>.allowBluetoothA2DP</code>, <code>.allowAirPlay</code>, <code>.defaultToSpeaker</code></p>
<p><strong>Interruption handling</strong>:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="type">NotificationCenter</span>.default.addObserver(</span><br><span class="line">    forName: <span class="type">AVAudioSession</span>.interruptionNotification,</span><br><span class="line">    object: <span class="literal">nil</span>, queue: .main</span><br><span class="line">) &#123; notification <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">guard</span> <span class="keyword">let</span> info <span class="operator">=</span> notification.userInfo,</span><br><span class="line">          <span class="keyword">let</span> typeValue <span class="operator">=</span> info[<span class="type">AVAudioSessionInterruptionTypeKey</span>] <span class="keyword">as?</span> <span class="type">UInt</span>,</span><br><span class="line">          <span class="keyword">let</span> type <span class="operator">=</span> <span class="type">AVAudioSession</span>.<span class="type">InterruptionType</span>(rawValue: typeValue) <span class="keyword">else</span> &#123; <span class="keyword">return</span> &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">switch</span> type &#123;</span><br><span class="line">    <span class="keyword">case</span> .began:</span><br><span class="line">        <span class="comment">// Pause playback</span></span><br><span class="line">    <span class="keyword">case</span> .ended:</span><br><span class="line">        <span class="keyword">let</span> options <span class="operator">=</span> info[<span class="type">AVAudioSessionInterruptionOptionKey</span>] <span class="keyword">as?</span> <span class="type">UInt</span> <span class="operator">??</span> <span class="number">0</span></span><br><span class="line">        <span class="keyword">if</span> <span class="type">AVAudioSession</span>.<span class="type">InterruptionOptions</span>(rawValue: options).contains(.shouldResume) &#123;</span><br><span class="line">            <span class="comment">// Resume playback</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="AVAudioPlayer"><a href="#AVAudioPlayer" class="headerlink" title="AVAudioPlayer"></a>AVAudioPlayer</h3><p>Simple local file playback with no streaming support:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> player <span class="operator">=</span> <span class="keyword">try</span> <span class="type">AVAudioPlayer</span>(contentsOf: fileURL)</span><br><span class="line">player.enableRate <span class="operator">=</span> <span class="literal">true</span>         <span class="comment">// Must set before play</span></span><br><span class="line">player.rate <span class="operator">=</span> <span class="number">1.5</span>                <span class="comment">// 0.5x to 2.0x</span></span><br><span class="line">player.volume <span class="operator">=</span> <span class="number">0.8</span>              <span class="comment">// 0.0 to 1.0</span></span><br><span class="line">player.numberOfLoops <span class="operator">=</span> <span class="operator">-</span><span class="number">1</span>        <span class="comment">// -1 = infinite, 0 = play once, N = play N+1 times</span></span><br><span class="line">player.pan <span class="operator">=</span> <span class="number">0.0</span>                 <span class="comment">// -1.0 (left) to 1.0 (right)</span></span><br><span class="line"></span><br><span class="line">player.prepareToPlay()           <span class="comment">// Pre-buffer for low-latency start</span></span><br><span class="line">player.play()</span><br><span class="line"></span><br><span class="line"><span class="comment">// Volume fade</span></span><br><span class="line">player.setVolume(<span class="number">0.0</span>, fadeDuration: <span class="number">2.0</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Metering</span></span><br><span class="line">player.isMeteringEnabled <span class="operator">=</span> <span class="literal">true</span></span><br><span class="line">player.updateMeters()</span><br><span class="line"><span class="keyword">let</span> avgPower <span class="operator">=</span> player.averagePower(forChannel: <span class="number">0</span>)  <span class="comment">// dBFS</span></span><br><span class="line"><span class="keyword">let</span> peakPower <span class="operator">=</span> player.peakPower(forChannel: <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Seek</span></span><br><span class="line">player.currentTime <span class="operator">=</span> <span class="number">30.0</span>       <span class="comment">// Jump to 30 seconds</span></span><br></pre></td></tr></table></figure>

<p><strong>Supported formats</strong>: AAC, ALAC, HE-AAC, MP3, AIFF, WAV, CAF, and anything Core Audio can decode.</p>
<p><strong>Limitations</strong>: Local files or in-memory <code>Data</code> only. No network streaming, no real-time effects, no time observation callbacks.</p>
<h3 id="AVAudioRecorder"><a href="#AVAudioRecorder" class="headerlink" title="AVAudioRecorder"></a>AVAudioRecorder</h3><p>Record audio to a file:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> settings: [<span class="type">String</span>: <span class="keyword">Any</span>] <span class="operator">=</span> [</span><br><span class="line">    <span class="type">AVFormatIDKey</span>: kAudioFormatMPEG4AAC,</span><br><span class="line">    <span class="type">AVSampleRateKey</span>: <span class="number">44100.0</span>,</span><br><span class="line">    <span class="type">AVNumberOfChannelsKey</span>: <span class="number">2</span>,</span><br><span class="line">    <span class="type">AVEncoderAudioQualityKey</span>: <span class="type">AVAudioQuality</span>.high.rawValue,</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> recorder <span class="operator">=</span> <span class="keyword">try</span> <span class="type">AVAudioRecorder</span>(url: outputURL, settings: settings)</span><br><span class="line">recorder.isMeteringEnabled <span class="operator">=</span> <span class="literal">true</span></span><br><span class="line">recorder.prepareToRecord()</span><br><span class="line">recorder.record()</span><br><span class="line"></span><br><span class="line"><span class="comment">// During recording</span></span><br><span class="line">recorder.updateMeters()</span><br><span class="line"><span class="keyword">let</span> level <span class="operator">=</span> recorder.averagePower(forChannel: <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">recorder.stop()</span><br></pre></td></tr></table></figure>

<h3 id="AVAudioFile-and-AVAudioPCMBuffer"><a href="#AVAudioFile-and-AVAudioPCMBuffer" class="headerlink" title="AVAudioFile and AVAudioPCMBuffer"></a>AVAudioFile and AVAudioPCMBuffer</h3><p>Read and write audio files with automatic format conversion:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Read any audio file into PCM buffers</span></span><br><span class="line"><span class="keyword">let</span> file <span class="operator">=</span> <span class="keyword">try</span> <span class="type">AVAudioFile</span>(forReading: url)</span><br><span class="line"><span class="keyword">let</span> format <span class="operator">=</span> file.processingFormat        <span class="comment">// In-memory PCM format</span></span><br><span class="line"><span class="keyword">let</span> length <span class="operator">=</span> <span class="type">AVAudioFrameCount</span>(file.length)</span><br><span class="line"><span class="keyword">let</span> buffer <span class="operator">=</span> <span class="type">AVAudioPCMBuffer</span>(pcmFormat: format, frameCapacity: length)<span class="operator">!</span></span><br><span class="line"><span class="keyword">try</span> file.read(into: buffer)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Access raw samples</span></span><br><span class="line"><span class="keyword">let</span> samples <span class="operator">=</span> buffer.floatChannelData<span class="operator">!</span>[<span class="number">0</span>]  <span class="comment">// Left channel</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="number">0</span><span class="operator">..&lt;</span><span class="type">Int</span>(buffer.frameLength) &#123;</span><br><span class="line">    <span class="keyword">let</span> sample <span class="operator">=</span> samples[i]  <span class="comment">// Float, -1.0 to 1.0</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Write PCM to file</span></span><br><span class="line"><span class="keyword">let</span> outputFile <span class="operator">=</span> <span class="keyword">try</span> <span class="type">AVAudioFile</span>(</span><br><span class="line">    forWriting: outputURL,</span><br><span class="line">    settings: [</span><br><span class="line">        AVFormatIDKey: kAudioFormatMPEG4AAC,</span><br><span class="line">        AVSampleRateKey: <span class="number">44100.0</span>,</span><br><span class="line">        AVNumberOfChannelsKey: <span class="number">2</span>,</span><br><span class="line">    ]</span><br><span class="line">)</span><br><span class="line"><span class="keyword">try</span> outputFile.write(from: buffer)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Random access</span></span><br><span class="line">file.framePosition <span class="operator">=</span> <span class="number">44100</span> <span class="operator">*</span> <span class="number">10</span>  <span class="comment">// Seek to 10 seconds</span></span><br><span class="line"><span class="keyword">try</span> file.read(into: buffer, frameCount: <span class="number">44100</span>)  <span class="comment">// Read 1 second</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="AVFoundation-Audio"><a href="#AVFoundation-Audio" class="headerlink" title="AVFoundation (Audio)"></a>AVFoundation (Audio)</h2><p>AVFoundation’s audio capabilities focus on media playback, editing, and export.</p>
<h3 id="iOS-26-additions"><a href="#iOS-26-additions" class="headerlink" title="iOS 26 additions"></a>iOS 26 additions</h3><p>Three things the post-2025 reader should know:</p>
<ul>
<li><strong>Spatial Audio recording.</strong> <code>AVAssetWriter</code> can now record Spatial Audio directly. Audio-only apps can save to the new QuickTime audio format <code>.qta</code>, which carries multiple alternate-track groups (the same shape Spatial Audio files already use).</li>
<li><strong>Input list &#x2F; source switching in AVKit.</strong> New AVKit API surfaces the system’s available audio inputs and lets the user switch sources from inside your app, replacing the manual <code>AVAudioSession.availableInputs</code> + custom-picker dance.</li>
<li><strong>Personalized Spatial Audio profile.</strong> The <code>com.apple.developer.spatial-audio.profile-access</code> entitlement lets your app render against the user’s personalized HRTF (set up in Settings via the TrueDepth camera). PHASE remains the framework for geometry-aware spatialization; the entitlement is the missing piece that lets it sound personalized.</li>
</ul>
<h3 id="AVPlayer"><a href="#AVPlayer" class="headerlink" title="AVPlayer"></a>AVPlayer</h3><p>The primary player for URL-based audio and video:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> player <span class="operator">=</span> <span class="type">AVPlayer</span>(url: streamURL)</span><br><span class="line">player.play()</span><br><span class="line"></span><br><span class="line"><span class="comment">// Rate control</span></span><br><span class="line">player.rate <span class="operator">=</span> <span class="number">1.5</span>                  <span class="comment">// Any rate (0.0 = paused)</span></span><br><span class="line">player.defaultRate <span class="operator">=</span> <span class="number">1.0</span>           <span class="comment">// Rate used by play()</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Volume</span></span><br><span class="line">player.volume <span class="operator">=</span> <span class="number">0.8</span></span><br><span class="line">player.isMuted <span class="operator">=</span> <span class="literal">false</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Seeking</span></span><br><span class="line">player.seek(to: <span class="type">CMTime</span>(seconds: <span class="number">30</span>, preferredTimescale: <span class="number">600</span>))</span><br><span class="line">player.seek(to: time, toleranceBefore: .zero, toleranceAfter: .zero)  <span class="comment">// Exact seek</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Buffer control</span></span><br><span class="line">player.automaticallyWaitsToMinimizeStalling <span class="operator">=</span> <span class="literal">true</span>  <span class="comment">// Default</span></span><br></pre></td></tr></table></figure>

<p><strong>Time observation</strong>:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Periodic -- fires at regular intervals</span></span><br><span class="line"><span class="keyword">let</span> observer <span class="operator">=</span> player.addPeriodicTimeObserver(</span><br><span class="line">    forInterval: <span class="type">CMTime</span>(seconds: <span class="number">0.5</span>, preferredTimescale: <span class="number">600</span>),</span><br><span class="line">    queue: .main</span><br><span class="line">) &#123; time <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">let</span> seconds <span class="operator">=</span> <span class="type">CMTimeGetSeconds</span>(time)</span><br><span class="line">    <span class="comment">// Update progress UI</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Boundary -- fires when specific times are crossed</span></span><br><span class="line"><span class="keyword">let</span> times <span class="operator">=</span> [<span class="type">CMTime</span>(seconds: <span class="number">30</span>, preferredTimescale: <span class="number">600</span>)]</span><br><span class="line"><span class="keyword">let</span> observer <span class="operator">=</span> player.addBoundaryTimeObserver(forTimes: times.map &#123; <span class="type">NSValue</span>(time: <span class="variable">$0</span>) &#125;, queue: .main) &#123;</span><br><span class="line">    <span class="comment">// Reached the 30-second mark</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Remove when done</span></span><br><span class="line">player.removeTimeObserver(observer)</span><br></pre></td></tr></table></figure>

<p><strong>Buffer monitoring</strong> (via <code>AVPlayerItem</code>):</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> item <span class="operator">=</span> player.currentItem<span class="operator">!</span></span><br><span class="line">item.loadedTimeRanges          <span class="comment">// [NSValue(CMTimeRange)] -- what&#x27;s buffered</span></span><br><span class="line">item.isPlaybackBufferEmpty     <span class="comment">// Buffer ran out</span></span><br><span class="line">item.isPlaybackLikelyToKeepUp <span class="comment">// Enough buffer to play smoothly</span></span><br></pre></td></tr></table></figure>

<p><strong>Status observation</strong> (KVO):</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">player.currentItem<span class="operator">?</span>.observe(\.status) &#123; item, <span class="keyword">_</span> <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">switch</span> item.status &#123;</span><br><span class="line">    <span class="keyword">case</span> .readyToPlay: <span class="comment">// Ready</span></span><br><span class="line">    <span class="keyword">case</span> .failed:      <span class="comment">// Check item.error</span></span><br><span class="line">    <span class="keyword">case</span> .unknown:     <span class="comment">// Loading</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="AVQueuePlayer"><a href="#AVQueuePlayer" class="headerlink" title="AVQueuePlayer"></a>AVQueuePlayer</h3><p>Sequential playback of multiple items:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> items <span class="operator">=</span> urls.map &#123; <span class="type">AVPlayerItem</span>(url: <span class="variable">$0</span>) &#125;</span><br><span class="line"><span class="keyword">let</span> queuePlayer <span class="operator">=</span> <span class="type">AVQueuePlayer</span>(items: items)</span><br><span class="line">queuePlayer.play()</span><br><span class="line">queuePlayer.advanceToNextItem()</span><br><span class="line"></span><br><span class="line"><span class="comment">// Add more items</span></span><br><span class="line">queuePlayer.insert(newItem, after: currentItem)</span><br><span class="line">queuePlayer.remove(item)</span><br></pre></td></tr></table></figure>

<h3 id="Audio-Editing-with-AVMutableComposition"><a href="#Audio-Editing-with-AVMutableComposition" class="headerlink" title="Audio Editing with AVMutableComposition"></a>Audio Editing with AVMutableComposition</h3><p>Non-destructive editing – trim, split, merge without re-encoding:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Trim</span></span><br><span class="line"><span class="keyword">let</span> asset <span class="operator">=</span> <span class="type">AVURLAsset</span>(url: audioURL)</span><br><span class="line"><span class="keyword">let</span> composition <span class="operator">=</span> <span class="type">AVMutableComposition</span>()</span><br><span class="line"><span class="keyword">let</span> track <span class="operator">=</span> composition.addMutableTrack(withMediaType: .audio, preferredTrackID: kCMPersistentTrackID_Invalid)<span class="operator">!</span></span><br><span class="line"><span class="keyword">let</span> sourceTrack <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> asset.loadTracks(withMediaType: .audio).first<span class="operator">!</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> startTime <span class="operator">=</span> <span class="type">CMTime</span>(seconds: <span class="number">10</span>, preferredTimescale: <span class="number">600</span>)</span><br><span class="line"><span class="keyword">let</span> duration <span class="operator">=</span> <span class="type">CMTime</span>(seconds: <span class="number">30</span>, preferredTimescale: <span class="number">600</span>)</span><br><span class="line"><span class="keyword">let</span> range <span class="operator">=</span> <span class="type">CMTimeRange</span>(start: startTime, duration: duration)</span><br><span class="line"><span class="keyword">try</span> track.insertTimeRange(range, of: sourceTrack, at: .zero)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Merge multiple files</span></span><br><span class="line"><span class="keyword">for</span> (index, url) <span class="keyword">in</span> urls.enumerated() &#123;</span><br><span class="line">    <span class="keyword">let</span> asset <span class="operator">=</span> <span class="type">AVURLAsset</span>(url: url)</span><br><span class="line">    <span class="keyword">let</span> sourceTrack <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> asset.loadTracks(withMediaType: .audio).first<span class="operator">!</span></span><br><span class="line">    <span class="keyword">let</span> duration <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> asset.load(.duration)</span><br><span class="line">    <span class="keyword">try</span> track.insertTimeRange(<span class="type">CMTimeRange</span>(start: .zero, duration: duration), of: sourceTrack, at: currentTime)</span><br><span class="line">    currentTime <span class="operator">=</span> <span class="type">CMTimeAdd</span>(currentTime, duration)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Export</span></span><br><span class="line"><span class="keyword">let</span> exporter <span class="operator">=</span> <span class="type">AVAssetExportSession</span>(asset: composition, presetName: <span class="type">AVAssetExportPresetAppleM4A</span>)<span class="operator">!</span></span><br><span class="line">exporter.outputURL <span class="operator">=</span> outputURL</span><br><span class="line">exporter.outputFileType <span class="operator">=</span> .m4a</span><br><span class="line">exporter.timeRange <span class="operator">=</span> range  <span class="comment">// Optional: trim during export</span></span><br><span class="line"><span class="keyword">await</span> exporter.export()</span><br></pre></td></tr></table></figure>

<h3 id="AVAudioMix"><a href="#AVAudioMix" class="headerlink" title="AVAudioMix"></a>AVAudioMix</h3><p>Apply volume changes and processing during AVPlayer playback or export:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> params <span class="operator">=</span> <span class="type">AVMutableAudioMixInputParameters</span>(track: audioTrack)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Volume ramp</span></span><br><span class="line">params.setVolumeRamp(fromStartVolume: <span class="number">1.0</span>, toEndVolume: <span class="number">0.0</span>,</span><br><span class="line">    timeRange: <span class="type">CMTimeRange</span>(start: fadeStart, duration: fadeDuration))</span><br><span class="line"></span><br><span class="line"><span class="comment">// Set volume at a point</span></span><br><span class="line">params.setVolume(<span class="number">0.5</span>, at: <span class="type">CMTime</span>(seconds: <span class="number">30</span>, preferredTimescale: <span class="number">600</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment">// Time pitch algorithm</span></span><br><span class="line">params.audioTimePitchAlgorithm <span class="operator">=</span> .spectral  <span class="comment">// .timeDomain, .spectral, .varispeed (.lowQualityZeroLatency deprecated)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> audioMix <span class="operator">=</span> <span class="type">AVMutableAudioMix</span>()</span><br><span class="line">audioMix.inputParameters <span class="operator">=</span> [params]</span><br><span class="line"></span><br><span class="line"><span class="comment">// Apply to player or exporter</span></span><br><span class="line">playerItem.audioMix <span class="operator">=</span> audioMix</span><br><span class="line">exportSession.audioMix <span class="operator">=</span> audioMix</span><br></pre></td></tr></table></figure>

<h3 id="MPNowPlayingInfoCenter"><a href="#MPNowPlayingInfoCenter" class="headerlink" title="MPNowPlayingInfoCenter"></a>MPNowPlayingInfoCenter</h3><p>Display metadata on Lock Screen, Control Center, and CarPlay:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> info <span class="operator">=</span> [<span class="type">String</span>: <span class="keyword">Any</span>]()</span><br><span class="line">info[<span class="type">MPMediaItemPropertyTitle</span>] <span class="operator">=</span> <span class="string">&quot;Episode 42&quot;</span></span><br><span class="line">info[<span class="type">MPMediaItemPropertyArtist</span>] <span class="operator">=</span> <span class="string">&quot;My Podcast&quot;</span></span><br><span class="line">info[<span class="type">MPMediaItemPropertyPlaybackDuration</span>] <span class="operator">=</span> <span class="number">3600.0</span></span><br><span class="line">info[<span class="type">MPNowPlayingInfoPropertyElapsedPlaybackTime</span>] <span class="operator">=</span> <span class="number">120.0</span></span><br><span class="line">info[<span class="type">MPNowPlayingInfoPropertyPlaybackRate</span>] <span class="operator">=</span> <span class="number">1.0</span></span><br><span class="line">info[<span class="type">MPMediaItemPropertyArtwork</span>] <span class="operator">=</span> <span class="type">MPMediaItemArtwork</span>(boundsSize: image.size) &#123; <span class="keyword">_</span> <span class="keyword">in</span> image &#125;</span><br><span class="line">info[<span class="type">MPNowPlayingInfoPropertyIsLiveStream</span>] <span class="operator">=</span> <span class="literal">false</span></span><br><span class="line"></span><br><span class="line"><span class="type">MPNowPlayingInfoCenter</span>.default().nowPlayingInfo <span class="operator">=</span> info</span><br></pre></td></tr></table></figure>

<h3 id="MPRemoteCommandCenter"><a href="#MPRemoteCommandCenter" class="headerlink" title="MPRemoteCommandCenter"></a>MPRemoteCommandCenter</h3><p>Handle external controls (Lock Screen, headphones, CarPlay, Siri):</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> center <span class="operator">=</span> <span class="type">MPRemoteCommandCenter</span>.shared()</span><br><span class="line"></span><br><span class="line">center.playCommand.addTarget &#123; <span class="keyword">_</span> <span class="keyword">in</span></span><br><span class="line">    player.play()</span><br><span class="line">    <span class="keyword">return</span> .success</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">center.pauseCommand.addTarget &#123; <span class="keyword">_</span> <span class="keyword">in</span></span><br><span class="line">    player.pause()</span><br><span class="line">    <span class="keyword">return</span> .success</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Skip forward/back (podcast style)</span></span><br><span class="line">center.skipForwardCommand.preferredIntervals <span class="operator">=</span> [<span class="number">15</span>]</span><br><span class="line">center.skipForwardCommand.addTarget &#123; event <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">let</span> seconds <span class="operator">=</span> (event <span class="keyword">as!</span> <span class="type">MPSkipIntervalCommandEvent</span>).interval</span><br><span class="line">    <span class="keyword">let</span> newTime <span class="operator">=</span> player.currentTime <span class="operator">+</span> seconds</span><br><span class="line">    player.seek(to: <span class="type">CMTime</span>(seconds: newTime, preferredTimescale: <span class="number">600</span>))</span><br><span class="line">    <span class="keyword">return</span> .success</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Playback position (scrubber)</span></span><br><span class="line">center.changePlaybackPositionCommand.addTarget &#123; event <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">let</span> position <span class="operator">=</span> (event <span class="keyword">as!</span> <span class="type">MPChangePlaybackPositionCommandEvent</span>).positionTime</span><br><span class="line">    player.seek(to: <span class="type">CMTime</span>(seconds: position, preferredTimescale: <span class="number">600</span>))</span><br><span class="line">    <span class="keyword">return</span> .success</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Playback rate</span></span><br><span class="line">center.changePlaybackRateCommand.supportedPlaybackRates <span class="operator">=</span> [<span class="number">0.5</span>, <span class="number">1.0</span>, <span class="number">1.25</span>, <span class="number">1.5</span>, <span class="number">2.0</span>]</span><br><span class="line">center.changePlaybackRateCommand.addTarget &#123; event <span class="keyword">in</span></span><br><span class="line">    player.rate <span class="operator">=</span> (event <span class="keyword">as!</span> <span class="type">MPChangePlaybackRateCommandEvent</span>).playbackRate</span><br><span class="line">    <span class="keyword">return</span> .success</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="Sound-Analysis"><a href="#Sound-Analysis" class="headerlink" title="Sound Analysis"></a>Sound Analysis</h2><p>Sound Analysis is Apple’s on-device ML framework for classifying audio. Introduced at WWDC 2019 (iOS 13), with a built-in classifier added at WWDC 2021 (iOS 15+).</p>
<h3 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h3><p>The framework answers <strong>“what kind of sound is this?”</strong> using neural networks that run entirely on-device. It supports both Apple’s built-in classifier (303 sound categories) and custom Core ML models trained with Create ML.</p>
<h3 id="Real-Time-Classification"><a href="#Real-Time-Classification" class="headerlink" title="Real-Time Classification"></a>Real-Time Classification</h3><p>Feed audio buffers from <code>AVAudioEngine</code> into <code>SNAudioStreamAnalyzer</code>:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> SoundAnalysis</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> analyzer <span class="operator">=</span> <span class="type">SNAudioStreamAnalyzer</span>(format: engine.mainMixerNode.outputFormat(forBus: <span class="number">0</span>))</span><br><span class="line"><span class="keyword">let</span> request <span class="operator">=</span> <span class="keyword">try</span> <span class="type">SNClassifySoundRequest</span>(classifierIdentifier: .version1)</span><br><span class="line">request.windowDuration <span class="operator">=</span> <span class="type">CMTime</span>(seconds: <span class="number">1.5</span>, preferredTimescale: <span class="number">1</span>)</span><br><span class="line">request.overlapFactor <span class="operator">=</span> <span class="number">0.5</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span> analyzer.add(request, withObserver: <span class="keyword">self</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Feed audio from engine tap</span></span><br><span class="line">engine.mainMixerNode.installTap(onBus: <span class="number">0</span>, bufferSize: <span class="number">8192</span>, format: format) &#123; buffer, time <span class="keyword">in</span></span><br><span class="line">    analysisQueue.async &#123;</span><br><span class="line">        analyzer.analyze(buffer, atAudioFramePosition: time.sampleTime)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="File-Analysis"><a href="#File-Analysis" class="headerlink" title="File Analysis"></a>File Analysis</h3><p>Classify a recorded audio file (runs faster than real-time):</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> fileAnalyzer <span class="operator">=</span> <span class="keyword">try</span> <span class="type">SNAudioFileAnalyzer</span>(url: audioFileURL)</span><br><span class="line"><span class="keyword">let</span> request <span class="operator">=</span> <span class="keyword">try</span> <span class="type">SNClassifySoundRequest</span>(classifierIdentifier: .version1)</span><br><span class="line"><span class="keyword">try</span> fileAnalyzer.add(request, withObserver: <span class="keyword">self</span>)</span><br><span class="line">fileAnalyzer.analyze()  <span class="comment">// Synchronous</span></span><br><span class="line"><span class="comment">// or</span></span><br><span class="line">fileAnalyzer.analyze &#123; didReachEnd <span class="keyword">in</span></span><br><span class="line">    <span class="comment">// Async completion</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Results-Observer"><a href="#Results-Observer" class="headerlink" title="Results Observer"></a>Results Observer</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extension</span> <span class="title class_">MyAnalyzer</span>: <span class="title class_ inherited__">SNResultsObserving</span> &#123;</span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">request</span>(<span class="keyword">_</span> <span class="params">request</span>: <span class="type">SNRequest</span>, <span class="params">didProduce</span> <span class="params">result</span>: <span class="type">SNResult</span>) &#123;</span><br><span class="line">        <span class="keyword">guard</span> <span class="keyword">let</span> classification <span class="operator">=</span> result <span class="keyword">as?</span> <span class="type">SNClassificationResult</span> <span class="keyword">else</span> &#123; <span class="keyword">return</span> &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Top classification</span></span><br><span class="line">        <span class="keyword">let</span> top <span class="operator">=</span> classification.classifications.first<span class="operator">!</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;<span class="subst">\(top.identifier)</span>: <span class="subst">\(top.confidence)</span>&quot;</span>)  <span class="comment">// &quot;speech: 0.95&quot;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// Check specific sound</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">let</span> speech <span class="operator">=</span> classification.classification(forIdentifier: <span class="string">&quot;speech&quot;</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> speech.confidence <span class="operator">&gt;</span> <span class="number">0.7</span> &#123;</span><br><span class="line">                <span class="comment">// Speech detected</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Time range</span></span><br><span class="line">        <span class="keyword">let</span> range <span class="operator">=</span> classification.timeRange  <span class="comment">// CMTimeRange</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">request</span>(<span class="keyword">_</span> <span class="params">request</span>: <span class="type">SNRequest</span>, <span class="params">didFailWithError</span> <span class="params">error</span>: <span class="type">Error</span>) &#123; &#125;</span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">requestDidComplete</span>(<span class="keyword">_</span> <span class="params">request</span>: <span class="type">SNRequest</span>) &#123; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Configuration"><a href="#Configuration" class="headerlink" title="Configuration"></a>Configuration</h3><table>
<thead>
<tr>
<th>Property</th>
<th>Default</th>
<th>Range</th>
<th>Impact</th>
</tr>
</thead>
<tbody><tr>
<td><code>windowDuration</code></td>
<td>~1.0s</td>
<td>Model-dependent (check <code>windowDurationConstraint</code>)</td>
<td>Larger &#x3D; more accurate, less time-precise</td>
</tr>
<tr>
<td><code>overlapFactor</code></td>
<td>0.5</td>
<td>0.0 - 1.0</td>
<td>Higher &#x3D; more frequent results, more CPU</td>
</tr>
</tbody></table>
<h3 id="Built-in-Sound-Categories-303-total"><a href="#Built-in-Sound-Categories-303-total" class="headerlink" title="Built-in Sound Categories (303 total)"></a>Built-in Sound Categories (303 total)</h3><p>The built-in classifier recognizes 303 sounds. Here are the categories organized by group:</p>
<p><strong>Human - Speech &amp; Vocalization</strong>: <code>speech</code>, <code>shout</code>, <code>yell</code>, <code>scream</code>, <code>laughter</code>, <code>babbling</code>, <code>crowd</code>, <code>conversation</code>, <code>narration</code>, <code>singing</code>, <code>whispering</code>, <code>battle_cry</code>, <code>humming</code>, <code>whistling</code>, <code>chatter</code>, <code>whimper</code></p>
<p><strong>Human - Respiratory</strong>: <code>breathing</code>, <code>cough</code>, <code>sneeze</code>, <code>snoring</code>, <code>gasp</code>, <code>sigh</code>, <code>throat_clearing</code>, <code>gargling</code>, <code>hiccup</code></p>
<p><strong>Human - Activity</strong>: <code>clapping</code>, <code>finger_snapping</code>, <code>typing</code>, <code>writing</code>, <code>walking</code>, <code>running</code>, <code>tapping</code>, <code>shuffling</code>, <code>stomping</code>, <code>chewing</code>, <code>drinking_sipping</code>, <code>burping</code></p>
<p><strong>Animals</strong>: <code>dog_bark</code>, <code>dog_bow_wow</code>, <code>dog_growl</code>, <code>dog_whimper</code>, <code>cat_meow</code>, <code>cat_purr</code>, <code>cat_hiss</code>, <code>bird_chirp</code>, <code>bird_squawk</code>, <code>bird_call</code>, <code>crow_caw</code>, <code>owl_hoot</code>, <code>rooster_crow</code>, <code>duck_quack</code>, <code>goose_honk</code>, <code>turkey_gobble</code>, <code>livestock</code>, <code>horse_clip_clop</code>, <code>horse_neigh</code>, <code>cow_moo</code>, <code>pig_oink</code>, <code>sheep_bleat</code>, <code>goat_bleat</code>, <code>frog_croak</code>, <code>cricket</code>, <code>insect_buzz</code>, <code>fly_buzz</code>, <code>bee_buzz</code>, <code>mosquito_buzz</code>, <code>snake_hiss</code>, <code>whale_vocalization</code>, <code>monkey</code>, <code>elephant</code>, <code>lion_roar</code>, <code>bear_growl</code></p>
<p><strong>Musical Instruments</strong>: <code>piano</code>, <code>guitar</code>, <code>electric_guitar</code>, <code>bass_guitar</code>, <code>ukulele</code>, <code>banjo</code>, <code>mandolin</code>, <code>harp</code>, <code>violin_fiddle</code>, <code>cello</code>, <code>double_bass</code>, <code>drum_kit</code>, <code>snare_drum</code>, <code>bass_drum</code>, <code>cymbal</code>, <code>hi_hat</code>, <code>tambourine</code>, <code>bongo</code>, <code>conga</code>, <code>steel_drum</code>, <code>marimba</code>, <code>xylophone</code>, <code>vibraphone</code>, <code>glockenspiel</code>, <code>trumpet</code>, <code>trombone</code>, <code>french_horn</code>, <code>tuba</code>, <code>saxophone</code>, <code>clarinet</code>, <code>flute</code>, <code>oboe</code>, <code>harmonica</code>, <code>accordion</code>, <code>bagpipe</code>, <code>organ</code>, <code>synthesizer</code>, <code>sitar</code>, <code>tabla</code>, <code>didgeridoo</code></p>
<p><strong>Music</strong>: <code>music</code>, <code>theme_music</code>, <code>jingle</code>, <code>soundtrack_music</code></p>
<p>Note: The classifier recognizes music as a general category and distinguishes individual instruments (listed above), but does not classify music by genre.</p>
<p><strong>Environmental - Weather</strong>: <code>rain</code>, <code>raindrop</code>, <code>heavy_rain</code>, <code>thunder</code>, <code>thunderstorm</code>, <code>wind</code>, <code>hail</code></p>
<p><strong>Environmental - Water</strong>: <code>water</code>, <code>stream</code>, <code>waterfall</code>, <code>ocean</code>, <code>waves</code>, <code>boiling</code>, <code>water_drip</code>, <code>splash</code>, <code>water_pour</code></p>
<p><strong>Environmental - Nature</strong>: <code>fire_crackle</code>, <code>rustling_leaves</code>, <code>branch_snap</code>, <code>avalanche</code>, <code>earthquake</code>, <code>volcano</code>, <code>landslide</code></p>
<p><strong>Vehicles</strong>: <code>car</code>, <code>car_horn</code>, <code>car_engine_starting</code>, <code>car_engine_idling</code>, <code>car_passing</code>, <code>tire_screech</code>, <code>truck</code>, <code>bus</code>, <code>motorcycle</code>, <code>train</code>, <code>train_horn</code>, <code>train_wheels</code>, <code>subway</code>, <code>aircraft</code>, <code>helicopter</code>, <code>propeller</code>, <code>jet_engine</code>, <code>boat</code>, <code>speedboat</code>, <code>bicycle_bell</code>, <code>skateboard</code>, <code>siren</code>, <code>ambulance_siren</code>, <code>police_siren</code>, <code>fire_engine_siren</code></p>
<p><strong>Household</strong>: <code>door_knock</code>, <code>door_slam</code>, <code>door_open_close</code>, <code>doorbell</code>, <code>key_jingle</code>, <code>lock_click</code>, <code>window</code>, <code>drawer</code>, <code>cupboard</code>, <code>toilet_flush</code>, <code>sink_filling</code>, <code>faucet</code>, <code>shower</code>, <code>bathtub</code>, <code>vacuum_cleaner</code>, <code>hair_dryer</code>, <code>blender</code>, <code>microwave</code>, <code>dishwasher</code>, <code>washing_machine</code>, <code>refrigerator</code>, <code>air_conditioner</code>, <code>fan</code>, <code>clock_tick</code>, <code>clock_alarm</code>, <code>telephone_ring</code>, <code>cellphone_ring</code>, <code>television</code>, <code>static_noise</code>, <code>printer</code>, <code>computer_keyboard</code>, <code>mouse_click</code>, <code>zipper</code></p>
<p><strong>Alarms &amp; Signals</strong>: <code>alarm</code>, <code>alarm_clock</code>, <code>smoke_detector</code>, <code>fire_alarm</code>, <code>car_alarm</code>, <code>security_alarm</code>, <code>beep</code>, <code>buzzer</code>, <code>bell</code>, <code>church_bell</code>, <code>school_bell</code>, <code>whistle</code>, <code>horn</code>, <code>foghorn</code>, <code>air_horn</code></p>
<p><strong>Food &amp; Kitchen</strong>: <code>chopping</code>, <code>sizzle</code>, <code>frying</code>, <code>stirring</code>, <code>pouring</code>, <code>cork_pop</code>, <code>can_opening</code>, <code>bottle_opening</code>, <code>glass_clink</code>, <code>cutlery</code>, <code>plate_clink</code>, <code>ice_cube</code></p>
<p><strong>Tools &amp; Machinery</strong>: <code>hammer</code>, <code>saw</code>, <code>drill</code>, <code>power_tool</code>, <code>jackhammer</code>, <code>nail_gun</code>, <code>sanding</code>, <code>welding</code>, <code>chainsaw</code>, <code>lawnmower</code>, <code>engine</code>, <code>compressor</code>, <code>pump</code>, <code>machinery</code></p>
<p><strong>Other</strong>: <code>explosion</code>, <code>gunshot</code>, <code>fireworks</code>, <code>glass_breaking</code>, <code>wood_breaking</code>, <code>metal_clang</code>, <code>paper_rustling</code>, <code>plastic_bag</code>, <code>cardboard</code>, <code>bubble_wrap</code>, <code>tape</code>, <code>coin_drop</code>, <code>cash_register</code>, <code>slot_machine</code>, <code>pinball</code>, <code>bowling</code>, <code>basketball_bounce</code>, <code>golf_swing</code>, <code>tennis_hit</code>, <code>crowd_cheer</code>, <code>crowd_boo</code>, <code>applause</code>, <code>baby_crying</code>, <code>baby_laughter</code>, <code>child_speech</code>, <code>silence</code>, <code>noise</code>, <code>white_noise</code>, <code>pink_noise</code>, <code>static</code></p>
<h3 id="Custom-Models"><a href="#Custom-Models" class="headerlink" title="Custom Models"></a>Custom Models</h3><p>Train your own sound classifier with Create ML:</p>
<ol>
<li>Organize audio files into folders (one folder per category)</li>
<li>Open Create ML, create a Sound Classification project</li>
<li>It uses <strong>Audio Feature Print</strong> transfer learning from Apple’s base model</li>
<li>Even small datasets (50-100 samples per class) produce good results</li>
<li>Export as <code>.mlmodel</code>, use with <code>SNClassifySoundRequest(mlModel:)</code></li>
</ol>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> model <span class="operator">=</span> <span class="keyword">try</span> <span class="type">MLModel</span>(contentsOf: customModelURL)</span><br><span class="line"><span class="keyword">let</span> request <span class="operator">=</span> <span class="keyword">try</span> <span class="type">SNClassifySoundRequest</span>(mlModel: model)</span><br></pre></td></tr></table></figure>

<p><strong>Audio requirements</strong>: 16 kHz recommended, mono (first channel only), window duration 0.5-15s.</p>
<h3 id="Sound-Analysis-vs-Speech-Framework"><a href="#Sound-Analysis-vs-Speech-Framework" class="headerlink" title="Sound Analysis vs. Speech Framework"></a>Sound Analysis vs. Speech Framework</h3><table>
<thead>
<tr>
<th>Framework</th>
<th>Question it answers</th>
<th>Output</th>
</tr>
</thead>
<tbody><tr>
<td>Sound Analysis</td>
<td>“What kind of sound?”</td>
<td>Category labels + confidence</td>
</tr>
<tr>
<td>Speech (<code>SFSpeechRecognizer</code>)</td>
<td>“What words are spoken?”</td>
<td>Transcribed text</td>
</tr>
</tbody></table>
<p>They complement each other: detect speech presence with Sound Analysis, then transcribe only when speech confidence is high.</p>
<hr>
<h2 id="AudioKit-Open-Source"><a href="#AudioKit-Open-Source" class="headerlink" title="AudioKit (Open Source)"></a>AudioKit (Open Source)</h2><p><a href="https://github.com/AudioKit/AudioKit">AudioKit</a> is an 11,000+-star MIT-licensed audio platform built on AVAudioEngine. It provides vastly more DSP nodes, analysis tools, and UI components than Apple’s built-in APIs.</p>
<h3 id="Architecture"><a href="#Architecture" class="headerlink" title="Architecture"></a>Architecture</h3><p>AudioKit is split into ~15 SPM packages (plus documentation, CI, and example repos). The core is <strong>pure Swift</strong> (no C&#x2F;C++). Heavy DSP lives in separate packages:</p>
<table>
<thead>
<tr>
<th>Package</th>
<th>What it provides</th>
<th>Backend</th>
</tr>
</thead>
<tbody><tr>
<td><strong>AudioKit</strong> (core)</td>
<td>AudioEngine, Node, Taps, MIDI, Sequencing</td>
<td>Pure Swift over AVAudioEngine</td>
</tr>
<tr>
<td><strong>AudioKitEX</strong></td>
<td>Fader, DryWetMixer, Parameter Automation, Sequencer</td>
<td>C</td>
</tr>
<tr>
<td><strong>SoundpipeAudioKit</strong></td>
<td>54 DSP nodes (14 generators + 40 effects)</td>
<td>Soundpipe C + KissFFT</td>
</tr>
<tr>
<td><strong>DunneAudioKit</strong></td>
<td>Sampler (SFZ), Synth, Chorus, Flanger, StereoDelay</td>
<td>C++</td>
</tr>
<tr>
<td><strong>STKAudioKit</strong></td>
<td>Physical instrument models (strings, brass, flutes)</td>
<td>Stanford STK C++</td>
</tr>
<tr>
<td><strong>DevoloopAudioKit</strong></td>
<td>Guitar amp sim, tube compressor</td>
<td>C++</td>
</tr>
<tr>
<td><strong>SporthAudioKit</strong></td>
<td>Functional DSL for audio programming</td>
<td>Sporth C</td>
</tr>
<tr>
<td><strong>AudioKitUI</strong></td>
<td>SwiftUI visualizations (FFT, spectrogram, waveform, meters)</td>
<td>Swift</td>
</tr>
<tr>
<td><strong>Controls</strong></td>
<td>SwiftUI knobs, sliders, XY pads, wheels</td>
<td>Swift</td>
</tr>
<tr>
<td><strong>Keyboard</strong></td>
<td>SwiftUI piano&#x2F;guitar&#x2F;isomorphic keyboard</td>
<td>Swift</td>
</tr>
<tr>
<td><strong>Tonic</strong></td>
<td>Music theory (notes, chords, scales, keys, intervals)</td>
<td>Swift</td>
</tr>
<tr>
<td><strong>Flow</strong></td>
<td>Node graph editor</td>
<td>Swift</td>
</tr>
</tbody></table>
<h3 id="What-It-Adds-Over-Apple’s-APIs"><a href="#What-It-Adds-Over-Apple’s-APIs" class="headerlink" title="What It Adds Over Apple’s APIs"></a>What It Adds Over Apple’s APIs</h3><table>
<thead>
<tr>
<th>Capability</th>
<th>Apple Built-in</th>
<th>AudioKit</th>
</tr>
</thead>
<tbody><tr>
<td>Effect nodes</td>
<td>~12</td>
<td>80+</td>
</tr>
<tr>
<td>Filters</td>
<td>6 (LP, HP, BP, shelf, parametric)</td>
<td>20+ (Moog, Korg, Roland TB-303, Diode Ladder, Modal Resonance, etc.)</td>
</tr>
<tr>
<td>Reverbs</td>
<td>1 (Reverb2 with 13 presets)</td>
<td>6 (Chowning, Costello, Convolution IR, Zita, Comb, Flat)</td>
</tr>
<tr>
<td>Distortion</td>
<td>1 (25 presets)</td>
<td>5+ (BitCrusher, Clipper, Tanh, Decimator, RingModulator)</td>
</tr>
<tr>
<td>Dynamics</td>
<td>1 (DynamicsProcessor)</td>
<td>5+ (Compressor, Expander, PeakLimiter, DynaRage, TransientShaper)</td>
</tr>
<tr>
<td>Modulation</td>
<td>None built-in</td>
<td>8 (AutoPanner, AutoWah, Chorus, Ensemble, Flanger, Phaser, Tremolo, Vibrato)</td>
</tr>
<tr>
<td>Oscillators</td>
<td>None (generator AU only)</td>
<td>7+ (Oscillator, FM, Morphing, PWM, Phase Distortion, Dynamic, etc.)</td>
</tr>
<tr>
<td>Physical models</td>
<td>None</td>
<td>5+ (PluckedString, MetalBar, VocalTract, Drip, Shaker + STK instruments)</td>
</tr>
<tr>
<td>Analysis taps</td>
<td>Raw buffer only</td>
<td>Amplitude, FFT, Pitch detection</td>
</tr>
<tr>
<td>MIDI</td>
<td>C-level CoreMIDI</td>
<td>Full Swift MIDI stack with Bluetooth, file parsing, listeners</td>
</tr>
<tr>
<td>UI components</td>
<td>None</td>
<td>30+ SwiftUI views (spectrogram, waveform, meters, knobs, keyboard)</td>
</tr>
<tr>
<td>Music theory</td>
<td>None</td>
<td>Tonic library (notes, chords, scales, keys)</td>
</tr>
</tbody></table>
<h3 id="Example-Usage"><a href="#Example-Usage" class="headerlink" title="Example Usage"></a>Example Usage</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> AudioKit</span><br><span class="line"><span class="keyword">import</span> SoundpipeAudioKit</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> engine <span class="operator">=</span> <span class="type">AudioEngine</span>()</span><br><span class="line"><span class="keyword">let</span> player <span class="operator">=</span> <span class="type">AudioPlayer</span>()</span><br><span class="line"></span><br><span class="line"><span class="comment">// Build effect chain</span></span><br><span class="line"><span class="keyword">let</span> reverb <span class="operator">=</span> <span class="type">CostelloReverb</span>(player)</span><br><span class="line">reverb.feedback <span class="operator">=</span> <span class="number">0.6</span></span><br><span class="line">reverb.cutoffFrequency <span class="operator">=</span> <span class="number">9900</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> moogFilter <span class="operator">=</span> <span class="type">MoogLadder</span>(reverb)</span><br><span class="line">moogFilter.cutoffFrequency <span class="operator">=</span> <span class="number">1000</span></span><br><span class="line">moogFilter.resonance <span class="operator">=</span> <span class="number">0.5</span></span><br><span class="line"></span><br><span class="line">engine.output <span class="operator">=</span> moogFilter</span><br><span class="line"><span class="keyword">try</span> engine.start()</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span> player.load(url: audioURL)</span><br><span class="line">player.play()</span><br></pre></td></tr></table></figure>

<h3 id="Analysis"><a href="#Analysis" class="headerlink" title="Analysis"></a>Analysis</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Amplitude tracking</span></span><br><span class="line"><span class="keyword">let</span> tap <span class="operator">=</span> <span class="type">AmplitudeTap</span>(player) &#123; amplitude <span class="keyword">in</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Level: <span class="subst">\(amplitude)</span>&quot;</span>)  <span class="comment">// RMS amplitude</span></span><br><span class="line">&#125;</span><br><span class="line">tap.start()</span><br><span class="line"></span><br><span class="line"><span class="comment">// FFT spectrum</span></span><br><span class="line"><span class="keyword">let</span> fftTap <span class="operator">=</span> <span class="type">FFTTap</span>(player) &#123; fftData <span class="keyword">in</span></span><br><span class="line">    <span class="comment">// fftData: [Float] -- magnitude spectrum</span></span><br><span class="line">&#125;</span><br><span class="line">fftTap.start()</span><br><span class="line"></span><br><span class="line"><span class="comment">// Pitch detection (SoundpipeAudioKit)</span></span><br><span class="line"><span class="keyword">let</span> pitchTap <span class="operator">=</span> <span class="type">PitchTap</span>(player) &#123; pitch, amplitude <span class="keyword">in</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Pitch: <span class="subst">\(pitch)</span> Hz, Amplitude: <span class="subst">\(amplitude)</span>&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line">pitchTap.start()</span><br></pre></td></tr></table></figure>

<h3 id="When-to-Use-AudioKit"><a href="#When-to-Use-AudioKit" class="headerlink" title="When to Use AudioKit"></a>When to Use AudioKit</h3><p><strong>Use AudioKit when</strong>: Building a synthesizer, music production app, guitar effects processor, audio visualizer with complex DSP, or any app that needs filters&#x2F;effects beyond Apple’s built-in 12 Audio Units.</p>
<p><strong>Use Apple’s APIs directly when</strong>: Building a podcast player, audiobook player, music player, voice recorder, or any app where the primary need is playback&#x2F;recording with basic effects (EQ, reverb, pitch&#x2F;speed). Apple’s built-in APIs cover these use cases without the overhead of AudioKit’s C&#x2F;C++ compilation and dependency graph.</p>
<h3 id="Trade-offs"><a href="#Trade-offs" class="headerlink" title="Trade-offs"></a>Trade-offs</h3><table>
<thead>
<tr>
<th>Pro</th>
<th>Con</th>
</tr>
</thead>
<tbody><tr>
<td>80+ DSP nodes</td>
<td>Each node &#x3D; one AVAudioUnit (overhead for complex graphs)</td>
</tr>
<tr>
<td>Swift-native API</td>
<td>C&#x2F;C++ compilation slows builds significantly</td>
</tr>
<tr>
<td>12 years battle-tested</td>
<td>Still swift-tools-version 5.9 (no strict concurrency mode)</td>
</tr>
<tr>
<td>MIT license, 110+ Cookbook demos</td>
<td>Multi-repo ecosystem can be fragmented</td>
</tr>
<tr>
<td>Full MIDI stack</td>
<td>Single primary maintainer (bus factor risk)</td>
</tr>
</tbody></table>
<hr>
<h2 id="Decision-Matrix"><a href="#Decision-Matrix" class="headerlink" title="Decision Matrix"></a>Decision Matrix</h2><table>
<thead>
<tr>
<th>Use case</th>
<th>Framework</th>
<th>Key types</th>
</tr>
</thead>
<tbody><tr>
<td>Play a sound effect (&lt; 30s)</td>
<td>AudioToolbox</td>
<td><code>AudioServicesPlaySystemSound</code></td>
</tr>
<tr>
<td>Play local audio file</td>
<td>AVFAudio</td>
<td><code>AVAudioPlayer</code></td>
</tr>
<tr>
<td>Stream audio from URL&#x2F;HLS</td>
<td>AVFoundation</td>
<td><code>AVPlayer</code>, <code>AVPlayerItem</code></td>
</tr>
<tr>
<td>Queue multiple tracks</td>
<td>AVFoundation</td>
<td><code>AVQueuePlayer</code></td>
</tr>
<tr>
<td>Real-time EQ &#x2F; reverb &#x2F; effects</td>
<td>AVFAudio</td>
<td><code>AVAudioEngine</code>, <code>AVAudioUnitEQ</code>, <code>AVAudioUnitReverb</code></td>
</tr>
<tr>
<td>Change speed without pitch change</td>
<td>AVFAudio</td>
<td><code>AVAudioUnitTimePitch</code></td>
</tr>
<tr>
<td>Record audio</td>
<td>AVFAudio</td>
<td><code>AVAudioRecorder</code> (simple), <code>AVAudioEngine</code> (with effects)</td>
</tr>
<tr>
<td>Trim &#x2F; split &#x2F; merge audio</td>
<td>AVFoundation</td>
<td><code>AVMutableComposition</code>, <code>AVAssetExportSession</code></td>
</tr>
<tr>
<td>Volume fades during playback</td>
<td>AVFoundation</td>
<td><code>AVMutableAudioMix</code>, <code>AVMutableAudioMixInputParameters</code></td>
</tr>
<tr>
<td>Access raw samples from AVPlayer</td>
<td>AudioToolbox</td>
<td><code>MTAudioProcessingTap</code></td>
</tr>
<tr>
<td>Parse streaming audio progressively</td>
<td>AudioToolbox</td>
<td><code>AudioFileStreamOpen</code>, <code>AudioFileStreamParseBytes</code></td>
</tr>
<tr>
<td>Convert audio formats</td>
<td>AudioToolbox</td>
<td><code>AudioConverterNew</code>, <code>AudioConverterFillComplexBuffer</code></td>
</tr>
<tr>
<td>Read any file format into PCM</td>
<td>AudioToolbox &#x2F; AVFAudio</td>
<td><code>ExtAudioFile</code> or <code>AVAudioFile</code></td>
</tr>
<tr>
<td>Classify sounds (ML)</td>
<td>Sound Analysis</td>
<td><code>SNAudioStreamAnalyzer</code>, <code>SNClassifySoundRequest</code></td>
</tr>
<tr>
<td>Play MIDI files</td>
<td>AudioToolbox</td>
<td><code>MusicSequence</code>, <code>MusicPlayer</code></td>
</tr>
<tr>
<td>Lock Screen now playing info</td>
<td>MediaPlayer</td>
<td><code>MPNowPlayingInfoCenter</code></td>
</tr>
<tr>
<td>Handle remote commands</td>
<td>MediaPlayer</td>
<td><code>MPRemoteCommandCenter</code></td>
</tr>
<tr>
<td>Custom AUv3 audio plug-in</td>
<td>AudioUnit</td>
<td><code>AUAudioUnit</code> subclass, App Extension</td>
</tr>
<tr>
<td>Host third-party audio plug-ins</td>
<td>AudioUnit</td>
<td><code>AVAudioUnitComponentManager</code>, <code>AVAudioUnit.instantiate</code></td>
</tr>
<tr>
<td>Synthesis &#x2F; complex DSP &#x2F; music apps</td>
<td>AudioKit</td>
<td><code>AudioEngine</code>, SoundpipeAudioKit nodes</td>
</tr>
<tr>
<td>Background audio playback</td>
<td>AVFAudio + UIKit</td>
<td><code>AVAudioSession.setCategory(.playback)</code> + <code>UIBackgroundModes: audio</code></td>
</tr>
<tr>
<td>Spatial &#x2F; 3D audio (basic)</td>
<td>AVFAudio</td>
<td><code>AVAudioEnvironmentNode</code></td>
</tr>
<tr>
<td>Spatial audio (geometry-aware)</td>
<td>PHASE</td>
<td><code>PHASEEngine</code>, <code>PHASESoundEvent</code>, occlusion &#x2F; reverb-zone modeling</td>
</tr>
<tr>
<td>Personalized Spatial Audio (iOS 26)</td>
<td>PHASE &#x2F; AVFAudio</td>
<td><code>com.apple.developer.spatial-audio.profile-access</code> entitlement</td>
</tr>
<tr>
<td>Record Spatial Audio (iOS 26)</td>
<td>AVFoundation</td>
<td><code>AVAssetWriter</code> + <code>.qta</code> container</td>
</tr>
<tr>
<td>Offline audio rendering</td>
<td>AVFAudio</td>
<td><code>AVAudioEngine.enableManualRenderingMode</code></td>
</tr>
<tr>
<td>Low-level hardware access (macOS)</td>
<td>Core Audio</td>
<td><code>AudioHardware</code>, <code>AudioDevice</code></td>
</tr>
</tbody></table>
<hr>
<h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>Apple’s audio stack is deep. Most apps only need AVFoundation (AVPlayer) or AVFAudio (AVAudioEngine). Drop to AudioToolbox for format conversion, stream parsing, or MTAudioProcessingTap. Use Audio Units for custom real-time DSP or hosting third-party plug-ins. Sound Analysis adds ML-powered sound classification. And AudioKit fills the gaps with 80+ DSP nodes when Apple’s built-in effects aren’t enough.</p>
<p>Start at the highest level. Drop down only when you need more control.</p>
]]></content>
      <categories>
        <category>iOS</category>
      </categories>
      <tags>
        <tag>audio</tag>
        <tag>AVFoundation</tag>
        <tag>CoreAudio</tag>
        <tag>AudioToolbox</tag>
        <tag>AudioUnit</tag>
        <tag>SoundAnalysis</tag>
        <tag>Swift</tag>
        <tag>AVAudioEngine</tag>
      </tags>
  </entry>
  <entry>
    <title>AI services guide: subscriptions, free tiers, and APIs (March 2026)</title>
    <url>/AI-Services-Guide-March-2026/</url>
    <content><![CDATA[<p>A March 2026 snapshot of every major AI provider: chat subscriptions, API pricing, and coding agents.</p>
<span id="more"></span>

<h2 id="Two-ways-to-use-AI-models"><a href="#Two-ways-to-use-AI-models" class="headerlink" title="Two ways to use AI models"></a>Two ways to use AI models</h2><p>Two layers, two bills:</p>
<ol>
<li><strong>Chat product</strong> – the app you talk to (ChatGPT, Claude, Gemini, Grok, Le Chat, Qwen, Copilot, Perplexity, Nova). Every provider has a free tier with limited usage, and paid subscriptions ($8-300&#x2F;mo) that unlock premium models, higher limits, and features like image gen, voice, and video.</li>
<li><strong>API</strong> – pay-per-token programmatic access for building apps. Separate billing, separate account, separate pricing.</li>
</ol>
<p>The only overlaps: Google AI Pro&#x2F;Ultra subscribers get Google Cloud credits ($10&#x2F;$100 per month) toward Gemini API usage. Perplexity Pro subscribers get $5&#x2F;month in API credits. Everyone else: your subscription gets you the chat app, not programmatic access.</p>
<h2 id="Quick-comparison"><a href="#Quick-comparison" class="headerlink" title="Quick comparison"></a>Quick comparison</h2><table>
<thead>
<tr>
<th>Provider</th>
<th>Chat product</th>
<th>Cheapest API (in&#x2F;out per 1M)</th>
<th>Free API tier</th>
<th>Max context</th>
<th>Open source</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Google Gemini</strong></td>
<td>Gemini</td>
<td>$0.10&#x2F;$0.40 (Flash-Lite 2.5)</td>
<td>Unlimited, no card</td>
<td>1M</td>
<td>Gemma (open weights)</td>
</tr>
<tr>
<td><strong>OpenAI</strong></td>
<td>ChatGPT</td>
<td>$0.10&#x2F;$0.40 (GPT-4.1 Nano)</td>
<td>Limited trial credits</td>
<td>1M+</td>
<td>GPT-OSS (Apache 2.0)</td>
</tr>
<tr>
<td><strong>Anthropic</strong></td>
<td>Claude</td>
<td>$1&#x2F;$5 (Haiku 4.5)</td>
<td>$5 trial credits</td>
<td>200K (1M beta)</td>
<td>No</td>
</tr>
<tr>
<td><strong>xAI</strong></td>
<td>Grok</td>
<td>$0.20&#x2F;$0.50 (Grok 4.1 Fast)</td>
<td>$25 sign-up credits</td>
<td>2M</td>
<td>Partial</td>
</tr>
<tr>
<td><strong>DeepSeek</strong></td>
<td>DeepSeek</td>
<td>$0.028&#x2F;$0.42 (V3.2 cached)</td>
<td>5M tokens free</td>
<td>128K</td>
<td>Yes (MIT)</td>
</tr>
<tr>
<td><strong>Mistral</strong></td>
<td>Le Chat</td>
<td>$0.02&#x2F;$0.04 (Nemo)</td>
<td>API trial credits</td>
<td>256K</td>
<td>Partial</td>
</tr>
<tr>
<td><strong>Meta Llama</strong></td>
<td>–</td>
<td>Free (self-host)</td>
<td>Models are free</td>
<td>10M (Scout)</td>
<td>Yes</td>
</tr>
<tr>
<td><strong>Alibaba Qwen</strong></td>
<td>Qwen</td>
<td>$0.05&#x2F;$0.20 (Qwen3-8B)</td>
<td>1M tokens&#x2F;model</td>
<td>10M (Qwen-Long)</td>
<td>Yes (Apache 2.0)</td>
</tr>
<tr>
<td><strong>Amazon Nova</strong></td>
<td>Nova</td>
<td>$0.035&#x2F;$0.14 (Nova Micro)</td>
<td>$200 AWS credits</td>
<td>1M (Premier)</td>
<td>No</td>
</tr>
<tr>
<td><strong>Microsoft Phi</strong></td>
<td>Copilot</td>
<td>$0.075&#x2F;$0.30 (Phi-4-mini)</td>
<td>GitHub Models free</td>
<td>128K</td>
<td>Yes (MIT)</td>
</tr>
<tr>
<td><strong>Perplexity</strong></td>
<td>Perplexity</td>
<td>$1&#x2F;$1 (Sonar) + per-req fee</td>
<td>No free tier</td>
<td>200K</td>
<td>No</td>
</tr>
<tr>
<td><strong>Groq</strong></td>
<td>–</td>
<td>$0.05&#x2F;$0.08 (Llama 3.1 8B)</td>
<td>Rate-limited, no card</td>
<td>256K</td>
<td>Hosts OSS only</td>
</tr>
</tbody></table>
<div class="note info"><p><strong>“Open weights” vs “open source”</strong>: The “Open source” column above uses industry shorthand. Strictly speaking, most providers release <strong>open weights</strong> (trained model parameters you can download and run) but not full training code or data. Here’s the distinction:</p>
<table>
<thead>
<tr>
<th>Term</th>
<th align="center">Model weights</th>
<th align="center">Training code</th>
<th align="center">Training data</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Open weights</strong></td>
<td align="center">✓</td>
<td align="center">✗</td>
<td align="center">✗</td>
</tr>
<tr>
<td><strong>Open source</strong> (OSI definition)</td>
<td align="center">✓</td>
<td align="center">✓</td>
<td align="center">✓</td>
</tr>
</tbody></table>
<p>In practice: Apache 2.0 &#x2F; MIT models (Qwen, DeepSeek, Phi, GPT-OSS) are the closest to true open source. Gemma and Llama release weights under more restrictive licenses. Anthropic, Amazon, and Perplexity release no weights at all.</p>
</div>

<hr>
<h2 id="Subscription-comparison"><a href="#Subscription-comparison" class="headerlink" title="Subscription comparison"></a>Subscription comparison</h2><h3 id="All-12-providers-at-a-glance"><a href="#All-12-providers-at-a-glance" class="headerlink" title="All 12 providers at a glance"></a>All 12 providers at a glance</h3><table>
<thead>
<tr>
<th>Provider</th>
<th>Chat product</th>
<th>Free tier</th>
<th>Cheapest paid</th>
<th>~$20 tier</th>
<th>High-end</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Google Gemini</strong></td>
<td><a href="https://gemini.google.com/">Gemini</a></td>
<td>50 daily credits</td>
<td>AI Plus $8&#x2F;mo</td>
<td>AI Pro $20&#x2F;mo</td>
<td>AI Ultra $250&#x2F;mo</td>
</tr>
<tr>
<td><strong>OpenAI</strong></td>
<td><a href="https://chatgpt.com/">ChatGPT</a></td>
<td>~10 msgs &#x2F; 5 hrs</td>
<td>Go $8&#x2F;mo</td>
<td>Plus $20&#x2F;mo</td>
<td>Pro $200&#x2F;mo</td>
</tr>
<tr>
<td><strong>Anthropic</strong></td>
<td><a href="https://claude.ai/">Claude</a></td>
<td>~15-40 msgs &#x2F; 5 hrs</td>
<td>–</td>
<td>Pro $20&#x2F;mo</td>
<td>Max 20x $200&#x2F;mo</td>
</tr>
<tr>
<td><strong>xAI</strong></td>
<td><a href="https://grok.com/">Grok</a></td>
<td>~10 msgs &#x2F; 2 hrs</td>
<td>X Premium $8&#x2F;mo</td>
<td>SuperGrok $30&#x2F;mo</td>
<td>Heavy $300&#x2F;mo</td>
</tr>
<tr>
<td><strong>DeepSeek</strong></td>
<td><a href="https://chat.deepseek.com/">DeepSeek</a></td>
<td>Unlimited (no sub)</td>
<td>–</td>
<td>–</td>
<td>–</td>
</tr>
<tr>
<td><strong>Mistral</strong></td>
<td><a href="https://chat.mistral.ai/">Le Chat</a></td>
<td>~25 msgs</td>
<td>Student $6&#x2F;mo</td>
<td>Pro $15&#x2F;mo</td>
<td>Enterprise (custom)</td>
</tr>
<tr>
<td><strong>Meta Llama</strong></td>
<td>–</td>
<td>Models are free</td>
<td>–</td>
<td>–</td>
<td>–</td>
</tr>
<tr>
<td><strong>Alibaba Qwen</strong></td>
<td><a href="https://chat.qwen.ai/">Qwen</a></td>
<td>Unlimited (no sub)</td>
<td>–</td>
<td>–</td>
<td>–</td>
</tr>
<tr>
<td><strong>Amazon Nova</strong></td>
<td><a href="https://nova.amazon.com/">Nova</a></td>
<td>Free (US only)</td>
<td>–</td>
<td>–</td>
<td>–</td>
</tr>
<tr>
<td><strong>Microsoft</strong></td>
<td><a href="https://copilot.microsoft.com/">Copilot</a></td>
<td>Limited GPT-5.x</td>
<td>–</td>
<td>Copilot Pro $20&#x2F;mo</td>
<td>M365 Enterprise $30&#x2F;seat&#x2F;mo</td>
</tr>
<tr>
<td><strong>Perplexity</strong></td>
<td><a href="https://www.perplexity.ai/">Perplexity</a></td>
<td>Limited Pro Searches</td>
<td>–</td>
<td>Pro $20&#x2F;mo</td>
<td>Max $200&#x2F;mo</td>
</tr>
<tr>
<td><strong>Groq</strong></td>
<td>–</td>
<td>Rate-limited API</td>
<td>Pay-as-you-go</td>
<td>–</td>
<td>–</td>
</tr>
</tbody></table>
<h3 id="Feature-comparison-20-tier"><a href="#Feature-comparison-20-tier" class="headerlink" title="Feature comparison (~$20 tier)"></a>Feature comparison (~$20 tier)</h3><p>Seven providers offer a tier around $15-30&#x2F;month. Here’s what you get:</p>
<table>
<thead>
<tr>
<th>Feature</th>
<th>ChatGPT Plus ($20)</th>
<th>Claude Pro ($20)</th>
<th>Gemini AI Pro ($20)</th>
<th>SuperGrok ($30)</th>
<th>Le Chat Pro ($15)</th>
<th>Copilot Pro ($20)</th>
<th>Perplexity Pro ($20)</th>
</tr>
</thead>
<tbody><tr>
<td>Top model</td>
<td>GPT-5.2 Thinking</td>
<td>Opus 4.6</td>
<td>Gemini 3.1 Pro</td>
<td>Grok 4.1</td>
<td>Mistral Large 3</td>
<td>GPT-5.x</td>
<td>GPT-5.4, Claude, Gemini</td>
</tr>
<tr>
<td>Context</td>
<td>32K</td>
<td>200K (1M beta)</td>
<td>128K+</td>
<td>128K</td>
<td>128K</td>
<td>–</td>
<td>200K</td>
</tr>
<tr>
<td>Image gen</td>
<td>DALL-E (~180&#x2F;day)</td>
<td>No</td>
<td>Nano Banana Pro</td>
<td>Aurora&#x2F;Imagine</td>
<td>Yes</td>
<td>DALL-E (M365 only)</td>
<td>Yes</td>
</tr>
<tr>
<td>Video gen</td>
<td>Sora (720p, 20s)</td>
<td>No</td>
<td>Veo (limited)</td>
<td>Imagine (720p, 6s)</td>
<td>No</td>
<td>No</td>
<td>Yes</td>
</tr>
<tr>
<td>Voice mode</td>
<td>Advanced (video&#x2F;screen)</td>
<td>No</td>
<td>Yes</td>
<td>Extended</td>
<td>No</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td>Web search</td>
<td>Yes</td>
<td>Yes</td>
<td>Deep Search</td>
<td>DeepSearch</td>
<td>AFP-verified</td>
<td>Bing</td>
<td>Grounded (citations)</td>
</tr>
<tr>
<td>Coding agent</td>
<td>Codex</td>
<td>Claude Code</td>
<td>Jules (5x free)</td>
<td>Grok Build (waitlist)</td>
<td>Vibe 2.0</td>
<td>–</td>
<td>–</td>
</tr>
<tr>
<td>Deep reasoning</td>
<td>GPT-5.2 Thinking</td>
<td>Extended Thinking</td>
<td>Deep Research</td>
<td>Big Brain Mode</td>
<td>Flash Answers</td>
<td>–</td>
<td>Deep Research</td>
</tr>
<tr>
<td>No training</td>
<td>No</td>
<td>Yes</td>
<td>No</td>
<td>No</td>
<td>Yes</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td>API credits</td>
<td>No</td>
<td>No</td>
<td>$10&#x2F;mo GCP</td>
<td>No</td>
<td>No</td>
<td>No</td>
<td>$5&#x2F;mo</td>
</tr>
<tr>
<td>Unique value</td>
<td>Broadest features</td>
<td>Best coding</td>
<td>Workspace + API credits</td>
<td>2M context, X data</td>
<td>Cheapest, no telemetry</td>
<td>M365 integration</td>
<td>Search + multi-model</td>
</tr>
</tbody></table>
<p><strong>Best free tier</strong>: <a href="https://chat.deepseek.com/">DeepSeek</a> and <a href="https://chat.qwen.ai/">Qwen</a> (both unlimited, no sub) or <a href="https://gemini.google.com/">Gemini</a> (50 daily credits).</p>
<p><strong>Best value at ~$20</strong>: Claude Pro for coding depth. ChatGPT Plus for broadest features. Gemini AI Pro for Workspace integration and the only one with API credits. Perplexity Pro for search + access to third-party models. Le Chat Pro is cheapest at $15 with no-telemetry.</p>
<p><strong>High-end tiers</strong>: Claude Max 20x ($200) for near-unlimited Opus 4.6 + Claude Code. ChatGPT Pro ($200) for unlimited everything + Sora 4K. Google AI Ultra ($250) for 25K credits + YouTube Premium + Deep Think. SuperGrok Heavy ($300) for Grok 4 Heavy multi-agent.</p>
<hr>
<h2 id="Coding-agents-comparison"><a href="#Coding-agents-comparison" class="headerlink" title="Coding agents comparison"></a>Coding agents comparison</h2><p>Every major provider now ships a coding agent. Plus a growing ecosystem of third-party agents that use their APIs.</p>
<h3 id="First-party-agents"><a href="#First-party-agents" class="headerlink" title="First-party agents"></a>First-party agents</h3><table>
<thead>
<tr>
<th>Agent</th>
<th>Provider</th>
<th>Type</th>
<th>Cheapest access</th>
<th>Rules</th>
<th>Memory</th>
<th>MCP</th>
<th>Sub-agents</th>
</tr>
</thead>
<tbody><tr>
<td><a href="https://openai.com/index/introducing-codex/">Codex</a></td>
<td>OpenAI</td>
<td>Cloud sandbox</td>
<td>Plus $20&#x2F;mo</td>
<td><code>AGENTS.md</code></td>
<td>No</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://claude.com/product/claude-code">Claude Code</a></td>
<td>Anthropic</td>
<td>Terminal CLI</td>
<td>Pro $20&#x2F;mo</td>
<td><code>CLAUDE.md</code></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://jules.google/docs/usage-limits/">Jules</a></td>
<td>Google</td>
<td>Async GitHub</td>
<td>Free (15 tasks&#x2F;day)</td>
<td><code>AGENTS.md</code></td>
<td>Partial</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr>
<td><a href="https://geminicli.com/">Gemini CLI</a></td>
<td>Google</td>
<td>Terminal CLI</td>
<td>Free (1K req&#x2F;day)</td>
<td><code>GEMINI.md</code></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://cloud.google.com/products/gemini-code-assist">Gemini Code Assist</a></td>
<td>Google</td>
<td>IDE assistant</td>
<td>Free</td>
<td>–</td>
<td>–</td>
<td>–</td>
<td>–</td>
</tr>
<tr>
<td><a href="https://developers.googleblog.com/build-with-google-antigravity-our-new-agentic-development-platform/">Antigravity</a></td>
<td>Google</td>
<td>Agentic IDE</td>
<td>Free (preview)</td>
<td><code>.agents/workflows/</code></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://grokai.build/">Grok Build</a></td>
<td>xAI</td>
<td>Local CLI</td>
<td>Waitlist</td>
<td><code>.grok/GROK.md</code></td>
<td>Partial</td>
<td>Yes</td>
<td>Yes (8x)</td>
</tr>
<tr>
<td><a href="https://mistral.ai/news/mistral-vibe-2-0">Vibe 2.0</a></td>
<td>Mistral</td>
<td>Terminal CLI</td>
<td>Le Chat Pro $15&#x2F;mo</td>
<td><code>AGENTS.md</code></td>
<td>No</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://github.com/QwenLM/qwen-code">Qwen Code</a></td>
<td>Alibaba</td>
<td>Terminal CLI</td>
<td>Free (1K req&#x2F;day)</td>
<td><code>QWEN.md</code></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://aws.amazon.com/q/developer/">Amazon Q Developer</a></td>
<td>Amazon</td>
<td>IDE + CLI</td>
<td>Free (50 agentic&#x2F;mo)</td>
<td><code>.amazonq/rules/</code></td>
<td>Partial</td>
<td>Yes</td>
<td>No</td>
</tr>
</tbody></table>
<p><strong>Other first-party agents</strong> (not coding-specific): <a href="https://openai.com/index/introducing-chatgpt-agent/">Agent Mode</a> (OpenAI, web automation, Plus $20&#x2F;mo), <a href="https://claude.com/blog/cowork-research-preview">Cowork</a> (Anthropic, desktop agent, Pro $20&#x2F;mo), <a href="https://platform.claude.com/docs/en/agents-and-tools/computer-use">Computer Use</a> (Anthropic, screen automation, API only).</p>
<h3 id="Third-party-agents"><a href="#Third-party-agents" class="headerlink" title="Third-party agents"></a>Third-party agents</h3><p>These use the APIs above. You pick the model, they provide the IDE&#x2F;workflow.</p>
<table>
<thead>
<tr>
<th>Agent</th>
<th>Type</th>
<th>Pricing</th>
<th>Rules</th>
<th>Memory</th>
<th>MCP</th>
<th>Sub-agents</th>
</tr>
</thead>
<tbody><tr>
<td><a href="https://cursor.com/">Cursor</a></td>
<td>VS Code fork</td>
<td>Free &#x2F; Pro $20 &#x2F; Ultra $200</td>
<td><code>.cursor/rules/*.mdc</code></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://windsurf.com/">Windsurf</a></td>
<td>Agentic IDE</td>
<td>Free &#x2F; Pro $15 &#x2F; Teams $30</td>
<td><code>.windsurf/rules/*.md</code></td>
<td>Yes</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr>
<td><a href="https://github.com/features/copilot/plans">GitHub Copilot</a></td>
<td>IDE integration</td>
<td>Free &#x2F; Pro $10 &#x2F; Pro+ $39</td>
<td><code>.github/copilot-instructions.md</code></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><a href="https://cline.bot/">Cline</a></td>
<td>VS Code extension</td>
<td>Free (OSS, pay API) &#x2F; Teams $20</td>
<td><code>.clinerules</code></td>
<td>Yes</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr>
<td><a href="https://aider.chat/">Aider</a></td>
<td>Terminal</td>
<td>Free (OSS, pay API)</td>
<td><code>CONVENTIONS.md</code></td>
<td>No</td>
<td>Partial</td>
<td>No</td>
</tr>
<tr>
<td><a href="https://www.continue.dev/">Continue</a></td>
<td>VS Code &#x2F; JetBrains</td>
<td>Solo free &#x2F; Team $10&#x2F;dev</td>
<td><code>.continue/rules/*.md</code></td>
<td>No</td>
<td>Yes</td>
<td>No</td>
</tr>
</tbody></table>
<div class="note info"><p><strong>Rules</strong>: Every agent now supports project-level instruction files, but there is no standard. <code>AGENTS.md</code> is gaining traction across OpenAI, Google, and Mistral tools. GitHub Copilot also reads <code>AGENTS.md</code> and <code>CLAUDE.md</code>.</p>
<p><strong>MCP</strong>: Near-universal. 18 of 19 agents support MCP natively. Aider has only community&#x2F;experimental support.</p>
<p><strong>Memory</strong>: Split. About half have true persistent cross-session memory. Others rely on rules files as static context.</p>
</div>

<p>Note: GitHub Copilot is a Microsoft product. The Pro+ tier ($39&#x2F;mo) includes an autonomous coding agent that creates PRs from GitHub issues and model choice across providers.</p>
<hr>
<h2 id="Google-Gemini"><a href="#Google-Gemini" class="headerlink" title="Google Gemini"></a>Google Gemini</h2><p><a href="https://gemini.google.com/">gemini.google.com</a> | <a href="https://aistudio.google.com/">AI Studio</a> | <a href="https://ai.google.dev/gemini-api/docs">API docs</a></p>
<p>The most generous free tier of any provider. No credit card, no trial period, just get an API key and go.</p>
<h3 id="Models"><a href="#Models" class="headerlink" title="Models"></a>Models</h3><table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Gemini 3.1 Pro</td>
<td>1M</td>
<td>$2.00 &#x2F; $12.00</td>
<td>Flagship reasoning, agentic workflows, 77.1% ARC-AGI-2</td>
</tr>
<tr>
<td>Gemini 3 Flash</td>
<td>1M</td>
<td>$0.50 &#x2F; $3.00</td>
<td>Near-Pro reasoning at fraction of cost</td>
</tr>
<tr>
<td>Gemini 3.1 Flash-Lite</td>
<td>1M</td>
<td>$0.25 &#x2F; $1.50</td>
<td>Most cost-efficient, high-volume workloads</td>
</tr>
<tr>
<td>Gemini 2.5 Pro</td>
<td>1M</td>
<td>$1.25 &#x2F; $10.00</td>
<td>Deep reasoning, coding, math, science</td>
</tr>
<tr>
<td>Gemini 2.5 Flash</td>
<td>1M</td>
<td>$0.30 &#x2F; $2.50</td>
<td>Best price-performance for reasoning</td>
</tr>
<tr>
<td>Gemini 2.5 Flash-Lite</td>
<td>1M</td>
<td>$0.10 &#x2F; $0.40</td>
<td>Fastest and cheapest multimodal model</td>
</tr>
</tbody></table>
<p>All models accept text, image, audio, video, and PDF input. 1M token context across the board.</p>
<p><strong>Specialized models</strong>: Imagen 4 (image gen, $0.02-0.06&#x2F;image), Veo 3.1 (video gen), Gemini Embedding 2 (multimodal embeddings), Computer Use (UI automation).</p>
<h3 id="Subscriptions"><a href="#Subscriptions" class="headerlink" title="Subscriptions"></a>Subscriptions</h3><table>
<thead>
<tr>
<th>Tier</th>
<th>Price</th>
<th>Storage</th>
<th>Models</th>
<th>Key features</th>
<th>API access</th>
</tr>
</thead>
<tbody><tr>
<td>Free</td>
<td>$0</td>
<td>15 GB</td>
<td>Gemini 3</td>
<td>50 daily AI credits, basic NotebookLM (100 notebooks, 50 queries&#x2F;day), limited image gen</td>
<td>No</td>
</tr>
<tr>
<td>AI Plus</td>
<td>$7.99&#x2F;mo</td>
<td>200 GB</td>
<td>Gemini 3 Pro</td>
<td>200 monthly credits, limited Veo 3.1 Fast video, limited Workspace AI, share with 5 family members</td>
<td>No</td>
</tr>
<tr>
<td>AI Pro</td>
<td>$19.99&#x2F;mo</td>
<td>2 TB</td>
<td>Gemini 3.1 Pro</td>
<td>1,000 monthly credits, Deep Research, Deep Search, NotebookLM (500 notebooks, 300 sources, 500 queries&#x2F;day), full Workspace AI (Gmail&#x2F;Docs&#x2F;Sheets&#x2F;Slides), Jules (5x free), Gemini Code Assist, Gems (custom AI personas), family sharing</td>
<td>$10&#x2F;mo GCP credits</td>
</tr>
<tr>
<td>AI Ultra</td>
<td>$249.99&#x2F;mo</td>
<td>30 TB</td>
<td>Gemini 3.1 Pro, Deep Think</td>
<td>25,000 monthly credits, Deep Think (extended reasoning, exclusive), highest Deep Research access, Veo 3.1 full + early Veo 3.16, Jules (20x free, multi-agent), YouTube Premium included, early access to experimental features, family sharing</td>
<td>$100&#x2F;mo GCP credits</td>
</tr>
</tbody></table>
<p>Credits do NOT roll over. See <a href="https://gemini.google/subscriptions/">subscription plans</a> and <a href="https://one.google.com/about/google-ai-plans/">Google One AI plans</a> for details.</p>
<h3 id="Free-tier-API"><a href="#Free-tier-API" class="headerlink" title="Free tier (API)"></a>Free tier (API)</h3><table>
<thead>
<tr>
<th>Model</th>
<th>Requests&#x2F;min</th>
<th>Tokens&#x2F;min</th>
<th>Requests&#x2F;day</th>
</tr>
</thead>
<tbody><tr>
<td>Gemini 3 Flash</td>
<td>10</td>
<td>250K</td>
<td>250</td>
</tr>
<tr>
<td>Gemini 3.1 Flash-Lite</td>
<td>15</td>
<td>250K</td>
<td>1,000</td>
</tr>
<tr>
<td>Gemini 2.5 Pro</td>
<td>5</td>
<td>250K</td>
<td>100</td>
</tr>
<tr>
<td>Gemini 2.5 Flash</td>
<td>10</td>
<td>250K</td>
<td>250</td>
</tr>
<tr>
<td>Gemini 2.5 Flash-Lite</td>
<td>15</td>
<td>250K</td>
<td>1,000</td>
</tr>
</tbody></table>
<p>Free tier covers Flash and Flash-Lite models across generations (not Pro). No credit card required. Content on free tier may be used to improve Google’s products.</p>
<h3 id="Open-source"><a href="#Open-source" class="headerlink" title="Open source"></a>Open source</h3><p>Gemini itself is proprietary. <a href="https://ai.google.dev/gemma">Gemma</a> (1B-27B params) is Google’s open-weight alternative, built on the same research. Free for commercial use.</p>
<h3 id="Mobile-SDK"><a href="#Mobile-SDK" class="headerlink" title="Mobile SDK"></a>Mobile SDK</h3><p>Google consolidated mobile access under <a href="https://firebase.google.com/products/firebase-ai-logic">Firebase AI Logic</a>. SDKs for Swift (iOS), Kotlin (Android), Flutter, React Native, Unity, and Web. The older standalone Google AI SDKs are deprecated.</p>
<h3 id="Links"><a href="#Links" class="headerlink" title="Links"></a>Links</h3><ul>
<li><a href="https://gemini.google/subscriptions/">Subscription plans</a></li>
<li><a href="https://ai.google.dev/gemini-api/docs/pricing">API pricing</a></li>
<li><a href="https://ai.google.dev/gemini-api/docs/models">Models reference</a></li>
<li><a href="https://ai.google.dev/gemini-api/docs/rate-limits">Rate limits</a></li>
<li><a href="https://firebase.google.com/products/firebase-ai-logic">Firebase AI Logic SDKs</a></li>
</ul>
<hr>
<h2 id="OpenAI"><a href="#OpenAI" class="headerlink" title="OpenAI"></a>OpenAI</h2><p><a href="https://chatgpt.com/">chatgpt.com</a> | <a href="https://platform.openai.com/">platform.openai.com</a> | <a href="https://developers.openai.com/api/docs">API docs</a></p>
<p>The largest model lineup. GPT-5.x for general use, o-series for reasoning, GPT-4.1 for low-latency tool calling.</p>
<h3 id="Models-1"><a href="#Models-1" class="headerlink" title="Models"></a>Models</h3><p><strong>GPT-5 series:</strong></p>
<table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>GPT-5.4</td>
<td>1M+</td>
<td>$2.50 &#x2F; $15.00</td>
<td>Latest flagship, computer-use, 33% fewer errors vs 5.2</td>
</tr>
<tr>
<td>GPT-5.4 Pro</td>
<td>1M+</td>
<td>$30.00 &#x2F; $180.00</td>
<td>Extended reasoning</td>
</tr>
<tr>
<td>GPT-5.2</td>
<td>400K</td>
<td>$1.75 &#x2F; $14.00</td>
<td>Workhorse model, cached input at $0.175</td>
</tr>
<tr>
<td>GPT-5</td>
<td>400K</td>
<td>$1.25 &#x2F; $10.00</td>
<td>Original GPT-5</td>
</tr>
<tr>
<td>GPT-5 Mini</td>
<td>400K</td>
<td>$0.25 &#x2F; $2.00</td>
<td>Faster, cheaper</td>
</tr>
<tr>
<td>GPT-5 Nano</td>
<td>400K</td>
<td>$0.05 &#x2F; $0.40</td>
<td>Cheapest GPT-5, edge-friendly</td>
</tr>
</tbody></table>
<p><strong>O-series (reasoning):</strong></p>
<table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>o3</td>
<td>200K</td>
<td>$2.00 &#x2F; $8.00</td>
<td>Advanced reasoning (math, science, coding)</td>
</tr>
<tr>
<td>o3 Pro</td>
<td>200K</td>
<td>$20.00 &#x2F; $80.00</td>
<td>Extended compute</td>
</tr>
<tr>
<td>o4 Mini</td>
<td>200K</td>
<td>$1.10 &#x2F; $4.40</td>
<td>Fast reasoning, efficient</td>
</tr>
<tr>
<td>o4 Mini High</td>
<td>200K</td>
<td>$1.10 &#x2F; $4.40</td>
<td>Same per-token price, higher reasoning compute budget</td>
</tr>
</tbody></table>
<p>O-series models use hidden “reasoning tokens” billed as output. A 500-token visible response may cost 2,000+ tokens total.</p>
<p><strong>GPT-4.1 series (still available):</strong></p>
<table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>GPT-4.1</td>
<td>1M</td>
<td>$2.00 &#x2F; $8.00</td>
<td>Instruction following, tool calling</td>
</tr>
<tr>
<td>GPT-4.1 Mini</td>
<td>1M</td>
<td>$0.40 &#x2F; $1.60</td>
<td>Same strengths, lower cost</td>
</tr>
<tr>
<td>GPT-4.1 Nano</td>
<td>1M</td>
<td>$0.10 &#x2F; $0.40</td>
<td>Cheapest in the lineup</td>
</tr>
</tbody></table>
<p>All models accept text and image input. Batch API available at 50% off across all models.</p>
<h3 id="Subscriptions-1"><a href="#Subscriptions-1" class="headerlink" title="Subscriptions"></a>Subscriptions</h3><p><strong>Individual plans:</strong></p>
<table>
<thead>
<tr>
<th>Tier</th>
<th>Price</th>
<th>Models</th>
<th>Message limits</th>
<th>Key features</th>
</tr>
</thead>
<tbody><tr>
<td>Free</td>
<td>$0</td>
<td>GPT-5.2 Instant (lightweight)</td>
<td>~10 per 5 hours, falls back to Mini</td>
<td>Basic DALL-E (2-3 images&#x2F;day), limited Sora 2, limited Deep Research, basic voice, can use GPTs but not create them</td>
</tr>
<tr>
<td>ChatGPT Go</td>
<td>$8&#x2F;mo</td>
<td>GPT-5.2 Instant (unlimited)</td>
<td>~10x Free</td>
<td>DALL-E, file upload, code interpreter, web browsing, create custom GPTs. No Sora, no Deep Research, no Agent Mode, no Codex</td>
</tr>
<tr>
<td>ChatGPT Plus</td>
<td>$20&#x2F;mo</td>
<td>GPT-5.2, GPT-5.2 Thinking, o3-pro</td>
<td>~3,000&#x2F;week (Thinking)</td>
<td>DALL-E (~180 images&#x2F;day), Sora (1,000 credits&#x2F;mo, 720p, 20s clips), Deep Research, Agent Mode, Codex, Canvas, Tasks, Advanced Voice (video&#x2F;screen sharing), 32K context</td>
</tr>
<tr>
<td>ChatGPT Pro</td>
<td>$200&#x2F;mo</td>
<td>All Plus models + GPT-5.2 Pro</td>
<td>Effectively unlimited</td>
<td>Unlimited DALL-E, Sora (10,000 credits + unlimited relaxed mode, 1080p&#x2F;4K, 90s clips, no watermark), 128K context (4x Plus), ChatGPT Pulse (exclusive), early access to new features</td>
</tr>
</tbody></table>
<p><strong>Business plans:</strong></p>
<table>
<thead>
<tr>
<th>Tier</th>
<th>Price</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Team</td>
<td>$25-30&#x2F;user&#x2F;mo</td>
<td>Same as Plus models + higher Thinking caps, admin console, conversations NOT used for training, shared workspace GPTs</td>
</tr>
<tr>
<td>Enterprise</td>
<td>Custom</td>
<td>Unlimited higher-speed GPT-5.2, extended context, SSO, SCIM, SOC 2 + ISO compliance, custom data retention, not trained on</td>
</tr>
</tbody></table>
<p>None of these include API credits. See <a href="https://chatgpt.com/pricing/">ChatGPT plans</a> for details. API is a completely separate billing system at <a href="https://platform.openai.com/">platform.openai.com</a>.</p>
<h3 id="Free-tier-API-1"><a href="#Free-tier-API-1" class="headerlink" title="Free tier (API)"></a>Free tier (API)</h3><p>The free trial credit program ($5) was discontinued in mid-2025. Reports conflict on whether new accounts still receive credits. Check <a href="https://platform.openai.com/">platform.openai.com</a> directly. Pay-as-you-go with no minimum spend once you add a payment method.</p>
<h3 id="Open-source-1"><a href="#Open-source-1" class="headerlink" title="Open source"></a>Open source</h3><p>OpenAI released two open-weight models under <strong>Apache 2.0</strong>:</p>
<ul>
<li><strong>GPT-OSS-120B</strong>: ~117B params (5.1B active via MoE), runs on a single 80GB GPU. Matches o4-mini on reasoning.</li>
<li><strong>GPT-OSS-20B</strong>: Runs on 16GB memory. Matches o3-mini.</li>
</ul>
<p>Both on <a href="https://huggingface.co/openai">Hugging Face</a> with MXFP4 quantization.</p>
<h3 id="Mobile-SDK-1"><a href="#Mobile-SDK-1" class="headerlink" title="Mobile SDK"></a>Mobile SDK</h3><p>No official native iOS&#x2F;Android SDK. The API is standard REST&#x2F;HTTP. Community options:</p>
<ul>
<li><a href="https://github.com/MacPaw/OpenAI">MacPaw&#x2F;OpenAI</a> – third-party Swift package for iOS&#x2F;macOS</li>
</ul>
<p>OpenAI also offers an <strong>Apps SDK</strong> (preview) for building apps that run inside ChatGPT, and a <strong>Realtime API</strong> via WebRTC for client-side voice&#x2F;text streaming.</p>
<h3 id="Links-1"><a href="#Links-1" class="headerlink" title="Links"></a>Links</h3><ul>
<li><a href="https://chatgpt.com/pricing/">ChatGPT plans</a></li>
<li><a href="https://openai.com/api/pricing/">API pricing</a></li>
<li><a href="https://developers.openai.com/api/docs/models">Models docs</a></li>
<li><a href="https://openai.com/open-models/">Open models</a></li>
</ul>
<hr>
<h2 id="Anthropic-Claude"><a href="#Anthropic-Claude" class="headerlink" title="Anthropic Claude"></a>Anthropic Claude</h2><p><a href="https://claude.ai/">claude.ai</a> | <a href="https://console.anthropic.com/">console.anthropic.com</a> | <a href="https://platform.claude.com/docs">API docs</a></p>
<p>The smallest model lineup but strong across the board. Known for instruction following, safety, and coding quality.</p>
<h3 id="Models-2"><a href="#Models-2" class="headerlink" title="Models"></a>Models</h3><table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Claude Opus 4.6</td>
<td>200K (1M beta)</td>
<td>$5 &#x2F; $25</td>
<td>Most intelligent; agents, coding, complex reasoning</td>
</tr>
<tr>
<td>Claude Sonnet 4.6</td>
<td>200K (1M beta)</td>
<td>$3 &#x2F; $15</td>
<td>Best balance of speed and intelligence</td>
</tr>
<tr>
<td>Claude Haiku 4.5</td>
<td>200K</td>
<td>$1 &#x2F; $5</td>
<td>Fastest, near-frontier intelligence</td>
</tr>
</tbody></table>
<p>All models support text + image input and PDF processing (up to 100 images per request). Extended thinking available on all three. 1M context in beta for Opus and Sonnet (via <code>context-1m-2025-08-07</code> header).</p>
<p><strong>Prompt caching</strong>: Cache hits cost 0.1x input price (90% savings). Cache write costs 1.25x-2x depending on TTL (5 min or 1 hour).</p>
<p><strong>Batch API</strong>: 50% discount on all models.</p>
<h3 id="Subscriptions-2"><a href="#Subscriptions-2" class="headerlink" title="Subscriptions"></a>Subscriptions</h3><p><strong>Individual plans:</strong></p>
<table>
<thead>
<tr>
<th>Tier</th>
<th>Price</th>
<th>Models</th>
<th>Usage</th>
<th>Key features</th>
</tr>
</thead>
<tbody><tr>
<td>Free</td>
<td>$0</td>
<td>Sonnet 4.6</td>
<td>~15-40 msgs per 5-hour window</td>
<td>200K context, up to 20 file uploads (30 MB each), web search, Projects, Artifacts. No Extended Thinking, no Computer Use, no Claude Code, no Opus. Conversations may be used for training</td>
</tr>
<tr>
<td>Pro</td>
<td>$20&#x2F;mo ($17 annual)</td>
<td>Sonnet 4.6, Opus 4.5, Opus 4.6, Haiku 4.5</td>
<td>5x Free (~75-200 per 5 hours)</td>
<td>Extended Thinking, Claude Code, Cowork, unlimited Projects, limited Computer Use, priority access. NOT used for training</td>
</tr>
<tr>
<td>Max 5x</td>
<td>$100&#x2F;mo</td>
<td>All models including Opus 4.6</td>
<td>5x Pro &#x2F; 25x Free</td>
<td>Higher Extended Thinking compute, Computer Use, 200K context (1M beta, 128K max output)</td>
</tr>
<tr>
<td>Max 20x</td>
<td>$200&#x2F;mo</td>
<td>All models including Opus 4.6</td>
<td>20x Pro &#x2F; 100x Free</td>
<td>Everything in Max 5x with 4x the usage ceiling. Best for heavy developers needing near-unlimited Opus 4.6</td>
</tr>
</tbody></table>
<p><strong>Business plans:</strong></p>
<table>
<thead>
<tr>
<th>Tier</th>
<th>Price</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Team Standard</td>
<td>$25&#x2F;seat&#x2F;mo</td>
<td>Same as Pro, shared project folders, admin console, SSO, domain capture. Min 5 seats. NOT trained on</td>
</tr>
<tr>
<td>Team Premium</td>
<td>$150&#x2F;seat&#x2F;mo</td>
<td>Everything in Standard + Claude Code included</td>
</tr>
<tr>
<td>Enterprise</td>
<td>Custom</td>
<td>Fine-grained RBAC, SCIM provisioning, SAML 2.0 SSO, compliance API, audit logs, custom data retention, usage-based billing with admin spending caps, Claude Code included</td>
</tr>
</tbody></table>
<p>See <a href="https://claude.com/pricing">Claude plans</a> for details. API is separate at <a href="https://console.anthropic.com/">console.anthropic.com</a>.</p>
<h3 id="Free-tier-API-2"><a href="#Free-tier-API-2" class="headerlink" title="Free tier (API)"></a>Free tier (API)</h3><p>$5 in free credits upon creating a developer account (phone verification required, region-dependent). After that, pure pay-as-you-go with tier-based rate limits.</p>
<h3 id="Open-source-2"><a href="#Open-source-2" class="headerlink" title="Open source"></a>Open source</h3><p>Not open source. No model weights released. Anthropic has been explicit about this as a safety position. The <a href="https://modelcontextprotocol.io/">Model Context Protocol (MCP)</a> is open source.</p>
<h3 id="Mobile-SDK-2"><a href="#Mobile-SDK-2" class="headerlink" title="Mobile SDK"></a>Mobile SDK</h3><p>No official Swift or Kotlin SDK. Seven official server-side SDKs: Python, TypeScript, Java, Go, Ruby, C#, PHP. Community mobile options:</p>
<ul>
<li><a href="https://github.com/jamesrochabrun/SwiftAnthropic">SwiftAnthropic</a> – third-party Swift package</li>
<li><a href="https://guitaripod.github.io/AnthropicKit/">AnthropicKit</a> – third-party Swift SDK</li>
</ul>
<h3 id="Links-2"><a href="#Links-2" class="headerlink" title="Links"></a>Links</h3><ul>
<li><a href="https://claude.com/pricing">Claude plans</a></li>
<li><a href="https://platform.claude.com/docs/en/about-claude/pricing">API pricing</a></li>
<li><a href="https://platform.claude.com/docs/en/about-claude/models/overview">Models overview</a></li>
<li><a href="https://platform.claude.com/docs/en/api/client-sdks">Client SDKs</a></li>
</ul>
<hr>
<h2 id="xAI-Grok"><a href="#xAI-Grok" class="headerlink" title="xAI (Grok)"></a>xAI (Grok)</h2><p><a href="https://grok.com/">grok.com</a> | <a href="https://x.ai/api">x.ai&#x2F;api</a> | <a href="https://docs.x.ai/">API docs</a></p>
<p>Largest context window (2M tokens) and aggressive sign-up credits. Built into X (Twitter) with real-time social data access.</p>
<h3 id="Models-3"><a href="#Models-3" class="headerlink" title="Models"></a>Models</h3><table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Grok 4.20 Multi-Agent</td>
<td>2M</td>
<td>$2.00 &#x2F; $6.00</td>
<td>Multi-agent orchestration</td>
</tr>
<tr>
<td>Grok 4.20 (reasoning)</td>
<td>2M</td>
<td>$2.00 &#x2F; $6.00</td>
<td>Flagship reasoning</td>
</tr>
<tr>
<td>Grok 4.1 Fast (reasoning)</td>
<td>2M</td>
<td>$0.20 &#x2F; $0.50</td>
<td>Best value</td>
</tr>
<tr>
<td>Grok Code Fast 1</td>
<td>256K</td>
<td>$0.20 &#x2F; $1.50</td>
<td>Code-specialized</td>
</tr>
</tbody></table>
<p>Image input supported on Grok 4.x models. Image generation ($0.02-0.07&#x2F;image) and video generation ($0.05&#x2F;second) available as separate endpoints.</p>
<h3 id="Subscriptions-3"><a href="#Subscriptions-3" class="headerlink" title="Subscriptions"></a>Subscriptions</h3><table>
<thead>
<tr>
<th>Tier</th>
<th>Price</th>
<th>Models</th>
<th>Limits</th>
<th>Key features</th>
</tr>
</thead>
<tbody><tr>
<td>Free</td>
<td>$0</td>
<td>Grok 3 (limited)</td>
<td>~10 msgs per 2 hours</td>
<td>~10 Aurora images per 2 hours, basic voice. No DeepSearch, no Think mode</td>
</tr>
<tr>
<td>X Premium</td>
<td>$8&#x2F;mo</td>
<td>Grok 3, limited Grok 4</td>
<td>Higher than Free</td>
<td>Image gen, voice, basic memory. Bundled with blue checkmark + reduced ads. 50% off SuperGrok</td>
</tr>
<tr>
<td>X Premium+</td>
<td>$40&#x2F;mo</td>
<td>Grok 4, Grok 4.1</td>
<td>~100 prompts per 2 hours</td>
<td>Limited DeepSearch, limited Think (~30 per 2 hours), extended voice + memory. Bundled with ad revenue sharing + ad-free browsing. 50% off SuperGrok</td>
</tr>
<tr>
<td>SuperGrok</td>
<td>$30&#x2F;mo</td>
<td>Grok 4, Grok 4.1</td>
<td>~30 per 2 hours (Think&#x2F;DeepSearch)</td>
<td>128K context memory, full DeepSearch, Big Brain Mode (extended reasoning), Imagine (image gen + 720p 6s video), Aurora image editing, priority routing</td>
</tr>
<tr>
<td>SuperGrok Heavy</td>
<td>$300&#x2F;mo</td>
<td>Grok 4 Heavy (multi-agent)</td>
<td>Dramatically higher</td>
<td>256K context (2x SuperGrok), maximum compute priority, 100% AIME 2025, Grok 4 Heavy exclusive</td>
</tr>
<tr>
<td>Grok Business</td>
<td>$30-300&#x2F;seat&#x2F;mo</td>
<td>Standard or Heavy</td>
<td>Team-managed</td>
<td>Admin controls, API access included</td>
</tr>
</tbody></table>
<p>See <a href="https://grok.com/plans">Grok plans</a> for details. API is separate pay-as-you-go for non-Business tiers. $25 in free sign-up credits.</p>
<h3 id="Open-source-3"><a href="#Open-source-3" class="headerlink" title="Open source"></a>Open source</h3><p>Grok 1 and Grok 2.5 weights are on <a href="https://huggingface.co/xai-org/grok-2">Hugging Face</a>. Grok 3 open-source planned. Current frontier models (4.x) are proprietary.</p>
<h3 id="Mobile-SDK-3"><a href="#Mobile-SDK-3" class="headerlink" title="Mobile SDK"></a>Mobile SDK</h3><p>No dedicated SDK. The API is OpenAI-compatible, so any OpenAI client library works. Grok mobile app available on iOS and Android.</p>
<h3 id="Links-3"><a href="#Links-3" class="headerlink" title="Links"></a>Links</h3><ul>
<li><a href="https://grok.com/plans">Grok plans</a></li>
<li><a href="https://x.ai/api">API pricing</a></li>
<li><a href="https://docs.x.ai/developers/models">Models docs</a></li>
</ul>
<hr>
<h2 id="DeepSeek"><a href="#DeepSeek" class="headerlink" title="DeepSeek"></a>DeepSeek</h2><p><a href="https://chat.deepseek.com/">chat.deepseek.com</a> | <a href="https://platform.deepseek.com/">platform.deepseek.com</a> | <a href="https://api-docs.deepseek.com/">API docs</a></p>
<p>The price disruptor. V3.2 costs roughly 95% less than GPT-4 Turbo with competitive quality.</p>
<h3 id="Models-4"><a href="#Models-4" class="headerlink" title="Models"></a>Models</h3><table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Cache hit</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>deepseek-chat (V3.2)</td>
<td>128K</td>
<td>$0.28 &#x2F; $0.42</td>
<td>$0.028</td>
<td>Non-thinking, 8K max output</td>
</tr>
<tr>
<td>deepseek-reasoner (V3.2)</td>
<td>128K</td>
<td>$0.28 &#x2F; $0.42</td>
<td>$0.028</td>
<td>Thinking mode, 64K max output</td>
</tr>
</tbody></table>
<p>Text-only as of March 2026. V4 with multimodal is in development.</p>
<p>Off-peak discount: 50-75% off during 16:30-00:30 GMT.</p>
<h3 id="Subscriptions-4"><a href="#Subscriptions-4" class="headerlink" title="Subscriptions"></a>Subscriptions</h3><p>No subscription model. The <a href="https://chat.deepseek.com/">chat app</a> (web + mobile) is completely <strong>free</strong> with unlimited messages within daily quotas. No paid tier exists – they just give you the models for free. API is pure pay-as-you-go at <a href="https://platform.deepseek.com/">platform.deepseek.com</a>. New accounts get 5M free tokens (30-day validity).</p>
<h3 id="Open-source-4"><a href="#Open-source-4" class="headerlink" title="Open source"></a>Open source</h3><p><strong>Fully open source under MIT license.</strong> Weights on <a href="https://huggingface.co/deepseek-ai">Hugging Face</a> for V3.2 and R1. Self-host on your own hardware. A quantized 7B version runs on Android.</p>
<h3 id="Mobile-SDK-4"><a href="#Mobile-SDK-4" class="headerlink" title="Mobile SDK"></a>Mobile SDK</h3><p>No dedicated SDK. Official apps on iOS and Android (free). The API is OpenAI-compatible.</p>
<h3 id="Links-4"><a href="#Links-4" class="headerlink" title="Links"></a>Links</h3><ul>
<li><a href="https://chat.deepseek.com/">Chat app</a> (free)</li>
<li><a href="https://api-docs.deepseek.com/quick_start/pricing">API pricing</a></li>
<li><a href="https://api-docs.deepseek.com/">API docs</a></li>
</ul>
<hr>
<h2 id="Mistral-AI"><a href="#Mistral-AI" class="headerlink" title="Mistral AI"></a>Mistral AI</h2><p><a href="https://chat.mistral.ai/">chat.mistral.ai</a> | <a href="https://console.mistral.ai/">console.mistral.ai</a> | <a href="https://docs.mistral.ai/">API docs</a></p>
<p>European provider with the widest range from ultra-cheap (Nemo at $0.02&#x2F;M) to frontier.</p>
<h3 id="Models-5"><a href="#Models-5" class="headerlink" title="Models"></a>Models</h3><table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Mistral Large 3</td>
<td>128K</td>
<td>$0.50 &#x2F; $1.50</td>
<td>Flagship</td>
</tr>
<tr>
<td>Mistral Medium 3</td>
<td>128K</td>
<td>$0.40 &#x2F; $2.00</td>
<td>Mid-tier</td>
</tr>
<tr>
<td>Mistral Small 3.1 (24B)</td>
<td>128K</td>
<td>$0.35 &#x2F; $0.56</td>
<td>Efficient</td>
</tr>
<tr>
<td>Mistral Nemo</td>
<td>128K</td>
<td>$0.02 &#x2F; $0.04</td>
<td>Ultra-budget</td>
</tr>
<tr>
<td>Codestral</td>
<td>256K</td>
<td>Varies</td>
<td>Code specialist (22B, 80+ languages)</td>
</tr>
<tr>
<td>Devstral 2</td>
<td>128K</td>
<td>$0.40 &#x2F; $2.00</td>
<td>Powers Vibe 2.0 coding agent</td>
</tr>
<tr>
<td>Pixtral Large</td>
<td>128K</td>
<td>Varies</td>
<td>Multimodal (124B, text + image, up to 30 images)</td>
</tr>
</tbody></table>
<h3 id="Subscriptions-Le-Chat"><a href="#Subscriptions-Le-Chat" class="headerlink" title="Subscriptions (Le Chat)"></a>Subscriptions (Le Chat)</h3><table>
<thead>
<tr>
<th>Tier</th>
<th>Price</th>
<th>Limits</th>
<th>Key features</th>
</tr>
</thead>
<tbody><tr>
<td>Free</td>
<td>$0</td>
<td>~25 msgs before throttling</td>
<td>128K context, code interpreter, document uploads, web search (AFP-verified), Canvas (web pages&#x2F;graphs&#x2F;presentations), image gen. Prompts may be used for training</td>
</tr>
<tr>
<td>Pro</td>
<td>$14.99&#x2F;mo</td>
<td>~6x Free (soft cap, fair use)</td>
<td>150 Flash Answers&#x2F;day (ultra-fast, 1,000 words&#x2F;sec), Vibe 2.0 coding agent, No Telemetry Mode (never trained on), priority responses, early feature access</td>
</tr>
<tr>
<td>Student</td>
<td>$5.99&#x2F;mo</td>
<td>Same as Pro</td>
<td>&gt;50% discount, verified students, 12-month validity</td>
</tr>
<tr>
<td>Team</td>
<td>$24.99&#x2F;user&#x2F;mo</td>
<td>Pro per seat</td>
<td>30 GB&#x2F;user shared RAG libraries, admin console, data training opt-out default</td>
</tr>
<tr>
<td>Enterprise</td>
<td>Custom</td>
<td>Custom</td>
<td>On-prem deployment, custom models, multi-step agent pipelines</td>
</tr>
</tbody></table>
<p>See <a href="https://mistral.ai/pricing">Le Chat pricing</a> for details. API is separate pay-as-you-go at <a href="https://console.mistral.ai/">console.mistral.ai</a>.</p>
<h3 id="Open-source-5"><a href="#Open-source-5" class="headerlink" title="Open source"></a>Open source</h3><p>Mixed. <strong>Apache 2.0</strong>: Mistral 7B, Mixtral 8x7B, Mistral Nemo, Mistral Small 3. <strong>Non-production license</strong>: Codestral. <strong>Proprietary</strong>: Large, Medium, Pixtral Large.</p>
<h3 id="Mobile-SDK-5"><a href="#Mobile-SDK-5" class="headerlink" title="Mobile SDK"></a>Mobile SDK</h3><p>No dedicated SDK. Python and JavaScript&#x2F;TypeScript client libraries available.</p>
<h3 id="Links-5"><a href="#Links-5" class="headerlink" title="Links"></a>Links</h3><ul>
<li><a href="https://mistral.ai/pricing">Le Chat pricing</a></li>
<li><a href="https://docs.mistral.ai/">API docs</a></li>
<li><a href="https://docs.mistral.ai/getting-started/models">Models</a></li>
</ul>
<hr>
<h2 id="Meta-Llama"><a href="#Meta-Llama" class="headerlink" title="Meta Llama"></a>Meta Llama</h2><p><a href="https://www.llama.com/">llama.com</a> | <a href="https://huggingface.co/meta-llama">Hugging Face</a></p>
<p>Not a service – it’s a family of models you can run anywhere. Free weights, massive ecosystem.</p>
<h3 id="Models-6"><a href="#Models-6" class="headerlink" title="Models"></a>Models</h3><table>
<thead>
<tr>
<th>Model</th>
<th>Params (Active&#x2F;Total)</th>
<th>Context</th>
<th>Multimodal</th>
</tr>
</thead>
<tbody><tr>
<td>Llama 4 Scout</td>
<td>17B &#x2F; 109B (16 experts)</td>
<td><strong>10M</strong></td>
<td>Text, image, video</td>
</tr>
<tr>
<td>Llama 4 Maverick</td>
<td>17B &#x2F; 400B (128 experts)</td>
<td><strong>1M</strong></td>
<td>Text, image, video</td>
</tr>
<tr>
<td>Llama 4 Behemoth</td>
<td>288B &#x2F; 2T (16 experts)</td>
<td>TBD</td>
<td>Yes (still training)</td>
</tr>
<tr>
<td>Llama 3.3 70B</td>
<td>70B</td>
<td>128K</td>
<td>Text only</td>
</tr>
<tr>
<td>Llama 3.1 8B</td>
<td>8B</td>
<td>128K</td>
<td>Text only</td>
</tr>
</tbody></table>
<p>Scout’s 10M token context is the longest of any open model. Scout fits on a single H100 with INT4 quantization.</p>
<h3 id="API-pricing-via-providers"><a href="#API-pricing-via-providers" class="headerlink" title="API pricing (via providers)"></a>API pricing (via providers)</h3><table>
<thead>
<tr>
<th>Provider</th>
<th>Llama 4 Scout</th>
<th>Llama 3.3 70B</th>
<th>Speed</th>
</tr>
</thead>
<tbody><tr>
<td>Groq</td>
<td>$0.11 &#x2F; $0.34</td>
<td>$0.59 &#x2F; $0.79</td>
<td>594 t&#x2F;s</td>
</tr>
<tr>
<td>Together AI</td>
<td>–</td>
<td>~$0.88 &#x2F; $0.88</td>
<td>–</td>
</tr>
<tr>
<td>DeepInfra</td>
<td>–</td>
<td>~$0.15 &#x2F; $0.15</td>
<td>Budget</td>
</tr>
<tr>
<td>Self-hosted</td>
<td>Free</td>
<td>Free</td>
<td>You pay compute</td>
</tr>
</tbody></table>
<p>Prices vary up to 6.8x across providers for the same model.</p>
<h3 id="Open-source-6"><a href="#Open-source-6" class="headerlink" title="Open source"></a>Open source</h3><p><strong>Yes.</strong> Llama Community License – free for commercial use (companies under 700M MAU). Weights on <a href="https://huggingface.co/meta-llama">Hugging Face</a> and <a href="https://www.llama.com/">llama.com</a>.</p>
<h3 id="Mobile-SDK-6"><a href="#Mobile-SDK-6" class="headerlink" title="Mobile SDK"></a>Mobile SDK</h3><p>No official SDK. On-device inference via llama.cpp, MLX, ONNX. Llama 3.1 8B and Scout can run locally on capable devices.</p>
<h3 id="Links-6"><a href="#Links-6" class="headerlink" title="Links"></a>Links</h3><ul>
<li><a href="https://www.llama.com/">Llama official site</a></li>
<li><a href="https://huggingface.co/meta-llama">Hugging Face</a></li>
<li><a href="https://ai.meta.com/blog/llama-4-multimodal-intelligence/">Llama 4 announcement</a></li>
</ul>
<hr>
<h2 id="Alibaba-Qwen"><a href="#Alibaba-Qwen" class="headerlink" title="Alibaba Qwen"></a>Alibaba Qwen</h2><p><a href="https://qwen.ai/">qwen.ai</a> | <a href="https://chat.qwen.ai/">chat.qwen.ai</a> | <a href="https://qwen.ai/apiplatform">API platform</a> | <a href="https://www.alibabacloud.com/help/en/model-studio/qwen-api-reference/">API docs</a></p>
<p>China’s strongest open-source model family. Massive lineup from 0.8B to 480B, all Apache 2.0. The chat app is completely free.</p>
<h3 id="Models-7"><a href="#Models-7" class="headerlink" title="Models"></a>Models</h3><p><strong>Text generation (API):</strong></p>
<table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Qwen3-Max (235B MoE)</td>
<td>262K</td>
<td>$1.20 &#x2F; $6.00</td>
<td>Flagship reasoning</td>
</tr>
<tr>
<td>Qwen3-Max Thinking</td>
<td>262K</td>
<td>$0.78 &#x2F; $3.90</td>
<td>Thinking mode, flat rate</td>
</tr>
<tr>
<td>Qwen3.5-Plus (MoE)</td>
<td>1M</td>
<td>$0.26 &#x2F; $1.56</td>
<td>Balanced performance</td>
</tr>
<tr>
<td>Qwen3.5-Flash (MoE)</td>
<td>1M</td>
<td>$0.10 &#x2F; $0.40</td>
<td>Fast and cheap</td>
</tr>
<tr>
<td>QwQ-Plus (32B reasoning)</td>
<td>131K</td>
<td>$0.80 &#x2F; $2.40</td>
<td>Reasoning specialist</td>
</tr>
<tr>
<td>Qwen-Long</td>
<td><strong>10M</strong></td>
<td>Tiered</td>
<td>Ultra-long document analysis</td>
</tr>
</tbody></table>
<p><strong>Coding models:</strong></p>
<table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Qwen3-Coder-Plus (480B MoE)</td>
<td>1M</td>
<td>$0.65 &#x2F; $3.25</td>
<td>Flagship coding, SWE-bench leading</td>
</tr>
<tr>
<td>Qwen3-Coder-Next (80B MoE)</td>
<td>262K</td>
<td>$0.07 &#x2F; $0.30</td>
<td>Open-weight (Apache 2.0), hybrid attention</td>
</tr>
</tbody></table>
<p><strong>Vision &#x2F; multimodal</strong>: Qwen3-VL-Plus (262K), QVQ-Max (visual reasoning), Qwen3-Omni-Flash (audio + video + text). <strong>Specialized</strong>: Qwen3-Deep-Research (1M), Qwen-OCR, Qwen-MT (translation, 92 languages).</p>
<p>Tiered pricing: rates increase as input length grows (0-32K cheapest, 128K+ most expensive). Batch API: 50% off.</p>
<h3 id="Open-weight-models"><a href="#Open-weight-models" class="headerlink" title="Open-weight models"></a>Open-weight models</h3><table>
<thead>
<tr>
<th>Model</th>
<th>Params (Active)</th>
<th>Context</th>
<th>License</th>
</tr>
</thead>
<tbody><tr>
<td>Qwen3.5-397B-A17B</td>
<td>397B (17B)</td>
<td>262K</td>
<td>Apache 2.0</td>
</tr>
<tr>
<td>Qwen3.5-122B-A10B</td>
<td>122B (10B)</td>
<td>262K</td>
<td>Apache 2.0</td>
</tr>
<tr>
<td>Qwen3.5-27B (dense)</td>
<td>27B</td>
<td>262K</td>
<td>Apache 2.0</td>
</tr>
<tr>
<td>Qwen3.5-9B (dense)</td>
<td>9B</td>
<td>262K</td>
<td>Apache 2.0</td>
</tr>
<tr>
<td>Qwen3.5-4B (dense)</td>
<td>4B</td>
<td>262K</td>
<td>Apache 2.0</td>
</tr>
<tr>
<td>Qwen3.5-2B (dense)</td>
<td>2B</td>
<td>262K</td>
<td>Apache 2.0</td>
</tr>
<tr>
<td>Qwen3.5-0.8B (dense)</td>
<td>0.8B</td>
<td>262K</td>
<td>Apache 2.0</td>
</tr>
<tr>
<td>QwQ-32B</td>
<td>32B</td>
<td>32K</td>
<td>Apache 2.0</td>
</tr>
</tbody></table>
<p>All on <a href="https://huggingface.co/Qwen">Hugging Face</a> and <a href="https://modelscope.cn/organization/qwen">ModelScope</a>. Apache 2.0 &#x3D; full commercial use.</p>
<h3 id="Subscriptions-5"><a href="#Subscriptions-5" class="headerlink" title="Subscriptions"></a>Subscriptions</h3><p><strong>Qwen Chat</strong> (web, iOS, Android, desktop) is completely <strong>free</strong> – full access to Qwen3, image gen, video understanding, deep thinking, web search, artifacts. No paid tier.</p>
<p><strong>Coding Plan</strong> (developer subscription, Feb 2026): Flat-rate alternative to per-token billing for coding workflows.</p>
<table>
<thead>
<tr>
<th>Tier</th>
<th>Price</th>
<th>Requests&#x2F;month</th>
<th>Works with</th>
</tr>
</thead>
<tbody><tr>
<td>Lite</td>
<td>~$10&#x2F;mo</td>
<td>18,000</td>
<td>Claude Code, Qwen Code, Cline, Cursor</td>
</tr>
<tr>
<td>Pro</td>
<td>~$50&#x2F;mo</td>
<td>90,000</td>
<td>Same + higher rate limits</td>
</tr>
</tbody></table>
<h3 id="Free-tier-API-3"><a href="#Free-tier-API-3" class="headerlink" title="Free tier (API)"></a>Free tier (API)</h3><p>New accounts: 1M tokens free per model (90-day validity). <a href="https://github.com/QwenLM/qwen-code">Qwen Code</a> OAuth: 1,000 free requests&#x2F;day with no billing setup.</p>
<h3 id="Open-source-7"><a href="#Open-source-7" class="headerlink" title="Open source"></a>Open source</h3><p><strong>Yes.</strong> All Qwen3 and Qwen3.5 open-weight models are <strong>Apache 2.0</strong> – full commercial use, no restrictions. Proprietary models (Qwen-Max, QwQ-Plus) are API-only.</p>
<h3 id="Mobile-SDK-7"><a href="#Mobile-SDK-7" class="headerlink" title="Mobile SDK"></a>Mobile SDK</h3><p>No official SDK. On-device via llama.cpp, MLX, Ollama, ONNX. The 0.8B-4B models are designed for on-device use. Qwen3.5-2B runs at 30-50 t&#x2F;s on iPhone&#x2F;M-series Macs via MLX.</p>
<h3 id="Links-7"><a href="#Links-7" class="headerlink" title="Links"></a>Links</h3><ul>
<li><a href="https://chat.qwen.ai/">Qwen Chat</a> (free)</li>
<li><a href="https://www.alibabacloud.com/help/en/model-studio/model-pricing">API pricing</a></li>
<li><a href="https://www.alibabacloud.com/help/en/model-studio/models">Models list</a></li>
<li><a href="https://www.alibabacloud.com/help/en/model-studio/coding-plan">Coding Plan</a></li>
<li><a href="https://github.com/QwenLM">GitHub</a></li>
<li><a href="https://huggingface.co/Qwen">Hugging Face</a></li>
</ul>
<hr>
<h2 id="Amazon-Nova"><a href="#Amazon-Nova" class="headerlink" title="Amazon Nova"></a>Amazon Nova</h2><p><a href="https://aws.amazon.com/nova/models/">aws.amazon.com&#x2F;nova</a> | <a href="https://nova.amazon.com/">nova.amazon.com</a> | <a href="https://aws.amazon.com/bedrock/pricing/">Bedrock pricing</a></p>
<p>AWS’s in-house model family. Cheapest token pricing in the industry at the low end (Nova Micro $0.035&#x2F;1M input). Consumer chat launched at <a href="https://nova.amazon.com/">nova.amazon.com</a>.</p>
<h3 id="Models-8"><a href="#Models-8" class="headerlink" title="Models"></a>Models</h3><p><strong>Nova 1 (understanding):</strong></p>
<table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Nova Micro</td>
<td>128K</td>
<td>$0.035 &#x2F; $0.14</td>
<td>Text only, lowest cost</td>
</tr>
<tr>
<td>Nova Lite</td>
<td>300K</td>
<td>$0.06 &#x2F; $0.24</td>
<td>Text + image + video</td>
</tr>
<tr>
<td>Nova Pro</td>
<td>300K</td>
<td>$0.80 &#x2F; $3.20</td>
<td>Best accuracy&#x2F;cost balance</td>
</tr>
<tr>
<td>Nova Premier</td>
<td>1M</td>
<td>$2.50 &#x2F; $12.50</td>
<td>Most capable, 200+ languages</td>
</tr>
</tbody></table>
<p><strong>Nova 2 (reasoning, Dec 2025):</strong></p>
<table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Nova 2 Lite</td>
<td>1M</td>
<td>Built-in code interpreter, web grounding, MCP support</td>
</tr>
<tr>
<td>Nova 2 Pro</td>
<td>1M</td>
<td>Advanced multi-step reasoning, agentic workflows (Preview)</td>
</tr>
<tr>
<td>Nova 2 Omni</td>
<td>–</td>
<td>All modalities (text, image, video, speech in&#x2F;out) (Preview)</td>
</tr>
</tbody></table>
<p><strong>Creative</strong>: Nova Canvas (image gen), Nova Reel (video gen, 1280x720 24fps). <strong>Other</strong>: Nova Sonic (speech-to-speech), Nova Act ($4.75&#x2F;agent-hour browser automation).</p>
<p>Batch API: 50% off on-demand pricing.</p>
<h3 id="Subscriptions-6"><a href="#Subscriptions-6" class="headerlink" title="Subscriptions"></a>Subscriptions</h3><p><strong><a href="https://nova.amazon.com/">Nova Chat</a></strong> – free consumer chat app. Uses your Amazon.com account (not AWS). Powered by Nova Pro. Image creation, code gen. US only at launch.</p>
<p><strong><a href="https://www.amazon.com/alexa-plus">Alexa+</a></strong> – consumer voice assistant upgraded with Nova models under the hood.</p>
<p>No traditional subscription tiers like ChatGPT&#x2F;Claude. Access is either free (Nova Chat) or pay-per-token (Bedrock API).</p>
<h3 id="Free-tier-API-4"><a href="#Free-tier-API-4" class="headerlink" title="Free tier (API)"></a>Free tier (API)</h3><p>No permanent free API tier. New AWS accounts get <strong>$200 in credits</strong> ($100 sign-up + $100 onboarding). Credits apply to all AWS services including Bedrock, valid for 6 months. At Nova Micro pricing, that’s ~5.7B input tokens.</p>
<h3 id="Open-source-8"><a href="#Open-source-8" class="headerlink" title="Open source"></a>Open source</h3><p><strong>No.</strong> All Nova models are proprietary. Weights not released. Amazon publishes detailed <a href="https://www.amazon.science/publications/the-amazon-nova-family-of-models-technical-report-and-model-card">technical reports</a> but not weights. Bedrock also hosts third-party open models (Llama, Mistral).</p>
<h3 id="Mobile-SDK-8"><a href="#Mobile-SDK-8" class="headerlink" title="Mobile SDK"></a>Mobile SDK</h3><p><a href="https://docs.amplify.aws/react/ai/concepts/models/">AWS Amplify AI Kit</a> for React Native and Android. Direct Bedrock API calls via AWS SDK with SigV4 signing. Also supported via <a href="https://ai-sdk.dev/providers/ai-sdk-providers/amazon-bedrock">Vercel AI SDK</a>. No first-party Swift SDK (use Objective-C bindings from Swift).</p>
<h3 id="Links-8"><a href="#Links-8" class="headerlink" title="Links"></a>Links</h3><ul>
<li><a href="https://aws.amazon.com/nova/models/">Nova models</a></li>
<li><a href="https://aws.amazon.com/nova/pricing/">Nova pricing</a></li>
<li><a href="https://aws.amazon.com/bedrock/pricing/">Bedrock pricing</a></li>
<li><a href="https://nova.amazon.com/">Nova Chat</a></li>
<li><a href="https://aws.amazon.com/q/developer/">Q Developer</a></li>
<li><a href="https://aws.amazon.com/q/developer/pricing/">Q Developer pricing</a></li>
</ul>
<hr>
<h2 id="Microsoft-Phi"><a href="#Microsoft-Phi" class="headerlink" title="Microsoft Phi"></a>Microsoft Phi</h2><p><a href="https://azure.microsoft.com/en-us/products/phi">azure.microsoft.com&#x2F;products&#x2F;phi</a> | <a href="https://github.com/features/models">GitHub Models</a> | <a href="https://azure.microsoft.com/en-us/pricing/details/ai-foundry-models/microsoft/">Azure AI pricing</a></p>
<p>Best small models in the industry. All MIT-licensed. Phi-4-mini at 3.8B params punches far above its weight.</p>
<h3 id="Models-9"><a href="#Models-9" class="headerlink" title="Models"></a>Models</h3><table>
<thead>
<tr>
<th>Model</th>
<th>Params</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Phi-4</td>
<td>14B</td>
<td>16K</td>
<td>$0.125 &#x2F; $0.50</td>
<td>Base reasoning&#x2F;instruction</td>
</tr>
<tr>
<td>Phi-4-mini</td>
<td>3.8B</td>
<td>128K</td>
<td>$0.075 &#x2F; $0.30</td>
<td>Grouped-query attention, function calling</td>
</tr>
<tr>
<td>Phi-4-multimodal</td>
<td>5.6B</td>
<td>128K</td>
<td>$0.08 &#x2F; $0.32</td>
<td>Text + vision + audio in one model</td>
</tr>
<tr>
<td>Phi-4-reasoning</td>
<td>14B</td>
<td>32K</td>
<td>–</td>
<td>Chain-of-thought fine-tune</td>
</tr>
<tr>
<td>Phi-4-reasoning-plus</td>
<td>14B</td>
<td>32K</td>
<td>–</td>
<td>Higher RL compute</td>
</tr>
<tr>
<td>Phi-4-reasoning-vision-15B</td>
<td>15B</td>
<td>32K</td>
<td>–</td>
<td>Multimodal reasoning (MIT, Mar 2026)</td>
</tr>
<tr>
<td>Phi-3.5-MoE</td>
<td>42B (6.6B active)</td>
<td>128K</td>
<td>$0.16 &#x2F; $0.64</td>
<td>16 experts, efficient inference</td>
</tr>
</tbody></table>
<p>Pricing is on Azure AI Foundry. Phi-4-mini is cheaper per token than GPT-4o-mini.</p>
<h3 id="Subscriptions-7"><a href="#Subscriptions-7" class="headerlink" title="Subscriptions"></a>Subscriptions</h3><p>Microsoft’s consumer AI is <strong>Copilot</strong>, not Phi directly:</p>
<table>
<thead>
<tr>
<th>Tier</th>
<th>Price</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Copilot Free</td>
<td>$0</td>
<td>GPT-5.x limited, Bing integration</td>
</tr>
<tr>
<td>Copilot Pro</td>
<td>$20&#x2F;mo</td>
<td>Priority GPT-5.x, M365 integration (Word, Excel, Outlook, Teams) for M365 subscribers</td>
</tr>
<tr>
<td>M365 Copilot Business</td>
<td>$18-25&#x2F;user&#x2F;mo</td>
<td>Full M365 Copilot in all business apps, meeting recap</td>
</tr>
<tr>
<td>M365 Copilot Enterprise</td>
<td>$30&#x2F;user&#x2F;mo</td>
<td>Compliance, audit, data residency, Copilot Studio</td>
</tr>
</tbody></table>
<p>Copilot Pro is the ~$20 tier. It’s an M365 productivity tool, not a general chatbot – useful if you’re deep in the Microsoft ecosystem.</p>
<h3 id="Free-tier-API-5"><a href="#Free-tier-API-5" class="headerlink" title="Free tier (API)"></a>Free tier (API)</h3><p><strong><a href="https://github.com/features/models">GitHub Models</a></strong> is the easiest entry point – free playground + API with rate limits (50-150 req&#x2F;day). Just needs a GitHub account, no credit card. Phi-4 and Phi-4-mini both available. Paid usage mirrors Azure AI Foundry rates.</p>
<p>Azure free account: $200 credits for 30 days.</p>
<h3 id="Open-source-9"><a href="#Open-source-9" class="headerlink" title="Open source"></a>Open source</h3><p><strong>Yes.</strong> All Phi models are <strong>MIT license</strong> – full commercial use, modification, distribution. Weights on <a href="https://huggingface.co/microsoft/phi-4">Hugging Face</a>. MAI-1 (Microsoft’s ~500B frontier model) is proprietary and not publicly available.</p>
<h3 id="Mobile-SDK-9"><a href="#Mobile-SDK-9" class="headerlink" title="Mobile SDK"></a>Mobile SDK</h3><p>No dedicated SDK. On-device via <strong>ONNX Runtime Mobile</strong> (iOS with CoreML, Android with NNAPI). Pre-quantized ONNX weights on Hugging Face. Phi-4-mini at 3.8B is feasible on devices with 6-8GB RAM.</p>
<h3 id="Links-9"><a href="#Links-9" class="headerlink" title="Links"></a>Links</h3><ul>
<li><a href="https://azure.microsoft.com/en-us/products/phi">Phi product page</a></li>
<li><a href="https://azure.microsoft.com/en-us/pricing/details/ai-foundry-models/microsoft/">Azure AI pricing</a></li>
<li><a href="https://huggingface.co/microsoft/phi-4">Phi-4 on Hugging Face</a></li>
<li><a href="https://github.com/features/models">GitHub Models</a></li>
<li><a href="https://github.com/features/copilot/plans">GitHub Copilot plans</a></li>
<li><a href="https://www.microsoft.com/en-us/microsoft-365-copilot/pricing">Copilot Pro</a></li>
</ul>
<hr>
<h2 id="Perplexity"><a href="#Perplexity" class="headerlink" title="Perplexity"></a>Perplexity</h2><p><a href="https://www.perplexity.ai/">perplexity.ai</a> | <a href="https://docs.perplexity.ai/">API docs</a> | <a href="https://www.perplexity.ai/api-platform">API platform</a></p>
<p>Search-first AI. Every response is grounded in web sources with citations. The Pro subscription includes access to third-party models (GPT-5.4, Claude, Gemini).</p>
<h3 id="Models-10"><a href="#Models-10" class="headerlink" title="Models"></a>Models</h3><table>
<thead>
<tr>
<th>Model</th>
<th>Context</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td>Sonar</td>
<td>128K</td>
<td>$1 &#x2F; $1 + per-req fee</td>
<td>Lightweight search + summarization</td>
</tr>
<tr>
<td>Sonar Pro</td>
<td>200K</td>
<td>$3 &#x2F; $15 + per-req fee</td>
<td>2x search results, deeper retrieval</td>
</tr>
<tr>
<td>Sonar Reasoning Pro</td>
<td>128K</td>
<td>$2 &#x2F; $8 + per-req fee</td>
<td>Chain-of-thought reasoning</td>
</tr>
<tr>
<td>Sonar Deep Research</td>
<td>128K</td>
<td>$2 &#x2F; $8 + per-req fee</td>
<td>Long-form synthesis, hundreds of sources</td>
</tr>
</tbody></table>
<p>Per-request fees: $5-14 per 1,000 requests depending on model and search context size. This hybrid pricing (tokens + requests) makes Perplexity more expensive per call than pure token-based providers.</p>
<p><strong>Search API</strong>: $5 per 1,000 requests (raw web results, no generation).</p>
<h3 id="Subscriptions-8"><a href="#Subscriptions-8" class="headerlink" title="Subscriptions"></a>Subscriptions</h3><table>
<thead>
<tr>
<th>Tier</th>
<th>Price</th>
<th>Key features</th>
</tr>
</thead>
<tbody><tr>
<td>Free</td>
<td>$0</td>
<td>Basic search, limited Pro Searches</td>
</tr>
<tr>
<td>Pro</td>
<td>$20&#x2F;mo ($200&#x2F;yr)</td>
<td>Unlimited Pro queries, file uploads, image&#x2F;video gen, third-party models (GPT-5.4, Claude Sonnet&#x2F;Opus, Gemini 3.1 Pro), $5&#x2F;mo API credits</td>
</tr>
<tr>
<td>Max</td>
<td>$200&#x2F;mo</td>
<td>Everything in Pro + Perplexity Computer (multi-agent), unlimited Labs, early access</td>
</tr>
<tr>
<td>Enterprise Pro</td>
<td>$40&#x2F;seat&#x2F;mo</td>
<td>Team features, SSO, shared Spaces, admin controls</td>
</tr>
</tbody></table>
<p><strong>Pro subscribers get $5&#x2F;month in API credits.</strong> Third-party model access in Pro means you can use GPT-5.4 and Claude Opus through Perplexity’s interface without separate subscriptions.</p>
<h3 id="Free-tier-API-6"><a href="#Free-tier-API-6" class="headerlink" title="Free tier (API)"></a>Free tier (API)</h3><p>No free API tier. Requires payment method. Pro subscribers get $5&#x2F;mo in API credits. <a href="https://www.perplexity.ai/startups">Startup program</a>: 6 months free Enterprise Pro + $5,000 in API credits.</p>
<h3 id="Open-source-10"><a href="#Open-source-10" class="headerlink" title="Open source"></a>Open source</h3><p><strong>No.</strong> Sonar models are proprietary (fine-tuned from Llama 3.3 70B and DeepSeek R1, but Perplexity’s weights and search infrastructure are closed).</p>
<h3 id="Mobile-SDK-10"><a href="#Mobile-SDK-10" class="headerlink" title="Mobile SDK"></a>Mobile SDK</h3><p>No dedicated SDK. Official Python and TypeScript SDKs. MCP server for Cursor, VS Code, Claude Desktop. OpenAI-compatible API. iOS and Android apps available.</p>
<h3 id="Links-10"><a href="#Links-10" class="headerlink" title="Links"></a>Links</h3><ul>
<li><a href="https://www.perplexity.ai/">Perplexity Pro</a></li>
<li><a href="https://docs.perplexity.ai/docs/getting-started/pricing">API pricing</a></li>
<li><a href="https://docs.perplexity.ai/">API docs</a></li>
<li><a href="https://www.perplexity.ai/enterprise/pricing">Enterprise pricing</a></li>
</ul>
<hr>
<h2 id="Groq"><a href="#Groq" class="headerlink" title="Groq"></a>Groq</h2><p><a href="https://groq.com/">groq.com</a> | <a href="https://console.groq.com/">console.groq.com</a> | <a href="https://console.groq.com/docs">API docs</a></p>
<p>Not a model provider – an inference provider. Runs open-source models on custom LPU hardware at the fastest speeds in the industry.</p>
<h3 id="Models-pricing"><a href="#Models-pricing" class="headerlink" title="Models &amp; pricing"></a>Models &amp; pricing</h3><table>
<thead>
<tr>
<th>Model</th>
<th>Input&#x2F;Output (per 1M)</th>
<th>Speed</th>
</tr>
</thead>
<tbody><tr>
<td>Llama 4 Maverick</td>
<td>$0.20 &#x2F; $0.60</td>
<td>562 t&#x2F;s</td>
</tr>
<tr>
<td>Llama 4 Scout</td>
<td>$0.11 &#x2F; $0.34</td>
<td>594 t&#x2F;s</td>
</tr>
<tr>
<td>Llama 3.3 70B</td>
<td>$0.59 &#x2F; $0.79</td>
<td>394 t&#x2F;s</td>
</tr>
<tr>
<td>Llama 3.1 8B</td>
<td>$0.05 &#x2F; $0.08</td>
<td>840 t&#x2F;s</td>
</tr>
<tr>
<td>GPT-OSS 120B</td>
<td>$0.15 &#x2F; $0.60</td>
<td>500 t&#x2F;s</td>
</tr>
<tr>
<td>GPT-OSS 20B</td>
<td>$0.075 &#x2F; $0.30</td>
<td>1,000 t&#x2F;s</td>
</tr>
<tr>
<td>Qwen3 32B</td>
<td>$0.29 &#x2F; $0.59</td>
<td>662 t&#x2F;s</td>
</tr>
<tr>
<td>Whisper V3 Large (STT)</td>
<td>$0.111&#x2F;hr</td>
<td>–</td>
</tr>
<tr>
<td>Whisper V3 Turbo (STT)</td>
<td>$0.04&#x2F;hr</td>
<td>–</td>
</tr>
</tbody></table>
<h3 id="Free-tier"><a href="#Free-tier" class="headerlink" title="Free tier"></a>Free tier</h3><p>No credit card required. Rate-limited access (e.g., ~6K tokens&#x2F;min for 70B models, ~500K tokens&#x2F;day). Never charged – just 429 errors at the limit.</p>
<p>Paid tier (pay-as-you-go) gets 10x the rate limits plus batch processing at 50% off.</p>
<h3 id="Open-source-11"><a href="#Open-source-11" class="headerlink" title="Open source"></a>Open source</h3><p>N&#x2F;A – Groq doesn’t make models. They host open-source models (Llama, Qwen, GPT-OSS) on custom LPU hardware.</p>
<h3 id="Links-11"><a href="#Links-11" class="headerlink" title="Links"></a>Links</h3><ul>
<li><a href="https://groq.com/pricing">Pricing</a></li>
<li><a href="https://console.groq.com/docs">API docs</a></li>
<li><a href="https://console.groq.com/docs/models">Models</a></li>
</ul>
<hr>
<h2 id="Mobile-SDK-summary"><a href="#Mobile-SDK-summary" class="headerlink" title="Mobile SDK summary"></a>Mobile SDK summary</h2><table>
<thead>
<tr>
<th>Provider</th>
<th>Official iOS SDK</th>
<th>Official Android SDK</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Google Gemini</strong></td>
<td>Yes (Firebase AI Logic, Swift)</td>
<td>Yes (Firebase AI Logic, Kotlin)</td>
<td>Also Flutter, React Native, Unity</td>
</tr>
<tr>
<td><strong>OpenAI</strong></td>
<td>No</td>
<td>No</td>
<td>Community: <a href="https://github.com/MacPaw/OpenAI">MacPaw&#x2F;OpenAI</a> (Swift)</td>
</tr>
<tr>
<td><strong>Anthropic</strong></td>
<td>No</td>
<td>No</td>
<td>Community: <a href="https://github.com/jamesrochabrun/SwiftAnthropic">SwiftAnthropic</a></td>
</tr>
<tr>
<td><strong>xAI</strong></td>
<td>No</td>
<td>No</td>
<td>OpenAI-compatible API (any client works)</td>
</tr>
<tr>
<td><strong>DeepSeek</strong></td>
<td>No</td>
<td>No</td>
<td>OpenAI-compatible API</td>
</tr>
<tr>
<td><strong>Mistral</strong></td>
<td>No</td>
<td>No</td>
<td>Python&#x2F;JS SDKs only</td>
</tr>
<tr>
<td><strong>Meta Llama</strong></td>
<td>No</td>
<td>No</td>
<td>On-device via llama.cpp, MLX, ONNX</td>
</tr>
<tr>
<td><strong>Alibaba Qwen</strong></td>
<td>No</td>
<td>No</td>
<td>On-device via MLX, Ollama, llama.cpp</td>
</tr>
<tr>
<td><strong>Amazon Nova</strong></td>
<td>Partial (Amplify AI Kit)</td>
<td>Partial (Amplify AI Kit)</td>
<td>React Native + Android. No native Swift SDK</td>
</tr>
<tr>
<td><strong>Microsoft Phi</strong></td>
<td>No</td>
<td>No</td>
<td>ONNX Runtime Mobile (iOS CoreML, Android NNAPI)</td>
</tr>
<tr>
<td><strong>Perplexity</strong></td>
<td>No</td>
<td>No</td>
<td>Python&#x2F;TS SDKs, MCP server</td>
</tr>
<tr>
<td><strong>Groq</strong></td>
<td>No</td>
<td>No</td>
<td>OpenAI-compatible API</td>
</tr>
</tbody></table>
<p>Google is the only provider with first-party mobile SDKs. Amazon has partial coverage via Amplify. Everyone else: use REST directly, community packages, or on-device inference (MLX, ONNX, llama.cpp).</p>
<hr>
<h2 id="Bottom-line"><a href="#Bottom-line" class="headerlink" title="Bottom line"></a>Bottom line</h2><p><strong>Best free chat</strong>: <a href="https://chat.deepseek.com/">DeepSeek</a> (unlimited, no sub), <a href="https://chat.qwen.ai/">Qwen</a> (full-featured, also free), or <a href="https://gemini.google.com/">Gemini</a> (50 daily credits). All genuinely useful without paying.</p>
<p><strong>Best ~$20 subscription</strong>: Claude Pro for coding depth. ChatGPT Plus for broadest features (image, video, voice, agents). Gemini AI Pro for Workspace integration. Perplexity Pro for search + access to third-party models. Le Chat Pro is cheapest at $15 with no-telemetry.</p>
<p><strong>Best free API</strong>: <a href="https://aistudio.google.com/">Gemini</a>. No credit card, generous limits. <a href="https://console.groq.com/">Groq</a> for fastest open-model inference. <a href="https://qwen.ai/apiplatform">Qwen</a> for 1M free tokens per model.</p>
<p><strong>Best budget API</strong>: DeepSeek at $0.28&#x2F;M input (or $0.028 cached). Mistral Nemo at $0.02&#x2F;M for ultra-cheap. Amazon Nova Micro at $0.035&#x2F;M for AWS users.</p>
<p><strong>Best coding agent</strong>: <a href="https://claude.com/product/claude-code">Claude Code</a> (Pro $20&#x2F;mo) or <a href="https://openai.com/index/introducing-codex/">Codex</a> (Plus $20&#x2F;mo) for first-party. <a href="https://cursor.com/">Cursor</a> ($20&#x2F;mo) or <a href="https://cline.bot/">Cline</a> (free, BYOM) for third-party. <a href="https://aws.amazon.com/q/developer/">Amazon Q Developer</a> free tier (50 agentic requests&#x2F;mo) if you’re on AWS.</p>
<p><strong>Best open-source models</strong>: <a href="https://www.llama.com/">Llama 4 Scout</a> (10M context), <a href="https://huggingface.co/Qwen">Qwen3.5</a> (Apache 2.0, up to 397B), <a href="https://huggingface.co/deepseek-ai">DeepSeek V3.2</a> (MIT), <a href="https://huggingface.co/microsoft/phi-4">Phi-4</a> (MIT, best small models).</p>
<p><strong>Best mobile SDK</strong>: Gemini via <a href="https://firebase.google.com/products/firebase-ai-logic">Firebase AI Logic</a> – only first-party option for iOS&#x2F;Android&#x2F;Flutter.</p>
<p><strong>Highest capability</strong>: Claude Opus 4.6, GPT-5.4, and Gemini 3.1 Pro are the three frontier models. Pick based on your use case and budget.</p>
<p>Subscriptions and APIs are separate worlds. Pick your subscription for daily use, pick your API for building things. Or just use Gemini&#x2F;Qwen’s free tier for both.</p>
]]></content>
      <categories>
        <category>AI</category>
      </categories>
      <tags>
        <tag>ai</tag>
        <tag>api</tag>
        <tag>llm</tag>
        <tag>subscription</tag>
        <tag>agent</tag>
      </tags>
  </entry>
  <entry>
    <title>Fibonacci across ten languages</title>
    <url>/Fibonacci-Number-in-Different-Programming-Languages/</url>
    <content><![CDATA[<p>The same trivial algorithm in ten languages, ordered by year of birth. Beyond “syntax differs,” the interesting part is what each language insists you write down: type signatures, memory ownership, package envelopes, entry-point ceremony. The shape of the boilerplate is the language’s taste.</p>
<span id="more"></span>

<h2 id="C-1972"><a href="#C-1972" class="headerlink" title="C (1972)"></a>C (1972)</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">fib</span><span class="params">(<span class="type">int</span> n)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (n &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> n;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> a = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> b = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">2</span>; i &lt;= n; i++) &#123;</span><br><span class="line">        <span class="type">int</span> c = a + b;</span><br><span class="line">        a = b;</span><br><span class="line">        b = c;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span> &#123;</span><br><span class="line">    <span class="type">int</span> n = atoi(argv[<span class="number">1</span>]);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fib(%d) = %d\n&quot;</span>, n, fib(n));</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc fibonacci.c</span><br><span class="line">./a.out 1</span><br><span class="line"><span class="comment"># fib(1) = 1</span></span><br></pre></td></tr></table></figure>

<p>C asks little of the programmer up front: no module system, no type inference to fight, no runtime to satisfy. In return it gives you <code>int</code> (overflow at fib(46)), manual <code>argv</code> parsing, and <code>atoi</code> with no error path. The minimalism is real, and so is the cost.</p>
<h2 id="Scheme-1975"><a href="#Scheme-1975" class="headerlink" title="Scheme (1975)"></a>Scheme (1975)</h2><figure class="highlight scheme"><table><tr><td class="code"><pre><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">fib</span> n)</span><br><span class="line">  (<span class="name"><span class="built_in">define</span></span> (<span class="name">iter</span> a b n)</span><br><span class="line">    (<span class="name"><span class="built_in">if</span></span> (<span class="name"><span class="built_in">&lt;=</span></span> n <span class="number">1</span>)</span><br><span class="line">        b</span><br><span class="line">        (<span class="name">iter</span> b (<span class="name"><span class="built_in">+</span></span> a b) (<span class="name"><span class="built_in">-</span></span> n <span class="number">1</span>))))</span><br><span class="line">  (<span class="name">iter</span> <span class="number">0</span> <span class="number">1</span> n))</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">racket -f fibonacci.scm -e <span class="string">&quot;(fib 2)&quot;</span></span><br><span class="line"><span class="comment"># 1</span></span><br></pre></td></tr></table></figure>

<p>Tail recursion replaces the loop entirely; no <code>for</code>, no mutable accumulator. Integer arithmetic is bignum by default, so fib(1000) returns the right answer without a library.</p>
<h2 id="Ada-1980"><a href="#Ada-1980" class="headerlink" title="Ada (1980)"></a>Ada (1980)</h2><figure class="highlight ada"><table><tr><td class="code"><pre><span class="line"><span class="keyword">with</span> Ada.Text_IO;</span><br><span class="line"><span class="keyword">with</span> Ada.Command_Line;</span><br><span class="line"></span><br><span class="line"><span class="keyword">procedure</span> <span class="title">fibonacci</span> <span class="keyword">is</span></span><br><span class="line">    N : <span class="type">Integer</span> := Integer<span class="symbol">&#x27;Value</span>(Ada.Command_Line.Argument(Number =&gt; <span class="number">1</span>));</span><br><span class="line">    <span class="keyword">function</span> <span class="title">fib</span>(</span><br><span class="line">        N: in Integer)</span><br><span class="line">        <span class="keyword">return</span> <span class="type">Integer</span> <span class="keyword">is</span></span><br><span class="line">        A : <span class="type">Integer</span> := <span class="number">0</span>;</span><br><span class="line">        B : <span class="type">Integer</span> := <span class="number">1</span>;</span><br><span class="line">        C : <span class="type">Integer</span> := <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">begin</span></span><br><span class="line">        <span class="keyword">if</span> (N &lt;= <span class="number">1</span>) <span class="keyword">then</span></span><br><span class="line">            <span class="keyword">return</span> N;</span><br><span class="line">        <span class="keyword">end</span> <span class="keyword">if</span>;</span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> <span class="number">2</span> .. N <span class="keyword">loop</span></span><br><span class="line">            C := A + B;</span><br><span class="line">            A := B;</span><br><span class="line">            B := C;</span><br><span class="line">        <span class="keyword">end</span> <span class="keyword">loop</span>;</span><br><span class="line">        <span class="keyword">return</span> B;</span><br><span class="line">    <span class="keyword">end</span>;</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line">    Ada.Text_IO.Put_Line(<span class="string">&quot;fib(&quot;</span> &amp; Integer<span class="symbol">&#x27;Image</span>(N) &amp; <span class="string">&quot;) = &quot;</span> &amp; Integer<span class="symbol">&#x27;Image</span>(fib(N)));</span><br><span class="line"><span class="keyword">end</span> fibonacci;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gnatmake fibonacci.adb</span><br><span class="line">./fibonacci 3</span><br><span class="line"><span class="comment"># fib( 3) =  2</span></span><br></pre></td></tr></table></figure>

<p>Every parameter has a mode (<code>in</code>, <code>out</code>, <code>in out</code>), every variable has a declared type, every package import is explicit. Even the assignment operator (<code>:=</code>) is different from equality (<code>=</code>), so a typo can’t quietly become a comparison.</p>
<h2 id="Standard-ML-1983"><a href="#Standard-ML-1983" class="headerlink" title="Standard ML (1983)"></a>Standard ML (1983)</h2><figure class="highlight sml"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fun</span> fib n =</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">fun</span> iter (a, b, n) = <span class="keyword">if</span> n &lt;= <span class="number">1</span> <span class="keyword">then</span> b <span class="keyword">else</span> iter (b, a + b, n - <span class="number">1</span>)</span><br><span class="line">    <span class="keyword">in</span></span><br><span class="line">        iter (<span class="number">0</span>, <span class="number">1</span>, n)</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">val</span> n_str = hd (<span class="type">CommandLine</span>.arguments<span class="literal">()</span>)</span><br><span class="line"><span class="keyword">val</span> n = valOf (<span class="type">Int</span>.fromString n_str)</span><br><span class="line"><span class="keyword">val</span> res = fib n</span><br><span class="line"><span class="keyword">val</span> _ = print (<span class="string">&quot;fib(&quot;</span> ^ <span class="type">Int</span>.toString n ^ <span class="string">&quot;) = &quot;</span> ^ <span class="type">Int</span>.toString res)</span><br><span class="line"><span class="keyword">val</span> _ = <span class="type">OS</span>.<span class="type">Process</span>.exit(<span class="type">OS</span>.<span class="type">Process</span>.success)</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sml fibonacci.sml 4</span><br><span class="line"><span class="comment"># fib(4) = 3</span></span><br></pre></td></tr></table></figure>

<p><code>let</code>-binding and pattern matching stand in for the imperative loop. Type inference fills in everything that isn’t a calling convention. The price: the runtime really does want you to handle <code>Option</code> types (<code>valOf</code> will raise if the argument can’t be parsed).</p>
<h2 id="C-1985"><a href="#C-1985" class="headerlink" title="C++ (1985)"></a>C++ (1985)</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">fib</span><span class="params">(<span class="type">int</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (n &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> n;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> a = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> b = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">2</span>; i &lt;= n; i++) &#123;</span><br><span class="line">        <span class="type">int</span> c = a + b;</span><br><span class="line">        a = b;</span><br><span class="line">        b = c;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> *argv[])</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> n = std::<span class="built_in">atoi</span>(argv[<span class="number">1</span>]);</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;fib(&quot;</span> &lt;&lt; n &lt;&lt; <span class="string">&quot;) = &quot;</span> &lt;&lt; <span class="built_in">fib</span>(n) &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">g++ fibonacci.cpp</span><br><span class="line">./a.out 5</span><br><span class="line"><span class="comment"># fib(5) = 5</span></span><br></pre></td></tr></table></figure>

<p>Same skeleton as C plus <code>std::</code> prefixes and stream insertion. (<code>using namespace std;</code> makes the body terser and hides which symbols come from where; the qualified form scales.) Modern C++ would reach for <code>std::format</code> and <code>&lt;charconv&gt;</code>, but the bones haven’t changed in forty years.</p>
<h2 id="Python-1991"><a href="#Python-1991" class="headerlink" title="Python (1991)"></a>Python (1991)</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">fib</span>(<span class="params">n</span>):</span><br><span class="line">    <span class="keyword">if</span> n &lt;= <span class="number">1</span>:</span><br><span class="line">        <span class="keyword">return</span> n</span><br><span class="line"></span><br><span class="line">    a, b = <span class="number">0</span>, <span class="number">1</span></span><br><span class="line">    <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(n - <span class="number">1</span>):</span><br><span class="line">        a, b = b, a + b</span><br><span class="line">    <span class="keyword">return</span> b</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    n = <span class="built_in">int</span>(sys.argv[<span class="number">1</span>])</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;fib(<span class="subst">&#123;n&#125;</span>) = <span class="subst">&#123;fib(n)&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python3 fibonacci.py 6</span><br><span class="line"><span class="comment"># fib(6) = 8</span></span><br></pre></td></tr></table></figure>

<p>Tuple swap (<code>a, b = b, a + b</code>) replaces the temporary. The <code>if __name__ == &#39;__main__&#39;:</code> guard exists because every Python file is both a module and a script, so the entry point has to be opt-in. Integers are arbitrary-precision, so overflow is not a thing.</p>
<h2 id="Java-1995"><a href="#Java-1995" class="headerlink" title="Java (1995)"></a>Java (1995)</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Fibonacci</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">fib</span><span class="params">(<span class="type">int</span> n)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (n &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> n;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">a</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">b</span> <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">2</span>; i &lt;= n; i++) &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">c</span> <span class="operator">=</span> a + b;</span><br><span class="line">            a = b;</span><br><span class="line">            b = c;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> b;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">n</span> <span class="operator">=</span> Integer.parseInt(args[<span class="number">0</span>]);</span><br><span class="line">        System.out.println(<span class="string">&quot;fib(&quot;</span> + n + <span class="string">&quot;) = &quot;</span> + fib(n));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">javac Fibonacci.java</span><br><span class="line">java Fibonacci 7</span><br><span class="line"><span class="comment"># fib(7) = 13</span></span><br></pre></td></tr></table></figure>

<p>Code lives inside a class even when there is nothing to encapsulate. The filename and the class name have to match. Other JVM languages spent twenty years deleting this ceremony; Java itself eventually got <code>var</code> (Java 10) and records (Java 14), but the class envelope remains.</p>
<h2 id="JavaScript-1995"><a href="#JavaScript-1995" class="headerlink" title="JavaScript (1995)"></a>JavaScript (1995)</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">fib</span>(<span class="params">n</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (n &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> n;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> a = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">let</span> b = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">2</span>; i &lt;= n; i++) &#123;</span><br><span class="line">        <span class="keyword">const</span> c = a + b;</span><br><span class="line">        a = b;</span><br><span class="line">        b = c;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> n = process.<span class="property">argv</span>[<span class="number">2</span>];</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`fib(<span class="subst">$&#123;n&#125;</span>) = <span class="subst">$&#123;fib(n)&#125;</span>`</span>);</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">node fibonacci.js 8</span><br><span class="line"><span class="comment"># fib(8) = 21</span></span><br></pre></td></tr></table></figure>

<p>No class envelope, no entry point, no type signatures. The discipline JavaScript skips is the same discipline TypeScript was invented to add back; the choice between them is whether you want compile-time errors to find typos or whether you want to ship right now.</p>
<h2 id="Scala-2004"><a href="#Scala-2004" class="headerlink" title="Scala (2004)"></a>Scala (2004)</h2><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="meta">@main</span> <span class="function"><span class="keyword">def</span> <span class="title">fibonacci</span></span>(n: <span class="type">Int</span>): <span class="type">Unit</span> =</span><br><span class="line">  <span class="function"><span class="keyword">def</span> <span class="title">fib</span></span>(n: <span class="type">Int</span>): <span class="type">BigInt</span> =</span><br><span class="line">    <span class="keyword">if</span> n &lt;= <span class="number">1</span> <span class="keyword">then</span> n</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="keyword">var</span> a: <span class="type">BigInt</span> = <span class="number">0</span></span><br><span class="line">      <span class="keyword">var</span> b: <span class="type">BigInt</span> = <span class="number">1</span></span><br><span class="line">      <span class="keyword">for</span> _ &lt;- <span class="number">2</span> to n <span class="keyword">do</span></span><br><span class="line">        <span class="keyword">val</span> c = a + b</span><br><span class="line">        a = b</span><br><span class="line">        b = c</span><br><span class="line">      b</span><br><span class="line"></span><br><span class="line">  println(<span class="string">s&quot;fib(<span class="subst">$n</span>) = <span class="subst">$&#123;fib(n)&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scala-cli run fibonacci.scala -- 9</span><br><span class="line"><span class="comment"># fib(9) = 34</span></span><br></pre></td></tr></table></figure>

<p>Scala 3’s <code>@main</code> annotation collapses the historical <code>object Fibonacci { def main(args: Array[String]) ... }</code> boilerplate down to a top-level function. The <code>var</code> loop here exists for cross-language parity; the idiomatic Scala for this problem is a lazy stream, <code>lazy val fibs: LazyList[BigInt] = 0 #:: 1 #:: fibs.zip(fibs.tail).map { (a, b) =&gt; a + b }</code>, which is a different post.</p>
<h2 id="Swift-2014"><a href="#Swift-2014" class="headerlink" title="Swift (2014)"></a>Swift (2014)</h2><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">func</span> <span class="title function_">fib</span>(<span class="keyword">_</span> <span class="params">n</span>: <span class="type">Int</span>) -&gt; <span class="type">Int</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> n <span class="operator">&lt;=</span> <span class="number">1</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> n</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> a <span class="operator">=</span> <span class="number">0</span></span><br><span class="line">    <span class="keyword">var</span> b <span class="operator">=</span> <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> <span class="keyword">_</span> <span class="keyword">in</span> <span class="number">2</span><span class="operator">...</span>n &#123;</span><br><span class="line">        <span class="keyword">let</span> c <span class="operator">=</span> a <span class="operator">+</span> b</span><br><span class="line">        a <span class="operator">=</span> b</span><br><span class="line">        b <span class="operator">=</span> c</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> b</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> args <span class="operator">=</span> <span class="type">CommandLine</span>.arguments</span><br><span class="line"><span class="keyword">guard</span> args.count <span class="operator">==</span> <span class="number">2</span>, <span class="keyword">let</span> n <span class="operator">=</span> <span class="type">Int</span>(args[<span class="number">1</span>]) <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Usage: swift fibonacci.swift &lt;number&gt;&quot;</span>)</span><br><span class="line">    exit(<span class="number">1</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;fib(<span class="subst">\(n)</span>) = <span class="subst">\(fib(n))</span>&quot;</span>)</span><br></pre></td></tr></table></figure>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">swift fibonacci.swift 10</span><br><span class="line"><span class="comment"># fib(10) = 55</span></span><br></pre></td></tr></table></figure>

<p><code>let</code> vs <code>var</code> is a hard distinction baked into the compiler, not a convention. Argument labels are part of the function signature (<code>_ n: Int</code> opts out of the external label). Optionals in argument parsing have to be unwrapped before use, so the <code>guard</code> is mandatory rather than defensive.</p>
]]></content>
      <categories>
        <category>Programming Languages</category>
      </categories>
      <tags>
        <tag>Swift</tag>
        <tag>C</tag>
        <tag>Scheme</tag>
        <tag>Ada</tag>
        <tag>Standard ML</tag>
        <tag>C++</tag>
        <tag>Python</tag>
        <tag>Java</tag>
        <tag>JavaScript</tag>
        <tag>Scala</tag>
      </tags>
  </entry>
  <entry>
    <title>Mac Usage</title>
    <url>/Mac-Usage/</url>
    <content><![CDATA[<p>Shortcuts and commands that earn their keep on a daily-driver Mac.</p>
<span id="more"></span>

<h2 id="Upgrading-and-Checking-Packages"><a href="#Upgrading-and-Checking-Packages" class="headerlink" title="Upgrading and Checking Packages"></a>Upgrading and Checking Packages</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># `bubu` is a personal alias: brew update &amp;&amp; brew outdated &amp;&amp; brew upgrade &amp;&amp; brew cleanup</span></span><br><span class="line">bubu</span><br><span class="line">brew doctor</span><br><span class="line"></span><br><span class="line">ncu -g       <span class="comment"># check global npm packages for newer versions</span></span><br><span class="line">npm doctor</span><br><span class="line"></span><br><span class="line">gem update</span><br><span class="line">gem cleanup</span><br></pre></td></tr></table></figure>

<p>For machine reproducibility (declarative install list), see Brewfile in <a href="/M1-Mac-Development-Setup/" title="Mac development setup">Mac development setup</a>.</p>
<h2 id="CLI-Utilities"><a href="#CLI-Utilities" class="headerlink" title="CLI Utilities"></a>CLI Utilities</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pbcopy &lt; file.txt              <span class="comment"># pipe stdin to clipboard</span></span><br><span class="line">pbpaste &gt; file.txt             <span class="comment"># pipe clipboard to stdout</span></span><br><span class="line">say <span class="string">&quot;build finished&quot;</span>           <span class="comment"># speak text aloud</span></span><br><span class="line">caffeinate -i make release     <span class="comment"># keep Mac awake while a command runs</span></span><br><span class="line">screencapture -i clip.png      <span class="comment"># interactive region capture</span></span><br><span class="line">sips -Z 1024 in.png --out out.png  <span class="comment"># resize, keeping aspect ratio</span></span><br><span class="line">mdls -name kMDItemKind file    <span class="comment"># Spotlight metadata for any file</span></span><br></pre></td></tr></table></figure>

<h2 id="Keyboard-Shortcuts"><a href="#Keyboard-Shortcuts" class="headerlink" title="Keyboard Shortcuts"></a>Keyboard Shortcuts</h2><p><a href="https://support.apple.com/en-us/HT201236">Mac keyboard shortcuts</a></p>
<h3 id="Common-Shortcuts"><a href="#Common-Shortcuts" class="headerlink" title="Common Shortcuts"></a>Common Shortcuts</h3><table>
<thead>
<tr>
<th>Shortcut</th>
<th>Action</th>
</tr>
</thead>
<tbody><tr>
<td><code>⌘,</code></td>
<td>Open preferences for the front app</td>
</tr>
<tr>
<td><code>⇧⌘T</code></td>
<td>Reopen the last closed tab</td>
</tr>
<tr>
<td><code>⇧⌘[</code> &#x2F; <code>⇧⌘]</code></td>
<td>Previous &#x2F; next tab</td>
</tr>
<tr>
<td><code>⌃⌘F</code></td>
<td>Toggle full screen</td>
</tr>
<tr>
<td><code>⌘W</code> &#x2F; <code>⌥⌘W</code></td>
<td>Close window &#x2F; close all windows</td>
</tr>
<tr>
<td><code>⌃⌘Q</code></td>
<td>Lock screen immediately</td>
</tr>
<tr>
<td><code>⌘⇧3</code> &#x2F; <code>⌘⇧4</code> &#x2F; <code>⌘⇧5</code></td>
<td>Full &#x2F; region &#x2F; capture overlay</td>
</tr>
</tbody></table>
<h3 id="Finder"><a href="#Finder" class="headerlink" title="Finder"></a>Finder</h3><table>
<thead>
<tr>
<th>Shortcut</th>
<th>Action</th>
</tr>
</thead>
<tbody><tr>
<td><code>⇧⌘.</code></td>
<td>Toggle show hidden files</td>
</tr>
<tr>
<td><code>⌘↑</code> &#x2F; <code>⌘↓</code></td>
<td>Parent folder &#x2F; open selection</td>
</tr>
<tr>
<td><code>⌘⇧G</code></td>
<td>Go to folder by path</td>
</tr>
<tr>
<td><code>⌘⌥V</code></td>
<td>Move (cut&#x2F;paste) after <code>⌘C</code></td>
</tr>
<tr>
<td>Space</td>
<td>Quick Look preview</td>
</tr>
</tbody></table>
<h3 id="System-Sliders-open-the-right-pane-fast"><a href="#System-Sliders-open-the-right-pane-fast" class="headerlink" title="System Sliders (open the right pane fast)"></a>System Sliders (open the right pane fast)</h3><p><code>⌥</code> plus any media key opens the matching System Settings pane. Add <code>⇧</code> to step in finer increments:</p>
<table>
<thead>
<tr>
<th>Shortcut</th>
<th>Action</th>
</tr>
</thead>
<tbody><tr>
<td><code>⌥</code>-Volume</td>
<td>Open Sound settings</td>
</tr>
<tr>
<td><code>⌥⇧</code>-Volume</td>
<td>Adjust volume in smaller steps</td>
</tr>
<tr>
<td><code>⌥</code>-Brightness</td>
<td>Open Displays settings</td>
</tr>
<tr>
<td><code>⌥⇧</code>-Brightness</td>
<td>Adjust brightness in smaller steps</td>
</tr>
</tbody></table>
<h3 id="Text-Editing"><a href="#Text-Editing" class="headerlink" title="Text Editing"></a>Text Editing</h3><p><code>⌃A</code>, <code>⌃E</code>, <code>⌃K</code>, <code>⌃Y</code> work in any standard text field, since AppKit borrows Emacs bindings.</p>
<table>
<thead>
<tr>
<th>Shortcut</th>
<th>Action</th>
</tr>
</thead>
<tbody><tr>
<td><code>⌥←</code> &#x2F; <code>⌥→</code></td>
<td>Jump by word</td>
</tr>
<tr>
<td><code>⌥⌫</code></td>
<td>Delete previous word</td>
</tr>
<tr>
<td><code>⌃A</code> &#x2F; <code>⌃E</code></td>
<td>Start &#x2F; end of line</td>
</tr>
<tr>
<td><code>⌃K</code></td>
<td>Kill from cursor to end of line</td>
</tr>
<tr>
<td><code>⌃Y</code></td>
<td>Yank (paste killed text)</td>
</tr>
<tr>
<td><code>fn fn</code></td>
<td>Dictation (toggle in Keyboard settings)</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>Mac</category>
      </categories>
      <tags>
        <tag>Finder</tag>
        <tag>Homebrew</tag>
        <tag>shortcuts</tag>
      </tags>
  </entry>
  <entry>
    <title>Mac development setup</title>
    <url>/M1-Mac-Development-Setup/</url>
    <content><![CDATA[<p>What to install on a fresh Mac for development, organized by the job each tool does. Skip whichever layer you don’t need; nothing here is mandatory.</p>
<span id="more"></span>

<h2 id="Mac-Usage"><a href="#Mac-Usage" class="headerlink" title="Mac Usage"></a>Mac Usage</h2><a href="/Mac-Usage/" title="Mac Usage">Mac Usage</a>
<p><a href="https://github.com/jaywcjlove/awesome-mac">Awesome Mac</a></p>
<h2 id="Shell"><a href="#Shell" class="headerlink" title="Shell"></a>Shell</h2><a href="/Zsh-Usage/" title="Zsh Usage">Zsh Usage</a>

<h3 id="Starship"><a href="#Starship" class="headerlink" title="Starship"></a><a href="https://starship.rs/">Starship</a></h3><blockquote>
<p>The minimal, blazing-fast, and infinitely customizable prompt for any shell.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install starship</span><br></pre></td></tr></table></figure>
<p>Add to <code>~/.zshrc</code>:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">eval</span> <span class="string">&quot;<span class="subst">$(starship init zsh)</span>&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="Package-Manager"><a href="#Package-Manager" class="headerlink" title="Package Manager"></a>Package Manager</h2><h3 id="Homebrew"><a href="#Homebrew" class="headerlink" title="Homebrew"></a><a href="https://brew.sh/">Homebrew</a></h3><h4 id="Installation"><a href="#Installation" class="headerlink" title="Installation"></a>Installation</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/bin/bash -c <span class="string">&quot;<span class="subst">$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)</span>&quot;</span></span><br></pre></td></tr></table></figure>

<h4 id="Commands"><a href="#Commands" class="headerlink" title="Commands"></a>Commands</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install &lt;formula&gt;      <span class="comment"># Install formula.</span></span><br><span class="line">brew install --cask &lt;cask&gt;  <span class="comment"># Install cask.</span></span><br><span class="line">brew update                 <span class="comment"># Fetch the newest version of Homebrew and all formulae from GitHub.</span></span><br><span class="line">brew upgrade                <span class="comment"># Upgrade outdated casks and outdated, unpinned formulae.</span></span><br><span class="line">brew leaves                 <span class="comment"># List installed formulae that are not dependencies of another installed formula.</span></span><br><span class="line">brew tap                    <span class="comment"># Tap a formula repository.</span></span><br><span class="line">brew doctor                 <span class="comment"># Check your system for potential problems.</span></span><br><span class="line">brew cleanup                <span class="comment"># Remove stale lock files and outdated downloads for all formulae and casks, and remove old versions of installed formulae.</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># List installed leaf packages by size</span></span><br><span class="line">brew leaves | <span class="keyword">while</span> <span class="built_in">read</span> pkg; <span class="keyword">do</span></span><br><span class="line">  <span class="built_in">du</span> -sh <span class="string">&quot;<span class="subst">$(brew --cellar)</span>/<span class="variable">$pkg</span>&quot;</span> 2&gt;/dev/null | sed <span class="string">&quot;s|<span class="subst">$(brew --cellar)</span>/||&quot;</span></span><br><span class="line"><span class="keyword">done</span> | <span class="built_in">sort</span> -hr</span><br></pre></td></tr></table></figure>

<h4 id="Brewfile"><a href="#Brewfile" class="headerlink" title="Brewfile"></a>Brewfile</h4><p>Snapshot every cask and formula on the current machine into a <code>Brewfile</code>, then reinstall everything on a fresh Mac with one command:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew bundle dump --file=~/Brewfile     <span class="comment"># snapshot the current install</span></span><br><span class="line">brew bundle --file=~/Brewfile          <span class="comment"># restore on a new machine</span></span><br></pre></td></tr></table></figure>

<h3 id="npm"><a href="#npm" class="headerlink" title="npm"></a><a href="https://www.npmjs.com/">npm</a></h3><h4 id="Installation-1"><a href="#Installation-1" class="headerlink" title="Installation"></a>Installation</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install node</span><br></pre></td></tr></table></figure>

<h3 id="Yarn"><a href="#Yarn" class="headerlink" title="Yarn"></a><a href="https://yarnpkg.com/">Yarn</a></h3><h4 id="Installation-2"><a href="#Installation-2" class="headerlink" title="Installation"></a>Installation</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install yarn</span><br></pre></td></tr></table></figure>

<h3 id="CocoaPods"><a href="#CocoaPods" class="headerlink" title="CocoaPods"></a><a href="https://cocoapods.org/">CocoaPods</a></h3><h4 id="Installation-3"><a href="#Installation-3" class="headerlink" title="Installation"></a>Installation</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> gem install cocoapods</span><br></pre></td></tr></table></figure>

<h2 id="Terminal"><a href="#Terminal" class="headerlink" title="Terminal"></a>Terminal</h2><p>For a detailed comparison of all major terminal emulators (Ghostty, iTerm2, Kitty, Alacritty, WezTerm, and more), see <a href="/Terminal-Emulator-Comparison-2026/" title="Choosing a terminal emulator in 2026: Ghostty, iTerm2, Kitty, Alacritty, WezTerm">Choosing a terminal emulator in 2026: Ghostty, iTerm2, Kitty, Alacritty, WezTerm</a>.</p>
<h3 id="iTerm2"><a href="#iTerm2" class="headerlink" title="iTerm2"></a><a href="https://iterm2.com/">iTerm2</a></h3><h4 id="Installation-4"><a href="#Installation-4" class="headerlink" title="Installation"></a>Installation</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install --cask iterm2</span><br></pre></td></tr></table></figure>

<h4 id="iTerm-Color-Schemes"><a href="#iTerm-Color-Schemes" class="headerlink" title="iTerm Color Schemes"></a><a href="https://github.com/mbadolato/iTerm2-Color-Schemes">iTerm Color Schemes</a></h4><h2 id="Version-Control"><a href="#Version-Control" class="headerlink" title="Version Control"></a>Version Control</h2><h3 id="Git"><a href="#Git" class="headerlink" title="Git"></a>Git</h3><a href="/Git-Solutions-Collection/" title="Git Solutions Collection">Git Solutions Collection</a>: common commands and solutions

<h4 id="SSH"><a href="#SSH" class="headerlink" title="SSH"></a>SSH</h4><p><a href="https://docs.github.com/en/authentication/connecting-to-github-with-ssh/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent">Generating a new SSH key and adding it to the ssh-agent</a></p>
<h4 id="Git-GUI"><a href="#Git-GUI" class="headerlink" title="Git GUI"></a>Git GUI</h4><p><a href="https://www.sourcetreeapp.com/">SourceTree</a></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install --cask sourcetree</span><br></pre></td></tr></table></figure>

<h2 id="Languages"><a href="#Languages" class="headerlink" title="Languages"></a>Languages</h2><h3 id="mise-multi-language-version-manager"><a href="#mise-multi-language-version-manager" class="headerlink" title="mise (multi-language version manager)"></a><a href="https://mise.jdx.dev/">mise</a> (multi-language version manager)</h3><p><code>mise</code> (the asdf successor) handles per-project versions of Node, Python, Ruby, Go, Java, and ~100 other tools from a single config. Replaces <code>nvm</code>&#x2F;<code>n</code>, <code>pyenv</code>, <code>chruby</code>&#x2F;<code>rbenv</code>, and similar with one binary. Trade-off: opinionated install paths; not always wanted in CI environments.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install mise</span><br><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;eval &quot;$(mise activate zsh)&quot;&#x27;</span> &gt;&gt; ~/.zshrc</span><br><span class="line"></span><br><span class="line">mise use --global node@22 python@3.13 ruby@3.4   <span class="comment"># set global versions</span></span><br><span class="line">mise use node@20                                 <span class="comment"># pin per-project (writes .mise.toml)</span></span><br></pre></td></tr></table></figure>

<p>If you’d rather use per-language tools, the sections below cover the standalone options.</p>
<h3 id="Python3"><a href="#Python3" class="headerlink" title="Python3"></a><a href="https://www.python.org/">Python3</a></h3><h4 id="Installation-5"><a href="#Installation-5" class="headerlink" title="Installation"></a>Installation</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install python3</span><br></pre></td></tr></table></figure>

<h3 id="JavaScript"><a href="#JavaScript" class="headerlink" title="JavaScript"></a>JavaScript</h3><a href="/Node-js-Usage/" title="Node.js in 2026: pick your stack">Node.js in 2026: pick your stack</a>

<h3 id="Java"><a href="#Java" class="headerlink" title="Java"></a><a href="https://adoptium.net/">Java</a></h3><h4 id="Installation-6"><a href="#Installation-6" class="headerlink" title="Installation"></a>Installation</h4><p><a href="https://adoptium.net/">Eclipse Temurin (Adoptium)</a> is the standard open JDK distribution since Oracle’s 2019 licensing change. Brew:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install --cask temurin       <span class="comment"># latest LTS</span></span><br><span class="line">brew install --cask temurin@21    <span class="comment"># specific LTS line (e.g. JDK 21)</span></span><br><span class="line">brew install --cask temurin@17    <span class="comment"># JDK 17 (still LTS for legacy projects)</span></span><br><span class="line">brew install --cask temurin@8     <span class="comment"># JDK 8 (only for projects that genuinely need it)</span></span><br></pre></td></tr></table></figure>

<p>Apple’s <a href="https://www.oracle.com/java/technologies/downloads/">Oracle JDK download</a> is still around if you specifically need Oracle’s commercial distribution.</p>
<h3 id="Swift"><a href="#Swift" class="headerlink" title="Swift"></a><a href="https://developer.apple.com/documentation/swift/">Swift</a></h3><h4 id="Package-Manager-1"><a href="#Package-Manager-1" class="headerlink" title="Package Manager"></a>Package Manager</h4><p><a href="https://docs.swift.org/swiftpm/documentation/packagemanagerdocs/">SPM</a><br>Organize, manage, and edit Swift packages.</p>
<p><a href="https://github.com/yonaskolb/Mint">Mint</a><br>A package manager that installs and runs Swift command line tool packages.</p>
<h3 id="Ruby"><a href="#Ruby" class="headerlink" title="Ruby"></a><a href="https://www.ruby-lang.org/en/">Ruby</a></h3><h4 id="Version-Manager"><a href="#Version-Manager" class="headerlink" title="Version Manager"></a>Version Manager</h4><p><a href="https://github.com/postmodern/chruby">chruby</a> and <a href="https://github.com/postmodern/ruby-install">ruby-install</a></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install chruby ruby-install</span><br></pre></td></tr></table></figure>

<h4 id="Installation-7"><a href="#Installation-7" class="headerlink" title="Installation"></a>Installation</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ruby-install ruby   <span class="comment"># Install the current stable version of Ruby</span></span><br><span class="line">chruby 3.2.0        <span class="comment"># Change the current Ruby.</span></span><br></pre></td></tr></table></figure>

<h4 id="Package-Manager-2"><a href="#Package-Manager-2" class="headerlink" title="Package Manager"></a>Package Manager</h4><p><a href="https://github.com/rubygems/rubygems">RubyGems</a></p>
<h4 id="Dependency-Manager"><a href="#Dependency-Manager" class="headerlink" title="Dependency Manager"></a>Dependency Manager</h4><p><a href="https://bundler.io/">Bundler</a></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gem install bundler</span><br></pre></td></tr></table></figure>

<h3 id="Scheme"><a href="#Scheme" class="headerlink" title="Scheme"></a><a href="https://racket-lang.org/">Scheme</a></h3><h4 id="Installation-8"><a href="#Installation-8" class="headerlink" title="Installation"></a>Installation</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install --cask racket</span><br></pre></td></tr></table></figure>

<h3 id="SML"><a href="#SML" class="headerlink" title="SML"></a><a href="http://www.smlnj.org/">SML</a></h3><h4 id="Installation-9"><a href="#Installation-9" class="headerlink" title="Installation"></a>Installation</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install smlnj</span><br></pre></td></tr></table></figure>

<h2 id="AI-Tools"><a href="#AI-Tools" class="headerlink" title="AI Tools"></a>AI Tools</h2><h3 id="Cursor"><a href="#Cursor" class="headerlink" title="Cursor"></a><a href="https://cursor.com/home">Cursor</a></h3><h3 id="Claude-Code"><a href="#Claude-Code" class="headerlink" title="Claude Code"></a><a href="https://code.claude.com/">Claude Code</a></h3><h2 id="Text-Editors"><a href="#Text-Editors" class="headerlink" title="Text Editors"></a>Text Editors</h2><h3 id="VSCode"><a href="#VSCode" class="headerlink" title="VSCode"></a><a href="https://code.visualstudio.com/">VSCode</a></h3><h3 id="Vim"><a href="#Vim" class="headerlink" title="Vim"></a><a href="https://www.vim.org/">Vim</a></h3><h4 id="Installation-10"><a href="#Installation-10" class="headerlink" title="Installation"></a>Installation</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install vim</span><br></pre></td></tr></table></figure>

<h2 id="IDE"><a href="#IDE" class="headerlink" title="IDE"></a>IDE</h2><h3 id="JetBrains"><a href="#JetBrains" class="headerlink" title="JetBrains"></a><a href="https://www.jetbrains.com/">JetBrains</a></h3><p><a href="https://www.jetbrains.com/toolbox-app/">Toolbox</a></p>
<h3 id="Xcode"><a href="#Xcode" class="headerlink" title="Xcode"></a><a href="https://developer.apple.com/xcode/">Xcode</a></h3><h2 id="Developer-Tools"><a href="#Developer-Tools" class="headerlink" title="Developer Tools"></a>Developer Tools</h2><h3 id="Insomnia"><a href="#Insomnia" class="headerlink" title="Insomnia"></a><a href="https://insomnia.rest/">Insomnia</a></h3><blockquote>
<p>The open-source, cross-platform API client for GraphQL, REST, and gRPC.</p>
</blockquote>
<h3 id="OrbStack"><a href="#OrbStack" class="headerlink" title="OrbStack"></a><a href="https://orbstack.dev/">OrbStack</a></h3><blockquote>
<p>Fast, lightweight Docker Desktop and Linux VM replacement for macOS. Free for personal use.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install --cask orbstack</span><br></pre></td></tr></table></figure>

<p>Works as a drop-in for the <code>docker</code> CLI, with much lower memory and battery cost than Docker Desktop. Also runs lightweight Linux VMs.</p>
<h2 id="Tools"><a href="#Tools" class="headerlink" title="Tools"></a>Tools</h2><h3 id="Finder-Tools"><a href="#Finder-Tools" class="headerlink" title="Finder Tools"></a>Finder Tools</h3><h4 id="quick-look-plugins"><a href="#quick-look-plugins" class="headerlink" title="quick-look-plugins"></a><a href="https://github.com/sindresorhus/quick-look-plugins">quick-look-plugins</a></h4><blockquote>
<p>List of useful Quick Look plugins for developers</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install apparency betterzip provisionql qlcolorcode qlmarkdown qlstephen qlvideo quicklookase quicklook-json quicklook-pat suspicious-package webpquicklook</span><br></pre></td></tr></table></figure>
<p>Restart the QuickLook manager</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">qlmanage -r</span><br></pre></td></tr></table></figure>

<h4 id="OpenInTerminal"><a href="#OpenInTerminal" class="headerlink" title="OpenInTerminal"></a><a href="https://github.com/Ji4n1ng/OpenInTerminal/">OpenInTerminal</a></h4><blockquote>
<p>Finder Toolbar app for macOS to open the current directory in Terminal, iTerm, Hyper or Alacritty.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install --cask openinterminal</span><br></pre></td></tr></table></figure>
<p><a href="https://github.com/Ji4n1ng/OpenInTerminal/blob/master/Resources/README-Config.md">Configurations</a></p>
<h3 id="Menu-Bar-Tools"><a href="#Menu-Bar-Tools" class="headerlink" title="Menu Bar Tools"></a>Menu Bar Tools</h3><h4 id="Itsycal"><a href="#Itsycal" class="headerlink" title="Itsycal"></a><a href="https://www.mowglii.com/itsycal/">Itsycal</a></h4><blockquote>
<p>Itsycal is a tiny menu bar calendar.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install --cask itsycal</span><br></pre></td></tr></table></figure>

<h4 id="MenubarX"><a href="#MenubarX" class="headerlink" title="MenubarX"></a><a href="https://menubarx.app/">MenubarX</a></h4><blockquote>
<p>A powerful menu bar browser</p>
</blockquote>
<h4 id="Dozer"><a href="#Dozer" class="headerlink" title="Dozer"></a><a href="https://github.com/Mortennn/Dozer">Dozer</a></h4><blockquote>
<p>Hide status bar icons on macOS.</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">brew install --cask dozer</span><br></pre></td></tr></table></figure>

<h4 id="xbar"><a href="#xbar" class="headerlink" title="xbar"></a><a href="https://github.com/matryer/xbar">xbar</a></h4><blockquote>
<p>Put the output from any script or program into your macOS Menu Bar (the BitBar reboot)</p>
</blockquote>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">brew install --cask xbar</span><br></pre></td></tr></table></figure>

<h3 id="Screen-Saver"><a href="#Screen-Saver" class="headerlink" title="Screen Saver"></a>Screen Saver</h3><ul>
<li><a href="https://aerialscreensaver.github.io/">Aerial</a></li>
<li><a href="https://github.com/pedrommcarrasco/Brooklyn">Brooklyn</a></li>
</ul>
<h3 id="AppCleaner"><a href="#AppCleaner" class="headerlink" title="AppCleaner"></a><a href="https://freemacsoft.net/appcleaner/">AppCleaner</a></h3><blockquote>
<p>AppCleaner is a small application which allows you to thoroughly uninstall unwanted apps.</p>
</blockquote>
<h3 id="Maccy"><a href="#Maccy" class="headerlink" title="Maccy"></a><a href="https://github.com/p0deje/Maccy">Maccy</a></h3><blockquote>
<p>Lightweight clipboard manager for macOS</p>
</blockquote>
<h4 id="Installation-11"><a href="#Installation-11" class="headerlink" title="Installation"></a>Installation</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install --cask maccy</span><br></pre></td></tr></table></figure>

<h3 id="OpenEmu"><a href="#OpenEmu" class="headerlink" title="OpenEmu"></a><a href="http://openemu.org/">OpenEmu</a></h3><blockquote>
<p>Retro video game emulation for macOS</p>
</blockquote>
<h2 id="Command-Line-Tools"><a href="#Command-Line-Tools" class="headerlink" title="Command Line Tools"></a>Command Line Tools</h2><h3 id="bat"><a href="#bat" class="headerlink" title="bat"></a><a href="https://github.com/sharkdp/bat">bat</a></h3><blockquote>
<p>A cat(1) clone with syntax highlighting and Git integration.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bat &lt;file&gt;</span><br></pre></td></tr></table></figure>

<h3 id="btop"><a href="#btop" class="headerlink" title="btop"></a><a href="https://github.com/aristocratos/btop">btop</a></h3><blockquote>
<p>Resource monitor that shows usage and stats for processor, memory, disks, network and processes.</p>
</blockquote>
<h3 id="diff-so-fancy"><a href="#diff-so-fancy" class="headerlink" title="diff-so-fancy"></a><a href="https://github.com/so-fancy/diff-so-fancy">diff-so-fancy</a></h3><blockquote>
<p>Good-lookin’ diffs with diff-highlight and more.</p>
</blockquote>
<h3 id="eza"><a href="#eza" class="headerlink" title="eza"></a><a href="https://github.com/eza-community/eza">eza</a></h3><blockquote>
<p>A modern alternative to ls</p>
</blockquote>
<h3 id="fd"><a href="#fd" class="headerlink" title="fd"></a><a href="https://github.com/sharkdp/fd">fd</a></h3><blockquote>
<p>A simple, fast and user-friendly alternative to <code>find</code></p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">fd &lt;pattern&gt;</span><br></pre></td></tr></table></figure>

<h3 id="ffmpeg"><a href="#ffmpeg" class="headerlink" title="ffmpeg"></a><a href="https://www.ffmpeg.org/">ffmpeg</a></h3><blockquote>
<p>A complete, cross-platform solution to record, convert and stream audio and video.</p>
</blockquote>
<h3 id="fzf"><a href="#fzf" class="headerlink" title="fzf"></a><a href="https://github.com/junegunn/fzf">fzf</a></h3><blockquote>
<p>A command-line fuzzy finder</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">fzf</span><br></pre></td></tr></table></figure>

<h3 id="gh"><a href="#gh" class="headerlink" title="gh"></a><a href="https://cli.github.com/">gh</a></h3><blockquote>
<p>Work seamlessly with GitHub from the command line.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gh release create [&lt;tag&gt;] [&lt;filename&gt;... | &lt;pattern&gt;...]</span><br></pre></td></tr></table></figure>

<h3 id="graphicsmagick"><a href="#graphicsmagick" class="headerlink" title="graphicsmagick"></a><a href="http://www.graphicsmagick.org/">graphicsmagick</a></h3><blockquote>
<p>Image processing tools collection.</p>
</blockquote>
<h3 id="htop"><a href="#htop" class="headerlink" title="htop"></a><a href="https://htop.dev/">htop</a></h3><blockquote>
<p>A cross-platform interactive process viewer.</p>
</blockquote>
<h3 id="httrack"><a href="#httrack" class="headerlink" title="httrack"></a><a href="https://www.httrack.com/">httrack</a></h3><blockquote>
<p>Website copier&#x2F;offline browser.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">httrack &lt;URLs&gt;</span><br></pre></td></tr></table></figure>

<h3 id="mtr"><a href="#mtr" class="headerlink" title="mtr"></a><a href="https://www.bitwizard.nl/mtr/">mtr</a></h3><blockquote>
<p>mtr combines the functionality of the <code>traceroute</code> and <code>ping</code> programs in a single network diagnostic tool.</p>
</blockquote>
<h3 id="navi"><a href="#navi" class="headerlink" title="navi"></a><a href="https://github.com/denisidoro/navi">navi</a></h3><blockquote>
<p>An interactive cheatsheet tool for the command-line</p>
</blockquote>
<h3 id="ncdu"><a href="#ncdu" class="headerlink" title="ncdu"></a><a href="https://dev.yorhel.nl/ncdu">ncdu</a></h3><blockquote>
<p>NCurses Disk Usage.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ncdu &lt;<span class="built_in">dir</span>&gt;</span><br></pre></td></tr></table></figure>

<h3 id="pandoc"><a href="#pandoc" class="headerlink" title="pandoc"></a><a href="https://pandoc.org/">pandoc</a></h3><blockquote>
<p>General markup converter.</p>
</blockquote>
<h3 id="pngquant"><a href="#pngquant" class="headerlink" title="pngquant"></a><a href="https://pngquant.org/">pngquant</a></h3><blockquote>
<p>Lossy PNG compressor</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pngquant --ext .png --force 256 *.png */*.png</span><br></pre></td></tr></table></figure>

<h3 id="jpegoptim"><a href="#jpegoptim" class="headerlink" title="jpegoptim"></a><a href="https://github.com/tjko/jpegoptim">jpegoptim</a></h3><blockquote>
<p>JPEG optimizer: lossless or lossy compression with quality control</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jpegoptim --max=95 *.jpg *.jpeg</span><br></pre></td></tr></table></figure>

<h3 id="prettyping"><a href="#prettyping" class="headerlink" title="prettyping"></a><a href="https://denilsonsa.github.io/prettyping/">prettyping</a></h3><blockquote>
<p><code>prettyping</code> is a wrapper around the standard <code>ping</code> tool, making the output prettier, more colorful, more compact, and easier to read.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">prettyping</span><br></pre></td></tr></table></figure>

<h3 id="rlwrap"><a href="#rlwrap" class="headerlink" title="rlwrap"></a><a href="https://github.com/hanslub42/rlwrap">rlwrap</a></h3><blockquote>
<p><code>rlwrap</code> is a ‘readline wrapper’, a small utility that uses the GNU Readline library to allow the editing of keyboard input for any command.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rlwrap &lt;<span class="built_in">command</span>&gt;</span><br></pre></td></tr></table></figure>

<h3 id="ripgrep-rg"><a href="#ripgrep-rg" class="headerlink" title="ripgrep (rg)"></a><a href="https://github.com/BurntSushi/ripgrep">ripgrep (<code>rg</code>)</a></h3><blockquote>
<p>Recursively search for a regex pattern. Faster than <code>grep</code>, <code>ag</code>, and <code>ack</code>. Respects <code>.gitignore</code> by default.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rg &lt;pattern&gt;</span><br><span class="line">rg -t swift <span class="string">&quot;TODO&quot;</span>        <span class="comment"># restrict to Swift files</span></span><br><span class="line">rg -g <span class="string">&#x27;!*.test.ts&#x27;</span> bug    <span class="comment"># exclude a glob</span></span><br></pre></td></tr></table></figure>

<h3 id="the-silver-searcher"><a href="#the-silver-searcher" class="headerlink" title="the_silver_searcher"></a><a href="https://github.com/ggreer/the_silver_searcher">the_silver_searcher</a></h3><blockquote>
<p>A code searching tool similar to <code>ack</code>, with a focus on speed.</p>
</blockquote>
<h3 id="tig"><a href="#tig" class="headerlink" title="tig"></a><a href="https://jonas.github.io/tig/">tig</a></h3><blockquote>
<p>Tig is an ncurses-based text-mode interface for git.</p>
</blockquote>
<h3 id="tldr"><a href="#tldr" class="headerlink" title="tldr"></a><a href="https://tldr.sh/">tldr</a></h3><blockquote>
<p>Simplified and community-driven man pages</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tldr &lt;<span class="built_in">command</span>&gt;</span><br></pre></td></tr></table></figure>

<h3 id="tree"><a href="#tree" class="headerlink" title="tree"></a><a href="http://mama.indstate.edu/users/ice/tree/">tree</a></h3><blockquote>
<p>List contents of directories in a tree-like format.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tree -L &lt;num&gt; -a</span><br></pre></td></tr></table></figure>

<h3 id="unrar"><a href="#unrar" class="headerlink" title="unrar"></a><a href="https://theunarchiver.com/command-line">unrar</a></h3><blockquote>
<p>Command-line unarchiving tools supporting multiple formats</p>
</blockquote>
<h3 id="xxh"><a href="#xxh" class="headerlink" title="xxh"></a><a href="https://github.com/xxh/xxh">xxh</a></h3><blockquote>
<p>Bring your favorite shell wherever you go through the ssh.</p>
</blockquote>
<h3 id="zoxide"><a href="#zoxide" class="headerlink" title="zoxide"></a><a href="https://github.com/ajeetdsouza/zoxide">zoxide</a></h3><blockquote>
<p>Smarter <code>cd</code> that learns from your history. Modern, faster successor to <code>z</code> and <code>autojump</code>.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install zoxide</span><br><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;eval &quot;$(zoxide init zsh)&quot;&#x27;</span> &gt;&gt; ~/.zshrc</span><br><span class="line"></span><br><span class="line">z foo            <span class="comment"># jump to a recent directory matching &quot;foo&quot;</span></span><br><span class="line">zi               <span class="comment"># interactive picker (requires fzf)</span></span><br></pre></td></tr></table></figure>

<h4 id="autojump-vs-z-vs-zoxide"><a href="#autojump-vs-z-vs-zoxide" class="headerlink" title="autojump vs z vs zoxide"></a><code>autojump</code> vs <code>z</code> vs <code>zoxide</code></h4><table>
<thead>
<tr>
<th></th>
<th>autojump</th>
<th>z (rupa&#x2F;z)</th>
<th>zoxide</th>
</tr>
</thead>
<tbody><tr>
<td><strong>First release</strong></td>
<td>2008</td>
<td>2009</td>
<td>2020</td>
</tr>
<tr>
<td><strong>Language</strong></td>
<td>Python</td>
<td>Bash&#x2F;Zsh script</td>
<td>Rust (single binary)</td>
</tr>
<tr>
<td><strong>Install footprint</strong></td>
<td>Python runtime + module</td>
<td>One shell script</td>
<td>One static binary (~5 MB)</td>
</tr>
<tr>
<td><strong>Default command</strong></td>
<td><code>j foo</code></td>
<td><code>z foo</code></td>
<td><code>z foo</code>, plus <code>zi</code> for fzf-picker mode</td>
</tr>
<tr>
<td><strong>Ranking model</strong></td>
<td>Frecency (frequency + recency)</td>
<td>Frecency</td>
<td>Frecency, refined</td>
</tr>
<tr>
<td><strong>Database</strong></td>
<td>Plain text</td>
<td>Plain text (<code>~/.z</code>)</td>
<td>Binary, written atomically</td>
</tr>
<tr>
<td><strong>Shells</strong></td>
<td>bash, zsh, fish (via plugin)</td>
<td>bash, zsh</td>
<td>bash, zsh, fish, PowerShell, nushell, xonsh, elvish</td>
</tr>
<tr>
<td><strong>Speed</strong></td>
<td>Slow (Python startup per call)</td>
<td>Fast</td>
<td><strong>Faster</strong>: 10-20x autojump</td>
</tr>
<tr>
<td><strong>Interactive picker</strong></td>
<td>No</td>
<td>No</td>
<td>Yes, <code>zi</code> with fzf</td>
</tr>
<tr>
<td><strong>Maintenance</strong></td>
<td>Effectively stale (last release 2022)</td>
<td>Slow</td>
<td><strong>Active</strong></td>
</tr>
<tr>
<td><strong><code>cd</code> replacement</strong></td>
<td>Separate <code>j</code> command</td>
<td>Separate <code>z</code> command</td>
<td>Optional <code>cd=z</code> alias covers everything</td>
</tr>
</tbody></table>
<p><strong>Verdict</strong>: zoxide. Faster, single binary, works on every shell, and the <code>zi</code> interactive picker is a real productivity win the other two don’t have. Keep <code>z</code> if you want zero binary dependencies; keep <code>autojump</code> only if you already have a long-standing setup. zoxide imports either database in one shot:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zoxide import --from autojump ~/.local/share/autojump/autojump.txt</span><br><span class="line">zoxide import --from z ~/.z</span><br></pre></td></tr></table></figure>

<h2 id="Virtual-Machines"><a href="#Virtual-Machines" class="headerlink" title="Virtual Machines"></a>Virtual Machines</h2><h3 id="Multipass"><a href="#Multipass" class="headerlink" title="Multipass"></a><a href="https://canonical.com/multipass">Multipass</a></h3><blockquote>
<p>Get an instant Ubuntu VM with a single command. Multipass can launch and run virtual machines and configure them with cloud-init like a public cloud.</p>
</blockquote>
<h2 id="Font"><a href="#Font" class="headerlink" title="Font"></a>Font</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew tap homebrew/cask-fonts</span><br></pre></td></tr></table></figure>
<h3 id="Fira-Code"><a href="#Fira-Code" class="headerlink" title="Fira Code"></a><a href="https://github.com/tonsky/FiraCode">Fira Code</a></h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">brew install --cask font-fira-code</span><br></pre></td></tr></table></figure>

<h3 id="Nerd-Fonts"><a href="#Nerd-Fonts" class="headerlink" title="Nerd Fonts"></a><a href="https://github.com/ryanoasis/nerd-fonts">Nerd Fonts</a></h3><p><a href="https://github.com/ryanoasis/nerd-fonts/tree/master/patched-fonts/FiraCode">Fira Code Nerd Font</a></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">brew install --cask font-fira-code-nerd-font</span><br></pre></td></tr></table></figure>

<h3 id="Font-Awesome"><a href="#Font-Awesome" class="headerlink" title="Font Awesome"></a><a href="https://fontawesome.com/">Font Awesome</a></h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install --cask font-fontawesome</span><br></pre></td></tr></table></figure>

<h2 id="Configuration"><a href="#Configuration" class="headerlink" title="Configuration"></a>Configuration</h2><h3 id="Git-Config"><a href="#Git-Config" class="headerlink" title="Git Config"></a>Git Config</h3><p><a href="https://github.com/so-fancy/diff-so-fancy#with-git">diff-so-fancy</a></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global core.pager <span class="string">&quot;diff-so-fancy | less --tabs=4 -RFX&quot;</span></span><br><span class="line">git config --global interactive.diffFilter <span class="string">&quot;diff-so-fancy --patch&quot;</span></span><br><span class="line"></span><br><span class="line">git config --global color.ui <span class="literal">true</span></span><br><span class="line"></span><br><span class="line">git config --global color.diff-highlight.oldNormal    <span class="string">&quot;red bold&quot;</span></span><br><span class="line">git config --global color.diff-highlight.oldHighlight <span class="string">&quot;red bold 52&quot;</span></span><br><span class="line">git config --global color.diff-highlight.newNormal    <span class="string">&quot;green bold&quot;</span></span><br><span class="line">git config --global color.diff-highlight.newHighlight <span class="string">&quot;green bold 22&quot;</span></span><br><span class="line"></span><br><span class="line">git config --global color.diff.meta       <span class="string">&quot;11&quot;</span></span><br><span class="line">git config --global color.diff.frag       <span class="string">&quot;magenta bold&quot;</span></span><br><span class="line">git config --global color.diff.func       <span class="string">&quot;146 bold&quot;</span></span><br><span class="line">git config --global color.diff.commit     <span class="string">&quot;yellow bold&quot;</span></span><br><span class="line">git config --global color.diff.old        <span class="string">&quot;red bold&quot;</span></span><br><span class="line">git config --global color.diff.new        <span class="string">&quot;green bold&quot;</span></span><br><span class="line">git config --global color.diff.whitespace <span class="string">&quot;red reverse&quot;</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Mac</category>
      </categories>
      <tags>
        <tag>Homebrew</tag>
        <tag>Package Manager</tag>
        <tag>Git</tag>
        <tag>Ruby</tag>
        <tag>Font</tag>
      </tags>
  </entry>
  <entry>
    <title>Choosing a terminal emulator in 2026: Ghostty, iTerm2, Kitty, Alacritty, WezTerm</title>
    <url>/Terminal-Emulator-Comparison-2026/</url>
    <content><![CDATA[<p>Eight terminals on macOS in 2026 compared. Pick by what you actually need.</p>
<span id="more"></span>

<h2 id="The-contenders"><a href="#The-contenders" class="headerlink" title="The contenders"></a>The contenders</h2><table>
<thead>
<tr>
<th>Category</th>
<th>Options</th>
<th>Best for</th>
</tr>
</thead>
<tbody><tr>
<td><strong>GPU-native, feature-rich</strong></td>
<td>Ghostty, iTerm2, Kitty, WezTerm</td>
<td>Developers who want speed + features</td>
</tr>
<tr>
<td><strong>GPU-native, minimal</strong></td>
<td>Alacritty</td>
<td>tmux users who want raw speed</td>
</tr>
<tr>
<td><strong>Alternative approaches</strong></td>
<td>Warp, Hyper</td>
<td>AI-assisted or web-based workflows</td>
</tr>
</tbody></table>
<p><a href="https://support.apple.com/guide/terminal/welcome/mac">Terminal.app</a> (macOS built-in) appears in the comparison tables as a baseline but doesn’t get its own section. macOS Tahoe (26) finally gave it 24-bit color and Powerline-font support, but every option above is still a meaningful upgrade.</p>
<hr>
<h2 id="Quick-comparison"><a href="#Quick-comparison" class="headerlink" title="Quick comparison"></a>Quick comparison</h2><h3 id="Core-features"><a href="#Core-features" class="headerlink" title="Core features"></a>Core features</h3><table>
<thead>
<tr>
<th>Feature</th>
<th align="center">Ghostty</th>
<th align="center">iTerm2</th>
<th align="center">Kitty</th>
<th align="center">Alacritty</th>
<th align="center">WezTerm</th>
<th align="center">Warp</th>
<th align="center">Hyper</th>
<th align="center">Terminal.app</th>
</tr>
</thead>
<tbody><tr>
<td><strong>GPU accelerated</strong></td>
<td align="center">Metal</td>
<td align="center">Metal (opt-in)</td>
<td align="center">OpenGL</td>
<td align="center">OpenGL&#x2F;Metal</td>
<td align="center">OpenGL&#x2F;Metal</td>
<td align="center">Metal</td>
<td align="center">No (Electron)</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>Tabs</strong></td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
</tr>
<tr>
<td><strong>Splits&#x2F;panes</strong></td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes (layouts)</td>
<td align="center">No</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes (horizontal only)</td>
</tr>
<tr>
<td><strong>Ligatures</strong></td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>Inline images</strong></td>
<td align="center">Yes</td>
<td align="center">Yes (imgcat)</td>
<td align="center">Yes (protocol)</td>
<td align="center">No</td>
<td align="center">Yes (multi)</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>tmux integration</strong></td>
<td align="center">No</td>
<td align="center">Native control mode</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>Shell integration</strong></td>
<td align="center">Deep</td>
<td align="center">Deep</td>
<td align="center">Basic</td>
<td align="center">No</td>
<td align="center">Basic</td>
<td align="center">Deep</td>
<td align="center">No</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>Session save&#x2F;restore</strong></td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Session files</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">Yes (window groups)</td>
</tr>
<tr>
<td><strong>Undo close tab</strong></td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">–</td>
<td align="center">No</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">No</td>
</tr>
</tbody></table>
<h3 id="Configuration-and-extensibility"><a href="#Configuration-and-extensibility" class="headerlink" title="Configuration and extensibility"></a>Configuration and extensibility</h3><table>
<thead>
<tr>
<th>Feature</th>
<th>Ghostty</th>
<th>iTerm2</th>
<th>Kitty</th>
<th>Alacritty</th>
<th>WezTerm</th>
<th>Warp</th>
<th>Hyper</th>
<th>Terminal.app</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Config format</strong></td>
<td>key&#x3D;value</td>
<td>GUI</td>
<td>key&#x3D;value</td>
<td>TOML</td>
<td>Lua</td>
<td>GUI + YAML</td>
<td>JS</td>
<td>GUI</td>
</tr>
<tr>
<td><strong>Config location</strong></td>
<td><code>~/.config/ghostty/config</code></td>
<td>Preferences UI</td>
<td><code>~/.config/kitty/kitty.conf</code></td>
<td><code>~/.config/alacritty/alacritty.toml</code></td>
<td><code>~/.config/wezterm/wezterm.lua</code></td>
<td><code>~/.warp/</code></td>
<td><code>~/.hyper.js</code></td>
<td>Preferences UI</td>
</tr>
<tr>
<td><strong>Live reload</strong></td>
<td>Keybind</td>
<td>N&#x2F;A (GUI)</td>
<td><code>ctrl+shift+f5</code></td>
<td>Yes</td>
<td>Yes</td>
<td>N&#x2F;A</td>
<td>Yes</td>
<td>N&#x2F;A</td>
</tr>
<tr>
<td><strong>Scripting</strong></td>
<td>AppleScript (macOS)</td>
<td>Python API</td>
<td>Python (kittens)</td>
<td>No</td>
<td>Lua (full)</td>
<td>No</td>
<td>JS plugins (npm)</td>
<td>No</td>
</tr>
<tr>
<td><strong>Profiles</strong></td>
<td>No</td>
<td>Yes (extensive)</td>
<td>No</td>
<td>No</td>
<td>Domains</td>
<td>Yes</td>
<td>No</td>
<td>Yes</td>
</tr>
<tr>
<td><strong>Config complexity</strong></td>
<td>Very low</td>
<td>Low (GUI)</td>
<td>Low</td>
<td>Low</td>
<td>Medium-High</td>
<td>Low (GUI)</td>
<td>Medium</td>
<td>Very low</td>
</tr>
</tbody></table>
<h3 id="Performance"><a href="#Performance" class="headerlink" title="Performance"></a>Performance</h3><table>
<thead>
<tr>
<th>Metric</th>
<th>Ghostty</th>
<th>iTerm2</th>
<th>Kitty</th>
<th>Alacritty</th>
<th>WezTerm</th>
<th>Warp</th>
<th>Hyper</th>
<th>Terminal.app</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Rendering speed</strong></td>
<td>Excellent</td>
<td>Good</td>
<td>Excellent</td>
<td>Best</td>
<td>Good</td>
<td>Good</td>
<td>Poor</td>
<td>Adequate</td>
</tr>
<tr>
<td><strong>Input latency</strong></td>
<td>Very low</td>
<td>Low</td>
<td>Very low</td>
<td>Lowest</td>
<td>Low</td>
<td>Low</td>
<td>High</td>
<td>Medium</td>
</tr>
<tr>
<td><strong>Memory baseline</strong></td>
<td>~30 MB</td>
<td>~80-120 MB</td>
<td>~30 MB</td>
<td>~20 MB</td>
<td>~80-150 MB</td>
<td>~150 MB</td>
<td>~200 MB</td>
<td>~40 MB</td>
</tr>
<tr>
<td><strong>Startup time</strong></td>
<td>Fast</td>
<td>Moderate</td>
<td>Fast</td>
<td>Fastest</td>
<td>Moderate</td>
<td>Slow</td>
<td>Slow</td>
<td>Instant</td>
</tr>
<tr>
<td><strong>Heavy output (<code>cat</code> large file)</strong></td>
<td>Excellent</td>
<td>Good</td>
<td>Excellent</td>
<td>Excellent</td>
<td>Good</td>
<td>Good</td>
<td>Poor</td>
<td>Slow</td>
</tr>
</tbody></table>
<div class="note info"><p><strong>Memory numbers are approximate</strong> and vary by OS, tab count, scrollback size, and configuration. Measured with a single tab, default scrollback, on macOS.</p>
</div>

<h3 id="Platform-support"><a href="#Platform-support" class="headerlink" title="Platform support"></a>Platform support</h3><table>
<thead>
<tr>
<th>Platform</th>
<th align="center">Ghostty</th>
<th align="center">iTerm2</th>
<th align="center">Kitty</th>
<th align="center">Alacritty</th>
<th align="center">WezTerm</th>
<th align="center">Warp</th>
<th align="center">Hyper</th>
<th align="center">Terminal.app</th>
</tr>
</thead>
<tbody><tr>
<td><strong>macOS</strong></td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
</tr>
<tr>
<td><strong>Linux</strong></td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>Windows</strong></td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No (WSL)</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">Yes</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>Native feel (macOS)</strong></td>
<td align="center">Excellent</td>
<td align="center">Excellent</td>
<td align="center">Native chrome</td>
<td align="center">Custom chrome</td>
<td align="center">Custom chrome</td>
<td align="center">Custom chrome</td>
<td align="center">Custom chrome</td>
<td align="center">Native</td>
</tr>
</tbody></table>
<p>Alacritty and Kitty also support BSD.</p>
<h3 id="Appearance-and-theming"><a href="#Appearance-and-theming" class="headerlink" title="Appearance and theming"></a>Appearance and theming</h3><table>
<thead>
<tr>
<th>Feature</th>
<th>Ghostty</th>
<th>iTerm2</th>
<th>Kitty</th>
<th>Alacritty</th>
<th>WezTerm</th>
<th>Warp</th>
<th>Hyper</th>
<th>Terminal.app</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Bundled themes</strong></td>
<td>300+</td>
<td>~10 (200+ community)</td>
<td>Community (<code>kitten themes</code>)</td>
<td>Community (TOML)</td>
<td>700+</td>
<td>Built-in</td>
<td>Community (npm)</td>
<td>~10</td>
</tr>
<tr>
<td><strong>Background image</strong></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>No</td>
<td>Yes (Lua)</td>
<td>No</td>
<td>Yes (CSS)</td>
<td>No</td>
</tr>
<tr>
<td><strong>Tab bar customization</strong></td>
<td>Native macOS</td>
<td>Extensive</td>
<td>Extensive</td>
<td>–</td>
<td>Extensive</td>
<td>Custom</td>
<td>CSS</td>
<td>Native macOS</td>
</tr>
</tbody></table>
<p>All terminals support background opacity, font fallback chains, colored bold, and block&#x2F;beam&#x2F;underline cursor styles.</p>
<h3 id="Developer-facing-features"><a href="#Developer-facing-features" class="headerlink" title="Developer-facing features"></a>Developer-facing features</h3><table>
<thead>
<tr>
<th>Feature</th>
<th align="center">Ghostty</th>
<th align="center">iTerm2</th>
<th align="center">Kitty</th>
<th align="center">Alacritty</th>
<th align="center">WezTerm</th>
<th align="center">Warp</th>
<th align="center">Hyper</th>
<th align="center">Terminal.app</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Vi mode (scrollback)</strong></td>
<td align="center">No</td>
<td align="center">Yes (copy mode)</td>
<td align="center">Pager-based</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>Scrollback search</strong></td>
<td align="center">Yes (1.3+, ⌘F)</td>
<td align="center">Yes</td>
<td align="center">Pager-based</td>
<td align="center">Yes (vi mode)</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">Yes</td>
</tr>
<tr>
<td><strong>Regex triggers</strong></td>
<td align="center">No</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">Yes (hints)</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>Broadcast to panes</strong></td>
<td align="center">No</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>Paste history</strong></td>
<td align="center">No</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>SSH multiplexing</strong></td>
<td align="center">No</td>
<td align="center">Yes (tmux)</td>
<td align="center">SSH kitten</td>
<td align="center">No</td>
<td align="center">Built-in</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>Command palette</strong></td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">Yes</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>AI integration</strong></td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">Yes (built-in)</td>
<td align="center">No</td>
<td align="center">No</td>
</tr>
<tr>
<td><strong>Block-based output</strong></td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">No</td>
<td align="center">Yes</td>
<td align="center">No</td>
<td align="center">No</td>
</tr>
</tbody></table>
<h3 id="Project-info"><a href="#Project-info" class="headerlink" title="Project info"></a>Project info</h3><table>
<thead>
<tr>
<th>Detail</th>
<th>Ghostty</th>
<th>iTerm2</th>
<th>Kitty</th>
<th>Alacritty</th>
<th>WezTerm</th>
<th>Warp</th>
<th>Hyper</th>
<th>Terminal.app</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Language</strong></td>
<td>Zig + Swift</td>
<td>Obj-C&#x2F;Swift</td>
<td>C + Python</td>
<td>Rust</td>
<td>Rust</td>
<td>Rust</td>
<td>Electron (JS)</td>
<td>Obj-C (Apple)</td>
</tr>
<tr>
<td><strong>License</strong></td>
<td>MIT</td>
<td>GPLv2</td>
<td>GPLv3</td>
<td>Apache 2.0</td>
<td>MIT</td>
<td>Proprietary</td>
<td>MIT</td>
<td>Proprietary</td>
</tr>
<tr>
<td><strong>First release</strong></td>
<td>2024</td>
<td>2010</td>
<td>2017</td>
<td>2017</td>
<td>2018</td>
<td>2022</td>
<td>2016</td>
<td>2001</td>
</tr>
<tr>
<td><strong>Account required</strong></td>
<td>No</td>
<td>No</td>
<td>No</td>
<td>No</td>
<td>No</td>
<td>Yes</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td><strong>Telemetry</strong></td>
<td>No</td>
<td>No</td>
<td>No</td>
<td>No</td>
<td>No</td>
<td>Yes</td>
<td>No</td>
<td>Yes (Apple)</td>
</tr>
<tr>
<td><strong>Install</strong></td>
<td>Homebrew &#x2F; .dmg</td>
<td>Homebrew &#x2F; .dmg</td>
<td>Homebrew &#x2F; installer</td>
<td>Homebrew &#x2F; cargo</td>
<td>Homebrew &#x2F; .dmg</td>
<td>.dmg</td>
<td>Homebrew &#x2F; .dmg</td>
<td>Pre-installed</td>
</tr>
</tbody></table>
<hr>
<h2 id="Detailed-breakdown"><a href="#Detailed-breakdown" class="headerlink" title="Detailed breakdown"></a>Detailed breakdown</h2><h3 id="Ghostty"><a href="#Ghostty" class="headerlink" title="Ghostty"></a><a href="https://ghostty.org/">Ghostty</a></h3><p>Created by Mitchell Hashimoto (HashiCorp co-founder). Written in Zig (with Swift&#x2F;AppKit for macOS GUI), GPU-accelerated via Metal (macOS) and OpenGL (Linux).</p>
<p><strong>What makes it different</strong>: Ghostty is the only GPU-native terminal that uses real platform UI. On macOS, it renders with AppKit – native title bar, native tabs, native menus. It doesn’t look like a cross-platform app pretending to be a Mac app. It looks like a Mac app.</p>
<p>The config format is the simplest of any terminal in this list – flat key&#x3D;value pairs, no nesting:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">font-family = FiraCode Nerd Font</span><br><span class="line">font-size = 14</span><br><span class="line">theme = catppuccin-mocha</span><br><span class="line">window-padding-x = 8</span><br><span class="line">window-padding-y = 4</span><br><span class="line">keybind = super+d=new_split:right</span><br><span class="line">keybind = super+shift+d=new_split:down</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Exceptional rendering performance on par with Alacritty</li>
<li>Native macOS experience (AppKit, not custom chrome), including system-aware native scrollbars (1.3)</li>
<li>Simplest config format – easy to learn, easy to share</li>
<li>Built-in splits without tmux, with drag handles to tear them into tabs&#x2F;windows (1.3)</li>
<li>Scrollback search with <code>⌘F</code> (1.3) and OSC 133 click-to-position prompt (1.3)</li>
<li>Large bundled theme collection</li>
<li>Active, rapid development; “Set as Default Terminal App” landed in 1.3</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>No Windows support</li>
<li>No profiles (one global config)</li>
<li>Younger than alternatives – some edge cases may not be handled yet</li>
</ul>
<p><strong>Linux:</strong> in addition to Homebrew, Ghostty is now in the Ubuntu 26.04 official repos (<code>apt install ghostty</code>).</p>
<p><strong>Best for</strong>: macOS developers who want speed without sacrificing native UX. If you care about your terminal feeling like a “real Mac app” and want sub-millisecond rendering, this is the one.</p>
<hr>
<h3 id="iTerm2"><a href="#iTerm2" class="headerlink" title="iTerm2"></a><a href="https://iterm2.com/">iTerm2</a></h3><p>The long-standing default upgrade from Terminal.app. Built by George Nachman, maintained for over 15 years.</p>
<p><strong>What makes it different</strong>: Feature density. iTerm2 has more features than any other terminal, period. The tmux integration alone (where remote tmux sessions render as native iTerm2 tabs and splits) is unique across all terminals. The Python scripting API lets you automate anything.</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># iTerm2 Python API example: auto-switch profile by hostname</span></span><br><span class="line"><span class="keyword">import</span> iterm2</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">main</span>(<span class="params">connection</span>):</span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">with</span> iterm2.SessionTerminationMonitor(connection) <span class="keyword">as</span> mon:</span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            session = <span class="keyword">await</span> mon.async_get()</span><br><span class="line">            <span class="keyword">if</span> <span class="string">&quot;production&quot;</span> <span class="keyword">in</span> session.name:</span><br><span class="line">                <span class="keyword">await</span> session.async_set_profile(<span class="string">&quot;Production&quot;</span>)</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Most feature-complete terminal on any platform</li>
<li>tmux control mode – remote tmux sessions as native tabs</li>
<li>Python scripting API for automation</li>
<li>Instant replay (rewind terminal output like a DVR)</li>
<li>Triggers (regex-based actions on output)</li>
<li>Paste history, autocomplete, smart selection</li>
<li>GUI preferences – no config file needed</li>
<li>Inline images (imgcat)</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Heavier resource usage than GPU-native terminals</li>
<li>macOS only</li>
<li>Startup is slower than lightweight alternatives</li>
<li>Can feel bloated if you only need basics</li>
</ul>
<p><strong>Best for</strong>: Power users who want everything in one terminal. If you SSH into servers frequently, use tmux, need per-host profiles, or want a scripting API, iTerm2 is hard to beat.</p>
<hr>
<h3 id="Kitty"><a href="#Kitty" class="headerlink" title="Kitty"></a><a href="https://sw.kovidgoyal.net/kitty/">Kitty</a></h3><p>Written in C and Python by Kovid Goyal. GPU-rendered from the ground up.</p>
<p><strong>What makes it different</strong>: Kittens. Kitty’s extension system lets you write Python scripts that run inside the terminal process, with access to screen content, terminal state, and input. Built-in kittens include <code>icat</code> (inline images), <code>diff</code> (side-by-side diffs), <code>hints</code> (clickable URLs&#x2F;paths), and <code>ssh</code> (auto-copy terminfo to remote hosts). The Kitty graphics protocol is becoming a de facto standard adopted by other tools.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Display an image inline</span></span><br><span class="line">kitty +kitten icat photo.jpg</span><br><span class="line"></span><br><span class="line"><span class="comment"># Side-by-side diff</span></span><br><span class="line">kitty +kitten diff file1.py file2.py</span><br><span class="line"></span><br><span class="line"><span class="comment"># Copy terminfo + shell integration to a remote host</span></span><br><span class="line">kitty +kitten ssh user@host</span><br></pre></td></tr></table></figure>

<p>Kitty’s built-in layouts replace basic tmux usage:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># kitty.conf -- tiling layouts</span><br><span class="line">enabled_layouts tall:bias=70,fat,grid,splits,stack</span><br><span class="line">map ctrl+shift+l next_layout</span><br><span class="line">map ctrl+shift+enter new_window</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Kittens extension system – deeply programmable</li>
<li>Graphics protocol becoming an industry standard</li>
<li>Excellent tiling layouts without tmux</li>
<li>SSH kitten (auto-copies terminfo, shell integration)</li>
<li>Scrollback via configurable pager (vim, less, etc.)</li>
<li>Broadcast input to all panes</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Requires configuration to feel native on macOS</li>
<li>Community friction (author has strong opinions in issue tracker)</li>
<li>No GUI settings</li>
<li>Kitten API has a learning curve</li>
</ul>
<p><strong>Best for</strong>: Developers who want a programmable terminal. If you write custom tools, display images&#x2F;charts in the terminal, or want deep control without tmux, Kitty’s extensibility is unmatched.</p>
<hr>
<h3 id="Alacritty"><a href="#Alacritty" class="headerlink" title="Alacritty"></a><a href="https://alacritty.org/">Alacritty</a></h3><p>Famously billed as the “fastest terminal emulator in existence.” Written in Rust, GPU-accelerated.</p>
<p><strong>What makes it different</strong>: Radical minimalism. Alacritty deliberately omits tabs, splits, ligatures, and inline images. The philosophy: the terminal should render text fast and nothing else. Use tmux or your window manager for multiplexing. This means less code, fewer bugs, and the absolute best performance.</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># alacritty.toml -- that&#x27;s really all you need</span></span><br><span class="line"><span class="section">[font]</span></span><br><span class="line"><span class="attr">normal.family</span> = <span class="string">&quot;FiraCode Nerd Font&quot;</span></span><br><span class="line"><span class="attr">size</span> = <span class="number">14.0</span></span><br><span class="line"></span><br><span class="line"><span class="section">[window]</span></span><br><span class="line"><span class="attr">opacity</span> = <span class="number">0.95</span></span><br><span class="line"><span class="attr">padding</span> = &#123; x = <span class="number">8</span>, y = <span class="number">4</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="section">[colors.primary]</span></span><br><span class="line"><span class="attr">background</span> = <span class="string">&quot;#1e1e2e&quot;</span></span><br><span class="line"><span class="attr">foreground</span> = <span class="string">&quot;#cdd6f4&quot;</span></span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Fastest and lightest terminal available – the performance benchmark</li>
<li>Rock solid stability – minimal code means minimal bugs</li>
<li>Broadest platform support (macOS, Linux, Windows, BSD)</li>
<li>TOML config – clean, well-documented</li>
<li>Vi mode for scrollback navigation</li>
<li>Consistent behavior across all platforms</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>No tabs or splits (by design – use tmux)</li>
<li>No ligature support (long-standing, intentional decision)</li>
<li>No inline images</li>
<li>No scripting or plugins</li>
</ul>
<p><strong>Best for</strong>: tmux users who want the fastest possible rendering layer. If tmux is already your multiplexer and you don’t need ligatures or inline images, Alacritty is pure speed with zero bloat.</p>
<hr>
<h3 id="WezTerm"><a href="#WezTerm" class="headerlink" title="WezTerm"></a><a href="https://wezterm.org/">WezTerm</a></h3><p>Written in Rust by Wez Furlong. GPU-accelerated with a Lua configuration engine.</p>
<p><strong>What makes it different</strong>: Lua. Your config isn’t a static file – it’s a program. You can write conditionals, functions, import modules, and react to events. WezTerm also has the best built-in multiplexer of any terminal: tabs, splits, and workspaces that can genuinely replace tmux. The built-in SSH client with multiplexing means you get your full WezTerm experience on remote servers without installing anything.</p>
<figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- wezterm.lua -- conditional config based on hostname</span></span><br><span class="line"><span class="keyword">local</span> wezterm = <span class="built_in">require</span> <span class="string">&#x27;wezterm&#x27;</span></span><br><span class="line"><span class="keyword">local</span> <span class="built_in">config</span> = wezterm.config_builder()</span><br><span class="line"></span><br><span class="line"><span class="built_in">config</span>.font = wezterm.font(<span class="string">&#x27;FiraCode Nerd Font&#x27;</span>)</span><br><span class="line"><span class="built_in">config</span>.font_size = <span class="number">14.0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- Different theme on work vs personal machine</span></span><br><span class="line"><span class="keyword">if</span> wezterm.hostname() == <span class="string">&#x27;work-laptop&#x27;</span> <span class="keyword">then</span></span><br><span class="line">  <span class="built_in">config</span>.color_scheme = <span class="string">&#x27;Catppuccin Latte&#x27;</span></span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">  <span class="built_in">config</span>.color_scheme = <span class="string">&#x27;Catppuccin Mocha&#x27;</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- Custom status bar</span></span><br><span class="line">wezterm.on(<span class="string">&#x27;update-status&#x27;</span>, <span class="function"><span class="keyword">function</span><span class="params">(window)</span></span></span><br><span class="line">  <span class="keyword">local</span> <span class="built_in">date</span> = wezterm.strftime(<span class="string">&#x27;%H:%M&#x27;</span>)</span><br><span class="line">  window:set_right_status(<span class="built_in">date</span>)</span><br><span class="line"><span class="keyword">end</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="built_in">config</span></span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Lua config – full programming language, not just key&#x3D;value</li>
<li>Best built-in multiplexer (can genuinely replace tmux)</li>
<li>SSH multiplexing (remote tabs&#x2F;splits without tmux on server)</li>
<li>Supports all three inline image protocols (iTerm2, Kitty, Sixel)</li>
<li>Excellent native Windows support</li>
<li>Serial port support (embedded development)</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Lua config is overkill for simple setups</li>
<li>Heavier than Alacritty&#x2F;Ghostty</li>
<li>Non-native window chrome on macOS</li>
<li>Documentation can be hard to navigate</li>
<li>Development pace has slowed in 2025-2026</li>
</ul>
<p><strong>Best for</strong>: Power users who want maximum programmability and cross-platform consistency. If you work on macOS + Linux + Windows, or want to replace tmux entirely, WezTerm is the most capable all-in-one option.</p>
<hr>
<h3 id="Warp"><a href="#Warp" class="headerlink" title="Warp"></a><a href="https://www.warp.dev/">Warp</a></h3><p>Rust-based, GPU-accelerated, with a fundamentally different UX model.</p>
<p><strong>What makes it different</strong>: Warp treats the terminal like an IDE. Output is grouped into collapsible blocks per command. There’s a persistent input area at the bottom (like a text editor, with cursor navigation). AI is built in – describe what you want in natural language, and Warp suggests the command. Workflows (saved command templates with parameters) let you share complex commands with your team.</p>
<p><strong>Strengths:</strong></p>
<ul>
<li>AI command suggestions – natural language to shell</li>
<li>Block-based output – collapse, copy, or share individual commands</li>
<li>Modern text editing in the input area (multi-cursor, select, undo)</li>
<li>Workflows – parameterized command templates</li>
<li>Team features (shared workflows, team themes)</li>
<li>Polished, approachable UI</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li><strong>Account required</strong> – you must sign up to use a terminal (controversial)</li>
<li><strong>Proprietary</strong> – closed source</li>
<li><strong>Telemetry</strong> – sends usage data</li>
<li>macOS and Linux only (no Windows)</li>
<li>Heavier memory footprint (~150 MB)</li>
<li>Non-standard UX can clash with existing muscle memory</li>
</ul>
<p><strong>Pricing (2026):</strong> terminal features are free across all platforms; only AI is metered. The free tier ships 75-150 monthly credits. Build is $20&#x2F;mo (1,500 credits), Business $50&#x2F;user&#x2F;mo, with batched&#x2F;flex&#x2F;priority queueing on top. So if you only want the block-based UX, never pay.</p>
<p><strong>Best for</strong>: Developers new to the terminal who want guidance, or teams that benefit from shared workflows. If the account requirement and closed source don’t bother you, the UX is genuinely innovative.</p>
<hr>
<h3 id="Hyper"><a href="#Hyper" class="headerlink" title="Hyper"></a><a href="https://hyper.is/">Hyper</a></h3><p>Built on Electron by Vercel (the Next.js company). HTML, CSS, and JavaScript all the way down. Themes are CSS, plugins are npm packages. Beautiful out of the box, but Electron means significantly slower rendering (~200 MB memory, noticeable input latency). Development has slowed considerably. <strong>Best for</strong> web developers who want CSS&#x2F;JS customization and don’t mind the performance trade-off. Not recommended for heavy CLI workloads.</p>
<hr>
<h2 id="Decision-guide"><a href="#Decision-guide" class="headerlink" title="Decision guide"></a>Decision guide</h2><table>
<thead>
<tr>
<th>What you want</th>
<th>Best pick</th>
<th>Runner-up</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Raw speed</strong></td>
<td><strong>Alacritty</strong> – minimal code, maximum performance</td>
<td>Ghostty</td>
</tr>
<tr>
<td><strong>macOS native feel</strong></td>
<td><strong>Ghostty</strong> – real AppKit UI, not custom chrome</td>
<td>iTerm2</td>
</tr>
<tr>
<td><strong>Most features</strong></td>
<td><strong>iTerm2</strong> – 15 years of feature accumulation</td>
<td>WezTerm</td>
</tr>
<tr>
<td><strong>Drop tmux entirely</strong></td>
<td><strong>WezTerm</strong> – best built-in multiplexer</td>
<td>Kitty</td>
</tr>
<tr>
<td><strong>tmux power user</strong></td>
<td><strong>Alacritty</strong> – fastest rendering layer</td>
<td>Ghostty</td>
</tr>
<tr>
<td><strong>SSH to many servers</strong></td>
<td><strong>iTerm2</strong> – native tmux control mode</td>
<td>WezTerm (SSH mux)</td>
</tr>
<tr>
<td><strong>Extensibility</strong></td>
<td><strong>Kitty</strong> (Python kittens) or <strong>WezTerm</strong> (Lua)</td>
<td>–</td>
</tr>
<tr>
<td><strong>Cross-platform (incl. Windows)</strong></td>
<td><strong>WezTerm</strong> or <strong>Alacritty</strong></td>
<td>–</td>
</tr>
<tr>
<td><strong>Simplest config</strong></td>
<td><strong>Ghostty</strong> – flat key&#x3D;value, no nesting</td>
<td>Alacritty (TOML)</td>
</tr>
<tr>
<td><strong>New to the terminal</strong></td>
<td><strong>Warp</strong> – AI guidance, IDE-like UX</td>
<td>iTerm2 (GUI)</td>
</tr>
<tr>
<td><strong>Privacy &#x2F; no telemetry</strong></td>
<td><strong>Alacritty</strong>, <strong>Ghostty</strong>, or <strong>Kitty</strong></td>
<td>–</td>
</tr>
<tr>
<td><strong>Team collaboration</strong></td>
<td><strong>Warp</strong> – shared workflows</td>
<td>–</td>
</tr>
</tbody></table>
<h3 id="Migration-path-from-Terminal-app"><a href="#Migration-path-from-Terminal-app" class="headerlink" title="Migration path from Terminal.app"></a>Migration path from Terminal.app</h3><p>Most macOS developers follow this progression:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Terminal.app → iTerm2 → (split here)</span><br><span class="line">                          ├→ Ghostty (want native + fast)</span><br><span class="line">                          ├→ Alacritty + tmux (want minimal + fast)</span><br><span class="line">                          ├→ Kitty (want extensible)</span><br><span class="line">                          └→ WezTerm (want all-in-one)</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="Installing"><a href="#Installing" class="headerlink" title="Installing"></a>Installing</h2><p>All terminals are available via Homebrew:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install --cask ghostty</span><br><span class="line">brew install --cask iterm2</span><br><span class="line">brew install --cask kitty</span><br><span class="line">brew install --cask alacritty</span><br><span class="line">brew install --cask wezterm</span><br><span class="line">brew install --cask warp</span><br><span class="line">brew install --cask hyper</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="Verdict"><a href="#Verdict" class="headerlink" title="Verdict"></a>Verdict</h2><p>There’s no single “best” terminal – it depends on what you value. But here’s how I’d summarize the landscape in 2026:</p>
<ul>
<li><strong>Ghostty</strong> is the new default recommendation for macOS. Native feel, excellent performance, simple config. It’s what Terminal.app would be if Apple rebuilt it today.</li>
<li><strong>iTerm2</strong> remains king for feature completeness. If you need a specific capability, iTerm2 probably has it.</li>
<li><strong>Kitty</strong> is the hacker’s terminal. Kittens and the graphics protocol give you capabilities no other terminal offers.</li>
<li><strong>Alacritty</strong> is the purist’s choice. Fastest, lightest, most stable – if you’re already married to tmux.</li>
<li><strong>WezTerm</strong> is the Swiss Army knife. Does everything, runs everywhere, configured with a real programming language.</li>
<li><strong>Warp</strong> is genuinely innovative but the account requirement and closed source are dealbreakers for many developers.</li>
</ul>
<p>Pick one, configure it, and move on. The best terminal is the one you stop thinking about.</p>
]]></content>
      <categories>
        <category>Mac</category>
      </categories>
      <tags>
        <tag>Terminal</tag>
        <tag>Ghostty</tag>
        <tag>iTerm2</tag>
        <tag>Kitty</tag>
        <tag>Alacritty</tag>
        <tag>WezTerm</tag>
        <tag>Warp</tag>
        <tag>CLI</tag>
      </tags>
  </entry>
  <entry>
    <title>Git Solutions Collection</title>
    <url>/Git-Solutions-Collection/</url>
    <content><![CDATA[<p>Recipes for the Git situations that come up often enough to forget the exact incantation.</p>
<span id="more"></span>

<h2 id="Amend-the-last-commit"><a href="#Amend-the-last-commit" class="headerlink" title="Amend the last commit"></a>Amend the last commit</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git commit --amend                <span class="comment"># also opens $EDITOR for the message</span></span><br><span class="line">git commit --amend --no-edit      <span class="comment"># keep the existing message</span></span><br></pre></td></tr></table></figure>

<p>Don’t amend a commit that’s already been pushed to a shared branch; force-push rewrites others’ history.</p>
<h2 id="Force-push-safely"><a href="#Force-push-safely" class="headerlink" title="Force-push safely"></a>Force-push safely</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git push --force-with-lease</span><br></pre></td></tr></table></figure>

<p>Pushes only if the remote ref is still exactly what was last fetched locally. Catches the case where someone else pushed to the same branch in the meantime. Plain <code>--force</code> will obliterate that work.</p>
<h2 id="Switch-branches-and-discard-changes-modern-verbs"><a href="#Switch-branches-and-discard-changes-modern-verbs" class="headerlink" title="Switch branches and discard changes (modern verbs)"></a>Switch branches and discard changes (modern verbs)</h2><p><code>switch</code> and <code>restore</code> (Git 2.23+) replace the overloaded <code>checkout</code>:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git switch other-branch                  <span class="comment"># was: git checkout other-branch</span></span><br><span class="line">git switch -c new-branch                 <span class="comment"># create + switch</span></span><br><span class="line">git restore .                            <span class="comment"># discard all unstaged changes</span></span><br><span class="line">git restore --staged .                   <span class="comment"># unstage everything</span></span><br><span class="line">git restore --<span class="built_in">source</span>=HEAD~3 file.js      <span class="comment"># pull a single file from 3 commits ago</span></span><br></pre></td></tr></table></figure>

<h2 id="Pull-until-a-specific-commit"><a href="#Pull-until-a-specific-commit" class="headerlink" title="Pull until a specific commit"></a>Pull until a specific commit</h2><p><a href="https://stackoverflow.com/questions/31462683/git-pull-till-a-particular-commit">Stack Overflow</a>:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git fetch origin &lt;branch&gt;</span><br><span class="line">git merge &lt;commit_hash&gt;</span><br></pre></td></tr></table></figure>

<h2 id="“Your-branch-and-‘origin-main’-have-diverged”"><a href="#“Your-branch-and-‘origin-main’-have-diverged”" class="headerlink" title="“Your branch and ‘origin&#x2F;main’ have diverged”"></a>“Your branch and ‘origin&#x2F;main’ have diverged”</h2><p><a href="https://stackoverflow.com/questions/19864934/git-your-branch-and-origin-master-have-diverged-how-to-throw-away-local-com">Stack Overflow</a>. Discards local commits in favour of the remote:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git fetch origin</span><br><span class="line">git reset --hard origin/main             <span class="comment"># or origin/master</span></span><br></pre></td></tr></table></figure>

<h2 id="Edit-squash-or-reorder-recent-commits"><a href="#Edit-squash-or-reorder-recent-commits" class="headerlink" title="Edit, squash, or reorder recent commits"></a>Edit, squash, or reorder recent commits</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git rebase -i HEAD~5</span><br></pre></td></tr></table></figure>

<p>In the rebase-todo editor: <code>pick</code> keeps the commit, <code>reword</code> opens the message, <code>edit</code> pauses for amends, <code>squash</code>&#x2F;<code>fixup</code> fold the commit into the previous one, deleting a line drops the commit entirely.</p>
<h2 id="Cherry-pick-a-single-commit"><a href="#Cherry-pick-a-single-commit" class="headerlink" title="Cherry-pick a single commit"></a>Cherry-pick a single commit</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git cherry-pick &lt;<span class="built_in">hash</span>&gt;</span><br><span class="line">git cherry-pick &lt;hash1&gt;^..&lt;hash2&gt;        <span class="comment"># range, inclusive of both ends</span></span><br></pre></td></tr></table></figure>

<h2 id="Find-the-commit-that-broke-something"><a href="#Find-the-commit-that-broke-something" class="headerlink" title="Find the commit that broke something"></a>Find the commit that broke something</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git bisect start</span><br><span class="line">git bisect bad                           <span class="comment"># current revision is broken</span></span><br><span class="line">git bisect good &lt;known_good_hash&gt;</span><br><span class="line"><span class="comment"># Then test each revision Git checks out and mark it:</span></span><br><span class="line">git bisect good   <span class="comment"># or</span></span><br><span class="line">git bisect bad</span><br><span class="line"><span class="comment"># When it converges, exit:</span></span><br><span class="line">git bisect reset</span><br></pre></td></tr></table></figure>

<h2 id="Work-on-multiple-branches-in-parallel-worktrees"><a href="#Work-on-multiple-branches-in-parallel-worktrees" class="headerlink" title="Work on multiple branches in parallel (worktrees)"></a>Work on multiple branches in parallel (worktrees)</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git worktree add ../proj-fix bugfix-branch</span><br><span class="line"><span class="comment"># Same .git, separate working tree. Edit both checkouts in parallel.</span></span><br><span class="line">git worktree remove ../proj-fix</span><br></pre></td></tr></table></figure>

<h2 id="Recover-a-deleted-branch"><a href="#Recover-a-deleted-branch" class="headerlink" title="Recover a deleted branch"></a>Recover a deleted branch</h2><p>The reflog remembers commits even when no ref points to them:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reflog                                <span class="comment"># locate the tip SHA</span></span><br><span class="line">git switch -c recovered-branch &lt;sha&gt;</span><br></pre></td></tr></table></figure>

<h2 id="Recover-a-dropped-stash-entry"><a href="#Recover-a-dropped-stash-entry" class="headerlink" title="Recover a dropped stash entry"></a><a href="https://stackoverflow.com/questions/89332/how-to-recover-a-dropped-stash-in-git">Recover a dropped stash entry</a></h2><p>If you ran the following by mistake:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git stash pop</span><br><span class="line">git checkout -- .</span><br></pre></td></tr></table></figure>

<p>Find the dangling commit and reapply it:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gitk --all $( git fsck --no-reflog | awk <span class="string">&#x27;/dangling commit/ &#123;print $3&#125;&#x27;</span> )</span><br><span class="line">git stash apply &lt;stash_hash&gt;</span><br></pre></td></tr></table></figure>

<h2 id="Remove-all-unreachable-objects"><a href="#Remove-all-unreachable-objects" class="headerlink" title="Remove all unreachable objects"></a><a href="https://stackoverflow.com/questions/3765234/listing-and-deleting-git-commits-that-are-under-no-branch-dangling/4528593#4528593">Remove all unreachable objects</a></h2><blockquote>
<p>Warning: also removes any stashed entries that aren’t referenced by a branch.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reflog expire --expire-unreachable=now --all</span><br><span class="line">git gc --prune=now</span><br></pre></td></tr></table></figure>

<h2 id="Commit-case-sensitive-only-filename-changes"><a href="#Commit-case-sensitive-only-filename-changes" class="headerlink" title="Commit case-sensitive-only filename changes"></a><a href="https://www.geeksforgeeks.org/git/how-to-commit-case-sensitive-only-filename-changes-in-git/">Commit case-sensitive-only filename changes</a></h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global core.ignorecase <span class="literal">false</span></span><br><span class="line">git <span class="built_in">mv</span> filename.txt Filename.txt</span><br><span class="line">git commit -m <span class="string">&quot;Case sensitive file rename&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="Untrack-files-newly-added-to-gitignore"><a href="#Untrack-files-newly-added-to-gitignore" class="headerlink" title="Untrack files newly added to .gitignore"></a><a href="https://stackoverflow.com/questions/1274057/how-do-i-make-git-forget-about-a-file-that-was-tracked-but-is-now-in-gitignore">Untrack files newly added to <code>.gitignore</code></a></h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">rm</span> --cached &lt;file&gt;</span><br><span class="line">git <span class="built_in">rm</span> -r --cached &lt;folder&gt;              <span class="comment"># recursive, for directories</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Git</category>
      </categories>
      <tags>
        <tag>CLI</tag>
      </tags>
  </entry>
  <entry>
    <title>Node.js in 2026: pick your stack</title>
    <url>/Node-js-Usage/</url>
    <content><![CDATA[<p>The 2021 Node stack was Node + npm + nvm and a third-party tool for everything else. By 2026 every layer has a serious challenger, and Node itself absorbed half of what used to be a separate dependency: test runner, watch mode, dotenv, native TypeScript. This is the snapshot of what to pick and why.</p>
<span id="more"></span>

<h2 id="Pick-a-Node-version"><a href="#Pick-a-Node-version" class="headerlink" title="Pick a Node version"></a>Pick a Node version</h2><p>Node ships an LTS each October on even-numbered majors. As of May 2026:</p>
<table>
<thead>
<tr>
<th>Version</th>
<th>Status</th>
<th>Use it when</th>
</tr>
</thead>
<tbody><tr>
<td>22</td>
<td>Maintenance LTS (until Apr 2027)</td>
<td>An existing project that won’t budge</td>
</tr>
<tr>
<td>24</td>
<td>Active LTS (until Oct 2026, then Maintenance)</td>
<td>Default for new work</td>
</tr>
<tr>
<td>26</td>
<td>Current (becomes Active LTS Oct 2026)</td>
<td>Trying out features that haven’t crystallised</td>
</tr>
</tbody></table>
<p>Default to <strong>24 LTS</strong> for new projects unless a dependency pins something older.</p>
<h2 id="Version-managers"><a href="#Version-managers" class="headerlink" title="Version managers"></a>Version managers</h2><table>
<thead>
<tr>
<th>Tool</th>
<th>Language</th>
<th>Speed</th>
<th>Notes</th>
</tr>
</thead>
<tbody><tr>
<td><code>n</code></td>
<td>shell</td>
<td>medium</td>
<td>Simple, sudo-required, Node-only</td>
</tr>
<tr>
<td><code>nvm</code></td>
<td>bash</td>
<td>slow shell startup</td>
<td>What every Stack Overflow answer assumes; sources a long bash script in every shell</td>
</tr>
<tr>
<td><a href="https://github.com/Schniz/fnm"><code>fnm</code></a></td>
<td>Rust</td>
<td>fast</td>
<td>Drop-in nvm replacement; reads <code>.nvmrc</code> and <code>.node-version</code></td>
</tr>
<tr>
<td><a href="https://volta.sh/"><code>volta</code></a></td>
<td>Rust</td>
<td>fast</td>
<td>Project versions baked into <code>package.json</code>; release cadence has been quiet</td>
</tr>
<tr>
<td><a href="https://mise.jdx.dev/"><code>mise</code></a></td>
<td>Rust</td>
<td>fast</td>
<td>Polyglot: Node + Python + Ruby + Go in one tool. asdf successor</td>
</tr>
<tr>
<td>Homebrew (<code>node@24</code>)</td>
<td>n&#x2F;a</td>
<td>n&#x2F;a</td>
<td>Fine for one global Node, painful for multi-version</td>
</tr>
</tbody></table>
<p>Pick by need:</p>
<ul>
<li>One global Node, no project pinning: Homebrew</li>
<li>Multiple Node versions, only Node: <code>fnm</code></li>
<li>Multiple runtimes (Node + Python + Ruby + …): <code>mise</code></li>
</ul>
<p><code>mise</code> reads <code>.tool-versions</code> (asdf format) and <code>.node-version</code>, so it inherits whatever pin a project already has. See <a href="/M1-Mac-Development-Setup/" title="Mac development setup">Mac development setup</a> for the wider polyglot story.</p>
<h2 id="Package-managers"><a href="#Package-managers" class="headerlink" title="Package managers"></a>Package managers</h2><table>
<thead>
<tr>
<th>Manager</th>
<th>Install</th>
<th>Speed</th>
<th>Disk</th>
</tr>
</thead>
<tbody><tr>
<td>npm</td>
<td>bundled</td>
<td>baseline</td>
<td>duplicates per project</td>
</tr>
<tr>
<td><a href="https://pnpm.io/">pnpm</a></td>
<td><code>corepack enable</code></td>
<td>fast</td>
<td>content-addressable global store (saves GBs across projects)</td>
</tr>
<tr>
<td><a href="https://yarnpkg.com/">yarn</a></td>
<td><code>corepack enable</code></td>
<td>fast</td>
<td>PnP or <code>node_modules</code>. Yarn 4 (berry) is the modern line; Yarn 1.x classic is unmaintained</td>
</tr>
<tr>
<td><a href="https://bun.sh/">Bun</a></td>
<td><code>brew install bun</code></td>
<td>fastest</td>
<td>global cache</td>
</tr>
</tbody></table>
<p><a href="https://nodejs.org/api/corepack.html"><strong>Corepack</strong></a> ships with Node and pins package-manager versions per project via the <code>packageManager</code> field in <code>package.json</code>. Enable it once globally; thereafter <code>pnpm</code> and <code>yarn</code> resolve to whatever each project pins, with no global-version drift across machines.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">corepack <span class="built_in">enable</span></span><br><span class="line"><span class="comment"># Then in package.json:</span></span><br><span class="line"><span class="comment"># &quot;packageManager&quot;: &quot;pnpm@9.13.2&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="Runtime-alternatives"><a href="#Runtime-alternatives" class="headerlink" title="Runtime alternatives"></a>Runtime alternatives</h2><p>Node is no longer the only thing that runs <code>package.json</code> projects:</p>
<ul>
<li><strong><a href="https://bun.sh/">Bun</a></strong> (1.x in 2026): runtime, bundler, test runner, and package manager in one Zig binary. Imports npm packages directly, runs most Node code unchanged. Faster cold start and noticeably faster <code>bun install</code>. Production-ready for backend services and CLIs; some native modules still flake on edge cases.</li>
<li><strong><a href="https://deno.com/">Deno</a></strong> (2.x): runs npm packages through <code>npm:</code> specifiers and ships a stable Node compatibility layer. Tighter security model (explicit <code>--allow-net</code>, <code>--allow-read</code>). Worth picking for first-class TypeScript and a smaller install surface.</li>
<li><strong>Node.js</strong> stays the safest default for libraries that depend on Node-specific behaviour. The 24 LTS feature gap with Bun&#x2F;Deno has narrowed: test runner, watch mode, dotenv, and TypeScript all ship in stock Node now.</li>
</ul>
<h2 id="Modern-Node-js-24-LTS"><a href="#Modern-Node-js-24-LTS" class="headerlink" title="Modern Node.js (24 LTS)"></a>Modern Node.js (24 LTS)</h2><p>What no longer needs a third-party dependency:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Watch mode (re-run on file change)</span></span><br><span class="line">node --watch app.js</span><br><span class="line"></span><br><span class="line"><span class="comment"># Test runner</span></span><br><span class="line">node --<span class="built_in">test</span></span><br><span class="line">node --<span class="built_in">test</span> --watch <span class="built_in">test</span>/</span><br><span class="line"></span><br><span class="line"><span class="comment"># Dotenv</span></span><br><span class="line">node --env-file=.<span class="built_in">env</span> app.js</span><br><span class="line"></span><br><span class="line"><span class="comment"># TypeScript: type-stripping is default in 23.6+; transform (enums, namespaces)</span></span><br><span class="line"><span class="comment"># is still flag-gated through 24.</span></span><br><span class="line">node app.ts</span><br><span class="line">node --experimental-transform-types app.ts</span><br></pre></td></tr></table></figure>

<p>A test, written against <code>node:test</code>:</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; test &#125; <span class="keyword">from</span> <span class="string">&#x27;node:test&#x27;</span>;</span><br><span class="line"><span class="keyword">import</span> assert <span class="keyword">from</span> <span class="string">&#x27;node:assert/strict&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="title function_">test</span>(<span class="string">&#x27;addition&#x27;</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  assert.<span class="title function_">equal</span>(<span class="number">1</span> + <span class="number">1</span>, <span class="number">2</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p><code>node --test</code> finds it without config, tsconfig, or babel preset.</p>
<h2 id="Modules-ESM-is-the-default"><a href="#Modules-ESM-is-the-default" class="headerlink" title="Modules: ESM is the default"></a>Modules: ESM is the default</h2><p><code>package.json</code> decides module resolution; <code>&quot;type&quot;: &quot;module&quot;</code> makes the whole project ESM:</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;type&quot;</span><span class="punctuation">:</span> <span class="string">&quot;module&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>Extension</th>
<th>Behavior</th>
</tr>
</thead>
<tbody><tr>
<td><code>.mjs</code></td>
<td>Always ESM</td>
</tr>
<tr>
<td><code>.cjs</code></td>
<td>Always CommonJS</td>
</tr>
<tr>
<td><code>.js</code></td>
<td>Follows the nearest <code>&quot;type&quot;</code> field</td>
</tr>
</tbody></table>
<p>ESM features worth using day-to-day:</p>
<ul>
<li>Top-level <code>await</code> (no IIFE wrapper around async startup)</li>
<li><code>import.meta.url</code> and <code>import.meta.dirname</code> replace <code>__dirname</code></li>
<li><code>require(esm)</code> works synchronously in Node 22+ behind a flag, default-on in 24</li>
</ul>
<h2 id="Cheat-sheet"><a href="#Cheat-sheet" class="headerlink" title="Cheat sheet"></a>Cheat sheet</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># REPL</span></span><br><span class="line">node</span><br><span class="line"></span><br><span class="line"><span class="comment"># Run with watch + dotenv + TypeScript in one go</span></span><br><span class="line">node --watch --env-file=.<span class="built_in">env</span> app.ts</span><br><span class="line"></span><br><span class="line"><span class="comment"># List downloaded versions</span></span><br><span class="line">n               <span class="comment"># interactive picker</span></span><br><span class="line">fnm list</span><br><span class="line">mise list node</span><br><span class="line"></span><br><span class="line"><span class="comment"># Install latest LTS</span></span><br><span class="line"><span class="built_in">sudo</span> n lts</span><br><span class="line">fnm install --lts</span><br><span class="line">mise use -g node@lts</span><br><span class="line"></span><br><span class="line"><span class="comment"># Bump dependencies</span></span><br><span class="line">npx npm-check-updates -u   <span class="comment"># write package.json</span></span><br><span class="line">npm install                <span class="comment"># resolve</span></span><br><span class="line">pnpm update --latest --interactive   <span class="comment"># pnpm equivalent</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>Node.js</tag>
        <tag>npm</tag>
        <tag>pnpm</tag>
        <tag>Bun</tag>
        <tag>Deno</tag>
        <tag>fnm</tag>
        <tag>mise</tag>
        <tag>Volta</tag>
        <tag>Corepack</tag>
      </tags>
  </entry>
  <entry>
    <title>WWDC 2008 revisited: the four sessions still shaping iOS</title>
    <url>/WWDC-2008-Revisited/</url>
    <content><![CDATA[<p>WWDC 2008 was the year third-party iPhone development became possible. The SDK opened in March, the App Store opened in July, and Apple shipped four design-principle sessions that year that still describe the shape of every iOS app I write in 2026: 312 (the SDK runtime), 348 (the Cocoa philosophy), 940 (Obj-C 2.0 properties and protocols), and 382 (the GCD preview). Three of the four bets paid off in the form they were originally pitched. One had to be rewritten twice before it stuck.</p>
<span id="more"></span>

<h2 id="The-four-sessions"><a href="#The-four-sessions" class="headerlink" title="The four sessions"></a>The four sessions</h2><table>
<thead>
<tr>
<th>Session</th>
<th>Subject</th>
<th>What it shipped</th>
<th>What survives in 2026</th>
</tr>
</thead>
<tbody><tr>
<td><strong>312</strong> iPhone Application Development Fundamentals</td>
<td>The SDK runtime</td>
<td><code>UIApplication</code> + <code>UIWindow</code> + <code>UIViewController</code> + <code>AppDelegate</code></td>
<td>The same four classes, in the same arrangement, in every iOS app</td>
</tr>
<tr>
<td><strong>348</strong> Cocoa Fundamentals</td>
<td>The design philosophy</td>
<td>MVC, delegation, target-action, KVO, <code>NSNotificationCenter</code>, retain&#x2F;release</td>
<td>All of the above (KVO via Combine, retain&#x2F;release via ARC)</td>
</tr>
<tr>
<td><strong>940</strong> Mastering Advanced Objective-C Features</td>
<td>The language layer</td>
<td><code>@property</code>, fast enumeration, <code>@protocol</code>, GC (Mac-only), 64-bit runtime</td>
<td>Properties, <code>for x in collection</code>, protocols (the GC pitch died)</td>
</tr>
<tr>
<td><strong>382</strong> Simplifying Multicore with GCD</td>
<td>The concurrency philosophy</td>
<td>“Queues, not threads” (preview only)</td>
<td>The same principle, restated through blocks (2009), <code>Operation</code> (2011), Combine (2019), <code>async</code>&#x2F;<code>await</code> (2021), Actors (2021)</td>
</tr>
</tbody></table>
<p>What strikes me reading these back to back is the <strong>substrate vs syntax</strong> split. The substrate (the four classes from 312, the broadcast contract from 348, the cooperative-pool bet from 382) is unchanged. The syntax around all three has been rewritten between two and four times. The mental model in your head when you write <code>Task { @MainActor in ... }</code> in 2026 is the same one in the head of the engineer who wrote <code>dispatch_async(dispatch_get_main_queue(), ^{ ... })</code> in 2010, which is the same one Apple was previewing on stage in 2008. The compiler check is new. The bet is not.</p>
<h2 id="The-312-reveal-a-runtime-that-owns-the-loop"><a href="#The-312-reveal-a-runtime-that-owns-the-loop" class="headerlink" title="The 312 reveal: a runtime that owns the loop"></a>The 312 reveal: a runtime that owns the loop</h2><p>Before 312, “an app” on a desktop OS was a <code>main()</code> function that drove its own runloop. You initialized your toolkit, opened a window, pumped events. The 312 reveal turned that inside out: the iPhone runtime owns the loop, your code lives inside <code>UIApplicationDelegate</code> callbacks, and the system tells you when to wake up, when to save state, when to release caches. You are a guest in the process.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 2026 AppDelegate, structurally identical to a 2008 one</span></span><br><span class="line"><span class="keyword">@main</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">AppDelegate</span>: <span class="title class_ inherited__">UIResponder</span>, <span class="title class_ inherited__">UIApplicationDelegate</span> &#123;</span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">application</span>(</span><br><span class="line">        <span class="keyword">_</span> <span class="params">application</span>: <span class="type">UIApplication</span>,</span><br><span class="line">        <span class="params">didFinishLaunchingWithOptions</span> <span class="params">launchOptions</span>: [<span class="type">UIApplication</span>.<span class="params">LaunchOptionsKey</span>: <span class="keyword">Any</span>]<span class="operator">?</span></span><br><span class="line">    ) -&gt; <span class="type">Bool</span> &#123;</span><br><span class="line">        <span class="comment">// configure Core Data, register fonts, register for remote notifications</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>The shape (singleton application, delegate protocol, lifecycle callbacks) is the 2008 reveal verbatim. Scenes (2019) added a layer above it for per-window state, but <code>application(_:didFinishLaunchingWithOptions:)</code> is still the canonical entry point, and it still does the same thing it did when the iPhone 3G shipped with 128 MB of RAM and one core.</p>
<p>The bet that paid: a system-managed lifecycle catches edge cases (incoming call, low-memory warning, push wake, scene disconnect) that hand-rolled main loops miss. Eighteen years of new edge cases (background fetch in iOS 4, background modes in iOS 7, scene lifecycle in iOS 13, BGTaskScheduler in iOS 13, Live Activities in iOS 16) all hung off the same delegate model. Apple never had to ask app developers to rewrite their entry point.</p>
<h2 id="The-348-reveal-how-the-framework-wants-to-be-talked-to"><a href="#The-348-reveal-how-the-framework-wants-to-be-talked-to" class="headerlink" title="The 348 reveal: how the framework wants to be talked to"></a>The 348 reveal: how the framework wants to be talked to</h2><p>If 312 is “here is the runtime”, 348 is “here is how to write code that fits the runtime”. Five patterns, repeated through every framework Apple ships:</p>
<ul>
<li><strong>MVC</strong>: Controller mediates; View and Model don’t know about each other.</li>
<li><strong>Delegation</strong>: an object holds a <code>weak</code> reference to a delegate and calls protocol methods on it for events. One class participates in many concerns without multiple inheritance.</li>
<li><strong>Target-action</strong>: <code>UIControl</code> reduces button taps and slider drags to a <code>(target, action)</code> selector pair. Predates blocks; cheaper than blocks.</li>
<li><strong>KVO</strong>: observe property changes by string key path. Modern Combine wraps this with type checking via <code>publisher(for: \.keyPath)</code>.</li>
<li><strong>NSNotificationCenter</strong>: broadcast when there is no clear delegate. One sender, many observers, no return value.</li>
</ul>
<p>Pick any modern detail screen in a real iOS app and you will find all five at work. The screen is a <code>UIViewController</code> (Controller) hosting a <code>UICollectionView</code> (View) backed by a repository object (Model). The collection view talks back through <code>UICollectionViewDataSource</code> and <code>UICollectionViewDelegate</code> (delegation). The Edit button is wired with <code>target: self, action: #selector(editTapped)</code> (target-action). Cross-device CloudKit sync arrives via <code>.NSPersistentStoreRemoteChange</code> (broadcast). The currently selected item’s ID surfaces to widgets via a Combine publisher on a <code>\.selectedItemID</code> key path (KVO with types).</p>
<p>That code would have read identically in 2010, with <code>[control addTarget:self action:@selector(editTapped:) forControlEvents:UIControlEventTouchUpInside]</code> instead of the <code>#selector</code> form. The only thing that changed is the spelling.</p>
<p>The one place 348 needed a refinement is the broadcast pattern. <code>NSNotificationCenter</code> itself is fine. What 348 did not anticipate is that CloudKit catchup republishes <code>.NSPersistentStoreRemoteChange</code> repeatedly during boot, so any consumer that does whole-store work from that notification needs to debounce or it stampedes itself. The pattern is right; the volume crossed a threshold the original designers did not see.</p>
<h2 id="The-940-reveal-the-language-Swift-inherited"><a href="#The-940-reveal-the-language-Swift-inherited" class="headerlink" title="The 940 reveal: the language Swift inherited"></a>The 940 reveal: the language Swift inherited</h2><p>940 was the <em>advanced</em> Obj-C 2.0 session. The language features themselves (<code>@property</code>, fast enumeration, <code>@protocol</code>, the modern runtime) had already shipped a year earlier with Leopard &#x2F; WWDC 2007; 940 was the deep dive on mastering them, with garbage collection as the headline topic. Reading it now is mostly a list of features that turned into Swift one-to-one:</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Obj-C 2.0, 2008</span></span><br><span class="line"><span class="keyword">@property</span> (<span class="keyword">nonatomic</span>, <span class="keyword">strong</span>) <span class="built_in">NSString</span> *name;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="type">id</span> obj <span class="keyword">in</span> array) &#123;</span><br><span class="line">    <span class="built_in">NSLog</span>(<span class="string">@&quot;%@&quot;</span>, obj);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">@protocol</span> <span class="title">UITableViewDelegate</span> &lt;<span class="title">NSObject</span>&gt;</span></span><br><span class="line">- (<span class="type">void</span>)tableView:(<span class="built_in">UITableView</span> *)tableView didSelectRowAtIndexPath:(<span class="built_in">NSIndexPath</span> *)indexPath;</span><br><span class="line"><span class="keyword">@end</span></span><br></pre></td></tr></table></figure>

<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Swift, 2026</span></span><br><span class="line"><span class="keyword">var</span> name: <span class="type">String</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> obj <span class="keyword">in</span> array &#123;</span><br><span class="line">    <span class="built_in">print</span>(obj)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">protocol</span> <span class="title class_">UITableViewDelegate</span>: <span class="title class_ inherited__">AnyObject</span> &#123;</span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">tableView</span>(<span class="keyword">_</span> <span class="params">tableView</span>: <span class="type">UITableView</span>, <span class="params">didSelectRowAt</span> <span class="params">indexPath</span>: <span class="type">IndexPath</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Same shape, same semantics, different syntax. The compiler-synthesized backing storage from <code>@synthesize</code> (which became implicit in Xcode 4) is exactly the compiler-synthesized backing storage Swift gives you for <code>var name: String</code>. The fast-enumeration protocol (<code>NSFastEnumeration</code>) is structurally <code>Sequence</code>&#x2F;<code>IteratorProtocol</code>. Existential types (<code>id&lt;MyProtocol&gt;</code> in Obj-C, <code>any MyProtocol</code> in modern Swift) are the same idea; Swift made the existential explicit because the implicit version turned out to hide a performance cliff (boxing) that became hard to reason about at scale.</p>
<p>The one bet from 940 that did not pay was <strong>garbage collection</strong>. Apple shipped opt-in tracing GC for Mac apps in 10.5, pitched it as the modern memory-management story, deprecated it five years later in 10.8 Mountain Lion (2012), and removed it from the runtime entirely in macOS Sierra (2016). iOS never had GC at all. ARC (2011) is the actual successor, and ARC took the lessons that GC taught: you cannot interop a tracing collector cleanly with C-level frameworks (Core Foundation, libdispatch, <code>malloc</code>-allocated buffers passed across the boundary), the finalizer ordering problem is real, and “the system handles memory” is not a promise an OS framework can make when half its surface area is C. ARC said “the system handles the <em>boilerplate</em>; you handle the <em>graph</em>“, and that is the rule we still live with: structure your retain graph, mark cycles <code>weak</code>, cancel your tasks on <code>deinit</code>. The 940 GC pitch is a historical curiosity; the principle that survived is the property-level memory annotation (<code>strong</code>&#x2F;<code>weak</code>&#x2F;<code>unowned</code>), which 940 introduced and ARC formalized.</p>
<h2 id="The-382-reveal-queues-not-threads"><a href="#The-382-reveal-queues-not-threads" class="headerlink" title="The 382 reveal: queues, not threads"></a>The 382 reveal: queues, not threads</h2><p>382 is the talk that aged best. It was previewed in 2008, fully shipped in 2009 (406), ported to iPhone in 2010 (206), and the design principle in the title is the same one driving every concurrency feature Apple has shipped since.</p>
<p>The framing: every modern Mac has 2+ cores, the iPhone is single-core today but multi-core is coming, and apps written with <code>pthread_create</code> are either too few threads (under-utilization) or too many (context-switch thrashing). The proposed answer is a system-shared thread pool, scaled by hardware, fed by per-app queues. Apps express <strong>what work</strong> (a closure), not <strong>which thread</strong> (an <code>NSThread</code> instance). The system multiplexes.</p>
<p>That sentence describes:</p>
<ul>
<li><strong>GCD blocks</strong> (2009): <code>dispatch_async(queue, ^{ ... })</code></li>
<li><strong><code>NSOperationQueue</code></strong> (2007, retrofitted onto GCD): <code>operationQueue.addOperation { ... }</code></li>
<li><strong>Combine schedulers</strong> (2019): <code>.receive(on: DispatchQueue.main)</code></li>
<li><strong>Swift Concurrency</strong> (2021): <code>Task { ... }</code>, <code>await</code>, actors, <code>withTaskGroup</code></li>
<li><strong>Swift 6 strict concurrency</strong> (2024): the same model with compile-time isolation checking</li>
</ul>
<p>The <code>Task { @MainActor in ... }</code> in your modern code is the 2008 bet with type checking on top. The cost of getting the bet wrong is paid not in concept, but in the syntax churn from <code>dispatch_async</code> to <code>OperationQueue.main.addOperation</code> to <code>Task { @MainActor in ... }</code> over three rewrites. If you only learned the latest spelling, you are reading 348 and 382 as historical trivia. If you have written code in two of the spellings, you can feel the through-line.</p>
<p>The one thing 382 could not anticipate is that <code>DispatchQueue.main.async { ... }</code> does not satisfy <code>@MainActor</code> in Swift 6. The runtime hop is correct (the closure does run on the main queue), but the compiler cannot prove it, so the closure inherits the isolation of its declaration site. That is a Swift 6 problem, not a GCD problem, and the fix is to write <code>Task { @MainActor in ... }</code> or mark the calling method <code>nonisolated</code> and dispatch from there. The 2008 mental model is intact. The compiler enforcement got stricter.</p>
<h2 id="What-got-redesigned-and-why"><a href="#What-got-redesigned-and-why" class="headerlink" title="What got redesigned, and why"></a>What got redesigned, and why</h2><p>Two things from 2008 had to be rewritten:</p>
<ul>
<li><strong>Manual retain&#x2F;release</strong> (the 312 + 348 + 940 memory model) became <strong>ARC</strong> in 2011. The rewrite was syntactic: the compiler inserts the <code>retain</code> and <code>release</code> calls; you reason about the graph. Same model, less typing.</li>
<li><strong>Garbage collection</strong> (the 940 Mac-only pitch) became <strong>ARC</strong> in 2011. The rewrite was conceptual: trace-collected memory does not interop with C-level frameworks cleanly, so Apple gave up on tracing and committed to refcounting with compiler insertion. The lesson taught: a memory-management strategy on a platform with extensive C interop has to play nicely with <code>malloc</code>. Tracing does not.</li>
</ul>
<p>Notice both rewrites land at ARC. There was a moment around 2008 to 2011 when Apple had three concurrent stories about how memory should work (retain&#x2F;release on iPhone, GC on Mac, the implicit promise that “the system will figure it out”), and ARC collapsed all three into one model. The 2008 reveals predate that consolidation; you can read the GC topic in 940 and feel the world that did not happen.</p>
<h2 id="The-audit-you-can-run-on-your-own-codebase"><a href="#The-audit-you-can-run-on-your-own-codebase" class="headerlink" title="The audit you can run on your own codebase"></a>The audit you can run on your own codebase</h2><p>The reason I wrote this post is that I was auditing my own iOS code against the 2008 reveals to see whether anything had drifted. Three short greps for any iOS codebase:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 940: pre-fast-enumeration iteration patterns. Should be 0 hits in modern Swift.</span></span><br><span class="line">grep -rn <span class="string">&quot;while let _ = .*next()&quot;</span> Sources/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 940: existential types missing the `any` qualifier (Swift 5.6+ rule).</span></span><br><span class="line">grep -rEn <span class="string">&quot;(let|var|func .* :|-&gt;)\s*[A-Z][A-Za-z]*Protocol\b&quot;</span> Sources/ \</span><br><span class="line">  | grep -v <span class="string">&quot;any &quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 348: high-frequency NSNotificationCenter consumers that may need debouncing.</span></span><br><span class="line"><span class="comment"># .NSPersistentStoreRemoteChange is the canonical example for CloudKit-backed apps.</span></span><br><span class="line">grep -rn <span class="string">&quot;NSPersistentStoreRemoteChange&quot;</span> Sources/</span><br></pre></td></tr></table></figure>

<p>The first one should be zero. The second one is a SwiftLint-able rule (<code>existential_any</code>); if you have a hit, the fix is one keyword. The third one is the only one that typically needs real action: any consumer that does whole-store work (full search-index rebuild, full UI regeneration, full export) wants <code>.debounce(for: .seconds(1), scheduler: DispatchQueue.main)</code> and a single replace-on-write <code>Task&lt;Void, Never&gt;?</code>, not a <code>Task</code> array. Per-event tasks are right for <em>independent</em> per-record work (one task per object that actually changed), and wrong for <em>replace-the-world</em> work, where the only one that matters is the last.</p>
<p>That is a 2026 problem, written against a 2008 broadcast contract, with a 2024 fix. Eighteen years end to end. The contract is fine. The pattern around it just had to grow up.</p>
<h2 id="If-you-want-to-read-the-originals"><a href="#If-you-want-to-read-the-originals" class="headerlink" title="If you want to read the originals"></a>If you want to read the originals</h2><p>Apple’s own developer.apple.com no longer hosts pre-2020 WWDC videos (the current video library starts at WWDC20). Session metadata (titles, abstracts, durations, speakers) is browsable at the <a href="https://nonstrict.eu/wwdcindex/wwdc2008/">nonstrict.eu WWDC Index</a>, and the videos themselves have been preserved on the <a href="https://archive.org/search?query=wwdc+2008">Internet Archive</a>. Four sessions worth reading in this order:</p>
<ol>
<li><strong>312</strong> for the SDK runtime</li>
<li><strong>940</strong> for the language Swift inherited</li>
<li><strong>348</strong> for the design patterns the framework expects</li>
<li><strong>382</strong> for the concurrency mental model</li>
</ol>
<p>About sixty minutes each. The video quality is rough (480p H.264 at best), and the slide decks date themselves with the iPhone OS 2.0 chrome, but the talks themselves do not feel old. Most of what you will recognize is your own code, written eighteen years before you arrived.</p>
]]></content>
      <categories>
        <category>iOS</category>
        <category>WWDC</category>
      </categories>
      <tags>
        <tag>Swift</tag>
        <tag>UIKit</tag>
        <tag>Cocoa</tag>
        <tag>Objective-C</tag>
        <tag>GCD</tag>
        <tag>history</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo Quick Guide</title>
    <url>/Hexo-Quick-Guide/</url>
    <content><![CDATA[<p>A short working reference for the Hexo + NexT setup that powers this blog.</p>
<span id="more"></span>

<ul>
<li><strong>Hexo</strong>: 8.1.1 (<a href="https://hexo.io/">hexo.io</a>)</li>
<li><strong>Theme</strong>: <a href="https://theme-next.js.org/">NexT</a> 8.27.0 (<a href="https://github.com/next-theme/hexo-theme-next">GitHub</a>)</li>
</ul>
<h2 id="Workflow"><a href="#Workflow" class="headerlink" title="Workflow"></a>Workflow</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo new <span class="string">&quot;My New Article&quot;</span>     <span class="comment"># scaffold source/_posts/My-New-Article.md</span></span><br><span class="line"><span class="comment"># Edit the post</span></span><br><span class="line">hexo clean</span><br><span class="line">hexo generate</span><br></pre></td></tr></table></figure>

<p>For pushing to Cloudflare Pages with Git auto-deploy, see <a href="/Deploy-Static-Site-to-Cloudflare-Pages/" title="Deploy a Static Site to Cloudflare Pages with GitHub Auto-Deploy">Deploy a Static Site to Cloudflare Pages</a>. Local generate + manual <code>wrangler pages deploy</code> works the same way against the <code>public/</code> (or <code>docs/</code>) output.</p>
<h2 id="Maintenance"><a href="#Maintenance" class="headerlink" title="Maintenance"></a>Maintenance</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ncu -u            <span class="comment"># bump versions in package.json</span></span><br><span class="line">npm install       <span class="comment"># resolve</span></span><br></pre></td></tr></table></figure>

<h2 id="Configuration"><a href="#Configuration" class="headerlink" title="Configuration"></a>Configuration</h2><p>Site config lives in <code>_config.yml</code>. The values worth knowing:</p>
<ul>
<li><code>permalink: :title/</code>: clean URLs without dates</li>
<li><code>index_generator.order_by: -updated</code>: sort the home feed by updated time, descending</li>
<li><code>updated_option: &#39;mtime&#39;</code>: when <code>updated:</code> is empty in front matter, fall back to file mtime, so a rewrite floats to the top without manually timestamping</li>
</ul>
<h2 id="Theme-NexT"><a href="#Theme-NexT" class="headerlink" title="Theme: NexT"></a>Theme: NexT</h2><ul>
<li><a href="https://theme-next.js.org/">Site</a> &#x2F; <a href="https://github.com/next-theme/hexo-theme-next">GitHub</a> &#x2F; <a href="https://theme-next.js.org/docs/">Docs</a></li>
<li>Theme config: <code>./_config.next.yml</code> (overrides <code>node_modules/hexo-theme-next/_config.yml</code>)</li>
</ul>
<h3 id="Config-priority-high-to-low"><a href="#Config-priority-high-to-low" class="headerlink" title="Config priority (high to low)"></a>Config priority (high to low)</h3><ol>
<li><code>theme_config</code> block in <code>_config.yml</code></li>
<li><code>_config.[theme].yml</code> (e.g. <code>_config.next.yml</code>)</li>
<li><code>_config.yml</code> inside the theme directory</li>
</ol>
<h3 id="Highlight-previews"><a href="#Highlight-previews" class="headerlink" title="Highlight previews"></a>Highlight previews</h3><p><a href="https://theme-next.js.org/highlight/">NexT highlight theme picker</a>.</p>
<h3 id="Legacy-repos-do-not-use"><a href="#Legacy-repos-do-not-use" class="headerlink" title="Legacy repos (do not use)"></a>Legacy repos (do not use)</h3><ul>
<li><code>iissnan/hexo-theme-next</code> (original, abandoned)</li>
<li><code>theme-next/hexo-theme-next</code> (intermediate fork, abandoned)</li>
</ul>
<h2 id="Tag-plugins"><a href="#Tag-plugins" class="headerlink" title="Tag plugins"></a>Tag plugins</h2><p><a href="https://hexo.io/docs/tag-plugins">Hexo’s tag plugins</a> handle the parts plain markdown can’t.</p>
<h3 id="Cross-link-another-post"><a href="#Cross-link-another-post" class="headerlink" title="Cross-link another post"></a>Cross-link another post</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;% post_link filename &quot;Display text&quot; %&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Don’t</strong> wrap a <code>post_link</code> tag in a markdown link: <code>[label](&#123;% post_link filename %&#125;)</code> does not work, because <code>post_link</code> outputs a full <code>&lt;a&gt;</code> element rather than a URL. Use the standalone form above with display text.</p>
<h3 id="Include-source-code"><a href="#Include-source-code" class="headerlink" title="Include source code"></a>Include source code</h3><p>Reads from <code>code_dir</code> (default <code>source/downloads/code/</code>):</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;% include_code [title] [lang:language] [from:line] [to:line] path/to/file %&#125;</span><br></pre></td></tr></table></figure>

<p>Broken in Hexo 7.2.0 onward (8.1.1 still affected). The path-traversal hardening in <a href="https://github.com/hexojs/hexo/pull/5251">PR #5251</a> replaced the direct filesystem read with a <code>Page.findOne(&#123; source &#125;)</code> lookup, which only resolves files Hexo has registered as pages. Files that Hexo treats as static assets (any extension without a registered renderer, e.g. <code>.c</code>, <code>.swift</code>, <code>.cpp</code>, <code>.scala</code>) are never in the Page model, so the tag silently returns nothing for them. <code>.js</code> happens to render because Hexo classifies it as a renderable page. Tracking issue: <a href="https://github.com/hexojs/hexo/issues/5486">hexojs&#x2F;hexo#5486</a>. Proposed fix introducing a separate Code model: <a href="https://github.com/hexojs/hexo/pull/5633">hexojs&#x2F;hexo#5633</a>, open since Feb 2025.</p>
<p>Until that lands, fall back to a plain fenced code block for everything that isn’t <code>.js</code>.</p>
<h3 id="Reference-assets"><a href="#Reference-assets" class="headerlink" title="Reference assets"></a>Reference assets</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;% asset_path filename %&#125;</span><br><span class="line">&#123;% asset_img [class] slug [width] [height] [title] [alt] %&#125;</span><br><span class="line">&#123;% asset_link filename &quot;title&quot; %&#125;</span><br></pre></td></tr></table></figure>

<p>Asset folders live alongside the post: a same-name folder under <code>source/_posts/</code> is auto-discovered when <code>post_asset_folder: true</code> is set in <code>_config.yml</code>.</p>
]]></content>
      <categories>
        <category>Web</category>
        <category>Static Site Generator</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>NexT</tag>
        <tag>hexo-theme-next</tag>
        <tag>Cloudflare Pages</tag>
      </tags>
  </entry>
  <entry>
    <title>WWDC 2009 revisited: iPhone OS 3.0 in June, Snow Leopard in August</title>
    <url>/WWDC-2009-Revisited/</url>
    <content><![CDATA[<p>WWDC 2009 was two releases doing different jobs. iPhone OS 3.0 shipped on June 17 and turned the App Store from a curiosity into a viable place to ship a product. Snow Leopard shipped on August 28 with no new user-facing features at all, marketed literally as “0 new features,” because the work that year went into the foundation: a 64-bit rewrite of every Mac framework, the Clang toolchain replacing GCC, and blocks plus Grand Central Dispatch. The two releases ship eleven weeks apart, in different keynotes, and are usually remembered separately. Reading them together is the only honest way to look at 2009, because each release filled a gap the other left.</p>
<span id="more"></span>

<p>Same substrate-vs-syntax split that ran through <a href="/WWDC-2008-Revisited/" title="WWDC 2008 revisited: the four sessions still shaping iOS">the 2008 sessions</a>: the contracts shipped in 2009 still describe modern iOS. The syntax around them has been rewritten two or three times since.</p>
<h2 id="iPhone-OS-3-0-June-apps-become-businesses"><a href="#iPhone-OS-3-0-June-apps-become-businesses" class="headerlink" title="iPhone OS 3.0 (June): apps become businesses"></a>iPhone OS 3.0 (June): apps become businesses</h2><p>The App Store was eleven months old when 3.0 shipped. The previous year’s SDK was a tech preview that could draw views and read JSON, but a developer who wanted to build something that paid rent had to invent or skip most of the missing pieces. 3.0 was the release where Apple shipped those missing pieces.</p>
<h3 id="Push-notifications"><a href="#Push-notifications" class="headerlink" title="Push notifications"></a>Push notifications</h3><p>Promised at WWDC 2008 for “this September,” slipped to June 2009. Apple Push Notification Service is the async wake-up channel: a server posts to APNs, the OS delivers it to the right device, the app’s delegate gets called. Without it, a backgrounded iPhone app was effectively dead, which made entire categories (news alerts, chat, multiplayer turn notifications) impossible on iPhone before 3.0.</p>
<p>The 2009 delegate API still works in 2026:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">func</span> <span class="title function_">application</span>(</span><br><span class="line">    <span class="keyword">_</span> <span class="params">application</span>: <span class="type">UIApplication</span>,</span><br><span class="line">    <span class="params">didRegisterForRemoteNotificationsWithDeviceToken</span> <span class="params">deviceToken</span>: <span class="type">Data</span></span><br><span class="line">) &#123;</span><br><span class="line">    <span class="comment">// send token to your server</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>The signature was extended in iOS 7 with <code>fetchCompletionHandler</code> for silent pushes, and visible alerts now route through <code>UNUserNotificationCenter</code>. Below those wrappers, the contract is the 2009 contract: register for a device token, send it to your server, handle the delivery callback when the OS wakes you.</p>
<p>What 2009 could not have anticipated is the silent variant becoming a transport substrate. CloudKit (2014), iCloud Keychain, Find My, Background App Refresh, and <code>NSPersistentCloudKitContainer</code> (2019) all ride the silent-push channel. The cross-device “I edited this on my iPhone, it showed up on my Mac” experience that defines modern Apple platforms is implemented on top of a 2009 design that was originally pitched as “your news app should beep when there’s news.”</p>
<h3 id="Core-Data-on-iPhone"><a href="#Core-Data-on-iPhone" class="headerlink" title="Core Data on iPhone"></a>Core Data on iPhone</h3><p>Core Data shipped on the Mac in Tiger (2005). In iPhone OS 3.0 it came to mobile. The three-class stack from session 421, <code>NSManagedObjectModel</code> for the schema, <code>NSPersistentStoreCoordinator</code> for the SQLite store, <code>NSManagedObjectContext</code> for the in-memory graph, is the same triple every Core Data app uses today.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> container <span class="operator">=</span> <span class="type">NSPersistentContainer</span>(name: <span class="string">&quot;Model&quot;</span>)</span><br><span class="line">container.loadPersistentStores &#123; <span class="keyword">_</span>, error <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> error &#123; <span class="built_in">fatalError</span>(<span class="string">&quot;loadPersistentStores failed: <span class="subst">\(error)</span>&quot;</span>) &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> context <span class="operator">=</span> container.viewContext</span><br></pre></td></tr></table></figure>

<p><code>NSPersistentContainer</code> (iOS 10) wrapped the three primitives in a convenience class. <code>NSPersistentCloudKitContainer</code> (iOS 13) added CloudKit sync as a thin layer. SwiftData (iOS 17) replaces the <code>.xcdatamodel</code> with <code>@Model</code> types declared in Swift. All three layers compile down to the same SQLite store format and the same <code>NSManagedObjectContext</code> semantics the 2009 session introduced.</p>
<p>Lightweight migration is the part that survived intact. Add or rename an attribute, bump the model version, set <code>NSMigratePersistentStoresAutomaticallyOption</code> and <code>NSInferMappingModelAutomaticallyOption</code>, and the framework rewrites the schema in place at load time. No migration script, no DDL, no rollback plan. The 2009 pattern is what carries existing users’ data through additive schema changes a decade and a half later.</p>
<p>What 2009 could not have anticipated is the multi-device dimension. When CloudKit was bolted on a decade later, the single-device assumptions had to be reinterpreted: the container now hosts two managed object contexts (Private and Shared databases under Family Sharing), and the merge policy is last-writer-wins and not configurable. Same chassis, new dimensions Apple wired around it.</p>
<h3 id="In-App-Purchase"><a href="#In-App-Purchase" class="headerlink" title="In-App Purchase"></a>In-App Purchase</h3><p>StoreKit shipped in 3.0 for paid apps in June; Apple extended IAP to free apps in October. The 2009 callsite API (<code>SKProductsRequest</code>, <code>SKPaymentQueue</code>, the <code>paymentQueue:updatedTransactions:</code> observer protocol) is the one piece of the iPhone OS 3.0 surface that StoreKit 2 (2021) replaced wholesale. Modern code calls <code>Product.products(for:)</code>, <code>for await</code>s over <code>Transaction.updates</code>, and there is no global observer to register or unregister. Nothing about the 2009 callsite code survives.</p>
<p>What did survive is the receipt format. The 2009 App Store receipt was a base64-encoded blob signed by Apple, carrying the original transaction identifier, product identifier, purchase date, and a signature you could verify against Apple’s certificate or against Apple’s verification endpoint. StoreKit 2 wraps that in <code>Transaction</code> and <code>Product</code> value types with typed field access and JWS verification you can perform locally, but the receipt itself is still the receipt: same signature scheme, same field positions for the original 2009 fields, same role as the single source of truth on whether a purchase happened. The subscription, family-sharing, and app-account-token fields were added on top of the 2009 layout, not in place of it.</p>
<p>What 2009 got wrong was the consumer-economics assumption. It assumed one-time consumable and non-consumable purchases with restoration as a corner case. Auto-renewing subscriptions (2011), introductory pricing (2016), subscription groups (2018), and the EU DMA carve-outs (2024) reshaped the App Store economy around recurring revenue, which the 2009 framing has nothing to say about. The chassis (products, transactions, receipts) is what 2009 nailed; the surface around it has been rewritten about every three years since.</p>
<h3 id="HTTP-Live-Streaming"><a href="#HTTP-Live-Streaming" class="headerlink" title="HTTP Live Streaming"></a>HTTP Live Streaming</h3><p>HLS shipped quietly in 3.0 with its own session (313), and it is the streaming format every video player on Apple platforms consumes in 2026. The 2009 design put HTTP at the transport layer (CDN-cacheable, no stateful streaming server) and pushed adaptive-bitrate negotiation to the client via an <code>.m3u8</code> manifest pointing at chunked segments. The streaming wars of the 2010s ended with HLS as the universal floor: RTMP died with Flash, MPEG-DASH coexists but never displaced HLS on iOS or Safari, and Smooth Streaming retreated to Microsoft-only deployments.</p>
<p>The 2009 transport was MPEG-2 TS segments. Apple added fragmented MP4 segments in 2016 and Low-Latency HLS in 2019, but a 3.0-era <code>.m3u8</code> still plays in <code>AVPlayer</code> today and the manifest grammar is unchanged. The decision that mattered most was publishing HLS as an IETF Internet-Draft rather than a proprietary spec. That is the reason HLS became the cross-platform floor instead of an Apple-only format like the FairPlay DRM layered on top of it.</p>
<h3 id="The-rest-of-3-0-in-one-line-each"><a href="#The-rest-of-3-0-in-one-line-each" class="headerlink" title="The rest of 3.0 in one line each"></a>The rest of 3.0 in one line each</h3><p>Three more 3.0 additions reshaped categories of apps despite not getting deep-dive sessions. MapKit made location-aware apps a default-on category. Game Kit’s peer-to-peer Bluetooth was the first system API for ad-hoc nearby networking and is the conceptual ancestor of Multipeer Connectivity. Accessibility and VoiceOver on iPhone, alongside system-wide cut&#x2F;copy&#x2F;paste, are the additions Apple’s marketing led with for non-developers and turn out to be the additions that quietly aged best.</p>
<h2 id="Snow-Leopard-August-no-features-an-entirely-new-foundation"><a href="#Snow-Leopard-August-no-features-an-entirely-new-foundation" class="headerlink" title="Snow Leopard (August): no features, an entirely new foundation"></a>Snow Leopard (August): no features, an entirely new foundation</h2><p>Apple’s marketing copy for Snow Leopard was the phrase “0 new features.” The release was, in their framing, devoted to performance, stability, and the underpinnings of the next decade of macOS. Three of those underpinnings are still load-bearing in 2026.</p>
<h3 id="Blocks-and-Grand-Central-Dispatch"><a href="#Blocks-and-Grand-Central-Dispatch" class="headerlink" title="Blocks and Grand Central Dispatch"></a>Blocks and Grand Central Dispatch</h3><p>The headline. Blocks (<code>^&#123; ... &#125;</code>) shipped as a clang extension, and GCD shipped on top of blocks. The two are intertwined by design: you cannot have a system-managed thread pool that accepts “what work to run” if the language has no way to express “a unit of work” without a class declaration. Pre-blocks Objective-C had <code>NSInvocation</code>, which works but is three lines per callsite and a manual <code>retain</code> per captured argument. Blocks compressed both into a single literal that the compiler closes over.</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 2009: GCD with blocks</span></span><br><span class="line"><span class="built_in">dispatch_async</span>(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, <span class="number">0</span>), ^&#123;</span><br><span class="line">    <span class="built_in">NSData</span> *data = [<span class="keyword">self</span> loadFromDisk];</span><br><span class="line">    <span class="built_in">dispatch_async</span>(dispatch_get_main_queue(), ^&#123;</span><br><span class="line">        <span class="keyword">self</span>.label.text = [<span class="keyword">self</span> renderText:data];</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 2026: Swift Concurrency from a MainActor-isolated caller</span></span><br><span class="line"><span class="type">Task</span> &#123; <span class="meta">@MainActor</span> <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">let</span> data <span class="operator">=</span> <span class="keyword">await</span> loadFromDisk()</span><br><span class="line">    label.text <span class="operator">=</span> renderText(data)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Two shifts are easy to miss reading these side by side. The priority dance (<code>DISPATCH_QUEUE_PRIORITY_DEFAULT</code>) is gone, absorbed first by QoS classes (2014) and then by Swift task priority (2021). The inner main-queue hop became implicit: if the outer <code>Task</code> is already <code>@MainActor</code>, the body runs on main without a nested dispatch, and you only hop explicitly when you cross an actor boundary. The work the runtime does is the same; the compiler does more of the bookkeeping.</p>
<p>What hasn’t aged perfectly is the secondary GCD vocabulary. Dispatch <em>groups</em>, <em>barriers</em>, and <em>semaphores</em> still ship and the runtime still implements them, but Swift Concurrency replaced groups with <code>withTaskGroup</code>, barriers with actor isolation, and semaphores with <code>CheckedContinuation</code> (or, more often, with a redesign that did not need them). A <code>DispatchSemaphore</code> in modern app code is almost always a sign that an async function is being called from a sync context, and the right fix is to make the caller async rather than to block a thread.</p>
<h3 id="Clang-and-the-toolchain-swap"><a href="#Clang-and-the-toolchain-swap" class="headerlink" title="Clang and the toolchain swap"></a>Clang and the toolchain swap</h3><p>The quiet Snow Leopard bet. Apple moved off GCC to clang&#x2F;LLVM as the default toolchain for new framework code, with the full developer-facing default following over the next two releases. The decision is not visible at any callsite, but every Swift language feature since 2014, ARC in 2011, modules in 2012, generics specialization, strict concurrency checking, and the entire Swift compiler being written against LLVM IR, all follow from that 2009 architectural choice. If Apple had stayed on GCC, the language Swift inherits would not be the language Swift is.</p>
<p>There is no shipped API to point at here, which is exactly why the move was easy to miss in 2009. The release-notes line was something like “Apple is contributing to clang and LLVM.” Sixteen years on, every binary on every Apple platform is compiled by it.</p>
<h3 id="64-bit-everywhere-on-the-Mac"><a href="#64-bit-everywhere-on-the-Mac" class="headerlink" title="64-bit everywhere on the Mac"></a>64-bit everywhere on the Mac</h3><p>Snow Leopard rewrote every Mac framework to be 64-bit clean and shipped a 64-bit kernel option. The iPhone stayed 32-bit until the A7 in 2013, so this work did not touch iOS code until later, but the Mac rewrite is the reason the Intel-to-Apple-silicon transition in 2020 went as smoothly as it did. By 2020, every framework had been 64-bit clean for eleven years, and the only architecture-specific code most apps had was Intel intrinsics. The work paid off twice, eleven years apart, and the second payoff is what the 2009 marketing said the release was for.</p>
<h2 id="What-got-rewritten"><a href="#What-got-rewritten" class="headerlink" title="What got rewritten"></a>What got rewritten</h2><p>Two patterns from 2009 had to be replaced wholesale rather than extended:</p>
<ul>
<li><strong>Manual block memory management</strong> (<code>Block_copy</code>, <code>Block_release</code>, <code>[block copy]</code> when storing into an ivar) became ARC-managed blocks in 2011 and then Swift closures in 2014. The 2009 model required developers to think about block lifetime separately from object lifetime, with bugs that only manifested at runtime when a stack-allocated block escaped its scope. ARC folded the copy-on-escape rule into the compiler; Swift made it impossible to express the failure case.</li>
<li><strong><code>SKProductsRequest</code> plus <code>SKPaymentQueue</code> observer</strong> became StoreKit 2 in 2021. The 2009 design was a pre-blocks delegate-and-notification pattern that had been bolted onto async&#x2F;await semantics with backwards-compatibility shims for a decade. StoreKit 2 is the clean break: typed <code>Transaction</code> values, async iteration, no global observer, no <code>.failed</code> transaction stuck in the queue across launches. The old API still works, and apps that do not need StoreKit 2 features can stay on it indefinitely.</li>
</ul>
<p>What did not get rewritten: the <code>UIApplicationDelegate</code> lifecycle that push hooks into (still the canonical wake-up surface), the Core Data three-class stack (now wrapped by <code>NSPersistentCloudKitContainer</code> and <code>@Model</code>, not replaced), <code>dispatch_get_main_queue()</code> (still the queue under <code>MainActor.run</code>), the HLS <code>.m3u8</code> manifest grammar, and the App Store receipt’s signed field positions. Modern iOS apps are written against a 2009 substrate with two or three layers of newer syntax above it.</p>
<h2 id="If-you-want-to-read-the-originals"><a href="#If-you-want-to-read-the-originals" class="headerlink" title="If you want to read the originals"></a>If you want to read the originals</h2><p>Same caveats as the <a href="/WWDC-2008-Revisited/" title="WWDC 2008 revisited: the four sessions still shaping iOS">2008 post</a>: Apple’s developer.apple.com no longer hosts pre-2020 WWDC videos. Session metadata lives at the <a href="https://nonstrict.eu/wwdcindex/wwdc2009/">nonstrict.eu WWDC Index</a>, and the videos themselves are on the <a href="https://archive.org/search?query=wwdc+2009">Internet Archive</a>.</p>
<p>Four sessions in this reading order if you want the same arc as the post:</p>
<ol>
<li><strong>406</strong> “Programming with Blocks and Grand Central Dispatch” for the Snow Leopard concurrency story</li>
<li><strong>421</strong> “Introduction to Core Data on iPhone” for the persistence framework that became <code>NSPersistentCloudKitContainer</code> and <code>@Model</code></li>
<li><strong>120</strong> “Apple Push Notification Service” for the lifecycle wake-up channel</li>
<li><strong>122</strong> “In-App Purchase on iPhone” for the receipt chassis that outlasted three SDKs</li>
</ol>
<p>Forty to sixty minutes each, 480p H.264, iPhone OS 3.0 or Snow Leopard chrome on the slides. The talks themselves are precise enough that you can map each one onto a method call you wrote this week.</p>
]]></content>
      <categories>
        <category>iOS</category>
        <category>WWDC</category>
      </categories>
      <tags>
        <tag>Swift</tag>
        <tag>GCD</tag>
        <tag>history</tag>
        <tag>Core Data</tag>
        <tag>StoreKit</tag>
        <tag>HLS</tag>
        <tag>LLVM</tag>
      </tags>
  </entry>
  <entry>
    <title>WWDC 2011 revisited: iCloud, ARC, and the year iOS stopped pretending the device was the world</title>
    <url>/WWDC-2011-Revisited/</url>
    <content><![CDATA[<p>The thing I keep coming back to about 2011 is that almost none of the APIs revealed that June are alive in the form they shipped. Core Data’s iCloud integration (the transaction-log mechanism that the 2011 reveal pitched) was deprecated in iOS 10 in 2016. <code>UILocalNotification</code> was deprecated the same year. Storyboards lost the argument in most production codebases. ARC’s <code>__weak</code> keyword survived as a curiosity in mixed Obj-C&#x2F;Swift bridging headers, and the rest of manual memory management died with Swift. And yet, when I read the seven sessions I revisited this week, every modern iOS app I touch is still organised around the <em>ideas</em> those sessions argued for. The API surface got rewritten. The conceptual model didn’t.</p>
<span id="more"></span>

<p>So this isn’t a victory lap for iOS 5 the way the <a href="/WWDC-2010-Revisited/" title="WWDC 2010 revisited: iPad ships, iOS 4 names the primitives">2010 post</a> was for iOS 4. 2010 shipped state machines that are still alive at the call-site level: <code>applicationDidEnterBackground:</code> still exists, <code>UIPinchGestureRecognizer</code> still exists, <code>UISplitViewController</code> still exists. 2011 is different. It’s the year Apple committed to a set of <em>philosophical positions</em> about what a device is, what memory belongs to, what layout means, how secrets travel, and where alerts come from, and then spent the decade after rewriting the implementations while leaving the positions alone.</p>
<h2 id="The-five-positions"><a href="#The-five-positions" class="headerlink" title="The five positions"></a>The five positions</h2><p>Five 2011 reveals are really five claims about how the world should be modeled:</p>
<table>
<thead>
<tr>
<th>Reveal</th>
<th>Claim</th>
<th>What it replaced</th>
<th>What carries it today</th>
</tr>
</thead>
<tbody><tr>
<td><strong>iCloud (501)</strong></td>
<td>User data belongs to the iCloud account; devices are caches over it</td>
<td>“data lives on this phone”</td>
<td><code>NSPersistentCloudKitContainer</code>, SwiftData with <code>cloudKitDatabase:</code></td>
</tr>
<tr>
<td><strong>ARC (323)</strong></td>
<td>Memory ownership is a compile-time contract, not a runtime convention</td>
<td>Manual <code>retain</code>&#x2F;<code>release</code>&#x2F;<code>autorelease</code></td>
<td>Swift’s ownership model, <code>[weak self]</code>, structured concurrency cancellation</td>
</tr>
<tr>
<td><strong>Auto Layout (103)</strong></td>
<td>Layout is a set of relationships between views, not absolute coordinates</td>
<td>Springs and struts (<code>autoresizingMask</code>)</td>
<td>SnapKit, <code>NSLayoutAnchor</code>, SwiftUI’s layout protocol</td>
</tr>
<tr>
<td><strong>GCD deep dive (210)</strong></td>
<td>Concurrency is queue confinement, not thread management</td>
<td><code>NSThread</code>, <code>pthread_create</code></td>
<td><code>Task</code>, actors, the dispatch primitives the actor runtime uses underneath</td>
</tr>
<tr>
<td><strong>Notifications (517)</strong></td>
<td>Alerts come from a system surface; apps are submitters, not consumers</td>
<td>Always-running apps, polling, modal alert pop-ups</td>
<td><code>UNUserNotificationCenter</code>, CloudKit silent push, time-sensitive interruption levels</td>
</tr>
</tbody></table>
<p>Two more (208 Keychain, 117 Calendar) are smaller in scope but make the same shape of move: they take something developers were doing ad hoc and name it as a discipline. The pattern is the same across all seven sessions. Apple took conventions that had been informal up to iOS 4, gave them types, names, and contracts, and made the alternative paths quietly second-class.</p>
<p>The rest of this post takes the five positions one at a time. I’ll skip the API list for each (you can find that in the original session videos or in Apple’s archived docs); I want to write down what the position <em>was</em>, what it broke from, and what it cost.</p>
<h2 id="iCloud-the-device-is-no-longer-the-boundary"><a href="#iCloud-the-device-is-no-longer-the-boundary" class="headerlink" title="iCloud: the device is no longer the boundary"></a>iCloud: the device is no longer the boundary</h2><p>iCloud was the headline of the June 6 keynote (the last one Steve Jobs would deliver before his resignation in August). The framing was that “the truth” of a user’s data lives in the cloud and every device is a window onto it. That framing is the position the rest of this section is about.</p>
<p>Pre-2011, every iPhone app I’d written assumed that “user data” meant “the SQLite file in <code>Documents/</code>.” When the user got a new phone, they restored from an iTunes or iCloud backup, the file came back, and the app picked up where it left off. The boundary was the device. The transport was the backup.</p>
<p>iCloud Documents reframed that. The boundary became the iCloud account. The device became one of N caches over that account’s private database. Files were no longer copied between devices via backup-and-restore; they were synced continuously, with the system handling the merge.</p>
<p>The implementation aged badly. Core Data + iCloud was unreliable enough that Apple formally deprecated its transaction-log path (<code>NSPersistentStoreUbiquitousContentNameKey</code> and friends) in iOS 10 in 2016, and developers spent the years between 2011 and 2014 either fighting it or rolling their own sync. But the framing it argued for outlived its implementation by a wide margin. When <code>NSPersistentCloudKitContainer</code> shipped in iOS 13 in 2019, it inherited the same model: user account is the boundary, devices are caches, the merge happens in the framework. SwiftData with <code>cloudKitDatabase:</code> in iOS 17 inherited it again. Any modern Apple-platform app that syncs through a user’s iCloud account runs against that contract. The 2011 vocabulary (ubiquity container, account change notification, account-bound private database) is still the vocabulary of every cross-device Apple sync experience, even though almost none of the 2011 API survives.</p>
<p>The cost was high and Apple paid most of it. The 2011 reveal made promises (transparent file conflict resolution, multi-device offline edits, schema migration during sync) that the implementation could not keep. The dev community remembers Core Data + iCloud in 2011-2014 as a regretted experiment. What I think the retrospective misses is that the <em>experiment failed at the implementation layer but succeeded at the conceptual layer</em>. By the time CloudKit shipped at WWDC 2014 with a record-based replacement, every developer in the Apple ecosystem already had the right mental model for it. The hardest part of “user account is the boundary” is not the implementation; it is convincing developers that their data model has to be reshaped around that boundary. iCloud 2011 did the convincing. CloudKit 2014 collected the cheque.</p>
<p>The trade-off that <em>did</em> survive intact, and is still load-bearing in 2026, is last-writer-wins. Apple’s iCloud sync model has never offered configurable merge policies; not in the 2011 KVS API, not in <code>NSPersistentCloudKitContainer</code>, not in SwiftData. The bet was that 95% of apps would rather have an opinionated sync layer that they can’t tune than a configurable one they have to reason about, because the alternative policies (vector clocks, CRDTs, three-way merges) all require the application to model conflict, and most applications can’t. Fifteen years on, that bet still looks right for the 95%. The 5% (collaborative editors, multi-user document apps) either build their own sync layer or live with it.</p>
<p>What 2011 could not foresee was that the silent-push channel revealed in 517 would become the transport that made the whole framework work. iCloud Documents in 2011 had no silent push (it came in iOS 7), so the catchup story was “open the app, wait, the file appears.” CloudKit on top of silent push gave you “make a change on phone, see it on Mac in two seconds.” Same conceptual model. Three years of background-execution improvements between them is what made it feel real.</p>
<h2 id="ARC-memory-becomes-the-compiler’s-job"><a href="#ARC-memory-becomes-the-compiler’s-job" class="headerlink" title="ARC: memory becomes the compiler’s job"></a>ARC: memory becomes the compiler’s job</h2><p>The 2011 ARC reveal looks small from a 2026 vantage point because Swift has been ARC-by-default since 2014 and Obj-C is a maintenance language. What’s easy to miss is how radical the position was at the time. Up to iOS 4, every Cocoa Touch developer had to internalise an informal rule set (“if you <code>alloc</code>&#x2F;<code>copy</code>&#x2F;<code>retain</code>, you <code>release</code>; if you didn’t, you don’t; autorelease pools batch deallocations at the runloop boundary; collection containers retain on insert; assigning to an ivar without retaining is a delegate-only pattern”) and apply it correctly across every method. Most apps had small but real memory bugs (over-release crashes, leaks, dangling pointers on autoreleased temporaries) that lived in the codebase for the entire life of the app and only manifested under specific timings.</p>
<p>ARC said: the compiler can see all of this. It can analyse the scope of every variable, derive the ownership transitions, and insert the <code>retain</code> and <code>release</code> calls for you. The programmer’s job becomes declaring <em>intent</em> (is this a strong reference, a weak one, or an unowned one?) and the compiler does the bookkeeping.</p>
<p>The mental model ARC introduced is exactly the mental model Swift inherited:</p>
<table>
<thead>
<tr>
<th>2011 Obj-C ARC</th>
<th>2014+ Swift</th>
<th>Meaning</th>
</tr>
</thead>
<tbody><tr>
<td><code>__strong</code> (default)</td>
<td><code>var</code> &#x2F; <code>let</code> reference</td>
<td>Holds the object; prevents dealloc</td>
</tr>
<tr>
<td><code>__weak</code></td>
<td><code>weak var</code></td>
<td>Doesn’t retain; auto-nils on dealloc</td>
</tr>
<tr>
<td><code>__unsafe_unretained</code></td>
<td><code>unowned</code></td>
<td>Doesn’t retain; not nilled; dangling-pointer risk</td>
</tr>
<tr>
<td><code>__weak typeof(self) weakSelf = self;</code></td>
<td><code>[weak self] in ...</code></td>
<td>The capture list</td>
</tr>
<tr>
<td><code>@autoreleasepool &#123; ... &#125;</code></td>
<td><code>autoreleasepool &#123; ... &#125;</code></td>
<td>Batched deallocation in tight loops</td>
</tr>
</tbody></table>
<p>Reading the 2011 talk back, the line I noticed most was the framing of “strong reference cycle” as the <em>one</em> class of memory bug the compiler can’t fix. The compiler can see when you write <code>strong</code> and <code>release</code>, but it can’t see whether two objects pointing at each other should logically be a parent-child or a peer-peer relationship. That decision stays with the developer, and it stays with the developer in 2026 too. Every <code>[weak self]</code> capture in a Swift closure is the developer saying “this closure outlives the lifetime of self, so don’t pin self to its lifetime.” Every absence of <code>[weak self]</code> is the developer saying the opposite.</p>
<p>What changed between 2011 and 2026 is the appearance of async patterns the 2011 reveal couldn’t anticipate (deferred commits, debounced writes, undo timers), where the right capture is sometimes the opposite of the reflexive <code>[weak self]</code>. The decision the 2011 reveal pushed back to the developer (“which references are strong, which are weak”) is still the right decision to push back, but the answer is occasionally counterintuitive.</p>
<p>The bet ARC made was that 95% of retain cycles are mechanical and the compiler can see them; the remaining 5% (closures, delegates) become explicit annotations the developer has to think about. Fifteen years on, the bet looks correct. The cycles that survive in modern Swift codebases are exactly the ones in the 5%: Combine subscriptions where the subscriber retains the publisher and the publisher retains the subscriber, view controllers retaining child VCs that retain their parent for navigation, and capture lists. None of these would have been mechanical to fix without language-level support, and all of them have idiomatic Swift solutions today (<code>.store(in: &amp;cancellables)</code>, <code>weak var parent</code>, <code>[weak self]</code>). The remaining surface where you can shoot yourself is small and well-mapped.</p>
<h2 id="Auto-Layout-relationships-not-coordinates"><a href="#Auto-Layout-relationships-not-coordinates" class="headerlink" title="Auto Layout: relationships, not coordinates"></a>Auto Layout: relationships, not coordinates</h2><p>Auto Layout shipped on the Mac in Lion. iOS had to wait until iOS 6 in 2012. So the 2011 session is a Mac-only reveal of a system that would, by the time SwiftUI shipped in 2019, become the default mental model for every Apple-platform layout system, including the one that doesn’t look like it.</p>
<p>The shift the talk argued for was: stop reasoning about layout as “this view goes at this rectangle.” Reason about it as “this view’s leading edge equals that view’s trailing edge plus eight points, with priority 1000.” The constraint is the unit of layout. The solver figures out the rectangles.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Pre-Auto Layout, springs and struts</span></span><br><span class="line">imageView.frame <span class="operator">=</span> <span class="type">CGRect</span>(x: <span class="number">16</span>, y: <span class="number">64</span>, width: <span class="number">80</span>, height: <span class="number">80</span>)</span><br><span class="line">imageView.autoresizingMask <span class="operator">=</span> [.flexibleRightMargin, .flexibleBottomMargin]</span><br><span class="line"></span><br><span class="line"><span class="comment">// Auto Layout via SnapKit (workspace standard)</span></span><br><span class="line">imageView.snp.makeConstraints &#123; make <span class="keyword">in</span></span><br><span class="line">    make.leading.equalToSuperview().offset(<span class="number">16</span>)</span><br><span class="line">    make.top.equalTo(view.safeAreaLayoutGuide).offset(<span class="number">16</span>)</span><br><span class="line">    make.size.equalTo(<span class="number">80</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>These two snippets look like they’re doing the same thing. They’re not. The frame version is a <em>promise</em> that the view is at (16, 64) and won’t be invalidated until something explicitly re-lays it out. The constraint version is a <em>contract</em> that says “leading is 16 from superview, top is 16 from safe area, size is 80×80,” and the system reapplies that contract whenever anything relevant changes (orientation, parent size, safe area inset, Dynamic Type, language direction). The promise breaks the first time the user rotates their iPad. The contract doesn’t.</p>
<p>What 2011 nailed and what every subsequent layout system reasserts is the <em>priority</em> concept. Constraints aren’t binary “true or false”; they’re prioritised, and when the solver finds a conflict it breaks the lower-priority one. The Mac talk introduced 1-1000 as the priority range and 750&#x2F;250 as the defaults for content hugging and compression resistance. SwiftUI, on the surface, looks like it doesn’t have priorities; under the surface its layout protocol returns size proposals and views negotiate over them, which is the same algorithm with a different vocabulary. SnapKit’s <code>.priority(.high)</code> and SwiftUI’s <code>.layoutPriority(1)</code> are the same primitive renamed.</p>
<p>The trap Auto Layout shipped with, and which is still alive in 2026, is the <code>UIView-Encapsulated-Layout-Width/Height</code> constraint that UITableView installs at required priority on header views, footer views, and cell contentViews. The 2011 talk could not anticipate this because it was a Mac reveal and UITableView didn’t exist on Mac. When Auto Layout came to iOS in 2012, the encapsulated layout was the bridge between the old “rectangle from <code>tableView:heightForRowAtIndexPath:</code>“ and the new “constraints derive the height.” The bridge competes with your own constraints at required priority, the solver breaks one of yours every layout pass, and the logs fill up. The community-endorsed fix has been “drop your competing constraints to priority 999” for a decade. The reason it works traces directly to the 2011 priority semantics: UIKit’s encapsulated constraint wins the transient sizing pass because it’s at 1000, your 999 constraints win during real layout because the encapsulated one disappears after sizing. The fix is correct precisely because the priority math is what 2011 said it was.</p>
<p>Looking at this from 2026, I think the Auto Layout reveal’s real contribution wasn’t the constraint solver. It was the cultural shift from frame-based thinking to relationship-based thinking. Frames-as-truth made apps that broke on iPad, broke on Dynamic Type, broke on RTL languages, and broke when the keyboard appeared. Relationships-as-truth let one set of layout code adapt to all of those. SwiftUI’s view tree-as-a-set-of-size-negotiations is not philosophically different. It’s the same bet expressed in a different syntax.</p>
<h2 id="GCD-deep-dive-queue-confinement-as-the-model"><a href="#GCD-deep-dive-queue-confinement-as-the-model" class="headerlink" title="GCD deep dive: queue confinement as the model"></a>GCD deep dive: queue confinement as the model</h2><p>The 2009 talk introduced GCD. The 2011 talk (session 210) is the one that establishes everything around <code>dispatch_async</code> that you’d actually use: custom serial queues, queue targeting, dispatch sources, dispatch groups, dispatch barriers, dispatch I&#x2F;O. The 2009 reveal was “here’s a way to schedule work.” The 2011 deep dive was “here’s the complete substrate.”</p>
<p>The position the talk argued for is one I’ve written and rewritten in my head every year since I learned it: <em>the unit of concurrency is the queue, not the thread</em>. You don’t ask “how many threads should I spawn?” You ask “what work needs to happen serially, what work can happen in parallel, and which work needs to come back to which queue when it’s done?” The runtime maps that onto a thread pool you don’t manage.</p>
<p>Fifteen years on, this is the position Swift Concurrency inherited:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// What 2011 GCD asked you to think about</span></span><br><span class="line"><span class="keyword">let</span> queue <span class="operator">=</span> <span class="type">DispatchQueue</span>(label: <span class="string">&quot;com.app.imageDecoder&quot;</span>)</span><br><span class="line">queue.async &#123;</span><br><span class="line">    <span class="keyword">let</span> image <span class="operator">=</span> decode(data)</span><br><span class="line">    <span class="type">DispatchQueue</span>.main.async &#123;</span><br><span class="line">        <span class="keyword">self</span>.imageView.image <span class="operator">=</span> image</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// What Swift Concurrency asks you to think about</span></span><br><span class="line"><span class="type">Task</span> &#123; <span class="meta">@MainActor</span> <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">let</span> image <span class="operator">=</span> <span class="keyword">await</span> decode(data)</span><br><span class="line">    imageView.image <span class="operator">=</span> image</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>The Swift version is shorter, but the decisions a developer has to make are exactly the same. You still have to decide that decoding doesn’t need the main thread (the <code>await</code> hops off MainActor onto the cooperative pool); you still have to decide that the UI update belongs back on main (the <code>@MainActor</code> annotation). Underneath, the actor runtime is using GCD’s dispatch primitives to schedule the work. <code>MainActor.run</code> is <code>DispatchQueue.main.async</code> with a type system on top.</p>
<p>What the 2011 deep dive added that I think is underappreciated is the <em>queue targeting hierarchy</em>. You could declare a serial queue and target it at the global concurrent queue, which targeted the system thread pool. You could declare ten serial queues all targeting the same parent. The hierarchy gave you bounded parallelism for free: if all ten queues funnel into a parent with QoS <code>.utility</code>, the system can throttle the lot of them under thermal pressure without you writing throttling code. Modern Swift Concurrency replaces this with <code>withTaskGroup</code> and structured cancellation, but the cancellation model (parent task cancels, children inherit the cancellation) is the same tree under a renamed set of leaves.</p>
<p>The thing that ages worst about 2011 GCD is the secondary vocabulary. Dispatch groups, dispatch semaphores, dispatch barriers, dispatch sources. The runtime still implements them, the headers are still there, but in a Swift 6 codebase I’ve stopped reaching for them. <code>dispatch_group</code> was the bounded-fanout primitive; <code>withTaskGroup</code> is its replacement and it’s better in every dimension (typed errors, structured cancellation, no manual <code>enter</code>&#x2F;<code>leave</code>). <code>dispatch_semaphore</code> was the bounded-concurrency primitive; an actor with a counter is its replacement and it’s better in every dimension (no risk of blocking the cooperative pool, no risk of priority inversion). A <code>DispatchSemaphore</code> in a 2026 codebase is almost always a sign of an async function being called from a sync context, and the fix is to make the caller async rather than to block a thread.</p>
<p>The interesting place to look in 2026 is the seam between the 2011 model (queues, where work runs) and the 2021 actor model (isolation, where work is <em>allowed</em> to run). The runtime still dispatches the way 2011 said it would, and the compiler enforces isolation the way 2021 said it would; the developer is the one who has to glue them together, particularly at the boundary where MainActor-isolated code hands a closure to an older callback-based Apple API that will run it on whatever queue it likes. The 2011 talk’s framing (“queue confinement is the unit of concurrency”) and the 2021 talk’s framing (“actor isolation is the unit of safety”) agree about everything except the boundary, and the boundary is where the bugs live.</p>
<h2 id="Notifications-the-system-is-the-surface-not-the-app"><a href="#Notifications-the-system-is-the-surface-not-the-app" class="headerlink" title="Notifications: the system is the surface, not the app"></a>Notifications: the system is the surface, not the app</h2><p>iOS 4 had local notifications in a primitive form. iOS 5 shipped Notification Center, which was the proper version of the idea: a <em>system</em> surface that aggregates alerts from all apps, displays them on its own schedule (swipe-down from the top of the screen, lock screen, banner area at the top), and lets the user act on them when they want, not when the originating app wants.</p>
<p>This is a position about whose attention belongs to whom. Pre-2011, an iOS app that wanted to alert the user did it via modal <code>UIAlertView</code>, <em>while the app was foregrounded</em>. The model was: app runs, app has something to say, app says it. Notification Center inverted that: app schedules an alert with the system, the system decides when to show it, and the user decides whether to engage with it. The app is a submitter, not a consumer. The user’s attention belongs to the user, mediated by the system.</p>
<p><code>UILocalNotification</code> and APNS shipped together because they’re the same primitive with different transports. Local notifications schedule against the device clock or app lifecycle events; remote notifications are submitted by your server to APNS and forwarded to the device. Both end up in the same Notification Center UI. Both fire the same delegate callback (<code>application(_:didReceiveLocalNotification:)</code> and <code>application(_:didReceiveRemoteNotification:)</code>, eventually unified by <code>UNUserNotificationCenter</code>). The submission mechanism is incidental; the surface is what matters.</p>
<p>The trade-off the system surface forced is that <em>the app does not control when the alert fires</em>. The system reorders, batches, holds, or drops notifications based on Focus modes, Do Not Disturb, the user’s notification preferences for that app, the time of day, and (since iOS 15) the alert’s interruption level. An app that wants its alert to show up at 9:00:00 sharp cannot guarantee it. The user might have Focus on. The system might batch the alert into a daily summary. The notification is a <em>request</em>, not a guarantee.</p>
<p>For a category of app (medical reminders, financial alerts, two-factor codes), the fact that the alert is best-effort is a problem. iOS 15 added <code>interruptionLevel = .timeSensitive</code> for these, which lets an alert break through most Focus modes but not Sleep mode. iOS 15 also added <code>.critical</code> (Apple-approved entitlement required) for alarms that need to break Sleep mode too. Both of those are concessions to the “but I really need this alert to fire” case, layered on top of the 2011 “the system decides” position. The interesting thing is that they’re concessions, not reversals: an app still cannot guarantee delivery, it can only request a higher tier of consideration from the system. The user, via Focus and per-app notification settings, remains the final arbiter.</p>
<p>What 2011 also couldn’t see was that the silent-push variant of the same channel would become the cross-device sync transport. Silent push (<code>content-available: 1</code>) shipped in iOS 7, two years after the 2011 reveal, and from 2014 onward CloudKit hooked it as the wake-up mechanism for “your data changed somewhere else.” Every modern Apple-platform sync (iCloud Drive, iCloud Photos, CloudKit, Find My, the Continuity handoffs) rides on silent push. The 2011 reveal pitched the API as “your news app should beep when there’s news.” The dominant use case in 2026 is “wake up to check the cloud,” with no UI surface at all. The same channel carries both, and the user never sees the difference; that the channel could absorb both roles is a sign the 2011 abstraction (a request to the system, delivered when the system chooses) was the right one.</p>
<h2 id="Two-more-positions-worth-naming"><a href="#Two-more-positions-worth-naming" class="headerlink" title="Two more positions worth naming"></a>Two more positions worth naming</h2><p>The other two sessions I revisited (208 Securing Application Data, 117 Performing Calendar Calculations) are smaller in scope but make the same shape of move as the five big ones: they take a thing developers were doing ad hoc and name it as a discipline.</p>
<p><strong>Keychain as the contract for secrets, not files.</strong> Before 2011, securing API keys, OAuth tokens, and passwords was a per-app decision: some apps stored them in plain <code>NSUserDefaults</code>, some encrypted them with a hardcoded key (which is no encryption), some used third-party wrappers around <code>SecItemAdd</code>. The 208 session said: there is one correct place, it’s the Keychain, it’s gated by device passcode and unlock state, it’s shareable across apps with the same App ID prefix, and the access policies (<code>WhenUnlocked</code>, <code>AfterFirstUnlock</code>, etc.) cover every reasonable threat model. Every Swift Keychain wrapper I’ve seen in a modern codebase is a thin sugar layer over the same C API the 2011 talk taught, with the same <code>kSecAttrAccessible</code> defaults. Apps that store API keys in UserDefaults still exist in 2026; they’re now flagged by static analysis instead of by a WWDC session, but the position is unchanged.</p>
<p>The corollary I didn’t appreciate until I started exporting user data is: don’t export settings that depend on secrets. If the user backs up their app data to a JSON file, and the file includes “Gemini integration enabled: true” plus the API key… the file is now a credential leak. The right answer is to keep the key in Keychain (where it doesn’t end up in the export), and also to skip the dependent settings (the “enabled” flag, the model selection) because they’re useless without the key anyway. The 2011 reveal didn’t articulate this corollary; it’s a 2024-era rule that descends from the 2011 boundary, surfacing the moment apps started shipping data-export features against third-party AI APIs.</p>
<p><strong>Calendar arithmetic goes through <code>Calendar</code>, never <code>TimeInterval</code>.</strong> Session 117 is the small-but-mighty one. The position is: do not do date math by multiplying seconds. <code>lastWatered.addingTimeInterval(7 * 24 * 3600)</code> is <em>wrong</em> for “water again in seven days” because the seventh day might straddle a DST transition and the result will be one hour off. Even worse, if you’re storing the result as the next-due timestamp, the off-by-an-hour bug accumulates over weeks. The 2011 reveal said: use <code>Calendar.current.date(byAdding: .day, value: 7, to: lastWatered)</code> instead. The calendar knows about DST, leap days, and locale-specific calendars (Hebrew, Islamic, Japanese), and it’ll do the right thing for all of them.</p>
<p>The canonical symptom is a recurring reminder that’s supposed to fire at a fixed local time drifting by an hour every six months, because <code>addingTimeInterval(N * 86400)</code> is wall-clock seconds and the wall clock loses or gains an hour twice a year. The fix is to rip out <code>addingTimeInterval</code> for anything dated in days or larger and replace with <code>Calendar.current.date(byAdding:)</code>. The discipline is 2011’s; the lesson is that fifteen years of “you should use <code>Calendar</code>“ hasn’t been enough to keep developers from reaching for the seconds-multiplication shortcut when nobody is watching.</p>
<h2 id="What-the-positions-cost"><a href="#What-the-positions-cost" class="headerlink" title="What the positions cost"></a>What the positions cost</h2><p>Reading the seven sessions back as a set, the thing I notice is how <em>expensive</em> each position was at the time. iCloud cost Apple three years of unreliable sync before CloudKit fixed it. ARC cost a year of bridging headers and migration tooling. Auto Layout cost a year of “the constraint solver is too slow” complaints before the layout engine was tuned. The notification center cost an entire app category (push-driven foreground-only apps) its previous model. The Keychain discipline cost developers a learning curve. The Calendar arithmetic discipline cost developers an extra line of code per date operation.</p>
<p>Each position is, in retrospect, a small upfront tax to avoid a class of bugs that would otherwise compound forever. Manual memory management compounds: one leak per developer per month, multiplied by the lifetime of the app, becomes the reason Twitter for Mac in 2010 needed a memory profiler open at all times. Frame-based layout compounds: every new device size adds an N×M matrix of layout bugs to chase. Polling-based notifications compound: every app polling for “is there new data?” adds to the per-device battery cost. The 2011 positions are taxes that prevent compounding.</p>
<p>What I don’t think the 2011 reveals got right was the <em>transition</em> story. iCloud Documents promised a path that didn’t work; ARC promised a migration tool that left subtle bugs; Auto Layout promised that springs-and-struts would coexist with constraints in the same view (true, but the boundary between them is where the worst bugs live). Storyboards, which I haven’t covered because every codebase I work in skipped them, promised a UI-as-data path that turned out to be merge-conflict hell and to make programmatic refactoring nearly impossible. The pattern is that 2011 was <em>right about the destination</em> and <em>wrong about the journey</em>. Apps that adopted the new positions early ate the rewrite cost up front. Apps that delayed paid it later in a different form (legacy code that didn’t compose with new APIs).</p>
<p>In 2026, the destination is uncontroversial. Every modern iOS app I write assumes user data lives in iCloud, memory is the compiler’s problem, layout is a set of constraints, concurrency is queue confinement, alerts come from the system, secrets live in Keychain, and dates are calendar arithmetic. None of those would have been the default in 2010. All of them were positions Apple staked out in 2011 and defended through the API rewrites of the decade after.</p>
<h2 id="If-you-want-to-read-the-originals"><a href="#If-you-want-to-read-the-originals" class="headerlink" title="If you want to read the originals"></a>If you want to read the originals</h2><p>Same caveats as the <a href="/WWDC-2010-Revisited/" title="WWDC 2010 revisited: iPad ships, iOS 4 names the primitives">2010 post</a>: Apple’s developer.apple.com no longer hosts pre-2020 WWDC videos. Session metadata is at the <a href="https://nonstrict.eu/wwdcindex/wwdc2011/">nonstrict.eu WWDC Index</a> and the videos themselves live on the <a href="https://archive.org/search?query=wwdc+2011">Internet Archive</a>.</p>
<p>Seven sessions in the reading order I’d recommend if you want the same arc as this post:</p>
<ol>
<li><strong>501</strong> “iCloud Storage Overview” for the account-as-boundary position</li>
<li><strong>323</strong> “Introducing Automatic Reference Counting” for the ownership-as-compile-time-contract position</li>
<li><strong>103</strong> “Cocoa Autolayout” for the relationships-not-coordinates position</li>
<li><strong>210</strong> “Mastering Grand Central Dispatch” for the queue-confinement position</li>
<li><strong>517</strong> “Using Local and Push Notifications” for the system-surface position</li>
<li><strong>208</strong> “Securing Application Data” for the Keychain-as-contract position</li>
<li><strong>117</strong> “Performing Calendar Calculations” for the calendar-arithmetic discipline</li>
</ol>
<p>Roughly fifty to sixty minutes each. The slides are dated (iOS 5 chrome on the demos, Mac OS X Lion’s brushed-metal window chrome on the Auto Layout examples), but the framing is precise enough that you can map each one onto a method call you wrote this week, even though the method call is going to have a different name than the one on the slide.</p>
]]></content>
      <categories>
        <category>iOS</category>
        <category>WWDC</category>
      </categories>
      <tags>
        <tag>Swift</tag>
        <tag>GCD</tag>
        <tag>history</tag>
        <tag>Core Data</tag>
        <tag>iCloud</tag>
        <tag>ARC</tag>
        <tag>Auto Layout</tag>
        <tag>Notifications</tag>
        <tag>Keychain</tag>
      </tags>
  </entry>
  <entry>
    <title>Zsh Usage</title>
    <url>/Zsh-Usage/</url>
    <content><![CDATA[<p>A working zsh config from 2021 plus the swap-ins worth making for a 2026 setup.</p>
<span id="more"></span>

<h2 id="What’s-current-2026"><a href="#What’s-current-2026" class="headerlink" title="What’s current (2026)"></a>What’s current (2026)</h2><p>The plugin manager and prompt below still work, but they are no longer the modern default:</p>
<ul>
<li><strong>Plugin manager</strong>: <a href="https://github.com/zsh-users/antigen">antigen</a> has had no real maintenance since 2018. New setups use <a href="https://github.com/zdharma-continuum/zinit">zinit</a> (fast, lazy-load, hooks for everything), <a href="https://github.com/marlonrichert/zsh-snap">znap</a> (smaller surface, faster startup), or oh-my-zsh’s built-in plugin loader (no third-party manager needed for the common case).</li>
<li><strong>Prompt</strong>: <a href="https://github.com/romkatv/powerlevel10k">Powerlevel10k</a> still works, but its maintainer paused most active development in 2024. <a href="https://starship.rs/">Starship</a> is the current cross-shell default: one Rust binary, one TOML config, identical prompt under zsh, bash, fish, nu. Old <code>~/.p10k.zsh</code> is not portable, so allow some time to recreate the segments worth keeping.</li>
<li><strong>CLI replacements</strong>: <code>bat</code> for <code>cat</code>, <code>fd</code> for <code>find</code>, <code>ripgrep</code> for <code>grep</code>, <code>dust</code> for <code>du</code>, <code>procs</code> for <code>ps</code>, <code>zoxide</code> for <code>z</code>&#x2F;<code>autojump</code>. See <a href="/M1-Mac-Development-Setup/" title="Mac development setup">Mac development setup</a> for the full list.</li>
</ul>
<p>Migrate the prompt first; plugins rarely need rewriting when only the manager changes.</p>
<h2 id="Install-oh-my-zsh"><a href="#Install-oh-my-zsh" class="headerlink" title="Install oh-my-zsh"></a>Install oh-my-zsh</h2><p><a href="https://ohmyz.sh/">oh-my-zsh</a> is still the easiest base, even if you skip antigen:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sh -c <span class="string">&quot;<span class="subst">$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)</span>&quot;</span></span><br></pre></td></tr></table></figure>

<p><a href="https://github.com/ohmyzsh/ohmyzsh/wiki/Plugins-Overview">Plugins overview</a>.</p>
<h2 id="Reference-zshrc-2021-setup-oh-my-zsh-antigen-Powerlevel10k"><a href="#Reference-zshrc-2021-setup-oh-my-zsh-antigen-Powerlevel10k" class="headerlink" title="Reference .zshrc (2021 setup, oh-my-zsh + antigen + Powerlevel10k)"></a>Reference <code>.zshrc</code> (2021 setup, oh-my-zsh + antigen + Powerlevel10k)</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">######################## Powerlevel10k ########################</span></span><br><span class="line"><span class="comment"># Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.</span></span><br><span class="line"><span class="comment"># Initialization code that may require console input (password prompts, [y/n]</span></span><br><span class="line"><span class="comment"># confirmations, etc.) must go above this block; everything else may go below.</span></span><br><span class="line"><span class="keyword">if</span> [[ -r <span class="string">&quot;<span class="variable">$&#123;XDG_CACHE_HOME:-<span class="variable">$HOME</span>/.cache&#125;</span>/p10k-instant-prompt-<span class="variable">$&#123;(%):-%n&#125;</span>.zsh&quot;</span> ]]; <span class="keyword">then</span></span><br><span class="line">  <span class="built_in">source</span> <span class="string">&quot;<span class="variable">$&#123;XDG_CACHE_HOME:-<span class="variable">$HOME</span>/.cache&#125;</span>/p10k-instant-prompt-<span class="variable">$&#123;(%):-%n&#125;</span>.zsh&quot;</span></span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.</span></span><br><span class="line">[[ ! -f ~/.p10k.zsh ]] || <span class="built_in">source</span> ~/.p10k.zsh</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">######################## Antigen ########################</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">source</span> /opt/homebrew/share/antigen/antigen.zsh</span><br><span class="line"><span class="built_in">source</span> <span class="variable">$HOMEBREW_PREFIX</span>/opt/chruby/share/chruby/chruby.sh</span><br><span class="line">chruby ruby-3.4.2</span><br><span class="line"></span><br><span class="line"><span class="comment"># Load the oh-my-zsh&#x27;s library.</span></span><br><span class="line">antigen use oh-my-zsh</span><br><span class="line"></span><br><span class="line"><span class="comment"># Bundles from the default repo (robbyrussell&#x27;s oh-my-zsh)</span></span><br><span class="line">antigen bundle colored-man-pages</span><br><span class="line">antigen bundle extract</span><br><span class="line">antigen bundle <span class="built_in">history</span></span><br><span class="line">antigen bundle z</span><br><span class="line">antigen bundle git</span><br><span class="line">antigen bundle npm</span><br><span class="line">antigen bundle gem</span><br><span class="line">antigen bundle python</span><br><span class="line">antigen bundle brew</span><br><span class="line">antigen bundle osx</span><br><span class="line">antigen bundle pod</span><br><span class="line"></span><br><span class="line"><span class="comment"># Fish shell like syntax highlighting for Zsh.</span></span><br><span class="line">antigen bundle zsh-users/zsh-syntax-highlighting</span><br><span class="line"></span><br><span class="line"><span class="comment"># Fish-like autosuggestions for zsh</span></span><br><span class="line">antigen bundle zsh-users/zsh-autosuggestions</span><br><span class="line"></span><br><span class="line"><span class="comment"># Additional completion definitions for Zsh.</span></span><br><span class="line">antigen bundle zsh-users/zsh-completions</span><br><span class="line"></span><br><span class="line"><span class="comment"># Load the theme.</span></span><br><span class="line">antigen theme romkatv/powerlevel10k</span><br><span class="line"></span><br><span class="line"><span class="comment"># Tell Antigen that you&#x27;re done.</span></span><br><span class="line">antigen apply</span><br><span class="line"></span><br><span class="line"><span class="comment">######################## User Configuration ########################</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># list</span></span><br><span class="line"><span class="built_in">alias</span> <span class="built_in">ls</span>=<span class="string">&#x27;ls -hFG&#x27;</span></span><br><span class="line"><span class="built_in">alias</span> tree=<span class="string">&#x27;tree -C -I node_modules&#x27;</span></span><br><span class="line"><span class="built_in">alias</span> l=<span class="string">&#x27;eza -aghlF --git --icons&#x27;</span></span><br><span class="line"><span class="built_in">alias</span> ltree=<span class="string">&#x27;eza -ghlTF --git -I=node_modules&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># ncdu</span></span><br><span class="line"><span class="built_in">alias</span> ncdu=<span class="string">&quot;ncdu --color dark -r -x&quot;</span></span><br><span class="line"><span class="built_in">alias</span> ncdu-exclude=<span class="string">&quot;ncdu --color dark -r -x --exclude .git --exclude node_modules&quot;</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Shell</category>
      </categories>
      <tags>
        <tag>zsh</tag>
        <tag>oh-my-zsh</tag>
        <tag>powerlevel10k</tag>
        <tag>antigen</tag>
        <tag>zinit</tag>
        <tag>starship</tag>
      </tags>
  </entry>
  <entry>
    <title>Every way to build a website in 2026: frameworks, SSGs, and platforms compared</title>
    <url>/Web-Framework-Comparison-2026/</url>
    <content><![CDATA[<p>Every major way to build a website in 2026, from a single HTML file to a full-stack React app, plus where to deploy them.</p>
<span id="more"></span>

<h2 id="Glossary"><a href="#Glossary" class="headerlink" title="Glossary"></a>Glossary</h2><p>Before diving in, here are the acronyms you’ll see throughout this post:</p>
<table>
<thead>
<tr>
<th>Acronym</th>
<th>Full name</th>
<th>What it means</th>
</tr>
</thead>
<tbody><tr>
<td><strong>SPA</strong></td>
<td>Single-Page Application</td>
<td>The browser loads one HTML page; JS handles all navigation and rendering client-side</td>
</tr>
<tr>
<td><strong>SSR</strong></td>
<td>Server-Side Rendering</td>
<td>The server generates HTML on each request, then sends it to the browser</td>
</tr>
<tr>
<td><strong>SSG</strong></td>
<td>Static Site Generation</td>
<td>Pages are pre-built to HTML at build time, then served as static files</td>
</tr>
<tr>
<td><strong>ISR</strong></td>
<td>Incremental Static Regeneration</td>
<td>Static pages that re-generate in the background after a set interval (Next.js concept)</td>
</tr>
<tr>
<td><strong>CSR</strong></td>
<td>Client-Side Rendering</td>
<td>The browser downloads JS, then renders the page entirely in the client</td>
</tr>
<tr>
<td><strong>RSC</strong></td>
<td>React Server Components</td>
<td>React components that run only on the server, shipping zero JS to the client</td>
</tr>
<tr>
<td><strong>HMR</strong></td>
<td>Hot Module Replacement</td>
<td>Code changes appear instantly in the browser during development without a full reload</td>
</tr>
<tr>
<td><strong>CDN</strong></td>
<td>Content Delivery Network</td>
<td>A network of servers worldwide that caches and serves your site from the nearest location</td>
</tr>
<tr>
<td><strong>SEO</strong></td>
<td>Search Engine Optimization</td>
<td>Making your site discoverable by search engines (Google, Bing, etc.)</td>
</tr>
<tr>
<td><strong>TTFB</strong></td>
<td>Time to First Byte</td>
<td>How long until the browser receives the first byte of the response from the server</td>
</tr>
<tr>
<td><strong>MDX</strong></td>
<td>Markdown + JSX</td>
<td>Markdown files that can embed React&#x2F;JSX components inline</td>
</tr>
<tr>
<td><strong>SFC</strong></td>
<td>Single-File Component</td>
<td>A file containing template, script, and style in one place (used by Vue and Svelte)</td>
</tr>
<tr>
<td><strong>JAMstack</strong></td>
<td>JavaScript, APIs, Markup</td>
<td>Architecture pattern: pre-rendered markup + client-side JS + API calls (no monolithic server)</td>
</tr>
<tr>
<td><strong>WAF</strong></td>
<td>Web Application Firewall</td>
<td>Filters and blocks malicious HTTP traffic before it reaches your site</td>
</tr>
</tbody></table>
<h2 id="The-landscape"><a href="#The-landscape" class="headerlink" title="The landscape"></a>The landscape</h2><p>Frameworks have split into two camps: ones tuned for <strong>web applications</strong> (dashboards, SaaS) and ones tuned for <strong>content sites</strong> (blogs, portfolios, docs). A few try to do both. Pick by what you’re building.</p>
<p>This guide covers 15 options across 5 categories:</p>
<table>
<thead>
<tr>
<th>Category</th>
<th>Options</th>
<th>Best for</th>
</tr>
</thead>
<tbody><tr>
<td><strong>No framework</strong></td>
<td>Plain HTML&#x2F;CSS&#x2F;JS, HTML + Vite</td>
<td>Maximum control, tiny sites, learning</td>
</tr>
<tr>
<td><strong>Full SPA</strong></td>
<td>React (Vite), Vue, Svelte, Angular</td>
<td>Interactive apps, dashboards, complex state</td>
</tr>
<tr>
<td><strong>Full-stack &#x2F; Hybrid</strong></td>
<td>Next.js, Nuxt, SvelteKit, Remix</td>
<td>Apps that need SSR, SEO, API routes</td>
</tr>
<tr>
<td><strong>Content-first</strong></td>
<td>Astro</td>
<td>Modern content sites with component islands</td>
</tr>
<tr>
<td><strong>Static site generators</strong></td>
<td>Hugo, Jekyll, Hexo, Eleventy</td>
<td>Blogs, docs, template-driven content sites</td>
</tr>
</tbody></table>
<h2 id="Quick-comparison"><a href="#Quick-comparison" class="headerlink" title="Quick comparison"></a>Quick comparison</h2><table>
<thead>
<tr>
<th>Option</th>
<th>Language</th>
<th>Rendering</th>
<th>JS shipped</th>
<th>Learning curve</th>
<th>Ecosystem</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Plain HTML&#x2F;CSS&#x2F;JS</strong></td>
<td>HTML&#x2F;CSS&#x2F;JS</td>
<td>Static</td>
<td>Only what you write</td>
<td>Lowest</td>
<td>N&#x2F;A (web standards)</td>
</tr>
<tr>
<td><strong>HTML + Vite</strong></td>
<td>HTML&#x2F;CSS&#x2F;JS&#x2F;TS</td>
<td>Static</td>
<td>Only what you write</td>
<td>Low</td>
<td>Vite plugins</td>
</tr>
<tr>
<td><strong>React + Vite</strong></td>
<td>JSX&#x2F;TSX</td>
<td>CSR (SPA)</td>
<td>Full bundle</td>
<td>Medium</td>
<td>Massive</td>
</tr>
<tr>
<td><strong>Next.js</strong></td>
<td>JSX&#x2F;TSX</td>
<td>SSR&#x2F;SSG&#x2F;ISR&#x2F;CSR</td>
<td>Varies (RSC helps)</td>
<td>High</td>
<td>Very large</td>
</tr>
<tr>
<td><strong>Vue</strong></td>
<td>SFC (.vue)</td>
<td>CSR (SPA)</td>
<td>Full bundle</td>
<td>Low-Medium</td>
<td>Large</td>
</tr>
<tr>
<td><strong>Nuxt</strong></td>
<td>SFC (.vue)</td>
<td>SSR&#x2F;SSG&#x2F;CSR</td>
<td>Varies</td>
<td>Medium</td>
<td>Large</td>
</tr>
<tr>
<td><strong>Angular</strong></td>
<td>TypeScript</td>
<td>CSR (SPA)</td>
<td>Full bundle</td>
<td>High</td>
<td>Very large</td>
</tr>
<tr>
<td><strong>Svelte</strong></td>
<td>.svelte</td>
<td>CSR (SPA)</td>
<td>Compiled (small)</td>
<td>Low</td>
<td>Growing</td>
</tr>
<tr>
<td><strong>SvelteKit</strong></td>
<td>.svelte</td>
<td>SSR&#x2F;SSG&#x2F;CSR</td>
<td>Compiled (small)</td>
<td>Medium</td>
<td>Growing</td>
</tr>
<tr>
<td><strong>Astro</strong></td>
<td>.astro + any</td>
<td>SSG (+ SSR opt-in)</td>
<td>Zero by default</td>
<td>Low</td>
<td>Moderate</td>
</tr>
<tr>
<td><strong>Remix</strong></td>
<td>JSX&#x2F;TSX</td>
<td>SSR</td>
<td>Varies</td>
<td>Medium-High</td>
<td>Moderate</td>
</tr>
<tr>
<td><strong>Hugo</strong></td>
<td>Go templates</td>
<td>SSG</td>
<td>Zero by default</td>
<td>Medium</td>
<td>Large</td>
</tr>
<tr>
<td><strong>Jekyll</strong></td>
<td>Liquid&#x2F;Ruby</td>
<td>SSG</td>
<td>Zero by default</td>
<td>Low-Medium</td>
<td>Large (mature)</td>
</tr>
<tr>
<td><strong>Hexo</strong></td>
<td>EJS&#x2F;Nunjucks</td>
<td>SSG</td>
<td>Zero by default</td>
<td>Low</td>
<td>Moderate</td>
</tr>
<tr>
<td><strong>Eleventy</strong></td>
<td>Any template lang</td>
<td>SSG</td>
<td>Zero by default</td>
<td>Low-Medium</td>
<td>Moderate</td>
</tr>
</tbody></table>
<hr>
<h2 id="No-framework"><a href="#No-framework" class="headerlink" title="No framework"></a>No framework</h2><p>Sometimes the best framework is no framework. Plain HTML, CSS, and JavaScript – the original web stack – still works and is often the right call.</p>
<h3 id="Plain-HTML-CSS-JS"><a href="#Plain-HTML-CSS-JS" class="headerlink" title="Plain HTML &#x2F; CSS &#x2F; JS"></a>Plain HTML &#x2F; CSS &#x2F; JS</h3><p>No build step, no dependencies, no node_modules. Write files, open in a browser, deploy anywhere.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">my-site/</span><br><span class="line">├── index.html</span><br><span class="line">├── about.html</span><br><span class="line">├── css/</span><br><span class="line">│   └── styles.css</span><br><span class="line">├── js/</span><br><span class="line">│   └── main.js</span><br><span class="line">└── images/</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Zero dependencies, zero build time, zero configuration</li>
<li>Works forever – no framework to upgrade, no breaking changes</li>
<li>Fastest possible page loads (nothing between you and the browser)</li>
<li>Full control over every byte shipped</li>
<li>Easy to understand for anyone who knows web basics</li>
<li>Deploy by dragging a folder to Cloudflare Pages, Netlify, or any web server</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>No components – copy-paste for shared headers&#x2F;footers (or use <code>&lt;template&gt;</code> &#x2F; Web Components)</li>
<li>No hot reload (unless you add a live-server tool)</li>
<li>No TypeScript, no bundling, no tree-shaking</li>
<li>No image optimization pipeline</li>
<li>Repetitive for sites with many pages that share layout</li>
</ul>
<p><strong>Best for</strong>: Small sites (1-5 pages), prototypes, landing pages, learning web development, sites that need to last decades without maintenance.</p>
<hr>
<h3 id="HTML-Vite"><a href="#HTML-Vite" class="headerlink" title="HTML + Vite"></a>HTML + Vite</h3><p>Take the plain HTML approach but add Vite as a dev server and build tool. You get hot reload, TypeScript, CSS preprocessing, and asset optimization – without adopting a UI framework.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm create vite@latest my-site -- --template vanilla-ts</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">my-site/</span><br><span class="line">├── index.html          # Entry point (Vite serves this)</span><br><span class="line">├── about.html</span><br><span class="line">├── src/</span><br><span class="line">│   ├── main.ts         # TypeScript, auto-reloads</span><br><span class="line">│   └── style.css       # PostCSS, autoprefixer, etc.</span><br><span class="line">├── public/             # Static assets (copied as-is)</span><br><span class="line">├── vite.config.ts</span><br><span class="line">└── package.json</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Hot Module Replacement (HMR) – instant feedback during development</li>
<li>TypeScript out of the box</li>
<li>CSS modules, PostCSS, Sass&#x2F;Less support (just <code>npm install</code>)</li>
<li>Asset optimization on build (minification, hashing, tree-shaking); Vite 8 (Mar 2026) replaced Rollup with Rolldown 1.0 (Rust) for 10-30x faster builds</li>
<li>Multi-page support via <code>build.rollupOptions.input</code></li>
<li>Still just HTML files – no framework abstraction</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Still no component model (shared layout requires manual solutions)</li>
<li>Adds a build step and <code>node_modules</code></li>
<li>Multi-page config is manual (list each <code>.html</code> in rollup input)</li>
<li>No routing – each page is a separate <code>.html</code> file</li>
</ul>
<p><strong>Best for</strong>: Small-to-medium sites where you want modern tooling (TS, CSS processing, minification) without a framework. Great for portfolios, experiments, and sites with light interactivity.</p>
<hr>
<h3 id="When-to-go-frameworkless"><a href="#When-to-go-frameworkless" class="headerlink" title="When to go frameworkless"></a>When to go frameworkless</h3><table>
<thead>
<tr>
<th>Situation</th>
<th align="center">No framework</th>
<th align="center">Framework</th>
</tr>
</thead>
<tbody><tr>
<td>1-5 static pages</td>
<td align="center">Yes</td>
<td align="center">Overkill</td>
</tr>
<tr>
<td>Portfolio with 3 sections</td>
<td align="center">Yes (+ Vite)</td>
<td align="center">Optional</td>
</tr>
<tr>
<td>Blog with 50+ posts</td>
<td align="center">No</td>
<td align="center">SSG or Astro</td>
</tr>
<tr>
<td>Shared components across pages</td>
<td align="center">Maybe (Web Components)</td>
<td align="center">Yes</td>
</tr>
<tr>
<td>Complex interactivity (drag-drop, real-time)</td>
<td align="center">No</td>
<td align="center">Yes</td>
</tr>
<tr>
<td>Team of 3+ developers</td>
<td align="center">Probably not</td>
<td align="center">Yes (consistency)</td>
</tr>
</tbody></table>
<hr>
<h2 id="Full-SPA-frameworks"><a href="#Full-SPA-frameworks" class="headerlink" title="Full SPA frameworks"></a>Full SPA frameworks</h2><p>These render entirely in the browser. Great for apps behind a login where SEO doesn’t matter. Not ideal for public content sites.</p>
<h3 id="React-with-Vite"><a href="#React-with-Vite" class="headerlink" title="React (with Vite)"></a>React (with Vite)</h3><p>The industry default. React itself is a UI library, not a framework – you assemble the stack yourself.</p>
<p><strong>Stack</strong>: React 19.2 + Vite 8 (Rolldown 1.0 bundler) + React Router + your choice of state management</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm create vite@latest my-app -- --template react-ts</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Largest ecosystem: any problem has a library for it</li>
<li>Huge hiring pool and community knowledge</li>
<li>React Compiler eliminates manual <code>useMemo</code>&#x2F;<code>useCallback</code> optimization</li>
<li>Server Components available (via Next.js&#x2F;Remix)</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Assembly required: routing, state, data fetching, CSS – all separate decisions</li>
<li>Virtual DOM overhead (small but real vs compiled frameworks)</li>
<li>JSX is polarizing – mixing markup and logic in one file</li>
<li><code>useEffect</code> footgun – still the #1 source of React bugs</li>
</ul>
<p><strong>Best for</strong>: Teams with React experience building interactive apps (dashboards, admin panels, internal tools).</p>
<p><strong>Avoid for</strong>: Simple content sites, blogs, portfolios – you’ll ship a full JS runtime for static content.</p>
<hr>
<h3 id="Vue-3"><a href="#Vue-3" class="headerlink" title="Vue 3"></a>Vue 3</h3><p>Vue takes an opinionated middle ground: single-file components (<code>.vue</code>) with template, script, and style in one file. The Composition API (<code>ref</code>, <code>computed</code>, <code>watch</code>) is now the standard.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm create vue@latest</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Gentler learning curve than React – clear separation of template&#x2F;logic&#x2F;style</li>
<li>First-party solutions: Vue Router, Pinia (state), Vite (build) – all official</li>
<li>Excellent docs (consistently rated best in surveys)</li>
<li><code>&lt;script setup&gt;</code> syntax is concise and TypeScript-friendly</li>
<li>Reactivity is fine-grained (no virtual DOM diffing for state updates)</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Smaller ecosystem than React (fewer third-party libraries)</li>
<li>Two API styles (Options vs Composition) can confuse beginners reading tutorials</li>
<li>Less dominant in the US job market (stronger in Asia and Europe)</li>
</ul>
<p><strong>Best for</strong>: Solo devs or small teams who want batteries-included without the complexity of React’s ecosystem decisions.</p>
<hr>
<h3 id="Svelte-5"><a href="#Svelte-5" class="headerlink" title="Svelte 5"></a>Svelte 5</h3><p>Svelte compiles components at build time into efficient imperative DOM updates. No virtual DOM, no runtime framework code shipped to the browser.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npx sv create my-app</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Smallest bundle sizes – compiled output is vanilla JS</li>
<li>Runes (<code>$state</code>, <code>$derived</code>, <code>$effect</code>) are intuitive reactivity primitives</li>
<li><code>.svelte</code> files are clean: HTML + JS + CSS, minimal boilerplate</li>
<li>Built-in transitions and animations</li>
<li>Fastest “time to interactive” of any major framework</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Smallest ecosystem of the three (fewer component libraries, fewer StackOverflow answers)</li>
<li>Svelte 5 (Runes) is a significant paradigm shift from Svelte 4 – some churn</li>
<li>Fewer job listings specifically requesting Svelte</li>
<li>Some edge cases in TypeScript support</li>
</ul>
<p><strong>Best for</strong>: Performance-critical apps, small-to-medium projects, developers who value simplicity and small bundles.</p>
<hr>
<h3 id="Angular-19"><a href="#Angular-19" class="headerlink" title="Angular 19"></a>Angular 19</h3><p>Google’s opinionated, batteries-included framework. TypeScript-first, with a full solution for routing, forms, HTTP, testing, and i18n out of the box.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g @angular/cli</span><br><span class="line">ng new my-app</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Complete framework: routing, forms, HTTP client, dependency injection, testing – all built in</li>
<li>TypeScript is mandatory, not optional – enforces type safety across the entire project</li>
<li>Signals (new reactive primitive) bring fine-grained reactivity, replacing much of RxJS</li>
<li>Standalone components simplify the module system (NgModules now optional)</li>
<li>Strong enterprise adoption – many large companies standardize on Angular</li>
<li>Excellent CLI (<code>ng generate</code>, <code>ng test</code>, <code>ng build</code>) with schematics for code generation</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Steepest learning curve of any major framework (dependency injection, decorators, RxJS)</li>
<li>Verbose compared to React&#x2F;Vue&#x2F;Svelte – more boilerplate for simple tasks</li>
<li>Large initial bundle size</li>
<li>Frequent major version releases (every 6 months) with migration effort</li>
<li>Smaller community mindshare outside enterprise (fewer tutorials, blog posts, indie projects)</li>
</ul>
<p><strong>Best for</strong>: Enterprise applications, large teams that benefit from strong conventions, projects where TypeScript-everywhere and built-in tooling outweigh the learning curve.</p>
<hr>
<h2 id="Full-stack-hybrid-frameworks"><a href="#Full-stack-hybrid-frameworks" class="headerlink" title="Full-stack &#x2F; hybrid frameworks"></a>Full-stack &#x2F; hybrid frameworks</h2><p>These add SSR, SSG, API routes, and deployment adapters on top of a UI library.</p>
<h3 id="Next-js-16"><a href="#Next-js-16" class="headerlink" title="Next.js 16"></a>Next.js 16</h3><p>The dominant React meta-framework. Vercel-backed, used by a huge portion of the React ecosystem.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npx create-next-app@latest</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Most complete React framework: SSR, SSG, ISR, API routes, middleware, image optimization</li>
<li>React Server Components (RSC) reduce client-side JS significantly</li>
<li>App Router with nested layouts and parallel routes</li>
<li>Excellent Vercel deployment (but works on any Node host, Cloudflare, etc.)</li>
<li>Massive community, tutorials, and third-party integrations</li>
<li><strong>Turbopack is the default in v16</strong> for <code>next dev</code> and <code>next build</code>; React Compiler 1.0 is stable opt-in; React 19.2 View Transitions and Cache Components are production-ready.</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>High complexity: App Router vs Pages Router, server vs client components, caching layers</li>
<li>Vercel-centric defaults (some features work best on Vercel)</li>
<li>Bundle size can balloon if you’re not careful about <code>&quot;use client&quot;</code> boundaries</li>
<li>Frequent breaking changes between major versions</li>
<li>Caching behavior has been controversial and was reworked again in v16 (Cache Components)</li>
<li>RSC payload validation has been a real attack surface: CVE-2025-55182 (“React2Shell”, Dec 2025) was an unauthenticated RCE through Flight serialization. Stay on React 19.2.4+ &#x2F; Next.js 15.5.10+ &#x2F; 16.0.11+.</li>
</ul>
<p><strong>Best for</strong>: Production web apps that need SEO + interactivity, e-commerce, SaaS marketing + dashboard, content platforms.</p>
<p><strong>Avoid for</strong>: Simple static sites (overkill), projects where you want minimal vendor influence.</p>
<hr>
<h3 id="Nuxt-4"><a href="#Nuxt-4" class="headerlink" title="Nuxt 4"></a>Nuxt 4</h3><p>The Vue equivalent of Next.js. Full-stack framework with SSR, SSG, API routes, and auto-imports.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npx nuxi@latest init my-app</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Auto-imports: components, composables, and utilities just work without explicit imports</li>
<li>File-based routing (like Next.js)</li>
<li>Nitro server engine: deploy to 20+ platforms (Node, Cloudflare, Deno, Vercel, Netlify)</li>
<li>Built-in <code>useFetch</code>&#x2F;<code>useAsyncData</code> for data fetching with SSR hydration</li>
<li>Module ecosystem (Nuxt Content, Nuxt Image, Nuxt Auth, etc.)</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>“Magic” auto-imports can confuse TypeScript tooling and new developers</li>
<li>Smaller community than Next.js</li>
<li>Performance overhead from the module&#x2F;plugin system</li>
<li>Breaking changes between Nuxt 2 → 3 → 4 were significant; <strong>Nuxt v3 reaches EOL on 31 July 2026</strong>, so any v3 codebase should be planning its v4 migration now.</li>
</ul>
<p><strong>Best for</strong>: Vue developers who want a full-stack framework with conventions and deployment flexibility.</p>
<hr>
<h3 id="SvelteKit"><a href="#SvelteKit" class="headerlink" title="SvelteKit"></a>SvelteKit</h3><p>The official Svelte meta-framework. Handles routing, SSR, SSG, and API endpoints. Currently on the 2.5x line (2.50 in Feb 2026, 2.55 in Apr); Svelte 5’s runes system is now the default reactivity model.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npx sv create my-app</span><br><span class="line"><span class="comment"># Select SvelteKit when prompted</span></span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Inherits Svelte’s small bundle advantage in a full-stack context</li>
<li>File-based routing with layouts and loading functions</li>
<li><code>+page.server.ts</code> &#x2F; <code>+server.ts</code> for backend logic (form actions, API routes)</li>
<li>Adapter system: deploy to Node, Cloudflare, Vercel, Netlify, static, etc.</li>
<li>Forms work without JS by default (progressive enhancement)</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Smallest ecosystem of the meta-frameworks</li>
<li>Fewer community tutorials than Next.js</li>
<li>Adapter system still has occasional rough edges</li>
</ul>
<p><strong>Best for</strong>: Full-stack apps where performance and bundle size matter.</p>
<hr>
<h3 id="Remix"><a href="#Remix" class="headerlink" title="Remix"></a>Remix</h3><p>A React framework focused on web fundamentals: HTTP, forms, progressive enhancement. Now merged with React Router v7.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npx create-remix@latest</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Web-standards-first: uses native <code>&lt;form&gt;</code>, <code>Request</code>&#x2F;<code>Response</code>, HTTP caching</li>
<li>Nested routing with parallel data loading (no waterfalls)</li>
<li>Apps work without JS, then progressively enhance</li>
<li>Excellent error boundaries and loading states</li>
<li>Runs on any JS runtime (Node, Deno, Cloudflare Workers, etc.)</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Smaller community than Next.js</li>
<li>Merger with React Router v7 caused confusion about identity&#x2F;direction</li>
<li>No built-in static site generation (SSR-focused)</li>
<li>Fewer deployment-specific optimizations compared to Next.js on Vercel</li>
</ul>
<p><strong>Best for</strong>: Teams that care about web fundamentals, accessibility, and progressive enhancement.</p>
<hr>
<h2 id="Content-first-framework"><a href="#Content-first-framework" class="headerlink" title="Content-first framework"></a>Content-first framework</h2><h3 id="Astro-6"><a href="#Astro-6" class="headerlink" title="Astro 6"></a>Astro 6</h3><p>Astro’s core idea: ship <strong>zero JavaScript</strong> by default. Interactive components (React, Vue, Svelte, Solid) load only where needed via “islands.” Astro 6 (Feb 2026) added component-level caching and tightened runtime fidelity.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm create astro@latest</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Zero JS by default – pages are static HTML unless you opt in</li>
<li><strong>Islands architecture</strong>: drop in React&#x2F;Vue&#x2F;Svelte components only where interactivity is needed</li>
<li>Content Collections: type-safe markdown&#x2F;MDX&#x2F;JSON with schema validation</li>
<li>Built-in image optimization (<code>astro:assets</code>)</li>
<li>View Transitions API for page transitions without an SPA</li>
<li>Deploys to Cloudflare, Vercel, Netlify, or any static host</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Not suited for heavy interactivity – the island model doesn’t scale for complex client state</li>
<li>Mixing multiple UI frameworks in one project can get confusing</li>
<li>Younger ecosystem than React&#x2F;Next.js</li>
<li>SSR mode is less mature than Next.js</li>
<li><strong>Cloudflare acquired Astro in January 2026.</strong> The team is independent so far, but the long-term governance question is now real; expect tighter Cloudflare integration over time.</li>
</ul>
<p><strong>Best for</strong>: Blogs, portfolios, documentation, marketing sites, landing pages – any content-heavy site where performance matters.</p>
<p><strong>Avoid for</strong>: Dashboards, real-time features, or apps with heavy client state.</p>
<hr>
<h2 id="Static-site-generators"><a href="#Static-site-generators" class="headerlink" title="Static site generators"></a>Static site generators</h2><p>These take content (Markdown, data files) and templates, and produce plain HTML. No client-side JS runtime, no hydration – just HTML pages.</p>
<h3 id="Eleventy-11ty"><a href="#Eleventy-11ty" class="headerlink" title="Eleventy (11ty)"></a>Eleventy (11ty)</h3><p>A flexible, zero-config Node.js static site generator. No UI framework – just templates compiled to HTML.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install @11ty/eleventy</span><br><span class="line">npx eleventy --serve</span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Zero client JS (no framework runtime at all)</li>
<li>Template-language agnostic: Nunjucks, Liquid, Markdown, WebC, and 8 more</li>
<li>Fast builds (~2s for 1000 pages)</li>
<li>No lock-in to any UI framework</li>
<li>Data cascade system for content-heavy sites</li>
<li>Active development and growing community</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>No component model (templates, not components)</li>
<li>Adding interactivity requires manual JS (no island system)</li>
<li>Less polished DX than Astro (no built-in image optimization)</li>
<li>Fewer themes – relies on starter projects instead</li>
</ul>
<p><strong>Best for</strong>: Developers who want maximum control and minimal abstraction. Great for blogs and docs.</p>
<hr>
<h3 id="Hugo"><a href="#Hugo" class="headerlink" title="Hugo"></a>Hugo</h3><p>The fastest static site generator. Written in Go, single binary, no dependencies.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install hugo</span><br><span class="line">hugo new site my-blog</span><br><span class="line">hugo server  <span class="comment"># http://localhost:1313</span></span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Blazing fast builds – 10,000 pages in under 1 second</li>
<li>Single binary, no Node.js&#x2F;Ruby&#x2F;Python required</li>
<li>Built-in image processing, i18n, taxonomies, menus</li>
<li>Large theme ecosystem (300+ themes)</li>
<li>Shortcodes for reusable content snippets</li>
<li>Built-in asset pipeline (Sass, PostCSS, JS bundling)</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Go template syntax is unfamiliar and verbose (<code>&#123;&#123; .Params.title &#125;&#125;</code>, <code>&#123;&#123; range &#125;&#125;</code>, <code>&#123;&#123; with &#125;&#125;</code>)</li>
<li>Debugging template errors is painful (cryptic error messages)</li>
<li>Adding interactivity requires manual JS – no component system</li>
<li>Theme customization can be awkward (override files by path)</li>
<li>Opinionated content organization (sections, bundles) has a learning curve</li>
</ul>
<p><strong>Best for</strong>: High-volume blogs and docs sites where build speed matters. Technical users comfortable with Go templates.</p>
<hr>
<h3 id="Jekyll"><a href="#Jekyll" class="headerlink" title="Jekyll"></a>Jekyll</h3><p>The original static site generator (2008). Ruby-based, powers GitHub Pages natively.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gem install jekyll bundler</span><br><span class="line">jekyll new my-blog</span><br><span class="line">bundle <span class="built_in">exec</span> jekyll serve  <span class="comment"># http://localhost:4000</span></span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>GitHub Pages integration – push and it builds automatically, no CI needed</li>
<li>Mature ecosystem: hundreds of themes and plugins</li>
<li>Liquid templates are simple and readable</li>
<li>Great for developer blogs (Markdown + front matter)</li>
<li>Extensive documentation and community answers</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Ruby dependency – <code>gem install</code>, <code>bundler</code>, version management</li>
<li>Slow builds (10x-100x slower than Hugo for large sites)</li>
<li>Development has slowed significantly (fewer updates in recent years)</li>
<li>No built-in asset pipeline (need plugins for Sass, image optimization)</li>
<li>GitHub Pages restricts which plugins you can use (unless you build locally)</li>
</ul>
<p><strong>Best for</strong>: GitHub-hosted blogs and project documentation. Developers who want zero-config deployment via GitHub Pages.</p>
<hr>
<h3 id="Hexo"><a href="#Hexo" class="headerlink" title="Hexo"></a>Hexo</h3><p>A Node.js-based SSG popular in the Asian developer community. Markdown-focused with a plugin ecosystem.</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br><span class="line">hexo init my-blog</span><br><span class="line">hexo server  <span class="comment"># http://localhost:4000</span></span><br></pre></td></tr></table></figure>

<p><strong>Strengths:</strong></p>
<ul>
<li>Node.js ecosystem – familiar tooling for JS developers</li>
<li>Fast (faster than Jekyll, slower than Hugo)</li>
<li>NexT theme is excellent and highly configurable</li>
<li>Good i18n support</li>
<li>Plugin system for search, SEO, feeds, analytics</li>
<li>Simple deploy commands (<code>hexo deploy</code>)</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Smaller community than Hugo&#x2F;Jekyll in the English-speaking world</li>
<li>Fewer themes than Hugo</li>
<li>EJS&#x2F;Nunjucks templates are less modern than component-based alternatives</li>
<li>No built-in image optimization</li>
<li>Plugin quality varies</li>
</ul>
<p><strong>Best for</strong>: Node.js developers who want a simple, Markdown-based blog with good theme support.</p>
<div class="note info"><p><strong>Full disclosure</strong>: This blog runs on Hexo 8.1.1 + NexT 8.27.0, deployed to Cloudflare Pages.</p>
</div>

<hr>
<h3 id="SSG-comparison"><a href="#SSG-comparison" class="headerlink" title="SSG comparison"></a>SSG comparison</h3><table>
<thead>
<tr>
<th>Feature</th>
<th>Hugo</th>
<th>Jekyll</th>
<th>Hexo</th>
<th>Eleventy</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Language</strong></td>
<td>Go</td>
<td>Ruby</td>
<td>Node.js</td>
<td>Node.js</td>
</tr>
<tr>
<td><strong>Build speed (1000 pages)</strong></td>
<td>&lt;1s</td>
<td>~30s</td>
<td>~5s</td>
<td>~2s</td>
</tr>
<tr>
<td><strong>Template engine</strong></td>
<td>Go templates</td>
<td>Liquid</td>
<td>EJS&#x2F;Nunjucks</td>
<td>Any (11 options)</td>
</tr>
<tr>
<td><strong>Install</strong></td>
<td>Single binary</td>
<td><code>gem install</code></td>
<td><code>npm install</code></td>
<td><code>npm install</code></td>
</tr>
<tr>
<td><strong>GitHub Pages native</strong></td>
<td>No</td>
<td>Yes</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td><strong>Component model</strong></td>
<td>No (partials)</td>
<td>No (includes)</td>
<td>No (partials)</td>
<td>No (shortcodes&#x2F;WebC)</td>
</tr>
<tr>
<td><strong>Image optimization</strong></td>
<td>Built-in</td>
<td>Plugin</td>
<td>Plugin</td>
<td>Plugin</td>
</tr>
<tr>
<td><strong>i18n</strong></td>
<td>Built-in</td>
<td>Plugin</td>
<td>Built-in</td>
<td>Manual</td>
</tr>
<tr>
<td><strong>Themes</strong></td>
<td>300+</td>
<td>500+</td>
<td>100+</td>
<td>Starter projects</td>
</tr>
<tr>
<td><strong>Active development</strong></td>
<td>Very active</td>
<td>Slow</td>
<td>Moderate</td>
<td>Active</td>
</tr>
</tbody></table>
<h3 id="When-to-pick-an-SSG-over-Astro"><a href="#When-to-pick-an-SSG-over-Astro" class="headerlink" title="When to pick an SSG over Astro"></a>When to pick an SSG over Astro</h3><ul>
<li><strong>Eleventy</strong>: You want template flexibility without a framework, and don’t need islands</li>
<li><strong>Hugo</strong>: You need sub-second builds for thousands of pages, or you don’t want Node.js</li>
<li><strong>Jekyll</strong>: You want free GitHub Pages hosting with zero CI configuration</li>
<li><strong>Hexo</strong>: You’re in the Node.js ecosystem and want a blog framework with good themes</li>
</ul>
<p><strong>Astro</strong> offers the best DX for new content sites in 2026 (components, TypeScript, content collections, image optimization). But SSGs are battle-tested and have no plans of going away – pick whichever matches your workflow.</p>
<hr>
<h2 id="Deployment-platforms"><a href="#Deployment-platforms" class="headerlink" title="Deployment platforms"></a>Deployment platforms</h2><p>Frameworks produce output. Deployment platforms host it. These are the three major players for frontend deployment – all offer generous free tiers.</p>
<h3 id="Vercel"><a href="#Vercel" class="headerlink" title="Vercel"></a>Vercel</h3><p>The company behind Next.js. Optimized for frontend frameworks with serverless functions at the edge.</p>
<p><strong>Free tier</strong>: 100 GB bandwidth, 100 GB-hours serverless, unlimited static sites, automatic HTTPS</p>
<p><strong>Strengths:</strong></p>
<ul>
<li>Best-in-class Next.js support (unsurprisingly) – zero-config deployment</li>
<li>Preview deployments on every PR (unique URL per commit)</li>
<li>Edge Functions and Serverless Functions built in</li>
<li>Analytics and Web Vitals monitoring</li>
<li>Global CDN with smart caching</li>
<li>Works with any framework (not just Next.js) – Astro, SvelteKit, Nuxt, etc.</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Generous free tier, but costs scale quickly at production traffic</li>
<li>Some Next.js features work best (or only) on Vercel</li>
<li>Vendor lock-in concern for Next.js-specific features (ISR, middleware)</li>
<li>No free plan for team collaboration (Pro required)</li>
</ul>
<p><strong>Best for</strong>: Next.js projects, teams that want the smoothest deploy-preview-ship workflow.</p>
<hr>
<h3 id="Netlify"><a href="#Netlify" class="headerlink" title="Netlify"></a>Netlify</h3><p>The pioneer of the “git push to deploy” workflow. Framework-agnostic with a strong focus on developer experience.</p>
<p><strong>Free tier</strong>: 100 GB bandwidth, 300 build minutes&#x2F;month, 125K serverless invocations, 1 concurrent build</p>
<p><strong>Strengths:</strong></p>
<ul>
<li>Framework-agnostic – works equally well with any SSG or framework</li>
<li>Netlify Functions (serverless), Edge Functions, Forms, Identity (auth)</li>
<li>Split testing (A&#x2F;B) and branch deploys built in</li>
<li><code>netlify.toml</code> for declarative configuration (redirects, headers, build settings)</li>
<li>Large plugin ecosystem</li>
<li>Netlify CMS &#x2F; Decap CMS integration for content editing</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Build times can be slow (1 concurrent build on free tier)</li>
<li>Pricing jumps are steep (free → $19&#x2F;member&#x2F;month)</li>
<li>Some edge cases with framework-specific adapters</li>
<li>Analytics are a paid add-on</li>
</ul>
<p><strong>Best for</strong>: Static sites and JAMstack projects across any framework. Teams that want built-in forms, auth, and serverless without configuring separate services.</p>
<hr>
<h3 id="Cloudflare-Pages"><a href="#Cloudflare-Pages" class="headerlink" title="Cloudflare Pages"></a>Cloudflare Pages</h3><p>Static site hosting on Cloudflare’s global network. Tightly integrated with Cloudflare Workers for serverless compute.</p>
<p><strong>Free tier</strong>: Unlimited bandwidth, 500 builds&#x2F;month, 1 concurrent build, unlimited sites</p>
<p><strong>Strengths:</strong></p>
<ul>
<li><strong>Unlimited bandwidth on free tier</strong> – the most generous free plan</li>
<li>Cloudflare’s global network (300+ cities) – excellent TTFB worldwide</li>
<li>Workers integration for serverless compute (100K requests&#x2F;day free)</li>
<li><code>_headers</code> and <code>_redirects</code> files for configuration</li>
<li>Git integration (GitHub&#x2F;GitLab) or direct upload via Wrangler CLI</li>
<li>KV, D1 (SQLite), R2 (object storage) available alongside Pages</li>
<li>DDoS protection and WAF included</li>
</ul>
<p><strong>Weaknesses:</strong></p>
<ul>
<li>Fewer built-in developer features than Vercel&#x2F;Netlify (no preview comments, no built-in forms)</li>
<li>Framework adapter support is good but sometimes lags behind Vercel</li>
<li>Dashboard UX is less polished for frontend-specific workflows</li>
<li>Build environment is more limited (fewer pre-installed tools)</li>
<li>Worker&#x2F;Pages Functions have different APIs than Node.js (Web Standards-based)</li>
</ul>
<p><strong>Best for</strong>: Static sites that need global performance and unlimited bandwidth. Projects already using Cloudflare for DNS&#x2F;CDN. Cost-conscious teams.</p>
<hr>
<h3 id="Platform-comparison"><a href="#Platform-comparison" class="headerlink" title="Platform comparison"></a>Platform comparison</h3><table>
<thead>
<tr>
<th>Feature</th>
<th>Vercel</th>
<th>Netlify</th>
<th>Cloudflare Pages</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Free bandwidth</strong></td>
<td>100 GB&#x2F;mo</td>
<td>100 GB&#x2F;mo</td>
<td>Unlimited</td>
</tr>
<tr>
<td><strong>Free builds</strong></td>
<td>6,000 min&#x2F;mo</td>
<td>300 min&#x2F;mo</td>
<td>500 builds&#x2F;mo</td>
</tr>
<tr>
<td><strong>Preview deploys</strong></td>
<td>Yes (every commit)</td>
<td>Yes (every commit)</td>
<td>Yes (every commit)</td>
</tr>
<tr>
<td><strong>Serverless</strong></td>
<td>Edge + Serverless</td>
<td>Functions + Edge</td>
<td>Workers (edge)</td>
</tr>
<tr>
<td><strong>Global CDN</strong></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes (300+ cities)</td>
</tr>
<tr>
<td><strong>Custom domains</strong></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><strong>HTTPS</strong></td>
<td>Automatic</td>
<td>Automatic</td>
<td>Automatic</td>
</tr>
<tr>
<td><strong>Framework support</strong></td>
<td>Best for Next.js</td>
<td>Framework-agnostic</td>
<td>Good (adapters)</td>
</tr>
<tr>
<td><strong>Paid tier starts at</strong></td>
<td>$20&#x2F;user&#x2F;mo</td>
<td>$19&#x2F;member&#x2F;mo</td>
<td>$5&#x2F;mo (Workers Paid)</td>
</tr>
<tr>
<td><strong>Best integration</strong></td>
<td>Next.js</td>
<td>JAMstack, CMS</td>
<td>Cloudflare ecosystem</td>
</tr>
</tbody></table>
<h3 id="Which-platform-to-pick"><a href="#Which-platform-to-pick" class="headerlink" title="Which platform to pick"></a>Which platform to pick</h3><ul>
<li><strong>Already using Cloudflare DNS?</strong> → Cloudflare Pages (unified dashboard, unlimited bandwidth)</li>
<li><strong>Building with Next.js?</strong> → Vercel (best Next.js experience)</li>
<li><strong>Need built-in forms, auth, CMS?</strong> → Netlify (batteries included)</li>
<li><strong>Cost-sensitive?</strong> → Cloudflare Pages (unlimited bandwidth free tier)</li>
<li><strong>Don’t care &#x2F; just want it to work?</strong> → Any of the three – they all handle static sites perfectly</li>
</ul>
<div class="note info"><p><strong>You’re not locked in.</strong> All three platforms deploy from a Git repo. Switching between them is usually a 10-minute configuration change, not a rewrite. Pick one, start building, and migrate later if needed.</p>
</div>

<hr>
<h2 id="Head-to-head-common-scenarios"><a href="#Head-to-head-common-scenarios" class="headerlink" title="Head-to-head: common scenarios"></a>Head-to-head: common scenarios</h2><h3 id="“I’m-building-a-portfolio-site”"><a href="#“I’m-building-a-portfolio-site”" class="headerlink" title="“I’m building a portfolio site”"></a>“I’m building a portfolio site”</h3><table>
<thead>
<tr>
<th>Option</th>
<th>Fit</th>
<th>Why</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Astro</strong></td>
<td>Best</td>
<td>Zero JS, content collections for projects&#x2F;albums, image optimization</td>
</tr>
<tr>
<td><strong>HTML + Vite</strong></td>
<td>Great</td>
<td>Maximum control, no abstraction, modern tooling</td>
</tr>
<tr>
<td><strong>Plain HTML&#x2F;CSS</strong></td>
<td>Good</td>
<td>If it’s 3-5 pages with no build step needed</td>
</tr>
<tr>
<td><strong>Hugo</strong></td>
<td>Good</td>
<td>Fast builds, good themes, no Node.js</td>
</tr>
<tr>
<td><strong>Next.js</strong></td>
<td>Overkill</td>
<td>You’d ship React runtime for mostly static pages</td>
</tr>
</tbody></table>
<h3 id="“I’m-building-a-SaaS-dashboard”"><a href="#“I’m-building-a-SaaS-dashboard”" class="headerlink" title="“I’m building a SaaS dashboard”"></a>“I’m building a SaaS dashboard”</h3><table>
<thead>
<tr>
<th>Option</th>
<th>Fit</th>
<th>Why</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Next.js</strong></td>
<td>Best</td>
<td>SSR for auth pages, RSC for data-heavy views, API routes</td>
</tr>
<tr>
<td><strong>Angular</strong></td>
<td>Great</td>
<td>Enterprise-grade, built-in everything, strong typing</td>
</tr>
<tr>
<td><strong>Remix</strong></td>
<td>Great</td>
<td>Web-standards approach, excellent data loading</td>
</tr>
<tr>
<td><strong>SvelteKit</strong></td>
<td>Great</td>
<td>Smallest bundles, fast interactions</td>
</tr>
<tr>
<td><strong>Astro</strong></td>
<td>Poor</td>
<td>Not designed for heavy interactivity</td>
</tr>
</tbody></table>
<h3 id="“I’m-building-an-e-commerce-site”"><a href="#“I’m-building-an-e-commerce-site”" class="headerlink" title="“I’m building an e-commerce site”"></a>“I’m building an e-commerce site”</h3><table>
<thead>
<tr>
<th>Option</th>
<th>Fit</th>
<th>Why</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Next.js</strong></td>
<td>Best</td>
<td>ISR for product pages, SSR for cart&#x2F;checkout, image optimization</td>
</tr>
<tr>
<td><strong>Nuxt</strong></td>
<td>Great</td>
<td>Same capabilities, Vue ecosystem</td>
</tr>
<tr>
<td><strong>Remix</strong></td>
<td>Great</td>
<td>Progressive enhancement means checkout works without JS</td>
</tr>
<tr>
<td><strong>SvelteKit</strong></td>
<td>Good</td>
<td>Fast, but fewer e-commerce integrations</td>
</tr>
</tbody></table>
<h3 id="“I’m-building-a-blog”"><a href="#“I’m-building-a-blog”" class="headerlink" title="“I’m building a blog”"></a>“I’m building a blog”</h3><table>
<thead>
<tr>
<th>Option</th>
<th>Fit</th>
<th>Why</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Astro</strong></td>
<td>Best</td>
<td>Content collections, MDX, zero JS, image optimization</td>
</tr>
<tr>
<td><strong>Hugo</strong></td>
<td>Great</td>
<td>Fastest builds, single binary, good for large blogs</td>
</tr>
<tr>
<td><strong>Eleventy</strong></td>
<td>Great</td>
<td>Simpler, template-based, fast builds</td>
</tr>
<tr>
<td><strong>Hexo</strong></td>
<td>Good</td>
<td>Proven, good themes (NexT), Node.js ecosystem</td>
</tr>
<tr>
<td><strong>Jekyll</strong></td>
<td>Good</td>
<td>Free GitHub Pages hosting, zero CI config</td>
</tr>
<tr>
<td><strong>Next.js</strong></td>
<td>Overkill</td>
<td>Unless your blog has significant interactive features</td>
</tr>
</tbody></table>
<h3 id="“I-just-need-a-landing-page”"><a href="#“I-just-need-a-landing-page”" class="headerlink" title="“I just need a landing page”"></a>“I just need a landing page”</h3><table>
<thead>
<tr>
<th>Option</th>
<th>Fit</th>
<th>Why</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Plain HTML&#x2F;CSS</strong></td>
<td>Best</td>
<td>One page, no dependencies, deploy in seconds</td>
</tr>
<tr>
<td><strong>HTML + Vite</strong></td>
<td>Great</td>
<td>If you want TypeScript or CSS preprocessing</td>
</tr>
<tr>
<td><strong>Astro</strong></td>
<td>Good</td>
<td>If it might grow into a multi-page site</td>
</tr>
<tr>
<td><strong>Any framework</strong></td>
<td>Overkill</td>
<td>Don’t ship 50KB+ of JS for a landing page</td>
</tr>
</tbody></table>
<hr>
<h2 id="Performance-comparison"><a href="#Performance-comparison" class="headerlink" title="Performance comparison"></a>Performance comparison</h2><p>Ballpark figures from Lighthouse testing on a minimal starter project:</p>
<table>
<thead>
<tr>
<th>Option</th>
<th>Lighthouse score</th>
<th>JS bundle (hello world)</th>
<th>Build time (1000 pages)</th>
<th>TTFB (static)</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Plain HTML</strong></td>
<td>100</td>
<td>0 KB</td>
<td>0s (no build)</td>
<td>~20ms</td>
</tr>
<tr>
<td><strong>Hugo</strong></td>
<td>100</td>
<td>0 KB</td>
<td>&lt;1s</td>
<td>~20ms</td>
</tr>
<tr>
<td><strong>Astro</strong></td>
<td>100</td>
<td>0 KB</td>
<td>~3s</td>
<td>~20ms</td>
</tr>
<tr>
<td><strong>Eleventy</strong></td>
<td>100</td>
<td>0 KB</td>
<td>~2s</td>
<td>~20ms</td>
</tr>
<tr>
<td><strong>Jekyll</strong></td>
<td>100</td>
<td>0 KB</td>
<td>~30s</td>
<td>~20ms</td>
</tr>
<tr>
<td><strong>Hexo</strong></td>
<td>100</td>
<td>0 KB</td>
<td>~5s</td>
<td>~20ms</td>
</tr>
<tr>
<td><strong>SvelteKit</strong></td>
<td>95-100</td>
<td>~15 KB</td>
<td>~5s</td>
<td>~30ms</td>
</tr>
<tr>
<td><strong>Remix</strong></td>
<td>90-98</td>
<td>~50 KB</td>
<td>N&#x2F;A (SSR)</td>
<td>~40ms</td>
</tr>
<tr>
<td><strong>Next.js</strong></td>
<td>85-98</td>
<td>~80 KB</td>
<td>~8s</td>
<td>~35ms</td>
</tr>
<tr>
<td><strong>Nuxt</strong></td>
<td>85-95</td>
<td>~70 KB</td>
<td>~7s</td>
<td>~35ms</td>
</tr>
<tr>
<td><strong>React + Vite</strong></td>
<td>80-95</td>
<td>~45 KB</td>
<td>N&#x2F;A (SPA)</td>
<td>~20ms</td>
</tr>
<tr>
<td><strong>Vue + Vite</strong></td>
<td>80-95</td>
<td>~35 KB</td>
<td>N&#x2F;A (SPA)</td>
<td>~20ms</td>
</tr>
<tr>
<td><strong>Angular</strong></td>
<td>80-90</td>
<td>~55 KB</td>
<td>N&#x2F;A (SPA)</td>
<td>~20ms</td>
</tr>
</tbody></table>
<div class="note info"><p>Real-world performance depends heavily on what you build, how you configure it, and your deployment target. Always measure your own app.</p>
</div>

<hr>
<h2 id="Developer-experience"><a href="#Developer-experience" class="headerlink" title="Developer experience"></a>Developer experience</h2><table>
<thead>
<tr>
<th>Framework</th>
<th>TypeScript</th>
<th>Hot reload</th>
<th>IDE support</th>
<th>Docs quality</th>
<th>CLI tooling</th>
</tr>
</thead>
<tbody><tr>
<td><strong>React + Vite</strong></td>
<td>Excellent</td>
<td>Fast (Vite)</td>
<td>Excellent</td>
<td>Good (fragmented)</td>
<td>Good</td>
</tr>
<tr>
<td><strong>Angular</strong></td>
<td>Excellent</td>
<td>Fast (esbuild)</td>
<td>Excellent</td>
<td>Very good</td>
<td>Excellent (ng CLI)</td>
</tr>
<tr>
<td><strong>Next.js</strong></td>
<td>Excellent</td>
<td>Fast (Turbopack)</td>
<td>Excellent</td>
<td>Very good</td>
<td>Excellent</td>
</tr>
<tr>
<td><strong>Vue</strong></td>
<td>Very good</td>
<td>Fast (Vite)</td>
<td>Very good</td>
<td>Excellent</td>
<td>Good</td>
</tr>
<tr>
<td><strong>Nuxt</strong></td>
<td>Very good</td>
<td>Fast (Vite)</td>
<td>Good</td>
<td>Very good</td>
<td>Excellent (nuxi)</td>
</tr>
<tr>
<td><strong>Svelte</strong></td>
<td>Good</td>
<td>Fast (Vite)</td>
<td>Good</td>
<td>Very good</td>
<td>Good (sv)</td>
</tr>
<tr>
<td><strong>SvelteKit</strong></td>
<td>Good</td>
<td>Fast (Vite)</td>
<td>Good</td>
<td>Very good</td>
<td>Good</td>
</tr>
<tr>
<td><strong>Astro</strong></td>
<td>Very good</td>
<td>Fast (Vite)</td>
<td>Good</td>
<td>Excellent</td>
<td>Excellent</td>
</tr>
<tr>
<td><strong>Remix</strong></td>
<td>Excellent</td>
<td>Fast (Vite)</td>
<td>Excellent</td>
<td>Good</td>
<td>Good</td>
</tr>
</tbody></table>
<hr>
<h2 id="Deployment-targets"><a href="#Deployment-targets" class="headerlink" title="Deployment targets"></a>Deployment targets</h2><table>
<thead>
<tr>
<th>Option</th>
<th>Static hosting</th>
<th>Cloudflare Pages</th>
<th>Vercel</th>
<th>Netlify</th>
<th>Node server</th>
<th>Edge runtime</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Plain HTML</strong></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>N&#x2F;A</td>
<td>N&#x2F;A</td>
</tr>
<tr>
<td><strong>HTML + Vite</strong></td>
<td>Yes (build)</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>N&#x2F;A</td>
<td>N&#x2F;A</td>
</tr>
<tr>
<td><strong>React + Vite</strong></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>N&#x2F;A</td>
<td>N&#x2F;A</td>
</tr>
<tr>
<td><strong>Next.js</strong></td>
<td>SSG only</td>
<td>Yes (adapter)</td>
<td>Best</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><strong>Angular</strong></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>N&#x2F;A</td>
<td>N&#x2F;A</td>
</tr>
<tr>
<td><strong>Vue + Vite</strong></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>N&#x2F;A</td>
<td>N&#x2F;A</td>
</tr>
<tr>
<td><strong>Nuxt</strong></td>
<td>SSG + SSR</td>
<td>Yes (Nitro)</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><strong>SvelteKit</strong></td>
<td>SSG + SSR</td>
<td>Yes (adapter)</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><strong>Astro</strong></td>
<td>SSG + SSR</td>
<td>Yes (adapter)</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><strong>Remix</strong></td>
<td>No (SSR)</td>
<td>Yes (adapter)</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><strong>Hugo</strong></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>N&#x2F;A</td>
<td>N&#x2F;A</td>
</tr>
<tr>
<td><strong>Jekyll</strong></td>
<td>Yes</td>
<td>Yes (build)</td>
<td>Yes</td>
<td>Yes</td>
<td>N&#x2F;A</td>
<td>N&#x2F;A</td>
</tr>
<tr>
<td><strong>Hexo</strong></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>N&#x2F;A</td>
<td>N&#x2F;A</td>
</tr>
<tr>
<td><strong>Eleventy</strong></td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>Yes</td>
<td>N&#x2F;A</td>
<td>N&#x2F;A</td>
</tr>
</tbody></table>
<hr>
<h2 id="Decision-flowchart"><a href="#Decision-flowchart" class="headerlink" title="Decision flowchart"></a>Decision flowchart</h2><ol>
<li><p><strong>How many pages?</strong></p>
<ul>
<li>1-3 pages, no blog → <strong>Plain HTML&#x2F;CSS</strong> or <strong>HTML + Vite</strong></li>
<li>More → continue</li>
</ul>
</li>
<li><p><strong>Is it mostly static content?</strong> (blog, portfolio, docs, marketing)</p>
<ul>
<li>Yes, and I want components&#x2F;TypeScript → <strong>Astro</strong></li>
<li>Yes, and I want the fastest builds → <strong>Hugo</strong></li>
<li>Yes, and I want free GitHub Pages hosting → <strong>Jekyll</strong></li>
<li>Yes, and I want maximum simplicity → <strong>Eleventy</strong> or <strong>Hexo</strong></li>
<li>No → continue</li>
</ul>
</li>
<li><p><strong>Do you need server-side rendering?</strong> (SEO, auth, dynamic data)</p>
<ul>
<li>No → Pick your preferred SPA: <strong>React + Vite</strong>, <strong>Vue</strong>, <strong>Svelte</strong>, or <strong>Angular</strong></li>
<li>Yes → continue</li>
</ul>
</li>
<li><p><strong>Which UI library does your team know?</strong></p>
<ul>
<li>React → <strong>Next.js</strong> (full-featured) or <strong>Remix</strong> (web-standards-focused)</li>
<li>Vue → <strong>Nuxt</strong></li>
<li>Svelte → <strong>SvelteKit</strong></li>
<li>No preference → <strong>SvelteKit</strong> (smallest learning surface) or <strong>Next.js</strong> (largest ecosystem)</li>
</ul>
</li>
<li><p><strong>Where to deploy?</strong></p>
<ul>
<li>Unlimited free bandwidth → <strong>Cloudflare Pages</strong></li>
<li>Best Next.js experience → <strong>Vercel</strong></li>
<li>Built-in forms&#x2F;auth&#x2F;CMS → <strong>Netlify</strong></li>
<li>Any static host → All three work fine</li>
</ul>
</li>
</ol>
<hr>
<h2 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h2><p>There is no single best option – only the best option for your project:</p>
<ul>
<li><strong>Plain HTML&#x2F;CSS</strong>: When you need 1-3 pages and zero complexity</li>
<li><strong>HTML + Vite</strong>: When you want modern tooling without a framework</li>
<li><strong>Astro</strong>: When content is king and you want components + TypeScript</li>
<li><strong>Hugo</strong>: When you need the fastest builds for large content sites</li>
<li><strong>Hexo &#x2F; Jekyll</strong>: When you want a proven blog framework with good themes</li>
<li><strong>Next.js</strong>: When you need everything and have React experience</li>
<li><strong>SvelteKit</strong>: When you want the best performance-to-complexity ratio</li>
<li><strong>Remix</strong>: When you care deeply about web standards and progressive enhancement</li>
<li><strong>Nuxt</strong>: When you’re in the Vue ecosystem and need full-stack</li>
<li><strong>Angular</strong>: When your team needs strong conventions and enterprise-grade tooling</li>
<li><strong>React + Vite</strong>: When you’re building a pure client-side interactive app</li>
<li><strong>Vue &#x2F; Svelte</strong>: When you want a simpler SPA alternative to React</li>
</ul>
<p>And for hosting: <strong>Cloudflare Pages</strong> for free bandwidth, <strong>Vercel</strong> for Next.js, <strong>Netlify</strong> for batteries-included features. All three are excellent – you can switch between them in minutes.</p>
<p>Pick based on what you’re building, not what’s trending. A portfolio on plain HTML will always load faster than one on Next.js – not because Next.js is bad, but because simpler tools are better for simpler problems.</p>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>Cloudflare</tag>
        <tag>static-site</tag>
        <tag>Hexo</tag>
        <tag>HTML</tag>
        <tag>Vite</tag>
        <tag>React</tag>
        <tag>Next.js</tag>
        <tag>Vue</tag>
        <tag>Nuxt</tag>
        <tag>Angular</tag>
        <tag>Svelte</tag>
        <tag>SvelteKit</tag>
        <tag>Astro</tag>
        <tag>Remix</tag>
        <tag>Hugo</tag>
        <tag>Jekyll</tag>
        <tag>Eleventy</tag>
        <tag>Vercel</tag>
        <tag>Netlify</tag>
      </tags>
  </entry>
  <entry>
    <title>WWDC 2010 revisited: iPad ships, iOS 4 names the primitives</title>
    <url>/WWDC-2010-Revisited/</url>
    <content><![CDATA[<p>2010 was bracketed by two products. The iPad shipped on April 3 and gave Apple a second iOS device weeks before WWDC had a chance to teach it. iOS 4 shipped on June 21 as the first release where “iPhone OS” was renamed “iOS” to acknowledge a second device on one stack. WWDC 2010 sat between the two releases. The sessions that mattered share a single move: patterns developers had been hand-rolling against <code>touchesBegan:</code>, <code>pthread_create</code>, “is the app foregrounded?” booleans, single-screen iPhone layouts, and folk-wisdom method names finally got named primitives in the SDK to replace them.</p>
<span id="more"></span>

<p>Same substrate-vs-syntax thread that ran through <a href="/WWDC-2008-Revisited/" title="WWDC 2008 revisited: the four sessions still shaping iOS">2008</a> and <a href="/WWDC-2009-Revisited/" title="WWDC 2009 revisited: iPhone OS 3.0 in June, Snow Leopard in August">2009</a>: the contracts shipped in 2010 still describe modern iOS. What changed in 2010 is who held the pen. Up to this point Apple had taught third-party developers by handing them sample code and trusting them to read the pattern. From 2010 on, the patterns came with names, the names came with documented state machines, and the state machines came with delegate hooks that other Apple frameworks were already wired to talk to.</p>
<h2 id="What-the-calendar-looked-like"><a href="#What-the-calendar-looked-like" class="headerlink" title="What the calendar looked like"></a>What the calendar looked like</h2><p>April 3: iPad ships, ten weeks after Steve Jobs announced it. iPhone OS 3.2 is iPad-only; the iPhone is still on 3.1.3. WWDC convenes June 7. iOS 4 ships June 21 for iPhone (the iPad has to wait until November to get it). The platform now has two devices on two release cadences, sharing one SDK that pretends they are one platform. Five sessions explain the next decade.</p>
<h2 id="138-API-design-becomes-a-documented-contract"><a href="#138-API-design-becomes-a-documented-contract" class="headerlink" title="138: API design becomes a documented contract"></a>138: API design becomes a documented contract</h2><p>138 is the meta-talk. It is the year Apple stopped treating naming as taste and started treating it as a documented framework rule. The headline rule is clarity at the call site over brevity at the declaration: <code>removeObjectAtIndex:</code> not <code>rmObj:</code>, <code>tableView(_:cellForRowAt:)</code> not <code>cellAt:</code>. Verbose at the call site is cheap because callers read the line ten times for every time the implementer writes it.</p>
<p>The other rules 138 codified all show up unchanged in the <strong>Swift API Design Guidelines</strong> five years later. No <code>get</code> prefix on getters (<code>get</code> is reserved for buffer-fill semantics like <code>getBytes:length:</code>). Designated initializers chain upward; subclasses must override the one designated initializer of the superclass. Delegation is the composition primitive, not subclassing. Method names omit needless words, name function arguments to read as English phrases, and match the platform’s existing vocabulary (<code>addX:</code>, <code>removeX:</code>, <code>xAtIndex:</code>, <code>xCount</code>) rather than inventing parallel verbs.</p>
<p>Reading 138 back this year, the rule I noticed most was the one on naming the <em>role</em> of an argument rather than its type. The 2010 form is <code>tableView:didSelectRowAtIndexPath:</code>; the role-naming discipline says you write <code>didSelectRowAt indexPath:</code> because “row at index path” is the role and <code>NSIndexPath *</code> is the type. The Swift API Design Guidelines lifted that move directly, and it is the reason modern Swift signatures read aloud in a way that no other mainstream language’s signatures do.</p>
<p>What did not translate cleanly is class clusters. The pattern (abstract public class, private concrete subclass returned from the initializer) is the right shape for <code>NSString</code>&#x2F;<code>NSArray</code>&#x2F;<code>NSNumber</code>, whose Foundation implementations vary by size and contents. Swift’s value types displaced most of the new use cases. You do not author class clusters in Swift; you write value types and let the type checker specialize. The mental model survives in places like SwiftUI’s <code>View</code>-as-protocol (you do not see the concrete type the body returns), but it shows up under a different name now.</p>
<h2 id="105-the-app-lifecycle-gets-four-named-states"><a href="#105-the-app-lifecycle-gets-four-named-states" class="headerlink" title="105: the app lifecycle gets four named states"></a>105: the app lifecycle gets four named states</h2><p>Before iOS 4, an iPhone app was either running or not. When the user hit the Home button the app terminated. There was no “still in memory but not visible” state to live in. Apps that wanted to finish an upload, keep playing audio, or maintain a socket for a VoIP call had nowhere to do it. The third-party API surface for backgrounding was empty.</p>
<p>iOS 4 split “running” into four named states: active, inactive, background, suspended. Each transition got a delegate callback. Background work got a documented surface for finishing in-flight tasks (<code>beginBackgroundTaskWithExpirationHandler:</code>, paired with <code>endBackgroundTask:</code> or the system terminates you when the budget expires) and a documented Info.plist key (<code>UIBackgroundModes</code>) for declaring that the app needs to keep running for a small fixed set of reasons. The launch set was narrow: <code>audio</code> for music apps, <code>location</code> for navigation, a <code>voip</code> socket-maintenance mode for Skype-style calls. Apple added <code>newsstand-content</code> with iOS 5 (it retired alongside Newsstand itself in 2015), <code>fetch</code> and <code>remote-notification</code> with iOS 7, the modern deferrable <code>processing</code> task with iOS 13.</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 2010 backgrounding pattern</span></span><br><span class="line">- (<span class="type">void</span>)applicationDidEnterBackground:(<span class="built_in">UIApplication</span> *)application &#123;</span><br><span class="line">    <span class="keyword">self</span>.uploadTask = [application beginBackgroundTaskWithExpirationHandler:^&#123;</span><br><span class="line">        [application endBackgroundTask:<span class="keyword">self</span>.uploadTask];</span><br><span class="line">        <span class="keyword">self</span>.uploadTask = <span class="built_in">UIBackgroundTaskInvalid</span>;</span><br><span class="line">    &#125;];</span><br><span class="line">    <span class="comment">// finish in-flight upload, then call endBackgroundTask:</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>The state machine survived 16 years intact. iOS 7 added background fetch. iOS 13 split the lifecycle per scene (<code>UISceneDelegate</code>) for multi-window iPad and Mac Catalyst. iOS 13 also added <code>BGTaskScheduler</code>, the modern way to schedule opportunistic background work, which is the spiritual descendant of <code>beginBackgroundTaskWithExpirationHandler:</code>. The four state names did not change. The transitions did not change. The delegate-as-lifecycle-owner shape did not change.</p>
<p>What 2010 could not see is that <code>remote-notification</code> would join the list with iOS 7 and that the silent-push channel under it would become the transport substrate for CloudKit cross-device sync. The launch modes were for apps with their own reasons to keep running. The mode that mattered most by 2020 was for apps that needed to be woken up by Apple’s servers on someone else’s behalf.</p>
<h2 id="206-queues-replace-threads-on-iPhone"><a href="#206-queues-replace-threads-on-iPhone" class="headerlink" title="206: queues replace threads on iPhone"></a>206: queues replace threads on iPhone</h2><p>GCD shipped on the Mac with Snow Leopard in 2009 (the <a href="/WWDC-2009-Revisited/" title="WWDC 2009 revisited: iPhone OS 3.0 in June, Snow Leopard in August">2009 post</a> covers it). iOS 4 was where it crossed over. The 2010 session was essentially “this is now available on the device most of your users have.”</p>
<p>The pitch is the same it has always been: queues, not threads. You express what work needs to happen by handing the runtime a closure; the runtime schedules it onto a system-managed thread pool sized to the hardware. You stop reasoning about thread count and start reasoning about queue confinement. The pre-GCD alternatives (<code>NSThread</code>, <code>pthread_create</code>) forced you to manage thread lifetimes, signaling primitives, the choice of how many threads to spin up, and what to do when one of them stalled.</p>
<figure class="highlight objc"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 2010: nested dispatch_async to hop off-main and back</span></span><br><span class="line"><span class="built_in">dispatch_async</span>(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_DEFAULT, <span class="number">0</span>), ^&#123;</span><br><span class="line">    <span class="built_in">NSData</span> *data = loadFromDisk();</span><br><span class="line">    <span class="built_in">dispatch_async</span>(dispatch_get_main_queue(), ^&#123;</span><br><span class="line">        <span class="keyword">self</span>.label.text = renderText(data);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 2026: the same hops, expressed as a single Task on a MainActor-isolated caller</span></span><br><span class="line"><span class="type">Task</span> &#123; <span class="meta">@MainActor</span> <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">let</span> data <span class="operator">=</span> <span class="keyword">await</span> loadFromDisk()</span><br><span class="line">    label.text <span class="operator">=</span> renderText(data)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>The work the runtime does is identical. The 2026 syntax compresses two <code>dispatch_async</code> calls and an explicit queue selection into a single <code>Task &#123; @MainActor in &#125;</code>, but the closure still gets pulled off a queue, run, and the result still gets handed back across an actor boundary that is, underneath the type system, a queue hop. Swift’s <code>Task</code> and Swift’s actors are the GCD bet with a type system bolted on top.</p>
<p>The one thing 2010 GCD could not anticipate is that <code>DispatchQueue.main.async &#123; ... &#125;</code> would stop satisfying <code>@MainActor</code> in Swift 6. The runtime still hops to main correctly. The compiler cannot prove it does, so the closure inherits the isolation of its declaration site, and the fix is to write <code>Task &#123; @MainActor in ... &#125;</code> or mark the caller <code>nonisolated</code> and dispatch from there. The model is intact. The proof obligations got stricter.</p>
<h2 id="120-touch-events-become-a-state-machine"><a href="#120-touch-events-become-a-state-machine" class="headerlink" title="120: touch events become a state machine"></a>120: touch events become a state machine</h2><p>Before 2010, every iPhone app that wanted to recognize a pinch or a swipe wrote a <code>UIView</code> subclass with overridden <code>touchesBegan:withEvent:</code>, <code>touchesMoved:</code>, <code>touchesEnded:</code>, and <code>touchesCancelled:</code> methods, kept per-finger state in an <code>NSMutableDictionary</code> keyed by <code>UITouch</code> pointer identity, and did its own velocity, distance, duration, and hysteresis math. Gesture detection was bespoke per app, and most apps got it subtly wrong (the double-tap that fires while you are still mid-pan; the pinch that registers as a pan because the second finger arrived 30 ms late).</p>
<p><code>UIGestureRecognizer</code> collapsed all of that into a named state machine you attach to a view. Six built-in subclasses shipped in iOS 4: Tap, Pan, Pinch, Rotation, Swipe, LongPress. States are named (<code>possible → began → changed → ended</code> for continuous gestures; <code>possible → recognized</code> for discrete ones). Composition is a delegate concern: <code>shouldRecognizeSimultaneouslyWith:</code> to layer recognizers on the same view, <code>shouldRequireFailureOf:</code> to disambiguate (double-tap waits for the single-tap recognizer to fail first).</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// UIKit, 2010 pattern, still current</span></span><br><span class="line"><span class="keyword">let</span> pinch <span class="operator">=</span> <span class="type">UIPinchGestureRecognizer</span>(target: <span class="keyword">self</span>, action: <span class="keyword">#selector</span>(handlePinch(<span class="keyword">_</span>:)))</span><br><span class="line">imageView.addGestureRecognizer(pinch)</span><br></pre></td></tr></table></figure>

<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// SwiftUI, 2026</span></span><br><span class="line">imageView</span><br><span class="line">    .scaleEffect(scale)</span><br><span class="line">    .gesture(<span class="type">MagnifyGesture</span>()</span><br><span class="line">        .onChanged &#123; scale <span class="operator">=</span> <span class="variable">$0</span>.magnification &#125;</span><br><span class="line">    )</span><br></pre></td></tr></table></figure>

<p><code>MagnifyGesture</code> (renamed from <code>MagnificationGesture</code> in iOS 17) is <code>UIPinchGestureRecognizer</code> with the action-target plumbing folded into a modifier. <code>DragGesture</code>, <code>RotateGesture</code>, <code>LongPressGesture</code>, and <code>TapGesture</code> follow the same one-to-one mapping. The state machine SwiftUI is running under those names is the 2010 state machine, unmodified.</p>
<p>What got harder, not easier, is <em>composing</em> recognizers. Two recognizers on the same view still need either <code>shouldRecognizeSimultaneouslyWith:</code> returning true or one waiting on the other via <code>shouldRequireFailureOf:</code>. SwiftUI’s <code>.simultaneously(with:)</code> and <code>.exclusively(before:)</code> are the same primitives renamed. The pinch, pan, single-tap, and double-tap combination on a photo viewer is the same multi-recognizer puzzle in both eras, and the puzzle does not get easier when you rename the pieces.</p>
<h2 id="103-iPad-is-a-layout-opportunity-not-a-screen-size"><a href="#103-iPad-is-a-layout-opportunity-not-a-screen-size" class="headerlink" title="103: iPad is a layout opportunity, not a screen size"></a>103: iPad is a layout opportunity, not a screen size</h2><p>iPad shipped ten weeks before WWDC. 103 was Apple’s first formal teaching of how iPad apps should be shaped. The headline design principle was “more content per screen, not bigger UI.” iPad gave you a 1024×768 canvas. The wrong move was to scale the iPhone’s controls 2.3× and call it done. The right move was to show roughly three times more of the same content on one screen by adopting a two-column layout: a master list on the left, the detail of the selected item on the right.</p>
<p><code>UISplitViewController</code> shipped as the SDK primitive for that layout. It hosts a master view controller and a detail view controller, manages the orientation behavior (master collapses behind a button in portrait, both visible in landscape), and exposes a <code>displayModeButtonItem</code> to toggle the master pane. <code>UIPopoverController</code> shipped alongside it as the transient-overlay primitive: anchored at a source view or bar button item, auto-dismissed on outside tap, replacing the modal sheet for sub-decisions like “pick a date” or “choose a font.”</p>
<p>The 2010 design lesson survived; the 2010 implementations did not. <code>UISplitViewController</code> got rewritten in iOS 8 with adaptive presentation, again in iOS 14 with column-based initialization, and SwiftUI’s <code>NavigationSplitView</code> (2022) is the third generation of the same idea. <code>UIPopoverController</code> was deprecated in iOS 9 and replaced with <code>popoverPresentationController</code> on any view controller. The two-column iPad app is still the default shape (Mail, Notes, Settings, Files, every CRUD app in the App Store top 50), but none of those are running 2010 code under the hood.</p>
<p>What 2010 nailed and 16 years still owe it is the principle that screen size is a <em>layout</em> property, not a <em>scale</em> property. The 44pt minimum tap target is the same on iPhone and iPad. Body font is the same. What changes between the two devices is how many things fit. That principle anchors every responsive-layout system that came after it: size classes (2014), trait collections, <code>@Environment(\.horizontalSizeClass)</code> in SwiftUI, the iPadOS multitasking sidebar (2019), and the Stage Manager layout rules (2022) are all corollaries of “scale by adding peer regions, not by zooming.”</p>
<h2 id="What-2010-could-not-see"><a href="#What-2010-could-not-see" class="headerlink" title="What 2010 could not see"></a>What 2010 could not see</h2><p>One bet from 2010 needs caveats in 2026: the assumption that one app meant one foreground window. The state machine in 105 was four states for one process. The lifecycle delegate methods (<code>applicationDidBecomeActive:</code>, <code>applicationDidEnterBackground:</code>) fired once per app, because there was no notion of multiple scenes. iPadOS 13 added multi-window for iPad. Mac Catalyst brought it to the Mac. The scene-based lifecycle has the same four states, but they fire per scene now, and the app-level delegate methods became coarser-grained (they fire when the <em>last</em> scene resigns or when the <em>first</em> scene becomes active).</p>
<p>Apps written against the 2010 single-scene assumption mostly still work, because Apple kept the old delegate methods firing for backwards compatibility, but they handle multi-window awkwardly. A correct multi-window app routes state restoration, deep-link handling, per-scene UI state, and “the user just came back” refresh through the scene delegate, not the app delegate. The 2010 architecture had no language for that, and the iOS 13 retrofit left the app delegate sitting at a level of abstraction that does not quite match either the old single-process model or the new per-scene model.</p>
<p>The other thing 2010 could not anticipate is that the popover chrome would feel wrong on the Mac. iOS 13 Mac Catalyst inherits the iPad layout primitives, and <code>popoverPresentationController</code> does the right thing as an anchored popover on Mac. The iPad-era assumption that “popover equals small floating sheet with an arrow pointing at its source” maps unevenly to a desktop OS where the equivalent transient surface is more often a window-attached sheet without an arrow. The mismatch is small but real, and there is no clean fix that does not break iPad behavior.</p>
<h2 id="If-you-want-to-read-the-originals"><a href="#If-you-want-to-read-the-originals" class="headerlink" title="If you want to read the originals"></a>If you want to read the originals</h2><p>Same caveats as the <a href="/WWDC-2009-Revisited/" title="WWDC 2009 revisited: iPhone OS 3.0 in June, Snow Leopard in August">2009 post</a>: Apple’s developer.apple.com no longer hosts pre-2020 WWDC videos. Session metadata is at the <a href="https://nonstrict.eu/wwdcindex/wwdc2010/">nonstrict.eu WWDC Index</a> and the videos themselves live on the <a href="https://archive.org/search?query=wwdc+2010">Internet Archive</a>.</p>
<p>Five sessions in this reading order if you want the same arc as the post:</p>
<ol>
<li><strong>138</strong> “API Design for Cocoa and Cocoa Touch” for the documented grammar of every Apple framework that came after</li>
<li><strong>105</strong> “Adopting Multitasking on iPhone OS, Part 1” for the four-state lifecycle that became scene phases</li>
<li><strong>206</strong> “Introducing Blocks and Grand Central Dispatch on iPhone” for the concurrency model the modern Swift <code>Task</code> descends from</li>
<li><strong>120</strong> “Simplifying Touch Event Handling with Gesture Recognizers” for the state machine SwiftUI gestures are wrapped around</li>
<li><strong>103</strong> “iPad and iPhone User Interface Design” for the layout grammar that still defines two-column apps</li>
</ol>
<p>About sixty minutes each. The video quality is 480p H.264, the slides date themselves with the iPhone OS 3.x chrome, and the iPad demos run on the original 1024×768 iPad with the home button. The talks themselves do not feel old. Most of what you will recognize is the language Swift still speaks, sixteen years before it learned how to speak it.</p>
]]></content>
      <categories>
        <category>iOS</category>
        <category>WWDC</category>
      </categories>
      <tags>
        <tag>Swift</tag>
        <tag>UIKit</tag>
        <tag>GCD</tag>
        <tag>history</tag>
        <tag>iPad</tag>
        <tag>iOS 4</tag>
        <tag>gesture recognizers</tag>
        <tag>API Design</tag>
      </tags>
  </entry>
  <entry>
    <title>iOS Concurrency and Parallelism Complete Guide</title>
    <url>/iOS-Concurrency-and-Parallelism-Complete-Guide/</url>
    <content><![CDATA[<p>Every concurrency mechanism on Apple platforms, from <code>pthread</code> up to Swift 6.2’s approachable concurrency. Runnable examples and migration paths.</p>
<span id="more"></span>

<h2 id="Quick-comparison"><a href="#Quick-comparison" class="headerlink" title="Quick comparison"></a>Quick comparison</h2><table>
<thead>
<tr>
<th>Mechanism</th>
<th>Introduced</th>
<th>Abstraction level</th>
<th>Cancellation</th>
<th>Priority</th>
<th>Structured</th>
<th>Swift 6 safe</th>
</tr>
</thead>
<tbody><tr>
<td>POSIX Threads (<code>pthread</code>)</td>
<td>Unix (all Apple platforms)</td>
<td>Lowest</td>
<td>Manual</td>
<td>Manual</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td><code>Thread</code> (NSThread)</td>
<td>iOS 2.0 &#x2F; macOS 10.0 (2001)</td>
<td>Low</td>
<td>Manual via <code>cancel()</code></td>
<td>5 levels</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td>GCD (<code>DispatchQueue</code>)</td>
<td>iOS 4.0 &#x2F; macOS 10.6 (2009)</td>
<td>Medium</td>
<td><code>DispatchWorkItem</code></td>
<td>QoS classes</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td><code>OperationQueue</code></td>
<td>iOS 2.0 &#x2F; macOS 10.5 (2007)</td>
<td>Medium-High</td>
<td>Built-in <code>cancel()</code></td>
<td>QoS + dependencies</td>
<td>Partial</td>
<td>No</td>
</tr>
<tr>
<td>Combine</td>
<td>iOS 13 &#x2F; macOS 10.15, Swift 5.1 (2019)</td>
<td>High (reactive)</td>
<td><code>AnyCancellable</code></td>
<td>Scheduler-based</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td><code>async</code>&#x2F;<code>await</code></td>
<td>iOS 13+ (back-deploy) &#x2F; iOS 15 native, Swift 5.5 (2021)</td>
<td>High</td>
<td><code>Task.cancel()</code></td>
<td><code>TaskPriority</code></td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td>Actors</td>
<td>iOS 13+ (back-deploy) &#x2F; iOS 15 native, Swift 5.5 (2021)</td>
<td>High</td>
<td>Via tasks</td>
<td>Inherited</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td><code>AsyncSequence</code> &#x2F; <code>AsyncStream</code></td>
<td>iOS 13+ (back-deploy) &#x2F; iOS 15 native, Swift 5.5 (2021)</td>
<td>High</td>
<td>Task cancellation</td>
<td>Inherited</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td>Swift 6 strict concurrency</td>
<td>6.0 &#x2F; Xcode 16 (2024); 6.1 (Mar 2025); 6.2 approachable (Sept 2025)</td>
<td>Compile-time</td>
<td>N&#x2F;A</td>
<td>N&#x2F;A</td>
<td>Yes</td>
<td>Yes</td>
</tr>
</tbody></table>
<div class="note info"><p>Swift Concurrency (<code>async</code>&#x2F;<code>await</code>, actors, <code>AsyncSequence</code>) shipped natively with iOS 15 &#x2F; macOS 12 (2021), but was <strong>back-deployed to iOS 13 &#x2F; macOS 10.15</strong> starting with Xcode 13.2 (Dec 2021). Some newer APIs like <code>AsyncStream.makeStream</code> (Swift 5.9) and <code>withDiscardingTaskGroup</code> (Swift 5.9) require later minimum deployments.</p>
</div>

<hr>
<h2 id="Mental-model"><a href="#Mental-model" class="headerlink" title="Mental model"></a>Mental model</h2><p>A handful of ideas hold the rest of the chapter together. Worth carrying in your head before reading any specific API: most of the rules below are corollaries of these.</p>
<h3 id="Process-vs-thread-vs-task"><a href="#Process-vs-thread-vs-task" class="headerlink" title="Process vs thread vs task"></a>Process vs thread vs task</h3><p>A process is the OS container: own address space, own file descriptors. Your iOS app is one process; an app extension is a separate process. When the app crashes, iOS restarts it from scratch.</p>
<p>A thread is a unit of execution inside a process. Threads share their process’s memory, and the kernel schedules them onto cores. Creating one costs ~512 KB of stack by default plus a syscall, so you don’t want millions. <code>pthread</code>, <code>NSThread</code>, and GCD workers are all kernel threads dressed in different APIs.</p>
<p>A <code>Task</code> is a Swift Concurrency unit: a heap-allocated state machine the runtime schedules onto threads from the cooperative pool. Cheap (hundreds of bytes), cancellation-aware. Millions of tasks share roughly one-thread-per-core because most are suspended at any instant.</p>
<p>Why the distinction matters when something goes wrong:</p>
<ul>
<li>A crash inside a thread tears down the whole process; iOS relaunches the app cold.</li>
<li>An uncaught error inside a <code>Task</code> ends the task. The actor it called into keeps running, and other tasks on the cooperative pool keep going.</li>
<li>Across processes, nothing is shared. iOS uses XPC for app-to-extension calls, and the App Group container for shared files.</li>
</ul>
<p>The hierarchy also explains the most common Swift Concurrency bug: blocking a thread from inside a task. The task can’t suspend, so it parks the cooperative thread it borrowed. Two of those parked threads are half of a 4-core pool. The runtime can’t tell you anything is wrong; from its view, the worker is busy.</p>
<h3 id="Cooperative-pool-no-blocking"><a href="#Cooperative-pool-no-blocking" class="headerlink" title="Cooperative pool, no blocking"></a>Cooperative pool, no blocking</h3><p>Swift Concurrency’s runtime owns a small pool of threads, capped at roughly <strong>one thread per CPU core, per QoS class</strong>, and its contract is that <strong>every thread is always making progress</strong>. A <code>Task</code> that calls <code>Thread.sleep</code>, <code>DispatchSemaphore.wait</code>, blocks on a <code>pthread_mutex</code> held by a peer, or makes a synchronous I&#x2F;O call into a C library <em>parks the thread</em> it borrowed: the runtime can’t reclaim it, can’t reassign it, and can’t apologise.</p>
<p>The cap is what makes the contract load-bearing. GCD’s failure mode was thread explosion: a queue with ready work and no available worker would create one, easy to push past the kernel’s thread limit on I&#x2F;O-bound workloads. Adding more threads doesn’t help if everyone is parked, so the runtime forces you to <em>suspend</em> (release the thread) instead of <em>block</em> (occupy it). This is also why creating millions of tasks is fine: they share that handful of threads via cheap suspensions, not by spawning kernel threads.</p>
<h3 id="async-is-a-state-machine-not-a-thread"><a href="#async-is-a-state-machine-not-a-thread" class="headerlink" title="async is a state machine, not a thread"></a><code>async</code> is a state machine, not a thread</h3><p>The compiler splits each <code>async</code> function at every <code>await</code> into partial functions. Locals that survive a suspension live in a heap-allocated context; the function “returns” at <code>await</code> and the runtime invokes the next partial function when the awaited result is ready.</p>
<p>Two things follow:</p>
<ul>
<li><strong>Suspension is roughly a function call</strong>: no kernel transition, no stack copy, no thread context switch. <code>await</code> is closer to <code>return</code> than to <code>Thread.sleep</code>.</li>
<li><strong>Stack traces are split.</strong> A debugger paused inside an async function shows the current partial function, not the whole logical call chain. Xcode’s Async Backtrace view stitches the chain back together.</li>
</ul>
<h3 id="Tasks-are-coroutines"><a href="#Tasks-are-coroutines" class="headerlink" title="Tasks are coroutines"></a>Tasks are coroutines</h3><p>If you’ve written <code>async function</code> in JavaScript or <code>suspend fun</code> in Kotlin, Swift’s <code>Task</code> is the same construct those languages call a coroutine. The compiler-split state machine from the previous section is the standard implementation: every <code>await</code> is a continuation point, locals live in a heap-allocated frame, and the function “returns” at suspension while the runtime invokes the next part when the result is ready.</p>
<p>Where Swift sits among coroutine languages:</p>
<ul>
<li><strong>Stackless, like Kotlin or JavaScript.</strong> Go’s goroutines are stackful instead: each owns a small growable stack, and any function can yield without the compiler instrumenting call sites. Stackless gives a cheaper per-task footprint and a predictable cost model. Stackful makes calling into C from inside async free.</li>
<li><strong>Coloured functions.</strong> <code>async</code> is contagious: only callable from another <code>async</code> context. Kotlin’s <code>suspend</code> makes the same choice. Go and Lua avoid colour by being stackful. Colour is the price for not switching stacks on every call.</li>
<li><strong>Structured by default.</strong> A <code>withTaskGroup &#123; &#125;</code> block can’t return until its children finish, and cancellation flows down the tree. Kotlin’s <code>coroutineScope &#123; &#125;</code> is the direct equivalent. <code>go f()</code> in Go is unstructured: you get a goroutine and a hope.</li>
</ul>
<p>The piece still missing, four years in: a real <code>yield</code>. <code>Task.yield()</code> is a <em>hint</em> the runtime is allowed to ignore, and on a busy actor it routinely does. Kotlin’s <code>yield()</code> reliably hands control to the next ready coroutine. Python uses <code>await asyncio.sleep(0)</code>. JavaScript uses <code>await new Promise(setTimeout)</code>. Swift has no equivalent contract, so making a long-running task share the pool fairly with UI work means dropping a <code>try? await Task.sleep(for: .nanoseconds(1))</code> mid-loop.</p>
<p>When you read <code>Task</code> in Swift, mentally substitute “coroutine.” Most concurrency literature outside Swift uses that vocabulary.</p>
<h3 id="Structured-concurrency"><a href="#Structured-concurrency" class="headerlink" title="Structured concurrency"></a>Structured concurrency</h3><p>Every task belongs to a parent. A top-level <code>Task &#123; &#125;</code> is owned by the calling context; children created inside <code>withTaskGroup</code> or via <code>async let</code> are owned by the surrounding function, and the parent can’t return until they finish. That ownership chain is the single design choice the rest of the system spends its complexity on.</p>
<p>Two consequences worth holding onto:</p>
<ul>
<li><strong>Cancellation flows down the tree.</strong> Cancel a parent and every descendant sees <code>Task.isCancelled == true</code> at the next cooperative check: <code>try Task.checkCancellation()</code>, an <code>await</code> on a cancellation-aware API, or the next iteration of an <code>AsyncSequence</code>. You almost never need a manual cancellation flag.</li>
<li><strong>Errors abort siblings.</strong> When one child of a <code>withThrowingTaskGroup</code> throws, the group cancels its other children before re-throwing. The function returns or throws but never strands a worker.</li>
</ul>
<p><code>Task.detached &#123; &#125;</code> is the deliberate escape hatch: no parent, no cancellation propagation, no priority inheritance. Use it when you genuinely want the task to outlive its caller (a fire-and-forget log flush, a background refresh kicked off from a <code>deinit</code>). Most of the time you don’t, and a structured <code>Task &#123; &#125;</code> is the right default.</p>
<h3 id="Executors-decide-where-work-runs"><a href="#Executors-decide-where-work-runs" class="headerlink" title="Executors decide where work runs"></a>Executors decide where work runs</h3><p>Every <code>async</code> function runs on an <em>executor</em>. The ones you actually meet:</p>
<ul>
<li>The <strong>global concurrent executor</strong>: the cooperative pool above. The default for <code>Task &#123; &#125;</code> and <code>Task.detached &#123; &#125;</code>.</li>
<li>Each <strong>actor’s serial executor</strong>: every actor owns one, and it serializes incoming calls so actor state is data-race-free by construction.</li>
<li><strong><code>MainActor</code>‘s executor</strong>: the main run loop. <code>MainActor</code> is a global actor whose executor happens to be the main thread.</li>
</ul>
<p><code>await</code> is the place where the runtime can <em>change executors</em>. When you call an actor method from outside, the <code>await</code> is where you get enqueued on that actor’s serial executor. Most of the design choices later in this chapter (actor reentrancy, <code>Sendable</code>, region-based isolation, priority escalation) are answers to the same question: <em>what guarantees do we need to switch executors safely?</em></p>
<hr>
<p>The rest of this chapter walks each API in roughly the order it shipped. Default to Swift Concurrency in new code; the older systems survive because they predate it and Apple frameworks still hand them out.</p>
<h2 id="1-POSIX-Threads-pthread"><a href="#1-POSIX-Threads-pthread" class="headerlink" title="1. POSIX Threads (pthread)"></a>1. POSIX Threads (<code>pthread</code>)</h2><p>The lowest-level threading API available on Apple platforms. You almost never need this directly, but understanding it helps reason about everything built on top.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> Darwin</span><br><span class="line"></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">posixThreadExample</span>() &#123;</span><br><span class="line">    <span class="keyword">var</span> thread: pthread_t<span class="operator">?</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> result <span class="operator">=</span> pthread_create(<span class="operator">&amp;</span>thread, <span class="literal">nil</span>, &#123; <span class="keyword">_</span> <span class="keyword">in</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;Running on POSIX thread: <span class="subst">\(pthread_self())</span>&quot;</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">    &#125;, <span class="literal">nil</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> result <span class="operator">==</span> <span class="number">0</span>, <span class="keyword">let</span> thread &#123;</span><br><span class="line">        pthread_join(thread, <span class="literal">nil</span>) <span class="comment">// Block until thread finishes</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>When to use</strong>: Almost never. Only for C interop or extreme low-level control (custom thread attributes, real-time scheduling).</p>
<p><strong>Synchronization primitives</strong> (rarely needed directly: Foundation and the standard library wrap them):</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Mutex</span></span><br><span class="line"><span class="keyword">var</span> mutex <span class="operator">=</span> pthread_mutex_t()</span><br><span class="line">pthread_mutex_init(<span class="operator">&amp;</span>mutex, <span class="literal">nil</span>)</span><br><span class="line">pthread_mutex_lock(<span class="operator">&amp;</span>mutex)</span><br><span class="line"><span class="comment">// critical section</span></span><br><span class="line">pthread_mutex_unlock(<span class="operator">&amp;</span>mutex)</span><br><span class="line">pthread_mutex_destroy(<span class="operator">&amp;</span>mutex)</span><br></pre></td></tr></table></figure>

<p><code>pthread</code> also offers <code>pthread_rwlock_*</code> (reader-writer locks) and <code>pthread_cond_*</code> (condition variables). For new code use <code>OSAllocatedUnfairLock</code>, <code>NSCondition</code>, or an <code>actor</code> instead: they wrap the same kernel primitives with safe Swift semantics.</p>
<hr>
<h2 id="2-Thread-NSThread"><a href="#2-Thread-NSThread" class="headerlink" title="2. Thread (NSThread)"></a>2. <code>Thread</code> (NSThread)</h2><p>Objective-C era thread abstraction (iOS 2.0 &#x2F; macOS 10.2, 2002). Slightly higher level than pthreads but still manual.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Subclass approach</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">BackgroundWorker</span>: <span class="title class_ inherited__">Thread</span> &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="keyword">func</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">        <span class="keyword">guard</span> <span class="operator">!</span>isCancelled <span class="keyword">else</span> &#123; <span class="keyword">return</span> &#125;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;Worker running on: <span class="subst">\(Thread.current)</span>&quot;</span>)</span><br><span class="line">        <span class="comment">// Long-running work here</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> worker <span class="operator">=</span> <span class="type">BackgroundWorker</span>()</span><br><span class="line">worker.qualityOfService <span class="operator">=</span> .userInitiated</span><br><span class="line">worker.name <span class="operator">=</span> <span class="string">&quot;com.app.background-worker&quot;</span></span><br><span class="line">worker.start()</span><br><span class="line"></span><br><span class="line"><span class="comment">// Detached thread (fire and forget)</span></span><br><span class="line"><span class="type">Thread</span>.detachNewThread &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Detached thread: <span class="subst">\(Thread.current)</span>&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Perform selector on main thread (Obj-C interop)</span></span><br><span class="line"><span class="comment">// myObject.performSelector(onMainThread: #selector(updateUI), with: nil, waitUntilDone: false)</span></span><br></pre></td></tr></table></figure>

<p><strong>When to use</strong>: Legacy code, run loops that need a dedicated thread (e.g., streaming network connections). Prefer GCD or async&#x2F;await for new code. For per-thread context, Swift Concurrency’s <code>@TaskLocal</code> (§7) is the modern replacement for <code>Thread.current.threadDictionary</code>.</p>
<hr>
<h2 id="3-Grand-Central-Dispatch-GCD"><a href="#3-Grand-Central-Dispatch-GCD" class="headerlink" title="3. Grand Central Dispatch (GCD)"></a>3. Grand Central Dispatch (GCD)</h2><p>Apple’s C-based concurrency library, introduced at WWDC 2009 (iOS 4.0 &#x2F; macOS 10.6 Snow Leopard). Manages a pool of threads automatically: you submit work to queues, GCD decides which thread runs it.</p>
<p>GCD lives in an awkward middle layer today: too low-level for new code (no structured cancellation, no typed return values), too useful to retire (Apple frameworks pass dispatch queues everywhere). Worth knowing in detail because everything around it (Combine schedulers, <code>OperationQueue</code>, the Swift Concurrency interop layer) speaks dispatch.</p>
<h3 id="Serial-vs-concurrent-queues"><a href="#Serial-vs-concurrent-queues" class="headerlink" title="Serial vs concurrent queues"></a>Serial vs concurrent queues</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Serial queue: tasks execute one at a time, in order</span></span><br><span class="line"><span class="keyword">let</span> serial <span class="operator">=</span> <span class="type">DispatchQueue</span>(label: <span class="string">&quot;com.app.serial&quot;</span>)</span><br><span class="line">serial.async &#123; <span class="built_in">print</span>(<span class="string">&quot;Task 1&quot;</span>) &#125;</span><br><span class="line">serial.async &#123; <span class="built_in">print</span>(<span class="string">&quot;Task 2&quot;</span>) &#125; <span class="comment">// Always after Task 1</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Concurrent queue: tasks can run simultaneously</span></span><br><span class="line"><span class="keyword">let</span> concurrent <span class="operator">=</span> <span class="type">DispatchQueue</span>(label: <span class="string">&quot;com.app.concurrent&quot;</span>, attributes: .concurrent)</span><br><span class="line">concurrent.async &#123; <span class="built_in">print</span>(<span class="string">&quot;Task A&quot;</span>) &#125;</span><br><span class="line">concurrent.async &#123; <span class="built_in">print</span>(<span class="string">&quot;Task B&quot;</span>) &#125; <span class="comment">// May run alongside Task A</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Global concurrent queues (shared, system-managed)</span></span><br><span class="line"><span class="type">DispatchQueue</span>.global(qos: .userInteractive).async &#123; <span class="comment">/* Highest priority */</span> &#125;</span><br><span class="line"><span class="type">DispatchQueue</span>.global(qos: .userInitiated).async &#123; <span class="comment">/* User triggered, expects quick result */</span> &#125;</span><br><span class="line"><span class="type">DispatchQueue</span>.global(qos: .default).async &#123; <span class="comment">/* Normal priority */</span> &#125;</span><br><span class="line"><span class="type">DispatchQueue</span>.global(qos: .utility).async &#123; <span class="comment">/* Long tasks, progress bar OK */</span> &#125;</span><br><span class="line"><span class="type">DispatchQueue</span>.global(qos: .background).async &#123; <span class="comment">/* User doesn&#x27;t notice: backups, indexing */</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Main queue: always serial, always main thread</span></span><br><span class="line"><span class="type">DispatchQueue</span>.main.async &#123; <span class="comment">/* UI updates here */</span> &#125;</span><br></pre></td></tr></table></figure>

<h3 id="Sync-vs-async-dispatch"><a href="#Sync-vs-async-dispatch" class="headerlink" title="Sync vs async dispatch"></a>Sync vs async dispatch</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> queue <span class="operator">=</span> <span class="type">DispatchQueue</span>(label: <span class="string">&quot;com.app.sync-demo&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// async: returns immediately, work runs later</span></span><br><span class="line">queue.async &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Async work&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;This prints before async work&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// sync: blocks the calling thread until work completes</span></span><br><span class="line">queue.sync &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Sync work&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;This prints after sync work&quot;</span>)</span><br></pre></td></tr></table></figure>

<div class="note warning"><p>Never call <code>sync</code> on the main queue from the main thread: it deadlocks. Never call <code>sync</code> on a serial queue from that same queue.</p>
</div>

<h3 id="Dispatch-groups"><a href="#Dispatch-groups" class="headerlink" title="Dispatch groups"></a>Dispatch groups</h3><p>Coordinate multiple async tasks and get notified when all finish.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> group <span class="operator">=</span> <span class="type">DispatchGroup</span>()</span><br><span class="line"><span class="keyword">let</span> queue <span class="operator">=</span> <span class="type">DispatchQueue</span>.global(qos: .userInitiated)</span><br><span class="line"></span><br><span class="line">group.enter()</span><br><span class="line">queue.async &#123;</span><br><span class="line">    <span class="comment">// Fetch user profile</span></span><br><span class="line">    fetchProfile &#123; <span class="keyword">_</span> <span class="keyword">in</span> group.leave() &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">group.enter()</span><br><span class="line">queue.async &#123;</span><br><span class="line">    <span class="comment">// Fetch user settings</span></span><br><span class="line">    fetchSettings &#123; <span class="keyword">_</span> <span class="keyword">in</span> group.leave() &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Option 1: Block until all done (don&#x27;t use on main thread)</span></span><br><span class="line">group.wait()</span><br><span class="line"></span><br><span class="line"><span class="comment">// Option 2: Non-blocking notification</span></span><br><span class="line">group.notify(queue: .main) &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Both requests finished: update UI&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Option 3: Timeout</span></span><br><span class="line"><span class="keyword">let</span> result <span class="operator">=</span> group.wait(timeout: .now() <span class="operator">+</span> <span class="number">5</span>)</span><br><span class="line"><span class="keyword">switch</span> result &#123;</span><br><span class="line"><span class="keyword">case</span> .success: <span class="built_in">print</span>(<span class="string">&quot;All done&quot;</span>)</span><br><span class="line"><span class="keyword">case</span> .timedOut: <span class="built_in">print</span>(<span class="string">&quot;Timed out&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Dispatch-barriers"><a href="#Dispatch-barriers" class="headerlink" title="Dispatch barriers"></a>Dispatch barriers</h3><p>Reader-writer lock pattern on concurrent queues.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">ThreadSafeArray</span>&lt;<span class="type">Element</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">var</span> storage: [<span class="type">Element</span>] <span class="operator">=</span> []</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">let</span> queue <span class="operator">=</span> <span class="type">DispatchQueue</span>(label: <span class="string">&quot;com.app.thread-safe-array&quot;</span>, attributes: .concurrent)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">append</span>(<span class="keyword">_</span> <span class="params">element</span>: <span class="type">Element</span>) &#123;</span><br><span class="line">        queue.async(flags: .barrier) &#123; <span class="comment">// Exclusive access: no readers or writers</span></span><br><span class="line">            <span class="keyword">self</span>.storage.append(element)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> elements: [<span class="type">Element</span>] &#123;</span><br><span class="line">        queue.sync &#123; <span class="comment">// Concurrent read: multiple readers OK</span></span><br><span class="line">            storage</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> count: <span class="type">Int</span> &#123;</span><br><span class="line">        queue.sync &#123; storage.count &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Dispatch-semaphores"><a href="#Dispatch-semaphores" class="headerlink" title="Dispatch semaphores"></a>Dispatch semaphores</h3><p>Limit concurrent access to a resource.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Limit to 3 concurrent downloads</span></span><br><span class="line"><span class="keyword">let</span> semaphore <span class="operator">=</span> <span class="type">DispatchSemaphore</span>(value: <span class="number">3</span>)</span><br><span class="line"><span class="keyword">let</span> queue <span class="operator">=</span> <span class="type">DispatchQueue</span>.global(qos: .utility)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> url <span class="keyword">in</span> urls &#123;</span><br><span class="line">    queue.async &#123;</span><br><span class="line">        semaphore.wait()    <span class="comment">// Decrement; blocks if already 0</span></span><br><span class="line">        <span class="keyword">defer</span> &#123; semaphore.signal() &#125; <span class="comment">// Increment when done</span></span><br><span class="line">        downloadFile(from: url)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Binary semaphore (mutex)</span></span><br><span class="line"><span class="keyword">let</span> mutex <span class="operator">=</span> <span class="type">DispatchSemaphore</span>(value: <span class="number">1</span>)</span><br><span class="line">mutex.wait()</span><br><span class="line"><span class="comment">// critical section</span></span><br><span class="line">mutex.signal()</span><br></pre></td></tr></table></figure>

<h3 id="Dispatch-sources"><a href="#Dispatch-sources" class="headerlink" title="Dispatch sources"></a>Dispatch sources</h3><p>Event-driven callbacks from the system.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Timer source</span></span><br><span class="line"><span class="keyword">let</span> timer <span class="operator">=</span> <span class="type">DispatchSource</span>.makeTimerSource(queue: .main)</span><br><span class="line">timer.schedule(deadline: .now(), repeating: .seconds(<span class="number">1</span>))</span><br><span class="line">timer.setEventHandler &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Tick: <span class="subst">\(Date())</span>&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line">timer.resume() <span class="comment">// Don&#x27;t forget: sources start suspended</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// File monitoring</span></span><br><span class="line"><span class="keyword">let</span> fd <span class="operator">=</span> <span class="keyword">open</span>(<span class="string">&quot;/path/to/file&quot;</span>, <span class="type">O_EVTONLY</span>)</span><br><span class="line"><span class="keyword">let</span> fileMonitor <span class="operator">=</span> <span class="type">DispatchSource</span>.makeFileSystemObjectSource(</span><br><span class="line">    fileDescriptor: fd,</span><br><span class="line">    eventMask: [.write, .delete, .rename],</span><br><span class="line">    queue: .main</span><br><span class="line">)</span><br><span class="line">fileMonitor.setEventHandler &#123;</span><br><span class="line">    <span class="keyword">let</span> flags <span class="operator">=</span> fileMonitor.data</span><br><span class="line">    <span class="keyword">if</span> flags.contains(.write) &#123; <span class="built_in">print</span>(<span class="string">&quot;File modified&quot;</span>) &#125;</span><br><span class="line">    <span class="keyword">if</span> flags.contains(.delete) &#123; <span class="built_in">print</span>(<span class="string">&quot;File deleted&quot;</span>) &#125;</span><br><span class="line">&#125;</span><br><span class="line">fileMonitor.setCancelHandler &#123; close(fd) &#125;</span><br><span class="line">fileMonitor.resume()</span><br><span class="line"></span><br><span class="line"><span class="comment">// Memory pressure</span></span><br><span class="line"><span class="keyword">let</span> memorySource <span class="operator">=</span> <span class="type">DispatchSource</span>.makeMemoryPressureSource(eventMask: [.warning, .critical], queue: .main)</span><br><span class="line">memorySource.setEventHandler &#123;</span><br><span class="line">    <span class="keyword">if</span> memorySource.data.contains(.critical) &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;Critical memory pressure: purge caches&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">memorySource.resume()</span><br></pre></td></tr></table></figure>

<h3 id="Work-items-with-cancellation"><a href="#Work-items-with-cancellation" class="headerlink" title="Work items with cancellation"></a>Work items with cancellation</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Two-step pattern: declare first so the closure can capture it</span></span><br><span class="line"><span class="keyword">var</span> workItem: <span class="type">DispatchWorkItem</span>?</span><br><span class="line"></span><br><span class="line">workItem <span class="operator">=</span> <span class="type">DispatchWorkItem</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="number">0</span><span class="operator">..&lt;</span><span class="number">1000</span> &#123;</span><br><span class="line">        <span class="keyword">guard</span> <span class="operator">!</span>(workItem<span class="operator">?</span>.isCancelled <span class="operator">??</span> <span class="literal">true</span>) <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">&quot;Cancelled at iteration <span class="subst">\(i)</span>&quot;</span>)</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// Heavy computation</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">DispatchQueue</span>.global().async(execute: workItem<span class="operator">!</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Cancel after 2 seconds</span></span><br><span class="line"><span class="type">DispatchQueue</span>.main.asyncAfter(deadline: .now() <span class="operator">+</span> <span class="number">2</span>) &#123;</span><br><span class="line">    workItem<span class="operator">?</span>.cancel()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Notification when complete (or cancelled)</span></span><br><span class="line">workItem<span class="operator">?</span>.notify(queue: .main) &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Work item finished, cancelled: <span class="subst">\(workItem<span class="operator">?</span>.isCancelled <span class="operator">??</span> <span class="literal">false</span>)</span>&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<div class="note warning"><p>Do NOT use <code>Thread.current.isCancelled</code> inside a <code>DispatchWorkItem</code>: that checks NSThread cancellation, which is completely unrelated. Always use the work item’s own <code>isCancelled</code> property.</p>
</div>

<h3 id="Dispatch-specific-data-per-queue-context"><a href="#Dispatch-specific-data-per-queue-context" class="headerlink" title="Dispatch-specific data (per-queue context)"></a>Dispatch-specific data (per-queue context)</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> key <span class="operator">=</span> <span class="type">DispatchSpecificKey</span>&lt;<span class="type">String</span>&gt;()</span><br><span class="line"><span class="keyword">let</span> queue <span class="operator">=</span> <span class="type">DispatchQueue</span>(label: <span class="string">&quot;com.app.identified&quot;</span>)</span><br><span class="line">queue.setSpecific(key: key, value: <span class="string">&quot;my-queue&quot;</span>)</span><br><span class="line"></span><br><span class="line">queue.async &#123;</span><br><span class="line">    <span class="keyword">let</span> name <span class="operator">=</span> <span class="type">DispatchQueue</span>.getSpecific(key: key)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Running on: <span class="subst">\(name <span class="operator">??</span> <span class="string">&quot;unknown&quot;</span>)</span>&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<div class="note info"><p><strong>Design note: thread explosion.</strong> GCD’s worker model creates a new thread whenever a queue has ready work and no available worker. Under I&#x2F;O-bound load this can exceed the kernel’s thread limit and crash the process. Swift Concurrency’s cooperative pool (§Mental model) exists primarily to fix this: the same workload becomes thousands of suspended tasks sharing a small pool of threads, not thousands of parked threads.</p>
</div>

<hr>
<h2 id="4-OperationQueue-and-Operation"><a href="#4-OperationQueue-and-Operation" class="headerlink" title="4. OperationQueue and Operation"></a>4. <code>OperationQueue</code> and <code>Operation</code></h2><p>Object-oriented task abstraction (iOS 2.0 &#x2F; macOS 10.5, 2007). Originally built on threads, reimplemented on top of GCD in iOS 4.0 &#x2F; macOS 10.6 (2009) when <code>BlockOperation</code> was also added. Adds dependency graphs, priorities, KVO-observable state, and built-in cancellation.</p>
<p><strong>Why this still exists:</strong> GCD added blocks but dropped Operation’s structured cancellation, KVO state, and dependency graphs. Swift Concurrency replaces both with typed return values, structured cancellation, and compile-time isolation, and is the right answer for new code. <code>OperationQueue</code> survives because Apple frameworks still hand them out, and because explicit dependency graphs (rare) remain easier to read here than in a <code>TaskGroup</code>.</p>
<h3 id="Basic-usage"><a href="#Basic-usage" class="headerlink" title="Basic usage"></a>Basic usage</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> queue <span class="operator">=</span> <span class="type">OperationQueue</span>()</span><br><span class="line">queue.maxConcurrentOperationCount <span class="operator">=</span> <span class="number">3</span> <span class="comment">// Limit parallelism</span></span><br><span class="line">queue.qualityOfService <span class="operator">=</span> .userInitiated</span><br><span class="line"></span><br><span class="line"><span class="comment">// Block operations (simple inline work)</span></span><br><span class="line"><span class="keyword">let</span> op1 <span class="operator">=</span> <span class="type">BlockOperation</span> &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Operation 1: <span class="subst">\(Thread.current)</span>&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> op2 <span class="operator">=</span> <span class="type">BlockOperation</span> &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Operation 2: <span class="subst">\(Thread.current)</span>&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Dependencies: op2 runs after op1</span></span><br><span class="line">op2.addDependency(op1)</span><br><span class="line"></span><br><span class="line">queue.addOperations([op1, op2], waitUntilFinished: <span class="literal">false</span>)</span><br></pre></td></tr></table></figure>

<h3 id="Custom-operations"><a href="#Custom-operations" class="headerlink" title="Custom operations"></a>Custom operations</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">ImageDownloadOperation</span>: <span class="title class_ inherited__">Operation</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> url: <span class="type">URL</span></span><br><span class="line">    <span class="keyword">private(set)</span> <span class="keyword">var</span> image: <span class="type">UIImage</span>?</span><br><span class="line"></span><br><span class="line">    <span class="keyword">init</span>(<span class="params">url</span>: <span class="type">URL</span>) &#123;</span><br><span class="line">        <span class="keyword">self</span>.url <span class="operator">=</span> url</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="keyword">func</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">        <span class="keyword">guard</span> <span class="operator">!</span>isCancelled <span class="keyword">else</span> &#123; <span class="keyword">return</span> &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">guard</span> <span class="keyword">let</span> data <span class="operator">=</span> <span class="keyword">try?</span> <span class="type">Data</span>(contentsOf: url) <span class="keyword">else</span> &#123; <span class="keyword">return</span> &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">guard</span> <span class="operator">!</span>isCancelled <span class="keyword">else</span> &#123; <span class="keyword">return</span> &#125; <span class="comment">// Check again after slow work</span></span><br><span class="line"></span><br><span class="line">        image <span class="operator">=</span> <span class="type">UIImage</span>(data: data)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> downloadOp <span class="operator">=</span> <span class="type">ImageDownloadOperation</span>(url: imageURL)</span><br><span class="line"><span class="keyword">let</span> filterOp <span class="operator">=</span> <span class="type">BlockOperation</span> &#123; [<span class="keyword">weak</span> downloadOp] <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">guard</span> <span class="keyword">let</span> image <span class="operator">=</span> downloadOp<span class="operator">?</span>.image <span class="keyword">else</span> &#123; <span class="keyword">return</span> &#125;</span><br><span class="line">    <span class="comment">// Apply filter to image</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">filterOp.addDependency(downloadOp) <span class="comment">// Filter only runs after download</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> queue <span class="operator">=</span> <span class="type">OperationQueue</span>()</span><br><span class="line">queue.addOperations([downloadOp, filterOp], waitUntilFinished: <span class="literal">false</span>)</span><br></pre></td></tr></table></figure>

<h3 id="Async-operations"><a href="#Async-operations" class="headerlink" title="Async operations"></a>Async operations</h3><p>Wrapping a callback-based API (network, file I&#x2F;O) inside an <code>Operation</code> requires overriding <code>isAsynchronous</code>, manually toggling <code>isExecuting</code>&#x2F;<code>isFinished</code>, and emitting KVO notifications around each change. The pattern is well-documented but obsolete: Swift Concurrency’s <code>withCheckedContinuation</code> (§10) does the same job in three lines and is structured. If you’re hitting this in legacy code, the migration is mechanical: wrap the callback API with a continuation, kick the work off from a <code>Task</code>, and delete the <code>AsyncOperation</code> subclass.</p>
<h3 id="Cancellation-propagation"><a href="#Cancellation-propagation" class="headerlink" title="Cancellation propagation"></a>Cancellation propagation</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> queue <span class="operator">=</span> <span class="type">OperationQueue</span>()</span><br><span class="line"><span class="keyword">let</span> ops <span class="operator">=</span> (<span class="number">0</span><span class="operator">..&lt;</span><span class="number">10</span>).map &#123; i <span class="keyword">in</span></span><br><span class="line">    <span class="type">BlockOperation</span> &#123;</span><br><span class="line">        <span class="type">Thread</span>.sleep(forTimeInterval: <span class="number">0.5</span>)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;Completed operation <span class="subst">\(i)</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Chain dependencies: 0 → 1 → 2 → ... → 9</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="number">1</span><span class="operator">..&lt;</span>ops.count &#123;</span><br><span class="line">    ops[i].addDependency(ops[i <span class="operator">-</span> <span class="number">1</span>])</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">queue.addOperations(ops, waitUntilFinished: <span class="literal">false</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Cancel everything after 2 seconds</span></span><br><span class="line"><span class="type">DispatchQueue</span>.main.asyncAfter(deadline: .now() <span class="operator">+</span> <span class="number">2</span>) &#123;</span><br><span class="line">    queue.cancelAllOperations() <span class="comment">// Sets isCancelled on all pending ops</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="5-Locks-and-synchronization-primitives"><a href="#5-Locks-and-synchronization-primitives" class="headerlink" title="5. Locks and synchronization primitives"></a>5. Locks and synchronization primitives</h2><p>Beyond GCD barriers and semaphores, Foundation and the standard library provide several lock types.</p>
<h3 id="NSLock"><a href="#NSLock" class="headerlink" title="NSLock"></a><code>NSLock</code></h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> lock <span class="operator">=</span> <span class="type">NSLock</span>()</span><br><span class="line"><span class="keyword">var</span> balance <span class="operator">=</span> <span class="number">1000</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">withdraw</span>(<span class="keyword">_</span> <span class="params">amount</span>: <span class="type">Int</span>) -&gt; <span class="type">Bool</span> &#123;</span><br><span class="line">    lock.lock()</span><br><span class="line">    <span class="keyword">defer</span> &#123; lock.unlock() &#125;</span><br><span class="line">    <span class="keyword">guard</span> balance <span class="operator">&gt;=</span> amount <span class="keyword">else</span> &#123; <span class="keyword">return</span> <span class="literal">false</span> &#125;</span><br><span class="line">    balance <span class="operator">-=</span> amount</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="NSRecursiveLock"><a href="#NSRecursiveLock" class="headerlink" title="NSRecursiveLock"></a><code>NSRecursiveLock</code></h3><p>Allows the same thread to acquire the lock multiple times without deadlocking.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> recursiveLock <span class="operator">=</span> <span class="type">NSRecursiveLock</span>()</span><br><span class="line"></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">traverse</span>(<span class="params">node</span>: <span class="type">TreeNode</span>?) &#123;</span><br><span class="line">    recursiveLock.lock()</span><br><span class="line">    <span class="keyword">defer</span> &#123; recursiveLock.unlock() &#125;</span><br><span class="line">    <span class="keyword">guard</span> <span class="keyword">let</span> node <span class="keyword">else</span> &#123; <span class="keyword">return</span> &#125;</span><br><span class="line">    process(node)</span><br><span class="line">    traverse(node: node.left)  <span class="comment">// Re-enters the lock: OK</span></span><br><span class="line">    traverse(node: node.right)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="os-unfair-lock-C-level-fastest"><a href="#os-unfair-lock-C-level-fastest" class="headerlink" title="os_unfair_lock (C-level, fastest)"></a><code>os_unfair_lock</code> (C-level, fastest)</h3><p>The fastest user-space lock on Apple platforms. Cannot be used across processes. Must not be called from Swift directly in a <code>struct</code> (value semantics can copy the lock, causing undefined behavior): use a class wrapper or <code>OSAllocatedUnfairLock</code> (iOS 16+).</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line"><span class="comment">// iOS 16+ / macOS 13+: safe Swift wrapper</span></span><br><span class="line"><span class="keyword">let</span> lock <span class="operator">=</span> <span class="type">OSAllocatedUnfairLock</span>(initialState: <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">lock.withLock &#123; state <span class="keyword">in</span></span><br><span class="line">    state <span class="operator">+=</span> <span class="number">1</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> value <span class="operator">=</span> lock.withLock &#123; state <span class="keyword">in</span></span><br><span class="line">    state</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="NSCondition"><a href="#NSCondition" class="headerlink" title="NSCondition"></a><code>NSCondition</code></h3><p>A combined mutex + condition variable.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> condition <span class="operator">=</span> <span class="type">NSCondition</span>()</span><br><span class="line"><span class="keyword">var</span> dataReady <span class="operator">=</span> <span class="literal">false</span></span><br><span class="line"><span class="keyword">var</span> sharedData: [<span class="type">Int</span>] <span class="operator">=</span> []</span><br><span class="line"></span><br><span class="line"><span class="comment">// Producer</span></span><br><span class="line"><span class="type">Thread</span>.detachNewThread &#123;</span><br><span class="line">    condition.lock()</span><br><span class="line">    sharedData <span class="operator">=</span> [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]</span><br><span class="line">    dataReady <span class="operator">=</span> <span class="literal">true</span></span><br><span class="line">    condition.signal()  <span class="comment">// Wake one waiting thread</span></span><br><span class="line">    condition.unlock()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Consumer</span></span><br><span class="line">condition.lock()</span><br><span class="line"><span class="keyword">while</span> <span class="operator">!</span>dataReady &#123;</span><br><span class="line">    condition.wait()  <span class="comment">// Releases lock, re-acquires on wake</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;Got data: <span class="subst">\(sharedData)</span>&quot;</span>)</span><br><span class="line">condition.unlock()</span><br></pre></td></tr></table></figure>

<h3 id="Atomics-Swift-Atomics-package"><a href="#Atomics-Swift-Atomics-package" class="headerlink" title="Atomics (Swift Atomics package)"></a>Atomics (Swift Atomics package)</h3><p>For lock-free concurrent programming.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> Atomics</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> counter <span class="operator">=</span> <span class="type">ManagedAtomic</span>&lt;<span class="type">Int</span>&gt;(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// From multiple threads:</span></span><br><span class="line">counter.wrappingIncrement(ordering: .relaxed)</span><br><span class="line"><span class="keyword">let</span> value <span class="operator">=</span> counter.load(ordering: .acquiring)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Compare-and-swap</span></span><br><span class="line"><span class="keyword">let</span> (exchanged, original) <span class="operator">=</span> counter.compareExchange(</span><br><span class="line">    expected: <span class="number">5</span>,</span><br><span class="line">    desired: <span class="number">10</span>,</span><br><span class="line">    ordering: .acquiringAndReleasing</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<h3 id="Lock-comparison"><a href="#Lock-comparison" class="headerlink" title="Lock comparison"></a>Lock comparison</h3><table>
<thead>
<tr>
<th>Lock</th>
<th>Reentrant</th>
<th>Speed</th>
<th>Use case</th>
</tr>
</thead>
<tbody><tr>
<td><code>os_unfair_lock</code> &#x2F; <code>OSAllocatedUnfairLock</code></td>
<td>No</td>
<td>Fastest</td>
<td>Hot paths, counters, short critical sections</td>
</tr>
<tr>
<td><code>NSLock</code></td>
<td>No</td>
<td>Fast</td>
<td>General mutex, Obj-C interop</td>
</tr>
<tr>
<td><code>NSRecursiveLock</code></td>
<td>Yes</td>
<td>Moderate</td>
<td>Recursive algorithms</td>
</tr>
<tr>
<td><code>NSCondition</code></td>
<td>No</td>
<td>Moderate</td>
<td>Producer-consumer patterns</td>
</tr>
<tr>
<td><code>DispatchSemaphore</code></td>
<td>No</td>
<td>Fast</td>
<td>Resource limiting, async signaling</td>
</tr>
<tr>
<td>GCD barrier</td>
<td>N&#x2F;A</td>
<td>Fast</td>
<td>Reader-writer on concurrent queues</td>
</tr>
<tr>
<td>Swift Atomics</td>
<td>N&#x2F;A</td>
<td>Lock-free</td>
<td>Counters, flags, CAS loops</td>
</tr>
</tbody></table>
<hr>
<h2 id="6-Combine"><a href="#6-Combine" class="headerlink" title="6. Combine"></a>6. Combine</h2><p>Apple’s reactive framework, introduced at WWDC 2019 (iOS 13 &#x2F; macOS 10.15, Swift 5.1). Declarative chains of publishers and subscribers that handle async events over time.</p>
<div class="note info"><p>For new code, prefer <code>AsyncSequence</code> + the <a href="https://github.com/apple/swift-async-algorithms">Swift Async Algorithms</a> package (§9). Combine remains the right tool for SwiftUI bindings (<code>@Published</code>, <code>ObservableObject</code>) and existing pipelines, but most use cases that aren’t UIKit-bound now have native async equivalents.</p>
</div>

<p>For a closer side-by-side of Combine, RxSwift, and synchronous Swift collection chains, see <a href="/Combine-vs-RxSwift-vs-Swift-Collections/" title="Combine vs RxSwift vs Swift collection chains">Combine vs RxSwift vs Swift collection chains</a>.</p>
<h3 id="Basics"><a href="#Basics" class="headerlink" title="Basics"></a>Basics</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> Combine</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> cancellables <span class="operator">=</span> <span class="type">Set</span>&lt;<span class="type">AnyCancellable</span>&gt;()</span><br><span class="line"></span><br><span class="line"><span class="comment">// Simple publisher pipeline</span></span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>].publisher</span><br><span class="line">    .filter &#123; <span class="variable">$0</span> <span class="operator">%</span> <span class="number">2</span> <span class="operator">==</span> <span class="number">0</span> &#125;</span><br><span class="line">    .map &#123; <span class="variable">$0</span> <span class="operator">*</span> <span class="number">10</span> &#125;</span><br><span class="line">    .sink &#123; <span class="built_in">print</span>(<span class="string">&quot;Value: <span class="subst">\(<span class="variable">$0</span>)</span>&quot;</span>) &#125; <span class="comment">// Value: 20, Value: 40</span></span><br><span class="line">    .store(in: <span class="operator">&amp;</span>cancellables)</span><br></pre></td></tr></table></figure>

<h3 id="Concurrency-with-Combine"><a href="#Concurrency-with-Combine" class="headerlink" title="Concurrency with Combine"></a>Concurrency with Combine</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Background processing with main thread delivery</span></span><br><span class="line"><span class="type">URLSession</span>.shared.dataTaskPublisher(for: url)</span><br><span class="line">    .map(\.data)</span><br><span class="line">    .decode(type: [<span class="type">User</span>].<span class="keyword">self</span>, decoder: <span class="type">JSONDecoder</span>())</span><br><span class="line">    .receive(on: <span class="type">DispatchQueue</span>.main)     <span class="comment">// Switch to main for UI</span></span><br><span class="line">    .sink(</span><br><span class="line">        receiveCompletion: &#123; completion <span class="keyword">in</span></span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">case</span> .failure(<span class="keyword">let</span> error) <span class="operator">=</span> completion &#123;</span><br><span class="line">                <span class="built_in">print</span>(<span class="string">&quot;Error: <span class="subst">\(error)</span>&quot;</span>)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        receiveValue: &#123; users <span class="keyword">in</span></span><br><span class="line">            <span class="comment">// Update UI with users: guaranteed main thread</span></span><br><span class="line">        &#125;</span><br><span class="line">    )</span><br><span class="line">    .store(in: <span class="operator">&amp;</span>cancellables)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Subscribe on a background queue</span></span><br><span class="line">publisher</span><br><span class="line">    .subscribe(on: <span class="type">DispatchQueue</span>.global(qos: .background)) <span class="comment">// Work happens here</span></span><br><span class="line">    .receive(on: <span class="type">DispatchQueue</span>.main) <span class="comment">// Results delivered here</span></span><br><span class="line">    .sink &#123; value <span class="keyword">in</span> <span class="comment">/* UI update */</span> &#125;</span><br><span class="line">    .store(in: <span class="operator">&amp;</span>cancellables)</span><br></pre></td></tr></table></figure>

<h3 id="Parallel-with-MergeMany"><a href="#Parallel-with-MergeMany" class="headerlink" title="Parallel with MergeMany"></a>Parallel with <code>MergeMany</code></h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> urls: [<span class="type">URL</span>] <span class="operator">=</span> [url1, url2, url3]</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> publishers <span class="operator">=</span> urls.map &#123; url <span class="keyword">in</span></span><br><span class="line">    <span class="type">URLSession</span>.shared.dataTaskPublisher(for: url)</span><br><span class="line">        .map(\.data)</span><br><span class="line">        .catch &#123; <span class="keyword">_</span> <span class="keyword">in</span> <span class="type">Empty</span>&lt;<span class="type">Data</span>, <span class="type">Never</span>&gt;() &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">Publishers</span>.<span class="type">MergeMany</span>(publishers)</span><br><span class="line">    .collect()                    <span class="comment">// Wait for all to finish</span></span><br><span class="line">    .receive(on: <span class="type">DispatchQueue</span>.main)</span><br><span class="line">    .sink &#123; allData <span class="keyword">in</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;Got <span class="subst">\(allData.count)</span> responses&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    .store(in: <span class="operator">&amp;</span>cancellables)</span><br></pre></td></tr></table></figure>

<h3 id="Subjects-imperative-push"><a href="#Subjects-imperative-push" class="headerlink" title="Subjects (imperative push)"></a>Subjects (imperative push)</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// PassthroughSubject: no initial value, only emits to current subscribers</span></span><br><span class="line"><span class="keyword">let</span> eventBus <span class="operator">=</span> <span class="type">PassthroughSubject</span>&lt;<span class="type">String</span>, <span class="type">Never</span>&gt;()</span><br><span class="line">eventBus.send(<span class="string">&quot;Hello&quot;</span>) <span class="comment">// Lost if no subscriber</span></span><br><span class="line"></span><br><span class="line">eventBus</span><br><span class="line">    .sink &#123; <span class="built_in">print</span>(<span class="string">&quot;Event: <span class="subst">\(<span class="variable">$0</span>)</span>&quot;</span>) &#125;</span><br><span class="line">    .store(in: <span class="operator">&amp;</span>cancellables)</span><br><span class="line">eventBus.send(<span class="string">&quot;World&quot;</span>) <span class="comment">// Received</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// CurrentValueSubject: has a current value, replays to new subscribers</span></span><br><span class="line"><span class="keyword">let</span> counter <span class="operator">=</span> <span class="type">CurrentValueSubject</span>&lt;<span class="type">Int</span>, <span class="type">Never</span>&gt;(<span class="number">0</span>)</span><br><span class="line">counter.value <span class="comment">// 0</span></span><br><span class="line">counter.send(<span class="number">1</span>)</span><br><span class="line">counter.value <span class="comment">// 1</span></span><br><span class="line"></span><br><span class="line">counter</span><br><span class="line">    .sink &#123; <span class="built_in">print</span>(<span class="string">&quot;Count: <span class="subst">\(<span class="variable">$0</span>)</span>&quot;</span>) &#125; <span class="comment">// Immediately prints &quot;Count: 1&quot;</span></span><br><span class="line">    .store(in: <span class="operator">&amp;</span>cancellables)</span><br></pre></td></tr></table></figure>

<h3 id="Debounce-throttle-and-timing"><a href="#Debounce-throttle-and-timing" class="headerlink" title="Debounce, throttle, and timing"></a>Debounce, throttle, and timing</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> searchText <span class="operator">=</span> <span class="type">PassthroughSubject</span>&lt;<span class="type">String</span>, <span class="type">Never</span>&gt;()</span><br><span class="line"></span><br><span class="line">searchText</span><br><span class="line">    .debounce(for: .milliseconds(<span class="number">300</span>), scheduler: <span class="type">DispatchQueue</span>.main) <span class="comment">// Wait for pause</span></span><br><span class="line">    .removeDuplicates()           <span class="comment">// Skip if same as last</span></span><br><span class="line">    .filter &#123; <span class="operator">!</span><span class="variable">$0</span>.isEmpty &#125;       <span class="comment">// Skip empty strings</span></span><br><span class="line">    .flatMap &#123; query <span class="keyword">in</span>           <span class="comment">// Cancel previous request</span></span><br><span class="line">        searchAPI(query: query)</span><br><span class="line">            .catch &#123; <span class="keyword">_</span> <span class="keyword">in</span> <span class="type">Empty</span>() &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    .receive(on: <span class="type">DispatchQueue</span>.main)</span><br><span class="line">    .sink &#123; results <span class="keyword">in</span> <span class="comment">/* Update UI */</span> &#125;</span><br><span class="line">    .store(in: <span class="operator">&amp;</span>cancellables)</span><br></pre></td></tr></table></figure>

<h3 id="Future-single-value-async"><a href="#Future-single-value-async" class="headerlink" title="Future (single-value async)"></a>Future (single-value async)</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">func</span> <span class="title function_">fetchUser</span>(<span class="params">id</span>: <span class="type">Int</span>) -&gt; <span class="type">Future</span>&lt;<span class="type">User</span>, <span class="type">Error</span>&gt; &#123;</span><br><span class="line">    <span class="type">Future</span> &#123; promise <span class="keyword">in</span></span><br><span class="line">        <span class="type">URLSession</span>.shared.dataTask(with: userURL(id)) &#123; data, <span class="keyword">_</span>, error <span class="keyword">in</span></span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">let</span> error &#123;</span><br><span class="line">                promise(.failure(error))</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> <span class="keyword">let</span> data, <span class="keyword">let</span> user <span class="operator">=</span> <span class="keyword">try?</span> <span class="type">JSONDecoder</span>().decode(<span class="type">User</span>.<span class="keyword">self</span>, from: data) &#123;</span><br><span class="line">                promise(.success(user))</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;.resume()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">fetchUser(id: <span class="number">42</span>)</span><br><span class="line">    .receive(on: <span class="type">DispatchQueue</span>.main)</span><br><span class="line">    .sink(</span><br><span class="line">        receiveCompletion: &#123; <span class="keyword">_</span> <span class="keyword">in</span> &#125;,</span><br><span class="line">        receiveValue: &#123; user <span class="keyword">in</span> <span class="built_in">print</span>(user.name) &#125;</span><br><span class="line">    )</span><br><span class="line">    .store(in: <span class="operator">&amp;</span>cancellables)</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="7-async-await-Swift-Concurrency"><a href="#7-async-await-Swift-Concurrency" class="headerlink" title="7. async&#x2F;await (Swift Concurrency)"></a>7. <code>async</code>&#x2F;<code>await</code> (Swift Concurrency)</h2><p>Introduced in Swift 5.5 at WWDC 2021 (iOS 15 native, back-deployed to iOS 13+ with Xcode 13.2). The modern, recommended approach for all new code.</p>
<p><strong>The shape: lifetime ⊆ lexical scope.</strong> <code>async let</code> and <code>TaskGroup</code> guarantee child tasks complete (or are cancelled) before the enclosing scope exits. Errors propagate <strong>up</strong> the task tree, cancellation propagates <strong>down</strong>, and the compiler refuses code that would leak a child past its parent. <code>Task &#123; &#125;</code> is the deliberate exception: it returns a handle that outlives the surrounding scope, which is also why unstructured tasks are the most common source of “why is this still running?” bugs.</p>
<h3 id="Basic-async-functions"><a href="#Basic-async-functions" class="headerlink" title="Basic async functions"></a>Basic async functions</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">func</span> <span class="title function_">fetchUser</span>(<span class="params">id</span>: <span class="type">Int</span>) <span class="keyword">async</span> <span class="keyword">throws</span> -&gt; <span class="type">User</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> (data, response) <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> <span class="type">URLSession</span>.shared.data(from: userURL(id))</span><br><span class="line">    <span class="keyword">guard</span> <span class="keyword">let</span> http <span class="operator">=</span> response <span class="keyword">as?</span> <span class="type">HTTPURLResponse</span>, http.statusCode <span class="operator">==</span> <span class="number">200</span> <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="type">APIError</span>.invalidResponse</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">try</span> <span class="type">JSONDecoder</span>().decode(<span class="type">User</span>.<span class="keyword">self</span>, from: data)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Calling</span></span><br><span class="line"><span class="type">Task</span> &#123;</span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> user <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> fetchUser(id: <span class="number">42</span>)</span><br><span class="line">        <span class="built_in">print</span>(user.name)</span><br><span class="line">    &#125; <span class="keyword">catch</span> &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;Failed: <span class="subst">\(error)</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Sequential-vs-parallel-execution"><a href="#Sequential-vs-parallel-execution" class="headerlink" title="Sequential vs parallel execution"></a>Sequential vs parallel execution</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Sequential: each awaits before the next starts</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">loadProfile</span>() <span class="keyword">async</span> <span class="keyword">throws</span> -&gt; <span class="type">Profile</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> user <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> fetchUser(id: <span class="number">42</span>)       <span class="comment">// Wait...</span></span><br><span class="line">    <span class="keyword">let</span> avatar <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> fetchImage(user.avatarURL)  <span class="comment">// Then wait...</span></span><br><span class="line">    <span class="keyword">let</span> posts <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> fetchPosts(userId: <span class="number">42</span>)       <span class="comment">// Then wait...</span></span><br><span class="line">    <span class="keyword">return</span> <span class="type">Profile</span>(user: user, avatar: avatar, posts: posts)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Parallel with async let: all three start concurrently</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">loadProfileFast</span>() <span class="keyword">async</span> <span class="keyword">throws</span> -&gt; <span class="type">Profile</span> &#123;</span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">let</span> user <span class="operator">=</span> fetchUser(id: <span class="number">42</span>)</span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">let</span> avatar <span class="operator">=</span> fetchImage(avatarURL)</span><br><span class="line">    <span class="keyword">async</span> <span class="keyword">let</span> posts <span class="operator">=</span> fetchPosts(userId: <span class="number">42</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">try</span> <span class="keyword">await</span> <span class="type">Profile</span>(user: user, avatar: avatar, posts: posts)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<div class="note info"><p><code>async let</code> bindings start executing immediately when declared. The <code>await</code> keyword is where you wait for the result. If you never <code>await</code> an <code>async let</code>, the task is implicitly cancelled when the scope exits.</p>
</div>

<h3 id="Task-groups"><a href="#Task-groups" class="headerlink" title="Task groups"></a>Task groups</h3><p>For dynamic numbers of concurrent tasks.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">func</span> <span class="title function_">fetchAllUsers</span>(<span class="params">ids</span>: [<span class="type">Int</span>]) <span class="keyword">async</span> <span class="keyword">throws</span> -&gt; [<span class="type">User</span>] &#123;</span><br><span class="line">    <span class="keyword">try</span> <span class="keyword">await</span> withThrowingTaskGroup(of: <span class="type">User</span>.<span class="keyword">self</span>) &#123; group <span class="keyword">in</span></span><br><span class="line">        <span class="keyword">for</span> id <span class="keyword">in</span> ids &#123;</span><br><span class="line">            group.addTask &#123;</span><br><span class="line">                <span class="keyword">try</span> <span class="keyword">await</span> fetchUser(id: id)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">var</span> users: [<span class="type">User</span>] <span class="operator">=</span> []</span><br><span class="line">        <span class="keyword">for</span> <span class="keyword">try</span> <span class="keyword">await</span> user <span class="keyword">in</span> group &#123;</span><br><span class="line">            users.append(user)</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> users</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// With limited concurrency (manual sliding-window pattern)</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">fetchAllUsersLimited</span>(<span class="params">ids</span>: [<span class="type">Int</span>]) <span class="keyword">async</span> <span class="keyword">throws</span> -&gt; [<span class="type">User</span>] &#123;</span><br><span class="line">    <span class="keyword">try</span> <span class="keyword">await</span> withThrowingTaskGroup(of: <span class="type">User</span>.<span class="keyword">self</span>) &#123; group <span class="keyword">in</span></span><br><span class="line">        <span class="keyword">var</span> iterator <span class="operator">=</span> ids.makeIterator()</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Start initial batch of 5</span></span><br><span class="line">        <span class="keyword">for</span> <span class="keyword">_</span> <span class="keyword">in</span> <span class="number">0</span><span class="operator">..&lt;</span><span class="number">5</span> &#123;</span><br><span class="line">            <span class="keyword">guard</span> <span class="keyword">let</span> id <span class="operator">=</span> iterator.next() <span class="keyword">else</span> &#123; <span class="keyword">break</span> &#125;</span><br><span class="line">            group.addTask &#123; <span class="keyword">try</span> <span class="keyword">await</span> fetchUser(id: id) &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">var</span> users: [<span class="type">User</span>] <span class="operator">=</span> []</span><br><span class="line">        <span class="keyword">for</span> <span class="keyword">try</span> <span class="keyword">await</span> user <span class="keyword">in</span> group &#123;</span><br><span class="line">            users.append(user)</span><br><span class="line">            <span class="comment">// As each completes, start the next</span></span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">let</span> id <span class="operator">=</span> iterator.next() &#123;</span><br><span class="line">                group.addTask &#123; <span class="keyword">try</span> <span class="keyword">await</span> fetchUser(id: id) &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> users</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Discarding-task-groups-Swift-5-9"><a href="#Discarding-task-groups-Swift-5-9" class="headerlink" title="Discarding task groups (Swift 5.9+)"></a>Discarding task groups (Swift 5.9+)</h3><p>When you don’t need results from individual tasks: just fire-and-forget with structured cancellation.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span> <span class="keyword">await</span> withThrowingDiscardingTaskGroup &#123; group <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">for</span> connection <span class="keyword">in</span> connections &#123;</span><br><span class="line">        group.addTask &#123;</span><br><span class="line">            <span class="keyword">try</span> <span class="keyword">await</span> handleConnection(connection) <span class="comment">// Result is discarded</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// All tasks automatically cancelled if any throws</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Unstructured-tasks"><a href="#Unstructured-tasks" class="headerlink" title="Unstructured tasks"></a>Unstructured tasks</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Inherits actor context and priority</span></span><br><span class="line"><span class="keyword">let</span> task <span class="operator">=</span> <span class="type">Task</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> user <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> fetchUser(id: <span class="number">42</span>)</span><br><span class="line">    <span class="keyword">await</span> updateUI(with: user) <span class="comment">// Runs on the caller&#x27;s actor</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Does NOT inherit context: runs on global executor</span></span><br><span class="line"><span class="keyword">let</span> detached <span class="operator">=</span> <span class="type">Task</span>.detached(priority: .background) &#123;</span><br><span class="line">    <span class="keyword">let</span> data <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> processLargeFile()</span><br><span class="line">    <span class="keyword">return</span> data</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Cancellation</span></span><br><span class="line">task.cancel()</span><br><span class="line"><span class="keyword">let</span> result <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> task.value <span class="comment">// Still need to await (and handle errors)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Check cancellation inside a task</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">processItems</span>(<span class="keyword">_</span> <span class="params">items</span>: [<span class="type">Item</span>]) <span class="keyword">async</span> <span class="keyword">throws</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> item <span class="keyword">in</span> items &#123;</span><br><span class="line">        <span class="keyword">try</span> <span class="type">Task</span>.checkCancellation()  <span class="comment">// Throws CancellationError</span></span><br><span class="line">        <span class="comment">// Or:</span></span><br><span class="line">        <span class="keyword">guard</span> <span class="operator">!</span><span class="type">Task</span>.isCancelled <span class="keyword">else</span> &#123; <span class="keyword">return</span> &#125;</span><br><span class="line">        <span class="keyword">await</span> process(item)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<div class="note info"><p><strong>Design intent: cancellation is cooperative.</strong> <code>task.cancel()</code> only sets a flag. The runtime never kills a task; it relies on the task itself to call <code>Task.checkCancellation()</code> or check <code>Task.isCancelled</code> at safe points. This is the same model as <code>Operation.cancel()</code> and the opposite of <code>pthread_cancel</code> (preemptive at cancellation points). The upside: no broken invariants from a task killed mid-update. The downside: a long synchronous loop with no cancellation check ignores <code>cancel()</code> until it ends. Every long loop needs a check.</p>
</div>

<h3 id="Task-priority-and-priority-escalation"><a href="#Task-priority-and-priority-escalation" class="headerlink" title="Task priority and priority escalation"></a>Task priority and priority escalation</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="type">Task</span>(priority: .high) &#123;</span><br><span class="line">    <span class="comment">// High-priority work</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">Task</span>(priority: .low) &#123;</span><br><span class="line">    <span class="comment">// Low-priority work: may be escalated if a high-priority task awaits it</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Priority escalation happens automatically:</span></span><br><span class="line"><span class="keyword">let</span> lowTask <span class="operator">=</span> <span class="type">Task</span>(priority: .low) &#123; <span class="keyword">await</span> heavyComputation() &#125;</span><br><span class="line"><span class="type">Task</span>(priority: .high) &#123;</span><br><span class="line">    <span class="keyword">let</span> result <span class="operator">=</span> <span class="keyword">await</span> lowTask.value <span class="comment">// lowTask gets escalated to .high</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<div class="note info"><p><strong>Design: escalation, not inversion.</strong> Classic priority inversion (a high-priority task blocked behind a low-priority lock holder) is solved at the OS level by <strong>priority donation</strong>: the lock holder runs at the waiter’s priority for as long as it holds the lock. Swift Concurrency generalises this through <code>await</code>: when a high-priority task awaits a low-priority task, or an actor with pending low-priority callers, Swift escalates the awaited chain to the higher priority for the duration. You won’t usually see “priority inversion” in profilers because the runtime resolves it before it surfaces.</p>
</div>

<h3 id="Task-local-values"><a href="#Task-local-values" class="headerlink" title="Task-local values"></a>Task-local values</h3><p>Thread-local storage equivalent for structured concurrency.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">RequestContext</span> &#123;</span><br><span class="line">    <span class="meta">@TaskLocal</span> <span class="keyword">static</span> <span class="keyword">var</span> requestID: <span class="type">String</span> <span class="operator">=</span> <span class="string">&quot;none&quot;</span></span><br><span class="line">    <span class="meta">@TaskLocal</span> <span class="keyword">static</span> <span class="keyword">var</span> userID: <span class="type">Int</span>?</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">handleRequest</span>() <span class="keyword">async</span> &#123;</span><br><span class="line">    <span class="keyword">await</span> <span class="type">RequestContext</span>.<span class="variable">$requestID</span>.withValue(<span class="string">&quot;req-<span class="subst">\(UUID())</span>&quot;</span>) &#123;</span><br><span class="line">        <span class="keyword">await</span> <span class="type">RequestContext</span>.<span class="variable">$userID</span>.withValue(<span class="number">42</span>) &#123;</span><br><span class="line">            <span class="keyword">await</span> processRequest()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">processRequest</span>() <span class="keyword">async</span> &#123;</span><br><span class="line">    <span class="comment">// Available anywhere in the task tree</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Request: <span class="subst">\(RequestContext.requestID)</span>&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;User: <span class="subst">\(RequestContext.userID <span class="operator">??</span> <span class="operator">-</span><span class="number">1</span>)</span>&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Task-sleep-and-yielding"><a href="#Task-sleep-and-yielding" class="headerlink" title="Task sleep and yielding"></a>Task sleep and yielding</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Sleep (respects cancellation: throws if cancelled)</span></span><br><span class="line"><span class="keyword">try</span> <span class="keyword">await</span> <span class="type">Task</span>.sleep(for: .seconds(<span class="number">1</span>))          <span class="comment">// Swift 5.9+ Duration-based</span></span><br><span class="line"><span class="keyword">try</span> <span class="keyword">await</span> <span class="type">Task</span>.sleep(nanoseconds: <span class="number">1_000_000_000</span>) <span class="comment">// Older API</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Yield (give other tasks a chance to run)</span></span><br><span class="line"><span class="keyword">await</span> <span class="type">Task</span>.yield()</span><br><span class="line"></span><br><span class="line"><span class="comment">// Polling with sleep</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">waitForCondition</span>() <span class="keyword">async</span> <span class="keyword">throws</span> &#123;</span><br><span class="line">    <span class="keyword">while</span> <span class="operator">!</span>isReady &#123;</span><br><span class="line">        <span class="keyword">try</span> <span class="keyword">await</span> <span class="type">Task</span>.sleep(for: .milliseconds(<span class="number">100</span>))</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="8-Actors"><a href="#8-Actors" class="headerlink" title="8. Actors"></a>8. Actors</h2><p>Reference types that protect their mutable state from concurrent access. The compiler enforces isolation: you must <code>await</code> when crossing an actor boundary.</p>
<h3 id="Basic-actor"><a href="#Basic-actor" class="headerlink" title="Basic actor"></a>Basic actor</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">actor</span> <span class="title class_">BankAccount</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> id: <span class="type">String</span></span><br><span class="line">    <span class="keyword">private(set)</span> <span class="keyword">var</span> balance: <span class="type">Decimal</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">init</span>(<span class="params">id</span>: <span class="type">String</span>, <span class="params">balance</span>: <span class="type">Decimal</span>) &#123;</span><br><span class="line">        <span class="keyword">self</span>.id <span class="operator">=</span> id</span><br><span class="line">        <span class="keyword">self</span>.balance <span class="operator">=</span> balance</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">deposit</span>(<span class="keyword">_</span> <span class="params">amount</span>: <span class="type">Decimal</span>) &#123;</span><br><span class="line">        balance <span class="operator">+=</span> amount</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">withdraw</span>(<span class="keyword">_</span> <span class="params">amount</span>: <span class="type">Decimal</span>) <span class="keyword">throws</span> &#123;</span><br><span class="line">        <span class="keyword">guard</span> balance <span class="operator">&gt;=</span> amount <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="type">BankError</span>.insufficientFunds</span><br><span class="line">        &#125;</span><br><span class="line">        balance <span class="operator">-=</span> amount</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// nonisolated: can be called without await (no mutable state access)</span></span><br><span class="line">    <span class="keyword">nonisolated</span> <span class="keyword">var</span> description: <span class="type">String</span> &#123;</span><br><span class="line">        <span class="string">&quot;Account <span class="subst">\(id)</span>&quot;</span> <span class="comment">// Only accesses let property</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Usage: must await</span></span><br><span class="line"><span class="keyword">let</span> account <span class="operator">=</span> <span class="type">BankAccount</span>(id: <span class="string">&quot;001&quot;</span>, balance: <span class="number">1000</span>)</span><br><span class="line"><span class="keyword">await</span> account.deposit(<span class="number">500</span>)</span><br><span class="line"><span class="keyword">let</span> balance <span class="operator">=</span> <span class="keyword">await</span> account.balance</span><br><span class="line"><span class="built_in">print</span>(account.description) <span class="comment">// No await needed: nonisolated</span></span><br></pre></td></tr></table></figure>

<h3 id="Actor-reentrancy"><a href="#Actor-reentrancy" class="headerlink" title="Actor reentrancy"></a>Actor reentrancy</h3><p>Actors are <strong>reentrant</strong>: when an actor suspends (at an <code>await</code>), other callers can execute on it.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">actor</span> <span class="title class_">ImageCache</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">var</span> cache: [<span class="type">URL</span>: <span class="type">UIImage</span>] <span class="operator">=</span> [:]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">image</span>(<span class="params">for</span> <span class="params">url</span>: <span class="type">URL</span>) <span class="keyword">async</span> <span class="keyword">throws</span> -&gt; <span class="type">UIImage</span> &#123;</span><br><span class="line">        <span class="comment">// Check cache BEFORE suspension</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">let</span> cached <span class="operator">=</span> cache[url] &#123;</span><br><span class="line">            <span class="keyword">return</span> cached</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Suspension point: another caller could modify cache here</span></span><br><span class="line">        <span class="keyword">let</span> image <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> downloadImage(from: url)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Check AGAIN after suspension: another call may have cached it</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">let</span> cached <span class="operator">=</span> cache[url] &#123;</span><br><span class="line">            <span class="keyword">return</span> cached</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        cache[url] <span class="operator">=</span> image</span><br><span class="line">        <span class="keyword">return</span> image</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<div class="note info"><p><strong>Design intent: reentrancy avoids deadlock.</strong> A non-reentrant actor would deadlock in any A→B→A call chain: a classic mutex pattern that’s nearly impossible to avoid in practice. Swift’s actors are reentrant by deliberate choice: the runtime can interleave other callers on the actor across each <code>await</code>. The trade is that <strong>invariants don’t survive a suspension</strong>: any state you read before <code>await</code> may have been mutated by another caller by the time you resume. Always re-check conditions after suspension points (the <code>cache[url]</code> re-check above is the canonical pattern).</p>
</div>

<h3 id="MainActor"><a href="#MainActor" class="headerlink" title="@MainActor"></a><code>@MainActor</code></h3><p>A global actor that guarantees execution on the main thread. Essential for UI code.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="meta">@MainActor</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">ProfileViewController</span>: <span class="title class_ inherited__">UIViewController</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">var</span> user: <span class="type">User</span>?</span><br><span class="line"></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">loadUser</span>() <span class="keyword">async</span> &#123;</span><br><span class="line">        <span class="comment">// This runs on the main thread (we&#x27;re @MainActor)</span></span><br><span class="line">        <span class="keyword">let</span> user <span class="operator">=</span> <span class="keyword">try?</span> <span class="keyword">await</span> fetchUser(id: <span class="number">42</span>) <span class="comment">// Suspends, frees main thread</span></span><br><span class="line">        <span class="keyword">self</span>.user <span class="operator">=</span> user <span class="comment">// Back on main thread</span></span><br><span class="line">        tableView.reloadData() <span class="comment">// Safe: main thread</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Annotate individual functions</span></span><br><span class="line"><span class="meta">@MainActor</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">updateUI</span>(<span class="params">with</span> <span class="params">data</span>: <span class="type">Data</span>) &#123;</span><br><span class="line">    label.text <span class="operator">=</span> <span class="type">String</span>(data: data, encoding: .utf8)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Annotate closures</span></span><br><span class="line"><span class="type">Task</span> &#123; <span class="meta">@MainActor</span> <span class="keyword">in</span></span><br><span class="line">    progressView.isHidden <span class="operator">=</span> <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Custom-global-actors"><a href="#Custom-global-actors" class="headerlink" title="Custom global actors"></a>Custom global actors</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="meta">@globalActor</span></span><br><span class="line"><span class="keyword">actor</span> <span class="title class_">DatabaseActor</span> &#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">let</span> shared <span class="operator">=</span> <span class="type">DatabaseActor</span>()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@DatabaseActor</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">UserRepository</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">var</span> cache: [<span class="type">Int</span>: <span class="type">User</span>] <span class="operator">=</span> [:]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">getUser</span>(<span class="params">id</span>: <span class="type">Int</span>) -&gt; <span class="type">User</span>? &#123;</span><br><span class="line">        cache[id]</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">save</span>(<span class="keyword">_</span> <span class="params">user</span>: <span class="type">User</span>) &#123;</span><br><span class="line">        cache[user.id] <span class="operator">=</span> user</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// All methods on UserRepository are isolated to DatabaseActor</span></span><br><span class="line"><span class="comment">// Must await from outside:</span></span><br><span class="line"><span class="keyword">let</span> repo <span class="operator">=</span> <span class="type">UserRepository</span>()</span><br><span class="line"><span class="keyword">let</span> user <span class="operator">=</span> <span class="keyword">await</span> repo.getUser(id: <span class="number">42</span>)</span><br></pre></td></tr></table></figure>

<h3 id="Actor-isolated-properties-and-Sendable"><a href="#Actor-isolated-properties-and-Sendable" class="headerlink" title="Actor-isolated properties and Sendable"></a>Actor-isolated properties and <code>Sendable</code></h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Sendable: safe to pass across actor boundaries</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">UserDTO</span>: <span class="title class_ inherited__">Sendable</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> id: <span class="type">Int</span></span><br><span class="line">    <span class="keyword">let</span> name: <span class="type">String</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Not Sendable: has mutable reference state</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MutableState</span> &#123;</span><br><span class="line">    <span class="keyword">var</span> count <span class="operator">=</span> <span class="number">0</span> <span class="comment">// Compiler warns if sent across actors</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Manually mark as @unchecked Sendable (you&#x27;re responsible for thread safety)</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">ThreadSafeCounter</span>: @unchecked <span class="title class_ inherited__">Sendable</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">let</span> lock <span class="operator">=</span> <span class="type">OSAllocatedUnfairLock</span>(initialState: <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">increment</span>() &#123;</span><br><span class="line">        lock.withLock &#123; <span class="variable">$0</span> <span class="operator">+=</span> <span class="number">1</span> &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<div class="note info"><p><strong>Design: <code>Sendable</code> is a typing discipline, not a runtime check.</strong> It’s a marker protocol: the compiler reasons about it at compile time and emits zero runtime overhead. A class is implicitly <code>Sendable</code> only if it’s <code>final</code> <em>and</em> every stored property is immutable and Sendable, because that’s the only structural form Swift can verify safe without help. <code>@unchecked Sendable</code> is the explicit “I’ve taken a lock, trust me” escape hatch. The discipline was deliberately strict at first, then loosened in Swift 6 by region-based isolation (see §11) when the strictness turned out to make a lot of plainly-safe code uncompilable.</p>
</div>

<hr>
<h2 id="9-AsyncSequence-and-AsyncStream"><a href="#9-AsyncSequence-and-AsyncStream" class="headerlink" title="9. AsyncSequence and AsyncStream"></a>9. <code>AsyncSequence</code> and <code>AsyncStream</code></h2><p>Async equivalents of <code>Sequence</code>. Values arrive over time, and iteration suspends between elements.</p>
<h3 id="Built-in-AsyncSequences"><a href="#Built-in-AsyncSequences" class="headerlink" title="Built-in AsyncSequences"></a>Built-in AsyncSequences</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// URL bytes</span></span><br><span class="line"><span class="keyword">let</span> (bytes, <span class="keyword">_</span>) <span class="operator">=</span> <span class="keyword">try</span> <span class="keyword">await</span> <span class="type">URLSession</span>.shared.bytes(from: url)</span><br><span class="line"><span class="keyword">for</span> <span class="keyword">try</span> <span class="keyword">await</span> byte <span class="keyword">in</span> bytes &#123;</span><br><span class="line">    process(byte)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// File lines</span></span><br><span class="line"><span class="keyword">let</span> fileURL <span class="operator">=</span> <span class="type">URL</span>(filePath: <span class="string">&quot;/path/to/file.txt&quot;</span>)</span><br><span class="line"><span class="keyword">for</span> <span class="keyword">try</span> <span class="keyword">await</span> line <span class="keyword">in</span> fileURL.lines &#123;</span><br><span class="line">    <span class="built_in">print</span>(line)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Notifications</span></span><br><span class="line"><span class="keyword">let</span> notifications <span class="operator">=</span> <span class="type">NotificationCenter</span>.default.notifications(named: .<span class="type">NSManagedObjectContextDidSave</span>)</span><br><span class="line"><span class="keyword">for</span> <span class="keyword">await</span> notification <span class="keyword">in</span> notifications &#123;</span><br><span class="line">    handleSyncChange(notification)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="AsyncStream-custom-producer"><a href="#AsyncStream-custom-producer" class="headerlink" title="AsyncStream (custom producer)"></a><code>AsyncStream</code> (custom producer)</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Continuation-based (push model)</span></span><br><span class="line"><span class="keyword">let</span> heartbeats <span class="operator">=</span> <span class="type">AsyncStream</span>&lt;<span class="type">Date</span>&gt; &#123; continuation <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">let</span> timer <span class="operator">=</span> <span class="type">Timer</span>.scheduledTimer(withTimeInterval: <span class="number">1.0</span>, repeats: <span class="literal">true</span>) &#123; <span class="keyword">_</span> <span class="keyword">in</span></span><br><span class="line">        continuation.yield(<span class="type">Date</span>())</span><br><span class="line">    &#125;</span><br><span class="line">    continuation.onTermination <span class="operator">=</span> &#123; <span class="keyword">_</span> <span class="keyword">in</span></span><br><span class="line">        timer.invalidate()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> <span class="keyword">await</span> beat <span class="keyword">in</span> heartbeats &#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Heartbeat: <span class="subst">\(beat)</span>&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// With buffering policy</span></span><br><span class="line"><span class="keyword">let</span> buffered <span class="operator">=</span> <span class="type">AsyncStream</span>&lt;<span class="type">Int</span>&gt;(bufferingPolicy: .bufferingNewest(<span class="number">5</span>)) &#123; continuation <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="number">0</span><span class="operator">..&lt;</span><span class="number">100</span> &#123;</span><br><span class="line">        continuation.yield(i) <span class="comment">// Only keeps newest 5 if consumer is slow</span></span><br><span class="line">    &#125;</span><br><span class="line">    continuation.finish()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// AsyncThrowingStream: can produce errors</span></span><br><span class="line"><span class="keyword">let</span> dataStream <span class="operator">=</span> <span class="type">AsyncThrowingStream</span>&lt;<span class="type">Data</span>, <span class="type">Error</span>&gt; &#123; continuation <span class="keyword">in</span></span><br><span class="line">    startMonitoring &#123; result <span class="keyword">in</span></span><br><span class="line">        <span class="keyword">switch</span> result &#123;</span><br><span class="line">        <span class="keyword">case</span> .success(<span class="keyword">let</span> data):</span><br><span class="line">            continuation.yield(data)</span><br><span class="line">        <span class="keyword">case</span> .failure(<span class="keyword">let</span> error):</span><br><span class="line">            continuation.finish(throwing: error)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="AsyncStream-makeStream-Swift-5-9"><a href="#AsyncStream-makeStream-Swift-5-9" class="headerlink" title="AsyncStream.makeStream (Swift 5.9+)"></a><code>AsyncStream.makeStream</code> (Swift 5.9+)</h3><p>Returns a tuple of stream + continuation for when the producer and consumer are set up in different scopes.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> (stream, continuation) <span class="operator">=</span> <span class="type">AsyncStream</span>.makeStream(of: <span class="type">String</span>.<span class="keyword">self</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Producer (e.g., delegate callback)</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">locationManager</span>(<span class="keyword">_</span> <span class="params">manager</span>: <span class="type">CLLocationManager</span>, <span class="params">didUpdateLocations</span> <span class="params">locations</span>: [<span class="type">CLLocation</span>]) &#123;</span><br><span class="line">    <span class="keyword">for</span> location <span class="keyword">in</span> locations &#123;</span><br><span class="line">        continuation.yield(location.description)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Consumer</span></span><br><span class="line"><span class="type">Task</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> <span class="keyword">await</span> locationString <span class="keyword">in</span> stream &#123;</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;Location: <span class="subst">\(locationString)</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Async-algorithms-Swift-Async-Algorithms-package"><a href="#Async-algorithms-Swift-Async-Algorithms-package" class="headerlink" title="Async algorithms (Swift Async Algorithms package)"></a>Async algorithms (Swift Async Algorithms package)</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> AsyncAlgorithms</span><br><span class="line"></span><br><span class="line"><span class="comment">// Merge multiple sequences</span></span><br><span class="line"><span class="keyword">let</span> merged <span class="operator">=</span> merge(notificationsStream, timerStream)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Debounce</span></span><br><span class="line"><span class="keyword">let</span> debounced <span class="operator">=</span> searchTextStream.debounce(for: .milliseconds(<span class="number">300</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment">// Throttle</span></span><br><span class="line"><span class="keyword">let</span> throttled <span class="operator">=</span> sensorStream.throttle(for: .seconds(<span class="number">1</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment">// Combine latest</span></span><br><span class="line"><span class="keyword">let</span> combined <span class="operator">=</span> combineLatest(locationStream, headingStream)</span><br><span class="line"><span class="keyword">for</span> <span class="keyword">await</span> (location, heading) <span class="keyword">in</span> combined &#123;</span><br><span class="line">    updateMap(location: location, heading: heading)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Zip (pairs elements 1:1)</span></span><br><span class="line"><span class="keyword">let</span> zipped <span class="operator">=</span> <span class="built_in">zip</span>(requestStream, responseStream)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Chain (sequential concatenation)</span></span><br><span class="line"><span class="keyword">let</span> chained <span class="operator">=</span> chain(cachedResults.async, networkResults)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Chunked</span></span><br><span class="line"><span class="keyword">let</span> batched <span class="operator">=</span> dataPoints.chunks(ofCount: <span class="number">10</span>)</span><br><span class="line"><span class="keyword">for</span> <span class="keyword">await</span> batch <span class="keyword">in</span> batched &#123;</span><br><span class="line">    <span class="keyword">await</span> uploadBatch(<span class="type">Array</span>(batch))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="10-Continuations-bridging-callback-→-async"><a href="#10-Continuations-bridging-callback-→-async" class="headerlink" title="10. Continuations (bridging callback → async)"></a>10. Continuations (bridging callback → async)</h2><p>Convert callback-based APIs to async&#x2F;await.</p>
<h3 id="withCheckedContinuation"><a href="#withCheckedContinuation" class="headerlink" title="withCheckedContinuation"></a><code>withCheckedContinuation</code></h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">func</span> <span class="title function_">fetchLocation</span>() <span class="keyword">async</span> -&gt; <span class="type">CLLocation</span> &#123;</span><br><span class="line">    <span class="keyword">await</span> withCheckedContinuation &#123; continuation <span class="keyword">in</span></span><br><span class="line">        locationManager.requestLocation &#123; location <span class="keyword">in</span></span><br><span class="line">            continuation.resume(returning: location)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="withCheckedThrowingContinuation"><a href="#withCheckedThrowingContinuation" class="headerlink" title="withCheckedThrowingContinuation"></a><code>withCheckedThrowingContinuation</code></h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">func</span> <span class="title function_">fetchData</span>(<span class="params">from</span> <span class="params">url</span>: <span class="type">URL</span>) <span class="keyword">async</span> <span class="keyword">throws</span> -&gt; <span class="type">Data</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> <span class="keyword">await</span> withCheckedThrowingContinuation &#123; continuation <span class="keyword">in</span></span><br><span class="line">        <span class="type">URLSession</span>.shared.dataTask(with: url) &#123; data, <span class="keyword">_</span>, error <span class="keyword">in</span></span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">let</span> error &#123;</span><br><span class="line">                continuation.resume(throwing: error)</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> <span class="keyword">let</span> data &#123;</span><br><span class="line">                continuation.resume(returning: data)</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                continuation.resume(throwing: <span class="type">URLError</span>(.unknown))</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;.resume()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<div class="note warning"><p>A continuation must be resumed <strong>exactly once</strong>. Resuming zero times leaks the task. Resuming more than once is undefined behavior. <code>withCheckedContinuation</code> traps on double-resume in debug builds; <code>withUnsafeContinuation</code> does not check (faster but dangerous).</p>
</div>

<h3 id="Delegate-pattern-bridging"><a href="#Delegate-pattern-bridging" class="headerlink" title="Delegate pattern bridging"></a>Delegate pattern bridging</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">LocationBridge</span>: <span class="title class_ inherited__">NSObject</span>, <span class="title class_ inherited__">CLLocationManagerDelegate</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">var</span> continuation: <span class="type">CheckedContinuation</span>&lt;<span class="type">CLLocation</span>, <span class="type">Error</span>&gt;?</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">let</span> manager <span class="operator">=</span> <span class="type">CLLocationManager</span>()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="keyword">init</span>() &#123;</span><br><span class="line">        <span class="keyword">super</span>.<span class="keyword">init</span>()</span><br><span class="line">        manager.delegate <span class="operator">=</span> <span class="keyword">self</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">currentLocation</span>() <span class="keyword">async</span> <span class="keyword">throws</span> -&gt; <span class="type">CLLocation</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> <span class="keyword">await</span> withCheckedThrowingContinuation &#123; continuation <span class="keyword">in</span></span><br><span class="line">            <span class="keyword">self</span>.continuation <span class="operator">=</span> continuation</span><br><span class="line">            manager.requestLocation()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">locationManager</span>(<span class="keyword">_</span> <span class="params">manager</span>: <span class="type">CLLocationManager</span>, <span class="params">didUpdateLocations</span> <span class="params">locations</span>: [<span class="type">CLLocation</span>]) &#123;</span><br><span class="line">        continuation<span class="operator">?</span>.resume(returning: locations[<span class="number">0</span>])</span><br><span class="line">        continuation <span class="operator">=</span> <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">locationManager</span>(<span class="keyword">_</span> <span class="params">manager</span>: <span class="type">CLLocationManager</span>, <span class="params">didFailWithError</span> <span class="params">error</span>: <span class="type">Error</span>) &#123;</span><br><span class="line">        continuation<span class="operator">?</span>.resume(throwing: error)</span><br><span class="line">        continuation <span class="operator">=</span> <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="11-Swift-6-strict-concurrency"><a href="#11-Swift-6-strict-concurrency" class="headerlink" title="11. Swift 6 strict concurrency"></a>11. Swift 6 strict concurrency</h2><p>Swift 6.0 (Xcode 16, Sep 2024) makes data-race safety a compile-time guarantee. Code that was valid in Swift 5 may produce errors in Swift 6 strict mode.</p>
<h3 id="Enabling-strict-concurrency"><a href="#Enabling-strict-concurrency" class="headerlink" title="Enabling strict concurrency"></a>Enabling strict concurrency</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Package.swift</span></span><br><span class="line">.target(</span><br><span class="line">    name: <span class="string">&quot;MyTarget&quot;</span>,</span><br><span class="line">    swiftSettings: [</span><br><span class="line">        .swiftLanguageMode(.v6),</span><br><span class="line">    ]</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Or per-target in Xcode:</span></span><br><span class="line"><span class="comment">// Build Settings → Swift Language Version → 6</span></span><br></pre></td></tr></table></figure>

<h3 id="Sendable-enforcement"><a href="#Sendable-enforcement" class="headerlink" title="Sendable enforcement"></a><code>Sendable</code> enforcement</h3><p>In Swift 6, the compiler checks that values crossing isolation boundaries are <code>Sendable</code>.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Automatically Sendable: structs/enums with all Sendable stored properties</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Point</span>: <span class="title class_ inherited__">Sendable</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> x: <span class="type">Double</span></span><br><span class="line">    <span class="keyword">let</span> y: <span class="type">Double</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Classes must be final + immutable to be implicitly Sendable</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">Config</span>: <span class="title class_ inherited__">Sendable</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> apiKey: <span class="type">String</span></span><br><span class="line">    <span class="keyword">let</span> timeout: <span class="type">Int</span></span><br><span class="line">    <span class="keyword">init</span>(<span class="params">apiKey</span>: <span class="type">String</span>, <span class="params">timeout</span>: <span class="type">Int</span>) &#123;</span><br><span class="line">        <span class="keyword">self</span>.apiKey <span class="operator">=</span> apiKey</span><br><span class="line">        <span class="keyword">self</span>.timeout <span class="operator">=</span> timeout</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// @Sendable closures: no mutable captures</span></span><br><span class="line"><span class="keyword">let</span> task <span class="operator">=</span> <span class="type">Task</span> &#123; <span class="keyword">@Sendable</span> <span class="keyword">in</span></span><br><span class="line">    <span class="comment">// Cannot capture mutable local variables</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// @Sendable function types</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">transform</span>&lt;<span class="type">T</span>: <span class="type">Sendable</span>&gt;(<span class="keyword">_</span> <span class="params">items</span>: [<span class="type">T</span>], <span class="params">using</span> <span class="params">block</span>: <span class="keyword">@Sendable</span> (<span class="type">T</span>) -&gt; <span class="type">T</span>) -&gt; [<span class="type">T</span>] &#123;</span><br><span class="line">    items.map(block)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Region-based-isolation-Swift-6-0"><a href="#Region-based-isolation-Swift-6-0" class="headerlink" title="Region-based isolation (Swift 6.0)"></a>Region-based isolation (Swift 6.0)</h3><p><strong>Why it exists:</strong> before SE-0414, you couldn’t pass a freshly-built non-Sendable value across an actor boundary, even when no other code could reach it. A <code>[User]</code> you just constructed and never aliased was <em>plainly safe</em> to transfer, but the compiler had no way to know: <code>Sendable</code> is a per-type discipline, but aliasing is a per-value question.</p>
<p>Region-based isolation tracks <strong>isolation regions</strong>: a value with no aliases is in its own region and can be transferred. <code>sending</code> declares “this parameter (or return) is in a disconnected region: the callee&#x2F;caller may take ownership.” The compiler tracks which “region” a value belongs to, and values in disconnected regions can be sent across isolation boundaries even if not <code>Sendable</code>.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// This works in Swift 6 because `array` is in a disconnected region</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">makeArray</span>() -&gt; sending [<span class="type">String</span>] &#123;</span><br><span class="line">    <span class="keyword">var</span> array <span class="operator">=</span> [<span class="string">&quot;hello&quot;</span>]</span><br><span class="line">    array.append(<span class="string">&quot;world&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> array <span class="comment">// Transferred to the caller&#x27;s region</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">actor</span> <span class="title class_">Processor</span> &#123;</span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">process</span>() <span class="keyword">async</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> data <span class="operator">=</span> makeArray() <span class="comment">// Receives ownership of the array</span></span><br><span class="line">        <span class="built_in">print</span>(data)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="sending-parameter-and-return-types-Swift-6-0"><a href="#sending-parameter-and-return-types-Swift-6-0" class="headerlink" title="sending parameter and return types (Swift 6.0)"></a><code>sending</code> parameter and return types (Swift 6.0)</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// The caller must give up ownership of the value</span></span><br><span class="line"><span class="keyword">actor</span> <span class="title class_">ImageProcessor</span> &#123;</span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">process</span>(<span class="keyword">_</span> <span class="params">image</span>: sending <span class="type">UIImage</span>) &#123;</span><br><span class="line">        <span class="comment">// This actor now owns the image</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// The callee guarantees the return value is in a disconnected region</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">createBuffer</span>() -&gt; sending [<span class="type">UInt8</span>] &#123;</span><br><span class="line">    [<span class="type">UInt8</span>](repeating: <span class="number">0</span>, count: <span class="number">1024</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="nonisolated-unsafe-escape-hatch"><a href="#nonisolated-unsafe-escape-hatch" class="headerlink" title="nonisolated(unsafe) (escape hatch)"></a><code>nonisolated(unsafe)</code> (escape hatch)</h3><p>When you know a value is safe but can’t prove it to the compiler.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Global mutable state that&#x27;s actually only accessed from one context</span></span><br><span class="line"><span class="keyword">nonisolated</span>(unsafe) <span class="keyword">var</span> legacyCache: [<span class="type">String</span>: <span class="keyword">Any</span>] <span class="operator">=</span> [:]</span><br><span class="line"></span><br><span class="line"><span class="comment">// Module-level configurable strings</span></span><br><span class="line"><span class="keyword">nonisolated</span>(unsafe) <span class="keyword">var</span> appName <span class="operator">=</span> <span class="string">&quot;My App&quot;</span></span><br></pre></td></tr></table></figure>

<div class="note warning"><p><code>nonisolated(unsafe)</code> disables all compiler checks. Use only when you’ve manually verified safety and can’t restructure the code to satisfy the compiler.</p>
</div>

<h3 id="preconcurrency-incremental-adoption"><a href="#preconcurrency-incremental-adoption" class="headerlink" title="@preconcurrency (incremental adoption)"></a><code>@preconcurrency</code> (incremental adoption)</h3><p>Suppress warnings from pre-concurrency modules you don’t control.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="meta">@preconcurrency</span> <span class="keyword">import</span> SomeOldFramework</span><br><span class="line"></span><br><span class="line"><span class="comment">// SomeOldFramework&#x27;s types are treated as implicitly Sendable</span></span><br><span class="line"><span class="comment">// Warnings are suppressed at the import boundary</span></span><br></pre></td></tr></table></figure>

<h3 id="Swift-6-1-Xcode-16-3-Mar-2025"><a href="#Swift-6-1-Xcode-16-3-Mar-2025" class="headerlink" title="Swift 6.1 (Xcode 16.3, Mar 2025)"></a>Swift 6.1 (Xcode 16.3, Mar 2025)</h3><p>Smaller cycle, two notable additions:</p>
<ul>
<li><strong>Isolated synchronous deinits</strong> (<code>isolated deinit</code>): a deinit can run on a specific actor, fixing a long-standing “deinit can’t safely touch isolated state” gap.</li>
<li><strong><code>isolated(any)</code> parameters</strong>: a parameter that says “I run on whichever actor I was given,” without making the surrounding function isolated. Useful for higher-order async APIs that need to call a callback on the user’s actor.</li>
</ul>
<h3 id="Swift-6-2-approachable-concurrency-Xcode-26-Sept-2025"><a href="#Swift-6-2-approachable-concurrency-Xcode-26-Sept-2025" class="headerlink" title="Swift 6.2: approachable concurrency (Xcode 26, Sept 2025)"></a>Swift 6.2: approachable concurrency (Xcode 26, Sept 2025)</h3><p>Swift 6.2 reframes the defaults around a different mental model: <strong>start on the main thread, opt into background work</strong>. The 6.0 model was “every async function may hop to the global pool, every closure must prove it’s <code>Sendable</code>.” That produced a mountain of <code>Sendable</code> warnings on code that ran nowhere except the main thread. Three pieces, one philosophy.</p>
<p><strong><code>nonisolated(nonsending)</code>: async stays on the caller’s executor.</strong> A <code>nonisolated async</code> function used to implicitly hop to the global executor on entry. Now you can ask it to stay where it was called from:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="meta">@MainActor</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">ProfileViewModel</span> &#123;</span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">loadHeader</span>() <span class="keyword">async</span> &#123;</span><br><span class="line">        <span class="comment">// No hop, no Sendable check at the boundary, no main-thread reschedule on return</span></span><br><span class="line">        <span class="keyword">let</span> title <span class="operator">=</span> <span class="keyword">await</span> formatTitle(for: user)</span><br><span class="line">        <span class="keyword">self</span>.title <span class="operator">=</span> title</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">nonisolated</span>(nonsending) <span class="keyword">func</span> <span class="title function_">formatTitle</span>(<span class="params">for</span> <span class="params">user</span>: <span class="type">User</span>) <span class="keyword">async</span> -&gt; <span class="type">String</span> &#123;</span><br><span class="line">    <span class="string">&quot;<span class="subst">\(user.name)</span>&#x27;s profile&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><code>@concurrent</code>: opt in to the old behaviour.</strong> When you actually want background execution (heavy CPU work, JSON decoding, image processing), mark the function explicitly:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="meta">@concurrent</span> <span class="keyword">func</span> <span class="title function_">decode</span>(<span class="keyword">_</span> <span class="params">data</span>: <span class="type">Data</span>) <span class="keyword">async</span> <span class="keyword">throws</span> -&gt; [<span class="type">User</span>] &#123;</span><br><span class="line">    <span class="keyword">try</span> <span class="type">JSONDecoder</span>().decode([<span class="type">User</span>].<span class="keyword">self</span>, from: data) <span class="comment">// Runs on the global pool</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>@concurrent</code> reads as “I will hop to the global pool”; <code>nonisolated(nonsending)</code> reads as “I stay where you called me.” The 6.0 default was the former implicitly; the 6.2 default (with the upcoming-feature flag below) is the latter.</p>
<p><strong><code>NonisolatedNonsendingByDefault</code> upcoming-feature flag</strong>: flips the default. Any unannotated <code>nonisolated async</code> declaration behaves as <code>nonisolated(nonsending)</code>; functions that need background execution must say <code>@concurrent</code> explicitly.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">.target(</span><br><span class="line">    name: <span class="string">&quot;MyTarget&quot;</span>,</span><br><span class="line">    swiftSettings: [</span><br><span class="line">        .swiftLanguageMode(.v6),</span><br><span class="line">        .enableUpcomingFeature(<span class="string">&quot;NonisolatedNonsendingByDefault&quot;</span>),</span><br><span class="line">    ]</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p><strong>Default actor isolation (SE-0466)</strong>: set a default isolation for an entire module:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Every top-level declaration is implicitly @MainActor</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ProfileViewModel</span> &#123;         <span class="comment">// Implicitly @MainActor</span></span><br><span class="line">    <span class="keyword">var</span> name <span class="operator">=</span> <span class="string">&quot;&quot;</span>                <span class="comment">// Isolated to MainActor</span></span><br><span class="line">    <span class="keyword">func</span> <span class="title function_">load</span>() <span class="keyword">async</span> &#123; &#125;        <span class="comment">// Isolated to MainActor</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Opt out explicitly</span></span><br><span class="line"><span class="keyword">nonisolated</span> <span class="keyword">func</span> <span class="title function_">pureComputation</span>(<span class="keyword">_</span> <span class="params">x</span>: <span class="type">Int</span>) -&gt; <span class="type">Int</span> &#123;</span><br><span class="line">    x <span class="operator">*</span> <span class="number">2</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Approachable Concurrency</strong>: a package-level setting that bundles 6.2’s new defaults: MainActor isolation by default, async-stays-on-caller, fewer Sendable checks at hot boundaries. The recommended starting point for new app and UI modules:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line">.target(</span><br><span class="line">    name: <span class="string">&quot;MyUIModule&quot;</span>,</span><br><span class="line">    swiftSettings: [</span><br><span class="line">        .swiftLanguageMode(.v6),</span><br><span class="line">        .defaultIsolation(<span class="type">MainActor</span>.<span class="keyword">self</span>),                            <span class="comment">// SE-0466</span></span><br><span class="line">        .enableUpcomingFeature(<span class="string">&quot;NonisolatedNonsendingByDefault&quot;</span>),     <span class="comment">// SE-0461</span></span><br><span class="line">    ]</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>With both on, a UI module looks closer to single-threaded code with explicit <code>@concurrent</code> doors into the background pool: the inverse of the 6.0 default, and a much shorter path through Sendable warnings for app code.</p>
<h3 id="Concurrency-migration-checklist"><a href="#Concurrency-migration-checklist" class="headerlink" title="Concurrency migration checklist"></a>Concurrency migration checklist</h3><table>
<thead>
<tr>
<th>Swift 5 pattern</th>
<th>Swift 6 equivalent</th>
</tr>
</thead>
<tbody><tr>
<td><code>DispatchQueue.main.async &#123; &#125;</code></td>
<td><code>Task &#123; @MainActor in &#125;</code> or <code>MainActor.run &#123; &#125;</code></td>
</tr>
<tr>
<td><code>var</code> in closure capture</td>
<td><code>@Sendable</code> closure + <code>Sendable</code> captures</td>
</tr>
<tr>
<td><code>class</code> with <code>var</code> properties across threads</td>
<td><code>actor</code> or <code>@MainActor class</code></td>
</tr>
<tr>
<td>Global <code>var</code></td>
<td><code>nonisolated(unsafe)</code> or actor-isolated</td>
</tr>
<tr>
<td><code>NotificationCenter</code> + <code>@objc</code> handler</td>
<td><code>NotificationCenter.default.notifications(named:)</code> async sequence</td>
</tr>
<tr>
<td>Delegate pattern</td>
<td><code>AsyncStream</code> or continuation</td>
</tr>
<tr>
<td><code>DispatchGroup</code></td>
<td><code>async let</code> or <code>TaskGroup</code></td>
</tr>
<tr>
<td>GCD barrier queue</td>
<td><code>actor</code></td>
</tr>
<tr>
<td><code>@objc</code> callback closure</td>
<td><code>withCheckedContinuation</code></td>
</tr>
<tr>
<td><code>Thread.sleep()</code></td>
<td><code>try await Task.sleep(for:)</code></td>
</tr>
</tbody></table>
<p>Under Swift 6.2 with <code>.defaultIsolation(MainActor.self)</code>, most <code>@MainActor</code> annotations in the right column become implicit: UI code is on <code>MainActor</code> by default and only background work needs explicit annotation (<code>@concurrent</code>).</p>
<hr>
<h2 id="12-Concurrency-debugging-and-profiling"><a href="#12-Concurrency-debugging-and-profiling" class="headerlink" title="12. Concurrency debugging and profiling"></a>12. Concurrency debugging and profiling</h2><h3 id="Thread-Sanitizer-TSan"><a href="#Thread-Sanitizer-TSan" class="headerlink" title="Thread Sanitizer (TSan)"></a>Thread Sanitizer (TSan)</h3><p>Detects data races at runtime.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Product → Scheme → Edit Scheme → Diagnostics → Thread Sanitizer ✓</span><br></pre></td></tr></table></figure>

<p>Or via <code>xcodebuild</code>:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">xcodebuild <span class="built_in">test</span> \</span><br><span class="line">    -enableThreadSanitizer YES \</span><br><span class="line">    -scheme MyApp \</span><br><span class="line">    -destination <span class="string">&#x27;platform=iOS Simulator,name=iPhone 17&#x27;</span></span><br></pre></td></tr></table></figure>

<h3 id="Instruments-concurrency-tools"><a href="#Instruments-concurrency-tools" class="headerlink" title="Instruments concurrency tools"></a>Instruments concurrency tools</h3><table>
<thead>
<tr>
<th>Instrument</th>
<th>Purpose</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Swift Concurrency</strong></td>
<td>Visualize task trees, actor contention, task creation&#x2F;completion</td>
</tr>
<tr>
<td><strong>Thread State Trace</strong></td>
<td>Thread blocking, context switches, runnable vs blocked</td>
</tr>
<tr>
<td><strong>System Trace</strong></td>
<td>Low-level thread scheduling, priority inversions</td>
</tr>
<tr>
<td><strong>Time Profiler</strong></td>
<td>CPU time per thread, identify hot code on wrong threads</td>
</tr>
<tr>
<td><strong>os_signpost</strong></td>
<td>Custom intervals for measuring concurrency regions</td>
</tr>
</tbody></table>
<h3 id="Runtime-concurrency-checks"><a href="#Runtime-concurrency-checks" class="headerlink" title="Runtime concurrency checks"></a>Runtime concurrency checks</h3><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Assert main thread (UIKit code)</span></span><br><span class="line">dispatchPrecondition(condition: .onQueue(.main))</span><br><span class="line"></span><br><span class="line"><span class="comment">// Assert NOT on main thread</span></span><br><span class="line">dispatchPrecondition(condition: .notOnQueue(.main))</span><br><span class="line"></span><br><span class="line"><span class="comment">// In async context</span></span><br><span class="line"><span class="built_in">assert</span>(<span class="type">Thread</span>.isMainThread, <span class="string">&quot;Must be on main thread&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// Swift 6 strict concurrency checking catches this at compile time</span></span><br></pre></td></tr></table></figure>

<h3 id="Strict-concurrency-checking-pre-Swift-6"><a href="#Strict-concurrency-checking-pre-Swift-6" class="headerlink" title="Strict concurrency checking (pre-Swift 6)"></a>Strict concurrency checking (pre-Swift 6)</h3><p>Enable warnings before fully migrating:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Package.swift</span></span><br><span class="line">.target(</span><br><span class="line">    name: <span class="string">&quot;MyTarget&quot;</span>,</span><br><span class="line">    swiftSettings: [</span><br><span class="line">        .enableExperimentalFeature(<span class="string">&quot;StrictConcurrency&quot;</span>),</span><br><span class="line">    ]</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="When-to-use-what"><a href="#When-to-use-what" class="headerlink" title="When to use what"></a>When to use what</h2><table>
<thead>
<tr>
<th>Scenario</th>
<th>Recommended approach</th>
</tr>
</thead>
<tbody><tr>
<td>New async code (iOS 15+)</td>
<td><code>async</code>&#x2F;<code>await</code></td>
</tr>
<tr>
<td>Protecting mutable state</td>
<td><code>actor</code> (or <code>@MainActor</code> for UI)</td>
</tr>
<tr>
<td>Parallel independent tasks (known count)</td>
<td><code>async let</code></td>
</tr>
<tr>
<td>Parallel tasks (dynamic count)</td>
<td><code>TaskGroup</code></td>
</tr>
<tr>
<td>UI updates from background</td>
<td><code>@MainActor</code></td>
</tr>
<tr>
<td>Bridging callbacks to async</td>
<td><code>withCheckedContinuation</code></td>
</tr>
<tr>
<td>Event streams over time</td>
<td><code>AsyncStream</code> &#x2F; <code>AsyncSequence</code></td>
</tr>
<tr>
<td>Reactive chains with operators</td>
<td>Combine (or AsyncAlgorithms)</td>
</tr>
<tr>
<td>Legacy codebase (pre-iOS 15)</td>
<td>GCD</td>
</tr>
<tr>
<td>Task dependencies &#x2F; complex graphs</td>
<td><code>OperationQueue</code></td>
</tr>
<tr>
<td>Lock-free counters</td>
<td><code>OSAllocatedUnfairLock</code> or Swift Atomics</td>
</tr>
<tr>
<td>C interop threading</td>
<td><code>pthread</code></td>
</tr>
</tbody></table>
<hr>
<h2 id="Further-reading"><a href="#Further-reading" class="headerlink" title="Further reading"></a>Further reading</h2><ul>
<li><a href="https://gist.github.com/lattner/31ed37682ef1576b16bca1432ea9f782">Swift concurrency manifesto</a>: Chris Lattner’s original vision</li>
<li><a href="https://github.com/swiftlang/swift-evolution/blob/main/proposals/0296-async-await.md">SE-0296 async&#x2F;await</a>: the proposal that started it all</li>
<li><a href="https://github.com/swiftlang/swift-evolution/blob/main/proposals/0304-structured-concurrency.md">SE-0304 Structured concurrency</a>: task groups and child tasks</li>
<li><a href="https://github.com/swiftlang/swift-evolution/blob/main/proposals/0306-actors.md">SE-0306 Actors</a>: actor model for Swift</li>
<li><a href="https://github.com/swiftlang/swift-evolution/blob/main/proposals/0337-support-incremental-migration-to-concurrency-checking.md">SE-0337 Sendable</a>: incremental Sendable adoption</li>
<li><a href="https://github.com/swiftlang/swift-evolution/blob/main/proposals/0414-region-based-isolation.md">SE-0414 Region-based isolation</a>: the typing rules behind <code>sending</code></li>
<li><a href="https://github.com/swiftlang/swift-evolution/blob/main/proposals/0430-transferring-parameters-and-results.md">SE-0430 sending parameter</a>: region-based isolation</li>
<li><a href="https://github.com/swiftlang/swift-evolution/blob/main/proposals/0461-async-function-isolation.md">SE-0461 Run nonisolated async functions on the caller’s actor</a>: <code>nonisolated(nonsending)</code> and <code>@concurrent</code></li>
<li><a href="https://github.com/swiftlang/swift-evolution/blob/main/proposals/0466-control-default-actor-isolation.md">SE-0466 Default isolation</a>: module-level default actor isolation</li>
<li><a href="https://github.com/apple/swift-async-algorithms">Swift Async Algorithms</a>: merge, debounce, throttle, combineLatest</li>
<li><a href="https://github.com/apple/swift-atomics">Swift Atomics</a>: lock-free atomic operations</li>
<li><a href="https://developer.apple.com/videos/play/wwdc2021/10254/">WWDC21: Swift concurrency: Behind the scenes</a>: cooperative pool, hop counts, runtime contract</li>
<li><a href="https://developer.apple.com/videos/play/wwdc2022/110351/">WWDC22: Eliminate data races using Swift Concurrency</a></li>
<li><a href="https://developer.apple.com/videos/play/wwdc2023/10170/">WWDC23: Beyond the basics of structured concurrency</a></li>
<li><a href="https://www.donnywals.com/exploring-concurrency-changes-in-swift-6-2/">Donny Wals: Exploring concurrency changes in Swift 6.2</a></li>
<li><a href="https://www.donnywals.com/what-is-concurrent-in-swift-6-2/">Donny Wals: What is @concurrent in Swift 6.2?</a></li>
<li><a href="https://forums.swift.org/t/how-is-the-cooperative-thread-pool-integrated-in-swift/67466">How is the Cooperative Thread Pool integrated in Swift?: Swift Forums</a></li>
</ul>
]]></content>
      <categories>
        <category>iOS</category>
      </categories>
      <tags>
        <tag>Swift</tag>
        <tag>GCD</tag>
        <tag>concurrency</tag>
        <tag>async-await</tag>
        <tag>actors</tag>
        <tag>Combine</tag>
        <tag>OperationQueue</tag>
      </tags>
  </entry>
  <entry>
    <title>WWDC 2012 revisited: the year iOS caught up to AppKit, and the year a few things shipped that wouldn&#39;t survive</title>
    <url>/WWDC-2012-Revisited/</url>
    <content><![CDATA[<p>The thing about 2012 that’s easy to forget is that for the first five years of iOS, UIKit was a junior framework. AppKit had been shipping since 1989; iOS was a phone OS that borrowed Cocoa’s runtime and naming conventions but quietly did without the harder pieces. There was no constraint-based layout on iOS until 2012. There was no <code>NSAttributedString</code> accepted by <code>UILabel</code> until 2012. There was no grid view, no proper view controller containment API, no canonical async substrate. Developers building iPhone apps reached for <code>CGRect.frame</code>, sprung-and-strut autoresizing masks, and Core Text bridges, while their colleagues on the Mac had stopped writing that kind of code years earlier.</p>
<span id="more"></span>

<p>The June 2012 keynote was about iOS 6 and OS X Mountain Lion, but the conference underneath the keynote was about closing that gap. The five days of sessions are the most “AppKit comes to iOS” week Apple has ever shipped. Auto Layout, attributed strings, view controller containment, modern Core Data threading, Keychain access, GCD as the canonical async layer: all of these were either Mac-originated APIs being ported, or Mac-originated patterns being formalized for iOS. By the end of WWDC 2012, the iOS toolkit had what it needed to be the equal of AppKit. The visual rewrite that arrived twelve months later in iOS 7 needed exactly this groundwork; you cannot build a translucent layout-aware design system on top of springs and struts.</p>
<p>The other thing 2012 did, which I want to be honest about because it’s the part that doesn’t make it into most retrospectives, is that it also shipped two products that would not survive contact with the world. Apple Maps launched buggy enough to become a meme and cost the head of iOS his job. Core Data + iCloud’s transaction-log sync was deprecated four years later after a stretch where it was the most-complained-about API at Apple. Both of those are also part of the year. The interesting question isn’t whether the launches were good, but what survived them: in both cases, the <em>mental model</em> the launch argued for is still the mental model in use today, even though almost none of the original code is.</p>
<h2 id="What-“catching-up-to-AppKit”-actually-meant"><a href="#What-“catching-up-to-AppKit”-actually-meant" class="headerlink" title="What “catching up to AppKit” actually meant"></a>What “catching up to AppKit” actually meant</h2><p>Five sessions are the ones that close the gap.</p>
<table>
<thead>
<tr>
<th>Reveal</th>
<th>What AppKit had</th>
<th>What iOS gained in 2012</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Auto Layout for iOS (202, 228, 232)</strong></td>
<td><code>NSLayoutConstraint</code> since 10.7 (2011)</td>
<td><code>NSLayoutConstraint</code> on UIKit; same constraint algebra, same priority system, same content-hugging &#x2F; compression-resistance defaults</td>
</tr>
<tr>
<td><strong><code>UICollectionView</code> (205)</strong></td>
<td>A primitive <code>NSCollectionView</code> had existed since 10.5 (2007), but with no cell reuse and a fixed grid; AppKit didn’t get a UICollectionView-equivalent rewrite until 10.11 in 2015</td>
<td>The reworked, layout-pluggable version arrived on iOS first this year; generalized <code>UITableView</code> to any 2D layout</td>
</tr>
<tr>
<td><strong><code>NSAttributedString</code> on UIKit (200, 222)</strong></td>
<td>First-class on AppKit since the beginning</td>
<td><code>UILabel</code>, <code>UITextView</code>, <code>UITextField</code>, <code>UIButton</code>, <code>UISegmentedControl</code> accept attributed strings directly; no more Core Text bridging</td>
</tr>
<tr>
<td><strong>View controller containment (236)</strong></td>
<td>AppKit’s view hierarchy never had this problem (NSView containment was always public)</td>
<td><code>addChild(_:)</code>, <code>removeFromParent</code>, <code>didMove(toParent:)</code> as a public, lifecycle-aware contract for custom container VCs</td>
</tr>
<tr>
<td><strong>Async patterns (712)</strong></td>
<td>GCD revealed 2009, deep-dived 2011 on the Mac</td>
<td>Same talk, restaged for iOS-first developers; blocks, queues, groups, semaphores, QoS as the canonical substrate</td>
</tr>
</tbody></table>
<p>Look at this table sideways and the shape jumps out. Most of these are Apple admitting that the iPhone-era convenience version of a Cocoa API was insufficient and re-shipping the Mac-grown version. <code>UICollectionView</code> is the one row that goes the other direction: iOS shipped the modern, layout-pluggable design first and AppKit followed three years later in 10.11. Either way, the mental model of “Mac is for serious developers, iOS is for everyone else” got buried at this conference.</p>
<p>Reading the 202 transcript back is the most affecting one for me. The talk is mostly an Auto Layout intro, but the slide that does the work is the one that says: a constraint is a linear equation of the form <code>view1.attribute = multiplier * view2.attribute + constant</code>, and a layout is a system of these equations resolved by a Cassowary-style simplex solver. That sentence is doing more lifting than it looks like. It’s saying: layout is not a procedure your code runs. Layout is a <em>declarative specification</em> the system runs on your behalf. Your job is to declare the relationships you want to hold; the solver figures out which rectangles satisfy them.</p>
<p>That’s the same sentence SwiftUI’s <code>Layout</code> protocol says seven years later in different words. The Auto Layout reveal in 2012 didn’t just unblock iOS; it shipped the conceptual scaffolding for every subsequent layout system Apple would build. SnapKit’s DSL, <code>NSLayoutAnchor</code>, SwiftUI’s view tree-as-a-set-of-size-negotiations, even visionOS’s spatial layout: all of them are restatements of the 2012 position with different syntax over the top. The reason <code>.layoutPriority(1)</code> in SwiftUI feels intuitive is that it’s the same primitive as <code>.priority(.high)</code> in SnapKit, which is the same primitive as <code>setContentHuggingPriority(_:for:)</code> in 2012 Auto Layout, which is the same primitive as <code>NSLayoutPriority</code> in 2011 AppKit. The vocabulary survived four generations of API rewrite intact.</p>
<p>The piece of the 2012 Auto Layout reveal that did <em>not</em> survive intact is the syntax 228 spent the most time on: the Visual Format Language, the ASCII-art DSL where <code>@&quot;H:|-[button]-|&quot;</code> meant “pin button’s leading and trailing edges to the superview’s margins.” VFL was Apple’s bet on a string-based mini-language for constraints, pitched as more readable than the equation form. The bet lost. The strings weren’t typechecked, the metric dictionary was easy to typo, the syntax couldn’t express most non-trivial relationships, and the error messages when something went wrong pointed at the parser rather than the layout. <code>NSLayoutAnchor</code> arrived in iOS 9 (2015) as the type-safe replacement, and every layout DSL since has either wrapped anchors directly (SnapKit) or used a builder pattern over the underlying constraint algebra (SwiftUI). VFL still parses on iOS 26 but you don’t see it in modern code. The conceptual frame from 202 outlasted its 2012 sibling syntax by ten years and counting.</p>
<h2 id="UICollectionView-and-the-moment-“subclass-the-view”-stopped-being-the-answer"><a href="#UICollectionView-and-the-moment-“subclass-the-view”-stopped-being-the-answer" class="headerlink" title="UICollectionView and the moment “subclass the view” stopped being the answer"></a>UICollectionView and the moment “subclass the view” stopped being the answer</h2><p><code>UICollectionView</code> is the 2012 reveal I find most interesting to reread, because it shipped with a piece of architectural advice that has been the right advice ever since and that almost no other UIKit API followed. The advice is: <strong>don’t subclass the view to change the layout. Plug in a layout object.</strong></p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Pre-UICollectionView: subclassing UITableView to fake a grid</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">GridTableView</span>: <span class="title class_ inherited__">UITableView</span> &#123; <span class="comment">/* manual frame math, prepareForReuse hacks */</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2012: layout is a separate object</span></span><br><span class="line"><span class="keyword">let</span> layout <span class="operator">=</span> <span class="type">UICollectionViewFlowLayout</span>()</span><br><span class="line">layout.itemSize <span class="operator">=</span> <span class="type">CGSize</span>(width: <span class="number">80</span>, height: <span class="number">80</span>)</span><br><span class="line">layout.minimumInteritemSpacing <span class="operator">=</span> <span class="number">8</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> collectionView <span class="operator">=</span> <span class="type">UICollectionView</span>(frame: .zero, collectionViewLayout: layout)</span><br></pre></td></tr></table></figure>

<p>The bet <code>UICollectionView</code> made was that layout is <em>data</em>, not a <em>subclass</em>. A grid is a <code>UICollectionViewFlowLayout</code> instance with specific properties. A magazine is a custom <code>UICollectionViewLayout</code> subclass that computes attributes from sections. A circular dial is a custom <code>UICollectionViewLayout</code> subclass that places items on a circle. The data source contract (what cells exist, how they get configured) stays constant; the layout contract (where each cell goes) is pluggable.</p>
<p>Seven years later in 2019, <code>UICollectionViewCompositionalLayout</code> doubled down on the bet. Layout itself became composable, built from sections, groups, and items, each of which is a value type you assemble in code. A grid plus a horizontally-scrolling carousel plus a sticky header is no longer a custom layout subclass; it’s a couple of <code>NSCollectionLayoutSection</code> values handed to a single compositional layout instance. The 2012 contract (the view delegates to a layout object) didn’t change; the layout object got the inside of a small DSL added to it.</p>
<p>The contrast is <code>UITableView</code>, which made the opposite bet in 2008 (subclass the cell, configure the view, fight with <code>heightForRowAtIndexPath:</code>) and which <code>UICollectionView</code> quietly replaced for almost every use case over the next decade. Every modern UIKit list is a <code>UICollectionView</code> with a compositional layout. The reason is that <code>UITableView</code> couldn’t decouple “what’s in the list” from “how the list is shaped,” and <code>UICollectionView</code> could. The 2012 reveal was the moment Apple admitted UIKit had built the wrong abstraction the first time and shipped the right one.</p>
<p>The corollary I didn’t appreciate until much later is that this is the only place in UIKit where Apple successfully did “configuration over subclassing” in the iOS 5 era. UITabBarController, UISplitViewController, UINavigationController are all still classes you subclass. UIScrollView delegates everything, but you still subclass it to add behavior. UICollectionView is the one place where the design says: don’t subclass me; hand me an object that describes the behavior you want. That position survived intact. Every modern Apple framework that ships a “give me an object that describes the layout you want” API (Layout protocol in SwiftUI, the view builder in WidgetKit, the snippet-and-view system in App Intents) descends from this.</p>
<h2 id="View-controller-containment-or-how-custom-containers-stopped-leaking"><a href="#View-controller-containment-or-how-custom-containers-stopped-leaking" class="headerlink" title="View controller containment, or how custom containers stopped leaking"></a>View controller containment, or how custom containers stopped leaking</h2><p>Before 2012, writing a custom container view controller on iOS was a known footgun. The runtime had <code>UINavigationController</code>, <code>UITabBarController</code>, <code>UISplitViewController</code>, and <code>UIPageViewController</code>. If you wanted anything else (a side menu, a paging shell, a dual-pane that doesn’t behave like split view), you wrote a <code>UIViewController</code> subclass, added another VC’s <code>view</code> as a subview, and hoped.</p>
<p>What you got for “hoped” was: appearance callbacks fired for the parent but not for the child. Rotation propagated to the parent but not always to the child. Status bar style updates were lost. Memory warnings reached the parent’s <code>didReceiveMemoryWarning</code> but the child wouldn’t drop its caches because nobody told it. Modal presentation got confused about who the presenting VC was. The child’s <code>parent</code> property was <code>nil</code> even though its view was on screen. You’d ship the app, the user would rotate the phone, and the inner navigation bar would stay in portrait orientation while the rest of the screen rotated. There was no way to debug it because there was no public contract being violated; the runtime simply didn’t know your VC was a container.</p>
<p>Session 236 fixed this by promoting containment from “thing some VCs do” to “thing any VC can do, here’s the contract”:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// The 2012 contract for adding a child VC to your custom container</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">embed</span>(<span class="keyword">_</span> <span class="params">child</span>: <span class="type">UIViewController</span>, <span class="params">in</span> <span class="params">container</span>: <span class="type">UIView</span>) &#123;</span><br><span class="line">    addChild(child)</span><br><span class="line">    container.addSubview(child.view)</span><br><span class="line">    child.view.snp.makeConstraints &#123; make <span class="keyword">in</span></span><br><span class="line">        make.edges.equalToSuperview()</span><br><span class="line">    &#125;</span><br><span class="line">    child.didMove(toParent: <span class="keyword">self</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// And for removing</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">unembed</span>(<span class="keyword">_</span> <span class="params">child</span>: <span class="type">UIViewController</span>) &#123;</span><br><span class="line">    child.willMove(toParent: <span class="literal">nil</span>)</span><br><span class="line">    child.view.removeFromSuperview()</span><br><span class="line">    child.removeFromParent()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Four lines on each side. The <code>addChild</code> &#x2F; <code>didMove(toParent:)</code> pairing is the API saying “I’m about to put this VC into my hierarchy; once it’s actually in the view tree, fire its appearance callbacks.” The <code>willMove(toParent: nil)</code> &#x2F; <code>removeFromParent</code> pairing says the inverse. Once these are wired up correctly, every lifecycle method works: <code>viewWillAppear</code> fires on the child when the container appears, rotation propagates, modal presentation walks the tree the right way, status bar style requests find the right VC to ask.</p>
<p>The reason this matters in 2026 isn’t that anyone is writing side-menu controllers anymore. It’s that <em>every</em> embedded view controller in a modern iOS app goes through this contract. A list VC that shows an empty-state placeholder VC inside its view: containment. A SwiftUI <code>UIHostingController</code> embedded inside a UIKit screen: containment. A <code>UITabBarController</code> subclass that wants to delegate status bar appearance to its selected child: containment. The 2012 reveal codified what every iOS app would later assume is just how things work.</p>
<p>There’s an addendum I find interesting that the 2012 talk couldn’t anticipate: the contract works exactly the same way when the parent is UIKit and the child is SwiftUI, or vice versa. <code>UIHostingController</code> is a <code>UIViewController</code> subclass; <code>UIViewControllerRepresentable</code> wraps a UIKit VC into a SwiftUI view that internally manages a UIHostingController-equivalent shim. Both rely on the 2012 lifecycle contract being correct. Without 236, the bridges between SwiftUI and UIKit would be unreliable in a way that would have made SwiftUI’s gradual-adoption story impossible. The view-controller substrate that lets you mix the two was paid for in 2012.</p>
<h2 id="Core-Data-finally-gets-a-threading-story"><a href="#Core-Data-finally-gets-a-threading-story" class="headerlink" title="Core Data finally gets a threading story"></a>Core Data finally gets a threading story</h2><p>The 2012 Core Data session (214) is the one that defines, in modern form, what every Core Data architecture in the workspace has been doing ever since. The headline change is <code>NSPrivateQueueConcurrencyType</code> and <code>NSMainQueueConcurrencyType</code> as the two real concurrency types, with <code>NSConfinementConcurrencyType</code> (the iOS 4 model where each context was tied to whatever thread created it) deprecated. The substantive change is <code>performBlock:</code> &#x2F; <code>performBlockAndWait:</code> as the only safe way to touch a context off its own queue.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 2012 model, still the model in 2026</span></span><br><span class="line"><span class="keyword">let</span> backgroundContext <span class="operator">=</span> persistentContainer.newBackgroundContext()</span><br><span class="line">backgroundContext.perform &#123;</span><br><span class="line">    <span class="comment">// Inside this block, we own the context&#x27;s queue.</span></span><br><span class="line">    <span class="keyword">let</span> request <span class="operator">=</span> <span class="type">NSFetchRequest</span>&lt;<span class="type">Pet</span>&gt;(entityName: <span class="string">&quot;Pet&quot;</span>)</span><br><span class="line">    <span class="keyword">let</span> pets <span class="operator">=</span> <span class="keyword">try?</span> backgroundContext.fetch(request)</span><br><span class="line">    <span class="comment">// ... mutations, save, etc.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>The architectural claim is that a Core Data context is not “a piece of thread-safe data” the way a <code>Set</code> you wrap in a lock is. It’s a <em>queue-confined object</em>, and access to it is mediated by submitting work to the queue it belongs to. This is the same model GCD argued for at the queue level (session 712, same conference), applied to Core Data. The substrate is consistent: Apple’s answer to “how do I do work off the main thread safely” in 2012 was, across all of Core Data, GCD, and AVFoundation, “submit a block to the appropriate queue and let the system schedule it.” Threads went from being a thing apps thought about to being a thing the runtime managed.</p>
<p>What 2012 also tried to ship in Core Data, and didn’t get right, was iCloud sync via <code>NSPersistentStoreUbiquitousContentNameKey</code>. The 227 session pitched it as: opt into the option, the framework writes a transaction log alongside the SQLite store in the user’s ubiquity container, other devices consume the log and replay. In practice, the transaction logs collided, the merge had no conflict-policy API, schema migration was effectively unsupported across devices, and the recommended response to “my sync is broken” was “wipe the user’s data and resync from scratch.” It was deprecated in 2016.</p>
<p>The interesting part of that failure is what it taught Apple about the <em>shape</em> of the problem, which they used when they shipped <code>NSPersistentCloudKitContainer</code> in 2019. The replacement abandoned transaction logs in favor of CloudKit’s record-based sync, but kept the conceptual claim from 2012 intact: user account is the boundary, devices are caches, last-writer-wins is the merge policy. The 2012 design was right about the model and wrong about the implementation. CloudKit fixed the implementation and inherited the model. Apps that ship Core Data + CloudKit sync today are running on the conceptual scaffolding of the 2012 reveal even though none of the original code survived.</p>
<p>The last-writer-wins decision is worth dwelling on because it is the same decision SwiftData inherited in 2023. None of Apple’s iCloud sync layers offer a configurable merge policy. The 2012 transaction-log sync didn’t. <code>NSPersistentCloudKitContainer</code> doesn’t. SwiftData with <code>cloudKitDatabase:</code> doesn’t. The bet is that 95% of apps want an opinionated sync layer they can’t tune, because the alternative (vector clocks, CRDTs, three-way merges) demands that the app model conflict explicitly, and most apps can’t. Fifteen years on, the 95% bet still looks right. The 5% (collaborative editors, multi-user document apps) build their own sync layer or accept the limitation.</p>
<h2 id="The-GCD-deep-dive-that-became-the-substrate-for-everything-after"><a href="#The-GCD-deep-dive-that-became-the-substrate-for-everything-after" class="headerlink" title="The GCD deep dive that became the substrate for everything after"></a>The GCD deep dive that became the substrate for everything after</h2><p>Session 712 is the talk I find easiest to underrate when reading the 2012 lineup, because GCD had already shipped in 2009 and been deep-dived in 2011 on the Mac. The 2012 restaging is GCD as iOS-app developers were supposed to learn it. The contribution isn’t a new API; it’s a coherent argument for what async on Apple platforms is.</p>
<p>The argument has two halves. The first is that the unit of concurrency is the queue, not the thread. You don’t ask “how many threads should I spawn?” You ask “what work needs to happen serially, what work can happen in parallel, and which work needs to come back to which queue when it’s done?” The runtime maps that onto a thread pool you don’t manage. Custom serial queues for thread-confinement of mutable state. The global concurrent queue for stateless parallel work. The main queue for anything touching UIKit. Plus QoS classes (<code>.userInteractive</code>, <code>.userInitiated</code>, <code>.default</code>, <code>.utility</code>, <code>.background</code>) so the system can throttle low-priority work under thermal pressure without you writing throttling code.</p>
<p>The second is the queue-targeting hierarchy, which I think is underappreciated. You declare a serial queue and target it at a global concurrent queue. You declare ten serial queues all targeting the same parent. The hierarchy gives you bounded parallelism for free: if all ten queues funnel into a parent at QoS <code>.utility</code>, the system can pace them as a group. There’s no manual semaphore, no manual thread-pool size, no manual throttling logic. The runtime sees the tree and respects it.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 2012 GCD: queue-confined work</span></span><br><span class="line"><span class="keyword">let</span> imageQueue <span class="operator">=</span> <span class="type">DispatchQueue</span>(label: <span class="string">&quot;com.app.imageDecoder&quot;</span>, qos: .userInitiated)</span><br><span class="line">imageQueue.async &#123;</span><br><span class="line">    <span class="keyword">let</span> decoded <span class="operator">=</span> decode(data)</span><br><span class="line">    <span class="type">DispatchQueue</span>.main.async &#123;</span><br><span class="line">        <span class="keyword">self</span>.imageView.image <span class="operator">=</span> decoded</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2021 Swift Concurrency: same shape, different syntax</span></span><br><span class="line"><span class="type">Task</span> &#123; <span class="meta">@MainActor</span> <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">let</span> decoded <span class="operator">=</span> <span class="keyword">await</span> decode(data)</span><br><span class="line">    imageView.image <span class="operator">=</span> decoded</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>The Swift version is shorter. The decisions a developer makes are exactly the same: decoding doesn’t need the main thread (the <code>await</code> hops off MainActor onto the cooperative pool), the UI update belongs back on main (the <code>@MainActor</code> annotation). The actor runtime that schedules Swift Concurrency is using GCD’s dispatch primitives underneath. <code>MainActor.run</code> is <code>DispatchQueue.main.async</code> with a type system on top. The 2012 talk’s framing (“queue confinement is the unit of concurrency”) and the 2021 talk’s framing (“actor isolation is the unit of safety”) agree about everything except the boundary, and the boundary is where the bugs live.</p>
<p>The thing that ages worst in the 712 talk is the secondary vocabulary. Dispatch groups, dispatch semaphores, dispatch barriers, dispatch sources. The runtime still implements them, the headers are still there, but in a modern Swift codebase most of them have been replaced. <code>dispatch_group</code> was the bounded-fanout primitive; <code>withTaskGroup</code> is the replacement and it’s better in every dimension (typed errors, structured cancellation, no manual <code>enter</code>&#x2F;<code>leave</code>). <code>dispatch_semaphore</code> was the bounded-concurrency primitive; an actor with a counter is the replacement and it’s better in every dimension (no risk of blocking the cooperative pool, no risk of priority inversion). A <code>DispatchSemaphore</code> in a 2026 codebase is almost always a sign of an async function being called from a sync context, and the fix is to make the caller async rather than to block a thread. The 2012 vocabulary has aged into a debugging hint: if you see it, something is bridging old-world synchronous code into the new-world structured concurrency, and you can usually push the bridge up the call stack.</p>
<h2 id="Apple-Maps-and-the-difference-between-a-bad-launch-and-a-wrong-model"><a href="#Apple-Maps-and-the-difference-between-a-bad-launch-and-a-wrong-model" class="headerlink" title="Apple Maps, and the difference between a bad launch and a wrong model"></a>Apple Maps, and the difference between a bad launch and a wrong model</h2><p>The 2012 Maps reveal (session 300) is the part of the conference everyone outside the developer audience remembers. Apple shipped its own vector-rendered Maps, ejected the Google tile service that had powered MapKit since iOS 2, and within weeks the press was running screenshots of Brooklyn Bridge melted into the East River, the Washington Monument labeled as a hospital, entire cities mislabeled or missing. Tim Cook published an apology in September. Scott Forstall left the company in October partly over the launch. Apple Maps became the synecdoche for “Apple shipped something half-done.”</p>
<p>What’s easy to miss in retrospect is that the 2012 reveal made two separate claims that have to be evaluated separately. The first was <em>the data is good</em>, which was false. The second was <em>apps don’t own map data</em>, which has held up perfectly.</p>
<p>The pre-2012 model was that MapKit’s <code>MKMapView</code> rendered tiles fetched from Google’s tile service; the agreement between Apple and Google governed what your app could and couldn’t do with those tiles, and when the agreement ran out, the entire map disappeared from your app. Apps that wanted custom cartography (a hiking app, a transit app) had to host their own tile server or pay for one. Geocoding, point-of-interest search, and directions were either Google’s or another third party’s.</p>
<p>The 2012 reveal said: maps are <em>infrastructure</em>, owned by the system, exposed to apps through a permission-gated API. <code>MKMapView</code> renders vector data the system manages. <code>MKLocalSearch</code> queries Apple’s POI database. <code>MKMapItem</code> is the system-blessed POI value type. <code>CLGeocoder</code> does forward and reverse geocoding against the system service. Apps don’t host map data, don’t pay for tile delivery, don’t sign contracts with a tile vendor, and don’t lose their map when the vendor relationship sours. The map is part of the platform.</p>
<p>That position survived the launch quality problem intact. The data caught up over time (it took years, but the Apple Maps of 2026 is comparable in coverage and accuracy to the Google Maps of 2026 in most regions), and the model the 2012 reveal staked out (system-owned, permission-mediated, value-typed) has since been the model for every other large-data Apple framework. <code>EKEventStore</code> is the same shape. <code>CKContainer</code> is the same shape. <code>PHPhotoLibrary</code> is the same shape. <code>PKPaymentRequest</code> is the same shape. All of them: a system service owns the data, apps consume it via a typed API, the user grants or revokes permission. The pattern Maps argued for in 2012 became the pattern for every system-mediated resource by 2020.</p>
<p>The lesson I take from the Apple Maps launch is not “Apple shouldn’t ship things until they’re ready” (which is the consumer-press lesson) but “the launch quality of a <em>platform shift</em> is decoupled from the durability of the <em>model</em> the platform shift embodies.” Apple Maps shipped with bad data, but the API shape was right, and the data is now fine. Core Data + iCloud shipped with bad sync semantics, but the conceptual frame (account is the boundary, devices are caches) was right, and CloudKit later made the semantics fine. The 2012 launches that failed at the implementation succeeded at the model often enough that I’ve stopped reading the launch reviews as the verdict on whether the launch mattered.</p>
<h2 id="Facebook-and-Passbook-or-how-the-same-year-shipped-one-thing-that-vanished-and-one-thing-that-became-a-billion-dollar-product"><a href="#Facebook-and-Passbook-or-how-the-same-year-shipped-one-thing-that-vanished-and-one-thing-that-became-a-billion-dollar-product" class="headerlink" title="Facebook and Passbook, or how the same year shipped one thing that vanished and one thing that became a billion-dollar product"></a>Facebook and Passbook, or how the same year shipped one thing that vanished and one thing that became a billion-dollar product</h2><p>The 2012 keynote sat two announcements next to each other that, in retrospect, are the cleanest illustration of how decoupled launch quality is from the durability of a model.</p>
<p>Facebook integration was iOS 6’s most-demoed feature for non-developers. The system Settings pane got a single Facebook account toggle, and once it was on, Facebook was <em>everywhere</em>: the share sheet (one-tap “Post to Facebook” from Photos, Safari, Maps, App Store), Siri (“Post to Facebook”), the Contacts app pulled friend photos and birthdays, the App Store added social signals to product pages, Game Center exposed Facebook friend lists, the Notification Center got a Facebook posting widget. The pitch was that social was now a system service the way calling and texting were. The same pitch had been made for Twitter twelve months earlier in iOS 5, and Facebook in iOS 6 was the consummation. By 2017 the entire integration was gone. iOS 11 quietly removed both the Facebook and Twitter system accounts and the share-sheet plumbing that fed them, on the principle that any third party who wanted into the share sheet could ship an App Extension and stand in the same line as everyone else. The model the 2012 reveal argued for (social platforms are system services) lost to the model 2014’s extension architecture argued for (every app is a peer, the system mediates between them through typed contracts). Worth noting that the latter is itself a corollary of the same “system mediates, apps contribute, user controls” pattern that Maps, EventKit, and Keychain were arguing for at this same conference. The Facebook integration was actually the <em>outlier</em> in 2012’s larger story; it just happened to be the one the keynote led with.</p>
<p>Passbook is the inverse. The keynote demo was a deliberately understated pitch (boarding passes and Starbucks gift cards in a card-stack UI), the sessions (301, 309) introduced a pass format that almost nobody outside the demo seemed excited about, and the launch was widely read as “Apple’s answer to a problem nobody had.” Then Apple shipped Apple Pay two years later in 2014, and Passbook turned out to be the data model and presentation layer that Apple Pay needed. Passbook got renamed to Wallet in iOS 9. Apple Card lives in it. Transit cards in Tokyo, Beijing, London, New York live in it. Driver’s licenses in a handful of US states live in it. Hotel room keys, event tickets, car keys, vaccination records. The <code>.pkpass</code> format the 2012 reveal introduced is still the wire format, fourteen years and several Apple Pay protocol generations later. Worth saying out loud: this is the 2012 product with the highest unit revenue impact on Apple’s business in 2026, and at the time it was the unloved cousin of the Facebook integration sitting next to it.</p>
<p>The two read together as a sharper version of the Maps&#x2F;Core Data lesson. Launch hype is not the verdict on what’s load-bearing. The 2012 announcement that defined the keynote (Facebook everywhere) was gone in five years. The 2012 announcement that was easy to dismiss (a card stack for boarding passes) became the substrate for Apple’s payments business. The model claim hides under the launch noise on the day of, and only the years afterward sort out which side of the model claim each launch was on.</p>
<h2 id="Privacy-strings-and-the-moment-“what-does-this-app-access”-became-a-typed-contract"><a href="#Privacy-strings-and-the-moment-“what-does-this-app-access”-became-a-typed-contract" class="headerlink" title="Privacy strings, and the moment “what does this app access” became a typed contract"></a>Privacy strings, and the moment “what does this app access” became a typed contract</h2><p>Session 710 is the one that introduces the iOS 6 purpose-string system. Pre-2012, an iPhone app could read your entire Contacts database, your Calendar, your Photos, your Reminders, without ever asking. Path’s social network famously got caught uploading users’ address books in February 2012. The 710 reveal a few months later closed the loophole: any sensitive data category now required a string in your Info.plist (<code>NSContactsUsageDescription</code>, <code>NSCalendarsUsageDescription</code>, <code>NSPhotoLibraryUsageDescription</code>, …), and the system showed your string in a one-shot prompt that the user could grant, deny, and later revoke.</p>
<p>The thing the 710 talk got right that I find genuinely impressive is the <em>declarative</em> framing. The purpose string is not a runtime API call you make at the moment of access. It is a <em>declaration</em> in your bundle’s Info.plist. The system reads the declaration, the system enforces the prompt, the system holds the user’s response. Your app never sees the access-denied branch unless the user explicitly denies. The framework around the declaration (the TCC daemon, the Settings → Privacy panel, the revoke flow) is system infrastructure your app doesn’t write any of. Permission becomes a <em>manifest entry</em>, not an API call.</p>
<p>That position is the direct ancestor of the <code>PrivacyInfo.xcprivacy</code> system that shipped in 2024. The 2024 manifest expanded the declared surface from <em>user-facing</em> permissions (Photos, Camera, Calendars) to <em>system-level</em> APIs that can be used for fingerprinting (UserDefaults, Disk Space, FileTimestamp, SystemBootTime), each declared with a numeric reason code that has to match one of Apple’s pre-approved buckets. The expansion was big, but the shape didn’t change. The same “you declare, the system enforces, the user can audit” contract that 710 codified in 2012 is the contract <code>PrivacyInfo.xcprivacy</code> codifies in 2024. The vocabulary grew. The position is the same one.</p>
<p>What I’d push back on, reading 710 back, is the just-in-time prompt convention that the talk doesn’t really argue for but that became the industry norm. The 2012 model is: ask when the user takes the action that needs the permission, not at launch. The talk gestures at this but doesn’t make it normative. The result is that, fourteen years later, you can find apps still prompting for every permission they might ever need at first launch, treating the prompts as a setup wizard. That pattern is worse for the user (it surfaces consent without context) and worse for the app (it inflates the deny rate). The convention the 2012 talk <em>should</em> have hammered harder is that permissions are coupled to actions, not to lifecycles. Apps that prompt for the Photos permission when the user taps “Add Photo” get higher grant rates than apps that prompt at launch. The model was already in 710; the discipline took another decade to spread.</p>
<h2 id="The-two-small-sessions-that-nobody-talks-about"><a href="#The-two-small-sessions-that-nobody-talks-about" class="headerlink" title="The two small sessions that nobody talks about"></a>The two small sessions that nobody talks about</h2><p>The Keychain talk (704) and the EventKit Reminders talk (304) are the ones I’d point at if asked to find the 2012 sessions whose APIs people are still writing directly against today.</p>
<p>The Keychain talk is a general Security Framework overview that crystallized the SecItem C API as the canonical place for credentials on both platforms. It covered Keychain, Secure Transport, certificate evaluation, and Common Crypto; iCloud Keychain itself wouldn’t ship until iOS 7 in 2013 (where <code>kSecAttrSynchronizable</code> would let a credential saved on one device appear on every other), so the 2012 talk’s contribution is the API floor rather than the sync story. The interesting part for me is that almost every Swift Keychain wrapper I’ve ever read is a thin sugar layer over the same C API the 2012 talk taught, with the same <code>kSecAttrAccessible</code> defaults (<code>WhenUnlocked</code> for things that should be unavailable while the device is locked, <code>AfterFirstUnlock</code> for things that need to be readable from a background launch before the user has unlocked the device that boot). In fifteen years, the API hasn’t moved; the wrappers around it have only changed names. A Keychain-based credential store on iOS 26 looks identical to one on iOS 6 because the underlying contract is.</p>
<p>The EventKit Reminders talk shipped <code>EKReminder</code> joining <code>EKEvent</code>, which gave apps permission-gated read&#x2F;write access to the system Reminders list. This is the one that ages best of any 2012 reveal, because the way to integrate “I want to remind the user about something at a time and a place” with the rest of the user’s life has not changed: you write to the system Reminders database via <code>EKEventStore</code>, the user sees it in the Reminders app and on Apple Watch and in Siri, and the user owns the reminder going forward (they can edit it, snooze it, mark it complete, share it via family Reminders lists). You don’t ship your own reminders UI; you contribute to the system one. The same architectural principle Apple was applying to maps and contacts in the same conference, applied to time-based reminders.</p>
<p>The category these two sessions point at is, I think, the most durable shape any platform API can have: <em>the system owns the data, your app contributes to it and reads from it via a typed API, the user has the final say over what happens.</em> Keychain, EventKit, Photos, Calendars, MapKit, CloudKit, Wallet: all of them are the same shape. The 2012 conference was the year this shape stopped being a few isolated APIs and started being the framework Apple was building everything around.</p>
<h2 id="The-pattern-across-all-of-it"><a href="#The-pattern-across-all-of-it" class="headerlink" title="The pattern across all of it"></a>The pattern across all of it</h2><p>If I had to compress the 2012 conference into one sentence, it would be: this is the year iOS stopped having “iOS versions” of Mac APIs and started having Mac-quality APIs that happened to also exist on iOS. Auto Layout, attributed strings, view controller containment, modern Core Data threading, Keychain, GCD: every one of these had a Mac-only or Mac-canonical version that iOS finally caught up to. The catching-up matters less in retrospect than the <em>standardization</em> it produced. By the end of WWDC 2012, the cross-platform Cocoa surface was symmetric enough that the same engineer could write the same kind of code on both platforms with the same vocabulary, and Apple could start building features (iCloud sync, Continuity, Universal Clipboard, Mac Catalyst, eventually visionOS) that assumed that symmetry as a baseline.</p>
<p>The other pattern, which I find more interesting because it’s about how Apple develops platforms, is that 2012 also shipped two products (Apple Maps, Core Data + iCloud) that were <em>wrong about the implementation but right about the model</em>. Both shipped with quality issues that became cautionary tales. Both had their implementations gutted and rewritten over the following years. Both inherited from the gutted version every assumption the 2012 reveal had argued for. The conceptual claim survived; the code didn’t.</p>
<p>I’ve come to think that’s the more useful retrospective lens than “did the launch go well.” A platform reveal is two assertions overlapping: a claim about the model (this is the shape the world should have) and a claim about the implementation (this is the code that holds that shape). The model is usually load-bearing forever, even when the implementation is replaced. Apple is unusually good at making the model claim correctly and patient about replacing the implementation underneath. iOS 7’s design system rests on Auto Layout’s 2012 reveal. <code>NSPersistentCloudKitContainer</code>‘s sync rests on the account-as-boundary claim of Core Data + iCloud. The modern map experience rests on the system-owned-data position of the original Apple Maps. None of those code paths still look like their 2012 originals, and the conceptual positions underneath them are unchanged from what shipped that June.</p>
<h2 id="If-you-want-to-read-the-originals"><a href="#If-you-want-to-read-the-originals" class="headerlink" title="If you want to read the originals"></a>If you want to read the originals</h2><p>Apple’s developer.apple.com still doesn’t host pre-2020 WWDC videos. Session metadata is at the <a href="https://nonstrict.eu/wwdcindex/wwdc2012/">nonstrict.eu WWDC Index</a> and the videos themselves live on the <a href="https://archive.org/search?query=wwdc+2012">Internet Archive</a>. About fifty minutes each on average.</p>
<p>The reading order I’d recommend if you want the arc this post traces:</p>
<ol>
<li><strong>200</strong> “What’s New in Cocoa Touch” for the umbrella view of what iOS 6 added</li>
<li><strong>202</strong> “Introduction to Auto Layout for iOS and OS X” for the constraint-as-equation framing, with <strong>228</strong> “Best Practices for Mastering Auto Layout” and <strong>232</strong> “Auto Layout by Example” as the two follow-ups</li>
<li><strong>222</strong> “Introduction to Attributed Strings for iOS” for the <code>UILabel</code>-takes-<code>NSAttributedString</code> change</li>
<li><strong>205</strong> “Introducing Collection Views” for the layout-as-data position</li>
<li><strong>236</strong> “The Evolution of View Controllers on iOS” for the containment contract</li>
<li><strong>214</strong> “Core Data Best Practices” for the queue-confined context model</li>
<li><strong>712</strong> “Asynchronous Design Patterns with Blocks, GCD, and XPC” for the queue-as-unit-of-concurrency argument</li>
<li><strong>300</strong> “Getting Around Using Map Kit” for the system-owned-data position</li>
<li><strong>710</strong> “Privacy Support in iOS and OS X” for the declarative-permission position</li>
<li><strong>227</strong> “Using iCloud with Core Data” if you want the historical context for why <code>NSPersistentCloudKitContainer</code> exists</li>
<li><strong>704</strong> “The Security Framework” and <strong>304</strong> “Events and Reminders in Event Kit” if you want to see the durable smaller APIs</li>
<li><strong>301</strong> “Introducing Passbook, Part 1” and <strong>309</strong> “Introducing Passbook, Part 2” if you want to see the file format that became Wallet</li>
</ol>
<p>The slides feel dated (the iOS 6 chrome on the demos, the brushed-metal AppKit windows in the Mac comparisons), but the framing maps cleanly onto code you’d write today, even where the method names have moved. Twelve years out, the part that holds up is the framing.</p>
<p>Previous in the series: <a href="/WWDC-2011-Revisited/" title="WWDC 2011 revisited: iCloud, ARC, and the year iOS stopped pretending the device was the world">WWDC 2011 revisited</a>.</p>
]]></content>
      <categories>
        <category>iOS</category>
        <category>WWDC</category>
      </categories>
      <tags>
        <tag>GCD</tag>
        <tag>history</tag>
        <tag>Core Data</tag>
        <tag>Auto Layout</tag>
        <tag>Keychain</tag>
        <tag>UICollectionView</tag>
        <tag>NSAttributedString</tag>
        <tag>MapKit</tag>
        <tag>EventKit</tag>
        <tag>Privacy</tag>
      </tags>
  </entry>
  <entry>
    <title>Storage everywhere: comparing on-device and cloud storage on iOS, Android, and Web in 2026</title>
    <url>/Memory-and-Storage-on-iOS-Android-and-Web-2026/</url>
    <content><![CDATA[<p>Every meaningful way to store data in 2026 across iOS, Android, and Web. On-device APIs, popular third-party libraries, and the cloud backends and sync engines that tie them together.</p>
<span id="more"></span>

<h2 id="Glossary"><a href="#Glossary" class="headerlink" title="Glossary"></a>Glossary</h2><p>Acronyms you’ll see throughout this post:</p>
<table>
<thead>
<tr>
<th>Acronym</th>
<th>Full name</th>
<th>What it means</th>
</tr>
</thead>
<tbody><tr>
<td><strong>KV</strong></td>
<td>Key-Value (store)</td>
<td>A simple <code>string -&gt; value</code> map (preferences, flags)</td>
</tr>
<tr>
<td><strong>ORM</strong></td>
<td>Object-Relational Mapper</td>
<td>Library that maps DB rows to objects (Core Data, Room, GRDB)</td>
</tr>
<tr>
<td><strong>BaaS</strong></td>
<td>Backend-as-a-Service</td>
<td>Hosted backend bundle: DB + auth + storage + functions (Firebase, Supabase)</td>
</tr>
<tr>
<td><strong>DBaaS</strong></td>
<td>Database-as-a-Service</td>
<td>Hosted database only (Neon, Turso, PlanetScale)</td>
</tr>
<tr>
<td><strong>RLS</strong></td>
<td>Row-Level Security</td>
<td>DB-side per-row access rules (Postgres feature, Supabase relies on it)</td>
</tr>
<tr>
<td><strong>CRDT</strong></td>
<td>Conflict-Free Replicated Data Type</td>
<td>Data structure that merges concurrent edits without conflicts (Yjs, Automerge)</td>
</tr>
<tr>
<td><strong>OPFS</strong></td>
<td>Origin Private File System</td>
<td>Per-origin sandboxed file system in browsers; substrate for SQLite-WASM</td>
</tr>
<tr>
<td><strong>WASM</strong></td>
<td>WebAssembly</td>
<td>Compiled binary format that runs in browsers (and elsewhere)</td>
</tr>
<tr>
<td><strong>SAF</strong></td>
<td>Storage Access Framework</td>
<td>Android’s user-driven file picker for files outside the app sandbox</td>
</tr>
<tr>
<td><strong>CHIPS</strong></td>
<td>Cookies Having Independent Partitioned State</td>
<td>Per-top-site cookie partitioning, Chrome’s third-party-cookie successor</td>
</tr>
<tr>
<td><strong>PWA</strong></td>
<td>Progressive Web App</td>
<td>Installable web app with offline support via Service Workers</td>
</tr>
<tr>
<td><strong>KMP</strong></td>
<td>Kotlin Multiplatform</td>
<td>Sharing Kotlin code across Android&#x2F;iOS&#x2F;JVM&#x2F;JS</td>
</tr>
<tr>
<td><strong>MAU</strong></td>
<td>Monthly Active Users</td>
<td>Common pricing unit for sync&#x2F;collab services</td>
</tr>
<tr>
<td><strong>EOL</strong></td>
<td>End-of-Life</td>
<td>Software no longer maintained; migrate off</td>
</tr>
</tbody></table>
<h2 id="The-landscape"><a href="#The-landscape" class="headerlink" title="The landscape"></a>The landscape</h2><p>Storage is not one decision – it’s at least four, often layered:</p>
<table>
<thead>
<tr>
<th>Layer</th>
<th>Question</th>
<th>Examples</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Preferences &#x2F; KV</strong></td>
<td>Where do tiny settings live?</td>
<td>UserDefaults, DataStore, localStorage</td>
</tr>
<tr>
<td><strong>Secrets</strong></td>
<td>Where do tokens live?</td>
<td>Keychain, Keystore, HttpOnly cookies</td>
</tr>
<tr>
<td><strong>Structured data</strong></td>
<td>Where does the app’s domain model live?</td>
<td>Core Data, Room, IndexedDB, SQLite</td>
</tr>
<tr>
<td><strong>Cloud &#x2F; sync</strong></td>
<td>How does it move between devices?</td>
<td>CloudKit, Firestore, Supabase, Replicache</td>
</tr>
</tbody></table>
<p>The structured-data and cloud-sync layers are merging. Local-first sync engines (PowerSync, Triplit, Replicache, ElectricSQL) treat the on-device DB as the source of truth and let the network catch up. SQLite (compiled to WASM, mirrored to the edge, or embedded everywhere) is the substrate underneath most of them.</p>
<h2 id="iOS"><a href="#iOS" class="headerlink" title="iOS"></a>iOS</h2><h3 id="System-Apple-provided"><a href="#System-Apple-provided" class="headerlink" title="System (Apple-provided)"></a>System (Apple-provided)</h3><table>
<thead>
<tr>
<th>API</th>
<th>What it is</th>
<th>Use case</th>
<th>Watch out for</th>
</tr>
</thead>
<tbody><tr>
<td><strong>UserDefaults</strong></td>
<td>Plist KV via cfprefsd</td>
<td>Preferences, flags, last-selected tab</td>
<td>Hard ~4 MB cap on App Group suites; not for blobs</td>
</tr>
<tr>
<td><strong>Keychain</strong></td>
<td>Encrypted store, Secure Enclave-backed</td>
<td>API keys, OAuth tokens, biometric secrets</td>
<td>Awful C API – always wrap; never put in exports</td>
</tr>
<tr>
<td><strong>FileManager &#x2F; sandbox</strong></td>
<td>Per-app <code>Documents/</code>, <code>Library/</code>, <code>tmp/</code></td>
<td>User docs, cached blobs, Live Photos</td>
<td>App Group containers for sharing with widgets&#x2F;extensions</td>
</tr>
<tr>
<td><strong>Core Data</strong></td>
<td>Object graph over SQLite</td>
<td>Complex models, lightweight + heavyweight migrations</td>
<td><code>NSPersistentStoreDescription.configuration</code> is a model lookup, not a label – setting a bogus name fails store load</td>
</tr>
<tr>
<td><strong>SwiftData</strong></td>
<td>Macro-driven <code>@Model</code> persistence (iOS 17+)</td>
<td>New apps, simple-to-moderate schemas</td>
<td>Still maturing – no merge policy API, no runtime CloudKit reconfiguration; many teams have rolled back to Core Data</td>
</tr>
<tr>
<td><strong>NSPersistentCloudKitContainer</strong></td>
<td>Core Data with automatic CloudKit mirroring</td>
<td>Family Sharing, multi-device sync without writing CloudKit code</td>
<td>Requires <code>UIBackgroundModes -&gt; remote-notification</code> AND <code>registerForRemoteNotifications()</code>; debounce remote-change notifications – they republish 60+ times during catchup</td>
</tr>
<tr>
<td><strong>CloudKit (direct)</strong></td>
<td><code>CKRecord</code>&#x2F;<code>CKAsset</code> against private&#x2F;shared&#x2F;public DBs</td>
<td>Public datasets, custom sync logic</td>
<td>Manual asset cleanup – cascade deletes don’t remove <code>CKAsset</code>s</td>
</tr>
<tr>
<td><strong>iCloud Documents</strong></td>
<td><code>NSMetadataQuery</code> document sync into Files.app</td>
<td>Pages-style document apps</td>
<td>Niche in 2026; most apps use CloudKit + custom UI</td>
</tr>
<tr>
<td><strong>App Group containers</strong></td>
<td>Shared sandbox for app + extensions</td>
<td>Widgets, share extensions, intents</td>
<td>Same 4 MB UserDefaults cap; use file URLs for blobs</td>
</tr>
<tr>
<td><strong>Plain SQLite (libsqlite3)</strong></td>
<td>Direct C API</td>
<td>Basically never anymore</td>
<td>Use GRDB or SQLite.swift instead</td>
</tr>
</tbody></table>
<h3 id="Third-party-iOS"><a href="#Third-party-iOS" class="headerlink" title="Third-party (iOS)"></a>Third-party (iOS)</h3><table>
<thead>
<tr>
<th>Library</th>
<th>What it is</th>
<th>Use case</th>
<th>Trade-offs</th>
</tr>
</thead>
<tbody><tr>
<td><strong>GRDB</strong></td>
<td>Swift-first SQLite wrapper</td>
<td>The default modern alternative to Core Data&#x2F;SwiftData</td>
<td>Best-in-class docs, type-safe, reactive observers, FTS5, SQLCipher; you write your own sync</td>
</tr>
<tr>
<td><strong>SQLite.swift</strong></td>
<td>Type-safe query builder over SQLite</td>
<td>Lightweight schemas</td>
<td>Less feature-rich than GRDB</td>
</tr>
<tr>
<td><strong>Realm</strong></td>
<td>Object DB with optional Atlas sync</td>
<td>Local Realm still works fine</td>
<td><strong>Atlas Device Sync sunset September 2025</strong> – new projects should not adopt it</td>
</tr>
<tr>
<td><strong>FMDB</strong></td>
<td>Objective-C SQLite wrapper</td>
<td>Legacy Obj-C codebases only</td>
<td>Maintenance mode</td>
</tr>
<tr>
<td><strong>Firestore</strong></td>
<td>Google NoSQL with offline cache</td>
<td>Cross-platform iOS+Android+web apps</td>
<td>~5 MB SDK weight; pricing scales with reads</td>
</tr>
<tr>
<td><strong>Supabase Swift</strong></td>
<td>Postgres BaaS client</td>
<td>Teams that want SQL + self-hostable escape</td>
<td>Less polished than the JS SDK; offline story is DIY</td>
</tr>
</tbody></table>
<h3 id="iOS-quick-picks-2026"><a href="#iOS-quick-picks-2026" class="headerlink" title="iOS quick picks (2026)"></a>iOS quick picks (2026)</h3><ul>
<li><strong>Settings&#x2F;flags</strong> -&gt; UserDefaults (under 4 MB)</li>
<li><strong>Secrets</strong> -&gt; Keychain</li>
<li><strong>Local-only relational</strong> -&gt; GRDB (modern) or Core Data (mature)</li>
<li><strong>CloudKit sync</strong> -&gt; Core Data + <code>NSPersistentCloudKitContainer</code>. SwiftData still has merge-policy gaps</li>
<li><strong>Cross-platform backend</strong> -&gt; Supabase (SQL) or Firestore (NoSQL)</li>
</ul>
<h2 id="Android"><a href="#Android" class="headerlink" title="Android"></a>Android</h2><h3 id="System-Google-provided"><a href="#System-Google-provided" class="headerlink" title="System (Google-provided)"></a>System (Google-provided)</h3><table>
<thead>
<tr>
<th>API</th>
<th>What it is</th>
<th>Use case</th>
<th>Watch out for</th>
</tr>
</thead>
<tbody><tr>
<td><strong>SharedPreferences</strong></td>
<td>XML-backed sync KV</td>
<td>Tiny prefs</td>
<td>Effectively legacy – main-thread I&#x2F;O, no transactions</td>
</tr>
<tr>
<td><strong>DataStore</strong></td>
<td>Coroutine&#x2F;Flow KV (Preferences) or schema-typed (Proto)</td>
<td>New code’s KV layer</td>
<td>Async-only; not a queryable DB</td>
</tr>
<tr>
<td><strong>EncryptedSharedPreferences</strong></td>
<td>KV with at-rest encryption</td>
<td>–</td>
<td><strong>Deprecated April 2025, no first-party successor.</strong> Roll your own with Keystore + Tink</td>
</tr>
<tr>
<td><strong>Internal storage (<code>filesDir</code>, <code>cacheDir</code>)</strong></td>
<td>App-private sandbox</td>
<td>Caches, DB files, downloaded models</td>
<td><code>cacheDir</code> is reclaimed without notice</td>
</tr>
<tr>
<td><strong>Scoped storage + MediaStore</strong></td>
<td>Shared media via collection APIs</td>
<td>Photos, videos, downloads</td>
<td>Migration pain is the #1 Android storage complaint – <code>WRITE_EXTERNAL_STORAGE</code> is a no-op on API 30+</td>
</tr>
<tr>
<td><strong>Storage Access Framework (SAF)</strong></td>
<td>System file picker returning persistable URIs</td>
<td>Cloud providers, USB, SD card</td>
<td>UX friction – every read goes through a picker</td>
</tr>
<tr>
<td><strong>Raw SQLite</strong></td>
<td><code>SQLiteOpenHelper</code></td>
<td>Almost never anymore</td>
<td>Use Room</td>
</tr>
<tr>
<td><strong>Room</strong></td>
<td>Jetpack ORM with codegen, now KMP</td>
<td>Relational app data, offline cache</td>
<td>Don’t wrap suspend DAOs in <code>withContext(Dispatchers.IO)</code> – Room manages its own dispatcher</td>
</tr>
<tr>
<td><strong>ContentProvider</strong></td>
<td>Cross-process data interface</td>
<td><code>FileProvider</code> for sharing, system providers</td>
<td>Overkill for in-app use</td>
</tr>
<tr>
<td><strong>Android Keystore</strong></td>
<td>Hardware-backed key container</td>
<td>Wrapping symmetric keys, biometric-gated keys</td>
<td>Keys can be invalidated on biometric enrollment changes</td>
</tr>
<tr>
<td><strong>Credential Manager</strong></td>
<td>Unified passkey&#x2F;password&#x2F;federated auth</td>
<td>All new auth flows</td>
<td>Requires Play Services – fallback needed on Huawei, etc. BiometricPrompt still used for in-app gating</td>
</tr>
<tr>
<td><strong>Auto Backup</strong></td>
<td>Free 25 MB cloud backup to user’s Drive</td>
<td>Restore-on-reinstall</td>
<td>Exclude secrets via <code>backup_rules.xml</code></td>
</tr>
</tbody></table>
<h3 id="Third-party-Android"><a href="#Third-party-Android" class="headerlink" title="Third-party (Android)"></a>Third-party (Android)</h3><table>
<thead>
<tr>
<th>Library</th>
<th>What it is</th>
<th>Use case</th>
<th>Trade-offs</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Realm Kotlin</strong></td>
<td>Live-object DB</td>
<td>KMP, offline-capable</td>
<td>Atlas sync deprecated – local only</td>
</tr>
<tr>
<td><strong>ObjectBox</strong></td>
<td>NoSQL object DB</td>
<td>Fastest benchmarks, vector search</td>
<td>Smaller community, commercial sync</td>
</tr>
<tr>
<td><strong>SQLDelight</strong></td>
<td>Codegen from <code>.sq</code> files</td>
<td>KMP-first, type-safe</td>
<td>More boilerplate than Room; no auto-migrations</td>
</tr>
<tr>
<td><strong>Supabase Kotlin</strong></td>
<td>Postgres BaaS client</td>
<td>SQL + self-hostable</td>
<td>Realtime less mature than Firestore</td>
</tr>
<tr>
<td><strong>AWS Amplify</strong></td>
<td>DataStore + Auth + Storage</td>
<td>Deep AWS integration</td>
<td>Heavy SDK, AWS lock-in</td>
</tr>
<tr>
<td><strong>PowerSync</strong></td>
<td>SQLite-based sync over Postgres&#x2F;MongoDB&#x2F;MySQL (SQL Server in alpha)</td>
<td>Offline-first, BYO backend</td>
<td>Newer, smaller ecosystem</td>
</tr>
</tbody></table>
<h3 id="Major-Android-shifts-to-call-out"><a href="#Major-Android-shifts-to-call-out" class="headerlink" title="Major Android shifts to call out"></a>Major Android shifts to call out</h3><ol>
<li><strong>SharedPreferences -&gt; DataStore</strong> is now the official path for new code</li>
<li><strong>Scoped storage</strong> is fully enforced; <code>MANAGE_EXTERNAL_STORAGE</code> is policy-restricted on Play</li>
<li><strong>Credential Manager</strong> consolidates passkeys, passwords, federated sign-in</li>
<li><strong>EncryptedSharedPreferences</strong> has no first-party successor – teams roll their own</li>
<li><strong>Room is KMP</strong> since the stable 2.7.0 release in April 2025, narrowing SQLDelight’s main differentiator</li>
</ol>
<h2 id="Web"><a href="#Web" class="headerlink" title="Web"></a>Web</h2><h3 id="Standards-based"><a href="#Standards-based" class="headerlink" title="Standards-based"></a>Standards-based</h3><table>
<thead>
<tr>
<th>API</th>
<th>What it is</th>
<th>Use case</th>
<th>Watch out for</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Cookies</strong></td>
<td>~4 KB&#x2F;cookie, sent on every request</td>
<td>Auth tokens, CSRF, sessions</td>
<td><code>SameSite=Lax</code> default, <code>HttpOnly</code>&#x2F;<code>Secure</code> for auth, <strong>CHIPS (<code>Partitioned</code>)</strong> for cross-site embeds</td>
</tr>
<tr>
<td><strong>localStorage</strong></td>
<td>Sync string KV, ~5-10 MB</td>
<td>Tiny prefs, flags</td>
<td>Blocks main thread; Safari ITP evicts after 7 days without user engagement</td>
</tr>
<tr>
<td><strong>sessionStorage</strong></td>
<td>Same API, tab-scoped</td>
<td>Wizard state</td>
<td>Lost on tab close</td>
</tr>
<tr>
<td><strong>IndexedDB</strong></td>
<td>Async transactional structured store</td>
<td>The real browser DB</td>
<td>Verbose API; quotas in 100s of MB to GB</td>
</tr>
<tr>
<td><strong>Cache API</strong></td>
<td>Request&#x2F;Response storage in Service Workers</td>
<td>PWA offline shells</td>
<td>Counts against same origin quota</td>
</tr>
<tr>
<td><strong>Storage API</strong></td>
<td><code>navigator.storage.estimate()</code>, <code>persist()</code></td>
<td>Resist eviction</td>
<td>Always call <code>persist()</code> for local-first apps</td>
</tr>
<tr>
<td><strong>OPFS</strong></td>
<td>Per-origin sandboxed file system, sync access in workers</td>
<td>Substrate for SQLite-WASM</td>
<td>Basic API universal since 2023 (Chrome 102, Safari 15.2, Firefox 111); <code>FileSystemSyncAccessHandle</code> (the perf-critical sync API) lagged in Firefox</td>
</tr>
<tr>
<td><strong>File System Access API</strong></td>
<td><code>showOpenFilePicker</code>, etc.</td>
<td>Real user-visible files</td>
<td><strong>Chromium-only</strong> in 2026</td>
</tr>
<tr>
<td><strong>WebSQL</strong></td>
<td>–</td>
<td>–</td>
<td><strong>Removed.</strong> Deprecated in Chromium 119 (Nov 2023), gone everywhere by early 2024</td>
</tr>
<tr>
<td><strong>Storage Buckets API</strong></td>
<td>Per-bucket eviction policies</td>
<td>Future-proofing</td>
<td>Chrome-stable; Firefox positive signal but not shipped; Safari has no signal</td>
</tr>
</tbody></table>
<h3 id="Library-wrapper-layer"><a href="#Library-wrapper-layer" class="headerlink" title="Library &#x2F; wrapper layer"></a>Library &#x2F; wrapper layer</h3><table>
<thead>
<tr>
<th>Library</th>
<th>What it is</th>
<th>Use case</th>
<th>Trade-offs</th>
</tr>
</thead>
<tbody><tr>
<td><strong>localForage</strong></td>
<td>Promise KV with IDB fallback</td>
<td>Drop-in localStorage upgrade</td>
<td>Mature, low-churn</td>
</tr>
<tr>
<td><strong>idb</strong></td>
<td>Thin promise wrapper over IDB</td>
<td>Raw IDB without ceremony</td>
<td>Default when you don’t need an ORM</td>
</tr>
<tr>
<td><strong>Dexie.js</strong></td>
<td>IDB ORM with queries, hooks, live queries</td>
<td>Most popular IDB layer</td>
<td>Sync addon available</td>
</tr>
<tr>
<td><strong>PouchDB</strong></td>
<td>CouchDB-compatible, master-master sync</td>
<td>Apps with CouchDB backend</td>
<td>Feels dated</td>
</tr>
<tr>
<td><strong>RxDB</strong></td>
<td>Reactive schema-based DB, multiple adapters</td>
<td>Offline-first apps</td>
<td>Strong 2026 choice; multiple storage backends including OPFS-SQLite</td>
</tr>
<tr>
<td><strong>WatermelonDB</strong></td>
<td>Lazy-loaded reactive DB</td>
<td>React Native + web</td>
<td>RN-first</td>
</tr>
<tr>
<td><strong>TanStack Query &#x2F; SWR</strong></td>
<td>In-memory server cache</td>
<td>Server-state caching</td>
<td>Optional <code>persistQueryClient</code> to IDB</td>
</tr>
<tr>
<td><strong>Apollo Client &#x2F; Relay</strong></td>
<td>GraphQL normalized stores</td>
<td>GraphQL apps</td>
<td><code>apollo3-cache-persist</code> for persistence</td>
</tr>
<tr>
<td><strong>Zustand persist &#x2F; redux-persist</strong></td>
<td>State persistence middleware</td>
<td>Mirror store to localStorage&#x2F;IDB</td>
<td>Trivial to add</td>
</tr>
<tr>
<td><strong>Yjs</strong></td>
<td>CRDT library</td>
<td>Collaborative text&#x2F;structured docs</td>
<td>Default CRDT pick; tiny, fast, huge ecosystem</td>
</tr>
<tr>
<td><strong>Automerge</strong></td>
<td>JSON-shaped CRDT</td>
<td>Richer document model</td>
<td>v3 (Rust core) closed perf gaps with Yjs</td>
</tr>
<tr>
<td><strong>sqlite-wasm</strong></td>
<td>Official SQLite WASM build with OPFS VFS</td>
<td>Serious local-first apps</td>
<td>Near-native perf in modern browsers</td>
</tr>
<tr>
<td><strong>Drizzle (SQLite-WASM adapter)</strong></td>
<td>Typed SQL ORM</td>
<td>TypeScript SQL in the browser</td>
<td>Pairs with <code>sqlite-wasm</code></td>
</tr>
</tbody></table>
<h3 id="Sync-engines-where-2026-actually-moved"><a href="#Sync-engines-where-2026-actually-moved" class="headerlink" title="Sync engines (where 2026 actually moved)"></a>Sync engines (where 2026 actually moved)</h3><table>
<thead>
<tr>
<th>Engine</th>
<th>What it is</th>
<th>Trade-offs</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Replicache</strong></td>
<td>Push-pull mutator protocol; you bring the backend</td>
<td>GA in 2024; battle-tested, commercial license, free under $200K ARR. Reflect (the multiplayer cousin) shut down Nov 2024</td>
</tr>
<tr>
<td><strong>Zero</strong> (Rocicorp)</td>
<td>Replicache successor: client-server with local cache, sync queries, custom mutators</td>
<td>Alpha through 2025, beta target late-2025&#x2F;early-2026. Not local-first (no offline writes), but instant reads&#x2F;writes against the cache</td>
</tr>
<tr>
<td><strong>ElectricSQL</strong></td>
<td>Postgres -&gt; local SQLite read-path sync; writes are app-owned. Hosted “Electric Cloud” Durable Streams launched Jan 2026</td>
<td>OSS Apache-2.0; explicitly does not do write-path sync</td>
</tr>
<tr>
<td><strong>PowerSync</strong></td>
<td>Postgres&#x2F;MongoDB&#x2F;MySQL&#x2F;SQL Server (alpha) &lt;-&gt; on-device SQLite</td>
<td>Production-grade, strong mobile SDKs, commercial with free tier</td>
</tr>
<tr>
<td><strong>Triplit</strong></td>
<td>TS-first sync engine + relational DB; 1.0 GA</td>
<td>Momentum among indie web devs; OSS AGPL&#x2F;commercial</td>
</tr>
<tr>
<td><strong>InstantDB</strong></td>
<td>Firebase-shaped client-side reactive DB</td>
<td>Triple-store model; web&#x2F;RN</td>
</tr>
<tr>
<td><strong>Liveblocks</strong></td>
<td>Presence + collab document primitives</td>
<td>Per-MAU pricing</td>
</tr>
<tr>
<td><strong>Yjs + Hocuspocus</strong></td>
<td>CRDT + production-grade server</td>
<td>Open-source collab stack</td>
</tr>
</tbody></table>
<h2 id="Cross-platform-cloud"><a href="#Cross-platform-cloud" class="headerlink" title="Cross-platform cloud"></a>Cross-platform cloud</h2><p>These are the cloud backends that work across iOS, Android, and Web – the ones you reach for when “where does this sync to?” matters more than “what platform am I on?”</p>
<h3 id="Backend-as-a-Service-BaaS"><a href="#Backend-as-a-Service-BaaS" class="headerlink" title="Backend-as-a-Service (BaaS)"></a>Backend-as-a-Service (BaaS)</h3><table>
<thead>
<tr>
<th>Service</th>
<th>Stack</th>
<th>Best for</th>
<th>Trade-offs</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Firebase</strong> (Firestore&#x2F;RTDB&#x2F;Storage&#x2F;Auth)</td>
<td>Google NoSQL + auth + blob</td>
<td>Default for hobbyists and indie mobile</td>
<td>Generous free tier, vendor lock-in, costs scale unpredictably with reads</td>
</tr>
<tr>
<td><strong>Supabase</strong></td>
<td>Postgres + PostgREST + Realtime + Auth + Storage</td>
<td>Open-source Firebase alternative; SQL-first</td>
<td>Real Postgres, RLS, self-hostable; mobile SDKs less polished than web</td>
</tr>
<tr>
<td><strong>Convex</strong></td>
<td>TS-first reactive DB</td>
<td>End-to-end TypeScript apps</td>
<td>Live queries free, transactional; mobile-native is second-class</td>
</tr>
<tr>
<td><strong>Appwrite</strong></td>
<td>Self-hostable BaaS</td>
<td>Teams wanting full control</td>
<td>Smaller community than Supabase</td>
</tr>
<tr>
<td><strong>PocketBase</strong></td>
<td>Single Go binary, embedded SQLite</td>
<td>Solo devs, side projects</td>
<td>Single-node by design; not for high-scale</td>
</tr>
<tr>
<td><strong>AWS Amplify</strong></td>
<td>DataStore + AppSync + S3</td>
<td>AWS-committed shops</td>
<td>Notoriously complex DX; conflict-resolution model has burned teams</td>
</tr>
<tr>
<td><strong>Parse Platform</strong></td>
<td>Legacy Facebook-era BaaS</td>
<td>Maintenance-mode apps</td>
<td>Community-maintained</td>
</tr>
</tbody></table>
<h3 id="Object-blob-storage-S3-compatible-is-the-lingua-franca"><a href="#Object-blob-storage-S3-compatible-is-the-lingua-franca" class="headerlink" title="Object &#x2F; blob storage (S3-compatible is the lingua franca)"></a>Object &#x2F; blob storage (S3-compatible is the lingua franca)</h3><table>
<thead>
<tr>
<th>Service</th>
<th>Notable</th>
<th>Why pick it</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Cloudflare R2</strong></td>
<td><strong>Zero egress fees</strong></td>
<td>New default for serving user content cheaply</td>
</tr>
<tr>
<td><strong>Backblaze B2</strong></td>
<td>Cheapest per-GB-stored</td>
<td>Free egress via Bandwidth Alliance with Cloudflare</td>
</tr>
<tr>
<td><strong>AWS S3</strong></td>
<td>The reference</td>
<td>Pick when you have a reason to pay AWS egress</td>
</tr>
<tr>
<td><strong>Google Cloud Storage</strong></td>
<td>Integrates with GCP&#x2F;Firebase</td>
<td>Same shape as S3, similar pricing</td>
</tr>
<tr>
<td><strong>Azure Blob Storage</strong></td>
<td>Microsoft equivalent</td>
<td>Pick if you’re already in Azure</td>
</tr>
<tr>
<td><strong>Wasabi</strong></td>
<td>Flat-rate, no egress fees</td>
<td>Backups, media archives</td>
</tr>
</tbody></table>
<h3 id="Database-as-a-Service"><a href="#Database-as-a-Service" class="headerlink" title="Database-as-a-Service"></a>Database-as-a-Service</h3><table>
<thead>
<tr>
<th>Service</th>
<th>What it is</th>
<th>Status</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Neon</strong></td>
<td>Serverless Postgres with branching, scale-to-zero</td>
<td>Generous free tier, healthy</td>
</tr>
<tr>
<td><strong>Turso</strong></td>
<td>libSQL (SQLite fork) at the edge</td>
<td>Insanely fast reads, cheap; OSS</td>
</tr>
<tr>
<td><strong>Cloudflare D1</strong></td>
<td>SQLite at the edge inside Workers</td>
<td>Tightly coupled to CF stack</td>
</tr>
<tr>
<td><strong>MongoDB Atlas</strong></td>
<td>Hosted Mongo</td>
<td>Mature, expensive at scale</td>
</tr>
<tr>
<td><strong>PlanetScale</strong></td>
<td>Vitess-backed MySQL with branching</td>
<td>Killed free tier in 2024; paid-only</td>
</tr>
<tr>
<td><strong>Upstash</strong></td>
<td>Serverless Redis&#x2F;Kafka&#x2F;QStash</td>
<td>Best for caches&#x2F;queues, not primary store</td>
</tr>
<tr>
<td><strong>Fauna</strong></td>
<td>Serverless distributed DB with FQL</td>
<td><strong>Public service shut down May 2025</strong> – effectively dead for indie devs</td>
</tr>
</tbody></table>
<h2 id="Deep-dive-mobile-clients"><a href="#Deep-dive-mobile-clients" class="headerlink" title="Deep dive: mobile clients"></a>Deep dive: mobile clients</h2><p>The previous tables list the options. This section answers the practical questions: <em>what does the code actually look like?</em> <em>how does sync actually work?</em> <em>what are the failure modes?</em></p>
<h3 id="Persistence-same-model-three-iOS-implementations"><a href="#Persistence-same-model-three-iOS-implementations" class="headerlink" title="Persistence: same model, three iOS implementations"></a>Persistence: same model, three iOS implementations</h3><p>Imagine a <code>Pet</code> entity with <code>id</code>, <code>name</code>, <code>species</code>. Here’s the same fetch in each major option.</p>
<p><strong>Core Data</strong> (object graph, mature, verbose):</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> request <span class="operator">=</span> <span class="type">Pet</span>.fetchRequest()</span><br><span class="line">request.predicate <span class="operator">=</span> <span class="type">NSPredicate</span>(format: <span class="string">&quot;species == %@&quot;</span>, <span class="string">&quot;dog&quot;</span>)</span><br><span class="line">request.sortDescriptors <span class="operator">=</span> [<span class="type">NSSortDescriptor</span>(key: <span class="string">&quot;name&quot;</span>, ascending: <span class="literal">true</span>)]</span><br><span class="line"><span class="keyword">let</span> dogs <span class="operator">=</span> <span class="keyword">try</span> context.fetch(request)</span><br></pre></td></tr></table></figure>

<p><strong>SwiftData</strong> (concise, but still maturing):</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Model</span> <span class="keyword">class</span> <span class="title class_">Pet</span> &#123;</span><br><span class="line">    <span class="meta">@Attribute</span>(.unique) <span class="keyword">var</span> id: <span class="type">UUID</span></span><br><span class="line">    <span class="keyword">var</span> name: <span class="type">String</span></span><br><span class="line">    <span class="keyword">var</span> species: <span class="type">String</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Query</span>(filter: #<span class="type">Predicate</span> &#123; <span class="variable">$0</span>.species <span class="operator">==</span> <span class="string">&quot;dog&quot;</span> &#125;, sort: \.name)</span><br><span class="line"><span class="keyword">var</span> dogs: [<span class="type">Pet</span>]</span><br></pre></td></tr></table></figure>

<p><strong>GRDB</strong> (SQL-first, type-safe via <code>Codable</code>):</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Pet</span>: <span class="title class_ inherited__">Codable</span>, <span class="title class_ inherited__">FetchableRecord</span>, <span class="title class_ inherited__">PersistableRecord</span> &#123;</span><br><span class="line">    <span class="keyword">var</span> id: <span class="type">UUID</span></span><br><span class="line">    <span class="keyword">var</span> name: <span class="type">String</span></span><br><span class="line">    <span class="keyword">var</span> species: <span class="type">String</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> dogs <span class="operator">=</span> <span class="keyword">try</span> dbQueue.read &#123; db <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">try</span> <span class="type">Pet</span>.filter(<span class="type">Column</span>(<span class="string">&quot;species&quot;</span>) <span class="operator">==</span> <span class="string">&quot;dog&quot;</span>)</span><br><span class="line">            .order(<span class="type">Column</span>(<span class="string">&quot;name&quot;</span>))</span><br><span class="line">            .fetchAll(db)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>The trade-off triangle: Core Data is the most powerful but the most verbose; SwiftData is the most concise but has gaps (no <code>NSMergePolicy</code> equivalent, awkward CloudKit reconfiguration); GRDB gives you full SQL with Swift ergonomics but you write your own sync.</p>
<h3 id="Persistence-same-model-two-Android-implementations"><a href="#Persistence-same-model-two-Android-implementations" class="headerlink" title="Persistence: same model, two Android implementations"></a>Persistence: same model, two Android implementations</h3><p><strong>Room</strong> (JetPack ORM, the default):</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span> <span class="keyword">data</span> <span class="keyword">class</span> <span class="title class_">Pet</span>(</span><br><span class="line">    <span class="meta">@PrimaryKey</span> <span class="keyword">val</span> id: String,</span><br><span class="line">    <span class="keyword">val</span> name: String,</span><br><span class="line">    <span class="keyword">val</span> species: String,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="meta">@Dao</span> <span class="keyword">interface</span> <span class="title class_">PetDao</span> &#123;</span><br><span class="line">    <span class="meta">@Query(<span class="string">&quot;SELECT * FROM Pet WHERE species = :species ORDER BY name&quot;</span>)</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">dogs</span><span class="params">(species: <span class="type">String</span> = <span class="string">&quot;dog&quot;</span>)</span></span>: Flow&lt;List&lt;Pet&gt;&gt;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>SQLDelight</strong> (codegen from <code>.sq</code> files, KMP-friendly):</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- src/commonMain/sqldelight/Pet.sq</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> Pet (id TEXT <span class="keyword">PRIMARY KEY</span>, name TEXT, species TEXT);</span><br><span class="line"></span><br><span class="line">selectDogs:</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> Pet <span class="keyword">WHERE</span> species <span class="operator">=</span> <span class="string">&#x27;dog&#x27;</span> <span class="keyword">ORDER</span> <span class="keyword">BY</span> name;</span><br></pre></td></tr></table></figure>

<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> dogs: Flow&lt;List&lt;Pet&gt;&gt; = database.petQueries.selectDogs().asFlow().mapToList()</span><br></pre></td></tr></table></figure>

<p>Room won the Android-only fight thanks to deeper Jetpack integration; SQLDelight’s edge is sharing the schema with iOS in KMP projects (and Room’s KMP support, while now stable, still feels less mature for non-Android targets).</p>
<h3 id="Sync-three-architectural-shapes"><a href="#Sync-three-architectural-shapes" class="headerlink" title="Sync: three architectural shapes"></a>Sync: three architectural shapes</h3><p>What “sync” actually means changes a lot depending on which model you pick.</p>
<p><strong>1. Apple’s CloudKit model (<code>NSPersistentCloudKitContainer</code>)</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[Device A: Core Data] &lt;-&gt; [iCloud Private DB] &lt;-&gt; [Device B: Core Data]</span><br><span class="line">                              ^</span><br><span class="line">                              | silent push (APNs)</span><br><span class="line">                              v</span><br><span class="line">                    NSPersistentStoreRemoteChange</span><br></pre></td></tr></table></figure>

<ul>
<li>Mirroring is automatic; you keep using normal <code>NSManagedObject</code> APIs.</li>
<li>Conflict resolution is <strong>last-writer-wins</strong> with no override (unlike Core Data’s standalone <code>NSMergePolicy</code>).</li>
<li>Free for end users, no backend cost for you, <strong>Apple-only</strong>.</li>
<li>Real gotcha: <code>NSPersistentStoreRemoteChange</code> republishes 60+ times during boot catchup – debounce any whole-store consumer (Spotlight reindex, exports), or you’ll churn pointlessly.</li>
</ul>
<p><strong>2. Firebase Firestore model</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[Device A: Firestore SDK + IDB cache] ─┐</span><br><span class="line">                                       v</span><br><span class="line">                          [Firestore: server-authoritative]</span><br><span class="line">                                       ^</span><br><span class="line">[Device B: Firestore SDK + IDB cache] ─┘</span><br><span class="line">                       (snapshot listeners stream changes)</span><br></pre></td></tr></table></figure>

<ul>
<li>Offline cache is LRU, ~100 MB by default on mobile; lossy across logout&#x2F;reinstall.</li>
<li>Server-authoritative – the cloud is truth, the device is a cache.</li>
<li>Pricing scales with read count, not data size: a per-document listener that fires on every change can be expensive at scale.</li>
</ul>
<p><strong>3. PowerSync &#x2F; Triplit &#x2F; Replicache (local-first)</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[Device A: SQLite (truth)]        [Device B: SQLite (truth)]</span><br><span class="line">     |                                 ^</span><br><span class="line">     | mutation queue                  | streamed changes</span><br><span class="line">     v                                 |</span><br><span class="line">     +------&gt; [Sync engine] -----------+</span><br><span class="line">                    |</span><br><span class="line">                    v</span><br><span class="line">             [Postgres / Mongo]</span><br></pre></td></tr></table></figure>

<ul>
<li>Local SQLite is the source of truth – the UI never waits on the network.</li>
<li>Mutations are queued locally and uploaded asynchronously.</li>
<li>The server applies them, then broadcasts deltas to other clients.</li>
<li>Conflict resolution is the engine’s responsibility (CRDT-flavored or transactional, depending).</li>
<li>The trade: setup complexity. You’re operating a sync server (or paying someone to).</li>
</ul>
<h3 id="Secret-storage-what-to-actually-use"><a href="#Secret-storage-what-to-actually-use" class="headerlink" title="Secret storage: what to actually use"></a>Secret storage: what to actually use</h3><table>
<thead>
<tr>
<th>Platform</th>
<th>Primitive</th>
<th>Practical pattern</th>
</tr>
</thead>
<tbody><tr>
<td>iOS</td>
<td>Keychain</td>
<td>Wrap the C API (<code>KeychainHelper</code>); <code>kSecAttrAccessControl</code> for biometric-gated tokens; never put secrets in <code>UserDefaults</code> or exports</td>
</tr>
<tr>
<td>Android</td>
<td>Keystore</td>
<td>Keystore stores <strong>keys</strong>, not arbitrary secrets. Generate an AES-GCM key with <code>KeyGenParameterSpec.Builder.setUserAuthenticationRequired(true)</code>, encrypt the secret, store ciphertext in DataStore or a file</td>
</tr>
<tr>
<td>Both</td>
<td>Tink</td>
<td>Google’s crypto library; the recommended successor for the deprecated <code>EncryptedSharedPreferences</code>. Wraps Keystore-backed AES-GCM with sensible defaults</td>
</tr>
</tbody></table>
<p>iOS Keychain wrapper sketch:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="type">SecItemAdd</span>([</span><br><span class="line">    kSecClass: kSecClassGenericPassword,</span><br><span class="line">    kSecAttrService: <span class="string">&quot;com.example.app&quot;</span>,</span><br><span class="line">    kSecAttrAccount: <span class="string">&quot;auth_token&quot;</span>,</span><br><span class="line">    kSecValueData: tokenData,</span><br><span class="line">    kSecAttrAccessible: kSecAttrAccessibleWhenUnlockedThisDeviceOnly,</span><br><span class="line">] <span class="keyword">as</span> <span class="type">CFDictionary</span>, <span class="literal">nil</span>)</span><br></pre></td></tr></table></figure>

<p>Android Keystore + Tink sketch:</p>
<figure class="highlight kotlin"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> aead = AeadConfig.register()</span><br><span class="line"><span class="keyword">val</span> keyset = AndroidKeysetManager.Builder()</span><br><span class="line">    .withSharedPref(context, <span class="string">&quot;keyset_name&quot;</span>, <span class="string">&quot;master_key_pref&quot;</span>)</span><br><span class="line">    .withKeyTemplate(KeyTemplates.<span class="keyword">get</span>(<span class="string">&quot;AES256_GCM&quot;</span>))</span><br><span class="line">    .withMasterKeyUri(<span class="string">&quot;android-keystore://master_key&quot;</span>)</span><br><span class="line">    .build()</span><br><span class="line">    .keysetHandle</span><br><span class="line"><span class="keyword">val</span> ciphertext = keyset.getPrimitive(Aead::<span class="keyword">class</span>.java).encrypt(plaintext, <span class="literal">null</span>)</span><br></pre></td></tr></table></figure>

<h3 id="Schema-migrations-what-happens-when-the-model-changes"><a href="#Schema-migrations-what-happens-when-the-model-changes" class="headerlink" title="Schema migrations: what happens when the model changes"></a>Schema migrations: what happens when the model changes</h3><table>
<thead>
<tr>
<th>Stack</th>
<th>Migration model</th>
<th>Reality check</th>
</tr>
</thead>
<tbody><tr>
<td>Core Data</td>
<td>Lightweight (auto) for additive changes; <code>NSMappingModel</code> for everything else</td>
<td>Lightweight handles most real-world changes; heavyweight is painful but works</td>
</tr>
<tr>
<td>SwiftData</td>
<td><code>VersionedSchema</code> + <code>SchemaMigrationPlan</code> with <code>MigrationStage.lightweight</code> &#x2F; <code>.custom</code></td>
<td>Newer, fewer war stories; large schemas have hit edge cases</td>
</tr>
<tr>
<td>Room</td>
<td><code>@Database(version = N)</code> + named <code>Migration(from, to)</code> with raw SQL</td>
<td>Predictable; you write SQL by hand. Auto-migrations exist but only for trivial changes</td>
</tr>
<tr>
<td>GRDB</td>
<td><code>DatabaseMigrator</code> with named, ordered, idempotent steps</td>
<td>Best-in-class – migrations are first-class citizens, easy to test</td>
</tr>
<tr>
<td>Realm</td>
<td><code>migrationBlock</code> with old&#x2F;new schema versions</td>
<td>Rigid; missing-field migrations require manual handling</td>
</tr>
</tbody></table>
<h3 id="Binary-footprint-reality-check"><a href="#Binary-footprint-reality-check" class="headerlink" title="Binary footprint reality check"></a>Binary footprint reality check</h3><table>
<thead>
<tr>
<th>SDK</th>
<th>Approximate APK &#x2F; IPA cost</th>
</tr>
</thead>
<tbody><tr>
<td>GRDB</td>
<td>~800 KB</td>
</tr>
<tr>
<td>SQLDelight</td>
<td>&lt;1 MB</td>
</tr>
<tr>
<td>Realm Kotlin</td>
<td>3-4 MB</td>
</tr>
<tr>
<td>ObjectBox</td>
<td>1-2 MB</td>
</tr>
<tr>
<td>Firebase Firestore (modular)</td>
<td>4-6 MB</td>
</tr>
<tr>
<td>AWS Amplify</td>
<td>5+ MB</td>
</tr>
<tr>
<td>Room &#x2F; DataStore</td>
<td>bundled with Jetpack (negligible)</td>
</tr>
<tr>
<td>Core Data &#x2F; SwiftData &#x2F; UserDefaults &#x2F; Keychain</td>
<td>system-provided (zero)</td>
</tr>
</tbody></table>
<p>Footprint matters more than people remember on Android, where Play store size limits and download conversion rates put pressure on every megabyte.</p>
<h2 id="Deep-dive-web"><a href="#Deep-dive-web" class="headerlink" title="Deep dive: web"></a>Deep dive: web</h2><p>The web stack changed more than mobile in the last two years. Here’s what the local-first architecture actually looks like, and the patterns that come with it.</p>
<h3 id="The-local-first-stack-in-one-diagram"><a href="#The-local-first-stack-in-one-diagram" class="headerlink" title="The local-first stack, in one diagram"></a>The local-first stack, in one diagram</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">    [React / Svelte / etc.]</span><br><span class="line">             |</span><br><span class="line">             | reactive query (live)</span><br><span class="line">             v</span><br><span class="line">[SQLite-WASM running in OPFS]   &lt;-- source of truth</span><br><span class="line">             |</span><br><span class="line">             | mutation queue</span><br><span class="line">             v</span><br><span class="line">[Sync engine: Triplit / Replicache / PowerSync / ElectricSQL]</span><br><span class="line">             |</span><br><span class="line">             | WebSocket</span><br><span class="line">             v</span><br><span class="line">        [Postgres / D1 / Turso]</span><br></pre></td></tr></table></figure>

<p>Three things to internalise:</p>
<ol>
<li><strong>The local DB is the truth.</strong> UI reads and writes it directly; queries are live (re-fire on changes).</li>
<li><strong>Sync is asynchronous.</strong> Mutations are queued locally; the engine handles upload, conflict resolution, and downstream rebroadcast.</li>
<li><strong>The server is for durability and fan-out.</strong> You can lose the network for hours and the app keeps working.</li>
</ol>
<h3 id="sqlite-wasm-OPFS-the-modern-substrate"><a href="#sqlite-wasm-OPFS-the-modern-substrate" class="headerlink" title="sqlite-wasm + OPFS, the modern substrate"></a>sqlite-wasm + OPFS, the modern substrate</h3><p>The official SQLite WASM build runs in a Web Worker and stores its database file in OPFS. With <code>FileSystemSyncAccessHandle</code>, perf is within shouting distance of native SQLite.</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> sqlite3InitModule <span class="keyword">from</span> <span class="string">&#x27;@sqlite.org/sqlite-wasm&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> sqlite3 = <span class="keyword">await</span> <span class="title function_">sqlite3InitModule</span>()</span><br><span class="line"><span class="keyword">const</span> db = <span class="keyword">new</span> sqlite3.<span class="property">oo1</span>.<span class="title class_">OpfsDb</span>(<span class="string">&#x27;/app.db&#x27;</span>)   <span class="comment">// persistent, per-origin</span></span><br><span class="line">db.<span class="title function_">exec</span>(<span class="string">`CREATE TABLE IF NOT EXISTS pets (id TEXT PRIMARY KEY, name TEXT)`</span>)</span><br><span class="line">db.<span class="title function_">exec</span>(&#123; <span class="attr">sql</span>: <span class="string">`INSERT INTO pets VALUES (?, ?)`</span>, <span class="attr">bind</span>: [<span class="string">&#x27;p1&#x27;</span>, <span class="string">&#x27;Fido&#x27;</span>] &#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> rows = db.<span class="title function_">exec</span>(&#123;</span><br><span class="line">    <span class="attr">sql</span>: <span class="string">`SELECT * FROM pets WHERE name LIKE ?`</span>,</span><br><span class="line">    <span class="attr">bind</span>: [<span class="string">&#x27;F%&#x27;</span>],</span><br><span class="line">    <span class="attr">returnValue</span>: <span class="string">&#x27;resultRows&#x27;</span>,</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>Layer Drizzle on top for typed queries and you have the same DX as a server SQL ORM, all in the browser.</p>
<h3 id="IndexedDB-without-the-pain"><a href="#IndexedDB-without-the-pain" class="headerlink" title="IndexedDB without the pain"></a>IndexedDB without the pain</h3><p>If you don’t need SQL, IndexedDB is still fine – but use a wrapper. Raw IDB looks like 1990s C; Dexie reads like a 2020s ORM.</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Dexie</span></span><br><span class="line"><span class="keyword">const</span> db = <span class="keyword">new</span> <span class="title class_">Dexie</span>(<span class="string">&#x27;app&#x27;</span>)</span><br><span class="line">db.<span class="title function_">version</span>(<span class="number">1</span>).<span class="title function_">stores</span>(&#123; <span class="attr">pets</span>: <span class="string">&#x27;id, species&#x27;</span> &#125;)  <span class="comment">// primary key + index</span></span><br><span class="line"><span class="keyword">await</span> db.<span class="property">pets</span>.<span class="title function_">put</span>(&#123; <span class="attr">id</span>: <span class="string">&#x27;p1&#x27;</span>, <span class="attr">name</span>: <span class="string">&#x27;Fido&#x27;</span>, <span class="attr">species</span>: <span class="string">&#x27;dog&#x27;</span> &#125;)</span><br><span class="line"><span class="keyword">const</span> dogs = <span class="keyword">await</span> db.<span class="property">pets</span>.<span class="title function_">where</span>(<span class="string">&#x27;species&#x27;</span>).<span class="title function_">equals</span>(<span class="string">&#x27;dog&#x27;</span>).<span class="title function_">toArray</span>()</span><br></pre></td></tr></table></figure>

<p>Dexie’s “live queries” (<code>useLiveQuery</code> for React, etc.) re-run automatically when the underlying data changes – the same pattern that local-first sync engines popularised.</p>
<h3 id="Collaboration-Yjs-in-twelve-lines"><a href="#Collaboration-Yjs-in-twelve-lines" class="headerlink" title="Collaboration: Yjs in twelve lines"></a>Collaboration: Yjs in twelve lines</h3><p>CRDTs sound exotic until you see how little code they take. Here’s a shared text doc that syncs over WebSocket and persists to IndexedDB:</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> Y <span class="keyword">from</span> <span class="string">&#x27;yjs&#x27;</span></span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">IndexeddbPersistence</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;y-indexeddb&#x27;</span></span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">WebsocketProvider</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;y-websocket&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> doc = <span class="keyword">new</span> Y.<span class="title class_">Doc</span>()</span><br><span class="line"><span class="keyword">new</span> <span class="title class_">IndexeddbPersistence</span>(<span class="string">&#x27;room-1&#x27;</span>, doc)                   <span class="comment">// local persistence</span></span><br><span class="line"><span class="keyword">new</span> <span class="title class_">WebsocketProvider</span>(<span class="string">&#x27;wss://yjs.example.com&#x27;</span>, <span class="string">&#x27;room-1&#x27;</span>, doc)   <span class="comment">// sync transport</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> yText = doc.<span class="title function_">getText</span>(<span class="string">&#x27;shared&#x27;</span>)</span><br><span class="line">yText.<span class="title function_">observe</span>(<span class="function">() =&gt;</span> <span class="title function_">render</span>(yText.<span class="title function_">toString</span>()))</span><br><span class="line">yText.<span class="title function_">insert</span>(<span class="number">0</span>, <span class="string">&#x27;hello, &#x27;</span>)                                <span class="comment">// shows up everywhere</span></span><br></pre></td></tr></table></figure>

<p>That’s the entire collaborative-editing primitive. Bind it to a textarea or a Prosemirror&#x2F;Tiptap&#x2F;Monaco binding, plug in a Hocuspocus server, and you have Google Docs-shaped collab.</p>
<h3 id="The-persistence-dance-never-assume-your-data-sticks"><a href="#The-persistence-dance-never-assume-your-data-sticks" class="headerlink" title="The persistence dance: never assume your data sticks"></a>The persistence dance: never assume your data sticks</h3><p>Browsers can evict your data under storage pressure. The contract you actually want is:</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// On app boot</span></span><br><span class="line"><span class="keyword">if</span> (navigator.<span class="property">storage</span> &amp;&amp; navigator.<span class="property">storage</span>.<span class="property">persist</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> already = <span class="keyword">await</span> navigator.<span class="property">storage</span>.<span class="title function_">persisted</span>()</span><br><span class="line">    <span class="keyword">if</span> (!already) &#123;</span><br><span class="line">        <span class="keyword">const</span> granted = <span class="keyword">await</span> navigator.<span class="property">storage</span>.<span class="title function_">persist</span>()</span><br><span class="line">        <span class="keyword">if</span> (!granted) <span class="variable language_">console</span>.<span class="title function_">warn</span>(<span class="string">&#x27;Storage NOT persisted; treat as cache&#x27;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Anywhere</span></span><br><span class="line"><span class="keyword">const</span> &#123; usage = <span class="number">0</span>, quota = <span class="number">0</span> &#125; = <span class="keyword">await</span> navigator.<span class="property">storage</span>.<span class="title function_">estimate</span>()</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`<span class="subst">$&#123;(usage / <span class="number">1e6</span>).toFixed(<span class="number">1</span>)&#125;</span> MB / <span class="subst">$&#123;(quota / <span class="number">1e9</span>).toFixed(<span class="number">1</span>)&#125;</span> GB`</span>)</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>Chrome</strong> auto-grants <code>persist()</code> based on engagement signals (bookmarks, install, frequent use).</li>
<li><strong>Safari &#x2F; Firefox</strong> require explicit user action – typically a PWA install prompt.</li>
<li><strong>Treat unpersisted storage as a cache</strong>, never as a database. Local-first apps that don’t request <code>persist()</code> will lose user data on memory-pressured devices.</li>
</ul>
<h3 id="The-Service-Worker-Cache-API-offline-shell"><a href="#The-Service-Worker-Cache-API-offline-shell" class="headerlink" title="The Service Worker + Cache API offline shell"></a>The Service Worker + Cache API offline shell</h3><p>For PWAs and offline-capable web apps, the durable pattern is <em>cache-first for shell, network-first for data</em>.</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// sw.js -- runs in service worker</span></span><br><span class="line"><span class="keyword">const</span> <span class="variable constant_">SHELL</span> = <span class="string">&#x27;shell-v3&#x27;</span></span><br><span class="line"><span class="keyword">const</span> <span class="variable constant_">SHELL_FILES</span> = [<span class="string">&#x27;/&#x27;</span>, <span class="string">&#x27;/index.html&#x27;</span>, <span class="string">&#x27;/app.js&#x27;</span>, <span class="string">&#x27;/app.css&#x27;</span>]</span><br><span class="line"></span><br><span class="line">self.<span class="title function_">addEventListener</span>(<span class="string">&#x27;install&#x27;</span>, <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">    e.<span class="title function_">waitUntil</span>(caches.<span class="title function_">open</span>(<span class="variable constant_">SHELL</span>).<span class="title function_">then</span>(<span class="function">(<span class="params">c</span>) =&gt;</span> c.<span class="title function_">addAll</span>(<span class="variable constant_">SHELL_FILES</span>)))</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">self.<span class="title function_">addEventListener</span>(<span class="string">&#x27;fetch&#x27;</span>, <span class="function">(<span class="params">e</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (e.<span class="property">request</span>.<span class="property">mode</span> === <span class="string">&#x27;navigate&#x27;</span>) &#123;</span><br><span class="line">        e.<span class="title function_">respondWith</span>(caches.<span class="title function_">match</span>(<span class="string">&#x27;/index.html&#x27;</span>))   <span class="comment">// shell from cache</span></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (e.<span class="property">request</span>.<span class="property">url</span>.<span class="title function_">includes</span>(<span class="string">&#x27;/api/&#x27;</span>)) &#123;</span><br><span class="line">        e.<span class="title function_">respondWith</span>(<span class="title function_">fetch</span>(e.<span class="property">request</span>))               <span class="comment">// data from network</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>The Cache API is one of the great unsung wins of the modern web. It’s a Request-&gt;Response store, scoped to the origin, accessible from both the page and the service worker, with no eviction policy beyond the global storage quota.</p>
<h3 id="Sync-engine-model-in-pseudocode"><a href="#Sync-engine-model-in-pseudocode" class="headerlink" title="Sync engine model in pseudocode"></a>Sync engine model in pseudocode</h3><p>What does a sync engine like Replicache or Triplit actually do? At its core:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">on user action:</span><br><span class="line">    1. compute mutation locally (e.g., addPet(&#123;id, name, species&#125;))</span><br><span class="line">    2. apply to local DB (UI reflects change immediately)</span><br><span class="line">    3. append mutation to push-log</span><br><span class="line">    4. POST /push to server (mutation list + last-known cookie)</span><br><span class="line">        - server applies mutations transactionally</span><br><span class="line">        - returns server cookie + patch (changed rows since cookie)</span><br><span class="line">    5. apply patch to local DB</span><br><span class="line">    6. UI reconciles</span><br><span class="line"></span><br><span class="line">periodically / on socket message:</span><br><span class="line">    POST /pull   (or receive WebSocket &quot;poke&quot;)</span><br><span class="line">        - server returns patch since last cookie</span><br><span class="line">    apply patch, reconcile UI</span><br></pre></td></tr></table></figure>

<p>The clever part is the <em>push log</em> and the <em>cookie</em>: both client and server agree on a position in the change history, so reconnects are cheap and ordering is preserved. You write mutator functions once on the client and once on the server – everything else falls out.</p>
<h3 id="When-to-pick-which-web-storage"><a href="#When-to-pick-which-web-storage" class="headerlink" title="When to pick which web storage"></a>When to pick which web storage</h3><table>
<thead>
<tr>
<th>Need</th>
<th>Pick</th>
</tr>
</thead>
<tbody><tr>
<td>Tiny prefs, sync access, no async hassle</td>
<td>localStorage (under 1 MB; expect Safari ITP eviction)</td>
</tr>
<tr>
<td>Auth tokens</td>
<td>HttpOnly+Secure cookie (server session) or in-memory + refresh token in IDB</td>
</tr>
<tr>
<td>Structured data, no SQL needed</td>
<td>Dexie over IndexedDB</td>
</tr>
<tr>
<td>Real SQL, large datasets</td>
<td>sqlite-wasm + OPFS (+ Drizzle for typed queries)</td>
</tr>
<tr>
<td>Sharing with other apps &#x2F; files</td>
<td>File System Access API (Chromium) – otherwise download fallback</td>
</tr>
<tr>
<td>Offline shell for PWA</td>
<td>Cache API + Service Worker</td>
</tr>
<tr>
<td>Collaboration &#x2F; multi-user</td>
<td>Yjs (text-shaped) or Automerge (JSON-shaped)</td>
</tr>
<tr>
<td>Offline-first with server backend</td>
<td>Triplit, Replicache, PowerSync, or ElectricSQL (read-only sync)</td>
</tr>
</tbody></table>
<h2 id="Master-comparison-matrix"><a href="#Master-comparison-matrix" class="headerlink" title="Master comparison matrix"></a>Master comparison matrix</h2><p>A wide-angle view of major options, sorted roughly by category. “Sync” means built-in cross-device sync without you writing it.</p>
<table>
<thead>
<tr>
<th>Option</th>
<th>Platform(s)</th>
<th>Type</th>
<th>Sync</th>
<th>OSS</th>
<th>Best for</th>
</tr>
</thead>
<tbody><tr>
<td>UserDefaults</td>
<td>iOS</td>
<td>KV</td>
<td>iCloud KV opt-in</td>
<td>–</td>
<td>Tiny prefs</td>
</tr>
<tr>
<td>Keychain</td>
<td>iOS</td>
<td>Secrets</td>
<td>iCloud opt-in</td>
<td>–</td>
<td>Tokens, secrets</td>
</tr>
<tr>
<td>Core Data</td>
<td>iOS</td>
<td>ORM&#x2F;SQLite</td>
<td>via CloudKit</td>
<td>–</td>
<td>Mature relational data</td>
</tr>
<tr>
<td>SwiftData</td>
<td>iOS</td>
<td>ORM&#x2F;SQLite</td>
<td>via CloudKit (limited)</td>
<td>–</td>
<td>New simple schemas</td>
</tr>
<tr>
<td>GRDB</td>
<td>iOS</td>
<td>SQLite</td>
<td>No</td>
<td>OSS</td>
<td>Modern SQLite control</td>
</tr>
<tr>
<td>Realm (local)</td>
<td>iOS&#x2F;Android</td>
<td>Object DB</td>
<td>Sync sunset</td>
<td>OSS</td>
<td>Existing Realm apps</td>
</tr>
<tr>
<td>DataStore</td>
<td>Android</td>
<td>KV</td>
<td>No</td>
<td>OSS</td>
<td>New KV layer</td>
</tr>
<tr>
<td>Room</td>
<td>Android (KMP)</td>
<td>ORM&#x2F;SQLite</td>
<td>No</td>
<td>OSS</td>
<td>Default Android relational</td>
</tr>
<tr>
<td>SQLDelight</td>
<td>Android (KMP)</td>
<td>SQLite codegen</td>
<td>No</td>
<td>OSS</td>
<td>KMP-shared schemas</td>
</tr>
<tr>
<td>ObjectBox</td>
<td>Android</td>
<td>Object DB</td>
<td>Commercial</td>
<td>Mixed</td>
<td>Performance-critical</td>
</tr>
<tr>
<td>localStorage</td>
<td>Web</td>
<td>KV (string)</td>
<td>No</td>
<td>–</td>
<td>Tiny prefs</td>
</tr>
<tr>
<td>IndexedDB</td>
<td>Web</td>
<td>Async DB</td>
<td>No</td>
<td>–</td>
<td>Real browser DB</td>
</tr>
<tr>
<td>OPFS</td>
<td>Web</td>
<td>File system</td>
<td>No</td>
<td>–</td>
<td>SQLite-WASM substrate</td>
</tr>
<tr>
<td>Dexie.js</td>
<td>Web</td>
<td>IDB ORM</td>
<td>Optional addon</td>
<td>OSS</td>
<td>Most popular IDB layer</td>
</tr>
<tr>
<td>RxDB</td>
<td>Web</td>
<td>Reactive DB</td>
<td>Pluggable</td>
<td>OSS</td>
<td>Offline-first</td>
</tr>
<tr>
<td>sqlite-wasm</td>
<td>Web</td>
<td>SQL DB</td>
<td>No</td>
<td>OSS</td>
<td>Serious local-first</td>
</tr>
<tr>
<td>Yjs</td>
<td>Web (any)</td>
<td>CRDT</td>
<td>via transports</td>
<td>OSS</td>
<td>Collaborative editing</td>
</tr>
<tr>
<td><strong>Firebase</strong></td>
<td>iOS&#x2F;Android&#x2F;Web</td>
<td>NoSQL BaaS</td>
<td>Yes</td>
<td>Mixed</td>
<td>Indie cross-platform</td>
</tr>
<tr>
<td><strong>Supabase</strong></td>
<td>iOS&#x2F;Android&#x2F;Web</td>
<td>Postgres BaaS</td>
<td>Yes (Realtime)</td>
<td>OSS</td>
<td>SQL + self-host escape</td>
</tr>
<tr>
<td><strong>Convex</strong></td>
<td>Web (mobile via REST)</td>
<td>TS reactive DB</td>
<td>Yes</td>
<td>Source-available</td>
<td>TS apps</td>
</tr>
<tr>
<td><strong>PocketBase</strong></td>
<td>All (REST)</td>
<td>Embedded SQLite BaaS</td>
<td>Yes (Realtime)</td>
<td>OSS</td>
<td>Solo devs</td>
</tr>
<tr>
<td><strong>AWS Amplify</strong></td>
<td>All</td>
<td>AWS BaaS</td>
<td>Yes</td>
<td>OSS clients</td>
<td>AWS shops</td>
</tr>
<tr>
<td><strong>Replicache</strong></td>
<td>Web&#x2F;RN</td>
<td>Sync engine</td>
<td>Yes</td>
<td>Commercial</td>
<td>Bring-your-own backend</td>
</tr>
<tr>
<td><strong>PowerSync</strong></td>
<td>iOS&#x2F;Android&#x2F;Web</td>
<td>Sync engine</td>
<td>Yes</td>
<td>Commercial</td>
<td>Postgres&#x2F;Mongo offline</td>
</tr>
<tr>
<td><strong>Triplit</strong></td>
<td>Web&#x2F;RN</td>
<td>DB + sync</td>
<td>Yes</td>
<td>OSS&#x2F;AGPL</td>
<td>TS-first local-first</td>
</tr>
<tr>
<td><strong>ElectricSQL</strong></td>
<td>Web</td>
<td>Read-only sync</td>
<td>Yes (read)</td>
<td>OSS</td>
<td>Postgres -&gt; SQLite stream</td>
</tr>
<tr>
<td>CloudKit</td>
<td>iOS&#x2F;macOS</td>
<td>Cloud DB + assets</td>
<td>Yes</td>
<td>–</td>
<td>Apple-only sync</td>
</tr>
<tr>
<td>R2 &#x2F; S3 &#x2F; B2</td>
<td>All</td>
<td>Object storage</td>
<td>–</td>
<td>Mixed</td>
<td>Blobs, user uploads</td>
</tr>
<tr>
<td>Neon &#x2F; Turso &#x2F; D1</td>
<td>All (server)</td>
<td>DBaaS</td>
<td>–</td>
<td>Mixed</td>
<td>Cloud Postgres&#x2F;SQLite</td>
</tr>
</tbody></table>
<h2 id="2026-trends-worth-calling-out"><a href="#2026-trends-worth-calling-out" class="headerlink" title="2026 trends worth calling out"></a>2026 trends worth calling out</h2><ol>
<li><strong>Local-first is mainstream.</strong> SQLite-WASM + OPFS + a sync engine on the web; PowerSync&#x2F;Triplit&#x2F;Replicache on mobile. “Online-only Firestore listeners” feels dated for serious apps.</li>
<li><strong>Realm Atlas Device Sync is dead</strong> (sunset September 2025). PowerSync, Triplit, and ElectricSQL absorbed the migration.</li>
<li><strong>Fauna shut down</strong>, <strong>PlanetScale killed its free tier</strong> – trust in pure DBaaS plays cooled, and indie devs migrated to Neon, Turso, and self-hostable options.</li>
<li><strong>R2’s zero-egress</strong> reshaped object-storage economics. S3 is now the “you have a reason” choice.</li>
<li><strong>SQLite at the edge</strong> (Turso, D1, embedded libSQL) replaced “serverless Postgres” hype for read-heavy workloads.</li>
<li><strong>Cookies retreated to auth-only.</strong> Third-party cookies are dead in Chrome; CHIPS handles legitimate cross-site embeds. App state lives in IDB or HttpOnly server sessions.</li>
<li><strong>SwiftData still has gaps.</strong> Many production iOS teams that adopted it in 2023-2024 quietly migrated back to Core Data + <code>NSPersistentCloudKitContainer</code> for CloudKit-heavy apps – the merge-policy gap and runtime-reconfiguration limitation hurt.</li>
<li><strong>Android’s encrypted-prefs successor is missing.</strong> Jetpack Security was deprecated without a replacement; teams roll their own with Keystore + Tink.</li>
<li><strong>Always call <code>persist()</code></strong> on the web. Treat unpersisted browser storage as cache, not truth – Safari especially evicts aggressively without explicit persistence.</li>
<li><strong>Convex &#x2F; Triplit &#x2F; Instant</strong> define the TS-app default; <strong>Firebase still wins hobbyist mobile</strong>.</li>
</ol>
<h2 id="How-to-choose-quick-picks"><a href="#How-to-choose-quick-picks" class="headerlink" title="How to choose: quick picks"></a>How to choose: quick picks</h2><h3 id="“I’m-building-a-small-iOS-app-for-myself”"><a href="#“I’m-building-a-small-iOS-app-for-myself”" class="headerlink" title="“I’m building a small iOS app for myself”"></a>“I’m building a small iOS app for myself”</h3><ul>
<li>Settings: UserDefaults</li>
<li>Tokens: Keychain</li>
<li>Data: GRDB or Core Data + <code>NSPersistentCloudKitContainer</code> if you want sync</li>
<li>Backend: probably none – CloudKit is free</li>
</ul>
<h3 id="“I’m-building-cross-platform-iOS-Android-Web”"><a href="#“I’m-building-cross-platform-iOS-Android-Web”" class="headerlink" title="“I’m building cross-platform iOS + Android + Web”"></a>“I’m building cross-platform iOS + Android + Web”</h3><ul>
<li><strong>Hobbyist &#x2F; fastest bring-up</strong>: Firebase. Still the default, all SDKs work, generous free tier</li>
<li><strong>SQL + self-host escape hatch</strong>: Supabase. Real Postgres, RLS, OSS</li>
<li><strong>TS-end-to-end web app</strong>: Convex. Best DX in the class</li>
<li><strong>You want SQLite locally + sync</strong>: PowerSync (mature, commercial) or Triplit (OSS, momentum)</li>
</ul>
<h3 id="“I-need-offline-first-with-real-conflict-resolution”"><a href="#“I-need-offline-first-with-real-conflict-resolution”" class="headerlink" title="“I need offline-first with real conflict resolution”"></a>“I need offline-first with real conflict resolution”</h3><ul>
<li><strong>Mobile-heavy</strong>: PowerSync over Postgres&#x2F;Mongo</li>
<li><strong>Web-heavy</strong>: Replicache, Triplit, or sqlite-wasm + ElectricSQL</li>
<li><strong>Collaborative document editing</strong>: Yjs + Hocuspocus</li>
</ul>
<h3 id="“I’m-uploading-user-photos-files”"><a href="#“I’m-uploading-user-photos-files”" class="headerlink" title="“I’m uploading user photos &#x2F; files”"></a>“I’m uploading user photos &#x2F; files”</h3><ul>
<li>Object storage: <strong>Cloudflare R2</strong> (zero egress) or Backblaze B2</li>
<li>For an end-to-end bundle including auth: Supabase Storage or Firebase Storage</li>
</ul>
<h3 id="“I-just-need-a-database-in-the-cloud”"><a href="#“I-just-need-a-database-in-the-cloud”" class="headerlink" title="“I just need a database in the cloud”"></a>“I just need a database in the cloud”</h3><ul>
<li>Postgres: Neon (serverless, branching)</li>
<li>SQLite-at-the-edge: Turso or Cloudflare D1</li>
<li>Avoid: Fauna (dead), PlanetScale (no free tier), pure Realm sync (sunset)</li>
</ul>
<h3 id="“I’m-building-a-side-project-with-a-single-binary-backend”"><a href="#“I’m-building-a-side-project-with-a-single-binary-backend”" class="headerlink" title="“I’m building a side project with a single binary backend”"></a>“I’m building a side project with a single binary backend”</h3><ul>
<li>PocketBase. SQLite + auth + realtime + admin UI in one Go binary</li>
</ul>
<h3 id="“I’m-in-a-regulated-environment-that-needs-self-hosting”"><a href="#“I’m-in-a-regulated-environment-that-needs-self-hosting”" class="headerlink" title="“I’m in a regulated environment that needs self-hosting”"></a>“I’m in a regulated environment that needs self-hosting”</h3><ul>
<li>Supabase, Appwrite, or PocketBase. All three self-host cleanly.</li>
</ul>
<hr>
<p>The honest summary: <strong>storage in 2026 is layered, and the right answer is usually two or three of these together</strong> – a system KV for prefs, a system or third-party DB for structured data, and a cloud sync engine that treats the device as the source of truth. The exciting movement is in that last layer; the on-device APIs have mostly stabilized.</p>
]]></content>
      <categories>
        <category>iOS</category>
      </categories>
      <tags>
        <tag>Core Data</tag>
        <tag>iOS</tag>
        <tag>Android</tag>
        <tag>Web</tag>
        <tag>SwiftData</tag>
        <tag>CloudKit</tag>
        <tag>Room</tag>
        <tag>IndexedDB</tag>
        <tag>OPFS</tag>
        <tag>SQLite</tag>
        <tag>Firebase</tag>
        <tag>Supabase</tag>
        <tag>PowerSync</tag>
        <tag>Triplit</tag>
        <tag>local-first</tag>
      </tags>
  </entry>
  <entry>
    <title>iOS toolchain in 2026: what each layer of your stack is for</title>
    <url>/iOS-App-From-Scratch-to-App-Store/</url>
    <content><![CDATA[<p>Building an iOS app in 2026 means assembling six layers: <strong>project scaffolding, code, build, common packages, distribution, and AI assist</strong>. Each layer answers a different question. The same project might use Xcode’s defaults at one layer and a third-party tool at another. The trick is knowing which question each tool actually answers, so you only reach for one when the default stops paying its rent.</p>
<span id="more"></span>

<h2 id="The-six-layers-at-a-glance"><a href="#The-six-layers-at-a-glance" class="headerlink" title="The six layers, at a glance"></a>The six layers, at a glance</h2><table>
<thead>
<tr>
<th>Layer</th>
<th>Question it answers</th>
<th>Default</th>
<th>Reach for instead</th>
</tr>
</thead>
<tbody><tr>
<td>1. Project scaffolding</td>
<td>What generates my <code>.xcodeproj</code>?</td>
<td>Xcode templates</td>
<td>XcodeGen, Monolith</td>
</tr>
<tr>
<td>2. Code</td>
<td>How do I keep style consistent and resources type-safe?</td>
<td>SwiftLint + SwiftFormat + Xcode native typed resources</td>
<td>R.swift (legacy)</td>
</tr>
<tr>
<td>3. Build</td>
<td>How do I drive Xcode from the terminal?</td>
<td><code>xcodebuild</code> + <code>xcbeautify</code></td>
<td>Xcode-Build-Server (for editor LSP)</td>
</tr>
<tr>
<td>4. Common packages</td>
<td>What libraries do most apps end up using?</td>
<td>SwiftPM + a small standard kit</td>
<td>(varies by app)</td>
</tr>
<tr>
<td>5. Distribution</td>
<td>How do I ship a build to TestFlight &#x2F; App Store Connect?</td>
<td><code>xcodebuild archive</code> + <code>notarytool</code> + a Makefile</td>
<td>Fastlane (legacy but still works)</td>
</tr>
<tr>
<td>6. AI assist</td>
<td>What’s writing code with you?</td>
<td>Xcode 26 Predictive Code Completion</td>
<td>Claude Code, Cursor</td>
</tr>
</tbody></table>
<p>The rest of the post walks each layer top to bottom: what the default does, why people drop to an alternative, and how to actually invoke it.</p>
<hr>
<h2 id="1-Project-scaffolding"><a href="#1-Project-scaffolding" class="headerlink" title="1. Project scaffolding"></a>1. Project scaffolding</h2><p>The question is mundane: how do you get a working <code>.xcodeproj</code> and folder structure to start typing into?</p>
<p><strong>Default: Xcode templates.</strong> File &gt; New &gt; Project still works for one-off apps and learning projects. The output is fine; the cost is the noisy <code>.xcodeproj</code> file format that merges badly under team work. Two defaults worth toggling on the way out: untick “Use Storyboard” if you’re going code-first (<a href="https://sarunw.com/posts/how-to-create-new-xcode-project-without-storyboard/#xcode-11">how-to</a>), and start from a known-good Swift <code>.gitignore</code> (<a href="https://github.com/github/gitignore/blob/main/Swift.gitignore">github&#x2F;gitignore Swift template</a>).</p>
<p><strong>Reach for <a href="https://github.com/yonaskolb/XcodeGen">XcodeGen</a></strong> when the project file becomes the bottleneck: regenerated from a <code>project.yml</code>, so you stop merging <code>.pbxproj</code> conflicts. The cost is the regeneration step in your dev loop and the YAML to maintain.</p>
<p><strong>Reach for <a href="https://github.com/luminoid/Monolith">Monolith</a></strong> when you’re starting a new project from scratch and want more than what Xcode’s wizard offers: it scaffolds iOS apps, Swift Packages, and Swift CLIs with 15 optional features (tabs, Mac Catalyst, dark mode, theme generation from a single hex colour, etc.) via an interactive wizard. Trade-off: opinionated, one-time bootstrap rather than a long-term project file generator.</p>
<hr>
<h2 id="2-Code-style-and-type-safe-resources"><a href="#2-Code-style-and-type-safe-resources" class="headerlink" title="2. Code: style and type-safe resources"></a>2. Code: style and type-safe resources</h2><p>Two distinct sub-questions: keep the source consistent across people, and stop dragging strings around to find images and localized text.</p>
<h3 id="Style-SwiftLint-SwiftFormat"><a href="#Style-SwiftLint-SwiftFormat" class="headerlink" title="Style: SwiftLint + SwiftFormat"></a>Style: SwiftLint + SwiftFormat</h3><p><a href="https://github.com/realm/SwiftLint">SwiftLint</a> (rule enforcement) and <a href="https://github.com/nicklockwood/SwiftFormat">SwiftFormat</a> (automatic formatting) both still set the standard in 2026. They don’t compete; they’re complementary. SwiftLint catches anti-patterns; SwiftFormat reformats. Run them on commit and CI alike.</p>
<h3 id="Type-safe-resources-Xcode-native-then-R-swift-only-if-you-must"><a href="#Type-safe-resources-Xcode-native-then-R-swift-only-if-you-must" class="headerlink" title="Type-safe resources: Xcode native, then R.swift only if you must"></a>Type-safe resources: Xcode native, then R.swift only if you must</h3><p>Until Xcode 15, <a href="https://github.com/mac-cain13/R.swift">R.swift</a> was the answer to <em>“why am I writing <code>UIImage(named: &quot;ic_check&quot;)</code> and crashing at runtime when someone renames the asset?”</em>. Xcode 15+ generates typed accessors from the asset catalogue natively:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> icon <span class="operator">=</span> <span class="type">UIImage</span>(resource: .icCheck)         <span class="comment">// typed; rename = compile error</span></span><br><span class="line"><span class="keyword">let</span> title <span class="operator">=</span> <span class="type">String</span>(localized: <span class="string">&quot;settings.title&quot;</span>) <span class="comment">// typed against the string catalogue</span></span><br><span class="line"><span class="keyword">let</span> color <span class="operator">=</span> <span class="type">Color</span>(.brandPrimary)</span><br></pre></td></tr></table></figure>

<p>That covers most of what R.swift used to do, with zero build-phase overhead. <strong>Recommendation</strong>: skip R.swift on new projects; only adopt it on legacy codebases that need its specific outputs (segues, fonts, R.image with custom rendering modes). If you do use R.swift, the <a href="https://github.com/mac-cain13/R.swift/issues/815">run-script-via-Mint</a> approach is the workaround for the broken plugin product.</p>
<p><a href="https://github.com/yonaskolb/Mint">Mint</a> itself is the package manager for Swift CLI tools (R.swift, SwiftLint, etc.); useful when you want pinned versions across machines without committing binaries.</p>
<hr>
<h2 id="3-Build-driving-Xcode-from-the-terminal"><a href="#3-Build-driving-Xcode-from-the-terminal" class="headerlink" title="3. Build: driving Xcode from the terminal"></a>3. Build: driving Xcode from the terminal</h2><p>The shell-driven build path matters whenever you want CI, repeatable local builds, or to run tests without launching the IDE.</p>
<h3 id="xcodebuild"><a href="#xcodebuild" class="headerlink" title="xcodebuild"></a><code>xcodebuild</code></h3><p>The cornerstone command. Worth memorising the four invocations:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Build</span></span><br><span class="line">xcodebuild -scheme MyApp -sdk iphonesimulator build</span><br><span class="line"></span><br><span class="line"><span class="comment"># Clean build (verify zero warnings; incremental builds hide them)</span></span><br><span class="line">xcodebuild -scheme MyApp -sdk iphonesimulator clean build</span><br><span class="line"></span><br><span class="line"><span class="comment"># Run tests</span></span><br><span class="line">xcodebuild -scheme MyApp -sdk iphonesimulator \</span><br><span class="line">    -destination <span class="string">&#x27;platform=iOS Simulator,name=iPhone 17,OS=26.2&#x27;</span> <span class="built_in">test</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># List schemes</span></span><br><span class="line">xcodebuild -list</span><br><span class="line"></span><br><span class="line"><span class="comment"># Archive (for distribution)</span></span><br><span class="line">xcodebuild -scheme MyApp -archivePath build/MyApp.xcarchive archive</span><br></pre></td></tr></table></figure>

<p>Always specify <code>OS=</code> in the destination string. Without it, Xcode picks a different runtime across machines, which means CI flakes that don’t reproduce locally.</p>
<h3 id="Editor-output-helpers"><a href="#Editor-output-helpers" class="headerlink" title="Editor &#x2F; output helpers"></a>Editor &#x2F; output helpers</h3><ul>
<li><strong><a href="https://github.com/cpisciotta/xcbeautify">xcbeautify</a></strong>: pipes <code>xcodebuild</code>‘s firehose into something readable. Standard on every CI script.</li>
<li><strong><a href="https://github.com/SolaWing/xcode-build-server">Xcode-Build-Server</a></strong>: bridges Xcode’s build settings into SourceKit-LSP so VS Code, Cursor, or Neovim can offer Swift completion against a real Xcode project.</li>
</ul>
<h3 id="LLDB-cheat-sheet"><a href="#LLDB-cheat-sheet" class="headerlink" title="LLDB cheat sheet"></a>LLDB cheat sheet</h3><p>When the debugger pauses, the same dozen commands cover most of what you need:</p>
<table>
<thead>
<tr>
<th>Command</th>
<th>Description</th>
</tr>
</thead>
<tbody><tr>
<td><code>bt</code></td>
<td>Backtrace, current thread</td>
</tr>
<tr>
<td><code>bt all</code></td>
<td>Backtrace, all threads</td>
</tr>
<tr>
<td><code>po &lt;expr&gt;</code></td>
<td>Print object: evaluate and print a Swift&#x2F;Obj-C expression</td>
</tr>
<tr>
<td><code>p &lt;expr&gt;</code></td>
<td>Print: evaluate with type info</td>
</tr>
<tr>
<td><code>frame variable</code></td>
<td>All locals in the current frame</td>
</tr>
<tr>
<td><code>thread list</code></td>
<td>List all threads</td>
</tr>
<tr>
<td><code>thread return</code></td>
<td>Force return from the current function</td>
</tr>
<tr>
<td><code>breakpoint list</code></td>
<td>List all breakpoints</td>
</tr>
<tr>
<td><code>watchpoint set variable &lt;var&gt;</code></td>
<td>Break when a variable changes</td>
</tr>
<tr>
<td><code>expr &lt;code&gt;</code></td>
<td>Execute code at runtime (e.g. <code>expr view.backgroundColor = .red</code>)</td>
</tr>
<tr>
<td><code>c</code> &#x2F; <code>s</code> &#x2F; <code>n</code> &#x2F; <code>finish</code></td>
<td>Continue &#x2F; step in &#x2F; step over &#x2F; step out</td>
</tr>
</tbody></table>
<h3 id="simctl-for-the-simulator"><a href="#simctl-for-the-simulator" class="headerlink" title="simctl for the simulator"></a><code>simctl</code> for the simulator</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">xcrun simctl list devices                                 <span class="comment"># what&#x27;s installed</span></span><br><span class="line">xcrun simctl boot <span class="string">&quot;iPhone 17&quot;</span>                             <span class="comment"># boot</span></span><br><span class="line">xcrun simctl openurl booted <span class="string">&quot;myapp://deeplink&quot;</span>            <span class="comment"># test deeplinks</span></span><br><span class="line">xcrun simctl io booted screenshot output.png              <span class="comment"># screenshots</span></span><br><span class="line">xcrun simctl erase <span class="string">&quot;iPhone 17&quot;</span>                            <span class="comment"># reset state</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="4-Common-packages"><a href="#4-Common-packages" class="headerlink" title="4. Common packages"></a>4. Common packages</h2><p>Most iOS apps end up depending on a small, predictable set. Pin them via <a href="https://www.swift.org/package-manager/">Swift Package Manager</a>, not CocoaPods, on new projects.</p>
<table>
<thead>
<tr>
<th>Package</th>
<th>Why you’d reach for it</th>
</tr>
</thead>
<tbody><tr>
<td><a href="https://github.com/SnapKit/SnapKit">SnapKit</a></td>
<td>UIKit programmatic Auto Layout DSL. Still the cleanest way to write constraints in code.</td>
</tr>
<tr>
<td><a href="https://github.com/onevcat/Kingfisher">Kingfisher</a></td>
<td>Async image loading + caching for <code>UIImageView</code>. Solves a problem Apple still hasn’t given you a stdlib answer to.</td>
</tr>
<tr>
<td><a href="https://github.com/airbnb/lottie-ios">Lottie</a></td>
<td>Renders After Effects vector animations natively. The <a href="https://lottiefiles.github.io/lottie-docs/playground/json_editor/">Lottie JSON Editor</a> is invaluable for hand-tweaking files.</td>
</tr>
<tr>
<td><a href="https://github.com/ReactiveX/RxSwift">RxSwift</a></td>
<td>Cross-platform reactive. Legacy in pure-Swift Apple work in 2026; reach for <code>async</code>&#x2F;<code>await</code> + <code>AsyncSequence</code> first, Combine if you need multicast or <code>@Published</code>. See <a href="/Combine-vs-RxSwift-vs-Swift-Collections/" title="Combine vs RxSwift vs Swift collection chains">Combine vs RxSwift vs Swift collection chains</a> for the full split.</td>
</tr>
</tbody></table>
<p>For inspecting a running app’s view hierarchy: <a href="https://github.com/QMUI/LookinServer/">Lookin</a> is the open-source Reveal alternative.</p>
<hr>
<h2 id="5-Distribution-shipping-to-TestFlight-App-Store-Connect"><a href="#5-Distribution-shipping-to-TestFlight-App-Store-Connect" class="headerlink" title="5. Distribution: shipping to TestFlight &#x2F; App Store Connect"></a>5. Distribution: shipping to TestFlight &#x2F; App Store Connect</h2><p>Two competing answers in 2026.</p>
<h3 id="Modern-a-Makefile-around-xcodebuild-notarytool"><a href="#Modern-a-Makefile-around-xcodebuild-notarytool" class="headerlink" title="Modern: a Makefile around xcodebuild + notarytool"></a>Modern: a Makefile around <code>xcodebuild</code> + <code>notarytool</code></h3><p>The path most new projects should pick. No Ruby, no gem management, no “what version of fastlane is on the CI runner” drift. A minimal <code>Makefile</code>:</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">SCHEME := MyApp</span><br><span class="line">DESTINATION := platform=iOS Simulator,name=iPhone 17,OS=26.2</span><br><span class="line">ARCHIVE := build/MyApp.xcarchive</span><br><span class="line">EXPORT := build/Export</span><br><span class="line">EXPORT_OPTIONS := ExportOptions.plist</span><br><span class="line"></span><br><span class="line"><span class="meta"><span class="keyword">.PHONY</span>: test archive ipa upload</span></span><br><span class="line"></span><br><span class="line"><span class="section">test:</span></span><br><span class="line">	xcodebuild test -scheme <span class="variable">$(SCHEME)</span> -destination &#x27;<span class="variable">$(DESTINATION)</span>&#x27; -quiet</span><br><span class="line"></span><br><span class="line"><span class="section">archive:</span></span><br><span class="line">	xcodebuild archive -scheme <span class="variable">$(SCHEME)</span> -archivePath <span class="variable">$(ARCHIVE)</span> \</span><br><span class="line">	  -destination &#x27;generic/platform=iOS&#x27; -quiet</span><br><span class="line"></span><br><span class="line"><span class="section">ipa: archive</span></span><br><span class="line">	xcodebuild -exportArchive -archivePath <span class="variable">$(ARCHIVE)</span> \</span><br><span class="line">	  -exportOptionsPlist <span class="variable">$(EXPORT_OPTIONS)</span> -exportPath <span class="variable">$(EXPORT)</span> -quiet</span><br><span class="line"></span><br><span class="line"><span class="section">upload: ipa</span></span><br><span class="line">	xcrun altool --upload-app -f <span class="variable">$(EXPORT)</span>/<span class="variable">$(SCHEME)</span>.ipa \</span><br><span class="line">	  --type ios --apiKey <span class="variable">$(ASC_KEY_ID)</span> --apiIssuer <span class="variable">$(ASC_ISSUER_ID)</span></span><br></pre></td></tr></table></figure>

<p>That’s the whole pipeline: <code>make test</code> runs the suite, <code>make upload</code> ships to App Store Connect (you need an <a href="https://appstoreconnect.apple.com/access/api">App Store Connect API key</a> and an <code>ExportOptions.plist</code>). Add a script to bump build numbers and you’re done.</p>
<h3 id="Legacy-Fastlane"><a href="#Legacy-Fastlane" class="headerlink" title="Legacy: Fastlane"></a>Legacy: <a href="https://docs.fastlane.tools/">Fastlane</a></h3><p>Fastlane has carried iOS distribution for ~10 years and still works fine. The trade is a heavy Ruby dependency tree, slower CI, and an abstraction layer between you and <code>xcodebuild</code> whenever something breaks. The day-to-day commands you’ll actually run:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">bundle <span class="built_in">exec</span> fastlane beta              <span class="comment"># build + upload to TestFlight</span></span><br><span class="line">bundle <span class="built_in">exec</span> fastlane generate_icon     <span class="comment"># regenerate icon set from one source image</span></span><br><span class="line">bundle <span class="built_in">exec</span> fastlane validate          <span class="comment"># pre-build sanity checks (e.g. localization)</span></span><br><span class="line">fastlane update_fastlane               <span class="comment"># keep gems current</span></span><br></pre></td></tr></table></figure>

<p>A Fastfile worth keeping around (covers iOS + Mac Catalyst from the same repo, and a pre-build validation hook):</p>
<figure class="highlight ruby"><table><tr><td class="code"><pre><span class="line">default_platform(<span class="symbol">:ios</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Mac Catalyst is a separate platform target, not an iOS lane.</span></span><br><span class="line"><span class="comment"># Keep it at the top-level so you can run `fastlane catalyst` directly.</span></span><br><span class="line">desc <span class="string">&quot;Build Mac Catalyst version of the app&quot;</span></span><br><span class="line">lane <span class="symbol">:catalyst</span> <span class="keyword">do</span></span><br><span class="line">  <span class="title class_">Dir</span>.chdir(<span class="string">&quot;..&quot;</span>) <span class="keyword">do</span></span><br><span class="line">    sh(<span class="string">&quot;python3 Scripts/analyze_localization.py&quot;</span>)</span><br><span class="line">  <span class="keyword">end</span></span><br><span class="line"></span><br><span class="line">  build_app(</span><br><span class="line">    <span class="symbol">scheme:</span> <span class="string">&quot;MyApp&quot;</span>,</span><br><span class="line">    <span class="symbol">clean:</span> <span class="literal">true</span>,</span><br><span class="line">    <span class="symbol">destination:</span> <span class="string">&quot;generic/platform=macOS&quot;</span>,</span><br><span class="line">    <span class="symbol">skip_package_ipa:</span> <span class="literal">true</span>,</span><br><span class="line">    <span class="symbol">export_method:</span> <span class="string">&quot;app-store&quot;</span>,</span><br><span class="line">    <span class="symbol">catalyst_platform:</span> <span class="string">&quot;macos&quot;</span>,</span><br><span class="line">    <span class="symbol">output_directory:</span> <span class="string">&quot;./build/Catalyst&quot;</span>,</span><br><span class="line">    <span class="symbol">xcargs:</span> <span class="string">&quot;ONLY_ACTIVE_ARCH=YES -skipPackagePluginValidation&quot;</span></span><br><span class="line">  )</span><br><span class="line"><span class="keyword">end</span></span><br><span class="line"></span><br><span class="line">platform <span class="symbol">:ios</span> <span class="keyword">do</span></span><br><span class="line">  desc <span class="string">&quot;Push a new beta build to TestFlight&quot;</span></span><br><span class="line">  lane <span class="symbol">:beta</span> <span class="keyword">do</span></span><br><span class="line">    <span class="comment"># Pre-build hook: any Python check, localization audit, etc.</span></span><br><span class="line">    <span class="title class_">Dir</span>.chdir(<span class="string">&quot;..&quot;</span>) <span class="keyword">do</span></span><br><span class="line">      sh(<span class="string">&quot;python3 Scripts/analyze_localization.py&quot;</span>)</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># With automatic signing, Xcode picks the App Store distribution profile</span></span><br><span class="line">    <span class="comment"># automatically. You must have opened the project in Xcode at least once</span></span><br><span class="line">    <span class="comment"># so it could download the App Store distribution provisioning profile</span></span><br><span class="line">    <span class="comment"># from your Apple Developer account.</span></span><br><span class="line">    build_app(<span class="symbol">scheme:</span> <span class="string">&quot;MyApp&quot;</span>, <span class="symbol">export_method:</span> <span class="string">&quot;app-store&quot;</span>)</span><br><span class="line">    upload_to_testflight</span><br><span class="line">  <span class="keyword">end</span></span><br><span class="line"></span><br><span class="line">  desc <span class="string">&quot;Generate app icon set from one source image&quot;</span></span><br><span class="line">  lane <span class="symbol">:generate_icon</span> <span class="keyword">do</span></span><br><span class="line">    appicon(</span><br><span class="line">      <span class="symbol">appicon_image_file:</span> <span class="string">&#x27;fastlane/metadata/app_icon.png&#x27;</span>,</span><br><span class="line">      <span class="symbol">appicon_devices:</span> %i[ipad iphone ios_marketing],</span><br><span class="line">      <span class="symbol">appicon_path:</span> <span class="string">&#x27;MyApp/Assets.xcassets&#x27;</span></span><br><span class="line">    )</span><br><span class="line">  <span class="keyword">end</span></span><br><span class="line"></span><br><span class="line">  desc <span class="string">&quot;Run pre-build validations only&quot;</span></span><br><span class="line">  lane <span class="symbol">:validate</span> <span class="keyword">do</span></span><br><span class="line">    <span class="title class_">Dir</span>.chdir(<span class="string">&quot;..&quot;</span>) <span class="keyword">do</span></span><br><span class="line">      sh(<span class="string">&quot;python3 Scripts/analyze_localization.py&quot;</span>)</span><br><span class="line">    <span class="keyword">end</span></span><br><span class="line">  <span class="keyword">end</span></span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>

<p>Useful Fastlane plugins that don’t have a clean Make equivalent: <a href="https://github.com/fastlane-community/fastlane-plugin-appicon">appicon</a> (icon set generation from one source image) and <a href="https://docs.fastlane.tools/actions/swiftlint/">the SwiftLint action</a>. If you’re already on Fastlane, no need to migrate; if you’re starting fresh, the Makefile path is shorter.</p>
<h3 id="Apple’s-official-references"><a href="#Apple’s-official-references" class="headerlink" title="Apple’s official references"></a>Apple’s official references</h3><ul>
<li><a href="https://developer.apple.com/documentation/xcode/preparing-your-app-for-distribution">Preparing your app for distribution</a></li>
<li><a href="https://developer.apple.com/documentation/xcode/distributing-your-app-for-beta-testing-and-releases">Distributing your app for beta testing and releases</a></li>
</ul>
<h3 id="Versioning-convention"><a href="#Versioning-convention" class="headerlink" title="Versioning convention"></a>Versioning convention</h3><p>Tag both marketing version and build number so CI can derive both:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git tag v1.2.0   <span class="comment"># marketing version</span></span><br><span class="line">git tag b10      <span class="comment"># build number</span></span><br></pre></td></tr></table></figure>

<p>Pair this with <a href="https://www.conventionalcommits.org/en/v1.0.0/">Conventional Commits</a> for changelogs that practically write themselves.</p>
<h3 id="App-Store-screenshots"><a href="#App-Store-screenshots" class="headerlink" title="App Store screenshots"></a>App Store screenshots</h3><ul>
<li><a href="https://developer.apple.com/design/resources/#product-bezels">Apple Product Bezels</a></li>
<li><a href="https://www.figma.com/community/device-mockups/iphone">Figma device mockups</a></li>
<li><a href="https://www.figma.com/community/file/1227376606592772837">iPhone screenshot template</a></li>
</ul>
<hr>
<h2 id="6-AI-assist"><a href="#6-AI-assist" class="headerlink" title="6. AI assist"></a>6. AI assist</h2><p>This layer didn’t exist as a category two years ago. In 2026, it’s a real choice with three credible options.</p>
<table>
<thead>
<tr>
<th>Tool</th>
<th>Where it lives</th>
<th>Best at</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Xcode 26 Predictive Code Completion</strong></td>
<td>Built into Xcode</td>
<td>Quick line-level completions, no setup, no extra context window cost</td>
</tr>
<tr>
<td><strong><a href="https://claude.com/product/claude-code">Claude Code</a></strong></td>
<td>Terminal CLI, can edit files in any project</td>
<td>Multi-file changes, refactors, structured tasks (the <a href="https://github.com/CharlesWiltgen/Axiom">Axiom</a> skill is a good iOS-flavored starting point)</td>
</tr>
<tr>
<td><strong><a href="https://cursor.com/home">Cursor</a></strong></td>
<td>VS Code fork</td>
<td>Inline AI edits while you’re already in a non-Xcode editor; pairs well with <a href="https://github.com/SolaWing/xcode-build-server">Xcode-Build-Server</a> so completions know your scheme. <a href="https://dimillian.medium.com/how-to-use-cursor-for-ios-development-54b912c23941">How-to write iOS in Cursor</a>.</td>
</tr>
</tbody></table>
<p>Pick one terminal&#x2F;CLI agent and one editor; running both fights for the same context window without much benefit.</p>
<hr>
<h2 id="Useful-links"><a href="#Useful-links" class="headerlink" title="Useful links"></a>Useful links</h2><ul>
<li><a href="https://developer.apple.com/design/human-interface-guidelines/guidelines/overview/">Apple’s Human Interface Guidelines</a></li>
<li><a href="https://appstoreconnect.apple.com/">App Store Connect</a></li>
<li><a href="https://developer.apple.com/account">Apple Developer account</a></li>
<li><a href="https://developer.apple.com/icloud/dashboards/">iCloud Dashboard</a> (CloudKit schema deploys, see also: silent push delivery in development vs production)</li>
<li><a href="https://stackoverflow.com/questions/1390329/trim-trailing-spaces-in-xcode">Trim trailing whitespace in Xcode</a></li>
<li><a href="https://smnh.me/hit-testing-in-ios">How hit-testing actually works</a></li>
<li><a href="https://sarunw.com/posts/how-to-disable-dark-mode-in-ios/">Disable dark mode for a single screen</a></li>
<li><a href="https://developer.apple.com/forums/thread/21660">Inspecting App data on disk</a></li>
</ul>
<hr>
<p>The temptation with iOS tooling is to adopt the whole stack on day one because someone’s blog post said so. Don’t. Start with Xcode + SwiftPM + a <code>.gitignore</code>, ship something to TestFlight via <code>xcodebuild archive</code>, then add tools as the friction at each layer becomes the thing actually slowing you down.</p>
]]></content>
      <categories>
        <category>iOS</category>
      </categories>
      <tags>
        <tag>Xcode</tag>
        <tag>toolchain</tag>
        <tag>SwiftLint</tag>
        <tag>SwiftFormat</tag>
        <tag>xcodebuild</tag>
        <tag>fastlane</tag>
        <tag>Claude Code</tag>
      </tags>
  </entry>
  <entry>
    <title>Combine vs RxSwift vs Swift collection chains</title>
    <url>/Combine-vs-RxSwift-vs-Swift-Collections/</url>
    <content><![CDATA[<p><code>map</code>, <code>filter</code>, <code>flatMap</code>, <code>reduce</code>. Swift has them on <code>Array</code>. RxSwift has them on <code>Observable</code>. Combine has them on <code>Publisher</code>. The names line up, the chains <em>look</em> the same, and that surface similarity is exactly what makes mixing the three confusing. They are not the same thing.</p>
<p>Three things separate them: sync vs async, eager vs lazy, and whether values arrive over time. Errors, cancellation, threading, and testing all fall out of those.</p>
<span id="more"></span>

<h2 id="TL-DR"><a href="#TL-DR" class="headerlink" title="TL;DR"></a>TL;DR</h2><table>
<thead>
<tr>
<th></th>
<th><strong>Swift collections</strong></th>
<th><strong>Combine</strong></th>
<th><strong>RxSwift</strong></th>
</tr>
</thead>
<tbody><tr>
<td><strong>Type</strong></td>
<td><code>Array</code>, <code>Sequence</code>, <code>LazySequence</code></td>
<td><code>Publisher</code>, <code>Subscriber</code></td>
<td><code>Observable</code>, <code>Observer</code></td>
</tr>
<tr>
<td><strong>Domain</strong></td>
<td>Values in memory</td>
<td>Values over time</td>
<td>Values over time</td>
</tr>
<tr>
<td><strong>Evaluation</strong></td>
<td>Eager (default), lazy via <code>.lazy</code></td>
<td>Lazy (cold) until subscribed</td>
<td>Lazy (cold) until subscribed</td>
</tr>
<tr>
<td><strong>Direction</strong></td>
<td>Pull (you iterate)</td>
<td>Push (subscribers receive) with demand</td>
<td>Push (subscribers receive)</td>
</tr>
<tr>
<td><strong>Backpressure</strong></td>
<td>N&#x2F;A</td>
<td>Yes (<code>Subscribers.Demand</code>)</td>
<td>No (manual)</td>
</tr>
<tr>
<td><strong>Errors</strong></td>
<td><code>throws</code> (sync) or none</td>
<td>Typed <code>Failure</code> associated type</td>
<td>Untyped <code>Error</code></td>
</tr>
<tr>
<td><strong>Cancellation</strong></td>
<td>N&#x2F;A</td>
<td><code>AnyCancellable</code></td>
<td><code>Disposable</code> &#x2F; <code>DisposeBag</code></td>
</tr>
<tr>
<td><strong>Threading</strong></td>
<td>Caller’s thread</td>
<td><code>subscribe(on:)</code> &#x2F; <code>receive(on:)</code></td>
<td><code>subscribeOn</code> &#x2F; <code>observeOn</code></td>
</tr>
<tr>
<td><strong>Subjects</strong></td>
<td>N&#x2F;A</td>
<td><code>PassthroughSubject</code>, <code>CurrentValueSubject</code></td>
<td><code>PublishSubject</code>, <code>BehaviorSubject</code>, <code>ReplaySubject</code></td>
</tr>
<tr>
<td><strong>Platform</strong></td>
<td>All Swift</td>
<td>iOS 13+, macOS 10.15+</td>
<td>All (third-party)</td>
</tr>
<tr>
<td><strong>Today’s role</strong></td>
<td>Always relevant</td>
<td>Apple’s official path</td>
<td>Legacy &#x2F; cross-platform</td>
</tr>
</tbody></table>
<h2 id="The-three-worlds-in-one-diagram"><a href="#The-three-worlds-in-one-diagram" class="headerlink" title="The three worlds, in one diagram"></a>The three worlds, in one diagram</h2><p>The mental model that helps most: collections operate over <strong>values in space</strong> (what’s already in memory); reactive streams operate over <strong>values in time</strong> (what arrives later). The operators look identical because both are pipelines, but one runs to completion now, the other runs whenever a value shows up.</p>
<pre>
<code class="mermaid">

flowchart LR
    subgraph C[&quot;Swift collections (space)&quot;]
        direction LR
        A1[&quot;[1, 2, 3, 4]&quot;] --&gt; A2[&quot;.filter { &gt; 1 }&quot;]
        A2 --&gt; A3[&quot;.map { * 2 }&quot;]
        A3 --&gt; A4[&quot;[4, 6, 8]&quot;]
    end

    subgraph R[&quot;Reactive streams (time)&quot;]
        direction LR
        B1[&quot;values...&lt;br&#x2F;&gt;over time&quot;] --&gt; B2[&quot;.filter&quot;]
        B2 --&gt; B3[&quot;.map&quot;]
        B3 --&gt; B4[&quot;subscriber&quot;]
    end

    style C fill:#eef,stroke:#669
    style R fill:#efe,stroke:#696
</code>
</pre>

<p>In the collection pipeline the entire dataset is known up front; the chain runs to completion synchronously when you call it (or on demand for each element if you wrap with <code>.lazy</code>). In the reactive pipeline the source emits whenever it wants, possibly forever, and operators are <em>templates</em> that get instantiated when somebody subscribes.</p>
<h2 id="Same-chain-three-implementations"><a href="#Same-chain-three-implementations" class="headerlink" title="Same chain, three implementations"></a>Same chain, three implementations</h2><p>Take a tiny task: “Given a list of numbers, keep the evens, double them, and sum the result.”</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Swift collections: eager, synchronous</span></span><br><span class="line"><span class="keyword">let</span> result <span class="operator">=</span> [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]</span><br><span class="line">    .filter &#123; <span class="variable">$0</span>.isMultiple(of: <span class="number">2</span>) &#125;</span><br><span class="line">    .map &#123; <span class="variable">$0</span> <span class="operator">*</span> <span class="number">2</span> &#125;</span><br><span class="line">    .reduce(<span class="number">0</span>, <span class="operator">+</span>)</span><br><span class="line"><span class="comment">// result == 24 immediately</span></span><br></pre></td></tr></table></figure>

<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Combine: lazy, push-based</span></span><br><span class="line"><span class="keyword">import</span> Combine</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> bag <span class="operator">=</span> <span class="type">Set</span>&lt;<span class="type">AnyCancellable</span>&gt;()</span><br><span class="line">[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>].publisher</span><br><span class="line">    .filter &#123; <span class="variable">$0</span>.isMultiple(of: <span class="number">2</span>) &#125;</span><br><span class="line">    .map &#123; <span class="variable">$0</span> <span class="operator">*</span> <span class="number">2</span> &#125;</span><br><span class="line">    .reduce(<span class="number">0</span>, <span class="operator">+</span>)</span><br><span class="line">    .sink &#123; <span class="built_in">print</span>(<span class="variable">$0</span>) &#125; <span class="comment">// prints 24 on subscription</span></span><br><span class="line">    .store(in: <span class="operator">&amp;</span>bag)</span><br></pre></td></tr></table></figure>

<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// RxSwift: lazy, push-based</span></span><br><span class="line"><span class="keyword">import</span> RxSwift</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> disposeBag <span class="operator">=</span> <span class="type">DisposeBag</span>()</span><br><span class="line"><span class="type">Observable</span>.from([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>])</span><br><span class="line">    .filter &#123; <span class="variable">$0</span>.isMultiple(of: <span class="number">2</span>) &#125;</span><br><span class="line">    .map &#123; <span class="variable">$0</span> <span class="operator">*</span> <span class="number">2</span> &#125;</span><br><span class="line">    .reduce(<span class="number">0</span>, accumulator: <span class="operator">+</span>)</span><br><span class="line">    .subscribe(onNext: &#123; <span class="built_in">print</span>(<span class="variable">$0</span>) &#125;) <span class="comment">// prints 24 on subscription</span></span><br><span class="line">    .disposed(by: disposeBag)</span><br></pre></td></tr></table></figure>

<p>Looks identical. They are not. The collection version runs the moment you write it. The other two build a pipeline description and don’t run anything until <code>sink</code> &#x2F; <code>subscribe</code> is called. Drop those final lines and Combine&#x2F;RxSwift produce zero work.</p>
<h2 id="Lazy-collections-the-closest-cousin"><a href="#Lazy-collections-the-closest-cousin" class="headerlink" title="Lazy collections: the closest cousin"></a>Lazy collections: the closest cousin</h2><p>Swift collections <em>can</em> be lazy, and that’s the closest sync analogue to a reactive pipeline:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> pipeline <span class="operator">=</span> (<span class="number">1</span><span class="operator">...</span>).lazy</span><br><span class="line">    .filter &#123; <span class="variable">$0</span>.isMultiple(of: <span class="number">2</span>) &#125;</span><br><span class="line">    .map &#123; <span class="variable">$0</span> <span class="operator">*</span> <span class="variable">$0</span> &#125;</span><br><span class="line"><span class="comment">// nothing computed yet</span></span><br><span class="line"><span class="keyword">let</span> firstThree <span class="operator">=</span> <span class="type">Array</span>(pipeline.prefix(<span class="number">3</span>)) <span class="comment">// [4, 16, 36]</span></span><br></pre></td></tr></table></figure>

<p><code>LazySequence</code> is pull-based (the consumer iterates), reactive streams are push-based (the producer emits), but the laziness is the same idea: nothing happens until somebody asks. The remaining gap is <strong>time</strong>. <code>LazySequence</code> cannot represent “a value will arrive in 200 ms,” only “a value will be computed when iterated.”</p>
<h2 id="Eager-vs-lazy-execution"><a href="#Eager-vs-lazy-execution" class="headerlink" title="Eager vs lazy execution"></a>Eager vs lazy execution</h2><pre>
<code class="mermaid">

sequenceDiagram
    participant Caller
    participant Pipeline
    Note over Caller,Pipeline: Swift collection (eager)
    Caller-&gt;&gt;Pipeline: filter
    Pipeline--&gt;&gt;Caller: [2,4,6]
    Caller-&gt;&gt;Pipeline: map
    Pipeline--&gt;&gt;Caller: [4,8,12]
    Caller-&gt;&gt;Pipeline: reduce
    Pipeline--&gt;&gt;Caller: 24
</code>
</pre>

<p>Each operator on <code>Array</code> allocates a new array. Three operators, three intermediate allocations. For small collections this is fine; for large ones, switch to <code>.lazy</code>.</p>
<pre>
<code class="mermaid">

sequenceDiagram
    participant Source as Publisher &#x2F; Observable
    participant Op1 as filter
    participant Op2 as map
    participant Sub as Subscriber
    Note over Source,Sub: Combine &#x2F; RxSwift (lazy, push)
    Sub-&gt;&gt;Op2: subscribe
    Op2-&gt;&gt;Op1: subscribe
    Op1-&gt;&gt;Source: subscribe
    Source--&gt;&gt;Op1: 1
    Op1--&gt;&gt;Source: (drop)
    Source--&gt;&gt;Op1: 2
    Op1--&gt;&gt;Op2: 2
    Op2--&gt;&gt;Sub: 4
    Source--&gt;&gt;Op1: 3
    Op1--&gt;&gt;Source: (drop)
    Source--&gt;&gt;Op1: complete
    Op1--&gt;&gt;Op2: complete
    Op2--&gt;&gt;Sub: complete
</code>
</pre>

<p>Subscription chains <em>upstream</em> (the subscriber is at the bottom and asks the source for values), values flow <em>downstream</em>. That direction is the bit reactive newcomers tend to get backwards.</p>
<h2 id="The-operator-name-overlap-is-a-trap"><a href="#The-operator-name-overlap-is-a-trap" class="headerlink" title="The operator name overlap is a trap"></a>The operator name overlap is a trap</h2><p><code>flatMap</code> is the canonical example. Three different things:</p>
<ul>
<li><strong><code>Array.flatMap</code></strong>: flatten one level. <code>[[1,2],[3]].flatMap &#123; $0 &#125; == [1,2,3]</code>. Synchronous.</li>
<li><strong><code>Publisher.flatMap</code></strong>: for each value, subscribe to a returned publisher and merge their outputs. Concurrency-bounded by <code>maxPublishers</code>.</li>
<li><strong><code>Observable.flatMap</code></strong>: project each value into an <code>Observable</code> and merge. RxSwift also has <code>flatMapLatest</code> (cancels the previous inner) and <code>concatMap</code> (queues serially). Combine has <code>switchToLatest</code> for the same purpose, applied differently.</li>
</ul>
<p>Knowing what <code>flatMap</code> returns isn’t enough; you also need to know which <em>flavor</em> you want: merge, switch-to-latest, or concat. Reach for the wrong one and you get either lost values or stale ones.</p>
<h2 id="Errors"><a href="#Errors" class="headerlink" title="Errors"></a>Errors</h2><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Swift collections</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">ParseError</span>: <span class="title class_ inherited__">Error</span> &#123;&#125;</span><br><span class="line"><span class="keyword">do</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> parsed <span class="operator">=</span> <span class="keyword">try</span> [<span class="string">&quot;1&quot;</span>, <span class="string">&quot;2&quot;</span>, <span class="string">&quot;x&quot;</span>].map &#123; s -&gt; <span class="type">Int</span> <span class="keyword">in</span></span><br><span class="line">        <span class="keyword">guard</span> <span class="keyword">let</span> n <span class="operator">=</span> <span class="type">Int</span>(s) <span class="keyword">else</span> &#123; <span class="keyword">throw</span> <span class="type">ParseError</span>() &#125;</span><br><span class="line">        <span class="keyword">return</span> n</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> &#123; <span class="comment">/* throw escapes out of the chain */</span> &#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Combine: typed errors</span></span><br><span class="line"><span class="keyword">let</span> p: <span class="type">AnyPublisher</span>&lt;<span class="type">String</span>, <span class="type">MyError</span>&gt; <span class="operator">=</span> <span class="operator">...</span></span><br><span class="line">p.tryMap &#123; <span class="keyword">try</span> parse(<span class="variable">$0</span>) &#125;      <span class="comment">// erases failure to Error</span></span><br><span class="line"> .mapError &#123; <span class="type">MyError</span>.wrap(<span class="variable">$0</span>) &#125; <span class="comment">// back to typed</span></span><br><span class="line"> .catch &#123; <span class="keyword">_</span> <span class="keyword">in</span> <span class="type">Just</span>(<span class="number">0</span>) &#125;        <span class="comment">// recover with another publisher</span></span><br></pre></td></tr></table></figure>

<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// RxSwift: Error (untyped)</span></span><br><span class="line"><span class="keyword">let</span> o: <span class="type">Observable</span>&lt;<span class="type">String</span>&gt; <span class="operator">=</span> <span class="operator">...</span></span><br><span class="line">o.map &#123; <span class="keyword">try</span> parse(<span class="variable">$0</span>) &#125;</span><br><span class="line"> .catch &#123; <span class="keyword">_</span> <span class="keyword">in</span> .just(<span class="number">0</span>) &#125;</span><br></pre></td></tr></table></figure>

<p>The practical difference: Combine’s typed <code>Failure</code> lets the compiler tell you when an upstream can or can’t fail. <code>Publisher&lt;Output, Never&gt;</code> is a strong guarantee, and <code>sink(receiveValue:)</code> (no completion handler) is only available when failure is <code>Never</code>. RxSwift erases all errors to <code>Error</code>, so the compiler can’t help you.</p>
<p>A reactive stream also <strong>terminates on error</strong>: once it errors, it’s done forever. I’ve shipped this bug more than once: catch a transient failure during dev, see one retry succeed, ship, then watch the pipeline silently die in production after the first real error. Use <code>catch</code> &#x2F; <code>retry</code> &#x2F; <code>replaceError</code> to keep it alive.</p>
<h2 id="Cancellation"><a href="#Cancellation" class="headerlink" title="Cancellation"></a>Cancellation</h2><p>Collections don’t cancel: they’re synchronous, you either let them finish or never call them. Reactive pipelines own real resources (timers, network requests, KVO observers) and must be torn down explicitly.</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Combine</span></span><br><span class="line"><span class="keyword">let</span> cancellable <span class="operator">=</span> publisher.sink &#123; <span class="operator">...</span> &#125;</span><br><span class="line">cancellable.cancel() <span class="comment">// or store in Set&lt;AnyCancellable&gt; and let it dealloc</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// RxSwift</span></span><br><span class="line"><span class="keyword">let</span> disposable <span class="operator">=</span> observable.subscribe &#123; <span class="operator">...</span> &#125;</span><br><span class="line">disposable.dispose() <span class="comment">// or .disposed(by: disposeBag)</span></span><br></pre></td></tr></table></figure>

<p>Both follow the same rule: when the cancellable &#x2F; disposable is deallocated, the subscription is torn down. Forget to retain it and the chain dies <em>before any values arrive</em>. Forget to release it and you get retain cycles via <code>[weak self]</code> mistakes.</p>
<h2 id="Threading-and-scheduling"><a href="#Threading-and-scheduling" class="headerlink" title="Threading and scheduling"></a>Threading and scheduling</h2><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Combine</span></span><br><span class="line"><span class="type">URLSession</span>.shared.dataTaskPublisher(for: url)</span><br><span class="line">    .subscribe(on: <span class="type">DispatchQueue</span>.global())   <span class="comment">// upstream work off main</span></span><br><span class="line">    .map &#123; decode(<span class="variable">$0</span>.data) &#125;</span><br><span class="line">    .receive(on: <span class="type">DispatchQueue</span>.main)         <span class="comment">// downstream delivery on main</span></span><br><span class="line">    .sink &#123; update(with: <span class="variable">$0</span>) &#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// RxSwift</span></span><br><span class="line"><span class="type">URLSession</span>.shared.rx.data(request: req)</span><br><span class="line">    .subscribeOn(<span class="type">ConcurrentDispatchQueueScheduler</span>(qos: .background))</span><br><span class="line">    .map &#123; decode(<span class="variable">$0</span>) &#125;</span><br><span class="line">    .observeOn(<span class="type">MainScheduler</span>.instance)</span><br><span class="line">    .subscribe(onNext: &#123; update(with: <span class="variable">$0</span>) &#125;)</span><br></pre></td></tr></table></figure>

<p>Same shape, slightly different vocabulary. The two non-obvious rules apply to both:</p>
<ol>
<li><code>subscribe(on:)</code> &#x2F; <code>subscribeOn</code> affects work <strong>upstream</strong> of where you call it (subscription, source emission). Calling it late in the chain is a common bug.</li>
<li><code>receive(on:)</code> &#x2F; <code>observeOn</code> affects everything <strong>downstream</strong>. UIKit&#x2F;SwiftUI work belongs on main.</li>
</ol>
<h2 id="Hot-vs-cold"><a href="#Hot-vs-cold" class="headerlink" title="Hot vs cold"></a>Hot vs cold</h2><p>A <strong>cold</strong> publisher&#x2F;observable starts producing on subscription, and each subscriber gets its own run (e.g. a network request fires per subscriber). A <strong>hot</strong> one is already running; subscribers see whatever’s emitted from now on.</p>
<p>Both libraries provide subjects to bridge the two:</p>
<table>
<thead>
<tr>
<th>Need</th>
<th>Combine</th>
<th>RxSwift</th>
</tr>
</thead>
<tbody><tr>
<td>Multicast event bus</td>
<td><code>PassthroughSubject</code></td>
<td><code>PublishSubject</code></td>
</tr>
<tr>
<td>“Current value, plus future”</td>
<td><code>CurrentValueSubject</code></td>
<td><code>BehaviorSubject</code></td>
</tr>
<tr>
<td>Replay last N</td>
<td><code>.share(replay:)</code> via custom</td>
<td><code>ReplaySubject</code></td>
</tr>
</tbody></table>
<p>The most common bug: subscribing to a cold publisher twice (e.g. binding the same <code>URLSession.dataTaskPublisher</code> to two views) and seeing two requests. Fix with <code>.share()</code> (Combine) or <code>.share(replay:)</code> (RxSwift) to make it hot&#x2F;multicast.</p>
<h2 id="Backpressure"><a href="#Backpressure" class="headerlink" title="Backpressure"></a>Backpressure</h2><p>Combine has it; RxSwift doesn’t.</p>
<p>In Combine, subscribers signal demand: <code>Subscribers.Demand.max(N)</code> or <code>.unlimited</code>. Operators respect that demand and propagate it upstream. This is what makes Combine viable for fast producers (file reads, large CSV streams) without forcing buffering.</p>
<p>RxSwift has no formal backpressure. If your producer is faster than your consumer, you buffer or drop manually with <code>buffer</code>, <code>throttle</code>, <code>debounce</code>, <code>sample</code>. RxJava has <code>Flowable</code> for this; RxSwift never adopted the equivalent.</p>
<p>In practice most app-level reactive code is event-rate-limited (taps, network responses, KVO) and backpressure doesn’t matter. It matters once you stream large datasets through a reactive pipeline.</p>
<h2 id="Testing"><a href="#Testing" class="headerlink" title="Testing"></a>Testing</h2><figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Combine: synchronous via Just / Fail / scheduler injection</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">testParse</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> cancellable <span class="operator">=</span> <span class="type">Just</span>(<span class="string">&quot;42&quot;</span>)</span><br><span class="line">        .tryMap &#123; <span class="keyword">try</span> <span class="type">Int</span>(<span class="variable">$0</span>, strict: <span class="literal">true</span>) &#125;</span><br><span class="line">        .sink(receiveCompletion: &#123; <span class="keyword">_</span> <span class="keyword">in</span> &#125;, receiveValue: &#123; <span class="type">XCTAssertEqual</span>(<span class="variable">$0</span>, <span class="number">42</span>) &#125;)</span><br><span class="line">    <span class="keyword">_</span> <span class="operator">=</span> cancellable</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// RxSwift: RxTest with TestScheduler and virtual time</span></span><br><span class="line"><span class="keyword">func</span> <span class="title function_">testParse</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> scheduler <span class="operator">=</span> <span class="type">TestScheduler</span>(initialClock: <span class="number">0</span>)</span><br><span class="line">    <span class="keyword">let</span> source <span class="operator">=</span> scheduler.createHotObservable([</span><br><span class="line">        .next(<span class="number">10</span>, <span class="string">&quot;42&quot;</span>),</span><br><span class="line">        .completed(<span class="number">20</span>)</span><br><span class="line">    ])</span><br><span class="line">    <span class="keyword">let</span> observer <span class="operator">=</span> scheduler.createObserver(<span class="type">Int</span>.<span class="keyword">self</span>)</span><br><span class="line">    source.map &#123; <span class="type">Int</span>(<span class="variable">$0</span>)<span class="operator">!</span> &#125;.subscribe(observer).disposed(by: bag)</span><br><span class="line">    scheduler.start()</span><br><span class="line">    <span class="type">XCTAssertEqual</span>(observer.events, [.next(<span class="number">10</span>, <span class="number">42</span>), .completed(<span class="number">20</span>)])</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>RxSwift’s <code>RxTest</code> virtual-time scheduler is genuinely excellent for time-based tests (debounce, throttle, retry intervals). Combine’s official testing story is weaker; the community settled on <a href="https://github.com/pointfreeco/combine-schedulers"><code>combine-schedulers</code></a> for the Combine equivalent, and <a href="https://github.com/pointfreeco/swift-clocks"><code>swift-clocks</code></a> for the <code>async</code>&#x2F;<code>await</code> side.</p>
<h2 id="Other-“values-over-time”-structures-in-the-Swift-ecosystem"><a href="#Other-“values-over-time”-structures-in-the-Swift-ecosystem" class="headerlink" title="Other “values over time” structures in the Swift ecosystem"></a>Other “values over time” structures in the Swift ecosystem</h2><p>The three I started with aren’t the whole story. A few more pipeline-shaped APIs you’ll meet in real Swift code:</p>
<h3 id="AsyncSequence-AsyncStream-AsyncThrowingStream"><a href="#AsyncSequence-AsyncStream-AsyncThrowingStream" class="headerlink" title="AsyncSequence &#x2F; AsyncStream &#x2F; AsyncThrowingStream"></a><code>AsyncSequence</code> &#x2F; <code>AsyncStream</code> &#x2F; <code>AsyncThrowingStream</code></h3><p>The Swift Concurrency answer to reactive streams. Pull-based (the consumer iterates), but the iteration itself can suspend until the next value arrives:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> <span class="keyword">await</span> value <span class="keyword">in</span> numbers.filter(&#123; <span class="variable">$0</span>.isMultiple(of: <span class="number">2</span>) &#125;).map(&#123; <span class="variable">$0</span> <span class="operator">*</span> <span class="number">2</span> &#125;) &#123;</span><br><span class="line">    sum <span class="operator">+=</span> value</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>AsyncStream</code> lets you bridge any callback-based source (KVO, delegate, <code>NotificationCenter</code>) into <code>AsyncSequence</code>:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> timer <span class="operator">=</span> <span class="type">AsyncStream</span>&lt;<span class="type">Date</span>&gt; &#123; continuation <span class="keyword">in</span></span><br><span class="line">    <span class="keyword">let</span> t <span class="operator">=</span> <span class="type">Timer</span>.scheduledTimer(withTimeInterval: <span class="number">1</span>, repeats: <span class="literal">true</span>) &#123; <span class="keyword">_</span> <span class="keyword">in</span></span><br><span class="line">        continuation.yield(<span class="type">Date</span>())</span><br><span class="line">    &#125;</span><br><span class="line">    continuation.onTermination <span class="operator">=</span> &#123; <span class="keyword">_</span> <span class="keyword">in</span> t.invalidate() &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span> <span class="keyword">await</span> tick <span class="keyword">in</span> timer.prefix(<span class="number">5</span>) &#123; <span class="built_in">print</span>(tick) &#125;</span><br></pre></td></tr></table></figure>

<p>Where it lines up with Combine&#x2F;RxSwift, and where it doesn’t:</p>
<table>
<thead>
<tr>
<th></th>
<th><code>AsyncSequence</code></th>
<th>Combine <code>Publisher</code></th>
</tr>
</thead>
<tbody><tr>
<td>Direction</td>
<td>Pull (consumer iterates)</td>
<td>Push (with demand)</td>
</tr>
<tr>
<td>Cancellation</td>
<td><code>Task</code> cancellation</td>
<td><code>AnyCancellable.cancel()</code></td>
</tr>
<tr>
<td>Multicast</td>
<td>No (single consumer)</td>
<td>Yes (<code>.share()</code>, subjects)</td>
</tr>
<tr>
<td>Backpressure</td>
<td>Implicit (consumer suspends)</td>
<td>Explicit (<code>Demand</code>)</td>
</tr>
<tr>
<td>Operators</td>
<td><code>map</code>, <code>filter</code>, <code>prefix</code>, <code>compactMap</code>, <code>dropFirst</code>…</td>
<td>Larger set, plus time operators</td>
</tr>
<tr>
<td>Time operators</td>
<td>None in stdlib (community: <a href="https://github.com/apple/swift-async-algorithms"><code>AsyncAlgorithms</code></a>)</td>
<td>Built-in (<code>debounce</code>, <code>throttle</code>…)</td>
</tr>
<tr>
<td>Cold&#x2F;Hot</td>
<td>Always cold per-iteration</td>
<td>Both, via subjects</td>
</tr>
</tbody></table>
<p><code>AsyncSequence</code> is closer to <code>LazySequence</code> with suspension points than to <code>Observable</code>. If you only need to <em>consume</em> a stream of values one place, async iteration wins. If you need to fan out to multiple consumers, share state, or use rich time operators, you still want Combine (or <code>AsyncAlgorithms</code>‘s <code>AsyncChannel</code> + multicast helpers).</p>
<h3 id="Combine-↔-AsyncSequence-bridges"><a href="#Combine-↔-AsyncSequence-bridges" class="headerlink" title="Combine ↔ AsyncSequence bridges"></a>Combine ↔ <code>AsyncSequence</code> bridges</h3><p>These exist in both directions and you’ll use them:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Publisher -&gt; AsyncSequence (built-in)</span></span><br><span class="line"><span class="keyword">for</span> <span class="keyword">await</span> value <span class="keyword">in</span> publisher.values &#123; <span class="built_in">print</span>(value) &#125;</span><br><span class="line"><span class="comment">// publisher.values is AsyncPublisher, which conforms to AsyncSequence.</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// AsyncSequence -&gt; Publisher (no built-in)</span></span><br><span class="line"><span class="comment">// Drive a Subject from a Task and expose its eraseToAnyPublisher().</span></span><br><span class="line"><span class="keyword">let</span> subject <span class="operator">=</span> <span class="type">PassthroughSubject</span>&lt;<span class="type">Int</span>, <span class="type">Never</span>&gt;()</span><br><span class="line"><span class="type">Task</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> <span class="keyword">await</span> v <span class="keyword">in</span> asyncSequence &#123; subject.send(v) &#125;</span><br><span class="line">    subject.send(completion: .finished)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> publisher <span class="operator">=</span> subject.eraseToAnyPublisher()</span><br></pre></td></tr></table></figure>

<p>In practice, most modern code subscribes once (<code>for await ... in publisher.values</code>) and lets Combine do the multicast &#x2F; hot-stream side, while consumers stay in async land. The reverse direction is rarer and you build it by hand.</p>
<h3 id="The-Observation-framework-Observable-iOS-17"><a href="#The-Observation-framework-Observable-iOS-17" class="headerlink" title="The Observation framework (@Observable, iOS 17+)"></a>The Observation framework (<code>@Observable</code>, iOS 17+)</h3><p>Not a pipeline, but worth flagging because it’s often confused with reactive streams. The new <code>@Observable</code> macro replaces <code>ObservableObject</code> + <code>@Published</code> for SwiftUI state observation:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Observable</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">ViewModel</span> &#123;</span><br><span class="line">    <span class="keyword">var</span> name <span class="operator">=</span> <span class="string">&quot;&quot;</span></span><br><span class="line">    <span class="keyword">var</span> count <span class="operator">=</span> <span class="number">0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>SwiftUI views auto-track <em>exactly</em> the properties they read: no <code>@Published</code>, no <code>objectWillChange</code>, no Combine subscription. It’s not a stream you can <code>map</code> over; it’s a fine-grained dependency tracker. If you tried to model “give me a stream of <code>count</code> changes” you’d reach for <code>withObservationTracking</code> (a low-level escape hatch) or wrap the property in <code>AsyncStream</code>. For normal SwiftUI work, you don’t need a stream at all.</p>
<h3 id="Published-the-bridge-in-the-middle"><a href="#Published-the-bridge-in-the-middle" class="headerlink" title="@Published (the bridge in the middle)"></a><code>@Published</code> (the bridge in the middle)</h3><p><code>@Published</code> is a Combine <code>Publisher</code>, full stop:</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">VM</span>: <span class="title class_ inherited__">ObservableObject</span> &#123;</span><br><span class="line">    <span class="meta">@Published</span> <span class="keyword">var</span> count <span class="operator">=</span> <span class="number">0</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> vm <span class="operator">=</span> <span class="type">VM</span>()</span><br><span class="line">vm.<span class="variable">$count</span>.sink &#123; <span class="built_in">print</span>(<span class="variable">$0</span>) &#125;</span><br></pre></td></tr></table></figure>

<p>The <code>$count</code> projection is a <code>Publisher&lt;Int, Never&gt;</code>. SwiftUI uses it via <code>ObservableObject</code>; you can also subscribe directly. As <code>@Observable</code> takes over, <code>@Published</code> is the “I still want a Combine pipeline off this property” tool.</p>
<h3 id="TCA-Effect-EffectPublisher"><a href="#TCA-Effect-EffectPublisher" class="headerlink" title="TCA Effect, EffectPublisher"></a>TCA <code>Effect</code>, <code>EffectPublisher</code></h3><p>If you use <a href="https://github.com/pointfreeco/swift-composable-architecture">The Composable Architecture</a>, <code>Effect</code> is a wrapper that started life as a Combine <code>Publisher</code> and migrated to async&#x2F;await. Same pipeline shape, narrower role: it represents work a reducer kicks off, with cancellation by ID baked in. Mention it because TCA codebases blur “Combine vs async”: the framework lets you write either inside an <code>Effect</code>.</p>
<h3 id="Result-Optional-also-chainable-also-not-streams"><a href="#Result-Optional-also-chainable-also-not-streams" class="headerlink" title="Result, Optional: also chainable, also not streams"></a><code>Result</code>, <code>Optional</code>: also chainable, also not streams</h3><p><code>Optional.map</code> and <code>Result.flatMap</code> use the same vocabulary, but each represents a <em>single</em> value (present-or-not, success-or-failure). They’re functor&#x2F;monad cousins of the others, not stream cousins. Worth knowing because the operator-name overlap can mislead: <code>flatMap</code> on <code>Result&lt;Int, E&gt;</code> doesn’t merge anything, it just chains a fallible step.</p>
<h2 id="Putting-them-on-one-axis"><a href="#Putting-them-on-one-axis" class="headerlink" title="Putting them on one axis"></a>Putting them on one axis</h2><pre>
<code class="mermaid">

flowchart LR
    subgraph Single[&quot;Single value&quot;]
        Opt[&quot;Optional&lt;br&#x2F;&gt;Result&quot;]
    end
    subgraph Many[&quot;Many values, in space&quot;]
        Coll[&quot;Array &#x2F; Sequence&quot;]
        LSeq[&quot;LazySequence&quot;]
    end
    subgraph Time[&quot;Many values, over time&quot;]
        Async[&quot;AsyncSequence &#x2F;&lt;br&#x2F;&gt;AsyncStream&quot;]
        Comb[&quot;Combine Publisher&quot;]
        Rx[&quot;RxSwift Observable&quot;]
    end
    Opt -.same operator vocabulary.-&gt; Coll
    Coll -.add laziness.-&gt; LSeq
    LSeq -.add suspension.-&gt; Async
    Async -.add multicast + push.-&gt; Comb
    Comb -.cross-platform variant.-&gt; Rx
</code>
</pre>

<p>Read it left-to-right as a feature ladder: each step adds one capability. Multiple values, then laziness, then time&#x2F;suspension, then push semantics + multicast. The operator names (<code>map</code>, <code>filter</code>, <code>flatMap</code>) carry across because the underlying algebra is the same; what changes is the runtime behaviour around them.</p>
<h2 id="When-to-pick-what"><a href="#When-to-pick-what" class="headerlink" title="When to pick what"></a>When to pick what</h2><pre>
<code class="mermaid">

flowchart TD
    Start{Are you operating on&lt;br&#x2F;&gt;values that exist NOW?}
    Start --&gt;|Yes, all in memory| Coll[Swift collections&lt;br&#x2F;&gt;&lt;i&gt;map &#x2F; filter &#x2F; reduce&lt;&#x2F;i&gt;]
    Start --&gt;|Yes, but huge &#x2F; infinite| Lazy[&quot;.lazy on the sequence&quot;]
    Start --&gt;|No - values arrive over time| Time{New code or existing?}
    Time --&gt;|New| Async{Need bidirectional &#x2F;&lt;br&#x2F;&gt;multicast &#x2F; hot streams?}
    Async --&gt;|No| AA[&quot;async&#x2F;await + AsyncSequence&quot;]
    Async --&gt;|Yes| Combine[&quot;Combine&quot;]
    Time --&gt;|Existing RxSwift codebase| Rx[&quot;RxSwift&lt;br&#x2F;&gt;&lt;i&gt;migrate gradually&lt;&#x2F;i&gt;&quot;]
</code>
</pre>

<p>In 2026 the picture is:</p>
<ul>
<li><strong>Collections</strong> are foundational; you use them every day regardless.</li>
<li><strong><code>async</code>&#x2F;<code>await</code> + <code>AsyncSequence</code></strong> is now the default for “values over time” in new Swift code. Easier to reason about, no subscription lifetime to manage, no operator-name memorisation. Use this first.</li>
<li><strong>Combine</strong> is still the right answer when you need true multicast, hot subjects, <code>@Published</code>, or when you’re glued to <code>URLSession.dataTaskPublisher</code> &#x2F; <code>NotificationCenter.publisher</code> &#x2F; SwiftUI bindings. Apple has slowed down Combine’s evolution but it’s not deprecated and remains everywhere in the system frameworks.</li>
<li><strong>RxSwift</strong> is legacy in pure-Swift Apple work. Keep it for codebases that already use it, or for cross-platform sharing with RxJava &#x2F; RxJS &#x2F; RxKotlin where the same mental model travels.</li>
</ul>
<h2 id="Operator-quick-reference"><a href="#Operator-quick-reference" class="headerlink" title="Operator quick-reference"></a>Operator quick-reference</h2><p>The same mental operation in all three:</p>
<table>
<thead>
<tr>
<th>Intent</th>
<th>Collection</th>
<th>Combine</th>
<th>RxSwift</th>
</tr>
</thead>
<tbody><tr>
<td>Transform</td>
<td><code>map</code></td>
<td><code>map</code></td>
<td><code>map</code></td>
</tr>
<tr>
<td>Filter</td>
<td><code>filter</code></td>
<td><code>filter</code></td>
<td><code>filter</code></td>
</tr>
<tr>
<td>Drop nils</td>
<td><code>compactMap</code></td>
<td><code>compactMap</code></td>
<td><code>compactMap</code></td>
</tr>
<tr>
<td>Flatten one level</td>
<td><code>flatMap</code></td>
<td><code>flatMap(maxPublishers:)</code></td>
<td><code>flatMap</code></td>
</tr>
<tr>
<td>Switch to newest</td>
<td>n&#x2F;a</td>
<td><code>map &#123; ... &#125;.switchToLatest()</code></td>
<td><code>flatMapLatest</code></td>
</tr>
<tr>
<td>Combine two streams</td>
<td><code>zip(a, b)</code></td>
<td><code>Publishers.Zip</code> &#x2F; <code>combineLatest</code></td>
<td><code>Observable.zip</code> &#x2F; <code>combineLatest</code></td>
</tr>
<tr>
<td>Accumulate</td>
<td><code>reduce(_:_:)</code></td>
<td><code>reduce</code> &#x2F; <code>scan</code></td>
<td><code>reduce</code> &#x2F; <code>scan</code></td>
</tr>
<tr>
<td>Drop duplicates</td>
<td>n&#x2F;a (use <code>Set</code>)</td>
<td><code>removeDuplicates()</code></td>
<td><code>distinctUntilChanged()</code></td>
</tr>
<tr>
<td>Take first</td>
<td><code>prefix(1)</code></td>
<td><code>prefix(1)</code> &#x2F; <code>first()</code></td>
<td><code>take(1)</code></td>
</tr>
<tr>
<td>Drop first</td>
<td><code>dropFirst(1)</code></td>
<td><code>dropFirst(1)</code></td>
<td><code>skip(1)</code></td>
</tr>
<tr>
<td>Throttle&#x2F;debounce</td>
<td>n&#x2F;a</td>
<td><code>throttle</code> &#x2F; <code>debounce</code></td>
<td><code>throttle</code> &#x2F; <code>debounce</code></td>
</tr>
<tr>
<td>Side effect</td>
<td><code>forEach</code></td>
<td><code>handleEvents</code></td>
<td><code>do(onNext:)</code></td>
</tr>
</tbody></table>
<h2 id="What-I-reach-for"><a href="#What-I-reach-for" class="headerlink" title="What I reach for"></a>What I reach for</h2><p>For new code I default to <code>async</code>&#x2F;<code>await</code> + <code>AsyncSequence</code>. Combine when I actually need multicast, hot subjects, or <code>@Published</code> for SwiftUI. RxSwift only when the codebase already runs on it.</p>
]]></content>
      <categories>
        <category>iOS</category>
      </categories>
      <tags>
        <tag>Swift</tag>
        <tag>Combine</tag>
        <tag>RxSwift</tag>
        <tag>Reactive</tag>
        <tag>Functional</tag>
      </tags>
  </entry>
</search>
